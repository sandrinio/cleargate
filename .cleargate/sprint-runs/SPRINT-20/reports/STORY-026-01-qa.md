# QA Report: STORY-026-01 Skill Auto-Load + Canonical Mirror

STORY: STORY-026-01
QA: PASS
TYPECHECK: pass (trusted from Dev report — SKIPPED re-run per orchestrator instruction)
TESTS: 60 passed, 0 failed, 0 skipped (trusted from Dev report — SKIPPED re-run per orchestrator instruction)
ACCEPTANCE_COVERAGE: 7 of 7 Gherkin scenarios have matching tests
MISSING: none
REGRESSIONS: none

---

## Verification performed

### Test re-run
SKIPPED per orchestrator instruction (MODE: LIGHT). Dev report states 60 passed, 0 failed. Trusted.

### File-surface compliance

`git diff sprint/SPRINT-20...story/STORY-026-01 --name-only` in worktree returns exactly 10 files:

```
cleargate-cli/src/commands/sprint.ts
cleargate-cli/test/commands/sprint-preflight.test.ts
cleargate-cli/test/commands/sprint.test.ts
cleargate-cli/test/hooks/session-start.test.ts
cleargate-cli/test/scaffold/sprint-execution-mirror.test.ts
cleargate-cli/test/snapshots/hooks/session-start.cr-008.sh
cleargate-cli/test/snapshots/hooks/session-start.cr-009.sh
cleargate-planning/.claude/hooks/session-start.sh
cleargate-planning/.claude/skills/sprint-execution/SKILL.md
cleargate-planning/MANIFEST.json
```

Story §3.1 lists 6 files; the diff shows 10. The additional 4 files are:
- `cleargate-cli/test/commands/sprint-preflight.test.ts` — new test file, in-scope for R3
- `cleargate-cli/test/commands/sprint.test.ts` — extended for R4 scenarios, in-scope
- `cleargate-cli/test/hooks/session-start.test.ts` — extended for R1 hook scenarios, in-scope
- `cleargate-cli/test/scaffold/sprint-execution-mirror.test.ts` — new mirror test, in-scope

Story §3.1 did not enumerate test files individually (story §4.1 specified where tests live). All 10 files are on-surface — no off-surface edits.

NOTE: `.claude/hooks/session-start.sh` (live hook, R1) does NOT appear in the git diff because `.claude/` is gitignored (verified: `.gitignore` has `/.claude/`). R1 was applied to the live file directly and is verified below.

### R1 — Live hook directive block (gitignored, not committable)

Read `/Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/session-start.sh` directly. Lines 17–21:

```bash
# --- Sprint-active skill auto-load directive (STORY-026-01) ---
ACTIVE_FILE="${REPO_ROOT}/.cleargate/sprint-runs/.active"
if [ -s "${ACTIVE_FILE}" ] && [ -n "$(tr -d '[:space:]' < "${ACTIVE_FILE}")" ]; then
  printf '→ Active sprint detected. Load skill: sprint-execution\n'
fi
```

Directive block is present. Uses `REPO_ROOT` (not re-reading env var), placed after doctor invocation on line 15, before the §14.9 sync-nudge block. POSIX-portable `tr -d '[:space:]'` guard is correct. Exit 0 at line 72 is unchanged. R1: VERIFIED.

### R2 — Canonical hook mirror

Read `cleargate-planning/.claude/hooks/session-start.sh` in worktree (lines 20–24): identical directive block present, using `REPO_ROOT`, after line 18 doctor invocation. Three-branch CLI resolver structure differs from live (as expected per M1 gotcha: content parity, not line-number parity). R2: VERIFIED.

### R3 — preflight emit

`cleargate-cli/src/commands/sprint.ts` commit diff shows:
```
+  if (allPass) { stdoutFn('→ Load skill: sprint-execution'); }
   return exitFn(allPass ? 0 : 1);
```
Added inside `sprintPreflightHandler`, not inside shared `emitPunchList` helper (correct per M1 plan). Fires only when `allPass` is true. R3: VERIFIED.

### R4 — init emit

```
+  if (code === 0) { stdoutFn('→ Load skill: sprint-execution'); }
   return exitFn(code);
```
Added inside `sprintInitHandler` before `exitFn` call. Type contract preserved (`exitFn` typed as `never`; emit is guarded and precedes it). R4: VERIFIED.

### R5 — Mirror parity (CRITICAL gate)

`diff /Users/ssuladze/Documents/Dev/ClearGate/.claude/skills/sprint-execution/SKILL.md /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/STORY-026-01/cleargate-planning/.claude/skills/sprint-execution/SKILL.md`

Exit code: 0. Zero output. Byte-identical. R5: VERIFIED.

### R6 — MANIFEST.json regeneration

`cleargate-planning/MANIFEST.json` contains entry:
```json
{
  "path": ".claude/skills/sprint-execution/SKILL.md",
  "sha256": "31cd09beba5dbc3c3e9d5d407ad0c7f1e02882b0c551eb314aea2b5b30ac8d32",
  "tier": "skill",
  "overwrite_policy": "always",
  "preserve_on_uninstall": false
}
```

The story §3.1 and M1.md architect plan cited `"overwrite_policy": "merge-3way"` and `"preserve_on_uninstall": "default-remove"` per protocol §13.2. However, inspection of `cleargate-cli/scripts/build-manifest.ts` shows the `skill` tier rule is defined as `overwrite_policy: "always"` and `preserve_on_uninstall: false` — matching the pre-existing `flashcard/SKILL.md` entry. The MANIFEST.json was auto-generated by prebuild (not hand-edited). The story spec cited incorrect metadata expectations (§13.2 example shows a `protocol` tier entry, not a `skill` tier). The generated output is consistent with actual build-manifest.ts rules. R6: VERIFIED (auto-generated output is internally consistent; spec metadata annotation was imprecise but the prebuild-generated values match existing skill-tier precedent).

### Snapshot lock: CR-008 vs CR-009 "one-line diff" invariant

Read both snapshot files:

- `session-start.cr-008.sh` line 18: `"${CG[@]}" doctor --session-start || true`
- `session-start.cr-009.sh` line 18: `"${CG[@]}" doctor --session-start 2>/dev/null || true`

Both files are 76 lines. Both contain the directive block at lines 20–24 (identical). The `init.test.ts` collision-check test at line 746 asserts `diffingLines` has length 1 and that differing line is line 18 — this invariant is preserved. The snapshot update was scope-justified: the `CR-008 snapshot` test (line 719) byte-compares the rendered session-start.sh (from `initHandler` using the planning payload) against the cr-008 lock; without updating the lock to include the directive block, the init test would fail. Snapshot justification: VERIFIED.

---

## Gherkin → Test mapping (7 of 7)

| # | Gherkin scenario | Test location | Status |
|---|---|---|---|
| 1 | Banner emits directive when sprint active | `session-start.test.ts` Scenario 1 (line 233) | COVERED |
| 2 | Banner quiet when no sprint active (missing OR whitespace-only) | `session-start.test.ts` Scenarios 2a + 2b (lines 253, 271) — both sub-cases per M1 plan | COVERED |
| 3 | `preflight` emits directive on full pass | `sprint-preflight.test.ts` line 347 | COVERED |
| 4 | `preflight` quiet on partial failure | `sprint-preflight.test.ts` line 359 | COVERED |
| 5 | `sprint init` emits directive on success | `sprint.test.ts` line 183 | COVERED |
| 6 | Canonical skill matches live byte-for-byte | `sprint-execution-mirror.test.ts` (4 assertions: exists, template match, MANIFEST entry, live diff) | COVERED |
| 7 | Scaffold rebuild produces clean MANIFEST.json | `sprint-execution-mirror.test.ts` line 75 (MANIFEST entry assertion: path, sha256, tier=skill) | COVERED |

Additional scenario not in story §1.4 but required by R4 spec ("emit ONLY on success"): `sprint init failure stays quiet` — test at `sprint.test.ts` line 209. Present. Aligns with M1 plan scenario #6.

Test count: ≥6 DoD requirement met (3 hook + 2 preflight CLI + 2 init CLI + 4 mirror = 11 new assertions across the new/extended test blocks). DoD §4.1 enforced.

---

## DoD §4.2 checklist

- [x] `npm run typecheck` clean for `cleargate-cli/` — trusted from Dev report (LIGHT mode)
- [x] `npm test` green — 60 passed, 0 failed — trusted from Dev report (LIGHT mode)
- [x] Mirror parity: diff is empty (EXIT:0 verified directly)
- [x] `npm run prebuild` ran; MANIFEST.json includes new skill entry with correct SHA
- [x] `cleargate doctor --check-scaffold` exits 0 — Dev report states verified; not re-run in LIGHT mode
- [x] Smoke verification §2.2(1) — Dev report confirms directive fires in session-start tests with `.active=SPRINT-20`
- [x] One commit `feat(EPIC-026): STORY-026-01 skill auto-load + canonical mirror` — commit 53e5f13
- [x] Pre-commit hook clean (no `--no-verify`) — commit proceeded normally

---

## Commit format

`feat(EPIC-026): STORY-026-01 skill auto-load + canonical mirror` — matches required format per CLAUDE.md (`feat(<epic>): STORY-NNN-NN <short desc>`). One commit. VERIFIED.

---

## Wave-1 → Wave-2 gate precondition

`cleargate-planning/.claude/skills/sprint-execution/SKILL.md` exists in the commit and is byte-equal to the live skill. CR-027 (Wave 2) has a valid edit target. Gate condition is satisfied.

