# Reporter Context Bundle — SPRINT-20

_Generated: 2026-05-02T19:19:58.517Z_

---

## Sprint Plan Slices

_Source: SPRINT-20_Skill_Adoption_And_Tooling_Cleanup.md_

## 1. Consolidated Deliverables

## 2. Execution Strategy

## 5. Metrics & Metadata

## State.json Summary

Sprint: SPRINT-20 | Status: Active

STORY-026-01 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-026-02 | Done | standard | qa_bounces=0 arch_bounces=0
CR-026 | Done | standard | qa_bounces=1 arch_bounces=0
CR-027 | Done | standard | qa_bounces=0 arch_bounces=0
CR-028 | Done | standard | qa_bounces=0 arch_bounces=0
BUG-025 | Done | fast | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

# Milestone: M1 — Skill Auto-Load + Canonical Mirror
## Stories: STORY-026-01
## Wave: W1 (3-agent parallel cohort with CR-026 + BUG-025; this dispatch is solo for STORY-026-01)

## Sprint-goal tie-in (one sentence)
M1 lays the foundation for SPRINT-20's primary deliverable (EPIC-026 skill adoption) by emitting deterministic load directives from SessionStart + sprint CLI commands AND creating the canonical `cleargate-planning/.claude/skills/sprint-execution/` mirror — without which CR-027's Wave-2 "five checks" edit and STORY-026-02's Wave-3 CLAUDE.md prune both lose their anchor.

## Order
- This is a single-story milestone. Internal sequencing: **R5 (canonical mirror copy) → R6 (`npm run prebuild`) → R1/R2 (hook edits) → R3/R4 (CLI edits) → tests → R7 (smoke verify)**.
- Within W1, this dispatch is parallel-safe with CR-026 (M3) and BUG-025 (M4) — disjoint surfaces, verified per Sprint Plan §2.2.
- **Wave-1-must-finish gate for CR-027 (Wave 2):** `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` MUST exist on `main` before CR-027's Developer dispatches. Orchestrator verifies via `ls cleargate-planning/.claude/skills/sprint-execution/SKILL.md` post-merge.

## Per-story blueprint

### STORY-026-01 — Skill Auto-Load + Canonical Mirror

#### Files to create
- `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` — byte-copy of live (`cleargate-cli/scripts/copy-planning-payload.mjs` will derive `cleargate-cli/templates/cleargate-planning/.claude/skills/sprint-execution/SKILL.md` automatically; do NOT hand-create the templates entry — FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`).
- `cleargate-cli/test/hooks/session-start.test.ts` — NEW (or extend an existing hook test if one already covers `session-start.sh`; verify before creating). Two scenarios per §1.4 Gherkin: sprint-active emits, sprint-inactive does not.

#### Files to modify
- `.claude/hooks/session-start.sh` — append the §3.2 directive block AFTER line 15 (`"${CG[@]}" doctor --session-start 2>/dev/null || true`) and BEFORE the existing §14.9 sync-nudge block at line 17. The new block uses `CLAUDE_PROJECT_DIR` (already set at line 3 as `REPO_ROOT`) — reuse `REPO_ROOT` rather than re-reading the env var. Keep the trailing `exit 0` at line 66 unchanged.
- `cleargate-planning/.claude/hooks/session-start.sh` — identical append AFTER line 18 (`"${CG[@]}" doctor --session-start || true`). Uses `REPO_ROOT` from line 3. Same content, mirror-parity invariant.
- `cleargate-cli/src/commands/sprint.ts`:
  - **R3 (preflight emit)** — `emitPunchList` at line 1042–1067 is shared between handlers; do NOT add the directive there (would fire on internal callers too). Instead, edit `sprintPreflightHandler` directly: after line 1105 (`emitPunchList(...)`) and BEFORE line 1107 (`const allPass = ...`), insert a guard `if (results.every((r) => r.pass || r.skipped)) { stdoutFn('→ Load skill: sprint-execution'); }`. This guarantees the emit fires ONLY on full pass and lands AFTER the existing success line written by `emitPunchList` at line 1051. Net: 1 line added (or 3 with brace).
  - **R4 (init emit)** — `sprintInitHandler` at line 91–245 currently shells out to `init_sprint.mjs` via `spawnSync('bash', [runScript, ...args], { stdio: 'inherit' })` at line 236 and returns `exitFn(result.status ?? 0)` at line 244. Insert between the rc-zero check and `exitFn(code)`: after line 243 (`const code = result.status ?? 0;`) and BEFORE line 244 (`return exitFn(code);`), add: `if (code === 0) { stdoutFn('→ Load skill: sprint-execution'); }`. Net: 3 lines (with brace).
  - **Note on `stdio: 'inherit'`:** because `init_sprint.mjs` writes to inherited stdout, the `→ Load skill` directive lands AFTER the script's own output — correct ordering for §1.4 Scenario "init emits load directive on success." Do not change the spawn options.
- `cleargate-planning/MANIFEST.json` — DO NOT hand-edit. `npm run prebuild` (= `tsx scripts/build-manifest.ts && node scripts/copy-planning-payload.mjs`, package.json line 46) regenerates the entry. Verify post-prebuild that the new entry has `tier: "skill"`, `overwrite_policy: "merge-3way"`, `preserve_on_uninstall: "default-remove"` per protocol §13.2 — story §3.1 calls these out explicitly.

#### Schema changes
None. No DB migration; no state.json schema delta; no env-var addition.

#### Test scenarios (from §1.4 Gherkin — agent must cover all 7)
1. **Hook: sprint-active emits directive.** Fixture `.cleargate/sprint-runs/.active` containing `SPRINT-20`; assert stdout includes `→ Active sprint detected. Load skill: sprint-execution`; exit 0.
2. **Hook: sprint-inactive stays quiet.** `.active` missing OR whitespace-only (test BOTH sub-cases — story §3.2 uses `tr -d '[:space:]'` so a single newline must NOT trigger emit); assert no `Load skill:` substring; exit 0.
3. **CLI preflight: success emits directive.** Mock all four checks to pass; assert last stdout line is `→ Load skill: sprint-execution`; exit 0.
4. **CLI preflight: partial failure stays quiet.** Mock at least one check failing; assert stdout does NOT contain `Load skill:`; exit non-zero.
5. **CLI init: success emits directive.** Mock `spawnSync` returning `{ status: 0 }`; assert stdout contains `→ Load skill: sprint-execution`; exit 0.
6. **CLI init: failure stays quiet.** Mock `spawnSync` returning `{ status: 1 }`; assert stdout does NOT contain `Load skill:`; exit non-zero. (NOT in story §1.4 but inferred from R4 spec "Emit ONLY on success" — add as parity with R3 partial-failure scenario.)
7. **Mirror diff is empty.** `diff .claude/skills/sprint-execution/SKILL.md cleargate-planning/.claude/skills/sprint-execution/SKILL.md` produces zero output. Implement as a vitest test under `cleargate-cli/test/scaffold/` or extend an existing `doctor-check-scaffold` test.
- **Total new test count: 7** (story §4.1 demands ≥6 — meets bar with margin; DoD §4.1 enforced per SPRINT-19 lesson).

#### Reuse (no duplication)
- **`stdoutFn` / `stderrFn` injection seam** — already present in both handlers (line 95 + 1079). Use it; do not call `console.log` or `process.stdout.write` directly.
- **`REPO_ROOT` shell variable** in both `session-start.sh` files (line 3 of each) — reuse for the new `ACTIVE_FILE` path; do NOT re-read `CLAUDE_PROJECT_DIR`.
- **`copy-planning-payload.mjs`** — handles the `cleargate-cli/templates/...` regeneration. Do NOT add a manual `cp` step into `cleargate-cli/templates/` (FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`: that path is DERIVED).
- **Test infra:** `cleargate-cli/test/commands/sprint.test.ts` is the existing test file for sprint handlers — extend it for R3/R4/R6 scenarios rather than creating a new file. Hook tests under `cleargate-cli/test/hooks/` (verify path before assuming).

#### Gotchas surfaced from code inspection
- **`emitPunchList` is shared infra.** Lines 1042–1067 are called from `sprintPreflightHandler` AND would be called from any future preflight-style handler. Adding the emit inside `emitPunchList` would leak the directive into unintended call sites. Plan puts the emit in the handler's success-guard, not in the helper.
- **`init_sprint.mjs` runs with `stdio: 'inherit'`.** The mjs script's own stdout (e.g., "state.json written") is NOT captured in `result.stdout`. The R4 emit appends AFTER the inherited child output flushes — natural ordering, no extra coordination needed. Tests must mock `spawnSync` to return `{ status: 0 }` and assert the directive emit occurs from the handler (not the child).
- **Live SessionStart hook differs from canonical.** Live (line 7–13) uses a 2-branch CLI resolver (`command -v cleargate` then `cleargate-cli/dist/cli.js`); canonical (line 10–16) uses a 3-branch resolver with `__CLEARGATE_VERSION__` placeholder for `npx`. Both files set `REPO_ROOT="${CLAUDE_PROJECT_DIR}"` at line 3 — that's the only common surface this story touches. Append the new block AFTER the doctor invocation in EACH file at its own line number (15 live / 18 canonical) — do NOT try to align line numbers; mirror parity is content parity, not line parity.
- **`.cleargate/sprint-runs/.active` currently contains `SPRINT-20`** (verified). Smoke test §2.2(1) MUST run on a session with this content — do NOT clear/recreate the sentinel during dev work. The file is non-test infra; treat as read-only.
- **Trailing newline gotcha.** `printf '%s\n' SPRINT-20 > .active` leaves a trailing `\n` byte; `tr -d '[:space:]' < .active` returns `SPRINT-20` (non-empty) → emit fires. An empty file (zero bytes) fails `[ -s "$file" ]` → no emit. A single-newline file (`printf '\n' > .active`) passes `[ -s ]` (non-zero size) but `tr -d` returns empty → no emit. The §3.2 block's compound condition handles all three cases correctly. Tests MUST cover the single-newline edge case.
- **`exitFn` does NOT return.** Type signature is `(code: number) => never`. The R4 emit must execute BEFORE `return exitFn(code)`. Putting it inside `if (code === 0)` and threading it on a separate line preserves type contracts.

## Cross-story risks
- **Mirror creation order is the W1→W2 gate.** STORY-026-01 R5 creates `cleargate-planning/.claude/skills/sprint-execution/SKILL.md`. CR-027 (Wave 2) edits that file's `### A.1 Sprint Execution Gate` section to add the fifth preflight check. If STORY-026-01 lands incomplete (e.g., R5 + R6 skipped or rolled back), CR-027 has no canonical edit target and Wave 2 dispatch fails. **Orchestrator gate condition (Wave 2 launch precondition):** verify on `main` post-merge that `ls cleargate-planning/.claude/skills/sprint-execution/SKILL.md` returns the file AND `diff` against the live skill is empty.
- **MANIFEST regeneration must complete IN this commit.** `cleargate doctor --check-scaffold` will fail on the next session-start hook if `MANIFEST.json` SHA entry doesn't match the new canonical skill file. FLASHCARD `2026-05-01 #manifest #prebuild` is the canonical lesson. The Developer's pre-commit checklist must include "ran `npm run prebuild` from `cleargate-cli/` AFTER the live→canonical copy AND before `git add`."
- **STORY-026-02 (Wave 3) prune surface.** STORY-026-02's prune deletes ~60 lines of CLAUDE.md including the dispatch convention paragraph + Sprint Execution Gate paragraph. STORY-026-01 does NOT touch CLAUDE.md prose — confirmed by §3.1 file list. Wave 3 is downstream; no contention.
- **CR-026 (Wave 1 sibling) edits CLAUDE.md "Orchestrator Dispatch Convention" paragraph.** STORY-026-01 does NOT touch CLAUDE.md, so no conflict with CR-026's parallel dispatch. Confirmed by §3.1.
- **`cleargate-cli/test/commands/sprint.test.ts` is shared with CR-027.** STORY-026-01 adds 3 scenarios (R3 success / R3 fail / R4 success / R4 fail = 4 actually). CR-027 (M5) will add 6 more for check #5. Extension is append-only; merge-clean. Plan calls this out for the M5 architect run.

## Wave-1 dispatch-time consideration (vitest pool cap)
- **FLASHCARD `2026-05-02 #vitest #ram #parallel-agents`:** vitest `maxForks=2` is per-process. Wave 1 = 3 parallel Developer agents (this story + CR-026 + BUG-025), each running `npm test` independently. Worst case: 3 agents × 2 forks = 6 forks ≈ 2.4 GB. On a tight-RAM laptop (Sprint Plan §2.3 surfaces this risk) the orchestrator should EITHER (a) export `VITEST_MAX_FORKS=1` for all three Wave-1 dispatches, OR (b) serialize the third dispatch (recommended ordering: STORY-026-01 + CR-026 first, then BUG-025 once one finishes). This decision is made by the orchestrator at dispatch time, not by the Developer agent. Plan flags it here so the orchestrator's pre-Wave-1 prep covers it.
- **FLASHCARD `2026-05-01 #vitest #leak #posttest`:** Developer MUST run `pkill -f vitest || true` after every `npm test` invocation. Tinypool workers (~4 GB each, 5–9 spawned) survive parent if not closed; observed ~30 GB orphan RAM after a single uncleaned run. Add to dev's local pre-commit script.
- **FLASHCARD `2026-05-01 #qa #vitest #npx`:** if `npx vitest` ETIMEDOUTs, invoke `node_modules/.bin/vitest` directly.

## Open decisions for orchestrator
- **None for this milestone.** Story §1.4 Open Questions is empty (Epic §6 resolved all five before Gate 1). All R1–R7 requirements are spelled out with exact line markers; no Architect-level interpretation gaps remain.
- **Reminder for orchestrator (not a decision):** before launching the Wave-1 cohort, run the vitest-cap decision above (export `VITEST_MAX_FORKS=1` OR serialize) and write the dispatch marker via `bash .cleargate/scripts/write_dispatch.sh STORY-026-01 developer` (per the live `Orchestrator Dispatch Convention` paragraph that CR-026 will fix in this same wave).

### M2.md

role: architect

# Milestone: M2 — CLAUDE.md Prune (Live + Canonical)
## Stories: STORY-026-02
## Wave: W3 (sequential — runs LAST after Waves 1+2 fully merged to `main`)

## Order
Single-story milestone. Wave 3 is the LAST CLAUDE.md edit of SPRINT-20.

**Hard dispatch preconditions (orchestrator MUST verify before launching the Developer):**
1. Wave 1 fully merged to `main` (STORY-026-01, CR-026, BUG-025) — confirmed: live `CLAUDE.md` line 145 carries the new "Orchestrator Dispatch Convention" paragraph rewritten by CR-026, AND `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` exists (24962 bytes, verified by `ls`).
2. Wave 2 fully merged to `main` (CR-027, CR-028) — confirmed: live `CLAUDE.md` line 125 reads "five checks" (CR-027), AND line 111 carries "Codebase is source of truth" + line 113 has the extended "Duplicate check" paragraph (CR-028).
3. Mirror parity baseline captured: `diff CLAUDE.md cleargate-planning/CLAUDE.md` ALREADY shows pre-existing canonical-only divergence above the CLEARGATE-block (live lines 1-97 vs canonical lines 1-6). FLASHCARD `2026-05-01 #mirror #parity` — DO NOT reconcile this; only mirror the prune itself.

## Sprint-goal tie
STORY-026-02 collapses the duplicated four-agent-loop / dispatch-convention / Sprint-Execution-Gate / Architect-runs-twice / sprint-close-ack prose in CLAUDE.md (live + canonical) to a single skill pointer + one always-on auto-load directive — making the EPIC-026 sprint-execution skill the sole source of truth for orchestration and delivering the SPRINT-20 metric "CLAUDE.md ≥60-line reduction".

## Per-story blueprint

### STORY-026-02 — CLAUDE.md Prune (Live + Canonical)

**Files to create:** none.

**Files to modify:**
1. `/Users/ssuladze/Documents/Dev/ClearGate/CLAUDE.md` — live (161 lines pre-prune, post-Wave-1+2).
2. `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/CLAUDE.md` — canonical (70 lines pre-prune, post-Wave-1+2). The CLEARGATE-block in this file spans lines 7–70 (verified `<!-- CLEARGATE:START -->` at L7, `<!-- CLEARGATE:END -->` at L70).

**Schema changes:** none.

**Test scenarios (from §2.1 Gherkin — 5 scenarios; all verified by post-edit grep, no new test files written):**
1. **Halt rules survive the prune.** Both files retain: Gate 4 close-ack paragraph (`--assume-ack` substring); triage-first-draft-second; Brief-is-the-universal-pre-push-handshake; halt-at-gates; State-aware surface paragraph; Sprint mode v1/v2 paragraph; Boundary gates (CR-017) paragraph (R5 KEEPs both per EPIC-026 §6 Q2).
2. **Skill pointer added.** Both files contain a one-line pointer naming `.claude/skills/sprint-execution/SKILL.md` AND a rule that mentions "Skill tool" + "SessionStart banner" together (the R3 always-on auto-load directive).
3. **Duplicate content removed.** Neither file contains "Orchestrator Dispatch Convention", "Architect runs twice per sprint", "Sprint Execution Gate.", or "Four-agent loop (roles in `.claude/agents/`)" headers/labels — but matches are still found inside `.claude/skills/sprint-execution/SKILL.md` (verified pre-prune at lines 110, 370, 391, 395, 398, 472).
4. **Net line-count reduction.** `wc -l CLAUDE.md` ≥ 60 lines fewer than 161 (i.e. ≤ 101). `wc -l cleargate-planning/CLAUDE.md` ≥ 60 lines fewer than 70 — **mathematically impossible** (prune surface inside canonical block totals ~36 lines of removable prose; see Net-line-count expectations below for the per-file breakdown and the exemption rationale + escalation path).
5. **Mirror parity holds.** `diff CLAUDE.md cleargate-planning/CLAUDE.md` shows ONLY pre-existing pre-CLEARGATE-block divergence (lines 1–97 of live vs lines 1–6 of canonical) — no NEW divergence introduced inside the CLEARGATE-block by the prune.

**Reuse (no duplication):**
- The pruned content's authoritative home is `.claude/skills/sprint-execution/SKILL.md` (live) + `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` (canonical, byte-mirrored by STORY-026-01 R5, edited in-place by CR-027 to carry the "five checks" content). Both confirmed present pre-dispatch.
- No new helper, no new script, no new test fixture. Pure prose edit.

**Gotchas surfaced from code inspection:**
- **Live `CLAUDE.md` is NOT bounded by CLEARGATE markers; canonical IS.** Live spans the whole file (lines 1–161, no markers). Canonical is wrapped: `<!-- CLEARGATE:START -->` at L7, `<!-- CLEARGATE:END -->` at L70. Prune surface in live is the meta-repo-specific orchestration prose (lines 59–67 = "Agent orchestration" block) PLUS bullets within the CLEARGATE-block (lines 137–141, 121, 125, 127, 145). Prune surface in canonical is bullets within L7–L70 only.
- **CR-026 already collapsed the Orchestrator Dispatch Convention paragraph to its skill-loaded form** (live L145 / canonical L54). Prune simply DELETES this paragraph in both files; the dispatch hook + write_dispatch.sh fallback are documented in the skill's §1.
- **CR-028's "Codebase is source of truth" line (live L111, canonical L20) and the extended "Duplicate check" paragraph (live L113, canonical L22) are OUTSIDE the prune surface and survive untouched.** Pre-edit grep MUST verify these survive post-edit.
- **CR-027's "five checks" content lives in the live file at L125 (CLEARGATE-block) and canonical L34. The "Sprint Execution Gate" PARAGRAPH (L125 / L34) is on the prune list — the orchestrator's preflight check inventory survives via the skill's `### A.1 Sprint Execution Gate (Gate 3) — preflight` section.** This is the precise scenario the SPRINT-20 SDR §2.5 Hard flag resolved.

## Complete prune surface — line-level

### Live `/Users/ssuladze/Documents/Dev/ClearGate/CLAUDE.md` (161 lines pre-prune)

**DELETE the following blocks verbatim:**

| Lines | Content head | Rationale |
| --- | --- | --- |
| 59–67 | `## Agent orchestration` header through `Communication model: agents don't talk to each other; they read/write artifact files and return structured text to the orchestrator.` | Duplicated in skill §1 + §A. R1 prune surface (meta-repo-specific block, NOT inside CLEARGATE markers). |
| 121 | `**Architect runs twice per sprint.** (1) Sprint Design Review writes §2 of the sprint plan before human confirm. (2) Per-milestone plan writes \`sprint-runs/<id>/plans/M<N>.md\` before Developer agents start that milestone.` | Duplicated in skill §A.2. R1. |
| 125 | `**Sprint Execution Gate.** Before transitioning Ready → Active, run \`cleargate sprint preflight <id>\`. The five checks (previous sprint Completed, no leftover worktrees, \`sprint/S-NN\` ref free, \`main\` clean, per-item readiness gates pass) must all pass. Halt and ask the human for resolution on any failure.` | Duplicated in skill §A.1 (which CR-027 updated to "five checks" mirror). R1. |
| 137–141 | `**Four-agent loop (roles in \`.claude/agents/\`):**` block (5 lines: header + 4 bullets for architect/developer/qa/reporter) | Duplicated in skill §1 contract block. R1. |
| 145 | `**Orchestrator Dispatch Convention.** The PreToolUse:Task hook (...) Example: \`bash .cleargate/scripts/write_dispatch.sh CR-026 architect\`.` | Duplicated in skill §1 dispatch-marker contract. R1. |

**KEEP the following blocks verbatim (R4 + R5 invariant):**

| Lines | Content | Reason |
| --- | --- | --- |
| 1–58 | All meta-repo prose ABOVE "Agent orchestration" (Repo layout, How work gets done, Flashcard protocol). | Outside the CLEARGATE-block; R4 — meta-repo-specific, not duplicated in skill. |
| 69–96 | Test + commit conventions, Active state, Stack versions, Guardrails for the conversational agent | Outside the CLEARGATE-block; R4. |
| 98–119 | CLEARGATE-block opening (Session-start orientation, Triage first, Codebase is source of truth, Duplicate check, Halt at gates, Sprint mode, Brief is the universal pre-push handshake) | R4 + R5 — all halt rules + Sprint-mode + Triage + Brief survive. **CR-028 additions (L111, L113) are inside this preserved range.** |
| 123 | `**Boundary gates (CR-017).** \`cleargate sprint init\` runs the decomposition gate; \`close_sprint.mjs\` runs the lifecycle reconciler. Both block in v2.` | R5 — KEEP (EPIC-026 §6 Q2). |
| 127 | `**Sprint close is Gate-4-class (CR-019).** Run \`close_sprint.mjs\` with no flags first; surface the prompt verbatim; halt. Never pass \`--assume-ack\` autonomously. Pre-close enforces Steps 2.7 (no leftover worktrees) + 2.8 (sprint branch merged to main) under v2; failure halts close. Post-close prints a 6-item handoff list (Step 8) summarizing commits, merge state, wiki ingest, flashcards, artifacts, and next-sprint preflight.` | R4 — Gate 4 halt rule survives verbatim. STORY-026-02 §3.2 `cited` the long form as a prune candidate, but the spec at R4 is unambiguous: **the Gate-4-class halt rule MUST survive**. KEEP this paragraph in full. |
| 129–135 | `**Drafting work items:**` bullet block + Story granularity rule + Initiative Intake paragraph | R4 — drafting/triage halt rule. |
| 143 | `**State-aware surface.** ...` long paragraph through `cleargate join`. | R4 — always-on rule. |
| 147 | `**Conversational style.** ...` paragraph | R4 — always-on rule. |
| 149 | `**Support infrastructure.** ...` paragraph | KEEP — references the flashcard skill + token-ledger hook (EPIC-026 §6 unaddressed → default KEEP). |
| 151 | `**Cross-project orchestration.** ...` paragraph | KEEP — non-sprint-execution-specific operational rule. |
| 153 | `**Project overrides.** ...` paragraph | R4 — block-boundary contract. |
| 155 | `**Scope reminder.** ...` paragraph | R4 — disclaimer. |
| 157–158 | `**Guardrails for the conversational agent:**` + Gate 4 close-ack bullet | R4 — Gate 4 halt rule survives in two places (L127 paragraph + L157-158 bullet). The duplication is intentional — STORY-026-02 spec R4 enumerates "Gate 4 close-ack rule" as a halt rule MUST. |
| 160 | `**Doc & metadata refresh on close.** ...` paragraph | KEEP — Gate-4-adjacent close protocol; not duplicated in skill (skill's §1 close playbook is procedural, not the doc-refresh checklist). |

### Canonical `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/CLAUDE.md` (70 lines pre-prune)

**DELETE the following blocks verbatim (mirroring the live prune within the CLEARGATE-block):**

| Lines | Content head | Rationale |
| --- | --- | --- |
| 30 | `**Architect runs twice per sprint.** ...` | Mirror of live L121. |
| 34 | `**Sprint Execution Gate.** ...The five checks ... Halt and ask the human for resolution on any failure.` | Mirror of live L125. |
| 46–50 | `**Four-agent loop (roles in \`.claude/agents/\`):**` 5-line bullet block | Mirror of live L137–141. |
| 54 | `**Orchestrator Dispatch Convention.** ... \`bash .cleargate/scripts/write_dispatch.sh CR-026 architect\`.` | Mirror of live L145. |

**KEEP all other content verbatim** (CLEARGATE-block lines 7–70 minus the 4 removals above; lines 1–6 frontmatter prose unchanged — pre-existing divergence that is NOT reconciled per FLASHCARD `2026-05-01 #mirror #parity`). The CLEARGATE-block has no "Agent orchestration" non-CLEARGATE block (which only exists in live L59–L67), so canonical loses 4 paragraphs/bullets while live loses 5 + an entire H2 section.

## Replacement bullet text (R2 + R3 — verbatim, single block, identical in both files)

**Insertion location:** Immediately after the `**Triage first, draft second.**` paragraph (live L109; canonical L18) AND before the `**Codebase is source of truth.**` paragraph (live L111; canonical L20). Insert exactly one blank line above and below each new bullet.

**R2 — single skill pointer (one bullet):**

```markdown
**Sprint execution.** When a sprint is active, the orchestration playbook lives at `.claude/skills/sprint-execution/SKILL.md` — load it before dispatching any execution agent (Architect / Developer / QA / Reporter). The skill is the canonical four-agent-loop spec; the always-on CLAUDE.md keeps only the halt-rules and the load-skill contract.
```

**R3 — Skill auto-load directive (one bullet, ALWAYS-ON, NOT pruned):**

Per EPIC-026 §6 Q3, this rule is the explicit contract that keeps the orchestrator's behavior deterministic when the skill description-match auto-load is unreliable. Insert immediately after the R2 bullet (same insertion point):

```markdown
**Skill auto-load directive.** When the SessionStart banner emits `Load skill: <name>`, invoke the Skill tool to load it before continuing. Claude Code's description-match auto-load is advisory; this rule is the contract.
```

**Combined insertion (apply identically to live + canonical, mirrored verbatim):**

```markdown
**Sprint execution.** When a sprint is active, the orchestration playbook lives at `.claude/skills/sprint-execution/SKILL.md` — load it before dispatching any execution agent (Architect / Developer / QA / Reporter). The skill is the canonical four-agent-loop spec; the always-on CLAUDE.md keeps only the halt-rules and the load-skill contract.

**Skill auto-load directive.** When the SessionStart banner emits `Load skill: <name>`, invoke the Skill tool to load it before continuing. Claude Code's description-match auto-load is advisory; this rule is the contract.

```

(Trailing blank line preserved so the next paragraph "**Codebase is source of truth.**" remains separated.)

## Net line-count expectations (R7 reconciliation)

**Live `CLAUDE.md` pre-prune: 161 lines.**

| Block | Lines deleted |
| --- | --- |
| L59–L67 (Agent orchestration H2 + 4-bullet block + role-contracts paragraph) | 9 |
| L121 (Architect runs twice paragraph) | 1 |
| L125 (Sprint Execution Gate paragraph) | 1 |
| L137–L141 (Four-agent loop block) | 5 |
| L145 (Orchestrator Dispatch Convention paragraph) | 1 |
| Adjacent blank lines collapsed (estimate) | ~5–8 |
| **Total deletion** | **~22–25 lines** |

| Block | Lines inserted |
| --- | --- |
| R2 + R3 bullets + 1 trailing blank | 3 (raw) |
| **Total insertion** | **~3 lines** |

**Live net delta:** ~ −19 to −22 lines. **R7's "≥60 lines deleted per file" target is NOT achievable in the live file** without expanding the prune surface beyond what STORY-026-02 §3.2 explicitly authorizes.

**Canonical `cleargate-planning/CLAUDE.md` pre-prune: 70 lines.**

| Block | Lines deleted |
| --- | --- |
| L30 (Architect runs twice) | 1 |
| L34 (Sprint Execution Gate) | 1 |
| L46–L50 (Four-agent loop block) | 5 |
| L54 (Orchestrator Dispatch Convention) | 1 |
| Adjacent blank lines | ~4 |
| **Total deletion** | **~12 lines** |

**Canonical net delta:** ~ −9 lines. **R7's ≥60-line target is mathematically impossible** — the canonical file is only 70 lines pre-prune.

### R7 escalation (DEV agent must surface this in the dev report; do NOT silently widen the prune)

R7's ≥60-line target was authored when the SPRINT-20 SDR estimated the CLAUDE.md prune would land in a 200+-line live file. After CR-026 (collapsed dispatch-convention paragraph), CR-027 (single "five checks" line edit), CR-028 (added "Codebase is source of truth" + extended "Duplicate check"), the pre-prune live file is 161 lines and canonical is 70 lines. **The R7 ≥60-line target is now numerically out of reach without crossing R4 / R5 (preserved halt rules, Sprint mode, Boundary gates).**

**Architect resolution (this plan):** apply the R1 prune surface STORY-026-02 §3.2 enumerates verbatim — nothing more, nothing less. Net delete ~22 live + ~12 canonical. The Developer dev report explicitly cites this plan's escalation note + the §3.2 prune-list closure as the authority for not satisfying R7. The orchestrator can either (a) accept the smaller prune as DoD-compliant under R1/R4/R5 and waive R7, or (b) file a follow-up CR for SPRINT-21 to widen the prune surface (e.g., move "Drafting work items" / "Initiative Intake" / "Conversational style" to the skill). **Architect recommends (a) waive R7 with documented metric override** — the R1 prune surface IS the contractual delete; R7 was a numeric metric on an out-of-date pre-prune SHA estimate.

DoD §4.2 line-count delta checkbox MUST be reframed in the commit body: "R7 numeric target waived; prune surface per §3.2 R1 fully applied. R4 + R5 preserved." Surface this in the dev report verbatim.

## Mirror-parity invariant (FLASHCARD `2026-05-01 #mirror #parity`)

- **Per-edit, NOT state-parity.** Apply identical prune + identical R2/R3 insertions inside each file's CLEARGATE-block (live's CLEARGATE-block spans L98–L161; canonical's spans L7–L70). Pre-existing live↔canonical divergence ABOVE the CLEARGATE-block (live L1–L97 = meta-repo prose; canonical L1–L6 = injection-spec preamble) is NOT touched.
- **Verification:** `diff CLAUDE.md cleargate-planning/CLAUDE.md` post-prune MUST show ONLY the same pre-existing divergence baseline as pre-prune. Capture pre-prune diff as `/tmp/pre-prune-mirror-diff.txt`; post-prune diff `/tmp/post-prune-mirror-diff.txt`; assert `diff /tmp/pre-prune-mirror-diff.txt /tmp/post-prune-mirror-diff.txt` returns empty.
- **CLEARGATE-block-only invariant.** No edit may touch live L1–L97 (outside CLEARGATE-block) EXCEPT the L59–L67 "Agent orchestration" deletion. The Agent-orchestration H2 block is a meta-repo-only duplication that has no canonical mirror — its deletion does not violate mirror-parity (canonical never had it). Confirm by `grep -n "Agent orchestration" cleargate-planning/CLAUDE.md` returning empty pre-edit AND post-edit.

## Verification recipe (Developer runs verbatim before commit)

```bash
# Step 1 — capture pre-prune baselines
git rev-parse HEAD:CLAUDE.md > /tmp/sprint20-m2-pre-live.sha
git rev-parse HEAD:cleargate-planning/CLAUDE.md > /tmp/sprint20-m2-pre-canonical.sha
wc -l CLAUDE.md cleargate-planning/CLAUDE.md > /tmp/sprint20-m2-pre-counts.txt
diff CLAUDE.md cleargate-planning/CLAUDE.md > /tmp/sprint20-m2-pre-mirror-diff.txt

# Step 2 — apply edits (live + canonical) per the prune surface above

# Step 3 — post-edit assertions
wc -l CLAUDE.md cleargate-planning/CLAUDE.md > /tmp/sprint20-m2-post-counts.txt
diff /tmp/sprint20-m2-pre-counts.txt /tmp/sprint20-m2-post-counts.txt
# Expect: live ~ -22, canonical ~ -9 (R7 waived per plan; document in dev report).

# Step 4 — halt-rule grep audit (≥6 hits per file)
for f in CLAUDE.md cleargate-planning/CLAUDE.md; do
  echo "=== $f ==="
  grep -cE "(--assume-ack|Triage first|Brief is the universal|Halt at gates|State-aware surface|Sprint mode|Boundary gates)" "$f"
done
# Expect: each file ≥ 6.

# Step 5 — pointer grep (R2 + R3 inserted)
grep -F ".claude/skills/sprint-execution/SKILL.md" CLAUDE.md
grep -F ".claude/skills/sprint-execution/SKILL.md" cleargate-planning/CLAUDE.md
grep -F "Skill auto-load directive" CLAUDE.md
grep -F "Skill auto-load directive" cleargate-planning/CLAUDE.md
# Expect: each grep returns exactly 1 hit.

# Step 6 — pruned-content absence
for f in CLAUDE.md cleargate-planning/CLAUDE.md; do
  echo "=== $f ==="
  grep -cE "(Orchestrator Dispatch Convention|Architect runs twice per sprint|^\*\*Sprint Execution Gate\.\*\*|Four-agent loop \(roles in)" "$f"
done
# Expect: 0 in each file.

# Step 7 — pruned content STILL EXISTS in skill
grep -c -E "(Orchestrator Dispatch|Architect runs twice|Sprint Execution Gate)" .claude/skills/sprint-execution/SKILL.md
# Expect: ≥ 3.

# Step 8 — mirror-parity invariant (no NEW divergences beyond pre-existing)
diff CLAUDE.md cleargate-planning/CLAUDE.md > /tmp/sprint20-m2-post-mirror-diff.txt
diff /tmp/sprint20-m2-pre-mirror-diff.txt /tmp/sprint20-m2-post-mirror-diff.txt
# Expect: empty output. ANY non-empty output flags a NEW divergence introduced by the prune; rejected for re-edit.

# Step 9 — typecheck + tests (no new tests written — story is doc-only)
cd cleargate-cli && npm run typecheck && npm test
# Expect: clean. Story is doc-only; no test surface change.

# Step 10 — smoke conversational turn (manual)
# Open a fresh Claude Code session; SessionStart banner should still emit "→ Active sprint detected. Load skill: sprint-execution"
# (STORY-026-01 hook). Orchestrator dispatches a synthetic Task; verify the Task() prompt
# references the skill (not CLAUDE.md prose) for dispatch syntax. Paste 3-line log evidence in dev report.
```

## Test runner policy (no new tests)

STORY-026-02 has **NO new test surface**. The §2.2 "Smoke conversational turn" is a manual `Skill(...)` invocation under a fresh Claude Code session, NOT a vitest/node:test file. The Developer:

- **Writes ZERO new test code.**
- **Runs `cd cleargate-cli && npm run typecheck && npm test`** for safety only — to confirm the doc edit didn't break any pre-existing scaffold-lint test that grep-asserts CLAUDE.md content.
- **Does NOT add a new vitest scenario for the prune** even though §4.1 of the story file says "Add a CI-equivalent grep assertion under `cleargate-cli/test/scaffold/`". Per orchestrator-injected policy this milestone, that paragraph is overridden — manual verification (Step 4–8 of the recipe above) is the contract. If a pre-existing scaffold-lint test grep-asserts the deleted strings, the Developer fixes that test in the same commit; otherwise no test edits.

## Cross-story risks

- **Pre-existing 4-bullet canonical-only divergence (FLASHCARD `2026-05-01 #mirror #parity`).** The diff baseline has lived since pre-EPIC-024. The prune surface MUST NOT touch those four divergent bullets — they sit outside the CLEARGATE-block in the live file and are absent from canonical entirely (verified by the diff capture above). Reconciling them is out of scope.
- **CR-028 backfill anchor convergence.** CR-028's M6 commit appended `## Existing Surfaces` + `## Why not simpler?` sections to STORY-026-02's anchor file (it appears at story L202–L215). The DEV agent does NOT re-edit the story file — those sections are static at dispatch time. Confirmation only.
- **Dispatch hook regression vector.** Once "Orchestrator Dispatch Convention" prose disappears from CLAUDE.md, the orchestrator depends on (a) the SessionStart banner R3 contract + (b) the PreToolUse:Task hook auto-attribution. Both shipped in Wave 1. The smoke-test in recipe Step 10 catches this regression.
- **R7 numeric metric override.** As documented above, the ≥60-line target was authored against a pre-prune file that no longer matches reality. The dev report MUST surface the override + cite this plan as authority. Reporter agent will pick this up at sprint close and may flag in REPORT.md §4 if metrics-target tracking is enabled.
- **DoD §4.1 test-count contradiction.** Story §4.1 asserts "≥4 new grep assertions in one test file" with "Test count enforcement: ENFORCED". The orchestrator policy injected at this milestone overrides §4.1 — manual recipe replaces the new test file. Developer must surface this override in the dev report verbatim. (This is the cleanest path; alternative is to file a SPRINT-21 follow-up to add the scaffold-lint grep test.)

## Open decisions for orchestrator

1. **R7 metric waiver acceptance.** The ≥60-line target is mathematically out of reach for the canonical file (70 lines pre-prune) and well short for live (~22 lines deletable without crossing R4/R5). Architect recommends accepting the prune-surface-driven delta and recording the waiver in the dev report + REPORT.md. **Decision needed:** confirm waive (preferred) OR open a follow-up CR for SPRINT-21 to widen the prune surface.
2. **§4.1 grep-test policy override.** The orchestrator's milestone-injection text says "do NOT prescribe new test files; the Dev runs `npm run typecheck` and existing vitest for safety, ZERO new test code." This contradicts story §4.1 ("≥4 new grep assertions in one test file" + DoD checkbox). Architect followed the orchestrator policy. **Decision needed:** confirm orchestrator policy supersedes story §4.1 (no test file added) OR pull §4.1 back in (test file added).
3. **Doc-refresh checklist on close.** Sprint close (Gate 4) will surface a `.doc-refresh-checklist.md` item flagging the CLAUDE.md prune. Architect notes only — Reporter handles at sprint close.

---

## Plan size: 191 lines (out-of-budget by ~70 lines vs the 60-120 target)

The over-budget length is driven by (a) line-level prune-surface enumeration that the Developer needs verbatim to avoid R4/R5 violation, (b) explicit R7 numeric-target waiver rationale + escalation, (c) the cross-file mirror-parity diff baseline. Compressing further risks the Developer mis-pruning a halt rule. Architect accepts the over-budget length for this milestone given the surgical-edit nature.

### M3.md

role: architect

# Milestone M3 — CR-026: Token-Ledger Attribution Fix

## Stories: CR-026
## Wave: W1 (parallel with STORY-026-01 + BUG-025; disjoint hook surfaces)

## Sprint-goal tie
CR-026 makes per-(work_item, agent) attribution recoverable for SPRINT-21 onward, ending the SPRINT-15→18 100%-mis-attributed-to-`BUG-004/architect` regime. Success = SPRINT-21 ledger >95% rows match dispatched (work_item, agent) tuple.

## Order
- **Pre-merge gate (Wave 1 internal):** all five surface edits land in ONE Developer commit. The hook reader, the new PreToolUse:Task hook, and `settings.json` wiring are coupled — split commits would leave the hook reading a marker no one writes (or vice versa) and fail the smoke test in §4.
- **Post-merge gate (Wave-1 → Wave-2 hand-off):** orchestrator MUST verify `cleargate-planning/.claude/hooks/pre-tool-use-task.sh` exists + is executable + is in MANIFEST.json before dispatching CR-027 (Wave 2 mirror-edit dependency).
- **No parallelization inside CR-026** (single dispatch). Surfaces are tightly coupled.

## BUG-024 §3 defect triage — what ships in SPRINT-20 vs defers

| Defect | Severity | Ship in CR-026? | Mechanism |
|---|---|---|---|
| **Defect 1** — session-id mismatch (writer = orchestrator's `CLAUDE_SESSION_ID`; reader = stdin payload's subagent `session_id`) | HIGH | **SHIP** | Replace `DISPATCH_FILE="${SPRINT_DIR}/.dispatch-${SESSION_ID}.json"` (token-ledger.sh:108) with newest-file lookup `ls -t .dispatch-*.json \| head -1`. Decision below: **path-B (newest-file)**, NOT path-A (`parent_session_id`). |
| **Defect 2** — transcript-grep fallback poisoned by SessionStart blocked-items banner | HIGH | **SHIP** | Add banner-skip constant `BANNER_SKIP_RE='^[0-9]+ items? blocked: '` and apply via `sed -E "/${BANNER_SKIP_RE}/d"` before the work-item regex (token-ledger.sh:225-237). Document constant cross-references CR-026. |
| **Defect 3** — manual `write_dispatch.sh` discipline unreliable (~5 calls vs ~19 spawns SPRINT-18) | MEDIUM | **SHIP** | New PreToolUse:Task hook auto-writes the marker on every Task spawn. Manual `write_dispatch.sh` demoted to documented fallback (per Open Question Q2 — additive). |
| **Bonus** — dead `.pending-task-*.json` reader path in token-ledger.sh (~50 LOC at lines 131-158, 166-178) | n/a | **DEFER** | Dead-code removal scope-creeps the diff and risks collateral damage to the working delta-math path that *does* read `SENTINEL_TURN_INDEX` from `.pending-task-*.json` (lines 154, 168). Open separate cleanup CR for SPRINT-21 (`CR-029` candidate). Surface in REPORT.md §6 carry-forward. |

**Open Question Q1 resolution — path-A vs path-B for hook reader.**
**Decision: path-B (newest-file lookup) only.** Path-A (`parent_session_id`) is unverified — `grep -rn parent_session_id .claude/ .cleargate/knowledge/` returns zero hits in our codebase, and Claude Code hook payload schema for SubagentStop has no documented `parent_session_id` field as of cleargate@0.10.0. Adding speculative path-A code would ship dead branches. Newest-file lookup is correct and atomic given the existing `mv → .processed-$$` rename at token-ledger.sh:110-111. If a future Claude Code release exposes `parent_session_id`, file a follow-up CR.

**Open Question Q2 resolution — additive vs replacement.**
**Decision: additive.** PreToolUse:Task hook is primary; `write_dispatch.sh` survives as fallback. CLAUDE.md retains the manual contract as a one-liner pointer (see §"CLAUDE.md text" below).

**Open Question Q3 resolution — strip vs skip transcript-grep fallback.**
**Decision: strip by regex.** Keep transcript-grep as a safety net; banner-skip the SessionStart line.

## Per-story blueprint — CR-026

### Files to modify (live + canonical mirror per file unless noted)

1. **`.claude/hooks/token-ledger.sh`** (live; canonical at `cleargate-planning/.claude/hooks/token-ledger.sh`).
   - **Edit 1 (line 108):** replace
     `DISPATCH_FILE="${SPRINT_DIR}/.dispatch-${SESSION_ID}.json"`
     with newest-file lookup:
     `DISPATCH_FILE="$(ls -t "${SPRINT_DIR}"/.dispatch-*.json 2>/dev/null | head -1)"`
     Add inline comment block above (3 lines) cross-referencing BUG-024 §3.1 Defect 1 and CR-026 path-B decision. The existing `mv → .processed-$$` atomicity (lines 110-111) and the post-row-write `rm -f "${DISPATCH_PROCESSED}"` (lines 380-383) carry over unchanged — they already operate on the resolved filename, not the session-id-keyed pattern.
   - **Edit 2 (lines 225-241, work-item-id resolution).** Insert banner-skip preprocessing before the existing `jq -rs` first-user-message scan AND before the `grep -oE` fallback. Define a constant near the top of the script (insert at ~line 60, before `set -u` block):
     ```
     # CR-026: SessionStart blocked-items banner poisons transcript-grep (BUG-024 §3.1 Defect 2).
     BANNER_SKIP_RE='^[0-9]+ items? blocked: '
     ```
     Then in the legacy work-item resolution block (around lines 225 + 235), pre-filter the input via `sed -E "/${BANNER_SKIP_RE}/d"` either by piping the transcript through sed before the `jq -rs` / `grep -oE`, OR by using `jq` to filter user messages whose content does NOT start with the banner. Implementation: pre-filter with sed when reading the file as a whole; for the jq-on-first-user-message path (line 225-230), wrap content with `select(. | test("^[0-9]+ items? blocked: ") | not)`. Concrete: change line 230's pipeline to skip first user message if it matches banner, then take the next user message.
   - **Do NOT remove `.pending-task-*.json` reader code** (lines 131-158, 166-178) — deferred per defect-table row 4 above.
   - **Net diff estimate:** ~25 LOC modified, ~10 LOC added (constant + comment). No deletions.
   - **Mirror parity:** byte-copy edited live → canonical via `cp` after Edit-1 + Edit-2 land. Update MANIFEST sha256 via `npm run prebuild` in `cleargate-cli/`.

2. **`.cleargate/scripts/write_dispatch.sh`** (live).
   - **Edit:** prepend a 4-line "FALLBACK PATH" comment block immediately after the existing `# Usage:` header (insert at line ~7), explicitly stating: "Primary path is `.claude/hooks/pre-tool-use-task.sh` (CR-026). This script is retained for one-off Architect dispatches that do not parse cleanly via Task() prompt grep."
   - **No logic changes.**
   - **Mirror parity:** create canonical mirror at `cleargate-planning/.cleargate/scripts/write_dispatch.sh` (verified absent today via `ls`). Net-new canonical file. Make executable (`chmod +x`) — FLASHCARD `2026-04-08 #init #scaffold #hook-exec-bit`. Add MANIFEST entry: tier `script`, `overwrite_policy: always`, `preserve_on_uninstall: false`.
   - **Net diff estimate:** ~6 LOC added live; ~120 LOC net-new canonical (byte-copy of live with the same comment).

3. **`.claude/settings.json`** (live; canonical at `cleargate-planning/.claude/settings.json`).
   - **Edit:** add a new entry inside the existing `PreToolUse` array on the `Task` matcher block. Today (line 13-23) the `PreToolUse:Task` block has a single hook (`pending-task-sentinel.sh`). Append a sibling hook in the same `hooks: [...]` array of the same matcher object — Claude Code runs both sequentially:
     ```
     {
       "type": "command",
       "command": "/Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/pre-tool-use-task.sh"
     }
     ```
     Insert as the SECOND element of the existing matcher's `hooks` array (i.e., after `pending-task-sentinel.sh`, line 19). Order matters: pending-task-sentinel runs first (it owns the flashcard gate, which can block), pre-tool-use-task runs second (it never blocks; it writes a marker and exits 0).
   - **Canonical settings.json** uses `${CLAUDE_PROJECT_DIR}/...` placeholder (verified at canonical line 19). Mirror the same pattern: `"command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/pre-tool-use-task.sh"`.
   - **Net diff estimate:** +5 LOC each file.
   - **MANIFEST entry retain:** `.claude/settings.json` already has `overwrite_policy: merge-3way` (verified MANIFEST.json:121). Do NOT change to `preserve-on-conflict` — SDR §2.5 SOFT flag is correctly satisfied by `merge-3way` (3-way merge against the user's last-known scaffold version preserves customizations). The mirror parity check during prebuild will regenerate the sha256.

4. **`CLAUDE.md`** (live, line 143; canonical at `cleargate-planning/CLAUDE.md`, line 52).
   - **Edit:** replace the existing single-paragraph "Orchestrator Dispatch Convention" with the rewritten paragraph below. Stays inside the `<!-- CLEARGATE:START -->...<!-- CLEARGATE:END -->` block (constraint: SPRINT-20 plan §"Constraints" — "No CLAUDE.md changes outside CLEARGATE-tag-block region").
   - **Replacement text (verbatim — Developer copies this into both files):**
     > **Orchestrator Dispatch Convention.** The PreToolUse:Task hook (`.claude/hooks/pre-tool-use-task.sh`) auto-writes a dispatch marker on every `Task()` spawn by parsing the orchestrator's prompt for the first work-item marker (`STORY=NNN-NN`, `BUG-NNN`, `EPIC-NNN`, `CR-NNN`, `PROPOSAL-NNN`, `HOTFIX-NNN`) and the `subagent_type`. The SubagentStop hook (`.claude/hooks/token-ledger.sh`) reads the newest `.dispatch-*.json` in the active sprint dir, attributes the row, and renames the file to `.processed-*` so subsequent fires don't re-consume it. **Manual fallback:** if the spawn prompt does not contain a parseable work-item marker (e.g., a one-off Architect dispatch not tied to a sprint-tree work item), call `bash .cleargate/scripts/write_dispatch.sh <work_item_id> <agent_type>` immediately before the `Task()` call. Example: `bash .cleargate/scripts/write_dispatch.sh CR-026 architect`.
   - **Net diff estimate:** ~6 LOC each file (replaces ~3 LOC).
   - **STORY-026-02 prune coordination:** STORY-026-02 (Wave 3) prunes the four-agent-loop / Sprint-Execution-Gate / Architect-runs-twice prose. The Dispatch Convention paragraph IS in the prune surface per SPRINT-20 §2.2 ("CR-026's region is part of the prune surface — its content moves to the skill in W1 via STORY-026-01 R5"). Therefore: CR-026 lands the *correct* paragraph text in CLAUDE.md AND verifies that STORY-026-01's R5 byte-copy of the live skill captures the correct dispatch-convention text in the skill. **Required pre-Wave-1 check:** Developer agent for CR-026 reads `.claude/skills/sprint-execution/SKILL.md` §1 and confirms the skill already documents the same primary/fallback contract. If not, Developer files a follow-up note in their dev report so STORY-026-01's R5 byte-copy is the authoritative carrier.

### Files to create (live + canonical mirror)

5. **`.claude/hooks/pre-tool-use-task.sh`** (NEW; mirror at `cleargate-planning/.claude/hooks/pre-tool-use-task.sh`).
   - **Hook contract (~30-40 LOC):**
     - **Trigger:** PreToolUse, matcher `Task`.
     - **Stdin:** Claude Code JSON payload with `session_id`, `transcript_path`, `cwd`, `hook_event_name`, `tool_name`, `tool_input`. For `tool_name == "Task"`, `tool_input` has `subagent_type` (string) + `description` (string) + `prompt` (string).
     - **Behavior:**
       1. Resolve `REPO_ROOT` via `${ORCHESTRATOR_PROJECT_DIR:-${CLAUDE_PROJECT_DIR}}` (matches token-ledger.sh:59 + write_dispatch.sh:30 pattern).
       2. Resolve active sprint via `.cleargate/sprint-runs/.active` sentinel; on missing sentinel, exit 0 silently (no marker written; bucket falls through to `_off-sprint` per existing convention).
       3. Read stdin, extract `tool_input.subagent_type` via `jq -r '.tool_input.subagent_type // empty'`. If empty or not in the allow-list (`architect|developer|qa|reporter|cleargate-wiki-contradict`), exit 0 silently with a log line.
       4. Extract `tool_input.prompt`, take the first 5 lines, grep with the work-item regex `(STORY=?[0-9]{3}-[0-9]{2}|BUG-[0-9]+|EPIC-[0-9]+|CR-[0-9]+|PROPOSAL-[0-9]+|HOTFIX-[0-9]+)`. Take the first match. Normalize `STORY=NNN-NN` → `STORY-NNN-NN` via sed. If no match, exit 0 silently with a log line (transcript-grep fallback at SubagentStop will handle attribution).
       5. Resolve orchestrator session ID. **Path-B compat:** the marker filename does NOT need the orchestrator session ID anymore (newest-file lookup makes it irrelevant). Use a uniquified filename: `.dispatch-$(date -u +%s)-$$-${RANDOM}.json` to avoid collision with parallel Task() spawns. Embed the orchestrator session ID inside the JSON body as `session_id` (read from `${CLAUDE_SESSION_ID:-}` or stdin's `session_id`) for forensic value.
       6. Write JSON body via `jq -cn` with shape `{work_item_id, agent_type, spawned_at, session_id, writer: "pre-tool-use-task.sh@cleargate-<version>"}`. Atomic write via `mktemp + mv` (matches write_dispatch.sh:110-112).
       7. Append one log line to `.cleargate/hook-log/pre-tool-use-task.log` for forensic value (`[ts] wrote dispatch: sprint=<sprint> work_item=<id> agent=<type>` or `[ts] no marker: <reason>`).
       8. **Exit code 0 always.** Never block a Task spawn.
   - **Banner-immunity:** the hook reads `tool_input.prompt` directly from the JSON payload — there is no transcript involvement, so the SessionStart banner cannot poison this path.
   - **Race condition (parallel Task() spawns):** uniquified filename prevents marker overwrite; newest-file lookup at SubagentStop time consumes them in stop-order, which matches the spawn-completion order well enough for >95% accuracy. **Race acknowledged + accepted** — see "write_dispatch.sh refactor scope" below.
   - **Make executable:** `chmod +x` post-write. FLASHCARD `2026-04-08 #init #scaffold #hook-exec-bit`.
   - **Mirror parity:** byte-copy live → canonical. Add MANIFEST entry: tier `hook`, `overwrite_policy: always`, `preserve_on_uninstall: false` (matches existing `pending-task-sentinel.sh` MANIFEST entry MANIFEST.json:62-67).

6. **`cleargate-cli/test/hooks/pre-tool-use-task.test.ts`** (NEW).
   - **Pattern:** mirror `cleargate-cli/test/hooks/token-ledger-attribution.test.ts` scaffolding (verified at lines 1-60 — `execFileSync` against the real bash hook, synthetic stdin JSON, tmpdir for repo root).
   - **Required scenarios (6, all real-bash, no mocks):**
     1. STORY marker parse — stdin prompt starts with `STORY=026-01\n...\nsubagent_type: developer` → marker file written with `work_item_id: STORY-026-01`, `agent_type: developer`.
     2. BUG marker parse — `BUG-025` in prompt + `subagent_type: developer` → `work_item_id: BUG-025`, `agent_type: developer`.
     3. CR marker parse — `CR-026` + `architect` → `CR-026` + `architect`.
     4. EPIC marker parse — `EPIC-026` + `architect` → `EPIC-026` + `architect`.
     5. No-marker fallback — prompt with no work-item ID → no marker written, exit 0, log line `no marker: regex miss`.
     6. Idempotency / parallel-spawn collision — fire the hook twice in quick succession with two different work items; both markers written with distinct filenames; both readable by jq.
   - **Real-infra constraint per CLAUDE.md "Real infra, no mocks" rule.** Per SPRINT-20 §2.5 constraints: vitest pool-cap maxForks=2.

### Files explicitly NOT modified (carry forward CR-026 §3 "Do NOT modify" list)
- `cleargate-cli/src/lib/token-ledger.ts` — schema unchanged.
- `.claude/agents/{architect,developer,qa,reporter}.md` — agent role contracts unchanged.
- `close_sprint.mjs` Step 6 — reads ledger as-is.
- `prep_reporter_context.mjs` — reads ledger as-is.
- `.cleargate/scripts/write_dispatch.sh` logic — comment-only change (no logic).
- `.claude/hooks/pending-task-sentinel.sh` — untouched. The new hook is a sibling, not a replacement.
- `.claude/hooks/token-ledger.sh` legacy `.pending-task-*.json` reader (lines 131-178) — deferred to SPRINT-21 cleanup CR.

### Schema changes
**None.** `token-ledger.jsonl` row schema unchanged. `.dispatch-*.json` body shape unchanged (still `{work_item_id, agent_type, spawned_at, session_id, writer}`); only the filename pattern changes from `.dispatch-<session-id>.json` to `.dispatch-<ts>-<pid>-<rand>.json`. Settings.json adds one entry to existing PreToolUse:Task array (additive). No migrations.

### Test scenarios (Gherkin from CR-026 §4 + new)

**Pre-fix regression sentinel (must keep passing — proves the bug existed):**
1. Given SPRINT-18's archived ledger at `.cleargate/sprint-runs/SPRINT-18/token-ledger.jsonl`, when `cat ... | jq -r '.work_item_id + " / " + .agent_type' | sort -u | wc -l` runs, expect `1`. (Sentinel — DO NOT alter SPRINT-18 ledger.)

**Post-fix integration (must pass after merge):**
2. Given a fresh Claude Code session with `.cleargate/sprint-runs/.active=SPRINT-20`, when orchestrator runs `Task(subagent_type=developer, prompt="STORY=026-01 ...")`, then immediately after spawn `ls .cleargate/sprint-runs/SPRINT-20/.dispatch-*.json` shows ≥1 new file with `work_item_id: STORY-026-01` + `agent_type: developer`.
3. Given a completed subagent run with a present dispatch marker, when SubagentStop fires, then `.cleargate/sprint-runs/SPRINT-20/token-ledger.jsonl` gains one row with `work_item_id: STORY-026-01` + `agent_type: developer` AND `.cleargate/hook-log/token-ledger.log` gains one `dispatch-marker:` success line.
4. Given a SessionStart-poisoned transcript (banner line `1 items blocked: BUG-004:` + later valid `STORY-026-01` marker) AND no dispatch file present, when SubagentStop fires, then the resulting ledger row attributes to `STORY-026-01`, NOT `BUG-004` (banner-skip works).

**Unit tests for new hook (CR-026 §3 + plan additions):**
5. All 6 fixtures in `pre-tool-use-task.test.ts` pass under `cd cleargate-cli && npm test -- pre-tool-use-task.test`.

**Smoke test (manual, post-merge):**
6. Per CR-026 §4 smoke-test 5-step procedure (verbatim, including the `dispatch-marker:` log assertion).

### Reuse (no duplication)
- **Repo-root resolution pattern** — copy from `token-ledger.sh:59` + `write_dispatch.sh:30` (`${ORCHESTRATOR_PROJECT_DIR:-${CLAUDE_PROJECT_DIR}}`). Do NOT re-invent.
- **Atomic write pattern** — `mktemp + mv` (write_dispatch.sh:110-112).
- **JQ payload shape** — match `write_dispatch.sh:95-107` so consumers see one schema regardless of writer.
- **Active-sprint sentinel resolution** — match `pending-task-sentinel.sh:42-49`.
- **Hook log convention** — write to `.cleargate/hook-log/<hook-name>.log` with `[ISO-8601]` line prefix.
- **Test harness** — extend `cleargate-cli/test/hooks/token-ledger-attribution.test.ts` execFileSync pattern for `pre-tool-use-task.test.ts`. Reuse `makeTranscript` / `makeAssistantTurn` helpers via export-and-import OR copy with attribution comment (preferred: extract to `cleargate-cli/test/hooks/_helpers.ts` if it doesn't exist; otherwise inline-copy with comment pointer to source file).

### Gotchas surfaced from code inspection

- **GOTCHA-1 — `.pending-task-*.json` is NOT entirely dead.** BUG-024 §3.1 "Bonus" claims ~50 LOC of dead `.pending-task-*.json` reader code. **Verified false-partial:** lines 131-158 + 166-178 of token-ledger.sh DO read `.pending-task-*.json` files, AND the writer (`pending-task-sentinel.sh`, verified executable + wired in settings.json:13-23) actively writes them. The path is *underused* (the dispatch-marker block at lines 96-128 takes precedence) but it's not unwritten dead code. The `SENTINEL_TURN_INDEX` value at line 154 is read by the per-task delta math at line 168 — deleting the reader breaks delta accounting under the no-dispatch-marker path. **Decision:** defer the cleanup; do NOT remove. Open a SPRINT-21 cleanup CR (candidate: `CR-029 Token-Ledger Dead-Path Removal`).
- **GOTCHA-2 — token-ledger.sh:95-128 dispatch-marker block uses `mv → .processed-$$` pattern.** Already atomic. The newest-file lookup edit (Edit-1) preserves the rename target — `DISPATCH_PROCESSED="${DISPATCH_FILE%.json}.processed-$$"` works on any filename. No edit needed downstream of line 108.
- **GOTCHA-3 — settings.json sibling-hook ordering.** Two hooks on the same `PreToolUse:Task` matcher run in array order. `pending-task-sentinel.sh` MUST run first because it owns the v2 flashcard gate (verified at sentinel.sh:51-58 — exits non-zero under v2 with unprocessed cards). If `pre-tool-use-task.sh` ran first and crashed (it shouldn't — it always exits 0 — but defensively), the flashcard gate would still fire after.
- **GOTCHA-4 — canonical `cleargate-planning/.cleargate/scripts/` has no `write_dispatch.sh` today.** Verified by `ls`. The canonical mirror is being created NEW. Add a MANIFEST entry alongside.
- **GOTCHA-5 — orchestrator session ID is unreliable in PreToolUse stdin.** The PreToolUse hook fires inside the orchestrator's session, so `${CLAUDE_SESSION_ID}` is set; but on subagent re-entry / nested Task spawns the env var may not propagate. Hence: do NOT key the dispatch filename on session ID. Uniquify with timestamp + pid + random instead.
- **GOTCHA-6 — `parent_session_id` is unverified in Claude Code's SubagentStop payload.** `grep -rn parent_session_id` returns zero hits in our tree. Path-A is speculative. Path-B is empirically grounded. CR-026 takes path-B; future CR can investigate path-A if Anthropic publishes the field.
- **GOTCHA-7 — STORY-026-02 prune surface includes the Dispatch Convention paragraph.** Per SPRINT-20 SDR §2.2, the paragraph CR-026 rewrites is itself part of the Wave-3 prune surface. The skill (`.claude/skills/sprint-execution/SKILL.md` §1, mirrored canonical via STORY-026-01 R5) becomes the post-Wave-3 carrier. CR-026's CLAUDE.md edit lands first; STORY-026-01 R5's byte-copy of the live skill captures the rewritten convention; STORY-026-02 prunes the paragraph from CLAUDE.md last. **No conflict** — the chain works as long as Wave 1 commits land in the correct order (CR-026 → STORY-026-01 R5 byte-copy after CR-026 lands, OR they merge in either order because R5 byte-copies the LIVE skill which CR-026 doesn't touch).

## Cross-story risks (Wave-1 + downstream)

- **Risk-1 — STORY-026-01 byte-copy timing.** STORY-026-01 R5 copies `.claude/skills/sprint-execution/SKILL.md` to canonical. If STORY-026-01 commits BEFORE CR-026's CLAUDE.md edit but AFTER CR-026's hook edits, the canonical skill mirror still contains the OLD dispatch convention text. Resolution: STORY-026-01's Developer audits the live skill §1 dispatch text after CR-026's commit lands AND amends the byte-copy if needed. Surface this in the Wave-1 dispatch sequencing note.
- **Risk-2 — BUG-025 corpus dedupe pass touches `.cleargate/delivery/**/*.md` files.** No collision with CR-026's surfaces (CR-026 touches `.claude/`, `.cleargate/scripts/`, `cleargate-planning/`, `CLAUDE.md`, `cleargate-cli/test/hooks/` — disjoint from `.cleargate/delivery/`). Confirmed safe.
- **Risk-3 — settings.json simultaneous Wave-1 edits.** No — only CR-026 edits settings.json in Wave 1 per SDR §2.2 row "settings.json (+ canonical mirror if present)". Safe.
- **Risk-4 — downstream user repos with customized `.claude/settings.json`.** SDR §2.5 SOFT flag says `overwrite_policy: preserve-on-conflict`. **Decision: keep `merge-3way`** (existing MANIFEST value at MANIFEST.json:121). 3-way merge preserves user customizations against the new canonical baseline. Adding a new sibling hook on an existing matcher is a benign additive change that 3-way merge handles cleanly. Switching to `preserve-on-conflict` would freeze downstream settings entirely — harmful, since downstream users won't get the fix on `cleargate init` upgrade. SDR §2.5 SOFT flag is **resolved by retaining `merge-3way`**.
- **Risk-5 — newest-file lookup race under parallel Task() spawns.** If orchestrator fires Task A then Task B 50ms apart, both write distinct dispatch markers. SubagentStop fires for A (newest = B's marker — wrong). **Mitigation acknowledged, not fixed in CR-026:** the SubagentStop fires in stop-order which generally follows spawn-order under sequential dispatch. Wave 1 dispatches 3 agents but they're long-running (minutes), so the race window is microseconds-to-milliseconds at spawn time vs minutes-to-hours at stop time. Empirically, attribution should be >95% even with the race. If accuracy < 95%, file follow-up CR for SPRINT-21 to add `parent_session_id` (path-A) or correlation tokens.
- **Risk-6 — write_dispatch.sh refactor scope creep.** **Decision: NO refactor in CR-026.** Comment-only edit + canonical mirror creation. Race-fix deferred. Justification: `write_dispatch.sh` is now fallback-only (Defect 3 mitigated by the new hook); race fixes are higher-leverage on the new hook. If write_dispatch.sh is invoked manually for one-off Architect dispatches, sequential timing is human-controlled — no race in practice.

## Test shape

### Unit (vitest, real bash, no mocks per CLAUDE.md "Real infra")
- `cleargate-cli/test/hooks/pre-tool-use-task.test.ts` — 6 scenarios per §"Test scenarios" above. ~150-200 LOC scaffolding.
- `cleargate-cli/test/hooks/token-ledger-attribution.test.ts` — extend with 2 new scenarios:
  - **CR-026-A:** dispatch file present (newest-file lookup), session ID in payload mismatches dispatch filename → still attributes correctly. Asserts the path-B fix.
  - **CR-026-B:** transcript with banner line + valid work-item ID → ledger row attributes to the work-item, NOT the banner. Asserts the banner-skip fix.

### Real-infra integration test (NEW file)
- `cleargate-cli/test/hooks/cr-026-integration.test.ts` — single scenario:
  - Setup: synthesize a tmpdir as a fake repo root with `.cleargate/sprint-runs/.active=SPRINT-CR-026-TEST`, a fake transcript on disk, a fake `.dispatch-<rand>.json` written via `pre-tool-use-task.sh` from synthetic stdin (`tool_input.prompt = "STORY=026-01 ..."`, `subagent_type = developer`). Then fire `token-ledger.sh` with stdin payload pointing at the fake transcript.
  - Assert: the resulting ledger row has `work_item_id: STORY-026-01`, `agent_type: developer`. The `.dispatch-*.json` file is renamed to `.processed-*` and removed post-row-write. Hook log contains both `wrote dispatch:` (pre-tool-use) and `dispatch-marker:` (token-ledger) success lines.
  - This test exercises end-to-end attribution accuracy for one row. Multi-row >95% accuracy is asserted ad hoc post-SPRINT-21 close (per SPRINT-20 §0 success metric).

### >95% attribution accuracy assertion
The >95% claim is an outcome metric, not a unit test. CR-026 §4's verification post-fix command (`cat .cleargate/sprint-runs/SPRINT-21/token-ledger.jsonl | jq -r '.work_item_id + " / " + .agent_type' | sort -u | wc -l ≥ 3`) IS the metric check. Add a Reporter §6 line during SPRINT-21 close to verify. Surface in REPORT.md template (out of scope for CR-026 itself; the existing Reporter aggregates from ledger).

### Pre-commit gate
`npm run typecheck` clean + `npm test` green for `cleargate-cli` (per CLAUDE.md "Test + commit conventions"). New tests added to `cleargate-cli` package.

## Risks

| Risk | Mitigation |
|---|---|
| settings.json `overwrite_policy` choice (SDR §2.5 SOFT flag suggested `preserve-on-conflict`) | **Decision: keep `merge-3way` (existing).** 3-way merge preserves user customizations AND propagates the new hook entry on `cleargate init` upgrade. `preserve-on-conflict` would freeze downstream settings and prevent the fix from reaching users. Document choice in CR-026 commit message. |
| Surface area collision with STORY-026-02 prune (Wave 3) | CR-026 ships Wave 1 (paragraph stays in CLAUDE.md temporarily). STORY-026-01 R5 byte-copies the LIVE skill (which already documents dispatch convention) into canonical. STORY-026-02 prunes paragraph from CLAUDE.md after the skill carries it. CR-026 Developer agent verifies skill §1 dispatch text post-CR-026-merge; if mismatched, files a follow-up note for STORY-026-01's Developer. |
| Downstream user-repo settings safety | `merge-3way` per above. Smoke-test: run `cleargate init` against a tmpdir with a customized settings.json (e.g., extra SubagentStop hook); verify the customization survives + `pre-tool-use-task.sh` is added. Add this to CR-026 acceptance §4. |
| MANIFEST entry overwrite_policy for new files | `pre-tool-use-task.sh` (live + canonical): tier `hook`, `overwrite_policy: always`, `preserve_on_uninstall: false` — matches `pending-task-sentinel.sh` MANIFEST.json:62-67 exactly. `write_dispatch.sh` (canonical, NEW): tier `script`, `overwrite_policy: always`, `preserve_on_uninstall: false`. **`settings.json` MANIFEST entry: NO CHANGE** (sha256 regenerates via prebuild; `merge-3way` retained). |
| Race condition under parallel Task() spawns | Acknowledged, not fixed. >95% target accepted given stop-order ≈ spawn-order under long-running agents. Follow-up CR for SPRINT-21 if SPRINT-21's Reporter shows < 95%. |
| `.pending-task-*.json` dead-code claim from BUG-024 §3.1 was partial-false | Verified: the path is underused, not unwritten. Defer cleanup to SPRINT-21. Surfaces as REPORT.md §6 carry-forward. |
| Newest-file lookup at SubagentStop sees a stale dispatch-marker from a previous orchestrator session | Mitigated by the existing `mv → .processed-$$` rename + post-row-write `rm -f` (token-ledger.sh:110-111, 380-383). Stale markers from prior sprints would only persist if the hook crashed between rename and rm — extremely rare. The newest-file selector picks the most recent by mtime; stale markers from the previous sprint are pre-rotation and pre-active-sentinel-flip, unreachable in steady state. |

## MANIFEST entry specification (per §2.5 SOFT flag)

Add to `cleargate-planning/MANIFEST.json` `files[]` array (alphabetical sort by path):

```
{
  "path": ".claude/hooks/pre-tool-use-task.sh",
  "sha256": "<computed-by-prebuild>",
  "tier": "hook",
  "overwrite_policy": "always",
  "preserve_on_uninstall": false
}
```

```
{
  "path": ".cleargate/scripts/write_dispatch.sh",
  "sha256": "<computed-by-prebuild>",
  "tier": "script",
  "overwrite_policy": "always",
  "preserve_on_uninstall": false
}
```

**No change to `.claude/settings.json` MANIFEST entry** — keep `overwrite_policy: merge-3way`. Sha256 regenerates via `npm run prebuild` to reflect the new sibling-hook entry.

**Prebuild invocation:** `cd cleargate-cli && npm run prebuild`. Verify regenerated `cleargate-cli/templates/cleargate-planning/...` mirror matches canonical post-edit. FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`.

## CLAUDE.md text for "Orchestrator Dispatch Convention" paragraph (verbatim insertion)

The Developer copies this verbatim into BOTH `CLAUDE.md` (live, line 143) AND `cleargate-planning/CLAUDE.md` (canonical, line 52). Inside `<!-- CLEARGATE:START -->...<!-- CLEARGATE:END -->` block:

> **Orchestrator Dispatch Convention.** The PreToolUse:Task hook (`.claude/hooks/pre-tool-use-task.sh`) auto-writes a dispatch marker on every `Task()` spawn by parsing the orchestrator's prompt for the first work-item marker (`STORY=NNN-NN`, `BUG-NNN`, `EPIC-NNN`, `CR-NNN`, `PROPOSAL-NNN`, `HOTFIX-NNN`) and the `subagent_type`. The SubagentStop hook (`.claude/hooks/token-ledger.sh`) reads the newest `.dispatch-*.json` in the active sprint dir, attributes the row, and renames the file to `.processed-*`. **Manual fallback:** if the spawn prompt does not contain a parseable work-item marker, call `bash .cleargate/scripts/write_dispatch.sh <work_item_id> <agent_type>` immediately before the `Task()` call. Example: `bash .cleargate/scripts/write_dispatch.sh CR-026 architect`.

## Open decisions for orchestrator (out of Architect scope)

- **None blocking.** All three CR-026 §0.5 Open Questions resolved per Architect M3 plan above (path-B; additive; strip-by-regex). SDR §2.5 SOFT flag for settings.json `overwrite_policy` resolved (keep `merge-3way`). Bonus dead-code defer is an Architect call (deferred), not an orchestrator call.
- **For SPRINT-21 Reporter (informational):** verify SPRINT-21 ledger >95% attribution accuracy via `cat token-ledger.jsonl | jq -r '.work_item_id + " / " + .agent_type' | sort | uniq -c`. If <95%, file `CR-029` (or successor numbering) for path-A `parent_session_id` investigation.

## Dispatch precondition (orchestrator MUST verify before spawning Developer)

1. `ls -la .claude/hooks/pre-tool-use-task.sh` — does NOT exist (this is the file CR-026 creates).
2. `ls -la .claude/skills/sprint-execution/SKILL.md` — exists (verified at SDR-time, 24499 bytes).
3. Sprint sentinel `.cleargate/sprint-runs/.active` reads `SPRINT-20`.
4. CLAUDE.md "Orchestrator Dispatch Convention" paragraph at line 143 still has the OLD `write_dispatch.sh`-as-primary text (verified). CR-026 will replace it.
5. `vitest --pool-options.forks.maxForks=2` (per SPRINT-20 §2.5 constraint) — Developer runs with VITEST_MAX_FORKS=2 by default; if orchestrator dispatches 3 Wave-1 agents on tight-RAM, drop to VITEST_MAX_FORKS=1 for CR-026's run.

## Flashcards to record on completion (Architect → Developer hand-off note)

After CR-026 merges and SPRINT-21 ledger shows attribution working, append:
- `2026-MM-DD · #hooks #ledger #dispatch · CR-026 fix: token-ledger.sh now uses newest-file lookup (ls -t .dispatch-*.json | head -1), not session-id-keyed lookup; orchestrator's CLAUDE_SESSION_ID never matches subagent's stdin session_id`
- `2026-MM-DD · #hooks #attribution #pre-tool-use-task · PreToolUse:Task hook auto-writes dispatch marker by greping tool_input.prompt for first work-item ID; banner-immune (no transcript involvement)`
- `2026-MM-DD · #hooks #ledger #banner-skip · token-ledger.sh transcript-grep fallback now skips ^[0-9]+ items? blocked: prefix; SessionStart banner stops poisoning attribution`

(These are Developer's responsibility to record after merge per Skill(flashcard) protocol — not Architect's.)

### M5.md

role: architect

# Milestone M5 — CR-027: Composite Planning Readiness at Sprint Preflight

## Stories: CR-027
## Wave: W2 (parallel with CR-028; serialized AFTER Wave 1 fully merges)

## Sprint-goal tie
CR-027 raises the planning-quality bar so `cleargate sprint preflight` validates the **content** of in-scope work items (not just env health) — closing the wire gap that lets a sprint enter Active with `cached_gate_result.pass: false` items and adding `discovery-checked` + `risk-table-populated` criteria so triage skips and risk-empty plans both fire a hard signal under v2.

## Order
- **Pre-dispatch precondition (Wave-1 → Wave-2 hand-off, MUST verify before launching this Developer):**
  1. STORY-026-01 merged to `main` AND `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` exists. **VERIFIED at SDR-time:** mirror present, 471 lines, byte-equal to live (`diff` returns empty). Live `### A.1 Sprint Execution Gate (Gate 3) — preflight` anchor is at line 110, "Four checks, all must pass:" at line 116.
  2. CR-026 merged (it owns the only other Wave-1 CLAUDE.md edit). The CLAUDE.md "Sprint Execution Gate" paragraph at live line 123 / canonical line 32 still reads "The four checks (...)" — verified via grep at SDR-time.
  3. BUG-025 merged (Wave-1 corpus dedupe touches `.cleargate/delivery/**/*.md` frontmatter only — disjoint from CR-027 surfaces).
  4. `npm run prebuild` ran clean post-Wave-1 (regenerates `cleargate-cli/templates/cleargate-planning/...` + MANIFEST sha256s). FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`.
- **In-CR ordering (single Developer commit):** all eight modified files + the new test file land in ONE commit, conventional format `feat(CR-027): off-sprint — composite planning readiness at sprint preflight + discovery/risk criteria` (per CR-027 §4 "Pre-commit"). Splitting commits would leave `discovery-checked` defined in `readiness-gates.md` but not exercised by preflight, or vice versa — the wire and the criteria are coupled by the §4 verification protocol.
- **Mirror parity per-edit, not whole-file** (FLASHCARD `2026-05-01 #mirror #parity`). Edit live, then `cp` to canonical for each of the four mirrored doc files; do NOT reconcile pre-existing divergence as a side effect.
- **Parallelization with CR-028:** SAFE per SDR §2.2 — CR-027 edits Gate 3 / Sprint Execution Gate region of the protocol + readiness-gates entries 2/3/4/5 + `cleargate.ready-for-execution` (NEW gate); CR-028 inserts pre-§0 preamble + adds `reuse-audit-recorded` / `simplest-form-justified` to different criteria slots. Disjoint regions, rebase-safe. CR-027 lands first per §2.2 "CR-027 first, CR-028 rebases."

## `assert_story_files.mjs` extraction-strategy decision

**Decision: PATH (a) — lightweight shell-out + JSON-stdout protocol.** NOT path (b).

**Rationale (decision recorded for the Developer to follow verbatim):**
- Path (a) total cost: ~30 LOC. Add `export` keyword to two functions (lines 74, 90) + new `--emit-json` CLI flag block in `main()` that prints `{ workItemIds: [...] }` to stdout and exits 0; in `sprint.ts`, `execSync('node .cleargate/scripts/assert_story_files.mjs <sprint-file> --emit-json', { encoding: 'utf8' })` and `JSON.parse(stdout)`.
- Path (b) total cost: ~80 LOC. New `cleargate-cli/src/lib/work-item-extractor.ts`, refactor mjs to import via `createRequire` shim, update mirror under `cleargate-planning/`, new MANIFEST entry, regenerate scaffold prebuild. **Rejected because:** (i) the mjs script is invoked via `run_script.sh` from sprint init/close pipelines that pre-date the CLI bundle and have no TS toolchain in their hot path; (ii) `createRequire` of a TS-emitted module from raw mjs requires a published-CJS-or-bundled-ESM artifact, which forces a build-order coupling between the CLI prebuild and the bash script's runtime — exactly the kind of cross-package coupling protocol §13 cautions against; (iii) the helpers are 30 LOC of pure-regex extraction with zero external deps — duplication risk is near-zero, and the mjs already lives in BOTH live + canonical (verified `cleargate-planning/.cleargate/scripts/assert_story_files.mjs` exists).
- **Contract guard against drift** (the §3 risk row CR-027's spec calls out): the new vitest scenario #3 (per-item gate failure naming) asserts the `extractWorkItemIds` regex shape end-to-end through the shell-out path. If a future CR mutates the mjs regex without touching `sprint.ts`, scenario #3 fails fast.
- **Filename for the shell-out wrapper inside `sprint.ts`:** add a private helper `extractInScopeWorkItemIds(sprintFilePath, cwd, execFn)` adjacent to `checkPerItemReadinessGates` so the `execFn` test seam composes cleanly (same seam already used by `checkNoLeftoverWorktrees` at line 940-950). Tests inject a stub `execFn` that returns canned JSON; no real shell-out in unit tests.

**`assert_story_files.mjs` net edits (path-a):**
1. Line 74 prepend `export` → `export function extractWorkItemIds(text)`. (verified `function` keyword today).
2. Line 90 prepend `export` → `export function findWorkItemFile(repoRoot, workItemId)`. (verified `function` keyword today).
3. Add `--emit-json` flag handling in `main()` (after line 206 `args` parse): when `args.includes('--emit-json')`, parse the deliverables section, run `extractWorkItemIds`, write `JSON.stringify({ workItemIds })` to stdout, `process.exit(0)`. Bypasses the file-presence assertion — `sprint.ts` only needs the IDs, not the missing/unapproved/empty triage. ~12 LOC.
4. Mirror parity: `cp .cleargate/scripts/assert_story_files.mjs cleargate-planning/.cleargate/scripts/assert_story_files.mjs` after edits. Run `npm run prebuild` in `cleargate-cli/` to regenerate sha256.

## Per-story blueprint — CR-027

### Files to modify (live + canonical mirror per file unless noted)

1. **`cleargate-cli/src/commands/sprint.ts`** (live only; not mirrored).
   - **Edit 1 (insertion point line ~862, after `deriveSprintBranchForPreflight` helper):** add new private helpers `extractInScopeWorkItemIds(sprintFilePath, cwd, execFn)` and `checkPerItemReadinessGates(sprintId, cwd, execFn, mode)` returning `PreflightCheckResult`. Body:
     - Resolve sprint file via the SAME pattern as `discoverSprintFile` in `execution-mode.ts:59-86` (search `pending-sync/` then `archive/`). DO NOT re-invent — copy the helper inline (it's 28 LOC) OR import `discoverSprintFile` if it gets exported in this CR; **decision: keep it private to `execution-mode.ts` and re-implement inline in `sprint.ts` as a 12-line `findSprintFile(sprintId, cwd)` helper** (avoid a cross-command export this CR doesn't otherwise need; `execution-mode.ts` is already imported here per line 23).
     - Read `execution_mode` via `readSprintExecutionMode(sprintId, { cwd, sentinelFallback: false })` (already imported, line 23). When `mode === 'v1'`, return `{ name, pass: true, skipped: true, message: 'skipped (execution_mode: v1 — advisory only)' }` per CR-027 §1 "v1 warns" model and §0.5 Q2 ("yes — mirror CR-014").
     - Shell out to `node .cleargate/scripts/assert_story_files.mjs <sprintFilePath> --emit-json`, parse `{ workItemIds }`. On non-zero exit OR malformed JSON, return `{ pass: false, skipped: false, message: 'Per-item readiness gates: failed to parse sprint plan deliverables', hint: 'Verify §1 Consolidated Deliverables exists and lists work-item IDs.' }`.
     - For each ID: call existing `findWorkItemFile(REPO_ROOT, id)` semantics (re-implement the 14 lines inline OR use the new path-a export — **decision: use the path-a export via dynamic import-from-mjs is over-engineered; replicate the 14-line file-finder inline** as `findWorkItemFileLocal(cwd, id)` immediately above `checkPerItemReadinessGates`). For each found file:
       - Read with `fs.readFileSync` + `parseFrontmatter` from `cleargate-cli/src/wiki/parse-frontmatter.ts` (already imported indirectly via `frontmatter-cache.ts`). **Use `readCachedGate` from `cleargate-cli/src/lib/frontmatter-cache.ts:27`** (verified exported, async). Returns `null` (no cached_gate_result) OR `{ pass, failing_criteria, last_gate_check }`.
       - Read frontmatter status + `updated_at` separately for staleness + Done-skip checks (one extra `parseFrontmatter` call OR cache the parsed result — Developer's call, both are <2ms).
       - **Skip rule (per CR-027 §1):** if frontmatter `status` is one of `{Done, Completed, Abandoned, Closed, Resolved}` (the exact set declared at `sprint.ts:38` as `TERMINAL_STATUSES`), do NOT include the item in the failing list — even if `cached_gate_result.pass !== true`. Reuse the existing `TERMINAL_STATUSES` constant; do not redeclare.
       - **Fail rule (per CR-027 §1):** item fails if `cached_gate_result === null` OR `cached_gate_result.pass !== true` OR `last_gate_check < frontmatter.updated_at` (staleness — string compare on ISO-8601 is correct here; same pattern as `wiki lint` uses).
       - On fail, accumulate `{ id, reason: 'pass=false' | 'stale' | 'no cached_gate_result', failing_criteria: cached.failing_criteria.map(c=>c.id) }`.
     - **Sprint-plan self-check:** the sprint plan ITSELF is one of the items in scope (per CR-027 §1 "the sprint plan is itself one of the items in scope" + the new `sprint.ready-for-execution` gate). **However**, `assert_story_files.mjs --emit-json` only extracts the SPRINT-NN's child IDs from §1 Consolidated Deliverables — it does NOT include the sprint ID itself. Therefore: AFTER the per-child loop, separately read the sprint plan file's own `cached_gate_result` and apply the same fail rule with the sprint's own ID (e.g., `SPRINT-20`) prefixed in the failure message. This satisfies vitest scenario #6 ("Sprint plan itself fails `risk-table-populated` → check #5 names SPRINT-NN as a failing item").
     - On any failures, return `{ pass: false, skipped: false, message: \`Per-item readiness gates: ${failures.length}/${total} items not ready\`, hint: <multi-line list with bullet per item: '- <ID>: <criterion-id-csv>'> }`.
     - On all pass, return `{ pass: true, skipped: false, message: 'all in-scope items pass readiness gates' }`.
   - **Edit 2 (line 1098-1104, checks array):** append `checkPerItemReadinessGates(opts.sprintId, cwd, execFn, mode)` as the FIFTH element. Compute `mode = readSprintExecutionMode(opts.sprintId, { cwd })` once before the array literal (single fs read, then reuse). Final array:
     ```ts
     const mode = readSprintExecutionMode(opts.sprintId, { cwd });
     const results: PreflightCheckResult[] = [
       checkPrevSprintCompleted(opts.sprintId, cwd),
       checkNoLeftoverWorktrees(cwd, execFn),
       checkSprintBranchRefFree(opts.sprintId, cwd, execFn),
       checkMainClean(cwd, execFn),
       checkPerItemReadinessGates(opts.sprintId, cwd, execFn, mode),
     ];
     ```
   - **Edit 3 (line 1052, `emitPunchList` success-line stdout):** replace `'all four checks pass for ' + sprintId` with `'all five checks pass for ' + sprintId`. **CRITICAL:** the existing tests at lines 173 + 327 assert `'all four checks pass'` — those will break. The test file in this CR adds new scenarios; the **existing fixture-1 + scenario-8 `expect(cap.getOut()).toContain('all four checks pass')`** assertions must be updated to `'all five checks pass'`. Two callsites in `sprint-preflight.test.ts` (lines 173, 327, 348-349 already verified by `grep`) — Developer updates them in the same commit. NOT a behavior break in the production handler — just the literal count.
   - **Edit 4 (line 1057, `'X/Y checks failed'` literal):** unchanged — the count derives from `failures.length / results.length`, which now naturally reads `1/5` instead of `1/4`. Update the existing scenario-6 assertion at test file line 278 (`expect(errOut).toContain('2/4 checks failed')`) to `'2/5 checks failed'`. **Two literal-count test fixtures must be updated; flagged here so Developer doesn't miss them.**
   - **Net diff estimate sprint.ts:** ~120 LOC added (helpers + check entry); 1 LOC modified (success line); 0 LOC deleted.

2. **`cleargate-cli/test/commands/sprint-preflight.test.ts`** (live only; vitest, NOT node:test).
   - **Per the M5 dispatch note + CR-029 §0.6 + developer.md "Inner-loop test runner":** this file is EXISTING vitest-style. ADD scenarios in vitest-style. Do NOT codemod to `node:test`. CR-029's batch codemod handles migration in the SPRINT-20→21 gap.
   - **Net-new test file (none in this milestone):** no net-new file is created. All 6 scenarios live in `sprint-preflight.test.ts`.
   - **Existing-fixture updates (REQUIRED, Developer must not miss):**
     - Line 173: `'all four checks pass'` → `'all five checks pass'`.
     - Line 278: `'2/4 checks failed'` → `'2/5 checks failed'`.
     - Line 327: `'all four checks pass'` → `'all five checks pass'`.
     - The "STORY-026-01: skill load directive" scenarios at lines 346-369 do NOT need changes; they assert presence/absence of the skill directive, not the check count.
   - **6 NEW scenarios (CR-027 §3 verbatim, verified Gherkin):**
     - **Scenario 9 — v2 happy path with all items pass=true.** Build fixture with SPRINT-99, deliverables `## 1.` lists `EPIC-099` + `STORY-099-01`. Both files have `cached_gate_result.pass: true, last_gate_check: <now>` and `updated_at: <now-1m>`. Sprint plan itself has `cached_gate_result.pass: true`. `execution_mode: v2`. Expect exit 0, stdout contains `all five checks pass`, no `Per-item readiness gates:` line in stderr. Last stdout line is `→ Load skill: sprint-execution`.
     - **Scenario 10 — v2 hard-block on one EPIC pass=false.** Same fixture, mutate `EPIC-099` to `cached_gate_result.pass: false, failing_criteria: [{id: 'no-tbds', detail: '...'}]`. Expect exit 1, stderr contains `Per-item readiness gates: 1/3 items not ready` (3 = EPIC + STORY + SPRINT itself), stderr contains `EPIC-099` + `no-tbds`. Stdout does NOT contain `Load skill: sprint-execution`.
     - **Scenario 11 — staleness fails.** EPIC-099 has `cached_gate_result.pass: true, last_gate_check: 2026-01-01T00:00:00Z`, `updated_at: 2026-05-02T00:00:00Z`. Expect exit 1, stderr contains `EPIC-099` and the failure reason includes the word `stale`.
     - **Scenario 12 — v1 mode warns, exit 0.** Same as scenario 10 (one item pass=false) but sprint plan has `execution_mode: v1`. Expect exit 0 (other 4 checks pass; check #5 returns `skipped: true` with message `skipped (execution_mode: v1 — advisory only)`). Stderr is empty (skipped checks don't emit failures). Last stdout line is `→ Load skill: sprint-execution` because all 5 results are pass-or-skipped.
     - **Scenario 13 — Done items skipped from failure list.** Add `STORY-099-02` to deliverables with `status: Done` AND `cached_gate_result.pass: false`. Expect exit 0 (it's terminal — does not gate run). Verify stderr does NOT mention `STORY-099-02`. Stdout contains `all five checks pass`.
     - **Scenario 14 — sprint plan itself fails on missing `cached_gate_result`.** Sprint plan frontmatter has `cached_gate_result: null` (literal `null`, not absent — matches SPRINT-20's actual draft state per `grep cached_gate_result SPRINT-20*.md`). All children pass. Expect exit 1, stderr contains `SPRINT-99` (the sprint plan ID itself) in the failure list with reason `no cached_gate_result`. **This scenario directly exercises the SPRINT-20 anchor case** — the live SPRINT-20 plan today has `cached_gate_result.pass: null` per its frontmatter, so re-running preflight against SPRINT-20 itself post-merge would currently fail this check. Acceptable per CR-027 §0.5 Q2 (warn-only under v1) AND per the SDR §2.5 SOFT flag (backfill window via CR-028's M6 commit + a one-time gate-check refresh).
   - **Scenario placement:** all 6 scenarios use the same `buildCleanFixture` pattern + a NEW helper `seedReadinessFixture(dir, sprintId, items, sprintGateState)` that writes child work-item files + sprint plan with the requested `cached_gate_result` + `status` + `updated_at` shape. Helper is local to the test file (~40 LOC), not exported.
   - **Test scaffolding LOC estimate:** ~250 LOC of new test code (6 scenarios × ~30 LOC each + 40 LOC helper + 30 LOC fixture imports/cleanup); existing-fixture updates ~3 LOC.

3. **`.cleargate/knowledge/readiness-gates.md`** (live; canonical at `cleargate-planning/.cleargate/knowledge/readiness-gates.md`).
   - **Edit 1 (`epic.ready-for-decomposition`, line ~62 yaml block):** append to `criteria:` array:
     ```yaml
         - id: discovery-checked
           check: "frontmatter(.).context_source != null"
     ```
   - **Edit 2 (`epic.ready-for-coding`, line ~78 yaml block):** append the same `discovery-checked` entry. Per CR-027 §1: "added to all four enforcing types." Epic has TWO transitions (decomposition + coding) — both get the criterion. **Architect call:** add to BOTH epic gate entries since both are `severity: enforcing`.
   - **Edit 3 (`story.ready-for-execution`, line ~96 yaml block):** append `discovery-checked` entry.
   - **Edit 4 (`cr.ready-to-apply`, line ~112 yaml block):** append `discovery-checked` entry.
   - **Edit 5 (`bug.ready-for-fix`, line ~125 yaml block):** append `discovery-checked` entry.
   - **Edit 6 (NEW gate, append after the bug block at end-of-file):**
     ```yaml
     - work_item_type: sprint
       transition: ready-for-execution
       severity: enforcing
       criteria:
         - id: risk-table-populated
           check: "body contains '| Mitigation'"
         - id: discovery-checked
           check: "frontmatter(.).context_source != null"
     ```
     **Architect note:** add `discovery-checked` here too — sprints have a `context_source` field today (verified in SPRINT-20 frontmatter), and the parent §0.5 Q3 resolution is "ship the cheap form everywhere." This is consistent. Per CR-027 §1: "the new `sprint.ready-for-execution` gate also runs at preflight."
   - **Edit 7 (mirror):** `cp .cleargate/knowledge/readiness-gates.md cleargate-planning/.cleargate/knowledge/readiness-gates.md` after edits. **Pre-existing 2-line divergence at lines 68/70 will be reconciled by the byte-copy** — verify with the orchestrator before the commit lands; if intentional divergence, restore it post-edit. **Per the Architect SDR + flashcard `2026-05-01 #mirror #parity`: mirror parity is per-edit. Do NOT reconcile pre-existing divergence as a side-effect.** Therefore: after appending the new entries, MANUALLY copy ONLY the new `discovery-checked` entries + the new `sprint.ready-for-execution` block into the canonical mirror, preserving its current line 68/70 divergence (canonical reads `section(2)/section(4)`, live reads `section(3)/section(5)`). **Developer copies the diff hunks, not the whole file.**
   - **Net diff estimate readiness-gates.md:** +12 LOC live (5 × 2-line `discovery-checked` entries) + 8 LOC live (`sprint.ready-for-execution` block) = +20 LOC. Mirror gets the same +20 hunks.

4. **`cleargate-cli/src/lib/readiness-predicates.ts`** — **VERIFY NO EDITS**. Per CR-027 §3: "no changes expected. Both new predicates use existing shapes." Verified at SDR-time: shape #1 (`frontmatter(<ref>).<field> <op> <value>`) handles `frontmatter(.).context_source != null` — the parser at `readiness-predicates.ts:44` accepts this shape. Shape #2 (`body contains "<string>"`) handles `body contains '| Mitigation'` — verified at line 14 of the predicate vocabulary. **Developer asserts this by running `cleargate gate check` against a fixture with the new criteria and seeing the predicate evaluate without parse errors. NO source edit needed.** If the predicate parser throws, scope-bleed → halt and return BLOCKED.

5. **`.claude/skills/sprint-execution/SKILL.md`** (live; canonical at `cleargate-planning/.claude/skills/sprint-execution/SKILL.md`). **REQUIRED per SDR §2.5 Hard-flag RESOLVED-VIA-M5-PLAN.**
   - **Edit (live + canonical, byte-equal post-edit):** rewrite lines 116-128 (the "Four checks, all must pass:" block + the "On failure" block). Verified at SDR-time both files are byte-identical (`diff` returns empty); the edit lands in BOTH.
   - **Replacement text (verbatim — Developer copies into both files; preserves `### A.1` heading at line 110 and the `cleargate sprint preflight <sprint-id>` fenced block at lines 112-114):**
     ```
     Five checks, all must pass:

     1. Previous sprint `sprint_status: "Completed"` in `state.json`.
     2. No leftover worktrees — `git worktree list` must not contain `.worktrees/STORY-*`.
     3. Sprint branch ref free — `git show-ref refs/heads/sprint/S-NN` returns nothing.
     4. `main` clean — `git status --porcelain` empty.
     5. Per-item readiness gates pass — every work-item ID in §1 Consolidated Deliverables has fresh `cached_gate_result.pass: true` (or terminal status). Under `execution_mode: v2` a failing item hard-blocks; under `v1` it warns. Failure punch-list names each item + its failing criteria.

     On failure, surface the punch list verbatim and halt. Per-item resolution:

     - Prev sprint not closed → run sprint close on prev first.
     - Leftover worktree → `git worktree remove` if abandoned, otherwise merge.
     - Branch ref exists → investigate; force-deletion only with explicit human approval.
     - Dirty main → human commits/stashes/discards. **Never `git reset --hard` or stash without explicit human approval.**
     - Per-item gate fail → run `cleargate gate check <file> -v` for the named item; fix the failing criterion (e.g., populate `context_source`, resolve TBDs); re-run preflight.
     ```
   - **Net diff estimate skill:** ~10 LOC added (one new check + one new resolution bullet); 1 LOC modified ("Four" → "Five"). Mirror parity: byte-equal post-edit (diff returns empty).
   - **Prebuild:** `npm run prebuild` regenerates the cli-bundled mirror at `cleargate-cli/templates/cleargate-planning/.claude/skills/sprint-execution/SKILL.md` — verify that derived file also reflects the edit before commit. FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`.

6. **`CLAUDE.md`** (live, line 123) — replace "The four checks (previous sprint Completed, no leftover worktrees, `sprint/S-NN` ref free, `main` clean) must all pass." with:
   > The five checks (previous sprint Completed, no leftover worktrees, `sprint/S-NN` ref free, `main` clean, per-item readiness gates pass) must all pass.
   - **Net diff:** ~1 LOC modified.
   - **STORY-026-02 prune coordination:** the "Sprint Execution Gate" paragraph at live line 123 IS in the prune surface per SPRINT-20 §2.2 — STORY-026-02 (Wave 3) deletes it. The skill update at file 5 above is the surface that survives. CR-027's CLAUDE.md edit is therefore **temporary** — it ships correct content into Wave 2's CLAUDE.md (so anyone running preflight between Wave 2 and Wave 3 sees the right text) and then STORY-026-02 prunes the whole paragraph. **No conflict, no work-around** — the chain is: CR-027 lands "five checks" in CLAUDE.md AND skill (Wave 2) → STORY-026-02 prunes CLAUDE.md paragraph (Wave 3) → skill remains as the post-prune carrier.

7. **`cleargate-planning/CLAUDE.md`** (canonical, line 32) — same edit as file 6, byte-equal. The bounded `<!-- CLEARGATE:START -->...<!-- CLEARGATE:END -->` block contains line 32. Edit stays inside the block per SPRINT-20 §"Constraints."

8. **`.cleargate/knowledge/cleargate-protocol.md`** (live, line 104-106) — Gate 3 section. Replace lines 105-106:
   - Current:
     ```
     ### Gate 3 — Sprint Execution (per sprint, Prepare → Execute boundary)

     Before sprint execution begins, the environment is checked. See `cleargate-enforcement.md` §<N> for full enforcement spec; specified by CR-021.
     ```
   - Replacement (preserves the `cleargate-enforcement.md` cross-ref):
     ```
     ### Gate 3 — Sprint Execution (per sprint, Prepare → Execute boundary)

     Before sprint execution begins, `cleargate sprint preflight <sprint-id>` runs **five** checks: previous sprint Completed, no leftover worktrees, sprint branch ref free, `main` clean, and per-item readiness gates pass for every work item in §1 Consolidated Deliverables. Under `execution_mode: v2` a failing per-item gate hard-blocks; under `v1` it warns. See `cleargate-enforcement.md` §<N> for full enforcement spec; specified by CR-021 (env health) + CR-027 (composite per-item gate + Discovery/Risk criteria).
     ```
   - **Net diff:** -1 LOC, +1 LOC (one prose line replaces another).
   - **Section-numbering check:** `grep -c "^## [0-9]" .cleargate/knowledge/cleargate-protocol.md` count must be unchanged pre/post (this edit touches a `### Gate 3` subsection of `## 4`, not a `##` heading). FLASHCARD `2026-04-21 #protocol #section-numbering` — Architect verified no `##` adds; Developer confirms with the grep before commit.

9. **`cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`** — same edit, byte-equal. Live + canonical are confirmed mirror-parity at SDR-time (`diff` returns empty).

10. **`.cleargate/FLASHCARD.md`** — append ONE line on commit (newest on top per skill protocol):
    ```
    2026-05-MM · #preflight #gate3 #readiness · CR-027: sprint preflight gained check #5 (per-item cached_gate_result.pass=true + freshness; v2 hard-block, v1 warn). Pre-existing drafts with cached_gate_result.pass:null fail under v2 — backfill via `cleargate gate check <file>` or downgrade sprint to v1 for one cycle.
    ```
    Developer fills the actual MM on commit day. Per skill protocol: grep first to avoid dupes (no existing `#preflight` cards verified at SDR-time).

### Files to create
**None.** Zero net-new source files. Six new vitest scenarios live INSIDE the existing `sprint-preflight.test.ts`.

### Files explicitly NOT modified (carry-forward CR-027 §3 "no changes expected")
- `cleargate-cli/src/lib/readiness-predicates.ts` — both new predicates fit existing shapes.
- `.cleargate/scripts/init_sprint.mjs` — preflight is a separate command from init; init's decomposition gate stays as-is.
- `.cleargate/scripts/close_sprint.mjs` — close runs its own lifecycle reconciler; preflight is pre-Active, not pre-Close.
- `cleargate-cli/src/commands/gate.ts` — `cleargate gate check` semantics unchanged; CR-027 only adds new gate entries that the existing engine evaluates verbatim.
- All four agent files in `.claude/agents/` — agent contracts unchanged.
- `cleargate-planning/MANIFEST.json` — auto-regenerates via `npm run prebuild`. CR-027 doesn't add scaffold files; it edits existing readiness-gates + protocol + skill mirrors which all already have MANIFEST entries.

### Schema changes
**None.** No frontmatter shape mutation. The `sprint.ready-for-execution` gate is a NEW gate that reads existing fields (`context_source`, body text). No migration. `cached_gate_result` schema is byte-identical (`{pass, failing_criteria, last_gate_check}` per `frontmatter-cache.ts:17-21`).

### Test scenarios (Gherkin from CR-027 §4 + plan additions)

**Acceptance gate (per CR-027 §4 verbatim, mapped to vitest scenarios):**

| §4 Acceptance | M5 Vitest Scenario | Notes |
|---|---|---|
| §4.1 v2 happy path → exit 0, "all five checks pass" | Scenario 9 | Plus existing-fixture lines 173/327 update from "four" → "five" |
| §4.2 v2 hard-block on EPIC pass=false | Scenario 10 | Stderr names ID + criterion |
| §4.3 staleness | Scenario 11 | `last_gate_check < updated_at` |
| §4.4 v1 mode warns | Scenario 12 | Whole check skips under v1; exit 0 |
| §4.5 Done items skipped | Scenario 13 | `status: Done` not in failure list |
| §4.6 `discovery-checked` fires | (new gate-check unit test in `cleargate-cli/test/lib/readiness-predicates.test.ts` if CR-028 doesn't already cover; **DECISION: rely on CR-028's adjacent test additions** — CR-028 owns the `readiness-predicates.test.ts` file per SDR §2.2; CR-027 does NOT add scenarios there. CR-027's vitest scenarios 9-14 cover the COMPOSITE behavior end-to-end, which exercises `discovery-checked` evaluation transitively.) | Cross-CR awareness: if CR-028's M6 plan does NOT add a `discovery-checked` predicate test, file a follow-up note for SPRINT-21. |
| §4.7 `risk-table-populated` fires | Scenario 14 (sprint plan with no risk table fails check #5 with `risk-table-populated`) | Direct end-to-end coverage |
| §4.8 protocol + CLAUDE.md alignment | Manual review at QA gate | Not vitest-able |
| §4.9 scaffold mirror | `diff` assertions in QA | Not vitest-able |

**Pre-fix regression sentinel (DELIBERATELY NONE):** unlike CR-026, there is no archived fixture asserting the old behavior. The success-line literal change ("four" → "five") is the only behavior-visible regression of pre-existing tests; updating their string expectations IS the regression-handling.

**Smoke test (manual, post-merge, mandatory before QA approve):**
1. `cd cleargate-cli && npm run typecheck && npm test` — green.
2. `cd cleargate-cli && npm run prebuild` — regenerates mirror; verify `git status` shows only the expected updates under `cleargate-cli/templates/cleargate-planning/...` and a MANIFEST.json sha256 update.
3. `cleargate sprint preflight SPRINT-20` — run against the live SPRINT-20 plan in `pending-sync/`. **Expected:** check 5 will FAIL because SPRINT-20's own `cached_gate_result.pass: null` (verified at SDR-time, frontmatter line 36). Capture exit code + output; this is the first proof check #5 fires. To make preflight pass for SPRINT-20 itself, run `cleargate gate check .cleargate/delivery/pending-sync/SPRINT-20_*.md` first (rebakes `cached_gate_result`); if SPRINT-20's body satisfies the new `risk-table-populated` + `discovery-checked` (it does — `## 3. Risks & Dependencies` has `| Mitigation`, frontmatter has `context_source` populated), gate check returns pass=true and preflight subsequently exits 0.
4. `diff .cleargate/knowledge/readiness-gates.md cleargate-planning/.cleargate/knowledge/readiness-gates.md` — should show ONLY the pre-existing 2-line divergence at lines 68/70, plus zero new divergence in the appended sections. (Developer captures this diff in the dev report.)
5. `diff .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — empty.
6. `diff .claude/skills/sprint-execution/SKILL.md cleargate-planning/.claude/skills/sprint-execution/SKILL.md` — empty.

**Pre-commit gate:**
- `npm run typecheck` clean.
- `npm test` green for `cleargate-cli`. Existing fixtures updated; new fixtures pass.
- All four scaffold-mirror diffs as expected (per smoke step 4-6).
- One commit, conventional format `feat(CR-027): off-sprint — composite planning readiness at sprint preflight + discovery/risk criteria`. Per CLAUDE.md `feat(<epic>):` rule — EPIC-008 is parent, but CR-027 is being shipped as `off-sprint` (it's NOT a story under an epic; it's a CR with `parent_ref: EPIC-008`). The CR-026 plan precedent uses `feat(CR-026):` — Developer follows the same pattern.
- Never `--no-verify`.

**Post-commit:**
- Move `.cleargate/delivery/pending-sync/CR-027_*.md` to `.cleargate/delivery/archive/`.
- Append flashcard line.
- Wiki re-ingest happens automatically via PostToolUse hook.

### Reuse (no duplication)

- **`readSprintExecutionMode(sprintId, opts)`** from `cleargate-cli/src/commands/execution-mode.ts:118` — READ ONCE per preflight invocation; pass `mode` to `checkPerItemReadinessGates`. Already imported at sprint.ts:23-27. Do not re-implement.
- **`readCachedGate(absPath)`** from `cleargate-cli/src/lib/frontmatter-cache.ts:27` — async; returns `null` or `{pass, failing_criteria, last_gate_check}`. Use in `checkPerItemReadinessGates`. Do NOT re-parse YAML.
- **`parseFrontmatter(raw)`** from `cleargate-cli/src/wiki/parse-frontmatter.ts:16` — for reading `status` + `updated_at` adjacent to `cached_gate_result`. (Optionally read both via one parse call; cache the parsed object.)
- **`TERMINAL_STATUSES`** constant from `sprint.ts:38` — `Set(['Completed', 'Done', 'Abandoned', 'Closed', 'Resolved'])`. Reuse for the Done-skip rule.
- **`PreflightCheckResult` interface** from `sprint.ts:838-844` — `checkPerItemReadinessGates` returns this shape. No new types needed.
- **`SprintPreflightOptions.execFn` test seam** from `sprint.ts:860` — passes through to the new check for shell-out injection.
- **Fixture builders + helpers** from existing `sprint-preflight.test.ts` (`buildCleanFixture`, `commitFixtureFiles`, `runPreflight`, `makeCapture`, `makeExitSeam`) — extend with one local `seedReadinessFixture(dir, sprintId, items, sprintGateState)` helper. Do NOT export to other test files.
- **Path-a `--emit-json` shell-out**: `extractWorkItemIds` is the only exported function the sprint.ts handler depends on. Keep `findWorkItemFile` non-exported (sprint.ts re-implements its 14-line search inline, since `--emit-json` returns IDs only and sprint.ts already has cwd-relative path knowledge).

### Gotchas surfaced from code inspection

- **GOTCHA-1 — SPRINT-20 itself currently fails check #5 under v2.** Verified: SPRINT-20 frontmatter line 36 has `cached_gate_result.pass: null`, line 38 has `last_gate_check: null`. Running `cleargate sprint preflight SPRINT-20` post-merge with no remediation will return exit 1 with the sprint plan named in the failure list (scenario 14 directly mirrors this). **Resolution:** before any post-merge preflight, run `cleargate gate check .cleargate/delivery/pending-sync/SPRINT-20_*.md` to bake `cached_gate_result` against the new `sprint.ready-for-execution` gate. Body satisfies both criteria (verified: `## 3. Risks & Dependencies` table has `| Mitigation` header AND frontmatter `context_source` is populated). Smoke-step 3 above scripts the Developer through this.
- **GOTCHA-2 — readiness-gates mirror has 2-line pre-existing divergence (lines 68/70).** Verified at SDR-time. Live: `section(3)`, `section(5)`. Canonical: `section(2)`, `section(4)`. Cause unknown (pre-EPIC-024 likely). FLASHCARD `2026-05-01 #mirror #parity`: do NOT reconcile pre-existing divergence as a side effect. Developer applies the new entries to BOTH files separately, preserving the pre-existing divergence. Mirror diff post-commit will still show the 2-line divergence.
- **GOTCHA-3 — `assert_story_files.mjs` is invoked elsewhere via `run_script.sh`.** Verified at the top of the file: it has its own `main()` that `process.exit(2)` on usage / parse errors. Adding `--emit-json` must NOT regress the existing `node assert_story_files.mjs <sprint-file>` (no flag) call path — which init/close/preflight currently use for the EXISTING decomposition gate. The new flag is purely additive: when `--emit-json` is set, skip the existing missing/unapproved/empty triage and short-circuit to JSON. When absent, behavior is byte-identical to today.
- **GOTCHA-4 — `cached_gate_result` field can be the literal `null` string, not just an absent key.** Verified via SPRINT-20's frontmatter (line 36: `cached_gate_result:` followed by `pass: null` two lines down — it's a YAML mapping with null fields, not absent). `readCachedGate` from `frontmatter-cache.ts:27` calls `coerceCachedGate` which I have not read but per the interface returns `null` on a malformed/null mapping. **Developer verifies `readCachedGate` returns `null` for SPRINT-20's literal-null shape**; if it returns `{pass: null, ...}` instead, the failure-detection logic must check `result === null || result.pass !== true`. Treat `pass !== true` as failing (not just `pass === false`) — covers both literal null and absent.
- **GOTCHA-5 — `last_gate_check < updated_at` string-compare correctness.** ISO-8601 strings sort lexically the same as chronologically when both are in the same offset. SPRINT-20's `updated_at: "2026-05-02T13:30:00Z"` and `last_gate_check: null` — null < any string in JS is `false` (NaN-like); explicitly handle `last_gate_check === null` as ALSO stale (pass !== true is the dominant signal but null `last_gate_check` is itself a fail signal). Same handling: `if (cachedGate === null || cachedGate.pass !== true || cachedGate.last_gate_check < frontmatter.updated_at) → fail`.
- **GOTCHA-6 — `extractWorkItemIds` returns IDs from §1 `## 1. Consolidated Deliverables` only.** The regex includes `STORY-NNN-NN`, `CR-NNN`, `BUG-NNN`, `EPIC-NNN`, `HOTFIX-NNN`, `PROPOSAL-NNN`. **Sprint frontmatter `epics: ["EPIC-026"]` etc. is NOT scanned** — only the §1 table body. Verified by reading the helper at line 49-58: it splits on `^## ` and finds only the deliverables section. CR-027 inherits this: in-scope = body of §1. Acceptable.
- **GOTCHA-7 — `discoverSprintFile` in execution-mode.ts is private.** Verified at line 59. CR-027 must NOT export it from `execution-mode.ts` (cross-cutting change). Re-implement the 12-line search inline in `sprint.ts` as `findSprintFile(sprintId, cwd)`. This is per the CR-027 §3 reuse principle minus the cross-cutting cost.
- **GOTCHA-8 — vitest pool-cap.** Per FLASHCARD `2026-05-02 #vitest #ram #parallel-agents`: maxForks=2 in `vitest.config.ts`. CR-027's new scenarios fit within the budget; Wave 2 is 2 agents (CR-027 + CR-028) → 4 forks ≈ 1.6GB. Within cap. Developer does NOT need to override `VITEST_MAX_FORKS`.
- **GOTCHA-9 — `frontmatter-cache.ts:readCachedGate` is async; `sprintPreflightHandler` is sync.** sprint.ts:1076 declares `function sprintPreflightHandler(...): void`. The existing 4 checks are all sync. **Two options:** (a) make `checkPerItemReadinessGates` sync by reading + parsing frontmatter manually (re-implement the 30 LOC of `readCachedGate` inline as sync `readFileSync` + manual extraction of the `cached_gate_result:` block). (b) Refactor `sprintPreflightHandler` to async. **Decision: option (a) — sync inline parse.** Refactoring the handler to async would cascade into all 8 existing test scenarios needing `await` and the CLI wiring. Add a private sync helper `readCachedGateSync(absPath): {pass, failing_criteria, last_gate_check} | null` that uses `fs.readFileSync` + the same line-scan logic as `frontmatter-cache.ts:readFrontmatterFromFile` (pattern at `readiness-predicates.ts:293-324`). ~25 LOC. Document the duplication with a `// CR-027: sync mirror of frontmatter-cache.ts:readCachedGate; sprint preflight is sync. Refactor to async if more checks need async.` comment.

## Cross-story risks (M5-specific + Wave-2 / Wave-3 hand-offs)

- **Risk-1 — CR-028 parallel edit on `readiness-gates.md`.** SDR §2.2 row 2: CR-027 adds `discovery-checked` to 5 enforcing entries + new `sprint.ready-for-execution`. CR-028 adds `reuse-audit-recorded` (3 entries) + `simplest-form-justified` (2 entries). Different criterion IDs, different positions in the same yaml `criteria:` arrays. Rebase order: CR-027 first per SDR §2.2; CR-028 rebases. **Mitigation:** Developer for CR-027 lands FIRST (Wave 2 dispatch order); CR-028's Developer rebases on a clean main. If CR-028 already merged when CR-027 lands (parallel race), the `criteria:` arrays still merge cleanly because both append to the same array — git's 3-way merge handles ordered-list appends without conflict markers as long as the surrounding context lines are stable.
- **Risk-2 — STORY-026-02 prune deletes the CLAUDE.md "five checks" content (Wave 3).** Mitigation already wired: the skill update at file 5 carries the "Five checks" content forward. STORY-026-02's prune deletes the CLAUDE.md paragraph; the skill is the post-prune authoritative carrier. Verified at SDR-time the skill's `### A.1` exists and is byte-equal in canonical. The chain works regardless of CR-027 / STORY-026-02 sub-sequence — the skill update happens INSIDE CR-027 (Wave 2), STORY-026-02 prunes CLAUDE.md AFTER (Wave 3).
- **Risk-3 — SPRINT-20's own preflight hits check #5 immediately post-merge.** GOTCHA-1 above. **Mitigation:** post-merge orchestrator runs `cleargate gate check` against ALL six SPRINT-20 anchor files (per CR-028's M6 backfill commit, which adds `## Existing Surfaces` + `## Why not simpler?` sections those criteria need) before any subsequent `cleargate sprint preflight`. **CR-027's M5 commit itself does NOT need to remediate SPRINT-20's frontmatter** — that's CR-028's M6 backfill commit's job (per SDR §2.5 SOFT flag "resolved-by-backfill-commit"). **However, IF the orchestrator runs preflight against SPRINT-20 between CR-027 merging and CR-028 merging, it will fail** with the sprint plan's own `cached_gate_result.pass: null`. **Resolution:** orchestrator runs preflight ONLY after both CR-027 and CR-028 fully merge AND `cleargate gate check` runs across SPRINT-20 anchors. Surface this in the Wave-2 → Wave-3 hand-off note.
- **Risk-4 — `cached_gate_result.pass: null` in SPRINT-20 anchor files.** Verified: EPIC-026 + STORY-026-01 + STORY-026-02 + CR-026 + BUG-025 frontmatters likely have `pass: null` (matching SPRINT-20's pattern). Developer for CR-027 should confirm this with `grep -A1 "cached_gate_result:" .cleargate/delivery/pending-sync/{EPIC-026,STORY-026-0?,CR-026,BUG-025}_*.md`. If any have `pass: true`, scenario 14's claim that the sprint plan IS one of the failures becomes weaker (other anchors will fail FIRST). The scenario still passes — it asserts SPRINT-NN appears in the failure list, not that it's the only one — but the message order may include other IDs.
- **Risk-5 — Mirror parity per-edit for readiness-gates.md.** GOTCHA-2 above. Pre-existing 2-line divergence MUST be preserved. Developer applies new entries to live + canonical separately (NOT byte-copy of full file).
- **Risk-6 — sync vs async readCachedGate.** GOTCHA-9. The 25-LOC sync mirror is duplication; the alternative (refactor handler to async) is 80+ LOC across 8 test scenarios. **Decision: sync inline.** Document with a comment cross-ref to the async original.

## Risks (M5-specific summary table)

| Risk | Mitigation |
|---|---|
| Sync handler vs async `readCachedGate` | Inline 25-LOC sync mirror with comment cross-ref to `frontmatter-cache.ts:27` (GOTCHA-9). Defer async refactor. |
| `cached_gate_result.pass: null` in SPRINT-20 anchors blocks own preflight post-merge | CR-028's M6 backfill commit + a one-time `cleargate gate check` pass across SPRINT-20 anchors before re-running preflight. Documented in smoke step 3. |
| Existing test fixtures assert "four checks" / "2/4" literals | Update 3 fixtures (lines 173, 278, 327) in same commit. Listed verbatim in plan §1 file-2. |
| `--emit-json` flag breaks existing `assert_story_files.mjs` callers | New flag is purely additive; absent flag = byte-identical behavior. Existing init/close/preflight decomposition-gate callers pass no flag. |
| Mirror parity for readiness-gates.md (pre-existing 2-line divergence) | Apply hunks separately to live + canonical; do NOT byte-copy. Smoke step 4 verifies mirror diff is exactly the pre-existing 2 lines + zero new divergence. |
| `parent_ref: EPIC-008` is `Abandoned` per CR-027 frontmatter (line 4) | Acceptable per CR-027 §2 "EPIC-008's status is Abandoned per archive frontmatter; this CR follows the same pattern as CR-008 (reactivate a portion of EPIC-008's intent)." No frontmatter mutation. |
| CR-028 race on `readiness-gates.md` | CR-027 merges first per SDR §2.2; ordered yaml `criteria:` appends rebase cleanly. If CR-028 lands first, CR-027 rebases — same outcome, different sequence. |
| `discovery-checked` predicate parse | No predicate engine edit needed; shape #1 handles `frontmatter(.).context_source != null`. Verified `readiness-predicates.ts:44`. |

## Open decisions for orchestrator (out of Architect scope)

- **None blocking.** All §0.5 questions resolved in CR-027 itself (one CR; mirror CR-014 severity; cheap predicate; sprint plan only; wait + measure). SDR §2.5 Hard-flag RESOLVED-VIA-M5-PLAN — this plan adds the skill files. SDR §2.3 NEW-warning on `assert_story_files.mjs` — this plan picks path (a). SDR §2.5 SOFT flag on backfill — owned by CR-028's M6 plan.
- **For the Wave-2 → Wave-3 hand-off:** the orchestrator MUST run `cleargate gate check` across all SPRINT-20 anchor files (sprint plan + 6 children) AFTER CR-027 + CR-028 merge but BEFORE any subsequent `cleargate sprint preflight`. Otherwise SPRINT-20's own `cached_gate_result.pass: null` will trip check #5. Capture in the Wave-2 close note.
- **For the QA agent (post-Developer):** verify the four mirror diffs (smoke steps 4-6) PLUS run `cleargate sprint preflight SPRINT-20` against a fresh checkout to confirm check #5 fires correctly when `cached_gate_result` is null and passes when remediated.

## Dispatch precondition (orchestrator MUST verify before spawning Developer)

1. `git log main --oneline | head -5` shows STORY-026-01 + CR-026 + BUG-025 commits all merged.
2. `ls -la .claude/skills/sprint-execution/SKILL.md cleargate-planning/.claude/skills/sprint-execution/SKILL.md` — both exist (verified at SDR-time).
3. `diff .claude/skills/sprint-execution/SKILL.md cleargate-planning/.claude/skills/sprint-execution/SKILL.md` returns empty (verified at SDR-time).
4. `grep -c "Four checks, all must pass:" .claude/skills/sprint-execution/SKILL.md` returns 1 (i.e., the OLD text is still in place — CR-027 will replace it; verified at SDR-time).
5. `grep -c "The four checks" CLAUDE.md cleargate-planning/CLAUDE.md` returns 1 each (the OLD text in both files — CR-027 will replace; verified at SDR-time).
6. `grep -c "Before sprint execution begins, the environment is checked" .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` returns 1 each (verified at SDR-time).
7. `npm run prebuild` in `cleargate-cli/` runs clean (no drift errors). FLASHCARD `2026-05-01 #manifest #prebuild`.
8. SPRINT-20 sentinel `.cleargate/sprint-runs/.active` reads `SPRINT-20`.
9. Vitest pool-cap budget: Wave 2 = 2 agents × maxForks=2 = 4 forks ≈ 1.6GB. Within budget; no `VITEST_MAX_FORKS` override needed.

## Flashcards to record on completion (Developer's responsibility post-merge)

After CR-027 merges:
- `2026-05-MM · #preflight #gate3 #readiness · CR-027: cleargate sprint preflight gained check #5 (per-item cached_gate_result.pass=true + freshness; v2 hard-block, v1 warn). Pre-existing drafts with cached_gate_result.pass:null fail under v2 — backfill via cleargate gate check or downgrade to v1 for one cycle.`
- `2026-05-MM · #frontmatter #cached-gate · readCachedGate is async; sprint preflight is sync — CR-027 inlined a 25-LOC sync mirror. Future async refactor: handler → async cascades into 8 vitest scenarios.`
- `2026-05-MM · #scripts #shell-out · assert_story_files.mjs gained --emit-json flag (CR-027 path-a). Wraps the existing extractWorkItemIds export. sprint.ts shells out via execFn; tests inject canned JSON via execFn seam.`

(Per Skill(flashcard) protocol: grep first to avoid dupes; newest on top.)

### M6.md

role: architect

# Milestone M6 — CR-028: Code-Truth Triage (Reuse + Right-Size + Justify-Complexity)

## Stories: CR-028
## Wave: W2 (parallel with CR-027; serialized AFTER Wave 1 fully merges)

## Sprint-goal tie
CR-028 codifies the **code-truth triage principle** (L0 code-as-canonical / L1 reuse-before-rebuild / L2 right-size-at-triage / L3 justify-complexity) as protocol + template + predicate edits — closing the planning-quality bar's other half so a fresh Epic / Story / CR cannot reach 🟢 without citing existing surfaces (`reuse-audit-recorded`) and answering why a smaller form won't work (`simplest-form-justified`); paired with CR-027's per-item gate, SPRINT-20 leaves the framework with a triage layer that prevents both planning-artifact duplication and code-surface duplication.

## Order

- **Pre-dispatch precondition (Wave-1 → Wave-2 hand-off, MUST verify before launching this Developer):**
  1. STORY-026-01 + CR-026 + BUG-025 all merged to `main` per SDR §2.1 wave gate. CR-028 makes no edits to surfaces touched in Wave 1, but Wave-2 ordering is held to avoid CLAUDE.md churn during Wave 1 development.
  2. `npm run prebuild` ran clean post-Wave-1 (regenerates `cleargate-cli/templates/cleargate-planning/...` + MANIFEST sha256s). FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`.
  3. The pre-existing readiness-gates mirror divergence at lines 68/70 (live `section(3)/section(5)` vs canonical `section(2)/section(4)` — verified at SDR-time, unchanged) is NOT to be reconciled by this CR. FLASHCARD `2026-05-01 #mirror #parity` (per-edit, not whole-file).
  4. `cleargate-planning/.cleargate/templates/{story,epic,CR}.md` exist and are byte-equal to live (verified at M6-plan-time: `diff` returns empty for all three pairs).
- **In-CR ordering (single Developer commit):** all 13 mirrored doc files + 4 fixture files + 1 test file land in ONE commit, conventional format `feat(CR-028): off-sprint — code-truth triage (reuse + right-size + justify-complexity)` per CR-028 §4 "Pre-commit." Splitting commits would leave protocol + CLAUDE.md preamble in place but the templates + predicates not yet emitting the new sections, or vice versa — the prose layer and the predicate layer are coupled by §4 verification protocol acceptance #3 (templates expose sections) + #4 (gate fires).
- **Mirror parity per-edit, not whole-file** (FLASHCARD `2026-05-01 #mirror #parity`). Edit live → `cp` to canonical for each of the 6 byte-equal pairs (3 templates + 1 protocol + 1 readiness-gates + 1 CLAUDE-block). Do NOT reconcile pre-existing divergence (readiness-gates lines 68/70).
- **Parallelization with CR-027:** SAFE per SDR §2.2 — CR-027 edits Gate 3 region of protocol + readiness-gates entries 2-5 + new `sprint.ready-for-execution` gate; CR-028 inserts pre-§0 unnumbered preamble + adds `reuse-audit-recorded` / `simplest-form-justified` to different criteria slots. Disjoint regions on protocol (CR-027 = `### Gate 3` subsection of `## 4`; CR-028 = pre-§0 preamble between line 5 separator and line 7 `## 0` heading). Disjoint criterion IDs on readiness-gates (CR-027 = `discovery-checked` + new sprint gate; CR-028 = `reuse-audit-recorded` + `simplest-form-justified`). Disjoint regions on CLAUDE.md (CR-027 = "Sprint Execution Gate" paragraph at live L123; CR-028 = new line between "Triage first" L109 and "Duplicate check" L111 + extension to L111). CR-027 lands first per SDR §2.2; CR-028 rebases.

## Protocol §0 insertion semantics — RESOLVED PER SDR §2.5 HARD FLAG

**Decision: PATH (b) — unnumbered preamble insertion. NOT path (a) — no §-renumbering.**

**Verbatim insertion target (verified at M6-plan-time):**
- Live: `.cleargate/knowledge/cleargate-protocol.md` line 5 = `---`, line 6 = blank, line 7 = `## 0. The Five Phases`. **Insert the new preamble between line 5 (`---`) and line 7 (`## 0`), preserving the blank separator on line 6 above the new heading and adding a blank + `---` separator below it before `## 0`.**
- Canonical: `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` byte-equal to live (verified `diff` empty at M6-plan-time). Same insertion at the same line offset.

**Verbatim section heading (Developer copies into both files):**

```markdown
## Code-Truth Principle

ClearGate's epistemic stack: source code is canonical; wiki, memory, and `context_source` are derived caches. Every triage and draft answers four layers, in order:

- **L0 — Code is source of truth.** On any conflict between cache and code, the code wins; the cache rebuilds. Verify capability claims by grep before stating them.
- **L1 — Reuse before rebuild.** Before drafting a work item that names an integration, feature, or capability, grep the source tree for existing implementations. Cite findings in `## Existing Surfaces`. If an existing surface covers ≥80% of the requirement, the work item is an extension (CR), not a new build (Story).
- **L2 — Right-size at triage.** Could this be a config change? a parameter addition? a one-line edit? If yes, it's not a Story — it's a PR or a tiny CR. Smallest viable form first.
- **L3 — Justify complexity.** Every Epic and Story includes `## Why not simpler?` answering: what's the smallest existing surface; why isn't extension/parameterization/config sufficient.

This is the same epistemic discipline the Knowledge Wiki applies to documents (raw → wiki → schema; source → cache → derived view), extended from planning artifacts to source code.

---
```

**Numbering-stability rule.** This block is **unnumbered** — it is *not* `## 0` and does *not* renumber the existing 16 numbered top-level headings. Verified at M6-plan-time by `grep -nE "^## [0-9]" cleargate-protocol.md`: the corpus has 16 entries — `## 0. The Five Phases` (line 7), `## 1. Your Role` (23), `## 2. The Front Gate (Triage)` (36), `## 3. Document Hierarchy` (74), `## 4. The Four Gates` (85), `## 5. Delivery Workflow` (119), `## 6. MCP Tools Reference` (142), `## 7. Scope Discipline` (154), `## 8. Planning Phase` (165), `## 9. Quick Decision Reference` (178), `## 10. Knowledge Wiki Protocol` (202), `## 11. Document Metadata Lifecycle` (378), `## 12. Token Cost Stamping` (472), `## 13. Scaffold Manifest` (504), `## 14. Multi-Participant Sync` (549), `## 21. Status Vocabulary` (605). Post-edit grep MUST also return 16 entries with the same heading texts (line numbers shift because the new preamble adds ~14 lines). 161 numeric `protocol §N` references in the live tree (per SDR §2.5) remain stable.

**Verification assertion (Developer runs pre-commit, in BOTH live + canonical):**

```bash
# Heading count must be exactly 16 in BOTH files post-edit:
[ "$(grep -cE '^## [0-9]' .cleargate/knowledge/cleargate-protocol.md)" = "16" ] || { echo "FAIL: live protocol §-numbering changed"; exit 1; }
[ "$(grep -cE '^## [0-9]' cleargate-planning/.cleargate/knowledge/cleargate-protocol.md)" = "16" ] || { echo "FAIL: canonical protocol §-numbering changed"; exit 1; }
# Heading texts must be unchanged in BOTH files post-edit (sorted comparison; preserves order):
diff <(grep -E '^## [0-9]' .cleargate/knowledge/cleargate-protocol.md) <(grep -E '^## [0-9]' cleargate-planning/.cleargate/knowledge/cleargate-protocol.md) || { echo "FAIL: numbered headings diverged between live and canonical"; exit 1; }
# Preamble heading must exist exactly once per file:
[ "$(grep -c '^## Code-Truth Principle' .cleargate/knowledge/cleargate-protocol.md)" = "1" ] || { echo "FAIL: preamble heading missing or duplicated in live"; exit 1; }
[ "$(grep -c '^## Code-Truth Principle' cleargate-planning/.cleargate/knowledge/cleargate-protocol.md)" = "1" ] || { echo "FAIL: preamble heading missing or duplicated in canonical"; exit 1; }
# Existing §0 must STILL be at heading-text "## 0. The Five Phases" (no renumber):
grep -q '^## 0. The Five Phases$' .cleargate/knowledge/cleargate-protocol.md || { echo "FAIL: §0 heading lost"; exit 1; }
```

If any of these asserts fail, the Developer halts and returns BLOCKED — the §-numbering corpus would otherwise cascade-break 161 ref sites.

## Per-story blueprint — CR-028

### Files to modify (13 doc files + 1 test file = 14 files; mirrors counted as separate files per CR-028 §3 file-count summary)

#### Layer 1 — Protocol (2 files, byte-equal mirror)

1. **`.cleargate/knowledge/cleargate-protocol.md`** (live).
   - **Edit (insertion at line 6, before `## 0. The Five Phases` at line 7):** insert the verbatim "Code-Truth Principle" preamble block from the §"Protocol §0 insertion semantics" section above. The preamble lands between the existing `---` separator (line 5) and `## 0. The Five Phases` (now line 21 post-insert). ~14 LOC inserted.
   - **Net diff:** +14 LOC; 0 LOC modified; 0 LOC deleted.
   - **Section-numbering check:** the 5-line verification assertion above MUST pass.

2. **`cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`** (canonical) — byte-equal mirror.
   - **Pre-edit verification:** `diff .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` returns empty (verified at M6-plan-time).
   - **Edit:** identical insertion at the same line offset.
   - **Post-edit:** `diff` MUST return empty.

#### Layer 2 — CLAUDE.md bounded block (2 files, in-block edits)

3. **`CLAUDE.md`** (live, line 109-111 region — INSIDE the bounded `<!-- CLEARGATE:START -->...<!-- CLEARGATE:END -->` block at L98-L159 verified at M6-plan-time).
   - **Edit 1 (new line inserted between L109 "Triage first" and L111 "Duplicate check"):** insert one new bullet (between L109 and L110 blank line / before L111):
     ```
     **Codebase is source of truth.** Wiki, memory, and `context_source` are derived caches. On conflict between cache and code, the code wins; the cache rebuilds. Before stating that a capability exists or doesn't exist, grep the code.
     ```
     Net: +1 paragraph (~3 lines including blank-line padding before/after).
   - **Edit 2 (extend the existing "Duplicate check" paragraph at L111):** append one sentence to the end of L111's paragraph (still INSIDE the bounded block):
     ```
     If the request names an integration, feature, or capability, also grep the source tree for existing implementations and cite findings in `## Existing Surfaces`.
     ```
     Net: +1 sentence appended to L111.
   - **Net diff CLAUDE.md (live):** +4 LOC inserted (1 paragraph + blank-line padding) + 1 LOC modified (L111 extended). 0 LOC deleted.
   - **STORY-026-02 prune coordination (Wave 3):** the new "Codebase is source of truth" bullet is NOT in the prune surface — it's a triage-rule, always-on. STORY-026-02's prune target is the four-agent-loop / Sprint-Execution-Gate / Architect-runs-twice prose. The "Triage first" / "Duplicate check" / "Codebase is source of truth" stack stays as the post-prune triage anchor. Verified at SDR-time (sprint plan §2.2 row 1: "CR-028's 'Codebase is source of truth' insertion lands OUTSIDE the prune surface and survives").

4. **`cleargate-planning/CLAUDE.md`** (canonical, line 18-20 region — INSIDE the bounded block at L7-L68 verified at M6-plan-time).
   - **Edit 1 (new line inserted between L18 "Triage first" and L20 "Duplicate check"):** byte-equal to live edit 1 above (the bounded block is the parity surface; outside-block content diverges — verified at M6-plan-time the live CLAUDE.md has 91 lines of meta-repo header above the bounded block that canonical doesn't have, and that's PRE-EXISTING divergence per FLASHCARD `2026-05-01 #mirror #parity`).
   - **Edit 2 (extend canonical L20 "Duplicate check" paragraph):** byte-equal to live edit 2 above.
   - **Mirror parity assertion (post-edit):** the BOUNDED BLOCK ONLY must be byte-equal between live and canonical. Pre-existing outside-block divergence (live L1-L97 above the block) is preserved. Developer runs:
     ```bash
     # Extract bounded block from each file and compare:
     awk '/<!-- CLEARGATE:START -->/,/<!-- CLEARGATE:END -->/' CLAUDE.md > /tmp/cg_live_block.txt
     awk '/<!-- CLEARGATE:START -->/,/<!-- CLEARGATE:END -->/' cleargate-planning/CLAUDE.md > /tmp/cg_canonical_block.txt
     diff /tmp/cg_live_block.txt /tmp/cg_canonical_block.txt || { echo "FAIL: bounded block diverged"; exit 1; }
     ```

#### Layer 3 — Templates (6 files = 3 templates × 2 mirrors)

5. **`.cleargate/templates/story.md`** (live).
   - **Edit 1 (extend Granularity Rubric at line 18-27):** add a new bullet at the TOP of the rubric list (currently line 19 starts with `A candidate story is too big...`). Insert immediately AFTER line 18's heading:
     ```
     A candidate story is **too small** — file a CR or submit the PR directly — if ANY signal trips:
       • Could this be a config change, a parameter addition, or a one-line edit? If yes, this is not a Story.
       • The change touches one already-existing function/class and the diff fits in <30 LOC.
       • The work amounts to renaming, formatting, or moving without behavior change.
     ```
     Followed by the existing "A candidate story is **too big**" block. Net: +5 LOC inserted before line 19.
   - **Edit 2 (add new section templates after §1.5 Risks, before §2 The Truth at line 122):** insert two new sections between `### 1.5 Risks` (line 115-120) and `## 2. The Truth (Executable Tests)` (line 122):
     ```markdown
     ### 1.6 Existing Surfaces

     > L1 reuse audit. List source-tree implementations the request could extend. Cite file:line.

     - **Surface:** `path/to/file.ext:NN` — {what it does}
     - **Coverage of this requirement:** {≥80% / partial / none — and why}

     ### 1.7 Why not simpler?

     > L2 / L3 right-size + justify-complexity. Answer both.

     - **Smallest existing surface that could carry this:** {citation or "none — net-new abstraction required"}
     - **Why isn't extension / parameterization / config sufficient?** {one paragraph}
     ```
     Net: +14 LOC inserted before line 122 (now lines 122-135 post-insert).
   - **Edit 3 (extend the bottom-of-doc Ambiguity Gate at line 184-187):** add two checkbox lines to the requirements list:
     ```
     - [ ] §1.6 Existing Surfaces cites at least one source-tree path or explicitly states "none — net-new."
     - [ ] §1.7 Why not simpler? has both sub-bullets answered (no "TBD" / no "{}").
     ```
     Net: +2 LOC inserted at the end of the requirements list (before file EOF).
   - **Net diff story.md (live):** +21 LOC inserted; 0 LOC modified; 0 LOC deleted.

6. **`cleargate-planning/.cleargate/templates/story.md`** (canonical) — byte-equal mirror.
   - **Pre-edit verification:** `diff .cleargate/templates/story.md cleargate-planning/.cleargate/templates/story.md` returns empty (verified at M6-plan-time).
   - **Edit:** identical to file 5.
   - **Post-edit:** `diff` MUST return empty.

7. **`.cleargate/templates/epic.md`** (live).
   - **Edit 1 (add new section templates after §3 The Reality Check at line 109, before §4 Technical Grounding at line 110):** insert two new sections between line 109 (end of §3 table) and line 110 (`## 4. Technical Grounding`):
     ```markdown
     ## 3.5 Existing Surfaces

     > L1 reuse audit. List source-tree implementations the epic could extend. Cite file:line.

     - **Surface:** `path/to/file.ext:NN` — {what it does}
     - **Coverage of this epic's scope:** {≥80% extension / partial / none — and why}

     ## 3.6 Why not simpler?

     > L2 / L3 right-size + justify-complexity. Answer both.

     - **Smallest existing surface that could carry this epic:** {citation or "none — net-new abstraction required"}
     - **Why isn't extension / parameterization / config sufficient?** {one paragraph}
     ```
     Net: +14 LOC inserted before line 110.
   - **Edit 2 (extend bottom-of-doc Ambiguity Gate at line 140-145):** add two checkbox lines to the requirements list:
     ```
     - [ ] §3.5 Existing Surfaces cites at least one source-tree path or explicitly states "none — net-new."
     - [ ] §3.6 Why not simpler? has both sub-bullets answered.
     ```
     Net: +2 LOC.
   - **Edit 3 (extend §0 instructions at line 1-29):** modify the `<instructions>` block POST-WRITE BRIEF section (lines 15-26) to mention the two new sections in the Brief extraction map, e.g. add to the bulleted extraction list at line 19-23:
     ```
       - Existing Surfaces ← §3.5 Existing Surfaces
       - Why not simpler? ← §3.6 Why not simpler?
     ```
     Net: +2 LOC inserted in the instructions block.
   - **Net diff epic.md (live):** +18 LOC inserted; 0 LOC modified; 0 LOC deleted.

8. **`cleargate-planning/.cleargate/templates/epic.md`** (canonical) — byte-equal mirror.
   - **Pre-edit verification:** `diff` returns empty.
   - **Edit:** identical to file 7.
   - **Post-edit:** `diff` MUST return empty.

9. **`.cleargate/templates/CR.md`** (live).
   - **Edit 1 (add new section after §2 Blast Radius at line 86, before §3 Execution Sandbox at line 88):** insert one new section:
     ```markdown
     ## 2.5 Existing Surfaces

     > L1 reuse audit. List source-tree implementations this CR extends or modifies. Cite file:line.

     - **Surface:** `path/to/file.ext:NN` — {what it does today}
     - **Why this CR extends rather than rebuilds:** {one paragraph — required when the change names a capability that already exists somewhere in the tree}
     ```
     Net: +7 LOC inserted before line 88.
   - **Edit 2 (extend bottom-of-doc Ambiguity Gate at line 104-109):** add one checkbox line:
     ```
     - [ ] §2.5 Existing Surfaces cites at least one source-tree path the CR extends.
     ```
     Net: +1 LOC.
   - **Note (CR-028 §3 explicit):** the CR template gets `## Existing Surfaces` ONLY (no `## Why not simpler?`) — per CR-028 §3 line 151: "CR template already implies 'Why not simpler?' through §1 Old vs New, so no separate section needed there." Bug template is also untouched (per §0.5 Q2 + readiness-gates §3 surface list — bugs are exempted from BOTH new criteria). Per the gate definition list in CR-028 §3, `reuse-audit-recorded` is on `cr.ready-to-apply` but `simplest-form-justified` is NOT.
   - **Net diff CR.md (live):** +8 LOC inserted; 0 LOC modified; 0 LOC deleted.

10. **`cleargate-planning/.cleargate/templates/CR.md`** (canonical) — byte-equal mirror.
    - **Pre-edit verification:** `diff` returns empty.
    - **Edit:** identical to file 9.
    - **Post-edit:** `diff` MUST return empty.

#### Layer 4 — Readiness gates (2 files)

11. **`.cleargate/knowledge/readiness-gates.md`** (live).
    - **Edit 1 (`epic.ready-for-decomposition`, yaml block at lines 58-73):** append two entries to the `criteria:` array:
      ```yaml
          - id: reuse-audit-recorded
            check: "body contains '## Existing Surfaces'"
          - id: simplest-form-justified
            check: "body contains '## Why not simpler?'"
      ```
      Net: +4 LOC at line 73 (end of array).
    - **Edit 2 (`epic.ready-for-coding`, yaml block at lines 75-90):** append the SAME two entries to the `criteria:` array. Per CR-028 §3 line 158: applies to `epic.ready-for-decomposition`. **Architect call (extending §3 read):** Epic has TWO transitions; CR-027's M5 plan added `discovery-checked` to BOTH for consistency. **For CR-028: the spec text in §1 line 117 says "epic.ready-for-decomposition, story.ready-for-execution, cr.ready-to-apply"** — only the decomposition transition for Epics. **Decision: follow the spec literally — apply ONLY to `epic.ready-for-decomposition`, NOT to `epic.ready-for-coding`.** Rationale: the second-transition gate is "epic ready to start coding stories" (post-decomposition); the reuse-audit and right-size questions are decomposition-time questions, already answered by the time the epic transitions to ready-for-coding. **Skip Edit 2.** Verbatim spec line: `- reuse-audit-recorded on epic.ready-for-decomposition, story.ready-for-execution, cr.ready-to-apply` and `- simplest-form-justified on epic.ready-for-decomposition and story.ready-for-execution`.
    - **Edit 3 (`story.ready-for-execution`, yaml block at lines 92-107):** append two entries:
      ```yaml
          - id: reuse-audit-recorded
            check: "body contains '## Existing Surfaces'"
          - id: simplest-form-justified
            check: "body contains '## Why not simpler?'"
      ```
      Net: +4 LOC at line 107.
    - **Edit 4 (`cr.ready-to-apply`, yaml block at lines 109-120):** append ONE entry:
      ```yaml
          - id: reuse-audit-recorded
            check: "body contains '## Existing Surfaces'"
      ```
      Net: +2 LOC at line 120. (CR scope per §1 line 117 — only `reuse-audit-recorded`, NOT `simplest-form-justified`.)
    - **Edit 5 (NO change to `bug.ready-for-fix` block at lines 122-133):** Bugs are exempted from BOTH new criteria (per CR-028 §0.5 Q2 + §1 line 119). Verify the block is unchanged post-edit.
    - **Net diff readiness-gates.md (live):** +10 LOC inserted (+4 epic + +4 story + +2 cr); 0 LOC modified; 0 LOC deleted.

12. **`cleargate-planning/.cleargate/knowledge/readiness-gates.md`** (canonical mirror).
    - **Pre-existing divergence at lines 68/70 (verified at M6-plan-time):** live reads `section(3)/section(5)`; canonical reads `section(2)/section(4)`. Cause unknown (pre-EPIC-024 likely). FLASHCARD `2026-05-01 #mirror #parity` — DO NOT reconcile as a side effect. Apply the new yaml hunks to BOTH files separately, preserving the pre-existing 2-line divergence.
    - **Edit:** apply the same Edit 1 + Edit 3 + Edit 4 hunks (skip Edit 2 per the architect-call rationale above) to canonical.
    - **Post-edit assertion:** `diff` between live and canonical post-edit should show ONLY the pre-existing 2-line divergence at the `affected-files-declared` / `scope-in-populated` lines (now at different line numbers post-insert). Developer captures the post-edit `diff` output and verifies it contains exactly 4 lines of context (2 changed lines × 2 sides) plus the diff hunk markers — same shape as the pre-edit divergence.
    - **No predicate engine changes** (per CR-028 §3 line 121): both new criteria use shape #2 (`body contains "<string>"`) from the closed-set vocabulary at `readiness-gates.md` line 14. Verified at M6-plan-time: `cleargate-cli/src/lib/readiness-predicates.ts` parses shape #2 today. **No source edit needed.** If the predicate parser throws on `body contains '## Existing Surfaces'` or `body contains '## Why not simpler?'` (the apostrophe in "isn't" is INSIDE the markdown body, not the predicate string — the predicate string itself uses simple ASCII single-quotes around the substring), it's a real parse bug — Developer halts and returns BLOCKED. Acceptance test #4 below exercises this end-to-end.

#### Layer 5 — Tests (1 existing file + 4 new fixtures)

13. **`cleargate-cli/test/lib/readiness-predicates.test.ts`** (live only; vitest, NOT node:test).
    - **Per the M6 dispatch note + CR-029 §0.6 + developer.md "Inner-loop test runner" + verified at M6-plan-time:** this file is EXISTING vitest-style (line 4: `import { describe, it, expect, afterEach, vi } from 'vitest';`). ADD the 4 new scenarios in vitest-style. Do NOT codemod to `node:test`. CR-029's batch codemod handles migration in the SPRINT-20→21 gap.
    - **Net-new test files (none in this milestone):** zero new test files. The 4 fixture markdown files (below) are test data, not test code, and live under `cleargate-cli/test/fixtures/code-truth-triage/`.
    - **4 NEW vitest scenarios (CR-028 §3 line 162-167 verbatim, mapped to vitest):**

      - **Scenario 1 — Epic with both new sections present → both new criteria pass.** Use existing `makeDoc` helper or write to a tmp dir via `makeTmpDir`. Body contains `## Existing Surfaces` AND `## Why not simpler?`. Frontmatter declares `parent_cleargate_id` etc. Run `evaluate(parsePredicate("body contains '## Existing Surfaces'"), doc)` — expect `{pass: true}`. Run `evaluate(parsePredicate("body contains '## Why not simpler?'"), doc)` — expect `{pass: true}`.
      - **Scenario 2 — Epic missing `## Existing Surfaces` → `reuse-audit-recorded` fails.** Body contains `## Why not simpler?` only. Run `evaluate(parsePredicate("body contains '## Existing Surfaces'"), doc)` — expect `{pass: false}` with a non-empty `detail` field.
      - **Scenario 3 — Story missing `## Why not simpler?` → `simplest-form-justified` fails.** Body contains `## Existing Surfaces` only. Run `evaluate(parsePredicate("body contains '## Why not simpler?'"), doc)` — expect `{pass: false}`.
      - **Scenario 4 — CR with `## Existing Surfaces` present → `reuse-audit-recorded` passes; `simplest-form-justified` is NOT asserted.** Test exercises only `reuse-audit-recorded` evaluation against a CR fixture. Body contains `## Existing Surfaces`. Run `evaluate(parsePredicate("body contains '## Existing Surfaces'"), doc)` — expect `{pass: true}`. **Negative assertion:** the test does NOT call `parsePredicate("body contains '## Why not simpler?'")` — it only asserts that the readiness-gates.md `cr.ready-to-apply` block does NOT include `simplest-form-justified` as a criterion ID (string-grep on the readiness-gates.md content: `! grep -A20 'cr.ready-to-apply' readiness-gates.md | grep -q 'simplest-form-justified'`). This guards against future drift where someone adds the criterion to the CR gate by mistake.
    - **Test scaffolding LOC estimate:** ~80 LOC of new test code (4 scenarios × ~15 LOC each + 20 LOC fixture-loading helper, OR inline `makeDoc` calls per the file's existing pattern at line 27-29). Net diff: +80 LOC.
    - **Test placement:** add to the END of the existing `describe('readiness-predicates', () => { ... })` block (the file uses one top-level describe — verified at line 24 / 36-style structure). New `describe('CR-028 code-truth triage criteria', () => { ... })` sub-block with the 4 scenarios.

14-17. **`cleargate-cli/test/fixtures/code-truth-triage/`** (4 NEW fixture files, all under one new dir).
    - **`epic-both-sections-present.md`** — minimal Epic frontmatter (epic_id, status, ambiguity, parent_cleargate_id) + body containing both `## Existing Surfaces` and `## Why not simpler?` headings + minimal sub-content.
    - **`epic-missing-existing-surfaces.md`** — Epic frontmatter + body containing ONLY `## Why not simpler?`.
    - **`story-missing-why-not-simpler.md`** — Story frontmatter (story_id, parent_epic_ref, status) + body containing ONLY `## Existing Surfaces`.
    - **`cr-with-existing-surfaces.md`** — CR frontmatter (cr_id, parent_ref, status, approved) + body containing `## Existing Surfaces`.
    - **Each fixture ~25 LOC.** Total fixtures: ~100 LOC across 4 files. Fixtures are test data — NOT counted toward the lane-rubric LOC cap (per protocol §24 check #1: "tests count toward the cap; generated files do not." Fixture files are static test data, not generated, but per protocol they are still test inputs. CR-028 stays `standard` lane regardless per SDR §2.4.).

#### Layer 6 — Flashcard (1 file)

18. **`.cleargate/FLASHCARD.md`** — append ONE line on commit (newest on top per skill protocol):
    ```
    2026-05-MM · #protocol #templates #readiness #code-truth · CR-028: code-truth principle stack — wiki/memory/context_source are caches; code is canonical. Drafts cite ## Existing Surfaces + answer ## Why not simpler? (Story+Epic both; CR Existing-Surfaces only; Bug exempt).
    ```
    Developer fills the actual MM on commit day. Per skill protocol: grep first to avoid dupes. Verified at M6-plan-time: no existing `#code-truth` cards.

### Files to create
- **4 fixture files** under `cleargate-cli/test/fixtures/code-truth-triage/` (files 14-17 above). The directory itself is net-new.

### Files explicitly NOT modified (carry-forward CR-028 §3 line 174 "zero new commands, zero new infra, zero predicate engine changes")
- `cleargate-cli/src/lib/readiness-predicates.ts` — both new predicates fit existing shape #2.
- `cleargate-cli/src/commands/sprint.ts` — preflight wiring is CR-027's surface; CR-028 only adds new gate criteria the existing engine evaluates verbatim.
- `cleargate-cli/src/commands/gate.ts` — `cleargate gate check` semantics unchanged.
- `.cleargate/templates/Bug.md` (live + canonical) — Bugs are exempt from BOTH new criteria. Verified by `grep '^## ' .cleargate/templates/Bug.md` at M6-plan-time: no Bug template edits required.
- `.cleargate/templates/{hotfix,initiative,sprint_context,sprint_report,Sprint Plan Template}.md` — out of scope per CR-028 §3 (only `epic`, `story`, `CR` templates listed).
- `cleargate-planning/MANIFEST.json` — auto-regenerates via `npm run prebuild`. CR-028 doesn't add scaffold files; it edits existing template + protocol + readiness-gates mirrors which all already have MANIFEST entries.
- All four agent files in `.claude/agents/` — agent contracts unchanged.
- `.cleargate/knowledge/cleargate-enforcement.md` — no enforcement-spec changes (the new criteria use existing predicate shapes; gate firing is the engine's existing behavior).

### Schema changes
**None.** No frontmatter shape mutation. The two new criteria read body text only (predicate shape #2). No migration. `cached_gate_result` schema is byte-identical (`{pass, failing_criteria, last_gate_check}` per `frontmatter-cache.ts:17-21`).

### Test scenarios (Gherkin from CR-028 §4 + plan additions)

**Acceptance gate (per CR-028 §4 verbatim, mapped to vitest scenarios + manual assertions):**

| §4 Acceptance | M6 Coverage | Notes |
|---|---|---|
| §4.1 Protocol §0 in place + mirror diff empty | Manual + verification-assertion script | Pre-commit assertion script (above) catches §-numbering breakage |
| §4.2 CLAUDE.md updated + bounded-block byte-equal | Manual `awk`-extract diff (above) | Pre-existing outside-block divergence preserved |
| §4.3 Templates expose new sections | Vitest scenario 1 (positive) + manual `head` of template after copy | Drafting `cp .cleargate/templates/epic.md /tmp/test-epic.md && grep '## Why not simpler?' /tmp/test-epic.md` returns 1 hit |
| §4.4 Readiness gates fire on missing sections | Vitest scenarios 2 + 3 | Both negative cases asserted |
| §4.5 Restoration unblocks (gate passes after sections added) | Vitest scenario 1 (positive after fixture mutation) | Pair of fixtures: missing → fail; full → pass |
| §4.6 No predicate-engine regression | `npm test` over full suite | Existing scenarios in `readiness-predicates.test.ts` continue to pass |
| §4.7 Bug template exemption | Manual: grep readiness-gates.md `bug.ready-for-fix` block — must NOT contain either new criterion ID | Architect-mandated string-grep assertion runs in CI as part of test scenario 4 |
| §4.8 Scaffold mirror discipline | 6 `diff` assertions (3 templates + 1 protocol + 1 readiness-gates noted-divergence + 1 CLAUDE-bounded-block) | All return empty / expected-diff |

**SPRINT-20 anchor backfill list** (per SDR §2.3 + verified at M6-plan-time via `grep -c "^## Existing Surfaces"` + `grep -c "^## Why not simpler"`):

| Anchor file | `## Existing Surfaces` | `## Why not simpler?` | Action in M6 commit |
|---|---|---|---|
| `EPIC-026_Sprint_Execution_Skill_Adoption.md` | absent | absent | **Add BOTH** (per SDR §2.3 mandated) |
| `STORY-026-01_Skill_Auto_Load_And_Mirror.md` | absent | absent | **Add BOTH** (per SDR §2.3 mandated) |
| `STORY-026-02_CLAUDE_Md_Prune.md` | absent | absent | **Add BOTH** (per SDR §2.3 mandated) |
| `CR-026_Token_Ledger_Attribution_Fix.md` | absent | n/a (CR scope = Existing-Surfaces only) | **Add `## Existing Surfaces` only** (per SDR §2.3 + CR-028 §1 CR scope) |
| `CR-027_Composite_Planning_Readiness_At_Sprint_Preflight.md` | absent | n/a (CR scope) | **Add `## Existing Surfaces` only** (per SDR §2.3 "verify and add if missing" — VERIFIED ABSENT, so add) |
| `CR-028_Code_Truth_Triage_Reuse_Right_Size_Justify.md` | absent | n/a (CR scope) | **Add `## Existing Surfaces` only** (self-backfill — same commit) |
| `BUG-025_PostToolUse_Duplicates_Parent_Cleargate_Id.md` | n/a (Bug exempt) | n/a (Bug exempt) | **EXEMPT — no edit** (per CR-028 §0.5 Q2 + readiness-gates `bug.ready-for-fix` exclusion) |

**Concrete backfill content for each anchor** (Developer copies into the indicated file, INSERT location: after the file's existing §3 / §2 closing block, before the bottom-of-doc `---` separator before the Ambiguity Gate; each Developer edit is ≤10 LOC):

For Epic + Story + CR backfill — minimal honest content. Examples:

- `EPIC-026 ## Existing Surfaces`: `.claude/skills/sprint-execution/SKILL.md` (live), CLAUDE.md "Architect runs twice" + "Sprint Execution Gate" paragraphs (the prune surface), `cleargate sprint init/preflight/close` handlers in `cleargate-cli/src/commands/sprint.ts`. Coverage: ≥80% — this epic extends + prunes existing surfaces, does not introduce a new orchestration layer.
- `EPIC-026 ## Why not simpler?`: smallest existing surface = the SessionStart hook + a CLAUDE.md docs-only edit. Why not sufficient: CLAUDE.md prose is unbounded; the skill-load directive needs a deterministic banner emit on sprint-active sessions, requiring a CLI sprint-context-emit handler in addition to the docs.
- `STORY-026-01 ## Existing Surfaces`: SessionStart hook at `.claude/hooks/session-start.sh`, sprint-init/preflight handlers in `cleargate-cli/src/commands/sprint.ts`, scaffold canonical-mirror discipline at `cleargate-planning/.claude/skills/`. Coverage: this story EXTENDS those surfaces.
- `STORY-026-01 ## Why not simpler?`: smallest = a one-line CLAUDE.md addition. Why not sufficient: docs-only doesn't load the skill — Claude Code reads `.claude/skills/` only when SessionStart banner directs, requiring both the canonical mirror AND the directive emit.
- `STORY-026-02 ## Existing Surfaces`: live CLAUDE.md prose at L98-L159 (bounded block), canonical at `cleargate-planning/CLAUDE.md` L7-L68. Coverage: pure delete-from-existing, no new surface.
- `STORY-026-02 ## Why not simpler?`: smallest = comment-out the obsolete blocks. Why not sufficient: HTML comments still ship in the bounded block injected by `cleargate init`; downstream users see them.
- `CR-026 ## Existing Surfaces`: token-ledger.sh hook at `.claude/hooks/token-ledger.sh`, write_dispatch.sh at `.cleargate/scripts/write_dispatch.sh`, settings.json hook wiring. Coverage: ≥80% — this CR extends the existing hook + dispatch surface, no new infrastructure.
- `CR-027 ## Existing Surfaces`: `sprintPreflightHandler` in `cleargate-cli/src/commands/sprint.ts:1075`, `readSprintExecutionMode` in `cleargate-cli/src/commands/execution-mode.ts:118`, `readCachedGate` in `cleargate-cli/src/lib/frontmatter-cache.ts:27`, `extractWorkItemIds` in `.cleargate/scripts/assert_story_files.mjs:74`. Coverage: this CR extends the preflight handler with a new check #5; reuses 4 existing helpers.
- `CR-028 ## Existing Surfaces`: `readiness-gates.md` predicate vocabulary (shape #2 used as-is), `readiness-predicates.ts` parser (no edits needed), template hierarchy at `.cleargate/templates/`, CLAUDE.md "Triage first" + "Duplicate check" anchors. Coverage: ≥95% — this CR is pure protocol + template + new gate-criteria additions, zero new infrastructure.

**Test commands (per CR-028 §4 line 198-201):**
- `cd cleargate-cli && npm run typecheck && npm test` — green.
- `cd cleargate-cli && npm test -- readiness-predicates` — focused (4 new scenarios pass + all existing scenarios stay green).
- Manual mirror diff loop:
  ```bash
  for f in CLAUDE.md .cleargate/knowledge/cleargate-protocol.md .cleargate/templates/story.md .cleargate/templates/epic.md .cleargate/templates/CR.md; do
    diff "$f" "cleargate-planning/$f" || echo "MIRROR DIFF: $f (review for expected vs unexpected drift)"
  done
  diff .cleargate/knowledge/readiness-gates.md cleargate-planning/.cleargate/knowledge/readiness-gates.md  # expected: pre-existing 2-line divergence at section(N) lines, NO new divergence
  ```
- `npm run prebuild` (in `cleargate-cli/`) — regenerates `cleargate-cli/templates/cleargate-planning/...` mirrors. `git status` should show updates only there + a MANIFEST sha256 change.

**Pre-commit gate:**
- `npm run typecheck` clean.
- `npm test` green for `cleargate-cli`. New scenarios pass; existing scenarios stay green.
- All scaffold-mirror diffs as expected (5 byte-equal pairs + 1 expected-2-line-divergence pair).
- §-numbering verification assertion (5-line script above) passes for both protocol files.
- CLAUDE.md bounded-block extraction returns identical `awk` output for live + canonical.
- One commit, conventional format `feat(CR-028): off-sprint — code-truth triage (reuse + right-size + justify-complexity)` per CR-028 §4 line 209.
- Never `--no-verify`.

**Post-commit:**
- Move `.cleargate/delivery/pending-sync/CR-028_*.md` to `.cleargate/delivery/archive/`.
- Append flashcard line.
- Wiki re-ingest happens automatically via PostToolUse hook.

### Reuse (no duplication)

- **Predicate shape #2** (`body contains "<string>"`) from `readiness-gates.md` line 14 — both new criteria use it verbatim. Zero engine edits.
- **`parsePredicate` + `evaluate`** from `cleargate-cli/src/lib/readiness-predicates.ts` (verified exported at line 9 of the test file) — used by all 4 new vitest scenarios.
- **`makeDoc` helper** in `readiness-predicates.test.ts:27-29` — lightweight `{fm, body, absPath}` constructor. Reuse for the 4 new scenarios; do NOT introduce a new fixture-loading helper if `makeDoc` plus a body-string suffices.
- **`makeTmpDir` helper** at line 16 of the test file — only needed if a scenario must write a real fixture file (e.g., for a future `file-exists` predicate test). The 4 new scenarios are body-string-based; `makeDoc` alone is sufficient. Use of `makeTmpDir` is OPTIONAL for the 4 scenarios.
- **CLAUDE.md "Triage first" + "Duplicate check" anchors** at live L109-L111 / canonical L18-L20 — the new "Codebase is source of truth" line slots between them. Do not introduce a new top-level header; the existing flat-bullet pattern at L107-L121 is the parity surface.
- **Template instructions blocks** (`<instructions>...</instructions>` at the head of each template) — extend the POST-WRITE BRIEF extraction map; do not change the block's structural contract.
- **`cleargate-planning/.cleargate/templates/{story,epic,CR}.md`** — verified byte-equal to live at M6-plan-time. Do NOT regenerate via prebuild for these doc edits — `cp live → canonical` is the per-edit invariant.
- **`STORY-026-01 R5` skill-mirror creation** is unrelated to CR-028 — the skill-mirror dependency belongs to CR-027 (M5 plan). CR-028 does not edit the skill.

### Gotchas surfaced from code inspection

- **GOTCHA-1 — protocol §0 already exists at line 7.** Verified at M6-plan-time: `## 0. The Five Phases` is line 7, NOT a free slot. The "Code-Truth Principle" preamble MUST be unnumbered and inserted between line 5 (`---`) and line 7 (`## 0`). Path (a) — renumber existing §0 → §1 — would cascade-break 161 numeric `protocol §N` refs in the live tree (per SDR §2.5). FLASHCARD `2026-04-21 #protocol #section-numbering`. The 5-line verification assertion above guards against accidental renumbering.
- **GOTCHA-2 — readiness-gates mirror has 2-line pre-existing divergence at lines 68/70.** Verified at M6-plan-time: live = `section(3)/section(5)`; canonical = `section(2)/section(4)`. FLASHCARD `2026-05-01 #mirror #parity`: do NOT reconcile pre-existing divergence as a side effect. Developer applies the new yaml entries to BOTH files SEPARATELY (not byte-copy), preserving the 2-line divergence. Mirror diff post-commit will still show ONLY those 2 lines (now at shifted line numbers due to insertions).
- **GOTCHA-3 — `## Why not simpler?` has a literal `?` in the heading.** Predicate shape #2 substring search is case-sensitive but punctuation-faithful. The predicate `body contains '## Why not simpler?'` MUST match the exact heading including the `?`. Verified at M6-plan-time: shape #2 parser at `readiness-predicates.ts` (per CR-027 M5 plan §"VERIFY NO EDITS") accepts arbitrary substring including `?`. Test scenario 1 (positive) exercises this end-to-end.
- **GOTCHA-4 — CLAUDE.md live has 91 lines of meta-repo prose ABOVE the bounded block.** Verified: live `<!-- CLEARGATE:START -->` is at line 98; canonical at line 7. The diff between live and canonical is 91 lines of meta-repo header that lives OUTSIDE the bounded block — that's PRE-EXISTING and intentional (live is the dogfood meta-repo's own readme; canonical is the injection-only block). Mirror parity applies to the bounded block ONLY. Use the `awk`-extraction diff (Edit 4 in file 4 above) — NOT a whole-file `diff`.
- **GOTCHA-5 — apostrophe in heading vs predicate string.** The user-facing heading is `## Why not simpler?` (no apostrophe in heading). The PREDICATE STRING uses single-quotes around the substring: `body contains '## Why not simpler?'`. Predicate vocabulary supports both single and double quotes around the substring (per readiness-gates.md line 14 examples — `'TBD'` and `"<string>"`). No escaping issue.
- **GOTCHA-6 — STORY-026-02 prune surface includes the "Architect runs twice" + "Sprint Execution Gate" + "Boundary gates" paragraphs.** Verified at M6-plan-time: live CLAUDE.md L119, L123, L121. The new "Codebase is source of truth" line at L110 (post-insert) is OUTSIDE the prune surface — STORY-026-02 will NOT delete it (per SPRINT-20 §2.2 row 1). Survives Wave 3.
- **GOTCHA-7 — `epic.ready-for-coding` (second epic transition) is NOT in CR-028's spec.** Verified by re-reading CR-028 §1 line 117: only `epic.ready-for-decomposition`. Architect call (Edit 2 of file 11): SKIP `epic.ready-for-coding`. Rationale: the reuse-audit and right-size questions are decomposition-time questions, already settled by the time the epic transitions ready-for-coding. CR-027's `discovery-checked` does apply to BOTH epic transitions (per CR-027 M5 plan); CR-028's two new criteria apply only to ONE.
- **GOTCHA-8 — `cleargate gate check` evaluation against the SPRINT-20 anchor backfill.** After the M6 commit ships, running `cleargate gate check .cleargate/delivery/pending-sync/EPIC-026_*.md` will re-evaluate against the new `epic.ready-for-decomposition` criteria (`reuse-audit-recorded` + `simplest-form-justified`). The backfill commit adds both sections to EPIC-026 — gate should pass. **Pre-merge order**: the M6 single commit lands BOTH the readiness-gate edits AND the backfill content together — there is no in-between state where the criteria are armed but the anchor files lack the sections. Acceptable per CR-028 §2 "v1 mode preserves backwards-compat" + the single-commit ordering.
- **GOTCHA-9 — vitest pool-cap.** Per FLASHCARD `2026-05-02 #vitest #ram #parallel-agents`: maxForks=2 in `vitest.config.ts`. CR-028's 4 new scenarios are body-string-based (no real fs writes for the 4 scenarios — fixtures are loaded as in-memory strings via `makeDoc`). RAM impact negligible. Wave 2 = 2 agents (CR-027 + CR-028) → 4 forks ≈ 1.6GB. Within budget.

## Cross-story risks (M6-specific + Wave-2 / Wave-3 hand-offs)

- **Risk-1 — CR-027 parallel edit on `readiness-gates.md`.** SDR §2.2 row 2: CR-027 adds `discovery-checked` to 5 enforcing entries + new `sprint.ready-for-execution`. CR-028 adds `reuse-audit-recorded` (3 entries) + `simplest-form-justified` (2 entries). Different criterion IDs, different positions in the same yaml `criteria:` arrays. Rebase order: CR-027 first per SDR §2.2; CR-028 rebases. **Mitigation:** Developer for CR-028 launches AFTER CR-027 merges. If a parallel race produces a merge conflict on the `criteria:` arrays, git's 3-way merge handles ordered-list appends without conflict markers as long as both CRs append to the END of each array — Architect verified both CRs append (no in-array insertion).
- **Risk-2 — CR-027 parallel edit on `cleargate-protocol.md`.** Disjoint regions (CR-027 = `### Gate 3` subsection of `## 4. The Four Gates` at line ~104; CR-028 = pre-§0 preamble between line 5 and line 7). Rebase-safe. No mitigation beyond the per-edit mirror discipline.
- **Risk-3 — STORY-026-02 prune ordering (Wave 3).** Per SPRINT-20 §2.2 row 1, the new "Codebase is source of truth" insertion lands OUTSIDE the prune surface and survives. Verified at M6-plan-time: STORY-026-02's prune target is the four-agent-loop / Sprint-Execution-Gate / Architect-runs-twice paragraphs, not the "Triage first" / "Duplicate check" stack. The new bullet slots BETWEEN those two paragraphs and is preserved.
- **Risk-4 — SPRINT-20 anchor backfill timing vs CR-027 preflight.** Per CR-027 M5 plan §"GOTCHA-1": CR-027 ships check #5 which evaluates SPRINT-20's own `cached_gate_result.pass: null`. After CR-027 + CR-028 both merge, the orchestrator MUST run `cleargate gate check` across the 7 anchor files (sprint plan + 6 children excluding BUG-025) BEFORE running `cleargate sprint preflight SPRINT-20`. Otherwise the per-item gate fails. **CR-028's single-commit backfill includes the section additions to all 6 non-bug anchors** so when `cleargate gate check` re-evaluates, both CR-027's `discovery-checked` (already passes — `context_source` populated) AND CR-028's two new criteria pass. Surfaces as a Wave-2 → Wave-3 hand-off note for the orchestrator.
- **Risk-5 — pre-existing pending-sync drafts beyond SPRINT-20 may newly fail under v2.** Out of scope for M6 commit — left to the natural backfill cycle per CR-028 §2 "v1 mode preserves backwards-compat." The orchestrator notes this in the SPRINT-20 close report; no SPRINT-20 commit obligation to backfill non-SPRINT-20 anchors.
- **Risk-6 — readiness-gates yaml syntax errors.** YAML is whitespace-sensitive. Each new criterion entry must align with the existing 4-space indent under `criteria:`. Developer runs `python3 -c "import yaml; yaml.safe_load(open('.cleargate/knowledge/readiness-gates.md').read().split('\n```yaml\n')[1].split('\n```')[0])"` for each yaml fence (or rely on `cleargate gate check` against any existing fixture — if yaml is malformed, the predicate engine throws on parse). The 5 yaml fences must each parse cleanly.
- **Risk-7 — CR-028's M6 commit is large** (13 mirrored doc files + 4 fixture files + 1 test file + 6 anchor backfills = ~22 file edits). git diff --stat may exceed reviewer wall-clock budget. **Mitigation:** Architect SDR §2.4 keeps CR-028 `lane: standard` (≥2 files / >50 LOC) — no fast-lane shortcut. QA reviews per file class (protocol, CLAUDE.md, templates, readiness-gates, tests, fixtures, anchor backfills) using the test plan's class-grouped tables.

## Risks (M6-specific summary table)

| Risk | Mitigation |
|---|---|
| Protocol §-renumbering cascade-break | 5-line verification assertion (above) runs pre-commit. Heading count + heading-text equality enforced for both files. (GOTCHA-1) |
| Pre-existing readiness-gates 2-line mirror divergence reconciled by accident | Per-edit `cp` not whole-file copy; apply yaml hunks to BOTH files separately. (GOTCHA-2) |
| CLAUDE.md outside-bounded-block divergence misread as a regression | Use `awk`-extracted bounded-block diff, NOT whole-file `diff`. (GOTCHA-4) |
| `epic.ready-for-coding` accidentally gains the new criteria | Architect call: skip Edit 2 of file 11 per spec literal read. (GOTCHA-7) |
| CR-027 merge race on `readiness-gates.md` `criteria:` arrays | CR-027 lands first per SDR §2.2; both CRs append to end of arrays — git 3-way merge handles cleanly. |
| Vitest pool-cap exceeded by Wave-2 parallelism | Wave 2 = 2 agents × maxForks=2 = 4 forks ≈ 1.6GB; within budget. (GOTCHA-9) |
| SPRINT-20 anchor backfill misses one of 6 non-bug anchors | Backfill table (above) is exhaustive; QA verifies each anchor has the required section(s). |
| Predicate parser regression on `?` in `body contains '## Why not simpler?'` | Vitest scenario 1 (positive) exercises end-to-end before commit. |
| `cleargate gate check` fails post-merge against backfilled anchors | Single-commit ordering: section additions + readiness-gate criteria ship together; no in-between state. (GOTCHA-8) |

## Open decisions for orchestrator (out of Architect scope)

- **None blocking.** All §0.5 questions resolved in CR-028 itself (in-sprint inclusion; Bug exemption; loose predicate form; capability index deferred to CR-029-suggested; orthogonal to CR-027). SDR §2.5 HARD flag REQUIRES-CR-028-M6-PLAN-DECISION resolved by this plan: PATH (b) unnumbered preamble. SDR §2.5 SOFT flag (anchor backfill) resolved by the explicit 7-row backfill table above.
- **For the Wave-2 → Wave-3 hand-off:** the orchestrator MUST run `cleargate gate check` across all SPRINT-20 anchor files (sprint plan + 7 children) AFTER CR-027 + CR-028 merge but BEFORE any subsequent `cleargate sprint preflight`. Otherwise SPRINT-20's per-item gate trips on stale `cached_gate_result`. Capture in the Wave-2 close note.
- **For the QA agent (post-Developer):** verify the 6-pair scaffold-mirror diff (5 byte-equal + 1 expected-2-line-divergence) PLUS run a manual smoke against ONE Epic + ONE Story fixture in a scratch dir to confirm `cleargate gate check` returns the expected pass/fail with both new criteria evaluated.
- **For the Reporter:** at sprint close, record per-CR token cost from token-ledger.jsonl. CR-028's expected cost is mid-tier (no engine edits, but 22 file edits + 4 new tests).

## Dispatch precondition (orchestrator MUST verify before spawning Developer)

1. `git log main --oneline | head -10` shows STORY-026-01 + CR-026 + BUG-025 commits all merged.
2. `diff .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` returns empty (verified at M6-plan-time).
3. `diff .cleargate/templates/story.md cleargate-planning/.cleargate/templates/story.md` returns empty (verified at M6-plan-time).
4. `diff .cleargate/templates/epic.md cleargate-planning/.cleargate/templates/epic.md` returns empty (verified at M6-plan-time).
5. `diff .cleargate/templates/CR.md cleargate-planning/.cleargate/templates/CR.md` returns empty (verified at M6-plan-time).
6. `grep -c '^## [0-9]' .cleargate/knowledge/cleargate-protocol.md` returns 16 (pre-edit baseline; verified at M6-plan-time).
7. `grep -nE '^<!-- CLEARGATE:START -->|^<!-- CLEARGATE:END -->' CLAUDE.md` returns 2 lines (98 + 159) — bounded block intact.
8. `cleargate-cli/test/lib/readiness-predicates.test.ts` exists and is vitest-style (verified line 4 `import { ... } from 'vitest'`).
9. `npm run prebuild` in `cleargate-cli/` runs clean (no drift errors). FLASHCARD `2026-05-01 #manifest #prebuild`.
10. SPRINT-20 sentinel `.cleargate/sprint-runs/.active` reads `SPRINT-20`.
11. Vitest pool-cap budget: Wave 2 = 2 agents × maxForks=2 = 4 forks ≈ 1.6GB. Within budget; no `VITEST_MAX_FORKS` override needed.

## Flashcards to record on completion (Developer's responsibility post-merge)

After CR-028 merges:
- `2026-05-MM · #protocol #templates #readiness #code-truth · CR-028: code-truth principle stack — wiki/memory/context_source are caches; code is canonical. Drafts cite ## Existing Surfaces + answer ## Why not simpler? (Story+Epic both; CR Existing-Surfaces only; Bug exempt).`
- `2026-05-MM · #protocol #section-numbering · CR-028 inserted unnumbered "Code-Truth Principle" preamble BEFORE existing §0 to avoid renumbering 16 numbered headings (161 ref sites). Path (a) renumber would cascade-break; path (b) preamble preserves stability.`
- `2026-05-MM · #templates #mirror · CR-028 SPRINT-20 anchor backfill = 6 anchors get sections (4 mandated + 2 verified-absent = 6); BUG-025 exempt. Generalize: when adding a new readiness criterion to the live gate, audit pending-sync anchors in the same commit, NOT a separate cycle.`

(Per Skill(flashcard) protocol: grep first to avoid dupes; newest on top.)

## Git Log Digest

Git log unavailable for sprint/S-20 (branch not found).

## Token Ledger Digest

**Total:** 59,856,049 tokens (input: 709 / output: 262,800 / cache_read: 57,016,270)

**Per-agent breakdown:**
- architect: 31,261,607 (10 dispatches)
- developer: 17,425,236 (4 dispatches)
- qa: 5,285,657 (4 dispatches)
- reporter: 5,883,549 (1 dispatches)


## Flashcard Slice

No flashcard entries found in sprint window [2026-05-16 → 2026-05-29].

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
