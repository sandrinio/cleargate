role: developer

STORY: CR-117
STATUS: done
COMMIT: d8ab60b3be937285063d5dbc2c25d93a574b3d52
TYPECHECK: skip (sprint_context.md §Test Stack — no typecheck this sprint, do not run one)
TESTS: 36 passed, 0 failed (`node --test .cleargate/scripts/state-scripts.test.mjs`, full file — was 31 passed/0 failed before this story; the 5 new CR-117 tests bring it to 36)
FILES_CHANGED:
  - /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/CR-117/.cleargate/scripts/validate_state.mjs
  - /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/CR-117/.cleargate/scripts/state-scripts.test.mjs
  - /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/CR-117/.cleargate/delivery/pending-sync/CR-117_cli_diagnostics_papercuts.md (Task Breakdown ticked)
  - /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/CR-117/.cleargate/wiki/index.md (auto-generated by ingest hook)
  - /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/CR-117/.cleargate/wiki/log.md (auto-generated by ingest hook)
  - /Users/ssuladze/Documents/Dev/ClearGate/.worktrees/CR-117/.cleargate/wiki/crs/CR-117.md (auto-generated by ingest hook, new file)
NOTES: Rewrote the CLI-mode argv block at validate_state.mjs:184-227 (was :186-220 pre-edit): a lone non-flag positional is now consumed as the state-file path (same branch structure, added as a new `else if` alongside the existing `--state-file` branch and unchanged discovery fallback); when both a positional and --state-file are given, both paths are printed to stderr and --state-file wins (matches the CR's "say which one wins" open-question resolution — flag beats positional since it's the more explicit form); any arg not consumed as the flag, its value, or the one allowed positional is now named in an `Error: unrecognised argument(s): ...` and exits 1 instead of being silently dropped. Usage string at :5 updated to `[<path>] [--state-file <path>]`. Discovery/validation logic below the branch (existence check, JSON.parse, validateState, checkFoldDrift) is byte-identical. Chose state-scripts.test.mjs (not a sibling file) per the dispatch's own suggestion — it already owns a "Scenario 6: validate_state" describe block and the shared makeState/makeStory/writeStateJson/runScript helpers, so a sibling file would have duplicated fixture plumbing for no benefit. One test (the "positional bypasses discovery" case) needed a fixture that copies validate_state.mjs + constants.mjs + state-events.mjs into a temp `.cleargate/scripts/` so the script's own REPO_ROOT (computed from import.meta.url, not cwd) lands inside the fixture and can reproduce the real "Multiple state.json files found" collision without touching the live repo's sprint-runs/; hit a real bug there — macOS os.tmpdir() returns a /var/folders/... path that is a symlink to /private/var/folders/..., and the CLI-mode guard's `process.argv[1] === fileURLToPath(import.meta.url)` compares an un-realpath'd argv[1] to a realpath'd import.meta.url, so the guard silently failed to match and the copied script never entered CLI mode (empty output, exit 0). Fixed by `fs.realpathSync()`-ing the mkdtemp'd base dir before use; recorded as a flashcard below. Verified red-before-green by stashing only validate_state.mjs and re-running the full suite: 3 of the 4 new describe blocks failed as expected (positional-accepted, both-supplied, unrecognised-argument); the 4th ("--state-file flag alone still works") stayed green both before and after, which is correct — it is a regression guard for unchanged legacy behavior, not a red test. The stash-off run also incidentally reproduced the CR's own motivating bug end-to-end: with no fix, my unrecognised-argument test (`--frobnicate` alone) fell through to the real repo's discovery path, found the ~15 real state.json files under .cleargate/sprint-runs/, and failed with the exact "Multiple state.json files found" wall of paths the field report describes — strong evidence the fix addresses the real incident, not just a synthetic one. Left .cleargate/knowledge/script-incident-corpus-analysis.md and test_pre_gate_lane_aware.sh untouched (out of the CR's Execution Sandbox) but flagged both below as adjacent — the former documents the now-partially-stale conclusion that the SPRINT-24 incident could only be fixed by adding --state-file explicitly; the latter statically copies validate_state.mjs into two fixture trees but (confirmed by reading) never invokes it with an argument pattern this change touches.
r_coverage: []
r_coverage_note: "CR-117 has no numbered R1..RN requirement list (a CR delivery doc, not a Story with a §3 Implementation Guide) -- defaulting to [] per the field's own instruction. Coverage is instead demonstrated by the CR's own §4 Verification Protocol table (4 required unit cases), all satisfied and named in TESTS above."
plan_deviations: []
adjacent_files:
  - ".cleargate/scripts/test/test_pre_gate_lane_aware.sh (copies validate_state.mjs into two fixture .cleargate/scripts/ trees as an update_state.mjs dependency; read both copy sites -- neither invokes the copied script with an argument pattern this change touches, so no regression expected, but it will silently pick up the new argv behavior on its next copy)"
  - ".cleargate/scripts/test/test_close_pipeline.sh (invokes validate_state.mjs via CLI with only CLEARGATE_STATE_FILE env var, no positional/flag args -- unaffected, verified by reading the invocation site)"
  - ".cleargate/knowledge/script-incident-corpus-analysis.md (documents the SPRINT-24 'Multiple state.json files found' incident this CR fixes; its root-cause narrative -- 'the fix is to invoke the script with --state-file <path>' -- is now incomplete, a positional path also resolves it. Doc content, not code; left untouched, flagging for the doc-refresh checklist.)"
flashcards_flagged:
  - "2026-09-02 · #test-harness #cli #danger · macOS os.tmpdir() is a symlink (/var/folders -> /private/var/folders); a CLI-mode guard comparing argv[1] to realpath'd import.meta.url silently no-ops on an un-realpath'd tmp fixture -- fs.realpathSync() the mkdtemp base before spawning a copied script."
