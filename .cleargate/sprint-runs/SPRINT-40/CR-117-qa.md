role: qa

STORY: CR-117
QA: PASS
TYPECHECK: skip (sprint_context.md §Test Stack — no typecheck this sprint, per explicit instruction)
TESTS: 36 passed, 0 failed, 0 skipped (`node --test .cleargate/scripts/state-scripts.test.mjs`, re-run independently via `bash .cleargate/scripts/run_script.sh node --test ...` — matches Developer's reported 36/36)
ACCEPTANCE_COVERAGE: 4 of 4 required unit cases (§4 Verification Protocol / Task Breakdown row 4) have matching, non-vacuous, passing tests: positional accepted, flag accepted (regression), both-supplied names both, unrecognised argument named. A 5th test (canary) additionally proves the multi-candidate fixture is not vacuous.
MISSING: none
REGRESSIONS: none

## Verification detail

1. **Argument-boundary discipline (CR §3 "do not change discovery or validation semantics").** Confirmed by direct diff read: the entire change is contained to `validate_state.mjs:190-233` (the CLI-mode argv-parsing block) plus the usage-string doc comment at `:5-10`. Everything below the branch — the `stateFile` existence check, `JSON.parse`, `validateState()`, `checkFoldDrift()`, and the discovery-fallback/env-var block (`:234-294`) — is byte-identical to the pre-CR-117 commit (`git diff main story/CR-117 -- validate_state.mjs` shows all `+`/`-` lines confined to the argv block and the header comment). Confirmed independently, not taken on the Developer's word.

2. **Additive, not just green.** Traced all three legacy input shapes through the new code by hand and confirmed each is byte-for-byte preserved: bare invocation (empty `args` → falls straight through to discovery, unchanged), `--state-file <path>` alone (covered by the Developer's own regression test, which I confirmed stays green both before AND after the fix), and `CLEARGATE_STATE_FILE` env var (untouched code path, never reached by the new branch logic). Grepped the full repo for every `validate_state.mjs` invocation site outside the test file itself: `test_close_pipeline.sh:413` calls it via `CLEARGATE_STATE_FILE=... bash "$RUN_SCRIPT" validate_state.mjs` with **zero** positional/flag args — unaffected. `test_pre_gate_lane_aware.sh:169,638` only `cp`'s the script into fixture trees as a static `update_state.mjs` dependency and never invokes it with an argument this change touches — confirmed by reading both copy sites myself, not just accepting the claim. No exit-code or output-format change on any existing call site.

3. **The four required cases, judged.** All four (§4 table) are implemented and tested: positional-only, flag-only (regression), both-supplied, unrecognised-argument. On the both-supplied decision: the CR's §0.5 Open Questions text ("Reject only when both... are given, and say which one wins") is genuinely ambiguous in isolation, but §1 "New Logic" — the authoritative spec section — resolves it unambiguously: "the script names both and states which it used" (informational, not a hard-reject/error exit). The Developer's implementation matches §1 exactly (exit reflects the *validity of the winning file*, not a forced error). The CR text does **not** specify *which* input wins when both are given — that specific choice (`--state-file` wins, "flag beats positional since it's the more explicit form") was the Developer's own call, filling a genuine gap the CR left open. It's a defensible, disclosed decision consistent with the CR's own "Prior work" precedent (CR-093: an explicit file argument is authoritative), the message clearly states which path won, and it's covered by a test that would fail if reversed. Flagging as a documented interpretation rather than a defect — worth an explicit CR amendment only if a future caller needs positional-wins semantics.

4. **Developer's own tests, verified adversarially, not accepted on report.** I did not trust the "3 of 4 failed on stash" claim — I reproduced it myself: copied the post-fix `validate_state.mjs` aside, checked out the pre-CR-117 (`HEAD~1`) version in its place, and re-ran the full suite. Result matched the Developer's report exactly: the 3 new-behavior describe blocks (`positional path`, `both supplied`, `unrecognised argument`) went red, the flag-only regression test stayed green, and — as a bonus confirmation — the reverted run's "unrecognised argument" test reproduced the *real* incident end-to-end (`--frobnicate` fell through the unfixed discovery path and hit the actual repo's `.cleargate/sprint-runs/` wall of ~24 real `state.json` candidates). Restored the file to the exact committed byte content afterward (`git status`/`git diff` clean). None of the 4 new assertions are satisfiable by a no-op: each pins either an exit code, an output-content check, or both, and each demonstrably fails against the pre-fix code for the *correct* reason (not an incidental one).

5. **The macOS `os.tmpdir()` symlink issue.** Confirmed the fix (`fs.realpathSync()` on the fixture's `mkdtemp` base) lives entirely in the test file (`state-scripts.test.mjs:1583`), not in `validate_state.mjs`. The CLI-mode guard the Developer identified as latently affected (`process.argv[1] === fileURLToPath(import.meta.url)` at `validate_state.mjs:190`) is untouched by this commit — it predates CR-117 (present, unchanged, in the `HEAD~1` version I diffed against) and sits *above* CR-117's declared Execution Sandbox (the argv-parsing block, not the guard that gates entry into it). This is correctly out of scope for a CR whose sandbox is explicitly "the argument boundary only" — fixing the guard itself would be undeclared scope creep for a fast-lane CR. It is, however, a real latent defect: any script invoked through a symlinked path (a symlinked home dir, an unresolved `/var/folders` tmp dir, etc.) would silently no-op instead of erroring loudly. The Developer flagged it via a flashcard rather than quietly patching around it — correct call. Not a CR-117 regression; a pre-existing edge case worth a future CR if it ever surfaces operationally (it hasn't, per the corpus doc).

## Context note

No `.qa-context-CR-117.md` pack existed at dispatch time (`prep_qa_context.mjs` supports CR ids per its own header, but none was generated for this item). The dispatch text itself supplied an equivalent, orchestrator-verified context bundle in place of the pack (worktree/commit, established suite results, established commit-scope diff stat, established before/after smoke test) and an explicit alternate Preflight (CR file → dev handoff → sprint-context). Proceeded per that explicit override rather than hard-failing on pack absence, since the orchestrator had already taken ownership of the context derivation the pack-absent rule exists to prevent agents from improvising themselves. All claims in that bundle were independently re-verified above (test run, diff read, revert-and-rerun, repo-wide grep), not taken as given.

## Script Incidents

None. All script invocations in this QA pass (`node --test .cleargate/scripts/state-scripts.test.mjs`, `node .cleargate/scripts/validate_state.mjs <path>`) were run through `bash .cleargate/scripts/run_script.sh` and exited 0; `.cleargate/sprint-runs/SPRINT-40/.script-incidents/` contains only a pre-existing incident from 00:07 (before this QA pass began), untouched.

VERDICT: Ship it. The commit stays cleanly at the argument boundary the CR declared — discovery, validation, and every legacy call path (bare, `--state-file`, `CLEARGATE_STATE_FILE`) are byte-identical to pre-fix behavior, independently traced and exercised, not just diffed. All four required cases are covered by tests I confirmed are non-vacuous by reverting the implementation myself and watching exactly 3 of them go red for the right reasons. The one real gap in the CR text — which input wins when both are supplied — was left unspecified by the CR itself; the Developer's resolution (`--state-file` wins) is disclosed, tested, and consistent with adjacent precedent, so I'm treating it as an acceptable fast-lane interpretation rather than a defect. The macOS symlink finding is real but correctly scoped out of this CR's diff (fixed in the test only) and flagged for the future rather than silently absorbed.

flashcards_flagged: []
