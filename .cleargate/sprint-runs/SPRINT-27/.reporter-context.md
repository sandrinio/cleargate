# Reporter Context Bundle — SPRINT-27

_Generated: 2026-05-15T05:04:05.154Z_

---

## Sprint Plan Slices

_Source: SPRINT-27_MCP_Type_Agnostic_And_Console_Connection.md_

## 1. Consolidated Deliverables

## 2. Execution Strategy (Sprint Design Review — Binding)

## 5. Metrics & Metadata

## State.json Summary

Sprint: SPRINT-27 | Status: Active

STORY-027-01 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-027-02 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-027-03 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-027-04 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-027-05 | Done | standard | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

# M1 — Wave 1 Plan (Parallel-Safe Quartet)

> Sprint: SPRINT-27 · Milestone: M1 (Wave 1) · Mode: `execution_mode: v2`
> Authored by Architect 2026-05-15 per SDR §2 binding plan.
> Scope: CR-065, BUG-030, CR-062, CR-063. STORY-027-05 (fast lane) handled out-of-band; not blueprinted here.

## Cross-cutting

Pulled from `sprint-context.md` (Cross-Cutting Rules + Active FLASHCARD Tags) and SDR-relevant FLASHCARDs. Mirror these as one-line bullets — every Developer dispatch starts from this same page.

- **L0 Code-Truth (CR-028).** Codebase is source of truth; wiki/memory/context_source are derived caches. On any conflict between this M-plan and code, the code wins — re-read and surface a Blockers Report (`spec-gap` category) before patching.
- **Mirror parity is per-edit, not whole-file (FLASHCARD 2026-05-01 `#mirror #parity`).** When a story edits a file that has a `cleargate-planning/` mirror, apply the IDENTICAL edit to both copies in the same commit. Never reconcile pre-existing divergence as a side effect. `close_sprint.mjs` lives in both `.cleargate/scripts/` (live) and `cleargate-planning/.cleargate/scripts/` (canonical) — CR-063 must edit both.
- **Scaffold payload is regenerated, not hand-edited (FLASHCARD 2026-05-01 `#scaffold #mirror #prebuild`).** Do NOT touch `cleargate-cli/templates/cleargate-planning/**` directly — `copy-planning-payload.mjs` rebuilds it on `npm run prebuild`.
- **Test-location convention for wiki commands (FLASHCARD 2026-04-24 `#test-location #wiki #cli`).** New wiki tests for CR-063 live at `cleargate-cli/test/wiki/<topic>.test.ts` (or `.node.test.ts`), NOT under `test/commands/`. Existing siblings: `test/wiki/build.test.ts`, `test/wiki/_fixture.ts`.
- **node:test is the default for new tests (per `feedback_node_test_over_vitest`).** All new test files in this wave use `*.node.test.ts` naming via `tsx --test`. Do NOT add new vitest files.
- **No mocks for DB tests.** BUG-030 + CR-062 test seeding hits the real docker-compose Postgres 18; reuse `seedProjectAndMember(deps)` and the test-mailer (`mcp/src/auth/identity/mailer.fake.ts`) — do NOT mock Drizzle.
- **Never `--no-verify`.** If pre-commit `typecheck` or `npm test` fails, fix the cause, do not bypass.
- **One commit per story.** Format: `feat(<epic>): <story-id> <short desc>` or `fix(<epic>): <bug-id> <short desc>`.

## Goal anchor

> Open MCP to any work-item type, prove it by syncing ClearGate's own sprint plans + reports to MCP, lock the codebase/PM-tool boundary, and give the admin console a copy-paste "connect an external agent" snippet that works for both HTTP and stdio (Claude Desktop) clients.

Per-item advancement clauses (verbatim from SDR §2.1):

- **CR-065** — Unblocks the stdio tab of CR-061 by giving Claude Desktop / Claude Code a `CLEARGATE_SERVICE_TOKEN` consumer path that bypasses keychain auth. Without it, the "copy-paste connect snippet" promise is half-complete (HTTP only).
- **BUG-030** — Removes the P1-High 500 blocker on the members surface that gates `cleargate join` recovery flows. Member-management must be reliable before the modal that hands out tokens (CR-061) is trustworthy.
- **CR-062** — Closes the members-list UX gap; admins recover from missed invites without shell access. Pairs with BUG-030 to make the members surface fully self-serve.
- **CR-063** — First half of the EPIC-027 proof loop. Sprint reports become a second wiki source class, so MCP-pushed sprint artifacts (CR-064 in Wave 3) and wiki-queryable sprint reports both ship in one sprint.

## Per-story blueprint

### CR-065 — `cleargate mcp serve` service-token env auth

- **Goal advancement:** Unblocks CR-061's stdio tab — Claude Desktop / Claude Code can connect with a token pasted into `mcpServers.<name>.env.CLEARGATE_SERVICE_TOKEN` instead of running `cleargate join` first.

- **File surface (by symbol, verified by grep):**
  - **Extend** `cleargate-cli/src/commands/mcp-serve.ts`:
    - `runServeCommand` function (the symbol `cleargate mcp serve` entry — currently constructs `new AuthFetcher({...})` at line 71 of the verified file). Insert the env-var branch BEFORE that constructor call.
    - `Authorization: Bearer ${accessToken}` header at line 178 — unchanged; `accessToken` source becomes a `TokenFetcher.getAccessToken()` return regardless of mode.
  - **Extend** `cleargate-cli/src/auth/refresh.ts`:
    - Symbol: `class AuthFetcher`. Extract `interface TokenFetcher { getAccessToken(): Promise<string> }` (export it); declare `class AuthFetcher implements TokenFetcher`.
  - **Add** `cleargate-cli/src/auth/service-token-fetcher.ts` (NEW):
    - `export class ServiceTokenFetcher implements TokenFetcher`. Constructor `(token: string)`. `async getAccessToken() { return this.token; }`. No-op `dispose()` only if `AuthFetcher` exposes one.
  - **Add** `cleargate-cli/src/auth/service-token-fetcher.node.test.ts` (NEW).
  - **Add or extend** `cleargate-cli/src/commands/mcp-serve.node.test.ts` (NEW or extension; existing file may be vitest — create the `.node.test.ts` variant per cross-cutting rule).

- **Implementation sketch (ordered):**
  1. In `refresh.ts`, define and export `interface TokenFetcher { getAccessToken(): Promise<string> }`. Mark `AuthFetcher implements TokenFetcher` — do NOT change its method body.
  2. Create `service-token-fetcher.ts` with the static class. ≤20 LOC.
  3. In `mcp-serve.ts` near top of `runServeCommand`, read `const serviceToken = process.env.CLEARGATE_SERVICE_TOKEN ?? '';` and branch: `if (serviceToken.length > 0) { fetcher = new ServiceTokenFetcher(serviceToken); stderr.write('cleargate mcp serve: auth mode = service-token\n'); }` else `{ fetcher = new AuthFetcher({...}); stderr.write('cleargate mcp serve: auth mode = keychain-refresh\n'); }`. Typing: `let fetcher: TokenFetcher;`. Empty string is treated as unset (per CR §4 failure-mode test).
  4. Generalize the request loop's `accessToken` source from `fetcher.refresh()` (AuthFetcher-specific) to `fetcher.getAccessToken()`. `AuthFetcher.getAccessToken()` internally still calls its existing refresh + cache logic. `ServiceTokenFetcher.getAccessToken()` returns the constructor-passed token verbatim.
  5. On 401 from `/mcp` when mode === service-token: write actionable error to stderr (`cleargate mcp serve: CLEARGATE_SERVICE_TOKEN rejected by /mcp (401). Issue a new token in the admin console: Tokens → Issue → copy snippet.`) and call `process.exit(1)`. No retry. No keychain fallback.
  6. On 401 in keychain mode: existing behavior unchanged.
  7. Lock the env var name `CLEARGATE_SERVICE_TOKEN` as a string LITERAL in the source — CR-061's stdio tab in Wave 3 will string-match this; no shared-constant import needed and adding one would risk a Wave 3 rebase.
  8. Author `service-token-fetcher.node.test.ts` + the new `mcp-serve.node.test.ts` cases (see Test shape).

- **Test shape (Red tests for QA-Red to author first):**
  1. `ServiceTokenFetcher#getAccessToken returns constructor-passed token verbatim` — pure unit.
  2. `mcp serve · with CLEARGATE_SERVICE_TOKEN set, emits auth mode = service-token and bypasses keychain` — assert keychain mock NOT called.
  3. `mcp serve · sends Authorization: Bearer <env-value> on first /mcp POST when service-token mode active`.
  4. `mcp serve · with CLEARGATE_SERVICE_TOKEN unset, emits auth mode = keychain-refresh and runs existing refresh path` — behavior identical to pre-CR.
  5. `mcp serve · empty CLEARGATE_SERVICE_TOKEN ('') treated as unset, falls to keychain mode` (CR §4 failure-mode line 184).
  6. `mcp serve · in service-token mode, on first /mcp 401, emits actionable error citing admin-console path and exits non-zero. No keychain fallback.` Assert exit code non-zero AND stderr contains the literal `CLEARGATE_SERVICE_TOKEN rejected by /mcp (401)`.

- **Risks / wiring traps:**
  - **Auth-adjacency** (SDR §2.3) — only auth-touching item in Wave 1. The env-unset path MUST be byte-identical to pre-CR behavior. QA-Verify must explicitly assert the keychain-mode test case behaves identically to a pre-CR baseline; this is the regression bar.
  - **`TokenFetcher` interface extraction can become invasive.** Keep it minimal: ONE method (`getAccessToken`). Do not pull in cache or expiry shape from `AuthFetcher`. If `AuthFetcher.refresh()` is the existing public API on the request-loop side, keep it AS WELL — `getAccessToken()` is the new common entry; nothing else changes.
  - **Env-var-name literal coupling with CR-061.** Do not refactor the literal `CLEARGATE_SERVICE_TOKEN` into a shared constant in this CR — CR-061 (Wave 3) will string-match it. A constant export would make Wave 3 import it; rebase risk if W2 churn touches `refresh.ts` exports. Defer constant-extraction to a follow-up CR.

- **DoD references (CR-065 file):**
  - L80-101 (Old vs New logic).
  - L131-137 (Execution Sandbox modify + add lists).
  - L148-184 (Verification Protocol — 4 unit case names + failure-mode).
  - L207-213 (Ambiguity Gate — all 4 §0.5 Qs resolved).

---

### BUG-030 — `DELETE /members/:mid` 500 → 204 FK fix

- **Goal advancement:** Removes the P1-High blocker on the members surface so admins can run the routine "remove a member" action without hitting 500s. Precondition for CR-062 trusting the same route region.

- **File surface (by symbol, verified by grep):**
  - **Modify** `mcp/src/db/schema.ts` (verified at line 96-98):
    - Symbol: `items.updatedByMemberId` column declaration. Currently `uuid().notNull().references(() => members.id)` — drop `.notNull()`, change `.references(() => members.id)` to `.references(() => members.id, { onDelete: 'set null' })`. Adjacent (line 92) — STORY-027-05 also touches this file (different line region, no overlap). Per SDR §2.2, BUG-030 lands first (ships with the migration); STORY-027-05's line-92 comment edit rebases.
  - **Add** `mcp/migrations/<next-numeric>.sql` (Drizzle migration generated via `drizzle-kit generate` OR hand-written per the SQL block in BUG-030 §4 lines 108):
    ```sql
    ALTER TABLE items ALTER COLUMN updated_by_member_id DROP NOT NULL;
    ALTER TABLE items DROP CONSTRAINT items_updated_by_member_id_members_id_fk;
    ALTER TABLE items
      ADD CONSTRAINT items_updated_by_member_id_members_id_fk
      FOREIGN KEY (updated_by_member_id)
      REFERENCES members(id)
      ON DELETE SET NULL;
    ```
  - **Modify** `mcp/src/admin-api/members.ts` (verified at line 250):
    - Symbol: the `await deps.db.delete(members).where(eq(members.id, row.memberId))` call inside the DELETE handler. Wrap in `try { ... } catch (err) { ... }`. Inside catch, detect PostgresError where `err.code === '23503'` and reply `409 { error: 'member_has_dependents', code: 23503 }`; rethrow otherwise (Fastify default error mapper).
  - **Modify** `mcp/src/admin-api/members.test.ts` — add two new cases (see Test shape). Existing test file is the `.node.test.ts` extension if present; else create `members.node.test.ts` per cross-cutting rule.

- **Implementation sketch (ordered):**
  1. Edit `schema.ts:96-98` per above. Verify: `git diff` shows only those three lines changed.
  2. Generate the migration: `cd mcp && npx drizzle-kit generate` — confirm output SQL matches the §4 block; rename the generated `.sql` file to fit existing numeric sequence if drizzle picks a non-sequential name.
  3. Run migration locally against docker-compose Postgres to confirm `ALTER` succeeds against a populated table.
  4. Wrap the `db.delete(members)` call in `members.ts:250` with try/catch. Use the PG error code check (Drizzle wraps Postgres errors; check `err.code === '23503'` OR the underlying `cause?.code === '23503'` — verify by reading an existing Drizzle error-handling site if present, else log the caught error type once via temporary `console.error` during test authoring and remove before commit).
  5. Add the two test cases.

- **Test shape (Red tests for QA-Red):**
  1. `DELETE /members/:mid · returns 204 when deleting a member who has authored items; item survives with updatedByMemberId = null` — verbatim from BUG-030 §5 lines 121-149. Pre-fix asserts 500; post-fix asserts 204 + null attribution.
  2. `DELETE /members/:mid · returns 409 with member_has_dependents code when FK violation slips past schema cascade` — defense-in-depth. Test setup: temporarily run the handler against a DB where the migration hasn't applied (or stub the FK enforcement by leaving an ON DELETE NO ACTION FK on a second referencing table) OR mock the `db.delete` call to throw a PostgresError with `code: '23503'` — the latter is the cleaner unit form.

- **Risks / wiring traps:**
  - **Shared file with CR-062** (SDR §2.2): `mcp/src/admin-api/members.ts`. BUG-030 lands first per strict ordering. Region: DELETE handler at line ~250. CR-062 touches `POST /:projectId/members` at line ~221 + a new route registration block — different region; no symbol overlap. But CR-062 dispatcher MUST rebase on the post-BUG-030 tree.
  - **Shared file with STORY-027-05** (SDR §2.2): `mcp/src/db/schema.ts`. BUG-030 first (line 96-98); STORY-027-05 second (line 92 comment vocabulary). Adjacent; no overlap.
  - **Audit-log cascade comment at schema.ts:168** — out of scope per BUG-030 §3 + SDR §2.5. Do NOT touch.
  - **Drizzle migration filename collision** — confirm next sequence number by `ls mcp/migrations/` before generating. SDR did not pre-resolve this; Developer picks the next free number.

- **DoD references (BUG-030 file):**
  - L106-117 (Execution Sandbox — investigate/modify + DO NOT modify).
  - L121-158 (Verification Protocol — failing test verbatim).
  - L162-171 (Ambiguity Gate — both §0.5 Qs resolved).

---

### CR-062 — Resend invite from members list + email on issue/resend

- **Goal advancement:** Closes the members-list UX gap so admins recover from missed invites without shell access; also wires the existing mailer to the invite-create path so invitees actually receive the URL by email. Pairs with BUG-030 to make the members surface fully self-serve.

- **File surface (by symbol; rebases on post-BUG-030 tree for members.ts):**

  **Server (mcp/):**
  - **Modify** `mcp/src/admin-api/members.ts` (post-BUG-030 tree):
    - Symbol 1: `POST /projects/:projectId/members` handler (existing). Locate the `invite_url: inviteUrl(...)` response-prep at line 221 (verified). IMMEDIATELY before the response is sent, call the new mail helper and capture `mail_sent: boolean`. Include in the response body.
    - Symbol 2 (NEW route): register `POST /:mid/resend-invite` — server-side flow:
      1. `assertProjectOwned` (reuse existing helper used by DELETE handler).
      2. `invites.findActivePendingByMember(memberId)` — see invites.ts add below. If none → `404 { error: 'no_pending_invite' }`. If row exists AND `consumed_at IS NOT NULL` → `409 { error: 'already_redeemed' }`.
      3. `invites.bumpExpiry(inviteId, ttlSec)` returns the new `expires_at`. TTL from env `CLEARGATE_INVITE_TTL_SECONDS` with default per existing code (verify the default value in `mcp/src/config.ts` while implementing; do NOT hardcode 86400 if the existing default is different).
      4. Call the mail helper → `mail_sent: boolean`.
      5. Respond `{ invite_url, invite_token, expires_in, mail_sent }`.
  - **Modify** `mcp/src/admin-api/invites.ts`:
    - Add `findActivePendingByMember(memberId): Promise<Invite | null>` — SELECT most recent invite row for member where `consumed_at IS NULL`, order by `created_at DESC` limit 1. Reuses `idx_invites_project_active` (per CR §2.5 line 107).
    - Add `bumpExpiry(inviteId, ttlSec): Promise<Date>` — UPDATE invites SET expires_at = now() + ttl WHERE id = inviteId; RETURNING expires_at.
  - **Add** `mcp/src/admin-api/invite-email.ts` (NEW): `export async function sendInviteEmail(mailer: Mailer, projectName: string, inviteUrl: string, recipientEmail: string, expiresAt: Date): Promise<boolean>` — wrap mailer call in try/catch; log failures via Fastify logger; return true on success, false on caught throw OR when mailer is the null/skip provider (no API key).
  - **Add** `mcp/src/admin-api/invite-email-template.ts` (NEW): `export function renderInviteEmail(input: { projectName: string; inviteUrl: string; expiresAt: Date }): { subject: string; text: string; html: string }`. Pure function. Subject: `You've been invited to <projectName> on ClearGate`. Body cites URL + ISO expiry.
  - **Modify** `mcp/src/admin-api/schemas.ts`: Zod schema for the resend response (`MemberResendInviteResponse`); extend the existing create-invite response schema to include `mail_sent: boolean`.
  - **Modify** `mcp/src/admin-api/index.ts`: wire the `Mailer` instance through to the members route deps if not already in the deps bag. Verify by reading the deps shape used by `assertProjectOwned` site.
  - **Regenerate** `mcp/src/admin-api/__snapshots__/openapi.test.ts.snap` — snapshot regen via `npm test -- admin-api/openapi --update-snapshots`.
  - **Add tests** `mcp/src/admin-api/members.node.test.ts` (extend or create) and `mcp/src/admin-api/invite-email-template.node.test.ts` (NEW).

  **UI (admin/):**
  - **Add** `admin/src/lib/components/MembersList.svelte` (NEW — locked decision per SDR Open Decision #1):
    - Renders the per-row layout previously inline in `+page.svelte`.
    - Per-row icon buttons via `lucide-svelte`: `Send` icon for Resend (only when `member.status === 'pending'`) AND `Trash2` icon for Remove (always). Reuse `admin/src/lib/components/IconButton.svelte` (verified to exist).
    - Each icon has `aria-label` + native `title` tooltip. Keyboard-activatable.
    - Click handlers dispatch up to the parent route.
  - **Modify** `admin/src/routes/projects/[id]/members/+page.svelte`:
    - Import `MembersList.svelte` and pass the members list.
    - Wire click handlers: Resend → `await apiResendInvite(memberId)` → open `InviteUrlModal` with returned `{ invite_url, expires_at, mail_sent, recipient_email }`. Remove → existing DELETE call.
    - Handle create-invite response's new `mail_sent` field by opening the same `InviteUrlModal`.
  - **Add** `admin/src/lib/components/InviteUrlModal.svelte` (NEW): props `{ inviteUrl, expiresAt, mailSent, recipientEmail }`. Renders the URL with copy button. If `mailSent === true`, green pill `Email sent to <recipientEmail>`. If false, amber pill `Email could not be sent — copy the URL manually`. Close button only — no "I've saved it" gate (URL is reissuable). Reuse `Modal.svelte` for the shell.
  - **Modify** `admin/src/lib/api/members.ts` (or `mcp-client.ts` — Developer verifies which path exists during implementation): add `resendInvite(memberId): Promise<MemberResendInviteResponse>`; extend `createMember` return type to include `mail_sent`.
  - **Add** `admin/src/lib/components/MembersList.test.ts` (NEW per locked decision):
    - Send icon visible only on `pending` rows; Trash2 icon visible always.
    - Both icons have `aria-label` AND `title` attributes.
    - Clicks dispatch the correct callback prop.
  - **Extend** `admin/tests/e2e/members.spec.ts`: issue → close modal → click Send icon → modal reopens with URL + green mail-sent pill.

- **Implementation sketch (ordered):**
  1. Server first (independently testable): add `invites.ts` helpers → `invite-email-template.ts` → `invite-email.ts` → register the resend route + wire the create-invite mailer call → update `schemas.ts` → regenerate OpenAPI snapshot.
  2. Run server tests; ensure 5 new members cases (pending/consumed/missing/non-owner/mail-skip) + 1 mail_sent case on create + template render unit test all pass against the test-mailer (`mailer.fake.ts`).
  3. UI: extract `MembersList.svelte` from the routes page → add `InviteUrlModal.svelte` → wire `+page.svelte` → add `MembersList.test.ts` → extend e2e.
  4. Manual smoke per CR §4 lines 165-174.

- **Test shape (Red tests for QA-Red):**

  Server-side (against real Postgres via docker-compose):
  1. `POST /members/:mid/resend-invite · pending invite · returns 200 with same UUID and bumped expires_at; test-mailer received exactly one send call`.
  2. `POST /members/:mid/resend-invite · consumed invite · returns 409 with error: "already_redeemed"`.
  3. `POST /members/:mid/resend-invite · no pending invite · returns 404 with error: "no_pending_invite"`.
  4. `POST /members/:mid/resend-invite · non-owner project · returns 404` (assertProjectOwned reuse — same 404 shape as DELETE).
  5. `POST /members/:mid/resend-invite · CLEARGATE_RESEND_API_KEY unset · returns 200 with mail_sent: false; mailer skip path; URL still returned`.
  6. `POST /projects/:projectId/members · create-invite now emits one mailer send call AND returns mail_sent: true`.
  7. `renderInviteEmail · returns subject containing project name, body containing inviteUrl AND ISO expiresAt`.

  Frontend:
  8. `MembersList · pending row · renders Send AND Trash2 icons; both have aria-label and title`.
  9. `MembersList · active row · renders only Trash2 icon; no Send`.
  10. `MembersList · Send icon click · dispatches resend handler with memberId`.

  E2E:
  11. `members.spec · issue invite → email sent pill green → close modal → click Send → modal reopens → green pill again`.

- **Risks / wiring traps:**
  - **Shared file with BUG-030** (SDR §2.2): `mcp/src/admin-api/members.ts`. **CR-062 dispatch waits on BUG-030 merge.** Different region (lines ~221 + new route registration vs. line ~250 DELETE), but rebase risk if line numbers shift — anchor by symbol (`POST /projects/:projectId/members` handler vs. `DELETE /:mid` handler).
  - **Mailer fall-through to a stub when no API key** (CR §0.5 Q4 line 58): `sendInviteEmail` must return `false` (not throw, not skip silently with no return) when (a) `CLEARGATE_RESEND_API_KEY` unset, OR (b) mailer throws at runtime. The handler then returns 200 with `mail_sent: false`. UI shows amber pill. The URL is the canonical handoff; email is convenience.
  - **OpenAPI snapshot regen** — running `--update-snapshots` blindly is fine here (legitimate API surface addition), but inspect the diff before commit. New entries only; no existing entries should change shape.
  - **MembersList.svelte locked extraction** (SDR Open Decision #1) — the CR file says "(verify name)" in four places; the locked decision is EXTRACT (do NOT inline-edit `+page.svelte`'s row markup). Do this even if the inline path looks shorter — `MembersList.test.ts` is named in CR §2 line 96.
  - **`lucide-svelte` already in deps** (CR §2.5 line 114). Do NOT add a new icon package. Use `Send` + `Trash2` named exports.

- **DoD references (CR-062 file):**
  - L67-89 (New Logic — server + UI).
  - L117-141 (Execution Sandbox modify/add server + UI).
  - L146-180 (Verification Protocol — 4 backend + 3 frontend + e2e + manual smoke).
  - L195-201 (Ambiguity Gate — all 4 §0.5 Qs resolved).

---

### CR-063 — Ingest sprint reports into wiki

- **Goal advancement:** First half of the EPIC-027 proof loop. Sprint reports become a second wiki source class, paving the way for CR-064 (Wave 3) which inserts the MCP-push step IMMEDIATELY BEFORE this CR's wiki-ingest step in `close_sprint.mjs`. Symbolic anchoring must hold for CR-064 to find the insertion point reliably.

- **File surface (by symbol, verified by grep):**
  - **Modify** `cleargate-cli/src/commands/wiki-ingest.ts` (verified at lines 66-72, 102-113, 240, 345):
    - Symbol: `const EXCLUDED_SUFFIXES = [...]` at line 66 — keep `.cleargate/sprint-runs/` in the list. Add an allowlist constant `const SPRINT_REPORT_FILENAMES = ['REPORT.md', /^SPRINT-\d{2,}_REPORT\.md$/] as const;` (or equivalent shape — string + regex pair).
    - Symbol: the path-validator block at lines 102-113 (the section that rejects anything outside `.cleargate/delivery/` with `exit(2)`). Extend to accept TWO permitted roots: `.cleargate/delivery/` OR `.cleargate/sprint-runs/SPRINT-NN/<allowlisted-filename>`. Order matters: the allowlist carve-out must run BEFORE the `EXCLUDED_SUFFIXES` check.
    - Symbol: `function buildPageBody` at line 345 — extend to compose `<plan-stub>` + optional `<!-- BEGIN sprint-report -->\n## Sprint Report\n\n<report-body>\n<!-- END sprint-report -->`. Behavior:
      - When ingesting a PLAN: if existing page has a `BEGIN sprint-report ... END sprint-report` block, preserve it verbatim; ONLY rewrite the plan-stub portion above it.
      - When ingesting a REPORT: rewrite ONLY the delimited block; preserve the plan-stub portion above it. Update frontmatter fields `report_raw_path` + `last_report_ingest_commit`.
  - **Modify** `cleargate-cli/src/wiki/scan.ts` (verified at lines 21-28): same `EXCLUDED_SUFFIXES` constant. Extend scan to walk `sprint-runs/SPRINT-NN/` for the two allowlisted filenames during backfill mode. Default scan (non-backfill) unchanged.
  - **Modify** `cleargate-cli/src/wiki/derive-bucket.ts`: extend signature to accept the relative path; when path matches `.cleargate/sprint-runs/SPRINT-NN/<allowlisted-filename>`, derive `id = 'SPRINT-NN'` from the parent directory name regardless of basename, `type = 'sprint'`, `bucket = 'sprints'`.
  - **Modify** `cleargate-cli/src/wiki/page-schema.ts`: extend `WikiPage` interface with optional `report_raw_path?: string` and `last_report_ingest_commit?: string`. Update `serializePage` + `parsePage` to round-trip these fields.
  - **Verify (read-only check)** `cleargate-cli/src/lib/wiki/contradict.ts`: confirm Phase-4 neighborhood selector includes the report block content when scoring. If the contradict logic reads only the plan stub today, extend to read the full page body (incl. the delimited block). Do NOT refactor; minimal edit only.
  - **Add** `cleargate-cli/scripts/backfill-sprint-reports.mjs` (NEW): iterate `SPRINT-03` through `SPRINT-26` (existing range — newer sprints are in-flight). For each, resolve report filename via the legacy-fallback rule from `lib/report-filename.mjs` (CR-022, reused unchanged). Run `cleargate wiki ingest <resolved-path>`. Output: per-sprint result line (`SPRINT-NN: ingested` / `SPRINT-NN: no report file`) + aggregate `<N> ingested, <M> unchanged, <K> skipped`. Idempotent.
  - **Modify** `.cleargate/scripts/close_sprint.mjs` (verified Step 7 ends at line 745, Step 8 begins at line 749):
    - Insert a NEW block "Step 7.5: wiki ingest sprint report" BETWEEN Step 7 (ends line 745) and Step 8 (starts line 749).
    - The block: write a header comment `// ── Step 7.5: wiki ingest sprint report ─────────────────`, `process.stdout.write('Step 7.5: ingesting sprint report into wiki...\n');`, resolve the report path via the same legacy-fallback rule, skip silently when the report file is absent (legacy sprints), run `cleargate wiki ingest <path>` via the existing CLI binary resolution pattern used by Step 7, log success / warning per Step 7's shape. The Step 7.5 header comment MUST be a literal anchor; do NOT use a generic comment. **Anchor format for Wave 3 CR-064 to find**: `// ── Step 7.5: wiki ingest sprint report ─────────────────` — exact dashes match Step 7's pattern. CR-064 inserts ITS step at "Step 7.4" IMMEDIATELY BEFORE this anchor.
  - **Modify** `cleargate-planning/.cleargate/scripts/close_sprint.mjs`: APPLY THE IDENTICAL Step 7.5 insertion (mirror parity per FLASHCARD 2026-05-04 `#mirror #parity`).
  - **Do NOT touch** `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` — regenerated by `npm run prebuild` (FLASHCARD 2026-05-01 `#scaffold #mirror #prebuild`). Verify post-build that the regenerated payload contains Step 7.5.
  - **Add tests:**
    - `cleargate-cli/test/wiki/sprint-report-ingest.node.test.ts` (NEW; co-located per FLASHCARD 2026-04-24 `#test-location #wiki #cli`): cases (a)–(i) from CR §3.
    - `cleargate-cli/test/wiki/sprint-report-scan.node.test.ts` (NEW): scan picks up sprint-runs report files in backfill mode; default scan unchanged.

- **Implementation sketch (ordered):**
  1. Add allowlist constant + carve-out in `wiki-ingest.ts`'s path validator (lines 102-113 region). Path validator now accepts two roots before the `EXCLUDED_SUFFIXES` check.
  2. Extend `derive-bucket.ts` and `page-schema.ts` minimally — these unblock test fixtures.
  3. Extend `buildPageBody` at line 345 with the two-source idempotency rule. Read existing page if present; preserve plan-stub vs. report-block per source. Use HTML comment delimiters as the marker.
  4. Author the test cases against fixtures — they will fail; iterate `buildPageBody` until green.
  5. Modify `scan.ts` to surface allowlisted sprint-runs files for backfill.
  6. Author `backfill-sprint-reports.mjs`; smoke against real `.cleargate/sprint-runs/` tree.
  7. Insert Step 7.5 into both `close_sprint.mjs` mirrors. Diff them: `diff .cleargate/scripts/close_sprint.mjs cleargate-planning/.cleargate/scripts/close_sprint.mjs` should show only the same Step 7.5 block in both (plus any pre-existing divergence, untouched).
  8. Run `npm run prebuild` from `cleargate-cli/`; verify the regenerated templates payload contains Step 7.5.
  9. Run `cleargate wiki lint` after backfill; assert no drift.

- **Test shape (Red tests for QA-Red):**
  1. `wiki ingest <report-path> · report-only · creates wiki page with sprint-report block + report_raw_path frontmatter`.
  2. `wiki ingest plan then report · single page contains both plan stub AND report block`.
  3. `wiki ingest report then plan · same — both blocks present after second ingest, plan stub didn't clobber report block`.
  4. `wiki ingest plan a second time · preserves report block verbatim` (idempotency contract).
  5. `wiki ingest report a second time · preserves plan stub verbatim`.
  6. `wiki ingest · idempotent no-op when neither raw source SHA changed`.
  7. `wiki ingest .cleargate/sprint-runs/SPRINT-12/REPORT.md · legacy filename works · id derived as SPRINT-12`.
  8. `wiki ingest .cleargate/sprint-runs/SPRINT-26/SPRINT-26_REPORT.md · canonical filename works · id derived as SPRINT-26`.
  9. `wiki ingest .cleargate/sprint-runs/SPRINT-26/token-ledger.jsonl · path validator rejects · exit code 2`.
  10. `wiki scan in backfill mode · picks up SPRINT-NN/REPORT.md and SPRINT-NN/SPRINT-NN_REPORT.md; ignores token-ledger.jsonl + plans/ + scripts/`.
  11. `close_sprint.mjs Step 7.5 · invoked after Step 7 + before Step 8; absent report file logs skip without failing`.

- **Risks / wiring traps:**
  - **Mirror parity** (SDR §2.2 + FLASHCARD 2026-05-04 `#mirror #parity`): `close_sprint.mjs` lives in TWO places. The Step 7.5 insertion must be byte-identical in both. Do NOT reconcile any pre-existing divergence between the two files as a side effect. Diff the two mirrors before commit; only the Step 7.5 block should be a new common addition.
  - **CR-064 anchor for Wave 3** (SDR §2.2 + §2.3): the literal comment `// ── Step 7.5: wiki ingest sprint report ─────────────────` is CR-064's insertion anchor. Do NOT use a generic comment. CR-064's Developer will grep for this exact string.
  - **Allowlist runs BEFORE exclusion** — order matters in the path validator. If the `EXCLUDED_SUFFIXES` check fires first, `.cleargate/sprint-runs/` still rejects the allowlisted files. Test case #1 will fail until this ordering is correct.
  - **Plan-ingest preserves report-block** (CR §0.5 Q1) — this is the asymmetric contract. The two raw sources have independent commit SHAs; idempotency is per-source (`last_ingest_commit` for plan vs. `last_report_ingest_commit` for report). Test case #4 is the canary.
  - **Backfill scope** (CR §1): SPRINT-03..SPRINT-26. SPRINT-27 is in-flight; do NOT pre-ingest its report. The backfill script must hard-stop at SPRINT-26 — either by iterating an explicit list OR by skipping any sprint whose plan has `status: Active`.
  - **Frontmatter `cites` field flashcard** (FLASHCARD 2026-04-19 `#wiki #schema #lint`): `WikiPage` schema deliberately omits `cites` (topic-page-only); CR-063 adds `report_raw_path` + `last_report_ingest_commit` which are sprint-page-only by analogy. If lint flares on these, the lint rule may need scope-by-bucket — surface as Blockers Report (`spec-gap`) before patching lint.

- **DoD references (CR-063 file):**
  - L86-93 (New Logic — both ingest sources, body composition, backfill).
  - L122-136 (Execution Sandbox — modify/add lists).
  - L142-178 (Verification Protocol — backfill + smoke + manual + lint).
  - L182-192 (Ambiguity Gate — all 5 §0.5 Qs resolved).

## Wave 1 ordering recap (from SDR §2.2)

All four items run in **one Wave 1 dispatch turn**. Within the wave, two file-surface constraints serialize a subset:

| Order constraint | Files | Reason |
|---|---|---|
| **BUG-030 → CR-062** | `mcp/src/admin-api/members.ts` | Different region (DELETE handler vs. new POST resend-invite route + create-invite mailer call), but rebase risk. CR-062 dispatcher MUST wait for BUG-030 merge before opening its worktree. |
| **BUG-030 → STORY-027-05** | `mcp/src/db/schema.ts` | BUG-030 edits line 96-98 (FK clause + migration); STORY-027-05 edits line 92 (type-column vocabulary comment). Adjacent regions; BUG-030 first because it ships with a migration. (STORY-027-05 is not in this M-plan but the ordering affects orchestrator scheduling.) |
| **Independent of all others** | CR-065 (cleargate-cli/) | Lives entirely in the CLI tree; no overlap with the mcp/ admin-api or schema edits. Can run first OR in parallel with BUG-030. |
| **Independent of all others** | CR-063 (cleargate-cli/ wiki + close_sprint.mjs) | Wiki + scaffold-script edits; no admin-api or mcp/ schema overlap. Can run first OR in parallel. |

**Recommended dispatch order to maximize parallelism:**
1. **Spawn together:** CR-065 + BUG-030 + CR-063 (no inter-dependencies).
2. **Spawn after BUG-030 merges:** CR-062.

All four merge into `sprint/S-27` before Wave 2 (the EPIC-027 strict-serial push-item.ts chain) opens.

## Open questions for orchestrator

> Per the SDR, three open decisions were already resolved (CR-062 component name = extract `MembersList.svelte`; STORY-027-04 audit_log = Wave 2; STORY-027-05 glob = fast-glob). No NEW gaps surfaced during M1 blueprint synthesis.

**Zero open questions.** All four items are dispatch-ready.

---

_End of M1 plan. Wave 2 (EPIC-027 strict-serial chain) and Wave 3 (CR-061 + CR-064 follow-on pair) will be authored as separate M-plans (`M2.md`, `M3.md`) when the orchestrator dispatches the next Architect turn._

### M2.md

# M2 — Wave 2 Plan (Strict-Serial 4-Story Chain)

> Sprint: SPRINT-27 · Milestone: M2 (Wave 2) · Mode: `execution_mode: v2`
> Authored by Architect 2026-05-15 per SDR §2 binding plan + 2026-05-15 human-locked decisions in commit `b038ec4`.
> Scope: STORY-027-01 → STORY-027-02 → STORY-027-03 → STORY-027-04. All four touch `mcp/src/tools/push-item.ts` and the new `mcp/src/lib/payload-contract.ts`; strict serial rebase chain. No parallelization.
> Inner-repo: all code work happens inside `/Users/ssuladze/Documents/Dev/ClearGate/mcp/` on inner-repo branch `sprint/S-27` (HEAD currently `b960448` after Wave 1).

## Cross-cutting

Pulled from `sprint-context.md`, M1 plan, and FLASHCARDs grepped for this wave. Mirror as one-line bullets — every Developer dispatch in M2 starts from this same page.

- **L0 Code-Truth (CR-028).** Codebase is source of truth; on any conflict between this M-plan and code, the code wins — re-read and surface a Blockers Report (`spec-gap` category) before patching.
- **Strict-serial chain.** No story in M2 may open a worktree until its predecessor has merged to `sprint/S-27` (inner mcp repo). Each story rebases on the post-prior-merge tree.
- **node:test is the default for new tests (`feedback_node_test_over_vitest`).** All four stories' NEW test files use `*.node.test.ts` naming via `tsx --test`. The existing `src/tools/push-item.test.ts` (vitest, 269 LOC) STAYS. Do NOT add new vitest cases for this work; co-locate Red/Green tests in the new `*.node.test.ts` files referenced per story.
- **Real-infra DB tests.** Drizzle hits docker-compose Postgres 18; do NOT mock `db`. Reuse the seed helpers at `src/tools/push-item.test.ts:44-58` pattern when porting to the new node:test files.
- **Mirror parity is per-edit.** Wave 2 stories edit ONLY inner-mcp surfaces (`mcp/src/...`) — no `cleargate-planning/` mirror exists for these paths. Exception: STORY-027-03 R8 edits `cleargate-cli/src/commands/push.ts` (outer repo, no mirror). No `close_sprint.mjs`-style mirror duplication in M2.
- **Drizzle migrations are generated, not hand-rolled (FLASHCARD 2026-05-15 `#migration #drizzle #snapshot`).** STORY-027-04's audit_log migration uses `npx drizzle-kit generate` to keep the meta-snapshot chain intact. Do NOT hand-write the `.sql` file.
- **Postgres 18 NOT-NULL constraints (FLASHCARD 2026-05-15 `#schema #migration #postgres`).** Not relevant for M2 — Wave 2 adds nullable columns only; no DROP NOT NULL in this wave.
- **Never `--no-verify`.** Pre-commit hook clean (`npm run typecheck` + `npm test`) before commit. Fix root cause if a check fails.
- **One commit per story.** Format: `feat(EPIC-027): <story-id> <short desc>`.

## Goal anchor

> Open MCP to any work-item type, prove it by syncing ClearGate's own sprint plans + reports to MCP, lock the codebase/PM-tool boundary, and give the admin console a copy-paste "connect an external agent" snippet that works for both HTTP and stdio (Claude Desktop) clients.

Per-story advancement clauses (verbatim from SDR §2.1):

- **STORY-027-01** — Opens the type validator from a closed six-type enum to a normalized lowercase-kebab string + creates `KNOWN_TYPES` advisory registry including `'sprint'` + `'sprint_report'`. Precondition for CR-064's "zero MCP code change to sync sprint artifacts" smoke (Wave 3).
- **STORY-027-02** — Hardens the payload contract with four guiding L1 rejects (`reserved_key`, `type_change_forbidden`, `payload_too_large`, `approved_not_boolean`) so AI-agent callers can self-correct without spelunking the source. Locks the `{code, message, hint}` error shape that -03 + -04 extend.
- **STORY-027-03** — Splits gate policy by `payload.origin` so adapter-driven pushes (`adapter:linear`, `system:sync-status`) bypass approved-gate + cached_gate_result automatically; retires the brittle `skipApprovedGate` hand-flag in production callers. Required for Jira/Linear/Azure adapter epics later.
- **STORY-027-04** — Adds the L2 warnings array (`unknown_type`, `missing_recommended_fields`, `unknown_id_format`) + `warningCode` + `origin` columns on `audit_log` — observability primitive that makes EPIC-027's headline metric (zero MCP code change to add a type) measurable per project per origin.

## Chain symbol map — `mcp/src/lib/payload-contract.ts` (NEW MODULE — created by -01)

`mcp/src/lib/` directory does NOT exist today. STORY-027-01 creates the parent dir AND the file. Subsequent stories extend the same file in symbol-anchored append regions.

| Story | Adds to `payload-contract.ts` |
|---|---|
| **-01** | `TYPE_REGEX: RegExp` (`/^[a-z][a-z0-9_-]*$/`); `KNOWN_TYPES: readonly string[]` literal `['initiative', 'epic', 'story', 'bug', 'cr', 'proposal', 'sprint', 'sprint_report']` (underscore on `sprint_report` — locked per SDR §2.3 + SPRINT-27 §1 cross-thread note); `normalizeType(input: string): string`; **forward-declared** `interface ValidationErrorShape { code: string; message: string; hint: string }` + minimal class stub that -02 will replace with the full `class ValidationError extends Error`. -01 ALSO emits `// TODO(SPRINT-28): extract to @cleargate/types` comment on `KNOWN_TYPES` (R7). |
| **-02** | `RESERVED_PAYLOAD_KEYS: readonly string[]` literal `['cleargate_id', 'type', 'server_pushed_at_version', 'pushed_by', 'pushed_at']`; `MAX_PAYLOAD_BYTES_DEFAULT = 1048576`; **full** `class ValidationError extends Error { readonly code: string; readonly hint: string; constructor(input: {code, message, hint}) }` (replacing -01's stub; same shape so -01's call site `throw new ValidationError({...})` requires zero edit). |
| **-03** | `ORIGIN_CLEARGATE_CLI = 'cleargate-cli' as const`; `originRequiresGates(origin: string \| undefined): boolean { return (origin ?? ORIGIN_CLEARGATE_CLI) === ORIGIN_CLEARGATE_CLI; }`. |
| **-04** | `CLEARGATE_ID_TYPE_REGEX = /^[A-Z][A-Z0-9_]*-\d+(-\d+)*$/`; `CLEARGATE_ID_NUMERIC_REGEX = /^\d{5}$/`; `isKnownIdFormat(id: string): boolean`; `Warning = { code: string; message: string; field?: string }` type alias (exported). |

**File location reminder:** `mcp/src/lib/payload-contract.ts`. Co-located test file: `mcp/src/lib/payload-contract.node.test.ts`. Both NEW.

## Chain symbol map — `mcp/src/tools/push-item.ts` (existing, 269 LOC pre-M2)

Anchor by SYMBOL because line numbers shift after each merge. Current line numbers (verified `b960448`) cited only for first-cut Developer orientation.

| Story | Edits `push-item.ts` (by symbol) | Current anchor symbol |
|---|---|---|
| **-01** | Swap the closed `ITEM_TYPES` z.enum at `pushItemInput`. Add `stored_type` to `PushItemResult`. Throw `ValidationError({code: 'invalid_type_format', ...})` on regex fail. | `export const ITEM_TYPES = [...] as const;` (line 7-14) + `type: z.enum(ITEM_TYPES)` (line 18) + `export interface PushItemResult` (line 47-56). Delete the export of `ITEM_TYPES`; replace with `import { TYPE_REGEX, normalizeType, KNOWN_TYPES, ValidationError } from '../lib/payload-contract.js';` |
| **-02** | Insert FOUR L1 reject checks IMMEDIATELY ABOVE the existing `if (!ctx.skipApprovedGate)` block (currently line 109). Order: (1) reserved-key, (2) type-change-forbid (queries existing row by `cleargate_id`; check fires only when row exists AND `currentVersion >= 2 OR created_at_version >= cleargate@0.12.0`), (3) payload-size, (4) approved_not_boolean. Each throws `ValidationError`. | `if (!ctx.skipApprovedGate) {` at line 109. The four new throws go ABOVE this line, AFTER `pushedAt` resolution would happen — but per FLASHCARD discipline, the four checks must NOT hit DB unless required. Type-change-forbid is the only one that needs DB read (use a SELECT before the existing transaction at line 179). |
| **-03** | Wrap the approved-gate block (line 109-114) AND the cached_gate_result block (line 121-139) in `if (originRequiresGates(args.payload.origin as string \| undefined) && !ctx.skipApprovedGate) { ... }`. Replace bare `!ctx.skipApprovedGate` predicate. Rewrite advisory-prefix injection at line 156-175 with idempotent strip-and-replace (regex `/^\[advisory: gate_failed — [^\]]+\]\n/` on existing body). Maintain a module-level `Set<string>` for once-per-process `console.warn` on legacy `skipApprovedGate: true`. | `if (!ctx.skipApprovedGate)` blocks at line 109 + 121; advisory-prefix region line 156-175 (the `if (gateStatus === 'open' && failingCriteria.length > 0)` block). |
| **-04** | Extend `PushItemResult` type with `warnings: Warning[]`. Build the warnings array AFTER all L1 checks pass and AFTER `pushedAt` is computed, BEFORE the transaction at line 179. Return `warnings` on both insert (line 221-228) and update (line 260-267) result branches. | `export interface PushItemResult` (currently line 47-56); insert + update return-object literals at line 221 and 260. |

**Chain note:** -01 shifts no lines materially (single enum→string swap + added field). -02 inserts ~50 LOC above line 109 (push line 109 → ~159). -03 wraps + edits in-place — net LOC delta small but the symbol regions move with -02. -04 extends the result type + return literals — additive.

## Chain symbol map — other files touched

| File | Stories | Edit |
|---|---|---|
| `mcp/src/mcp/register-tools.ts` | **-01** | Line 5 currently `import { ITEM_TYPES, pushItem, PushNotApprovedError, PushGateFailedError } from '../tools/push-item.js';`. Drop `ITEM_TYPES`. The two usages — line 99 (`type: z.enum(ITEM_TYPES)` inside `push_item` inputSchema) AND line 161 (`type: z.enum(ITEM_TYPES).optional()` inside `list_items` inputSchema) — swap to `type: z.string().min(1).max(64).regex(TYPE_REGEX)` (push_item) and `type: z.string().min(1).max(64).regex(TYPE_REGEX).optional()` (list_items). Import `TYPE_REGEX` from `../lib/payload-contract.js`. **SDR §2.3 surfaced this gap — STORY-027-01 §3.1 lists `register-tools.ts` in Related Files (locked decision).** |
| `mcp/src/tools/sync-status.ts` | **-03** | Line 40 currently `{ ...ctx, skipApprovedGate: true }`. Migrate to pass `payload.origin = "system:sync-status"` on the `pushItem` args (NOT ctx) — i.e. add `origin: 'system:sync-status'` to the `nextPayload` object at line 34-37 (or set inside the `pushItem({...})` args literal at line 39-45). Drop `skipApprovedGate: true` from the ctx spread. Type cast at line 43 (`as 'initiative' \| ...`) BECOMES `args.type: current.type` (no narrowing — open-type validator handles it after -01). |
| `mcp/src/tools/pull-item.ts` | **-03** | `class ItemNotFoundError` at line 26-36. Keep the class. Add `hint: string` field — constructor signature changes to compute hint inline. Alternative (cleaner): wire a new structured 404 response shape at the **HTTP layer** (register-tools.ts errorCodeFor + the MCP-protocol response builder). Decision below in -03 blueprint. |
| `cleargate-cli/src/commands/push.ts` | **-03** | Line 221 builds `payloadForPush: Record<string, unknown>`. Insert idempotent stamp BEFORE the `mcp.call` at line 237: `if (payloadForPush['origin'] === undefined) payloadForPush['origin'] = 'cleargate-cli';`. Outer-repo edit; no mirror. |
| `mcp/src/db/schema.ts` | **-04** | The `auditLog` table at line 133-152 has `errorCode: text()` (line 147) — verified. `warningCode` + `origin` columns ABSENT — confirmed by grep. **Migration REQUIRED.** Add both as nullable: `warningCode: text(), origin: text(),`. Migration is the next free numeric (current latest is `0009_aspiring_vapor.sql`; gap at 0008 ignored; -04 generates `0010_<drizzle-name>.sql`). |
| `mcp/src/middleware/audit.ts` | **-04** | `AuditInput` interface (line 4-15) extended with `warningCode?: string \| null` + `origin?: string \| null`. `writeAudit` (line 21-39) insert literal extends with the two new fields, defaulting to `null` when absent. |
| `mcp/src/admin-api/audit.ts` | **-04** | Read-side: the audit-list response shape inferred from `AuditLogRow`. Verify the query at line 125 (`eq(auditLog.projectId, ...)`) still typechecks after the additive columns. No logic change expected; this is a typecheck-confirmation pass, not an edit. |

## Per-story blueprint

### STORY-027-01 — Open-type validator + KNOWN_TYPES (incl. sprint/sprint_report)

- **Goal advancement:** Opens the MCP type validator so any lowercase-kebab string pushes without a Zod-enum bump; seeds `KNOWN_TYPES` with `'sprint'` + `'sprint_report'` so the CR-064 smoke pushes (Wave 3) emit `warnings: []` on those types. First step in the chain — every other Wave-2 story depends on the new module.

- **File surface (by symbol; verified by grep against `b960448`):**
  - **Create** `mcp/src/lib/` directory (does NOT exist today — verified by `ls mcp/src/lib/ 2>&1 → "No such file or directory"`).
  - **Add** `mcp/src/lib/payload-contract.ts` (NEW) exporting:
    - `export const TYPE_REGEX = /^[a-z][a-z0-9_-]*$/;`
    - `export const KNOWN_TYPES = ['initiative', 'epic', 'story', 'bug', 'cr', 'proposal', 'sprint', 'sprint_report'] as const;` — VERBATIM literal (underscore on `sprint_report`, not hyphen; locked per SDR §2.3). Add `// TODO(SPRINT-28): extract to @cleargate/types` comment immediately above this line per R7.
    - `export function normalizeType(input: string): string` — body: `return input.trim().toLowerCase().replace(/\s+/g, '-');` (R2 — whitespace → kebab; mixed-case → lowercase).
    - `export class ValidationError extends Error { readonly code: string; readonly hint: string; constructor(input: {code: string; message: string; hint: string}) { super(input.message); this.name = 'ValidationError'; this.code = input.code; this.hint = input.hint; } }` — STUB form. -02 will REPLACE this class verbatim with the identical implementation (no signature change; -02 just owns the "full" class for symbolic clarity).
  - **Add** `mcp/src/lib/payload-contract.node.test.ts` (NEW; node:test via `tsx --test`).
  - **Modify** `mcp/src/tools/push-item.ts`:
    - Line 7-14: DELETE the `export const ITEM_TYPES` array entirely.
    - Line 18: change `type: z.enum(ITEM_TYPES)` to `type: z.string().min(1).max(64)`. Defer regex enforcement: do NOT use Zod `.regex()` because regex error messages do not carry our `{code, message, hint}` shape. Instead, normalize-then-validate in handler code (inside `pushItem` at the top, before the existing `if (!ctx.skipApprovedGate)` block):
      ```ts
      const normalizedType = normalizeType(args.type);
      if (!TYPE_REGEX.test(normalizedType)) {
        throw new ValidationError({
          code: 'invalid_type_format',
          message: `type '${args.type}' is not a valid type format`,
          hint: 'type must match /^[a-z][a-z0-9_-]*$/ after normalize (1-64 chars)',
        });
      }
      args.type = normalizedType as typeof args.type;  // mutate for downstream use
      ```
      (R1, R2, R5 — single check covers regex failure + empty + leading-digit + over-length; the Zod `.max(64)` already gates length>64 with its own message — match scenarios "Empty type rejected", "Over-length type rejected" to the ValidationError path by tightening: prefer to drop `.min(1).max(64)` from Zod and do all checks in the normalize gate so the error shape is consistent.)
    - Line 47-56 `PushItemResult` interface: ADD `stored_type: string;` field (R3).
    - Line 221-228 + 260-267: include `stored_type: normalizedType` in BOTH return-object literals.
    - Line 1-5 imports: ADD `import { TYPE_REGEX, KNOWN_TYPES, normalizeType, ValidationError } from '../lib/payload-contract.js';` (KNOWN_TYPES unused by -01 but imported here to lock the surface; if linter flags unused, add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` with a comment that -04 will consume it, OR simply leave the import out and let -04 add it — Developer chooses the cleaner path).
  - **Modify** `mcp/src/mcp/register-tools.ts`:
    - Line 5: change `import { ITEM_TYPES, pushItem, PushNotApprovedError, PushGateFailedError }` to `import { pushItem, PushNotApprovedError, PushGateFailedError }`. Add a separate `import { TYPE_REGEX } from '../tools/../lib/payload-contract.js';` or via relative resolution `'../lib/payload-contract.js'`.
    - Line 99 (`push_item` inputSchema, `type: z.enum(ITEM_TYPES)`) → `type: z.string().min(1).max(64)` (regex enforcement moved into pushItem per the design above; the Zod schema stays loose because the throw-with-hint pattern is preferred).
    - Line 161 (`list_items` inputSchema, `type: z.enum(ITEM_TYPES).optional()`) → `type: z.string().min(1).max(64).optional()`.
  - **Modify** `mcp/src/tools/sync-status.ts` line 43: the `as 'initiative' \| 'epic' \| ...` cast becomes obsolete after type opens. Either drop the cast (`type: current.type`) or keep as `as string`. Pick the simpler path: drop the cast.
  - **Add** `mcp/src/tools/push-item.node.test.ts` (NEW; node:test). Existing `push-item.test.ts` (vitest) STAYS.

- **Implementation sketch (ordered):**
  1. `mkdir mcp/src/lib && touch mcp/src/lib/payload-contract.ts`.
  2. Author `payload-contract.ts` with TYPE_REGEX, KNOWN_TYPES (verbatim 8-element literal), normalizeType, ValidationError class.
  3. Author `payload-contract.node.test.ts` with unit cases for `normalizeType` (mixed-case, whitespace, kebab, idempotent) + `KNOWN_TYPES` literal-membership check (includes both `'sprint'` AND `'sprint_report'`).
  4. Edit `push-item.ts`: drop ITEM_TYPES, add imports, loosen Zod, insert normalize-then-validate gate at top of `pushItem`, add `stored_type` to interface + both return literals.
  5. Edit `register-tools.ts`: drop ITEM_TYPES from import, swap both z.enum usages, add TYPE_REGEX import (or skip if Zod-side regex is dropped per the design).
  6. Edit `sync-status.ts` line 43: drop the narrowing cast.
  7. Author `push-item.node.test.ts` with the nine Gherkin scenarios from STORY-027-01 §2.1.
  8. Run `cd mcp && npm run typecheck`; expect clean.
  9. Run `cd mcp && npx tsx --test src/lib/payload-contract.node.test.ts src/tools/push-item.node.test.ts`; expect green.
  10. Run `npm test` in mcp/ to confirm existing vitest suite still passes (legacy six-type pushes unaffected).
  11. Commit: `feat(EPIC-027): STORY-027-01 open-type validator + KNOWN_TYPES`.

- **Test shape (Red tests for QA-Red to author first, mapped to story §2.1 scenarios):**
  1. `payload-contract · KNOWN_TYPES includes "sprint" AND "sprint_report"` — literal membership test (scenario 9).
  2. `payload-contract · normalizeType "Epic" → "epic"` — case fold (scenario 1).
  3. `payload-contract · normalizeType "  Sprint Report  " → "sprint-report"` — trim + kebab (scenario 2).
  4. `payload-contract · normalizeType is idempotent` — `normalizeType(normalizeType(x)) === normalizeType(x)`.
  5. `pushItem · mixed-case type "Epic" stores as "epic" + returns stored_type "epic"` — scenario 1.
  6. `pushItem · type "  Sprint Report  " stores as "sprint-report"` — scenario 2.
  7. `pushItem · unknown valid type "risk-log" succeeds, no warning (yet — -04 adds warnings)` — scenario 3.
  8. `pushItem · type "!!bad type!!" throws ValidationError with code "invalid_type_format", hint "type must match /^[a-z][a-z0-9_-]*$/ after normalize (1-64 chars)"` — scenario 4. Assert `err.code === 'invalid_type_format' && err.hint.includes('1-64 chars')`.
  9. `pushItem · type "1story" throws invalid_type_format` — scenario 5 (leading digit).
  10. `pushItem · type "" throws invalid_type_format` — scenario 6.
  11. `pushItem · type "a".repeat(65) throws invalid_type_format` — scenario 7 (over-length).
  12. `pushItem · each of [initiative, epic, story, bug, cr, proposal] pushes unchanged with stored_type === input` — scenario 8.

- **Risks / wiring traps:**
  - **Zod `.enum()` to `.string().regex()` is a wire-shape change at the MCP-protocol layer too.** MCP clients calling `push_item` with `type: "Epic"` previously got a clean Zod enum error; after this story, the same input gets `ValidationError` instead. Existing vitest tests at `push-item.test.ts:67-71` ("EPIC-001" with type "epic") still pass — they use lowercase already. QA-Verify must run the full vitest suite to confirm.
  - **`stored_type` is a NEW result field.** Existing destructuring `{ pushed_by, pushed_at, version }` is unaffected (additive). Developer must NOT remove existing fields. Future CR-064 smoke (Wave 3) consumes `stored_type` directly.
  - **CR-064 KNOWN_TYPES coupling — locked literal.** SDR §2.3 mandates underscore on `sprint_report`. Hyphen breaks CR-064's smoke assertion of `warnings: []`. Verbatim test: `assert KNOWN_TYPES.includes('sprint_report')` (NOT `'sprint-report'`).
  - **`register-tools.ts` was a gap in original story §3.1.** SDR amended; this M-plan lists it explicitly. Without this edit, `cleargate_push_item` MCP tool still validates against the deleted `ITEM_TYPES` import — typecheck fails.
  - **node:test vs vitest co-existence.** `npm test` in mcp/ currently runs vitest only. Confirm with `cd mcp && cat package.json | grep '"test"'`. If `tsx --test src/lib/*.node.test.ts` is NOT in the npm test script, add it via a sibling `"test:node"` script + a `"test"` composite — OR (preferred for minimum surface) instruct QA-Verify to run both runners manually for M2. Final decision belongs to QA-Verify; Developer ships the `.node.test.ts` file regardless.

- **DoD references (STORY-027-01 file lines 198-204):**
  - All 9 §2.1 Gherkin scenarios green under `tsx --test`.
  - `mcp/src/tools/push-item.ts:7-14` ITEM_TYPES enum removed.
  - `payload-contract.ts` exports KNOWN_TYPES (incl. sprint + sprint_report), TYPE_REGEX, normalizeType.
  - `mcp/dist/` rebuilt; `npm run typecheck` clean.
  - Pre-commit hook clean.

---

### STORY-027-02 — RESERVED_KEYS + type-change forbid + size cap + approved_not_boolean

- **Hard precondition:** Rebase on -01's HEAD before opening worktree. Verify the post-merge tree has `mcp/src/lib/payload-contract.ts` AND `push-item.ts` Zod schema is `z.string()` not `z.enum(ITEM_TYPES)`.

- **Goal advancement:** Locks the L1 error contract — four guiding rejects with `{code, message, hint}` shape that AI-agent callers can act on without source-spelunking. Replaces -01's stub `ValidationError` class with the full implementation, becoming the contract -03 + -04 reuse.

- **File surface (by symbol; line numbers offset by -01's merge — anchor by symbol):**
  - **Modify** `mcp/src/lib/payload-contract.ts` (extend, do NOT rewrite):
    - REPLACE -01's stub `class ValidationError` with the identical signature implementation (this story owns it semantically; no API change). If the stub already had the full body, no edit; if it was a minimal extends-Error, expand to full readonly + name set.
    - APPEND `export const RESERVED_PAYLOAD_KEYS = ['cleargate_id', 'type', 'server_pushed_at_version', 'pushed_by', 'pushed_at'] as const;` (R1).
    - APPEND `export const MAX_PAYLOAD_BYTES_DEFAULT = 1048576;` (R4 — 1 MB per EPIC-027 §6 Q1).
  - **Modify** `mcp/src/tools/push-item.ts`:
    - **Anchor point for the four-check block:** IMMEDIATELY AFTER -01's `normalizeType + TYPE_REGEX.test` block and BEFORE the existing `if (!ctx.skipApprovedGate)` block (currently line 109 pre-M2, will be ~125 post -01). Insert the four checks in order:
      1. **Reserved-key check (R1):**
         ```ts
         const reservedHit = Object.keys(args.payload).find(k => (RESERVED_PAYLOAD_KEYS as readonly string[]).includes(k));
         if (reservedHit) {
           throw new ValidationError({
             code: 'reserved_key',
             message: `payload contains reserved key '${reservedHit}'; this field is stamped by the server`,
             hint: `remove '${reservedHit}' from your payload — the server populates it.`,
           });
         }
         ```
      2. **Payload-size check (R4):**
         ```ts
         const maxBytes = parseInt(process.env.MCP_MAX_PAYLOAD_BYTES ?? '', 10) || MAX_PAYLOAD_BYTES_DEFAULT;
         let payloadBytes = 0;
         try {
           payloadBytes = Buffer.byteLength(JSON.stringify(args.payload), 'utf8');
         } catch {
           throw new ValidationError({ code: 'payload_too_large', message: 'payload not JSON-serializable', hint: 'remove circular references' });
         }
         if (payloadBytes > maxBytes) {
           throw new ValidationError({
             code: 'payload_too_large',
             message: `payload size ${payloadBytes} bytes exceeds limit ${maxBytes}`,
             hint: 'set MCP_MAX_PAYLOAD_BYTES higher, or remove embedded artifacts from the payload',
           });
         }
         ```
      3. **approved_not_boolean check (R5):**
         ```ts
         if (args.payload['approved'] !== undefined && typeof args.payload['approved'] !== 'boolean') {
           const val = args.payload['approved'];
           throw new ValidationError({
             code: 'approved_not_boolean',
             message: `approved must be boolean true, got ${typeof val}: ${JSON.stringify(val)}`,
             hint: `use unquoted YAML: 'approved: true' (not 'approved: "true"')`,
           });
         }
         ```
      4. **Type-change-forbid check (R2 + R3 grandfather):** Requires a DB read BEFORE the existing transaction at line 179. Insert a `SELECT type, currentVersion, currentPayload->'created_at_version' AS createdAtVersion` query against `items` keyed on `(projectId, cleargateId)`. If row exists AND `(currentVersion >= 2 OR semverGte(createdAtVersion, 'cleargate@0.12.0'))` AND `args.type !== row.type` → throw `type_change_forbidden`. Use the existing semver helper if present; FLASHCARD `2026-04-30 #mcp #deps` notes js-yaml is not in mcp/package.json — semver may also be absent. **Verify during dev: `grep -rn "semver\|gte" mcp/src/ mcp/package.json`.** If absent: hand-roll a small `semverGte(a: string, b: string): boolean` in `payload-contract.ts` (strip `cleargate@` prefix, split on `.`, compare three integers). Do NOT add `node-semver` as a dep — keep surface minimal.
  - **HTTP layer error mapping (R6, R7, R8):** The MCP-protocol layer catches `ValidationError` and surfaces 400 with body shape `{code, message, hint}`. `runTool` in `register-tools.ts:67-81` writes the audit_log row via `writeAudit` + `errorCodeFor`. EXTEND `errorCodeFor` at line 58-65 to recognize `ValidationError`: add `if (err instanceof ValidationError) return err.code;` BEFORE the `instanceof Error` fallback. Add `import { ValidationError } from '../lib/payload-contract.js';` at the top of register-tools.ts. **HTTP body shape**: the MCP-protocol layer (which serializes the throw to the JSON-RPC error response) is the SDK; the `error.message + error.code` mapping needs verification — read `@modelcontextprotocol/sdk` usage at `register-tools.ts:90-133` for the push_item handler. If the SDK wraps throws into a generic `McpError`, ValidationError's `{code, message, hint}` needs explicit serialization. Concrete check during dev: throw a ValidationError from a test push, observe the on-wire response shape via the existing `vitest` integration. If the wire shape is `{error: {code, message}}` (no hint), Developer routes ValidationError through a small adapter that re-serializes to top-level `{code, message, hint}` per R8 ("top-level keys, not nested under `error`"). **Surface as Blockers Report (test-pattern) if the SDK forces nesting.**
  - **Modify** `mcp/src/middleware/audit.ts`: no change in -02 (audit pattern already writes errorCode via runTool's catch branch at register-tools.ts:78 — once -02 plugs ValidationError into errorCodeFor, the four new codes flow through with zero audit-helper edits). R7 ("one audit_log row per reject") is already satisfied by the existing pattern. **Confirm by reading the existing flow at register-tools.ts:67-81.**
  - **Add tests:**
    - Extend `mcp/src/lib/payload-contract.node.test.ts` with `ValidationError` constructor shape + RESERVED_PAYLOAD_KEYS literal-membership.
    - Extend `mcp/src/tools/push-item.node.test.ts` with the ten Gherkin scenarios from STORY-027-02 §2.1.

- **Implementation sketch (ordered):**
  1. Extend `payload-contract.ts` with RESERVED_PAYLOAD_KEYS + MAX_PAYLOAD_BYTES_DEFAULT + full ValidationError (replacing stub).
  2. If a semver helper is needed, add `semverGte` to `payload-contract.ts` (≤10 LOC, hand-rolled).
  3. Edit `push-item.ts`: insert the four checks in order between -01's normalize gate and the existing skipApprovedGate block. Type-change check executes BEFORE the existing transaction (so the SELECT happens once; later transaction does its own SELECT FOR UPDATE).
  4. Edit `register-tools.ts:58-65`: add ValidationError to errorCodeFor. Import.
  5. Confirm HTTP wire shape: write one new vitest case under `push-item.test.ts` (existing vitest file) that pushes a reserved key and asserts the on-wire response body has top-level `{code, message, hint}`. If the wire body wraps under `error: {...}`, surface a Blockers Report — this is the only spec-gap risk.
  6. Author the ten node:test cases in `push-item.node.test.ts`.
  7. `npm run typecheck` + `npm test` (vitest) + `npx tsx --test src/lib/payload-contract.node.test.ts src/tools/push-item.node.test.ts`.
  8. Commit: `feat(EPIC-027): STORY-027-02 reserved-keys + type-change + size-cap + approved guard`.

- **Test shape (Red tests for QA-Red to author first):**
  1. `pushItem · payload.server_pushed_at_version → 400 reserved_key with top-level {code, message, hint}` — scenarios 1 + 10.
  2. `pushItem · payload.cleargate_id → 400 reserved_key, message names the key` — scenario 2.
  3. `pushItem · existing item type "story" at currentVersion 2 + new push type "bug" → 400 type_change_forbidden + hint mentions new cleargate_id` — scenario 3.
  4. `pushItem · existing item type "story" + created_at_version "cleargate@0.11.4" + currentVersion 1 + new push type "bug" → succeeds (lazy grandfather); item now type "bug" at v2` — scenario 4.
  5. `pushItem · same legacy item at v2 (now type "bug") + push type "story" → 400 type_change_forbidden` — scenario 5.
  6. `pushItem · existing type "story" v2 + push type "Story" → succeeds (normalize matches)` — scenario 6.
  7. `pushItem · MCP_MAX_PAYLOAD_BYTES unset + 1.1 MB payload → 400 payload_too_large message "1100000 bytes exceeds limit 1048576"` — scenario 7.
  8. `pushItem · MCP_MAX_PAYLOAD_BYTES=4194304 + 2 MB payload → succeeds` — scenario 8.
  9. `pushItem · payload.approved = "true" (string) → 400 approved_not_boolean, message names "string"` — scenario 9.
  10. `pushItem · all four reject paths emit response body with EXACT top-level keys {code, message, hint}` — scenario 10. Snapshot/structural assertion.
  11. **Audit-log assertion (integration-style):** each reject writes exactly one row in `audit_log` with `errorCode = <code>` (use the vitest test pattern at `push-item.test.ts:50-56` which already verifies `errorCode: 'not_approved'` for the existing class).

- **Risks / wiring traps:**
  - **HTTP wire shape uncertainty (R6/R8).** The MCP SDK may force errors into a nested `error: {...}` envelope. R8 demands TOP-LEVEL `{code, message, hint}`. If the SDK forces nesting, two options: (a) surface as Blockers Report (`spec-gap`) and route through Architect for a CR; (b) write a small custom serializer that re-emits at top level. Prefer (a) — do NOT deviate from R8 silently. The existing PushNotApprovedError flows through unchanged today (`errorCode: 'not_approved'` lands in audit_log, but the wire response shape is whatever the SDK emits) — Developer must read the actual response a vitest integration test produces to know.
  - **Grandfather clause edge — `created_at_version` not on every row.** Items created before STORY-006-09's `created_at_version` tracking may have `null`. Treat `null` as legacy → grandfather. Test fixture: insert a row with `payload.created_at_version` UNSET.
  - **Reserved-key check vs. existing CLI write-back (push.ts:177).** push-item.ts line 177 currently stamps `server_pushed_at_version: versionInfo.version_string` into `stampedPayload`. The reserved-key check guards INCOMING payload keys (`args.payload`), not the stamped payload. The check fires BEFORE stamping — verified by ordering in the implementation sketch. No collision risk because the input payload from CLI never includes `server_pushed_at_version` (CLI writes `pushed_by` + `pushed_at` into frontmatter AFTER push; those are also reserved — confirm CLI test fixtures do NOT pre-stamp).
  - **Type-change check fires an additional DB read.** Negligible cost (single SELECT on a unique index) but Developer should ensure it lives OUTSIDE the existing transaction at line 179 — otherwise duplicate transaction-open complicates SELECT FOR UPDATE semantics. Place the check between the four L1 checks and the `// Attribution: resolve member email` block (line 141).
  - **Payload-size check uses JSON.stringify.** Circular references throw inside `Buffer.byteLength(JSON.stringify(...))`. The try/catch wraps; on stringify failure, throw `payload_too_large` with a "not JSON-serializable" message variant. Covered above.

- **DoD references (STORY-027-02 file lines 222-230):**
  - All 10 §2.1 Gherkin scenarios green under `tsx --test`.
  - `RESERVED_PAYLOAD_KEYS`, `ValidationError`, `MAX_PAYLOAD_BYTES_DEFAULT` exported from `payload-contract.ts`.
  - Four new errorCodes emit audit_log rows.
  - Grandfather clause verified by two-pair test (legacy lazy-pass + post-lock hard-reject).
  - `npm run typecheck` clean.
  - Pre-commit hook clean.

---

### STORY-027-03 — Origin-based gate policy split + idempotent advisory prefix + structured pull 404

- **Hard precondition:** Rebase on -02's HEAD. Verify ValidationError class is fully present in `payload-contract.ts`.

- **Goal advancement:** Splits gate policy by `payload.origin` so adapter callers (`adapter:linear`, `system:sync-status`) bypass approved-gate + cached_gate_result automatically; replaces the brittle `skipApprovedGate: true` hand-flag in the two production callers (`sync-status.ts:40` AND `register-tools.ts:106` — see SDR §2.3 + locked decisions).

- **File surface (by symbol; verified by grep against `b960448`, line numbers offset by -01/-02 merges — anchor by symbol):**
  - **Modify** `mcp/src/lib/payload-contract.ts` (extend):
    - APPEND `export const ORIGIN_CLEARGATE_CLI = 'cleargate-cli' as const;`
    - APPEND `export function originRequiresGates(origin: string | undefined): boolean { return (origin ?? ORIGIN_CLEARGATE_CLI) === ORIGIN_CLEARGATE_CLI; }` — single decision point (R3).
  - **Modify** `mcp/src/tools/push-item.ts`:
    - **Anchor for gate-policy wrap:** the existing `if (!ctx.skipApprovedGate) {` block (currently line 109, post -01/-02 offset ~155-160). Two such blocks: line 109 (approved-gate, R1) AND line 121 (cached_gate_result, R1).
    - **Refactor both predicates** to `if (originRequiresGates(args.payload.origin as string | undefined) && !ctx.skipApprovedGate) {`. The `ctx.skipApprovedGate` clause stays as the deprecation alias path (R4).
    - **Deprecation warning (R4):** at module top, add `const _deprecatedSkipApprovedGateSeen = new Set<string>();`. In each of the two predicate-wrapped blocks, at the start of the gate block, IF `ctx.skipApprovedGate === true`: write a once-per-process warning. Key the Set by something stable like the call site label (e.g. `'approved-gate'` and `'cached-gate'`) so each warning fires at most once across the process lifetime. Message: `'skipApprovedGate is deprecated; set payload.origin = "system:<service>" instead'`. Use `console.warn` (server-side stderr — NOT in response per R4).
    - **Idempotent advisory prefix (R6):** the existing advisory-injection block at line 156-175 builds `advisoryBody`. ABOVE the current logic, add the strip step: if `existingBody.match(/^\[advisory: gate_failed — [^\]]+\]\n/)` → strip that leading line BEFORE the H1 match. Concretely:
      ```ts
      const advisoryLineRegex = /^\[advisory: gate_failed — [^\]]+\]\n/;
      let bodyAfterStrip = existingBody.replace(advisoryLineRegex, '');
      // ... existing H1 match runs on bodyAfterStrip, not existingBody
      ```
      Then the H1-match + prepend logic operates on the stripped body, guaranteeing exactly one advisory line at top after re-push.
  - **Modify** `mcp/src/tools/pull-item.ts`:
    - `class ItemNotFoundError` at line 26-36: keep the class; ADD `readonly hint: string` field. Constructor signature changes to `(public readonly cleargate_id: string, public readonly version?: number)` (unchanged from current) but inside the constructor body, set `this.hint = 'push it first, or check the cleargate_id';`.
    - **HTTP layer mapping for structured 404 (R7):** `errorCodeFor` at register-tools.ts:60 already returns `err.code` for ItemNotFoundError (existing code path). The wire-shape question identical to -02's R8 question: does the MCP SDK render `{code, message, hint}` at top level? If -02's Developer already resolved this (either by routing through a custom serializer or by surfacing a Blockers Report), -03 inherits the resolution. If -02 deferred, -03 must resolve here. **Verification path: write a test that pulls a missing id and reads the on-wire body shape.**
  - **Modify** `mcp/src/tools/sync-status.ts`:
    - Line 34-37: extend `nextPayload` to include `origin: 'system:sync-status'`:
      ```ts
      const nextPayload =
        current.payload && typeof current.payload === 'object'
          ? { ...(current.payload as Record<string, unknown>), status: args.new_status, origin: 'system:sync-status' }
          : { status: args.new_status, origin: 'system:sync-status' };
      ```
    - Line 39-45 `pushItem` call: DROP `skipApprovedGate: true` from the ctx spread. The new call becomes `pushItem({ ...ctx }, { cleargate_id: args.cleargate_id, type: current.type, payload: nextPayload })`. (-01 already dropped the type cast at line 43.)
  - **Modify** `mcp/src/mcp/register-tools.ts` (locked decision from prompt — line 106 is NOT a skipApprovedGate setter; it's the `pushItem(...)` call inside the push_item handler):
    - The pushItem call at line 106-115 currently does NOT pass `skipApprovedGate` (verified). Under -03's R2 (missing origin defaults to cleargate-cli), this caller correctly inherits gate firing. **R5 second-bullet decision:** Add an EXPLICIT `payload.origin ??= 'cleargate-cli'` stamp on the wrapper for telemetry clarity. Concretely:
      ```ts
      // ── STORY-027-03: stamp origin default for telemetry clarity ───────
      const stampedArgs = {
        ...args,
        payload: {
          ...args.payload,
          origin: args.payload.origin ?? 'cleargate-cli',
        },
      };
      ```
      Then call `pushItem(..., stampedArgs)` instead of `pushItem(..., args)`. This is additive, telemetry-only, and the CLI's R8 default-stamp would have done the same thing anyway — but defense in depth keeps the audit_log `origin` column populated even if a future caller forgets.
  - **Modify** `cleargate-cli/src/commands/push.ts` (R8 — outer repo edit):
    - Anchor: line 230 (`payloadForPush['body'] = body;`). Insert immediately after:
      ```ts
      // STORY-027-03 R8: stamp origin idempotently (respect any pre-existing user value).
      if (payloadForPush['origin'] === undefined) {
        payloadForPush['origin'] = 'cleargate-cli';
      }
      ```
  - **Test files (NEW node:test files extend the M2 chain; existing `*.test.ts` vitest files stay):**
    - `mcp/src/tools/push-item.node.test.ts` — extend with 10 -03 scenarios.
    - `mcp/src/tools/pull-item.node.test.ts` (NEW).
    - `mcp/src/tools/sync-status.node.test.ts` (NEW).

- **Implementation sketch (ordered):**
  1. Extend `payload-contract.ts` with `ORIGIN_CLEARGATE_CLI` + `originRequiresGates`.
  2. **Audit grep first (R5):** run `grep -rn "pushItem\|cleargate_push_item\|skipApprovedGate" mcp/src/ | grep -v test`. Cite output verbatim in commit message. Expect: -01/-02 already gone through the file; only the two production callers (`sync-status.ts:40` + `register-tools.ts:106`) remain.
  3. Edit `push-item.ts` to wrap both gate-blocks in `originRequiresGates(...) && !ctx.skipApprovedGate`. Add deprecation-warning Set + warn-once logic.
  4. Edit `push-item.ts` advisory-prefix block: add strip-then-prepend logic.
  5. Edit `sync-status.ts` to set `origin: 'system:sync-status'` and drop `skipApprovedGate`.
  6. Edit `register-tools.ts` to add the `stampedArgs` defense-in-depth (R5 second bullet, locked).
  7. Edit `pull-item.ts` `ItemNotFoundError` constructor to add `hint`.
  8. Edit `cleargate-cli/src/commands/push.ts` to stamp origin idempotently at line 230.
  9. Author the three new node:test files with the 10 -03 Gherkin scenarios.
  10. Verify HTTP wire shape for pull-item 404 — if the SDK nests under `error: {...}`, surface Blockers Report (same risk class as -02).
  11. Run typecheck + tests across mcp/ AND cleargate-cli/ (CLI has its own typecheck).
  12. Commit: `feat(EPIC-027): STORY-027-03 origin-based gate policy split + idempotent prefix + structured pull 404`.

- **Test shape (Red tests for QA-Red, ten scenarios from §2.1):**
  1. `pushItem · adapter:linear origin + approved omitted → 200, currentVersion 1, no "not_approved" audit row` — scenario 1.
  2. `pushItem · adapter:jira origin + cached_gate_result.pass=false → 200, body has NO "[advisory: gate_failed —" prefix` — scenario 2.
  3. `pushItem · origin "cleargate-cli" + approved omitted → 400 not_approved` — scenario 3.
  4. `pushItem · origin omitted + approved omitted → 400 not_approved (default origin)` — scenario 4.
  5. `pushItem · ctx.skipApprovedGate=true + approved omitted → 200; console.warn fires once across process` — scenario 5. Stub `console.warn` to count invocations.
  6. `pushItem · origin "system:sync-status" + approved omitted → 200, gates bypassed` — scenario 6.
  7. `pushItem · re-push with stored body already prefixed by "[advisory: gate_failed — discovery-checked]\n" AND cached_gate_result.pass=false → stored body has exactly ONE advisory line at top` — scenario 7.
  8. `pushItem · same item with failing_criteria changed to ["dod-declared"] → stored body has "[advisory: gate_failed — dod-declared]" (criterion updated, single line)` — scenario 8.
  9. `pullItem · cleargate_id "NONESUCH-999" not found → throws ItemNotFoundError with code "item_not_found", hint "push it first, or check the cleargate_id"` — scenario 9. Plus assert HTTP wire shape `{code, message, hint}` top-level if wire-shape question resolved.
  10. `grep audit · skipApprovedGate: true returns zero hits in mcp/src/ (excluding test files + type definition + deprecation comment)` — scenario 10. Implement as a node:test that runs `grep` via `child_process.execFileSync` and asserts on stdout.

- **Risks / wiring traps:**
  - **`skipApprovedGate` audit (SDR §2.3) — verified TWO production callers only.** `sync-status.ts:40` (migrated to origin) + `register-tools.ts:106` (which does NOT currently set skipApprovedGate but inherits the default origin = cleargate-cli). The locked decision (prompt §"Locked decisions") confirms register-tools.ts:106 is the second caller — addressed by the defense-in-depth `stampedArgs` patch. **Test files unchanged** (8+ hits in `pull-item.test.ts`, `list-items.test.ts`, `sync-status.test.ts`, `push-item.test.ts` regression-protect the deprecation-alias path one minor; remove in SPRINT-28+).
  - **Idempotent prefix regex must match the writer's exact format.** Current writer at line 163 emits ``[advisory: gate_failed — ${criteriaStr}]`` — em-dash (U+2014), bracket, no trailing whitespace before `\n`. Regex `/^\[advisory: gate_failed — [^\]]+\]\n/` uses the same em-dash literal. **Developer MUST copy the regex's em-dash from the source line, not type it fresh** — different em-dash variants exist (e.g. en-dash U+2013) and would silently mis-match.
  - **`originRequiresGates(args.payload.origin)` typing.** `args.payload` is `Record<string, unknown>` from Zod's `z.record(z.string(), z.unknown())`. Cast inline: `args.payload['origin'] as string | undefined`. If origin is non-string at runtime (e.g. caller sent number), Zod has already let it through — treat as `undefined` (cast collapses to `as string | undefined` which TS narrows to undefined when not string at the type level, but runtime ambiguity). For defense, harden the helper: `originRequiresGates(typeof origin === 'string' ? origin : undefined)`.
  - **`sync-status.ts` ctx spread without `skipApprovedGate` removes the alias from the syncStatus call path.** Old behavior used the alias; new behavior uses `payload.origin = "system:sync-status"`. The alias-removal is intentional — the new origin signal IS the new mechanism. Test files using `skipApprovedGate: true` directly call `pushItem`, NOT `syncStatus` — they continue to work because the alias path stays alive in push-item.ts (R4).
  - **`register-tools.ts:106` stampedArgs is additive, NOT a gate-bypass.** Setting `origin = 'cleargate-cli'` makes the gate FIRE (per R2 missing-defaults-to-cli already does this; the explicit stamp just makes audit_log's `origin` column reliable). Do not confuse this with gate-bypass — CLI pushes still require `approved: true`.
  - **HTTP wire shape for pull-item 404 (R7).** If the MCP SDK wraps errors into a nested envelope, the `{code, message, hint}` top-level mandate cannot be met without a custom serializer. -02 may already have a fix; -03 inherits. If still unresolved, Blockers Report (`spec-gap`).
  - **CLI default-stamp idempotency (R8).** `payloadForPush['origin'] = 'cleargate-cli'` only sets when undefined — respects pre-existing user value. **Test in cleargate-cli/test/commands/push.node.test.ts** (or extend existing vitest) that a push with frontmatter `origin: "adapter:custom"` survives the stamp.

- **DoD references (STORY-027-03 file lines 212-219):**
  - All 10 §2.1 Gherkin scenarios green.
  - `grep -rn "skipApprovedGate: true" mcp/src/` returns zero in calling-code (excluding type definition + deprecation comment + test files).
  - sync-status.ts migrated to `payload.origin = "system:sync-status"`.
  - cleargate-cli push.ts stamps origin automatically.
  - Re-push test confirms exactly one advisory prefix line.
  - cleargate_pull_item of unknown ID returns `{code, message, hint}` body.
  - `npm run typecheck` clean both in `mcp/` AND `cleargate-cli/`.
  - Pre-commit hook clean.

---

### STORY-027-04 — L1 errorCode taxonomy + L2 warnings + audit_log integration

- **Hard precondition:** Rebase on -03's HEAD. Verify `originRequiresGates` is exported from payload-contract.ts AND CLI stamps origin automatically.

- **audit_log schema decision (per prompt locked rule + grep evidence):** `schema.ts` line 133-152 audit_log table has `errorCode: text()` (line 147) — verified by grep against `b960448`. `warningCode` + `origin` columns ABSENT — confirmed by `grep -nE "warningCode|origin" mcp/src/db/schema.ts` returning zero hits in the auditLog block. **Migration REQUIRED.** Story file §3.1 already declares: "REQUIRED per SDR grep of schema.ts:134-152: errorCode exists, warningCode + origin ABSENT." This M-plan confirms the grep result independently.

- **Goal advancement:** Final wave step — closes EPIC-027 Phase 1 by making the headline metric (zero MCP code change to add a type) observable via warnings telemetry. `audit_log` gains `warningCode` + `origin` columns so the future Admin UI can split adapter-vs-CLI push hygiene by source.

- **File surface (by symbol; line numbers offset by -01/-02/-03 merges — anchor by symbol):**
  - **Modify** `mcp/src/lib/payload-contract.ts` (extend):
    - APPEND `export const CLEARGATE_ID_TYPE_REGEX = /^[A-Z][A-Z0-9_]*-\d+(-\d+)*$/;` (R4 — TYPE-NNN convention).
    - APPEND `export const CLEARGATE_ID_NUMERIC_REGEX = /^\d{5}$/;` (R4 — 5-digit).
    - APPEND `export function isKnownIdFormat(id: string): boolean { return CLEARGATE_ID_TYPE_REGEX.test(id) || CLEARGATE_ID_NUMERIC_REGEX.test(id); }`.
    - APPEND `export type Warning = { code: string; message: string; field?: string };`.
  - **Modify** `mcp/src/tools/push-item.ts`:
    - **Anchor for warning-build block:** AFTER all L1 checks pass (i.e. after -02's four checks + -03's gate-policy split) AND AFTER the `pushedAt = new Date().toISOString();` line (currently line 151 pre-M2, offset post-merges). BEFORE the existing `return ctx.db.transaction(...)` at line 179. Insert:
      ```ts
      // ── STORY-027-04: L2 warnings build ─────────────────────────────────
      const warnings: Warning[] = [];
      const origin = (args.payload['origin'] as string | undefined) ?? ORIGIN_CLEARGATE_CLI;
      // unknown_type: suppressed for non-cleargate origins (R2)
      if (origin === ORIGIN_CLEARGATE_CLI && !(KNOWN_TYPES as readonly string[]).includes(args.type)) {
        warnings.push({
          code: 'unknown_type',
          message: `type '${args.type}' is not in KNOWN_TYPES (advisory)`,
        });
      }
      // missing_recommended_fields (R3)
      const missing: string[] = [];
      if (typeof args.payload['title'] !== 'string') missing.push('title');
      if (typeof args.payload['status'] !== 'string') missing.push('status');
      if (missing.length > 0) {
        warnings.push({
          code: 'missing_recommended_fields',
          message: `recommended fields missing: ${missing.join(', ')}`,
          field: missing.join(', '),
        });
      }
      // unknown_id_format (R4)
      if (!isKnownIdFormat(args.cleargate_id)) {
        warnings.push({
          code: 'unknown_id_format',
          message: `cleargate_id '${args.cleargate_id}' does not match TYPE-NNN or 5-digit conventions (advisory)`,
        });
      }
      ```
    - **PushItemResult interface (line 47-56 pre-M2 + -01's stored_type addition):** ADD `warnings: Warning[];` field.
    - **Return-object literals at lines 221-228 (insert path) AND 260-267 (update path):** ADD `warnings,` to both.
    - **Imports:** ADD `ORIGIN_CLEARGATE_CLI`, `KNOWN_TYPES`, `isKnownIdFormat`, `Warning` to the existing import from `'../lib/payload-contract.js'`.
  - **Modify** `mcp/src/middleware/audit.ts`:
    - **AuditInput interface (line 4-15):** ADD `warningCode?: string | null;` AND `origin?: string | null;`.
    - **writeAudit insert literal (line 21-39):** ADD `warningCode: entry.warningCode ?? null,` AND `origin: entry.origin ?? null,` inside the `.values({...})` call.
  - **Modify** `mcp/src/mcp/register-tools.ts`:
    - **auditBase function (line 37-56):** EXTEND to accept and pass through `warningCode` + `origin`. Signature change: `auditBase(ctx, tool, target, result, errorCode?, warningCode?, origin?)`. **Better path:** widen the existing `errorCode?: string` parameter into an object `extra: { errorCode?: string; warningCode?: string; origin?: string }`. Pick the cleaner refactor; both work. **Locked decision: use the object-parameter form** to avoid positional-arg ambiguity in future extensions.
    - **runTool success path (line 73-76):** after `fn()` succeeds with a PushItemResult that contains warnings, write ONE audit row per warning (looping). Today the success path writes ONE row total (`auditBase(ctx, tool, target, 'ok')`). New behavior: write the success row (no errorCode, with `origin`), then write one additional row per warning (`warningCode = w.code`, `origin = origin`). **Open question for orchestrator below.**
    - **runTool error path (line 77-80):** extend to pass `origin` through `errorCodeFor + auditBase`. The `origin` comes from `args.payload?.origin` if available (push_item handler scope), `undefined` for other tools — gracefully default to `null` in writeAudit.
    - **errorCodeFor (line 58-65):** add `if (err instanceof ValidationError) return err.code;` if -02 didn't already (final-state correctness).
  - **Modify** `mcp/src/db/schema.ts`:
    - **auditLog table (line 133-152):** ADD two columns:
      ```ts
      warningCode: text(),
      origin: text(),
      ```
      Both nullable (no `.notNull()`).
  - **Add migration** `mcp/src/db/migrations/0010_<drizzle-name>.sql` (NEW) — generated via `npx drizzle-kit generate` from inside `mcp/`. **DO NOT hand-write** (FLASHCARD 2026-05-15 `#migration #drizzle #snapshot`). The generated SQL will be approximately:
    ```sql
    ALTER TABLE "audit_log" ADD COLUMN "warning_code" text;
    ALTER TABLE "audit_log" ADD COLUMN "origin" text;
    ```
    Drizzle uses snake-case in SQL but the TS interface stays camelCase via the column-name auto-derivation. Verify the generated SQL matches; if drizzle picks a different name (e.g. `warningCode` mixed-case), accept it.
  - **Modify** `mcp/src/admin-api/audit.ts`:
    - Read-side: `AuditLogRow` (line 5) auto-inferred from schema; new fields surface in the query response. Verify the query at line 125 still typechecks. **No edit expected** — this is a typecheck-confirmation step.
  - **Test files:**
    - Extend `mcp/src/lib/payload-contract.node.test.ts` with `isKnownIdFormat` cases (TYPE-NNN matches, 5-digit matches, "weird-id-format" no match).
    - Extend `mcp/src/tools/push-item.node.test.ts` with the ten -04 Gherkin scenarios.
    - Add `mcp/src/middleware/audit.node.test.ts` (NEW) — verify writeAudit accepts the two new fields.

- **Implementation sketch (ordered):**
  1. Edit `schema.ts` to add the two columns.
  2. Run `cd mcp && npx drizzle-kit generate`. Verify the generated migration ` ` adds both columns nullable. Inspect the meta snapshot to confirm hash chain intact.
  3. Apply migration to local docker-compose Postgres 18.
  4. Edit `payload-contract.ts` to add the two regexes + helper + Warning type.
  5. Edit `middleware/audit.ts` to widen AuditInput + writeAudit.
  6. Edit `register-tools.ts`: refactor auditBase to object-parameter form; in push_item handler, capture `args.payload.origin` AND post-success `result.warnings` and write per-warning audit rows.
  7. Edit `push-item.ts` to build warnings array + extend PushItemResult + populate both return literals.
  8. Author `payload-contract.node.test.ts` extensions + `push-item.node.test.ts` extensions + `audit.node.test.ts`.
  9. Run typecheck + tests; expect green.
  10. Run a manual smoke: `tsx --test src/tools/push-item.node.test.ts --grep "warnings"`.
  11. Commit: `feat(EPIC-027): STORY-027-04 L1 errorCode taxonomy + L2 warnings + audit_log integration`.

- **Test shape (Red tests for QA-Red, ten scenarios from §2.1):**
  1. `pushItem · type "risk-log" + origin "cleargate-cli" + approved → 200; warnings contains {code: "unknown_type", message: "type 'risk-log' is not in KNOWN_TYPES (advisory)"}; audit_log row with warningCode "unknown_type" + origin "cleargate-cli"` — scenario 1.
  2. `pushItem · type "epic-equivalent" + origin "adapter:linear" → 200; warnings is empty; no unknown_type audit row` — scenario 2.
  3. `pushItem · type "sprint" + origin "cleargate-cli" + approved → 200; warnings has NO unknown_type entry. Same for "sprint_report".` — scenario 3 (CR-064 precondition).
  4. `pushItem · approved: true + no title + no status → 200; warnings contains {code: "missing_recommended_fields", message: "recommended fields missing: title, status", field: "title, status"}` — scenario 4.
  5. `pushItem · status: "Done" but no title → warnings contains {code: "missing_recommended_fields", field: "title"}` — scenario 5.
  6. `pushItem · cleargate_id "STORY-027-01" → no unknown_id_format warning` — scenario 6.
  7. `pushItem · cleargate_id "00027" → no unknown_id_format warning` — scenario 7.
  8. `pushItem · cleargate_id "weird-id-format" → 200; warnings contains {code: "unknown_id_format", message: "cleargate_id 'weird-id-format' does not match TYPE-NNN or 5-digit conventions (advisory)"}` — scenario 8.
  9. `audit_log · each L1 reject (invalid_type_format, reserved_key, type_change_forbidden, payload_too_large, approved_not_boolean, not_approved, item_not_found) → exactly one row with errorCode = matching code AND origin populated` — scenario 9. Requires running all six rejects sequentially and querying audit_log.
  10. `pushItem · known type + title + status + valid id + origin cleargate-cli → 200; warnings is empty []; no L2 audit rows for this push` — scenario 10.
  11. `migration · audit_log has warningCode + origin columns post-migrate` — integration check via DB introspection query.

- **Risks / wiring traps:**
  - **One-row-per-warning vs. one-row-per-push trade-off (OPEN QUESTION).** Today's audit_log writes ONE row per RPC call (success OR error). STORY-027-04 R6 + R7 say "every L2 warning writes one row" — three warnings = three audit rows + the success row = four rows per push. This is a 4x volume jump for warning-heavy pushes (e.g. CR-064 smoke pushes some adapter-imported issue with unknown type + missing fields + unconventional id). **Surface to orchestrator below.** Alternative: write ONE row per push with a JSONB `warnings` column. Story scenarios 1 + 9 EXPLICITLY assert "one audit_log row per warning" — taking the row-per-warning path. The volume concern is parking-lot per story §1.5 risk #2.
  - **Drizzle migration name unpredictability.** `npx drizzle-kit generate` auto-names files (e.g. `0010_aspiring_vapor.sql`). Developer accepts the name; do NOT rename post-generate (breaks meta snapshot hash chain per FLASHCARD 2026-05-15).
  - **`warningCode` column name camelCase vs snake_case.** Drizzle TS interface camelCase; SQL snake_case (`warning_code`). The `text()` declaration with no `.column()` override means Drizzle auto-converts. Verify the generated SQL uses snake_case; if it doesn't, the table column name comes through as-is. Tests must read the column via Drizzle's `auditLog.warningCode` (camelCase) — never raw SQL with the wrong case.
  - **`origin` column reused name with `payload.origin`.** Two distinct surfaces: the audit_log `origin` column (TEXT) snapshots the payload value at push-time. They share semantics but are not foreign-keyed. Tests must query the column directly (`SELECT origin FROM audit_log`), not the items table.
  - **Per-warning row writes happen in `runTool` success path.** Today's success path writes one row total. The change widens the success path's audit logic. Two new error modes: (a) post-success audit write fails (existing `writeAudit` swallows error per audit.ts:35-38 — preserved); (b) warnings array is `undefined` from non-push tools (other tools' results don't carry warnings). Guard: only loop warnings if `result && typeof result === 'object' && 'warnings' in result && Array.isArray(result.warnings)` — keep the runTool generic across all tools.
  - **CR-064 precondition.** Scenario 3 is the CR-064 dependency check. If KNOWN_TYPES literal in payload-contract.ts has hyphen on `sprint-report` instead of underscore, scenario 3 fails. -01's literal is the truth; this test reads it.
  - **`auditBase` refactor to object-parameter form is a register-tools.ts internal API change.** No external callers; all four `auditBase(...)` invocations are in the same file (line 75, 78). Mechanical rename; ensure all four call sites use the new shape.

- **DoD references (STORY-027-04 file lines 214-221):**
  - All 10 §2.1 Gherkin scenarios green.
  - `PushItemResult.warnings` exposed on success responses.
  - Three warning codes (`unknown_type`, `missing_recommended_fields`, `unknown_id_format`) emit correctly.
  - `audit_log` rows include `warningCode` + `origin` columns.
  - KNOWN_TYPES verified to include sprint + sprint_report.
  - `npm run typecheck` clean; migration applied.
  - Pre-commit hook clean.

---

## Wave 2 ordering recap

Strict serial; one Developer dispatch at a time. Each dispatch waits for the prior story's commit on `sprint/S-27` (inner mcp repo).

| Step | Story | Anchor in M2 chain | Architect role |
|---|---|---|---|
| 1 | STORY-027-01 | Creates `mcp/src/lib/payload-contract.ts`; loosens Zod type schema; adds `stored_type` to PushItemResult; swaps `register-tools.ts` ITEM_TYPES; drops sync-status.ts type cast. | TPV if QA-Red Red tests authored. |
| 2 | STORY-027-02 | Rebases on -01. Adds RESERVED_PAYLOAD_KEYS + ValidationError full class + MAX_PAYLOAD_BYTES_DEFAULT. Inserts four L1 reject checks. Extends register-tools.ts errorCodeFor with ValidationError. | TPV; route Blockers Report if HTTP wire shape forces nesting. |
| 3 | STORY-027-03 | Rebases on -02. Adds ORIGIN_CLEARGATE_CLI + originRequiresGates. Wraps both gate blocks. Idempotent advisory prefix. Migrates sync-status.ts + register-tools.ts:106. CLI default-stamp in cleargate-cli/. Structured 404 in pull-item.ts. | TPV; verify the grep audit in commit message. |
| 4 | STORY-027-04 | Rebases on -03. Adds CLEARGATE_ID regexes + Warning type. Builds warnings array. Extends PushItemResult. Widens AuditInput + writeAudit. Refactors auditBase. Generates migration 0010_*.sql for audit_log warningCode + origin columns. | TPV; verify migration generated (not hand-rolled). |

## Cross-story risks (chain-level)

- **Bug in -01 forces re-test of -02..-04.** SDR §2.3 + Risks table mitigation: lock KNOWN_TYPES literal verbatim in -01; QA-Red writes Red tests for open-type + normalize BEFORE -01 implementation; high-care merge for -01 even though bounce-exposure is rated low.
- **HTTP wire shape uncertainty for ValidationError (R8 from -02) + ItemNotFoundError (R7 from -03).** The MCP SDK may force errors into nested `error: {...}` envelopes. R8 demands top-level `{code, message, hint}`. Mitigation: first Developer to hit the wire-shape question surfaces a Blockers Report (`spec-gap`); the resolution applies to both stories. Do NOT silently nest.
- **`auditBase` object-parameter refactor in -04 is touched by every tool call site in register-tools.ts.** Mechanical but mechanical-rename failures (e.g. forgetting one of the four call sites) break audit_log writes silently. Mitigation: typecheck catches the call-shape change; ensure ALL four `auditBase(...)` invocations get the new shape in the same commit.
- **`KNOWN_TYPES` literal is the CR-064 precondition.** Scenario 3 in -04 + the smoke pushes in CR-064 (Wave 3) both assert `'sprint_report'` (underscore). If -01 ships with `'sprint-report'` (hyphen), Wave 3 detects the bug — but the bounce is 3 stories back. Mitigation: -01's M-plan cites underscore VERBATIM; QA-Verify on -01 reads the array literal char-by-char.
- **Migration 0010 timing.** -04 lands after -01/-02/-03. Migration 0010 runs on first server boot after merge. Smoke check: `docker compose down && docker compose up postgres && cd mcp && npm run db:migrate && psql -c "\d+ audit_log"` should show `warning_code` + `origin` columns.
- **Test runner duality.** Existing vitest suite (mcp/src/tools/*.test.ts) + new node:test files (mcp/src/tools/*.node.test.ts + mcp/src/lib/*.node.test.ts). Confirm `npm test` in mcp/ runs both, or surface a CR to extend the npm script. **Pre-Wave-2 verification:** `cd mcp && cat package.json | grep '"test"'` — if node:test is not wired, Developer for -01 either adds a sibling script or QA-Verify runs both manually for M2.

## Open questions for orchestrator

Two items require human/orchestrator response before -04 dispatch. -01/-02/-03 are dispatch-ready.

1. **STORY-027-04 audit_log row-per-warning vs. row-per-push.** Story scenarios 1 + 9 explicitly assert "one audit_log row per warning" (row-per-warning). This is a 4x audit-log volume jump on warning-heavy pushes vs. the current one-row-per-RPC pattern. Alternative: write one row per RPC with a JSONB `warnings` column for batched advisory codes. M-plan defaults to row-per-warning per spec; surface as confirmation. Recommend: KEEP row-per-warning (matches the L1 reject pattern — one row per errorCode — and gives the future Admin UI a clean filter). Volume mitigation is the parking-lot per story §1.5 risk #2.
2. **`mcp/` `npm test` runner duality.** Existing test script is vitest-only (verify with `cat mcp/package.json`). Wave 2 adds five new `*.node.test.ts` files via `tsx --test`. Decision: (a) keep vitest as `npm test`, run node:test via a sibling `npm run test:node` script, QA runs both manually for M2; (b) compose both runners under `npm test`; (c) defer to a follow-on CR. Recommend (a) — minimum surface change, leaves vitest-leak FLASHCARDs intact. Surface to orchestrator for confirmation.

---

_End of M2 plan. Wave 3 (CR-061 + CR-064 follow-on pair) authored as `M3.md` after Wave 2 merges._

### M3.md

# M3 — Wave 3 Plan (Parallel-Safe Pair)

> Sprint: SPRINT-27 · Milestone: M3 (Wave 3) · Mode: `execution_mode: v2`
> Authored by Architect 2026-05-15 after Wave 1+2 merged (outer `sprint/S-27` HEAD `e50869a`; inner-mcp `sprint/S-27` HEAD `a69536a`).
> Scope: **CR-061** (TokenIssuedModal 3-tab snippet) + **CR-064** (sync sprint plans + reports to MCP). Both lane=standard. Parallel-safe: zero file overlap (admin/ vs cleargate-cli/ + scripts).
> Goal: close the EPIC-027 proof loop (CR-064) and complete the "copy-paste connect an external agent" promise (CR-061).

## Cross-cutting

Pulled from `sprint-context.md` (Cross-Cutting Rules + Adjacent Implementations table re-populated for this dispatch) and SDR §§2.1–2.4. Mirror as one-line bullets — every Developer dispatch in M3 starts from this same page.

- **L0 Code-Truth (CR-028).** Codebase is source of truth; on any conflict between this M-plan and code, the code wins — re-read the cited symbol/anchor and surface a Blockers Report (`spec-gap` category) before patching.
- **Wave 3 dispatch happens AFTER Wave 1+2 merge.** Preconditions verified at dispatch time: `mcp/src/lib/payload-contract.ts` exports `KNOWN_TYPES` containing both `'sprint'` AND `'sprint_report'` (verified line 14–23 of merged file); `cleargate-cli/src/commands/mcp-serve.ts` reads `process.env['CLEARGATE_SERVICE_TOKEN']` literal at line 83 (verified); `.cleargate/scripts/close_sprint.mjs` Step 7.5 anchor literal `// CR-063: wiki ingest sprint report` lives at line 750 (verified). Outer-repo mirror at `cleargate-planning/.cleargate/scripts/close_sprint.mjs:750` also has the identical anchor (verified).
- **Parallel-safe pair.** CR-061 touches ONLY `admin/src/lib/components/TokenIssuedModal.svelte` + test files under `admin/`. CR-064 touches ONLY `cleargate-cli/src/commands/push.ts`, a new `cleargate-cli/scripts/smoke-push-sprint-artifacts.mjs`, and both `close_sprint.mjs` mirrors. Zero file overlap per SDR §2.2 + §2.3 — both stories may open worktrees simultaneously.
- **node:test is the default for new tests (`feedback_node_test_over_vitest`).** New test files use `*.node.test.ts` via `tsx --test`. EXCEPTION for CR-061: `admin/tests/unit/TokenIssuedModal.test.ts` already exists as vitest (verified file at `/admin/tests/unit/TokenIssuedModal.test.ts` uses `from 'vitest'` at line 18). Extend the existing vitest file — do NOT fork to node:test for a Svelte component (Svelte rune-mode test pattern uses `@testing-library/svelte` which is wired through vitest). Per the FLASHCARD, "existing 129 vitest files stay"; the modal test is one of them.
- **String-LITERAL coupling.** CR-061's stdio tab MUST contain the literal token `CLEARGATE_SERVICE_TOKEN` as a JSON key inside the snippet. CR-061 unit tests assert the snippet body string-matches this literal. Do NOT import the literal as a constant — CR-065 deliberately kept it as a source literal (per M1 §CR-065 risk note line 69) to avoid a circular constant-extraction CR.
- **Mirror parity per-edit (FLASHCARD 2026-05-01 `#mirror #parity`).** CR-064 edits BOTH `cleargate-cli/src/commands/push.ts` (single live copy — no mirror) AND `.cleargate/scripts/close_sprint.mjs` + `cleargate-planning/.cleargate/scripts/close_sprint.mjs` (live + canonical mirror — apply IDENTICAL Step 7.4 insertion in both, in the same commit). Do NOT touch `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` — regenerated by `npm run prebuild`.
- **Step 7.5 anchor is the literal `// CR-063: wiki ingest sprint report`** at `close_sprint.mjs:750` in BOTH mirrors. CR-064 inserts its new Step 7.4 block IMMEDIATELY BEFORE this comment line. The pre-anchor must be byte-identical in both mirrors after CR-064 lands. Verify with `diff .cleargate/scripts/close_sprint.mjs cleargate-planning/.cleargate/scripts/close_sprint.mjs` — only the same Step 7.4 block plus any pre-existing divergence (untouched).
- **Drizzle / Postgres / migrations** — N/A for M3. Neither story touches `mcp/src/db/schema.ts` or `mcp/migrations/`.
- **Never `--no-verify`.** Pre-commit hook clean (`npm run typecheck` + `npm test`) before commit per workspace edited. CR-061 → `cd admin && ...`. CR-064 → `cd cleargate-cli && ...` AND a smoke test of the script.
- **One commit per story.** Format: `feat(EPIC-027): CR-064 sync sprint plans + reports to MCP` for CR-064. Format for CR-061: `feat(EPIC-006): CR-061 token-issued modal 3-tab connection snippet` (parent_ref is STORY-006-05, parent_cleargate_id STORY-006-05 — EPIC-006 is the epic).

## Goal anchor

> Open MCP to any work-item type, prove it by syncing ClearGate's own sprint plans + reports to MCP, lock the codebase/PM-tool boundary, and give the admin console a copy-paste "connect an external agent" snippet that works for both HTTP and stdio (Claude Desktop) clients.

Per-item advancement clauses (verbatim from SDR §2.1):

- **CR-061** — Closes the "copy-paste snippet" half of the sprint goal. After this CR, the admin console's Token-Issued modal renders three tabs (HTTP JSON config, curl test, stdio Claude Desktop config). The stdio tab cites the `CLEARGATE_SERVICE_TOKEN` env var that CR-065 (Wave 1) added to `cleargate mcp serve` — the modal is the final user-facing surface that makes the W1 plumbing visible. Storage-leak invariant from STORY-006-05 stays green; the three snippet bodies interpolate `_plaintext` from in-memory state only.
- **CR-064** — Closes the EPIC-027 proof loop (other half of the sprint goal). After this CR, `cleargate push <sprint-plan-or-report-path>` succeeds with stored_type `sprint` / `sprint_report` against the deployed MCP — proving zero MCP code changes are needed to sync a new type once the validator opens (STORY-027-01..04 already shipped the validator + warnings + audit-log columns). Inserts Step 7.4 into `close_sprint.mjs` so future sprint closes auto-push the report to MCP before the wiki-ingest step (per the SDR-locked ordering: MCP push first, wiki ingest second).

## Per-story blueprint

### CR-061 — TokenIssuedModal 3-tab connection snippet

- **Cross-story coupling:** CR-061 depends on CR-065 (Wave 1, merged) for the stdio path. The stdio tab MUST cite the `CLEARGATE_SERVICE_TOKEN` env-var literal — string-matched in tests. Independent of CR-064 (different repo subtree). No coupling with any Wave 2 story.

- **File surface (by symbol; verified by grep against current HEAD):**
  - **Modify** `admin/src/lib/components/TokenIssuedModal.svelte` (verified at `/admin/src/lib/components/TokenIssuedModal.svelte`, 292 LOC):
    - **Import additions** at the top of the `<script lang="ts">` block (after the existing `import { toastStore } from '$lib/stores/toast.svelte.js';` at line 34): `import { env as publicEnv } from '$env/dynamic/public';` (canonical source for `PUBLIC_MCP_URL` per the modal CR §0.5 Q3 + verified usage in `admin/src/lib/mcp-client.ts:13` + `:30`).
    - **State addition** near the existing `let saved = $state(false);` at line 45: introduce `let activeTab = $state<'json' | 'curl' | 'stdio'>('json');` (default-select the HTTP JSON tab; CR §0.5 Q2 — three tabs).
    - **Derived snippet builders** (Svelte 5 `$derived`): three reactive constants below the `_plaintext` state at line 48:
      - `const mcpUrl = $derived(publicEnv.PUBLIC_MCP_URL ?? 'http://localhost:3000');` (mirror `getBaseUrl` default at `admin/src/lib/mcp-client.ts:30`).
      - `const jsonSnippet = $derived(JSON.stringify({ url: \`${mcpUrl}/mcp\`, headers: { Authorization: \`Bearer ${_plaintext}\` } }, null, 2));`
      - `const curlSnippet = $derived(\`curl -X POST ${mcpUrl}/mcp -H "Authorization: Bearer ${_plaintext}" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'\`);`
      - `const stdioSnippet = $derived(JSON.stringify({ mcpServers: { cleargate: { command: 'cleargate', args: ['mcp', 'serve'], env: { CLEARGATE_SERVICE_TOKEN: _plaintext } } } }, null, 2));` — note the literal property name `CLEARGATE_SERVICE_TOKEN` is a JSON key, not a code identifier; tests will string-match it.
    - **Markup insertion** below the existing Plaintext-chip block (currently lines 203–240, end at `</div>` line 240) and ABOVE the checkbox-gate block (line 243):
      - Add a `<section class="connect-snippets mb-4">` wrapper.
      - Inside: a tab-strip with three `<button type="button" role="tab" aria-selected={activeTab === '<id>'} onclick={() => activeTab = '<id>'}>` controls — IDs `json` / `curl` / `stdio` with visible labels `HTTP JSON config` / `curl test` / `Claude Desktop (stdio)`. Use `IconButton.svelte` shell? No — tabs use plain `<button>` per DG §6.4 (icon buttons are for icon-only; these have text labels).
      - Below the tab strip: a `<div role="tabpanel">` showing the active snippet inside a `<pre class="bg-base-200 rounded-xl p-4 font-mono text-xs whitespace-pre-wrap break-all">{snippet}</pre>` followed by a copy button (reuse the existing Copy-button SVG pattern at lines 215–238 — DG §6.4 IconButton). Each tab's panel emits a distinct copy handler that copies the matching snippet content via `copyToClipboard(...)` and triggers `toastStore.success('Copied to clipboard')` (toast text MUST NOT contain the snippet body or the token — same DG §6.10 constraint as the existing copy handler).
      - Below the tabpanel: a one-line footer `<p class="text-xs text-[#6B7280] mt-2">Pulled items land where the agent's instructions say. Run <code class="font-mono">npx cleargate init</code> in the target repo for <code class="font-mono">.cleargate/delivery/pending-sync/</code> auto-routing.</p>` (CR §0.5 Q4 — accepted).
    - **Storage-leak invariant** (LOAD-BEARING — STORY-006-05): the three derived snippets reference `_plaintext` reactively. They are NEVER written to localStorage/sessionStorage/IndexedDB and MUST NOT appear in any console.* call. Copy-to-clipboard via `copyToClipboard()` is the only egress path (allowed by STORY-006-05 contract). The existing `_plaintext = ''` zeroing in `handleCloseClick`, `beforeNavigate`, and `onDestroy` is sufficient — the derived snippets are re-evaluated to empty strings automatically when `_plaintext` resets (Svelte 5 `$derived` semantics).

  - **Modify** `admin/tests/unit/TokenIssuedModal.test.ts` (existing vitest file, verified `/admin/tests/unit/TokenIssuedModal.test.ts`):
    - **Extend with new cases** (see Test shape below) — do NOT fork to a `.node.test.ts` variant; the Svelte rune-mode test harness uses `@testing-library/svelte` wired through vitest. Per cross-cutting rule: existing vitest files stay; CR-061 adds cases inside the existing `describe('TokenIssuedModal', () => { ... })` block.
    - **Mock `$env/dynamic/public`** the same way `$app/navigation` is mocked at lines 30–35: `vi.mock('$env/dynamic/public', () => ({ env: { PUBLIC_MCP_URL: 'https://mcp.example.test' } }));` — placed alongside the other mocks.

  - **Modify or add** `admin/tests/e2e/tokens-modal.spec.ts` if it exists (Playwright). Grep first: if the file is absent, defer e2e to a follow-up CR — the unit-test layer is sufficient for the snippet renders + copy-button + storage-leak invariant. (Verified absent in the repo today: `find admin/tests/e2e -name "*token*"` returned nothing.) **Decision: skip Playwright in CR-061; CR file lists e2e but no fixture exists. Surface as a follow-up CR.** Document this in the Developer dispatch: do NOT author a new Playwright file from scratch — the e2e suite has its own scaffolding pattern (auth fixtures, dev-server boot) that is out of scope for a UI-only CR.

  - **Do NOT modify** (verbatim from CR-061 §3 Do-NOT-modify list):
    - `mcp/src/admin-api/tokens.ts` — server-side issuance unchanged.
    - `mcp/src/auth/service-token.ts` — auth path unchanged.
    - `cleargate-cli/src/commands/mcp-serve.ts` — CR-065 already shipped the env-var path; CR-061 only consumes the literal string.
    - The "I've saved it" gate, the plaintext-once discipline, or the storage-leak guards from STORY-006-05.

- **Implementation sketch (ordered):**
  1. Add the `publicEnv` import + `activeTab` state + three `$derived` snippet builders. Verify the modal still renders unchanged (visually) by running `cd admin && npm run dev` and issuing a test token.
  2. Insert the `<section class="connect-snippets">` markup between the Plaintext chip and the Checkbox gate. Render the JSON tab first; confirm the JSON snippet shows `https://mcp.example.test/mcp` (or the deploy's `PUBLIC_MCP_URL`) and `Bearer <43-char-plaintext>`.
  3. Wire the tab-switch logic + the two additional tab panels (curl + stdio). The stdio panel renders the JSON with key `CLEARGATE_SERVICE_TOKEN` — visually confirm the literal property name is present.
  4. Wire the three copy buttons. Reuse the existing copy-button SVG icon pattern from lines 215–238 (do NOT introduce `lucide-svelte` — admin already has its own inline-SVG idiom).
  5. Add the footer `<p>` line below the tabpanel.
  6. Author the seven new vitest cases (see Test shape). Pin the `PUBLIC_MCP_URL` mock value to `'https://mcp.example.test'` so the assertions can be exact-string.
  7. Run `cd admin && npm run typecheck` → clean. `cd admin && npm test -- TokenIssuedModal` → green.
  8. Manual smoke per CR §4 Manual block (issue a token in `/projects/<id>/tokens`; verify all three snippet renders, all three copy buttons, no localStorage/sessionStorage leak).
  9. Commit: `feat(EPIC-006): CR-061 token-issued modal 3-tab connection snippet`.

- **Test shape (Red tests for QA-Red to author first, mapped to CR-061 §4 + the three-tab decision):**
  1. `TokenIssuedModal · default tab on open is JSON (activeTab === 'json'); JSON snippet visible, curl + stdio panels hidden` — assert rendered text contains `"url": "https://mcp.example.test/mcp"` and `"Authorization": "Bearer ${SECRET_TOKEN}"`.
  2. `TokenIssuedModal · clicking "curl test" tab switches activeTab; curl snippet visible containing string-match \`curl -X POST https://mcp.example.test/mcp\` AND the literal `Authorization: Bearer ${SECRET_TOKEN}` AND `tools/list`` — assert via `screen.getByRole('tab', { name: /curl/i })`.
  3. `TokenIssuedModal · clicking "Claude Desktop (stdio)" tab switches activeTab; stdio snippet visible containing the literal property name "CLEARGATE_SERVICE_TOKEN"` — assert the rendered `<pre>` text matches `/"CLEARGATE_SERVICE_TOKEN":\s*"cg_test_/` (the test secret token is interpolated as the value).
  4. `TokenIssuedModal · stdio snippet contains the literal "cleargate" command and "mcp", "serve" args` — assert `/"command":\s*"cleargate"/` AND `/"args":\s*\[\s*"mcp",\s*"serve"\s*\]/`.
  5. `TokenIssuedModal · copy-JSON button calls copyToClipboard with the JSON snippet body; success toast fires without the token in the toast text` — assert `mockCopy` called with a string containing `"url"` AND `"Authorization"` AND `SECRET_TOKEN`; assert `mockToast.success` called with `'Copied to clipboard'` (NOT the snippet body).
  6. `TokenIssuedModal · copy-curl + copy-stdio buttons fire respective copyToClipboard calls` — similar to (5) but for the other two tabs.
  7. `TokenIssuedModal · no plaintext leak after close — three snippets rendered; checkbox ticked; Close clicked; component plaintext zeroed AND none of (localStorage, sessionStorage) received any of (SECRET_TOKEN, snippet bodies)` — extends the existing storage-leak invariant from STORY-006-05 to cover the snippet bodies as well (which contain the token). Use the existing `addEventListenerSpy` / `removeEventListenerSpy` pattern from the test file.
  8. `TokenIssuedModal · footer line contains "npx cleargate init" and ".cleargate/delivery/pending-sync/" literals` — assert via `screen.getByText` partial match.

- **Risks / wiring traps:**
  - **`$env/dynamic/public` mock must be set BEFORE component import** — vitest hoists `vi.mock()` calls. The existing file has this discipline (see `$app/navigation` mock at lines 30–35); copy the pattern exactly. Without the mock, the test runner errors on missing SvelteKit env.
  - **Storage-leak invariant covers SNIPPET BODIES now, not just the raw token.** The three `$derived` snippets contain the token verbatim. The Red test case #7 must assert NONE of (raw token, JSON snippet body, curl snippet body, stdio snippet body) appears in localStorage or sessionStorage after copy + close. The existing modal's invariant only checked the raw token; the new test extends it.
  - **No `lucide-svelte` dependency added.** The CR-062 (Wave 1) blueprint pulled `lucide-svelte` for Send + Trash2 icons; CR-061 does NOT need new icons. Reuse the existing inline-SVG clipboard icon pattern from lines 215–238 for the three copy buttons. Adding `lucide-svelte` here would be incremental dep churn for zero icon-set value.
  - **`PUBLIC_MCP_URL` fallback to `http://localhost:3000`.** Matches `admin/src/lib/mcp-client.ts:30` exactly. Do NOT diverge — if a future CR centralizes URL resolution, the modal will pick up the centralization automatically (since it imports from `publicEnv` directly, same source).
  - **Svelte 5 `$derived` reactivity in tests** — `_plaintext` is mutated via `$effect` when `open` flips. Tests must render the modal with `open: true` initially (the existing `baseProps` at line 64 does this); cases that toggle tabs do not require re-rendering.
  - **JSON.stringify with indent=2 vs minified** — keep `null, 2` for the JSON config + stdio config (multi-line readable form). The curl snippet is a single-line shell command — do NOT pretty-print it.
  - **Plaintext gate already disables Close until checkbox ticked.** The three snippet sections render alongside the plaintext (CR-061 §1.5 explicit: "snippet is shown alongside the plaintext, not after dismissal"). Do NOT add a gate to reveal the snippets.

- **DoD references (CR-061 file at `archive/CR-061_Token_Modal_Connection_Instructions.md`):**
  - L65-75 (Old vs. New logic — three snippet blocks + footer line).
  - L98-101 (Modify list — TokenIssuedModal.svelte + unit-test extension).
  - L103-107 (Do-NOT-modify list).
  - L111-126 (Verification Protocol — unit + e2e + manual smoke).
  - L144-155 (Ambiguity Gate — all 4 §0.5 Qs resolved 2026-05-14).

---

### CR-064 — Sync sprint plans + reports to MCP (EPIC-027 proof loop)

- **Cross-story coupling:** CR-064 depends on STORY-027-01..04 (Wave 2, all merged) for `KNOWN_TYPES`, `stored_type`, `warnings: []`, and the EPIC-027 audit_log surface. CR-064 also depends on CR-063 (Wave 1, merged) — the Step 7.5 anchor at `close_sprint.mjs:750` is CR-063's contribution; CR-064 inserts Step 7.4 IMMEDIATELY BEFORE this anchor. Independent of CR-061 (different repo subtree).

- **File surface (by symbol; verified by grep against current HEAD):**

  **cleargate-cli (CLI tree):**
  - **Modify** `cleargate-cli/src/commands/push.ts` (verified 421 LOC):
    - **Anchor: `function getItemType(fm)` at line 409–415** — currently:
      ```ts
      function getItemType(fm: Record<string, unknown>): string | null {
        const typeMap: Record<string, string> = {
          story_id: 'story',
          epic_id: 'epic',
          proposal_id: 'proposal',
          cr_id: 'cr',
          // ... bug_id at line 415 (verified by grep)
        };
        for (const [key, type] of Object.entries(typeMap)) { /* ... */ }
      }
      ```
      **Edit:** add ONE row to the `typeMap` literal: `sprint_id: 'sprint',` (between `proposal_id` and `cr_id` alphabetically, or appended; either works — Developer chooses readability). Per SDR §2.3 + CR-064 §0.5 Q1: `sprint_report` is NOT added to `typeMap` (filename-based override handles that path; see next edit). Frontmatter for sprint plans uses `sprint_id:` — verified pattern across the existing `SPRINT-NN_*.md` files (e.g. `pending-sync/SPRINT-27_MCP_Type_Agnostic_And_Console_Connection.md` frontmatter contains `sprint_id: "SPRINT-27"`).
    - **NEW path-aware type override** — add BEFORE the `getItemType(fm)` call site (search for `getItemType(fm)` invocations in the push handler; the canonical site is inside `handlePush`/`pushHandler` around the validate-frontmatter region). Implementation:
      ```ts
      const SPRINT_REPORT_PATH_REGEX = /\.cleargate\/sprint-runs\/SPRINT-\d{2,}\/(REPORT|SPRINT-\d{2,}_REPORT)\.md$/;
      function getItemTypeWithPathOverride(localPath: string, fm: Record<string, unknown>): string | null {
        if (SPRINT_REPORT_PATH_REGEX.test(localPath)) return 'sprint_report';
        return getItemType(fm);
      }
      ```
      The override returns `'sprint_report'` regardless of frontmatter when the path matches the carve-out. Replace the existing `getItemType(fm)` call site with `getItemTypeWithPathOverride(localPath, fm)`. CR §0.5 Q2 — accepted: filename override.
    - **NEW path validator extension** — the current handler reads files from `.cleargate/delivery/pending-sync/` and `.cleargate/delivery/archive/` (verified at `push.ts:356–357` in the `resolveLocalItem` helper). For `cleargate push <file>` invocations on a direct path argument (the common case for sprint plans + reports), the path validator must accept TWO permitted roots:
      - `.cleargate/delivery/` (existing) — sprint PLANS live under `delivery/pending-sync/SPRINT-NN_*.md` and `delivery/archive/SPRINT-NN_*.md`.
      - `.cleargate/sprint-runs/SPRINT-NN/` (new) — gated to two allowlisted basenames: `REPORT.md` OR `SPRINT-NN_REPORT.md`. Any other path under `sprint-runs/` is rejected as before (token-ledger, hook-log, plans/, scripts/, .script-incidents/, etc.).
      - **Implementation sketch:** the existing path-validator block (search for the `delivery/` substring check that rejects out-of-tree paths — likely inside `pushHandler` near the file-resolution region; Developer locates by grep) extends to: `if (path.includes('.cleargate/sprint-runs/')) { if (!SPRINT_REPORT_PATH_REGEX.test(path)) throw new Error('path not in allowlist'); /* else: allow */ }`.
      - **Order matters:** allowlist carve-out runs BEFORE the exclusion check (same ordering rule as CR-063's wiki-ingest extension — see M1 §CR-063 risk note line 259). Mirror the pattern.
    - **Title fallback for sprint plans + reports** — the existing push payload requires a `title` field (per CR-064 §3 line 119). Sprint plan frontmatter today has no `title:` field; sprint report frontmatter likewise. Both files start with `# SPRINT-NN <Title>` H1 (verified pattern). Edit: when `fm.title` is missing AND the path matches either sprint-plan or sprint-report carve-out, derive `title` from the first H1 line in the body. Implementation: regex `/^#\s+(.+)$/m` on the raw markdown post-frontmatter; first match → `title`.

  - **Modify or add** test file at `cleargate-cli/test/commands/push.node.test.ts` (NEW node:test file per cross-cutting rule; existing `push.test.ts` if any stays vitest — verify by `ls cleargate-cli/test/commands/push.*`). New cases per CR §4 (see Test shape below).

  - **Add** `cleargate-cli/scripts/smoke-push-sprint-artifacts.mjs` (NEW; verified `cleargate-cli/scripts/` dir exists with siblings `backfill-sprint-reports.mjs`, `build-manifest.ts`, etc.):
    - Push the most-recent two sprint plans (`archive/SPRINT-25_*.md`, `archive/SPRINT-26_*.md`) AND the two reports (`sprint-runs/SPRINT-25/SPRINT-25_REPORT.md`, fallback `REPORT.md`; same for SPRINT-26).
    - Resolution: glob for `archive/SPRINT-25*.md` (one file expected) and `archive/SPRINT-26*.md` (one file expected). For reports, prefer `SPRINT-NN_REPORT.md`; fallback to `REPORT.md` (legacy filename pattern — same legacy-fallback rule used by `close_sprint.mjs` Step 4 + Step 7.5 per CR-063).
    - For each of the 4 files, invoke `cleargate push <path>` via the local `dist/cli.js` binary (the smoke script is build-then-run; assume `cleargate-cli/dist/` exists post-`npm run build`).
    - Output format (per CR §4 expected output):
      ```
      ✓ SPRINT-25 plan pushed   type=sprint        version=N
      ✓ SPRINT-25 report pushed type=sprint_report version=N
      ✓ SPRINT-26 plan pushed   type=sprint        version=N
      ✓ SPRINT-26 report pushed type=sprint_report version=N
      4 pushed, 0 failed
      ```
    - **Idempotent** — re-runs bump version numbers but do NOT fail. Assert via the `stored_type` field on the MCP response that the path-override worked for the reports.
    - **Assert `warnings: []`** on all 4 pushes — this is the EPIC-027 headline-metric check (KNOWN_TYPES contains `'sprint'` AND `'sprint_report'`, so no `unknown_type` L2 warning fires).
    - Exit code 0 on `4 pushed, 0 failed`; non-zero otherwise. Print stderr on failure with the failing file path + MCP response.

  **Scripts (Gate-4 close pipeline):**
  - **Modify** `.cleargate/scripts/close_sprint.mjs` (live; verified line 749 = `// ── Step 7.5: wiki ingest sprint report ──...`, line 750 = `// CR-063: wiki ingest sprint report`):
    - **Insert Step 7.4 BLOCK** between line 747 (`}` closing Step 7 catch) and line 749 (Step 7.5 header). Format mirrors Step 7.5 verbatim (dashes match):
      ```js
        // ── Step 7.4: MCP push of sprint plan + report ───────────────────────────
        // CR-064: MCP push first, wiki ingest second (per SDR-locked ordering;
        // CR-064 §0.5 Q4 accepted). Runs after Step 7 (work-item sync) and BEFORE
        // Step 7.5 (CR-063 wiki ingest). Non-fatal on failure: sprint stays Completed.
        process.stdout.write('Step 7.4: pushing sprint plan + report to MCP...\n');
        try {
          const cliBin = path.join(REPO_ROOT, 'cleargate-cli', 'dist', 'cli.js');
          if (fs.existsSync(cliBin)) {
            // Resolve sprint plan path: prefer archive/, fall back to pending-sync/
            const planArchive = path.join(REPO_ROOT, '.cleargate', 'delivery', 'archive', `${sprintId}_*.md`);
            const planPending = path.join(REPO_ROOT, '.cleargate', 'delivery', 'pending-sync', `${sprintId}_*.md`);
            // Use globSync (or fs.readdirSync + match) — pattern mirrors Step 4 resolution
            const planPath = /* glob resolution */ null; // Developer implements via globSync or readdir-filter
            if (planPath && fs.existsSync(planPath)) {
              execSync(`node ${JSON.stringify(cliBin)} push ${JSON.stringify(planPath)}`, {
                stdio: 'inherit',
                env: process.env,
                timeout: 60000,
              });
              process.stdout.write('Step 7.4a passed: sprint plan pushed to MCP.\n');
            } else {
              process.stdout.write('Step 7.4a skipped: sprint plan file not found.\n');
            }
            // Resolve sprint report path: same legacy-fallback rule as Step 7.5 (reuses `reportFile` var)
            if (reportFile && fs.existsSync(reportFile)) {
              execSync(`node ${JSON.stringify(cliBin)} push ${JSON.stringify(reportFile)}`, {
                stdio: 'inherit',
                env: process.env,
                timeout: 60000,
              });
              process.stdout.write('Step 7.4b passed: sprint report pushed to MCP.\n');
            } else {
              process.stdout.write('Step 7.4b skipped: sprint report file not found (legacy sprint or report not yet written).\n');
            }
          } else {
            process.stdout.write('Step 7.4 skipped: CLI binary not found (non-fatal).\n');
          }
        } catch (err) {
          // Non-fatal — sprint stays Completed; MCP push can be retried manually
          process.stderr.write(`Step 7.4 warning: MCP push failed: ${/** @type {Error} */ (err).message}\n`);
          process.stderr.write('Run `cleargate push <plan-or-report-path>` manually to retry.\n');
        }
      ```
    - **Critical: insertion anchor.** The new Step 7.4 block goes BEFORE the literal `// ── Step 7.5: wiki ingest sprint report ──...` line (currently `close_sprint.mjs:749` live + `:749` canonical). The `// CR-063: wiki ingest sprint report` comment at line 750 MUST remain immediately after the Step 7.5 header. Verify post-edit: `grep -A2 "Step 7.4:" close_sprint.mjs` then `grep -A1 "Step 7.5:" close_sprint.mjs` — both anchors present and ordered.
    - **Plan-path resolution helper:** since Node `globSync` is available in Node 22+ (verified Node `>=24.0.0` per sprint-context), use `import { globSync } from 'node:fs'` (or the built-in `fs.globSync` API — verify Node 24 has it; if not, fall back to `fs.readdirSync(dir).filter(f => f.startsWith(`${sprintId}_`) && f.endsWith('.md'))`). Developer picks the cleaner path; either works.

  - **Modify** `cleargate-planning/.cleargate/scripts/close_sprint.mjs` (canonical mirror; verified line 749 has the identical Step 7.5 anchor — mirror parity confirmed pre-CR-064):
    - **Apply the IDENTICAL Step 7.4 block** in the same commit. Diff after edit: `diff .cleargate/scripts/close_sprint.mjs cleargate-planning/.cleargate/scripts/close_sprint.mjs` should show ONLY the same Step 7.4 block plus any pre-existing divergence (untouched per FLASHCARD `#mirror #parity`).

  - **Do NOT modify** (verbatim from CR-064 §3 Do-NOT-modify + §2.5):
    - `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` — regenerated by `npm run prebuild`. Verify post-commit that the regenerated payload contains Step 7.4 by running `cd cleargate-cli && npm run prebuild` then diffing.
    - The Reporter agent or report-generation logic.
    - Anything under `mcp/src/tools/` — Wave 2 already shipped the validator open + `KNOWN_TYPES` + warnings. CR-064 is pure CLI + script wiring.
    - The CR-063 wiki-ingest path validator. CR-064 adds a PARALLEL validator extension in `push.ts`; the two validators stay separate (different code paths, different domains).
    - `mcp/src/lib/payload-contract.ts` `KNOWN_TYPES` — STORY-027-01 already added `'sprint'` + `'sprint_report'`. Verified at line 14–23 of the merged file. No edit needed; CR-064 just consumes the constants implicitly via the open-type validator.

- **Implementation sketch (ordered):**
  1. Add `sprint_id: 'sprint'` to `typeMap` at `push.ts:410`. Add `SPRINT_REPORT_PATH_REGEX` constant + `getItemTypeWithPathOverride()` helper. Swap the existing `getItemType(fm)` call site to use the new helper.
  2. Extend the path validator: allow `.cleargate/sprint-runs/SPRINT-NN/{REPORT,SPRINT-NN_REPORT}.md`; reject all other paths under `sprint-runs/`. Place the allowlist check BEFORE the exclusion check.
  3. Add the H1-based title fallback for sprint plans + reports.
  4. Author `cleargate-cli/test/commands/push.node.test.ts` cases (see Test shape).
  5. Run `cd cleargate-cli && npm run typecheck && npm test` — both green.
  6. Author `cleargate-cli/scripts/smoke-push-sprint-artifacts.mjs`. Build CLI: `cd cleargate-cli && npm run build`. Run smoke against the deployed MCP (or a local MCP — verify with `CLEARGATE_MCP_URL` env). Expect `4 pushed, 0 failed`. Verify each response payload has `warnings: []`.
  7. Insert Step 7.4 block into BOTH `close_sprint.mjs` mirrors. Diff them: `diff .cleargate/scripts/close_sprint.mjs cleargate-planning/.cleargate/scripts/close_sprint.mjs` shows only common addition (plus any pre-existing divergence).
  8. Dry-run Gate 4 close on SPRINT-26 (or use a test fixture sprint): `node .cleargate/scripts/close_sprint.mjs --sprint-id SPRINT-26 --dry-run` if the script supports `--dry-run`, else inspect via stdout ordering. Confirm Step 7.4 logs BEFORE Step 7.5.
  9. Run `cd cleargate-cli && npm run prebuild` and confirm the regenerated `templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` contains Step 7.4. Do NOT commit any hand-edit to that file.
  10. Commit: `feat(EPIC-027): CR-064 sync sprint plans + reports to MCP`.

- **Test shape (Red tests for QA-Red to author first; CR-064 §4 unit cases verbatim):**
  1. `push · frontmatter has \`sprint_id: "SPRINT-26"\` AND path is \`.cleargate/delivery/archive/SPRINT-26_*.md\` → resolved type is \`"sprint"\`` — assert via mocked MCP client that the `push_item` call argument has `type: 'sprint'`.
  2. `push · path \`.cleargate/sprint-runs/SPRINT-26/SPRINT-26_REPORT.md\` → resolved type is \`"sprint_report"\` regardless of frontmatter` — path-override priority. Even when frontmatter has `sprint_id: "SPRINT-26"` (which would resolve to `"sprint"` via typeMap), the path-override wins.
  3. `push · path \`.cleargate/sprint-runs/SPRINT-12/REPORT.md\` (legacy basename) → resolved type is \`"sprint_report"\`` — legacy filename path-override.
  4. `push · path \`.cleargate/sprint-runs/SPRINT-26/token-ledger.jsonl\` → path-validator REJECTS with exit code 2` — non-allowlisted file under `sprint-runs/`.
  5. `push · path \`.cleargate/sprint-runs/SPRINT-26/plans/M1.md\` → path-validator REJECTS` — `plans/` subdir not allowlisted.
  6. `push · path \`.cleargate/sprint-runs/SPRINT-26/.script-incidents/foo.json\` → path-validator REJECTS` — additional carve-out cross-check.
  7. `push · sprint plan frontmatter lacks \`title:\` but body starts with \`# SPRINT-26 Sprint Title\` → push payload's \`title\` derives from the H1\` "SPRINT-26 Sprint Title"\`` — H1 title fallback for sprints.
  8. `push · sprint report frontmatter lacks \`title:\` but body starts with \`# SPRINT-26 Report: <Title>\` → push payload's \`title\` derives from the H1`.
  9. `push · response \`warnings: []\` on sprint + sprint_report pushes (KNOWN_TYPES suppression confirmed; STORY-027-04 \`unknown_type\` warning does NOT fire)` — smoke-script-level assertion; in unit form, mock the MCP response and assert the handler does not surface a warning.
  10. `push · response \`stored_type: 'sprint'\` is honored on the local frontmatter write-back path` — verifies STORY-027-01's `stored_type` field round-trips (no client-side regression).
  11. `close_sprint.mjs Step 7.4 · invoked after Step 7 AND BEFORE Step 7.5; absent plan or report file logs skip without failing the close` — anchor + skip-on-missing test. Run the script in a controlled fixture; capture stdout; assert the substring order `Step 7.4: pushing sprint plan + report to MCP` → ... → `Step 7.5: ingesting sprint report into wiki`.

- **Risks / wiring traps:**
  - **Step 7.4 insertion anchor is the literal `// ── Step 7.5: wiki ingest sprint report ──...`** at `close_sprint.mjs:749` (BOTH mirrors). Insert the new Step 7.4 header IMMEDIATELY BEFORE this line. Do NOT use a generic anchor like "after Step 7" — Step 7's `catch` block has trailing whitespace and the anchor is the Step 7.5 HEADER comment, not Step 7's end.
  - **Mirror parity is per-edit, not whole-file (FLASHCARD 2026-05-01 `#mirror #parity`).** BOTH `close_sprint.mjs` files get the IDENTICAL Step 7.4 block. The pre-CR-064 diff shows ONLY pre-existing divergence (untouched); the post-CR-064 diff shows the same plus the new common addition. Do NOT reconcile pre-existing divergence as a side effect.
  - **`SPRINT_REPORT_PATH_REGEX` strictness.** The regex `/\.cleargate\/sprint-runs\/SPRINT-\d{2,}\/(REPORT|SPRINT-\d{2,}_REPORT)\.md$/` requires the basename to be EXACTLY `REPORT.md` or `SPRINT-NN_REPORT.md` (capital R-E-P-O-R-T). Lowercase `report.md` is REJECTED. Sub-paths under `sprint-runs/SPRINT-NN/` (like `plans/M1.md`, `scripts/foo.sh`, `.script-incidents/*.json`, `token-ledger.jsonl`) are also REJECTED because they don't match the basename group.
  - **Path-override priority over frontmatter typeMap.** If a developer ever places a sprint plan file under `sprint-runs/SPRINT-NN/` by mistake (with frontmatter `sprint_id: "SPRINT-NN"`), the path-override forces `type: 'sprint_report'`. This is intentional per CR §0.5 Q2 (filename-based override; CR-064 §2.5 line 109 "path-aware override branch BEFORE the frontmatter check fires"). Document this in the smoke script comment so future maintainers don't trip on it.
  - **Smoke-script scope: 4 pushes only (SPRINT-25 + SPRINT-26 × {plan, report}).** Full backfill to SPRINT-03..SPRINT-24 deferred per CR §0.5 Q3 (accepted). Do NOT extend the smoke script to a full backfill in this CR. CR-063 (already merged) has `cleargate-cli/scripts/backfill-sprint-reports.mjs` for wiki-side backfill — a parallel CR-064-equivalent for MCP-side backfill is the future follow-up.
  - **`KNOWN_TYPES` literal-membership pre-check.** Before opening a Developer worktree, the Architect (this dispatch) MUST verify that `mcp/src/lib/payload-contract.ts` line 14–23 contains BOTH `'sprint'` AND `'sprint_report'` strings. Verified pre-dispatch — but if M2 amendments shifted the literal, the smoke-script's `warnings: []` assertion fails. Surface as Blockers Report (`spec-gap`) if the pre-check fails.
  - **CLI binary path resolution in `close_sprint.mjs`.** Current Step 7.5 uses `path.join(REPO_ROOT, 'cleargate-cli', 'dist', 'cli.js')` (verified line 755). Step 7.4 uses the IDENTICAL resolution. If `dist/` is missing (developer forgot `npm run build`), both steps skip with a non-fatal warning. This matches CR-063's behavior.
  - **`execSync` failure surface.** Both Step 7.4a (plan push) and Step 7.4b (report push) wrap `execSync` in a single `try/catch`. A failure in 7.4a does NOT prevent 7.4b from running — split into two nested try/catch blocks if the Developer wants per-step granularity, OR (preferred) keep the single outer try/catch and let the caller see the first failure (matches Step 7.5's pattern; CR-063 used a single try/catch). Decision: single try/catch, mirror Step 7.5's pattern.
  - **Smoke script env requirements.** The script invokes `cleargate push` which requires a valid `CLEARGATE_MCP_URL` + a valid identity (refresh token in keychain OR `CLEARGATE_SERVICE_TOKEN` env). Document in the script header that the smoke run requires one of these. Do NOT hardcode credentials.
  - **L2 warning `unknown_type` suppression depends on KNOWN_TYPES inclusion (locked).** SDR §2.3 + STORY-027-01 M-plan locked the underscore variant `'sprint_report'`. The smoke script's response-shape assertion `warnings: []` would fail if the literal were hyphenated. Verified at `mcp/src/lib/payload-contract.ts:22` — `'sprint_report'` (underscore).

- **DoD references (CR-064 file at `archive/CR-064_Sync_Sprint_Plans_And_Reports_To_MCP.md`):**
  - L79-93 (Old vs. New logic — typeMap entry + path-override + Step 7.4 insertion + smoke script).
  - L118-126 (Modify + Add lists — push.ts + push.test.ts + close_sprint.mjs mirrors + smoke script).
  - L132-136 (Do-NOT-modify list).
  - L138-195 (Verification Protocol — unit + smoke + MCP round-trip + Gate-4 integration + EPIC-027 headline metric).
  - L207-218 (Ambiguity Gate — all 4 §0.5 Qs resolved 2026-05-14).

## Wave 3 ordering

**Parallel-safe.** CR-061 + CR-064 dispatch together in a single orchestrator turn. Both depend on Wave 1+2 done (verified at dispatch time):

- CR-061 → depends on CR-065 (Wave 1, merged) — `CLEARGATE_SERVICE_TOKEN` env var is a string literal in `cleargate-cli/src/commands/mcp-serve.ts:83`. CR-061 references this literal verbatim in the stdio snippet body.
- CR-064 → depends on STORY-027-01..04 (Wave 2, all merged) — `KNOWN_TYPES` incl. `'sprint'` + `'sprint_report'`; `stored_type` on `PushItemResult`; `warnings: []` clean-push shape; audit_log columns. Also depends on CR-063 (Wave 1, merged) — Step 7.5 anchor literal at `close_sprint.mjs:750` (BOTH mirrors).

**No file overlap between CR-061 and CR-064:**
- CR-061: `admin/src/lib/components/TokenIssuedModal.svelte` + `admin/tests/unit/TokenIssuedModal.test.ts`.
- CR-064: `cleargate-cli/src/commands/push.ts` + `cleargate-cli/test/commands/push.node.test.ts` (NEW) + `cleargate-cli/scripts/smoke-push-sprint-artifacts.mjs` (NEW) + `.cleargate/scripts/close_sprint.mjs` + `cleargate-planning/.cleargate/scripts/close_sprint.mjs`.

Both stories merge into `sprint/S-27` before sprint close (Gate 4 ack).

## Cross-story risks

- **None inter-CR.** The two stories live in disjoint subtrees and consume disjoint Wave 1+2 surfaces. The Developer dispatches can run in any order — or simultaneously — without coordination.
- **Coupling to merged Wave 1 work:** if CR-065's `CLEARGATE_SERVICE_TOKEN` literal were ever renamed (it should not, per M1 §CR-065 risk note line 69), CR-061's snippet body + assertions would silently drift. The string-match Red test #3 is the canary.
- **Coupling to merged Wave 2 work:** if `KNOWN_TYPES` ever lost `'sprint_report'` (regression), the smoke script's `warnings: []` assertion fires. The Architect pre-dispatch verifies the literal is intact (verified for this dispatch).
- **`prebuild` regeneration of the templates payload close_sprint.mjs** — if Developer forgets `npm run prebuild` after editing both mirrors, the next `cleargate init` in a target repo will install a Step-7.4-less version. Document in the commit body that prebuild was run.

## Open questions for orchestrator

> Per the SDR + the Wave 1+2 merge state, both CR-061 and CR-064 have all §0.5 questions resolved at Gate-1 (CR-061 line 145, CR-064 line 208). No NEW gaps surfaced during M3 blueprint synthesis. Both stories are dispatch-ready.

**Zero open questions.** Both items are dispatch-ready.

---

_End of M3 plan. Next agent in the loop: orchestrator dispatches CR-061 + CR-064 to two Developer agents in parallel; QA-Red authors Red tests per the Test-shape sections above; QA-Verify runs after each Developer commit per the Cross-cutting rules. Sprint close (Gate 4) runs `close_sprint.mjs` against `SPRINT-27` after both CRs merge — at which point Step 7.4 (CR-064) AND Step 7.5 (CR-063) both fire on this very sprint, push the SPRINT-27 plan + report to MCP, and ingest the report into the wiki. Self-dogfooding closure of the EPIC-027 proof loop._

## Git Log Digest

Git log unavailable for sprint/S-27 (no commits or branch not found).

## Token Ledger Digest

**Total:** 153,197,667 tokens (input: 17,531 / output: 1,264,097 / cache_read: 148,320,980)

**Per-agent breakdown:**
- architect: 103,038,591 (27 dispatches)
- developer: 11,034,201 (12 dispatches)
- qa: 20,189,568 (23 dispatches)
- reporter: 9,400,969 (1 dispatches)
- devops: 9,534,338 (12 dispatches)

**Anomalies:**
- SPRINT-26: 10.6× higher than median story cost

**CR-035 two-line split (pre-computed for Reporter §3):**
sprint_work_tokens: 143,796,698
sprint_total_tokens: 153,197,667
reporter_pass_tokens: null


## Flashcard Slice

No flashcard entries found in sprint window [2026-05-19 → 2026-06-01].

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
