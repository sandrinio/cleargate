CR-064 · SPRINT-27 · Architect Post-Flight

```
ARCH-PASS: APPROVED
NOTES: All 9 verification points pass. (1) push.ts:typeMap gains `sprint_id: 'sprint'` (commit @ +444); `getItemTypeWithPathOverride()` exported with sprint_report path-override at line 458–462 of post-edit push.ts. (2) Path validator at push.ts:184 with `SPRINT_RUNS_PATH_REGEX` + `SPRINT_REPORT_PATH_REGEX` — accepts `.cleargate/sprint-runs/SPRINT-NN/{REPORT,SPRINT-NN_REPORT}.md`; rejects all other sprint-runs paths with `exit(2)`. (3) Smoke script `cleargate-cli/scripts/smoke-push-sprint-artifacts.mjs` (209 LOC) ships with `#!/usr/bin/env node` shebang, `CLEARGATE_SMOKE_DRY_RUN=1` dry-run mode, and warnings:[] assertion implemented as `/warning|unknown_type/i` stdout regex on each of 4 push invocations + structural confirmation line per artifact. (4) `close_sprint.mjs` Step 7.4 block lives at commit line 749–807 with its `// CR-064: mcp push sprint plan + report` comment at line 750; Step 7.5 header dashes at line 808 with `// CR-063: wiki ingest sprint report` at line 809 immediately following. Order verified by grep: Step 7 → Step 7.4 → Step 7.5. (5) Mirror parity confirmed three-way: `diff .cleargate/scripts/close_sprint.mjs ↔ cleargate-planning/.cleargate/scripts/close_sprint.mjs` empty at commit 247b380; `diff cleargate-planning/.cleargate/scripts/close_sprint.mjs ↔ cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` empty in working tree (post-prebuild — templates file is gitignored). MANIFEST.json bump (`+6/-6`) corroborates prebuild ran. (6) Path-validator placement BEFORE `fsPromises.readFile` is architecturally sound — guarantees exit 2 for all non-allowlisted sprint-runs paths instead of exit 1 from a readFile/parseFrontmatter failure on non-.md files like `token-ledger.jsonl`. The plan asked for "allowlist check BEFORE the exclusion check" within the path-validator block; Dev generalized this to "validator BEFORE file I/O" — strictly tighter contract, no regression. (7) Granular per-step try/catch (7.4a, 7.4b separately, inside the outer try) provides per-push isolation: 7.4a plan-push failure does not block 7.4b report-push. M3 plan §CR-064 line 254 explicitly invited this choice ("split into two nested try/catch blocks if the Developer wants per-step granularity"). Dev took the granularity path; both still non-fatal at the outer level. (8) EPIC-027 proof loop closed: `mcp/src/lib/payload-contract.ts:14-23` KNOWN_TYPES contains both `'sprint'` and `'sprint_report'` (verified working tree); `close_sprint.mjs` ordering is Step 7 → Step 7.4 (MCP push) → Step 7.5 (wiki ingest) matching CR-064 §0.5 Q4 SDR-locked decision; smoke script asserts the headline metric (warnings:[]). (9) Zero new dependencies — diff confirms only existing imports + builtins (`node:fs`, `node:child_process`, `node:path`, `node:url`).
STRUCTURAL_DEBT:
  - Smoke script's warnings:[] assertion is a stdout-regex heuristic (`/warning|unknown_type/i`), not a structured JSON-response parse. The CLI does not yet expose `--json` output; until it does, the heuristic is the available signal. A future CR adding `--json` to `cleargate push` would tighten this to a structural `response.warnings.length === 0` check. Non-blocking for CR-064.
  - Step 7.4 plan-path resolver duplicates the archive→pending-sync glob logic that already exists in `close_sprint.mjs` Step 4 (and again in the smoke script's `findSprintPlan()`). Three callsites of the same readdirSync-startsWith-endsWith pattern. Extract-helper opportunity for a future hygiene CR; not blocking.
DEVIATION_VERDICTS:
  - path-validator-first: ACCEPT (tightens exit-code contract; eliminates parseFrontmatter-on-jsonl edge case that would have leaked exit 1)
  - granular-trycatch: ACCEPT (per-step isolation; plan §line 254 explicitly invited this choice; non-fatal semantics preserved at outer level)
EPIC_027_PROOF_LOOP_CLOSED: yes
SPRINT_GOAL_ACHIEVED: yes
```
