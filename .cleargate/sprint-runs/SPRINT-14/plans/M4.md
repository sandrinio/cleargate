role: architect

# SPRINT-14 M4 Plan — Lane Classifier Execution

**Stories:** STORY-022-04, STORY-022-05, STORY-022-06 (all parallel — disjoint code surfaces)
**Wave:** W4 (parallel, three Developers concurrently)
**M3 prerequisites verified:** STORY-022-01 `112a799`, STORY-022-02 `cf8198e`, STORY-022-03 `86bf9af` — all in `git log --oneline`. Live + scaffold `update_state.mjs` byte-identical, `pre_gate_runner.sh` + `pre_gate_common.sh` byte-identical to scaffold mirror.

Flashcards consulted: `#hooks #bash #exit-capture` (CR-008), `#hooks #ledger #jq #regex` (BUG-010), `#bash #macos #portability`, `#cli #commander #subcommand-routing`, `#cli #frontmatter #parse`, `#cli #vitest #vi-mock-hoisting`. Each one is mapped into the surface that needs it below.

---

## §1 Per-Story File-Touch List (Verified)

### STORY-022-04 — `pre_gate_runner.sh` lane-aware post-pass routing

**Files to MODIFY:**
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/pre_gate_runner.sh` — current 308 lines. Insert lane logic between line 295 (case dispatch end) and line 297 (footer block start), wrapping the existing `exit $OVERALL_EXIT` at line 307. Specifically:
  - After line 295 (`esac`), before line 297 (`# Footer`), preserve `OVERALL_EXIT` capture as the "scanner exit" (it is already populated by `run_qa` / `run_arch` writes via the `OVERALL_EXIT=1` lines at 86, 120, 143, 189, 211, 240, 269).
  - Replace the bare `exit $OVERALL_EXIT` at line 307 with a lane-aware branch that resolves `STORY_ID` + `SPRINT_ID` first, runs the lane logic, and only then exits.
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/pre_gate_common.sh` — additive only. Add three helpers near the existing `record_result` block (line 47) and `print_summary` block (line 60):
  - `resolve_story_id_from_branch <branch>` — extracts `STORY-NNN-NN` (or `CR-NNN`, etc.) from a branch like `story/STORY-022-04`. Pattern `^(story|cr|bug)/([A-Z-]+-[0-9]+(-[0-9]+)?)$` → group 2.
  - `resolve_lane <state_json> <story_id>` — `jq -r --arg sid "$STORY_ID" '.stories[$sid].lane // "standard"' "$STATE_JSON"`. Returns `"standard"` on missing-lane, missing-story, or jq-failure (use `|| echo standard`).
  - `append_ld_event <sprint_md> <story_id> <reason>` — locates `## 4. Events` in the sprint markdown; if absent, appends the heading + table header (`| Event | Story | Timestamp | Reason |`/`|---|---|---|---|`) before appending one row. ISO timestamp via `date -u +%FT%TZ` (portable per BUG-010 §4b).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/pre_gate_runner.sh` — same edit as live; verified byte-identical to live before edit (`diff -q` returned empty 2026-04-26).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/pre_gate_common.sh` — same additive edits as live.

**Files to CREATE:**
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_pre_gate_lane_aware.sh` — bash harness covering the 5 Gherkin scenarios. Mirror the structure of `test_planning_v2.sh` (lines 1-50: `set -euo pipefail`, `REPO_ROOT` derivation, `pass`/`fail` counters). 
- Test fixtures: `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/fixtures/state.lane-fast.json`, `state.lane-standard.json`, `state.no-lane.json` (legacy). Each is a minimal v2 (or v1) state.json with one story.

**MUST NOT touch** (these belong to 022-05 or 022-06): `developer.md`, `cli.ts`, `commands/hotfix.ts`, `templates/hotfix.md`, `wiki/topics/hotfix-ledger.md`.

### STORY-022-05 — Developer agent lane-aware execution (doc-only)

**Files to MODIFY:**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/agents/developer.md` — current 101 lines. Append a new top-level section `## Lane-Aware Execution` after line 73 (end of `## Worktree Contract` section, before `## Circuit Breaker` at line 74). Section must contain:
  - Spawn-time lane read from `state.json` (resolve via sprint-active sentinel).
  - lane=fast → skip the architect-plan-citation block entirely (no plan exists for fast-lane stories).
  - lane=standard / lane absent / state.json missing → follow the existing four-agent contract verbatim.
  - Pre-gate scanner is NEVER skipped on lane=fast — that contract belongs to `pre_gate_runner.sh` (STORY-022-04). Single-commit rule, typecheck + tests, NEVER `--no-verify` rules ALL preserved.
  - Demotion handler delegated to orchestrator: if state.json `lane` flips from `fast` to `standard` mid-execution, the orchestrator re-dispatches the story with the architect plan attached. The Developer treats the new dispatch as a fresh spawn — no state machine on the Developer side.
  - First-line marker contract reaffirmed: the Developer's first response line still emits `STORY=NNN-NN` (or `CR=NNN`, etc., per BUG-010 §4b detector vocabulary). Lane is NOT part of the first-line marker.
- `/Users/ssuladze/Documents/Dev/ClearGate/.claude/agents/developer.md` — live mirror, gitignored. After scaffold edit lands and is committed, `cp` the scaffold over the live file. **Note (existing drift, do not "fix" in this story):** live currently differs from scaffold on lines 27-28 + line 34 (live says `npm run typecheck` + a stale flashcard line; scaffold says `cleargate gate typecheck` + the EPIC-018 flashcard). The `cp` will overwrite that drift — call this out in the commit message; do NOT cherry-pick.

**Files to CREATE:**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/lib/agent-developer-section.test.ts` — vitest with three structural assertions (one per Gherkin scenario). Mirror `protocol-section-24.test.ts` (lines 1-40) for shape — extract section between `## Lane-Aware Execution` heading and the next `## ` heading; assert keywords + byte-equality between live and scaffold.

**MUST NOT touch:** `pre_gate_runner.sh`, `pre_gate_common.sh`, `cli.ts`, anything under `commands/`, any template, `wiki/index.md`, `wiki/topics/*`.

### STORY-022-06 — Hotfix lane (template + scaffold mirror + CLI + ledger page)

**Files to CREATE:**
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/hotfix.md` — frontmatter + §1 Anomaly + §2 Files Touched + §3 Verification Steps (with one mandatory checkbox `- [ ]`) + §4 Rollback. Frontmatter shape per story §1.2 (1) — must include `draft_tokens:` and `cached_gate_result:` blocks (required by `template-stubs.test.ts` invariant, see §3 below).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/hotfix.md` — byte-identical scaffold mirror.
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/wiki/topics/hotfix-ledger.md` — frontmatter `type: "synthesis"`, `id: "hotfix-ledger"`, `generated_at: <ISO>`, plus a brief intro paragraph. Empty body (no rows yet — populated at hotfix merge time, not in this story).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/hotfix.ts` — exports `hotfixNewHandler({ slug }: { slug: string }, cli?: HotfixCliOptions): void`. Mirror the v2-aware shape of `commands/sprint.ts` lines 36-100 (`SprintCliOptions` → `HotfixCliOptions`, `defaultExit`, stdout/stderr seam, `cwd` seam) — but hotfix is NOT v2-gated (hotfixes are off-sprint and run regardless of execution_mode), so DROP the `readSprintExecutionMode` / `printInertAndExit` block. Keep the `stdout`/`stderr`/`exit`/`cwd` test seams.
  - Logic: scan `pending-sync/HOTFIX-*.md` and last-7-day `archive/HOTFIX-*.md` for cap (≤3 per rolling window). Read template from `.cleargate/templates/hotfix.md`. Substitute `{ID}` (next available `HOTFIX-NNN` from a max-scan), `{SLUG}` (validated kebab `^[a-z0-9-]+$`), `{ISO}` (current UTC ISO). Write to `.cleargate/delivery/pending-sync/HOTFIX-<NNN>_<slug>.md`. Validate slug; on invalid → exit 1 with `"slug must match ^[a-z0-9-]+$"`. On cap exceeded → exit 1 with the canonical message from §2.1 scenario 3.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/commands/hotfix-new.test.ts` — vitest covering all 5 Gherkin scenarios. Use `tmpdir` fixture pattern (mirror `cleargate-cli/test/commands/`'s existing handlers' test seams). Inject `cwd` via `HotfixCliOptions.cwd`. **No `vi.mock`** — pure filesystem fixtures + the seam (avoids the `vi.mock` hoisting pitfall, FLASHCARD `#cli #vitest #vi-mock-hoisting` 2026-04-18).

**Files to MODIFY:**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — register a sibling top-level group at line 449 (just before `program.parseAsync` at line 500). Pattern matches the `wiki` group (lines 88-150) or `sprint` group (lines 212-248) — `const hotfix = program.command('hotfix').description(...)` then `hotfix.command('new <slug>').action(...)`. **DO NOT** put `hotfix.command('<verb>')` as a parameterized fallback (FLASHCARD `#cli #commander #subcommand-routing` 2026-04-25 — Commander v12 emits `unknown command` before parameterized handlers fire when sibling literals exist; we only have `new` for now, so a literal `.command('new <slug>')` is fine).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/MANIFEST.json` — add a 10th template entry at line 209 (after the `story.md` block at lines 202-208, before the closing `]`). Shape mirrors the existing entries (lines 146-208): `path`, `category`, `overwrite_policy`. Live `.cleargate/templates/hotfix.md` ALSO needs an entry — verify the manifest's existing templates section enumerates BOTH live and mirror or only mirror, and follow that convention.
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/wiki/index.md` — append a new top-level `## Hotfix Ledger` section after the existing `## Sprints` section (line 91) with one bullet linking to `[[topics/hotfix-ledger]]`. **Note:** this section may be overwritten on next `cleargate wiki build` (auto-generated file; line 3 says "Do not edit manually") — flag in §6 ambiguities; story spec implicitly assumes wiki-build picks this up but PROPOSAL-002's wiki-build does NOT auto-include topic synthesis pages today.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/template-stubs.test.ts` — REQUIRED edit (see §3.022-06 below).

**Files to NOT CREATE in this story** (deferred per §6):
- `cleargate-cli/src/lib/hotfix-ledger.ts` — DEFERRED to SPRINT-15. Rationale per §6.022-06.

**MUST NOT touch:** `pre_gate_runner.sh`, `pre_gate_common.sh`, `developer.md`, `update_state.mjs`, `architect.md`, `cleargate-protocol.md`, sprint plan template, sprint_report template, story.md template.

---

## §2 Test Surface Per Story

### STORY-022-04 — five scenarios, bash harness

| Scenario | Test file | Approach |
|---|---|---|
| 1 — lane=fast + pass → skip QA | `.cleargate/scripts/test/test_pre_gate_lane_aware.sh` | Seed fixture `state.lane-fast.json` with `qa_bounces=0`, run a passing scanner harness, assert stdout contains `pre-gate: lane=fast → skipping QA spawn for STORY-XXX-XX` and exit 0. |
| 2 — lane=fast + fail → demote + LD event | same | Seed fixture, induce typecheck fail by pointing scanner at a fixture worktree with bad TS, assert (a) state.json shows `lane=standard`, `lane_demoted_at != null`, `qa_bounces=0`, `arch_bounces=0`; (b) sprint markdown contains a new `## 4. Events` heading + LD row; (c) script exit code = scanner exit (1). |
| 3 — lane=standard → unchanged | same | Run with `state.lane-standard.json`; assert state.json byte-equal pre/post (no lane fields written), no LD row, normal exit. |
| 4 — missing lane field (legacy v1) → treat as standard | same | Use a v1-shape state.json (no lane field on the story); assert behaviour matches scenario 3. Note: this exercises the `// "standard"` jq fallback in `resolve_lane`. |
| 5 — cross-OS portability | code review only (per story §2.1 explicit informational tag) | No automated test. Reviewer verifies `printf '%s\n'`, quoted vars, no `mapfile`/`readarray`, `date -u +%FT%TZ`, jq 1.5+ syntax. |

**Critical bash-exit-capture pattern (FLASHCARD `#hooks #bash #exit-capture` 2026-04-26):** the existing scanner already populates `OVERALL_EXIT` correctly (line 68 init, mutated in run_qa / run_arch). The new lane logic must NOT overwrite it via `$(cmd || true)` — that pattern always returns 0. Use either (a) the existing variable as-is, or (b) capture exit independently with `node ... > "$tmp_stdout" 2> "$tmp_stderr"; UPDATE_EXIT=$?`. The story's §3.2 pseudocode line `node update_state.mjs ... || true` SWALLOWS the exit code on the lane=fast happy-path; this is fine there because we want exit 0 regardless, but flag it in code review so future edits don't extend the `|| true` to the demotion branch.

### STORY-022-05 — three scenarios, vitest structural

| Scenario | Test file | Approach |
|---|---|---|
| 1 — `developer.md` documents lane-aware spawn | `cleargate-cli/test/lib/agent-developer-section.test.ts` | Read `cleargate-planning/.claude/agents/developer.md`; extract section under `## Lane-Aware Execution` heading; assert presence of keywords: `lane=fast`, `lane=standard`, `pre-gate scanner`, `never skipped`, `orchestrator re-dispatch`. |
| 2 — live mirror byte-identical | same | `expect(fs.readFileSync(LIVE)).toBe(fs.readFileSync(SCAFFOLD))` — direct read of both files. |
| 3 — first-line marker preserved | same | Extract pre-existing response-format block; assert `STORY=NNN-NN` literal still present; assert `## Lane-Aware Execution` does NOT contain `lane=` token in the first-line-marker context. |

### STORY-022-06 — five scenarios, vitest

| Scenario | Test file | Approach |
|---|---|---|
| 1 — `cleargate hotfix new copy-fix` scaffolds valid file | `cleargate-cli/test/commands/hotfix-new.test.ts` | tmpdir fixture, run `hotfixNewHandler({ slug: 'copy-fix' }, { cwd })`, read back `.cleargate/delivery/pending-sync/HOTFIX-001_copy_fix.md`, assert frontmatter (`hotfix_id: HOTFIX-001`, `lane: "hotfix"`, `status: "Draft"`) + structural (§1, §2, §3, §4 sections present and non-empty). |
| 2 — ID increments | same | Pre-stage `HOTFIX-001_old_fix.md` in tmpdir; run handler with slug `another-fix`; assert new file `HOTFIX-002_another_fix.md` AND HOTFIX-001 still byte-identical. |
| 3 — cap blocks 4th draft | same | Pre-stage 3 hotfix files (or 1 pending + 2 archived <7 days old via `fs.utimesSync`); run handler with slug `fourth-fix`; assert exit code 1, stderr contains `Hotfix cap: ≤3 per rolling 7-day window`, `pending-sync/HOTFIX-*` count unchanged. |
| 4 — template scaffold mirror byte-equality | extension to `template-stubs.test.ts` (see §3.022-06) | After hotfix is added to TEMPLATE_NAMES, the existing byte-equality test loop covers it. |
| 5 — wiki/index.md links to ledger page | same `hotfix-new.test.ts` OR a dedicated `cleargate-cli/test/wiki/hotfix-ledger-link.test.ts` | Read `.cleargate/wiki/index.md`; assert `/Hotfix Ledger/` heading present AND `topics/hotfix-ledger` link present. **Note:** this is a snapshot of the manually-edited wiki/index.md, NOT a wiki-build output (see §6.022-06 #1). Co-locate with `cleargate-cli/test/wiki/` per FLASHCARD `#test-location #wiki #cli` 2026-04-24. |

---

## §3 Snapshot / Regression Contracts

### §3.022-04 — pre_gate_runner.sh snapshot lock

The Developer must capture the pre-edit byte hash of `.cleargate/scripts/pre_gate_runner.sh` and `.cleargate/scripts/pre_gate_common.sh` BEFORE editing, then post-edit prove the only diffs are:
- The lane-aware branch insertion (between line 295 `esac` and line 297 footer comment).
- The replacement of `exit $OVERALL_EXIT` at line 307.
- The three new helpers in `pre_gate_common.sh` (additive only, no mutation of existing `read_config_field`/`record_result`/`print_summary` etc.).

Verification command: `diff -u <pre-edit> <post-edit>` reviewed by QA. No surprise edits to QA mode (lines 73-194) or ARCH mode (lines 199-287) — those mode handlers already feed `OVERALL_EXIT` correctly and need no change.

Live + scaffold mirror MUST stay byte-identical post-edit (`diff -q .cleargate/scripts/pre_gate_runner.sh cleargate-planning/.cleargate/scripts/pre_gate_runner.sh` empty; same for pre_gate_common.sh).

### §3.022-05 — developer.md byte-equality test

`agent-developer-section.test.ts` is the new snapshot test for this. The existing repo has NO byte-equality test for `developer.md` between live and scaffold. The new test in this story IS the snapshot. The test must run on EVERY `npm test` invocation (it is at `cleargate-cli/test/lib/`, picked up by `vitest run` per `package.json` `test` script).

### §3.022-06 — template-stubs.test.ts MUST be edited

**This is a known story-spec gap, surfaced and resolved here.** The story spec at §4.1 says: *"Template byte-equality (covered by `template-stubs.test.ts` post STORY-022-03 — verify it picks up the new template pair automatically; if not, extend the loop)."*

**Verified 2026-04-26:** `template-stubs.test.ts` lines 15-23 hard-code `TEMPLATE_NAMES = ['Bug.md', 'CR.md', 'Sprint Plan Template.md', 'epic.md', 'initiative.md', 'proposal.md', 'story.md']`. There are 7 names. New templates are NOT auto-picked up. The story body's "if not, extend the loop" clause activates: STORY-022-06 MUST add `'hotfix.md'` to the array (line 22 → 23). Note: even before this sprint, the array misses `sprint_context.md` and `sprint_report.md` (the live + mirror dirs hold 9 templates). Do NOT fix those misses in this story — that's scope bleed into STORY-022-03 retrospective. Add ONLY `'hotfix.md'`. File a flashcard recording the pre-existing 7-vs-9 drift.

### §3 cross-cutting — SPRINT-14 state.json prerequisite

Verified 2026-04-26 21:00 UTC: `.cleargate/sprint-runs/SPRINT-14/state.json` is `schema_version: 1` and contains M1 + M2 + STORY-022-01..03 + STORY-014-02 only. STORY-022-04, -05, -06, -07, -08 are NOT in the stories map. The orchestrator must add them before any M4 work begins. This is NOT a story-owned task — flag to orchestrator (§6).

The first run of `update_state.mjs` against this file will trigger the v1→v2 migration shipped by 022-02 (line 113), injecting `lane: standard` defaults. This is correct behaviour. Migration log line will appear on stderr (`migration: schema_version 1 → 2 for sprint SPRINT-14`).

---

## §4 Parallel-Execution Contract (Three Concurrent Developers)

M4 is fully parallel. Three Developer agents spawn concurrently in disjoint worktrees: `.worktrees/STORY-022-04/`, `.worktrees/STORY-022-05/`, `.worktrees/STORY-022-06/`.

### Strict file ownership (zero overlap verified)

| Owner | Files (live + scaffold mirror where applicable) |
|---|---|
| **STORY-022-04 only** | `.cleargate/scripts/pre_gate_runner.sh`, `.cleargate/scripts/pre_gate_common.sh`, `cleargate-planning/.cleargate/scripts/pre_gate_runner.sh`, `cleargate-planning/.cleargate/scripts/pre_gate_common.sh`, `.cleargate/scripts/test/test_pre_gate_lane_aware.sh`, `cleargate-cli/test/scripts/fixtures/state.lane-{fast,standard,no-lane}.json` |
| **STORY-022-05 only** | `cleargate-planning/.claude/agents/developer.md`, `.claude/agents/developer.md`, `cleargate-cli/test/lib/agent-developer-section.test.ts` |
| **STORY-022-06 only** | `.cleargate/templates/hotfix.md`, `cleargate-planning/.cleargate/templates/hotfix.md`, `.cleargate/wiki/topics/hotfix-ledger.md`, `cleargate-cli/src/commands/hotfix.ts`, `cleargate-cli/src/cli.ts`, `cleargate-cli/test/commands/hotfix-new.test.ts`, `cleargate-cli/test/scripts/template-stubs.test.ts`, `cleargate-planning/MANIFEST.json`, `.cleargate/wiki/index.md` |

### Cross-check: shared-file collisions

- **`.cleargate/scripts/pre_gate_runner.sh` / `pre_gate_common.sh`** — touched ONLY by 022-04. 022-05 / 022-06 must not modify.
- **`cleargate-planning/.claude/agents/developer.md`** — touched ONLY by 022-05.
- **`cleargate-planning/MANIFEST.json`** — touched ONLY by 022-06 in M4. (M3's STORY-022-03 already merged its `sprint_context.md` / `sprint_report.md` updates per commit `86bf9af`; no M4 conflict.)
- **`cleargate-cli/src/cli.ts`** — touched ONLY by 022-06 in M4.
- **`cleargate-cli/test/scripts/template-stubs.test.ts`** — touched ONLY by 022-06.
- **`.cleargate/wiki/index.md`** — touched ONLY by 022-06 in M4. STORY-022-04's LD-event-row append goes to a different file (sprint markdown `pending-sync/SPRINT-14_Process_v2.md`, NOT wiki/index.md).

### Sprint markdown shared file — addressed in §6

The SPRINT-14 sprint markdown (`pending-sync/SPRINT-14_Process_v2.md`) is the destination of STORY-022-04's LD event row at runtime. None of the three M4 stories MODIFY this file as part of their commit; 022-04's append happens at scanner-failure runtime, not at code-edit time. Test fixtures use a SEPARATE temp sprint markdown so production sprint plan stays unchanged during test runs. No collision.

### Dependency closure

- 022-05 depends on STORY-022-02's state.json schema only at READ time (the agent reads state.json at spawn). No code-level coupling — 022-05 ships pure documentation. ✓ already merged.
- 022-04 depends on STORY-022-02's `--lane-demote` CLI flag (verified line 153 of `update_state.mjs`). ✓ already merged.
- 022-06 has zero dependencies on M3 stories beyond `MANIFEST.json` shape. ✓ already merged.

---

## §5 Risk-Table Cross-Reference

| Sprint Plan Risk | Applies to M4 story | Plan-level mitigation |
|---|---|---|
| **R-09** (rolling-window walk slow on large repos) | STORY-022-06 | Cap-stub uses `fs.readdirSync` once on each of `pending-sync/` + `archive/` + `fs.statSync().mtimeMs` per archive entry. On a repo with <500 archive items the walk is sub-second. PROPOSAL-013 §2.5 explicitly defers full-window optimisation. **DO NOT** introduce a cache file or background indexer — that's SPRINT-15 scope. |
| **R-04** (state.json migration loss) | STORY-022-04 (touches state.json via `update_state.mjs`) | The migration is already in 022-02; 022-04 only consumes it. The `--lane-demote` path mutates `lane`, `lane_demoted_at`, `lane_demotion_reason`, `qa_bounces`, `arch_bounces`, `updated_at` — no other fields. Verified at `update_state.mjs` lines 161-167. |
| R-06 (sprint slip) | All three | M4 stories are explicitly named at §6 of the sprint plan as the scope-cut candidates: drop 022-08 first, then optionally 022-06 (if the rolling-window cap-stub is the time sink). 022-04 + 022-05 are the floor. |

R-01, R-02, R-03, R-05, R-07, R-08, R-10, R-11, R-12 do NOT apply to M4 directly (they govern M1 / M2 / M5 surfaces).

---

## §6 Acceptance Gherkin Reference + Open Decisions

### Canonical M4-close smoke test per story

- **STORY-022-04 →** Gherkin Scenario 2 ("lane=fast + scanner fail → auto-demote with LD event"). It exercises BOTH the demotion CLI path AND the LD-event-row append. Scenarios 1 / 3 / 4 cover the orthogonal branches; Scenario 5 is a code-review checkpoint. Pass on Scenario 2 + spot-check on 1/3/4 = M4-close gate.
- **STORY-022-05 →** Gherkin Scenario 1 ("developer.md documents lane-aware spawn behavior"). Scenarios 2 / 3 are byte-equality and contract preservation; both auto-pass once Scenario 1's section is well-formed.
- **STORY-022-06 →** Gherkin Scenario 1 ("`cleargate hotfix new` scaffolds a valid pending-sync file"). Scenario 3 is the cap-stub regression. Together they form M4-close gate.

### Open decisions for orchestrator (3 ambiguities)

1. **STORY-022-04 §1.2 step 2 — invented state name "QA-Skipped (Fast)" not in `VALID_STATES`.** The story spec at line 58 says: *"Update `state.json` story to `state: "QA-Skipped (Fast)"` (or whatever the existing state-vocabulary uses; verify by reading `.cleargate/scripts/constants.mjs` VALID_STATES first)."* Verified 2026-04-26: `constants.mjs` line 34-43 enumerates `VALID_STATES = ['Ready to Bounce', 'Bouncing', 'QA Passed', 'Architect Passed', 'Sprint Review', 'Done', 'Escalated', 'Parking Lot']`. There is no `QA-Skipped (Fast)` state. `update_state.mjs` line 212-214 hard-rejects unknown states. **Three resolution options for the orchestrator/user to pick one:**
   - (a) Use existing `Architect Passed` (semantically: pre-gate scanner pass + no QA spawn = Architect-equivalent gate clearance).
   - (b) Use existing `Sprint Review` (semantically: ready for human review/merge — closest to the fast-lane handoff).
   - (c) Add `QA-Skipped (Fast)` as a new state in `constants.mjs` AND `state.schema.json` AND `STATE_TRANSITIONS` map. This is a schema bump (touches `SCHEMA_VERSION` per protocol §15.4 — would force a `schema_version: 2 → 3` bump in M4, which contradicts the M3 lock).
   
   **Architect recommendation: (a) `Architect Passed`.** Rationale: matches the semantic of a successful pre-gate scan with no QA round-trip needed; avoids a same-sprint schema bump; the `lane: fast` + state `Architect Passed` combination is unambiguous for the Reporter's Lane Audit table since the lane field carries the "this skipped QA" signal. Decision needed before STORY-022-04 spawn.

2. **STORY-022-04 §1.2 step 3 — "sprint markdown §4 events list" does not exist.** Verified 2026-04-26: SPRINT-14 sprint markdown (`SPRINT-14_Process_v2.md`) headings are §1 Consolidated Deliverables, §2 Execution Strategy, ## Milestones, ## Risks & Dependencies, ## Metrics & Metadata, ## Execution Guidelines. **No §4. No § Events.** The Sprint Plan TEMPLATE has the same shape — STORY-022-03 added §2.4 Lane Audit but did NOT add an Events section. The closest "event log" surface is sprint_report.md §2 CR Change Log (per-story event tables) — but that's the REPORT, not the sprint plan.
   
   **Two resolution options:**
   - (a) The Developer's `append_ld_event` helper CREATES `## 4. Events` heading + table on first append, idempotent on subsequent appends. Simple, additive, no template change.
   - (b) STORY-022-04 also amends the Sprint Plan Template (`templates/Sprint Plan Template.md` + scaffold mirror) to include a `## 4. Events` section header by default. This crosses scope into STORY-022-03's surface (already shipped at `86bf9af`).
   
   **Architect recommendation: (a) — runtime auto-create.** Rationale: keeps STORY-022-04 self-contained; the sprint plan stays human-edited until an LD event actually fires, which is rare; future sprints get the section only if needed. Decision needed before STORY-022-04 spawn. The `update_state.mjs --lane-demote` path already mutates state.json with `lane_demoted_at`, so the sprint markdown row is supplementary audit, not source of truth.

3. **STORY-022-06 §1.2 (5) — `cleargate-cli/src/lib/hotfix-ledger.ts` helper.** Story body marks it "deferred optional — if scope is too tight, skip and document as a SPRINT-15 follow-up." 
   
   **Architect recommendation: DEFER to SPRINT-15.** Rationale: the helper isn't on the critical path. STORY-022-07's reporter reads `wiki/topics/hotfix-ledger.md` directly (not via the helper) per the story body's IN-SCOPE for 022-07. STORY-022-06's primary surface (template + CLI scaffold + cap stub + ledger page existence + wiki link) is enough to ship a complete fast-lane substrate. Adding `hotfix-ledger.ts` adds a second cli.ts subcommand registration, a second test file, and an extra commit risk in a sprint already at 15-item scope. File the deferral as a SPRINT-15 follow-up CR named `CR-013-suggested: Hotfix ledger writer helper + cleargate hotfix close subcommand`. The deferral entry must land in the Reporter's `follow_up_crs` field at sprint close.

### Other notes (not ambiguities — flag-only)

- **`.cleargate/wiki/index.md` is auto-generated.** Line 3 says `Auto-generated by cleargate wiki build. Do not edit manually.` STORY-022-06 §3.1 says to add a `## Hotfix Ledger` section linking to `topics/hotfix-ledger.md`. The next `cleargate wiki build` invocation will REGENERATE index.md from raw delivery items + a fixed topic list (currently `topics/sales-pitch-ai-sdlc.md` is the only known topic). There's no current mechanism for wiki-build to discover the new ledger page. **Resolution proposed:** Developer manually edits `wiki/index.md` to add the section (acceptance Scenario 5 passes); separately, file `BUG-011-suggested: wiki-build does not auto-discover new topic synthesis pages` for SPRINT-15. Do NOT extend wiki-build in this story (out-of-scope per story §1.3 OUT-OF-SCOPE list — wiki concerns aren't named, but the story's IN-SCOPE list doesn't include it either).

- **022-05 live developer.md has pre-existing drift.** Lines 27-28 + 34 differ from scaffold (FLASHCARD `#cli #test-seam #exit` 2026-04-20). The `cp` step at the end of 022-05 will overwrite this drift — not a bug, that's the intended behaviour (scaffold is canonical). Note in commit message; no separate flashcard.

---

## §7 Out-of-Scope-for-M4 Note

M4 does NOT touch any of the following — explicitly leave for M5:
- **Reporter contract changes** (`reporter.md` — STORY-022-07 owns).
- **`close_sprint.mjs` validation** of report sections + naming convention (STORY-022-07 owns).
- **Self-upgrade** (`cleargate upgrade` end-to-end on this repo, version bumps to `cleargate@0.6.0` / `mcp@0.2.0` — STORY-014-02 owns).
- **Dogfood end-to-end** of a fast-lane CR through the post-upgrade scaffold (STORY-022-08 owns).

M4 ALSO does NOT promote or change:
- **CR-008 Phase B PreToolUse `pre-edit-gate.sh`** stays in warn-only mode. M4 does not flip it to enforce. The 48h promotion criterion belongs to a separate post-sprint observation, not an M4 story.
- **`pre_gate_runner.sh` argument signature.** Currently `qa|arch <worktree-path> <branch>`. M4 does not add a positional `<story-id>` argument; the story ID is derived from the `<branch>` argument via the new `resolve_story_id_from_branch` helper. This keeps backward compatibility with the existing `cleargate gate qa` / `cleargate gate arch` CLI surface (cli.ts lines 172-186), which passes only worktree+branch.
- **`cleargate-cli/src/lib/hotfix-ledger.ts`** (deferred per §6.022-06 #3).
- **MANIFEST.json version bump** (deferred to M5 STORY-014-02 — sprint plan §"Version bump is sprint-close-only" in Execution Guidelines line 252).
- **state.json fixture migration** for SPRINT-09..13 archives (R-04 mitigated by 022-02; M4 does not retro-touch).

---

## §8 Architect-Side Pre-Spawn Checklist (orchestrator action)

Before spawning the three M4 Developers concurrently, the orchestrator MUST:

1. **Add stories to state.json.** Append `STORY-022-04`, `STORY-022-05`, `STORY-022-06` to `.cleargate/sprint-runs/SPRINT-14/state.json` `stories{}` map with `state: "Ready to Bounce"`, `qa_bounces: 0`, `arch_bounces: 0`, `worktree: null`, `lane: "standard"`, `lane_assigned_by: "architect"`, `lane_demoted_at: null`, `lane_demotion_reason: null`. Use `run_script.sh init_sprint.mjs` if the M3 close already left a clean v2 state.json; otherwise hand-write the additions and trigger v1→v2 migration via the next `update_state.mjs` call.
2. **Resolve §6 ambiguities #1 (state name) and #2 (LD event section).** Get user sign-off before spawn. Recommendations: (a) for #1, (a) for #2.
3. **Confirm §6 ambiguity #3 (hotfix-ledger.ts deferral) — DEFER.** Note in M4 spawn prompt to STORY-022-06 Developer.
4. **Spawn 3 Developers in parallel** with the standard contract from `developer.md`. Each gets the relevant section of this plan + the corresponding story file path.
5. **Pre-gate scanner runs after each story commit** but BEFORE M4-close — ensure the M3-shipped pre_gate_runner.sh executes against each worktree.

End of plan.
