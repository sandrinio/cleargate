# Milestone: M3 — Lane Classifier Substrate (3 Stories)
## Items: STORY-022-01, STORY-022-02, STORY-022-03
## Wave: W3 (strictly sequential — 022-01 → 022-02 → 022-03)

role: architect

All three stories spawn one at a time on the post-M2.5 `sprint/SPRINT-14` HEAD. Each Developer is allowed only the file set listed in §4. M3 sticks to its lane: documentation rubric (022-01) → state.json schema bump + migration (022-02) → templates skeleton (022-03). M3 does NOT touch `pre_gate_runner.sh`, agent files (`developer.md`, `qa.md`, `reporter.md`), `close_sprint.mjs`, hotfix scaffold, or any M1/M2/M2.5 surface.

---

## 1. Per-story file-touch list

All paths absolute. Every line range below was verified with Read at the cited offset on the post-M2.5 tree.

### STORY-022-01 — Architect Lane Classification + Protocol §24

**Modify:**

- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/agents/architect.md` — append a new section `## Lane Classification` AFTER the existing `## Protocol Numbering Resolver` section (verified ends at L116) and BEFORE the `## Guardrails` section (verified at L118). The new section contains:
  - The seven-check rubric copied verbatim from PROPOSAL-013 §2.3 (LOC cap, forbidden surfaces, no new dependency, single acceptance scenario, existing tests cover, `expected_bounce_exposure: low`, no epic-spanning subsystem touches).
  - The forbidden-surface file-path prefix list verbatim from STORY-022-01 §1.2 (six bullets: `mcp/src/db/`, `mcp/src/auth/`, `cleargate.config.json` / `mcp/src/config.ts`, `mcp/src/adapters/`, `cleargate-planning/MANIFEST.json`, security-relevant code).
  - The §2.4 "Lane Audit" emission contract: "Architect MUST write a `## §2.4 Lane Audit` subsection in the Sprint Plan listing every fast-lane story with a ≤80-char rationale. Empty by default; rows added only for non-`standard` lanes."
- **Live-mirror sync:** the live file at `/Users/ssuladze/Documents/Dev/ClearGate/.claude/agents/architect.md` is gitignored per `/.gitignore` (verified — `.claude/` is gitignored at repo root). Per STORY-022-01 §3.1 the live file is committed-via-scaffold-canonical only. **Resolution:** Developer edits ONLY the scaffold file; the live `.claude/agents/architect.md` is hand-mirrored post-edit (`cp cleargate-planning/.claude/agents/architect.md .claude/agents/architect.md`) so the running session sees the new rubric, but the commit contains only the scaffold change. Verified live and scaffold are byte-equal today (`diff` returned empty above; both are 8545 bytes).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md` — append `## 24. Lane Routing` AFTER the existing `## 23. Doctor Exit-Code Semantics` (verified at L946; this is the current max §). Body ≤30 lines per the protocol section-length convention. Content (per STORY-022-01 §1.2):
  - The seven-check rubric (mirrored from agent file — same numbering 1..7).
  - Demotion mechanics: one-way `fast → standard`; demotion writes `lane_demoted_at` + `lane_demotion_reason` and resets `qa_bounces` + `arch_bounces` to 0 (cites STORY-022-02 schema).
  - Forbidden-surface file-path prefix list (six prefixes; same list as the agent file).
  - **LD event-type addition** to §10 vocabulary. **§10 NOTE:** §10 is `## 10. Knowledge Wiki Protocol` (verified L206), NOT an event-type vocabulary. The event-type vocabulary lives in `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md:10-14` (header comment block) AND in `## 17. Mid-Sprint Change Request Triage (v2)` event tokens (verified L691). **Decision (Architect):** the LD event-type registration goes in §24's body as a self-contained sentence ("`LD: lane-demotion` — emitted by `pre_gate_runner.sh` when a fast-lane story is auto-demoted; counted in Sprint Report §3 alongside CR:* / UR:* tallies"). STORY-022-03 owns the parallel registration in `sprint_report.md` header vocabulary. M3 does NOT touch §10 or §17.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — same `## 24. Lane Routing` append, byte-identical to the live file. Verified the two files are byte-equal today.

**Create:** none.

**Stale-§ rewrite (per architect.md Protocol Numbering Resolver §3):** STORY-022-01 §1.2 + §3.2 cite `§14 "Lane Routing"`. **Verified §14 is already occupied by `## 14. Multi-Participant Sync` (live L518 + scaffold mirror; STORY-010-08; SPRINT-13).** Current max §: **23** (`## 23. Doctor Exit-Code Semantics`, L946). Per resolver rule, next free is **§24**. **STORY text cites §14 — stale, rewritten to §24.** Developer must NOT use §14.

### STORY-022-02 — state.json Schema v1→v2 + Migration + CLI Flags

**Modify:**

- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/constants.mjs:26` — change `export const SCHEMA_VERSION = 1` to `2`. Update the JSDoc shape comment block at L4-L24: re-label header as `state.json v2 Schema`, add the four new per-story fields with their default values:
  ```js
  // "lane": "standard" | "fast",                   // additive v2; default "standard"
  // "lane_assigned_by": "architect" | "human-override" | "migration-default",
  // "lane_demoted_at": "<ISO-8601>" | null,
  // "lane_demotion_reason": string | null,
  ```
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/constants.mjs` — same edit, byte-identical (verified the two files diff-equal today).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/state.schema.json` — JSON Schema bump:
  - L20 `"const": 1` → `"const": 2`. L4 title `"...v1"` → `"...v2"`. L3 `"$id": ".../state.v1.json"` → `state.v2.json`. L21 description: bump version note.
  - Add `lane`, `lane_assigned_by`, `lane_demoted_at`, `lane_demotion_reason` to `StoryEntry.properties` (L65-L107). Add the same four to `StoryEntry.required` (L57-L65). Enums: `lane: ["standard","fast"]`, `lane_assigned_by: ["architect","human-override","migration-default"]`. Both demotion fields: `{ "type": ["string","null"], "format": "date-time" }` for `lane_demoted_at` and `{ "type": ["string","null"] }` for `lane_demotion_reason`.
  - **No scaffold mirror for `state.schema.json`** — verified `cleargate-planning/.cleargate/scripts/` does NOT contain `state.schema.json` (only `constants.mjs` + `update_state.mjs` are mirrored; verified `find` output).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/validate_state.mjs:34-38` — **CRITICAL.** The validator currently does strict `state.schema_version !== SCHEMA_VERSION` equality (L34-L38). Once `SCHEMA_VERSION = 2`, every existing v1 file will fail validation BEFORE migration runs (because `update_state.mjs:75-80` calls `validateState` BEFORE any migration). Fix: split validation into a "shape validator" (no version check) + a callsite-level version gate. Two-step:
  1. `validateState(state)` keeps its current full check, including `schema_version === 2`.
  2. Add a new exported helper `validateShapeIgnoringVersion(state)` that runs every check EXCEPT the L34-L38 schema_version equality. Returns `{ valid, errors }`.
  3. Update `update_state.mjs:75` to call `validateShapeIgnoringVersion(state)` BEFORE migration, then `validateState(migratedState)` AFTER migration. Same change in `close_sprint.mjs:132` (verified — calls `validateState` at L132 with no version awareness).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/update_state.mjs` — full mirror of all `update_state.mjs` edits below. Both files are byte-equal today; must remain byte-equal post-edit. validate_state.mjs is **not** mirrored in the scaffold (verified — only `constants.mjs` + `update_state.mjs` are present in `cleargate-planning/.cleargate/scripts/`).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/update_state.mjs` — three additions:
  1. **Migration on read** (insert between L66-L80): after `state = JSON.parse(...)` at L68 succeeds, BEFORE `validateState` at L75, check `if (state.schema_version === 1)` and call a new `migrateV1ToV2(state)` that:
     - Sets `state.schema_version = 2`.
     - For each `storyId` in `state.stories`: assign `lane ??= "standard"`, `lane_assigned_by ??= "migration-default"`, `lane_demoted_at ??= null`, `lane_demotion_reason ??= null`.
     - Emits one stderr line: `migration: schema_version 1 → 2 for sprint <state.sprint_id> (<N> stories defaulted to lane: standard)\n`.
     - Returns the mutated state. Atomic write via existing `atomicWrite(stateFile, state)` immediately after migration so subsequent reads see v2.
  2. **`--lane <standard|fast>` flag** (insert as a new branch in the `main()` action chain at L89-L151, sibling to `--qa-bounce` / `--arch-bounce`):
     - Accepts via positional `args[1] === '--lane'` AND `args[2] ∈ {standard, fast}`.
     - Sets `story.lane = args[2]`; `story.lane_assigned_by = "human-override"`; updates `story.updated_at` + `state.last_action` + `state.updated_at`; atomic write.
     - **`expected_bounce_exposure: med|high` cross-read check (per STORY-022-02 §1.2 last bullet):** stub as TODO with a one-line `// TODO: STORY-022-04 — cross-read sprint plan to enforce rubric §6 contradiction.` See §6 ambiguity below for the routing decision.
  3. **`--lane-demote <reason>` flag** (sibling branch):
     - Sets `story.lane = "standard"`; `story.lane_demoted_at = new Date().toISOString()`; `story.lane_demotion_reason = args[2]`; resets `story.qa_bounces = 0` and `story.arch_bounces = 0`; updates timestamps + `last_action`; atomic write. Reject if `--story` arg missing or story absent (existing pattern at L82-L85).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/init_sprint.mjs:155-166` — extend the per-story stub at L157-L165 with the four new lane fields:
  ```js
  stories[id] = {
    state: 'Ready to Bounce',
    qa_bounces: 0,
    arch_bounces: 0,
    worktree: null,
    updated_at: now,
    notes: '',
    lane: 'standard',
    lane_assigned_by: 'migration-default',
    lane_demoted_at: null,
    lane_demotion_reason: null,
  };
  ```
  Per STORY-022-02 §2.1 Scenario 1 the default `lane_assigned_by` for fresh init is `"migration-default"` (the "or 'architect' if init was given a Sprint Plan §1 Lane column" branch is **deferred** — defer matches the spec language). The `state.schema_version` field at L169 already pulls from `SCHEMA_VERSION`, so it will become `2` automatically once constants.mjs bumps. No other change to init_sprint.mjs required.
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs:132` — switch the call from `validateState(state)` to `validateShapeIgnoringVersion(state)` then re-call `validateState(state)` only after a guard read. **Simpler resolution:** since close_sprint.mjs runs AFTER update_state.mjs has migrated the file at least once during the sprint, by close time every state.json on disk is v2. **Architect decision:** keep close_sprint.mjs calling `validateState(state)` unchanged AS LONG AS update_state.mjs's migration writes the v2 file synchronously on first read. Add an explicit migration call at close_sprint.mjs:130 (before validateState): `if (state.schema_version === 1) { state = migrateV1ToV2(state); atomicWrite(stateFile, state); }`. This keeps close_sprint.mjs's contract clean — it always sees v2 — without weakening the validator. Import `migrateV1ToV2` + `atomicWrite` from update_state.mjs (export the migration helper from update_state.mjs as a side-effect of the new branch).

**Create:**

- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/fixtures/state-v1-spri-12.json` — copy of a real SPRINT-12 state.json snapshot (or hand-built minimal v1 fixture). Used by the migration roundtrip test (§2 below). Content: a minimal `schema_version: 1` state with 3 stories, each carrying the v1 field set ONLY (state, qa_bounces, arch_bounces, worktree, updated_at, notes).

**Out of scope (per STORY-022-02 §1.3):**

- Architect rubric (022-01).
- Templates (022-03).
- pre_gate_runner.sh demotion event emission (022-04 / M4).
- Developer agent reading lane (022-05 / M4).
- Hotfix lane (022-06 / M4).
- Reporter contract (022-07 / M5).
- Self-disabling rubric (deferred per EPIC-022 §2 OUT-OF-SCOPE).

### STORY-022-03 — Sprint Plan + story + sprint_report Lane Skeletons

**Modify:**

- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/Sprint Plan Template.md` — two edits:
  1. L43-L45 §1 Consolidated Deliverables table: add `Lane` column. Per STORY-022-03 §1.2 the placement is "between `Title` and `Milestone`, or at end — pick one and document". **Architect picks: insert between `Title` and `Milestone`** (matches SPRINT-14's existing §1 table which has `Title | Complexity | Parallel? | Bounce Exposure | Milestone` shape; lane is an additional planning signal that belongs adjacent to Title). New row shape: `| {STORY-NNN-NN} | {Title} | standard / fast | M{N} | y / n | low / med / high |`. Update the header row + the example row identically.
  2. After §2.3 Shared-Surface Warnings (L63-L65) and BEFORE §2.4 ADR-Conflict Flags (L67-L69), insert a new `### 2.4 Lane Audit` subsection with the table skeleton:
     ```markdown
     ### 2.4 Lane Audit
     {Architect populates one row per fast-lane story during Sprint Design Review. Empty by default — rows added only for non-`standard` lanes.}

     | Story | Lane | Rationale (≤80 chars) |
     |---|---|---|
     | `STORY-NNN-NN` | fast | <one-line rationale> |
     ```
     **Renumber:** existing §2.4 "ADR-Conflict Flags" becomes `### 2.5 ADR-Conflict Flags`.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/Sprint Plan Template.md` — same edit; byte-identical (verified equal today).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/story.md:46` — add a new frontmatter field `lane: "standard"` immediately after `expected_bounce_exposure: "low"` (verified L46). Update the `<instructions>` §0.1 v2 Decomposition Signals block (L29-L32) with a new line documenting `lane`: `\`lane\`: "standard" | "fast" — Architect-set during Sprint Design Review per the seven-check rubric in protocol §24. Default "standard". Absent in pre-EPIC-022 stories means standard per the migration default in update_state.mjs.`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/story.md` — same edit; byte-identical.
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md` — five edits:
  1. L6 frontmatter: bump `template_version: 1` → `template_version: 2`.
  2. L9 first comment: change `template_version: 1` → `template_version: 2` to keep the inline comment in sync.
  3. L10-L14 vocabulary block: append a new line `     Lane-Demotion: LD` between L13 Circuit-breaker line and L14 closing comment.
  4. §3 Execution Metrics table (L71-L96): insert SIX new rows (per STORY-022-03 §1.2 sprint_report bullet — the six metric rows). Insert AFTER the existing `Stories planned / shipped / escalated / carried over` block (L73-L77) and BEFORE the `Total QA bounces` row (L78). New rows verbatim:
     ```markdown
     | Fast-Track Ratio | N% |
     | Fast-Track Demotion Rate | N% |
     | Hotfix Count (sprint window) | N |
     | Hotfix-to-Story Ratio | N |
     | Hotfix Cap Breaches | N |
     | LD events | N |
     ```
  5. §5 Framework Self-Assessment block (L128-L166): insert THREE new subsections AFTER the existing `### Process` table (which ends at L158) and BEFORE the existing `### Tooling` table (L160). Skeletons only — values populated by Reporter (STORY-022-07 / M5).
     - **`### Lane Audit`**: table with `Story | Files touched | LOC | Demoted? | In retrospect, was fast correct? (y/n) | Notes`.
     - **`### Hotfix Audit`**: table with `Hotfix ID | Originating signal | Files touched | LOC | Resolved-by SHA | Could this have been a sprint story? (y/n) | If y — why was it missed at planning?`.
     - **`### Hotfix Trend`**: one-paragraph narrative placeholder describing the rolling 4-sprint hotfix count + monotonic-increase flag. STORY-022-03 §1.2 names this "narrative placeholder"; literal text:
       ```markdown
       <Reporter writes a one-paragraph narrative summarising the rolling 4-sprint hotfix count
       and a monotonic-increase flag (yes/no). Empty by default — leave the placeholder text
       intact for sprints with no hotfixes.>
       ```
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/sprint_report.md` — same five edits; byte-identical.

**Create:** none.

**Out of scope (per STORY-022-03 §1.3):**

- Reporter validation enforcing the new sections (STORY-022-07 / M5).
- `close_sprint.mjs` activation logic (STORY-022-07 / M5).
- Architect emitting Lane Audit rows (STORY-022-01 owns the agent contract).
- Filling demonstration data (empty skeletons only).
- Hotfix template (STORY-022-06 / M4).

---

## 2. Test surface per story

### STORY-022-01 — 4 Gherkin scenarios

Map each Gherkin scenario from STORY-022-01 §2.1 to a concrete test:

- **Scenario 1 (Architect agent file documents the seven-check rubric):** new test in `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/protocol-section-24.test.ts` (mirror the pattern from `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/protocol-section-14.test.ts:1-55`). Read `cleargate-planning/.claude/agents/architect.md`, assert presence of `## Lane Classification`, of seven numbered checks (1..7), and of the Lane Audit emission contract sentence.
- **Scenario 2 (Protocol §24 documents rubric, demotion, forbidden surfaces, LD event):** same test file. Read `.cleargate/knowledge/cleargate-protocol.md`, assert `## 24. Lane Routing` heading present, content includes "rubric", "demotion", a forbidden-surface table (checked via `grep`-style `expect(content).toContain('mcp/src/db/')`), and the `LD: lane-demotion` event-type registration sentence.
- **Scenario 3 (Protocol byte-equality preserved):** same test file, mirroring `protocol-section-14.test.ts:50-54` exactly. Read both protocol files and `expect(dogfood).toBe(scaffold)`.
- **Scenario 4 (§24 length ≤30 lines):** same test file. Slice the protocol content from `## 24.` heading to the next `## ` heading or EOF; count lines; assert ≤30.

**Total new tests: 4** (one per Scenario, in one new file).

**Existing tests preserved:** `protocol-section-14.test.ts` (Multi-Participant Sync) MUST continue to pass — STORY-022-01 does not touch §14. `protocol-section-12.test.ts` and `protocol-section-13.test.ts` similarly preserved.

### STORY-022-02 — 5 Gherkin scenarios

Map each Gherkin scenario from STORY-022-02 §2.1 to a concrete test file:

- **Scenario 1 (Fresh init writes schema_version 2 + per-story lane defaults):** new test in `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_init_sprint_v2.sh` (bash table-driven harness mirroring `test_close_pipeline.sh:1-50` shape). Use `mktemp -d`, set `CLEARGATE_REPO_ROOT`, run `node .../init_sprint.mjs SPRINT-TEST --stories S1,S2`. Assert `state.json.schema_version === 2`, every story has `lane: "standard"` + `lane_assigned_by: "migration-default"` + both demotion fields `null`.
- **Scenario 2 (v1 state.json migrates additively on first read):** same harness. Seed a v1 fixture from `cleargate-cli/test/scripts/fixtures/state-v1-spri-12.json` (created in §1 above). Run `update_state.mjs <STORY-ID> Bouncing` (any flag — read-only counts since the migration runs pre-validate). Assert post-write `schema_version === 2`, every story carries the four new fields, AND the original v1 fields (state, qa_bounces, arch_bounces, worktree, updated_at, notes) byte-preserve. Capture stderr; assert it contains `migration: schema_version 1 → 2 for sprint <ID> (3 stories defaulted to lane: standard)`.
- **Scenario 3 (`--lane fast` sets the field on a single story):** same harness. Seed v2 state with story S1 at `lane: "standard"`. Run `update_state.mjs S1 --lane fast`. Assert S1's `lane === "fast"`, `lane_assigned_by === "human-override"`, no other story mutated.
- **Scenario 4 (`--lane-demote <reason>` flips lane to standard with reason + counter reset):** same harness. Seed v2 state with story S1 at `lane: "fast"`, `qa_bounces: 0`, `arch_bounces: 0`. Run `update_state.mjs S1 --lane-demote "pre-gate scanner failed"`. Assert `lane === "standard"`, `lane_demoted_at` is an ISO timestamp, `lane_demotion_reason === "pre-gate scanner failed"`, both bounce counters at 0. Add a sibling case where the bounce counters are pre-set to 2 each; assert they reset to 0.
- **Scenario 5 (Idempotency — re-reading v2 does not double-migrate):** same harness. Seed a v2 state.json (post-Scenario-2 output). Re-run `update_state.mjs <ID> Bouncing`. Assert (a) stderr does NOT contain a migration log line, (b) the file's `schema_version` stays `2`, (c) the file body is byte-equal modulo flag-driven mutations only (the `--qa-bounce`-style action increments a counter; the migration codepath stays silent).

**Plus a vitest unit test** `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/state-schema-v2.test.ts`:
- Reads `.cleargate/scripts/state.schema.json`; asserts `properties.schema_version.const === 2`, `StoryEntry.required` includes the four new fields, lane enum is `["standard","fast"]`.
- Asserts `validateState({...v1 state...}).valid === false` (post-bump v1 fails strict validation) AND `validateShapeIgnoringVersion({...v1 state...}).valid === true` (the new helper accepts v1 shape pre-migration).

**Cross-fixture regression sweep (R-04 mitigation, non-negotiable):** run all 5 existing tests that hard-code `schema_version: 1` and confirm they still pass post-bump:
- `cleargate-cli/test/commands/story.test.ts:67`
- `cleargate-cli/test/commands/sprint-archive-stamp.test.ts:94`
- `cleargate-cli/test/commands/sprint-archive.test.ts:78`
- `.cleargate/scripts/test/test_cross_project_routing.sh:86,207`
- `.cleargate/scripts/test/test_close_pipeline.sh:43`
- `.cleargate/scripts/test/test_report_body_stdin.sh:31`
- `.cleargate/scripts/test/test_flashcard_enforcement.sh:80`

If any of these test fixtures pipe v1 state through a code path that calls `validateState` BEFORE `update_state.mjs` migrates, the test will break. Developer-022-02 must run `npm test` for cleargate-cli AND `bash .cleargate/scripts/test/test_close_pipeline.sh` after the migration helper lands AND confirm zero regressions. **If any do regress, the fix is to either (a) bump the fixture to v2 inline, OR (b) route the test through update_state.mjs at fixture-setup time so migration auto-runs.** Architect prefers (a) for inline-state-json fixtures (the 5 test files above), (b) is unnecessary because the migration is forward-write.

### STORY-022-03 — 4 Gherkin scenarios

Map each Gherkin scenario from STORY-022-03 §2.1:

- **Scenario 1 (Sprint Plan Template has Lane column + §2.4 Lane Audit skeleton):** extend `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_planning_v2.sh` (verified the existing harness pattern, see L20-L60). Add three new assertions: (a) `grep -q '| Lane |' "$LIVE_SPT"` (header has the column), (b) `grep -q '^### 2.4 Lane Audit' "$LIVE_SPT"` (subsection present), (c) same two greps on `$MIRROR_SPT`. Also re-verify byte-equality (existing assertion at L46-L50 covers).
- **Scenario 2 (story.md frontmatter declares lane field with documented default):** same harness. Add `grep -q 'lane: "standard"' "$LIVE_STORY"` and on `$MIRROR_STORY`. Plus a grep on the `<instructions>` block for the lane-default documentation line.
- **Scenario 3 (sprint_report.md gains §3 metrics + §5 Process tables + Hotfix Trend + version bump + LD vocabulary):** new harness `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_sprint_report_v2.sh` (bash, mirrors test_planning_v2.sh shape). Greps:
  - `grep -q 'template_version: 2' "$LIVE_SR"` (frontmatter bump).
  - `grep -q '^| Fast-Track Ratio |' "$LIVE_SR"` and the other 5 metric rows.
  - `grep -q '^### Lane Audit' "$LIVE_SR"`, `grep -q '^### Hotfix Audit' "$LIVE_SR"`, `grep -q '^### Hotfix Trend' "$LIVE_SR"`.
  - `grep -q 'Lane-Demotion: LD' "$LIVE_SR"` (vocabulary block).
  - All same on `$MIRROR_SR`.
- **Scenario 4 (Scaffold mirror byte-equality for all three template pairs):** the existing `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/template-stubs.test.ts:47-55` already asserts byte-equality for all 7 templates in a loop (verified). Story 022-03 ADDS no new file to that test — the existing test surface naturally extends. Developer must re-run `npm test cleargate-cli/test/scripts/template-stubs.test.ts` post-edit and confirm green.

**Total new tests: 4 Gherkin coverage assertions** (Scenarios 1+2 extending `test_planning_v2.sh`, Scenario 3 in new `test_sprint_report_v2.sh`, Scenario 4 covered by existing test). Plus a sanity check that `template-sync-fields.test.ts:91-99` still passes (the byte-equality assertions for the 5 work-item templates are unaffected; story.md is in that set and gains a new field but stays byte-equal between live + mirror).

---

## 3. Snapshot / regression contracts

**STORY-022-01 — protocol byte-equality lock (preserves STORY-014-01 round-2 invariant):**
- The existing `protocol-section-14.test.ts:50-54` byte-equality test on the protocol file pair MUST continue to pass after §24 lands. Developer-022-01 verifies post-edit by running `diff .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` and confirming zero output.
- The new `protocol-section-24.test.ts` adds an independent byte-equality assertion specific to the §24 era; both tests must pass concurrently.

**STORY-022-02 — schema fixture lock:**
- The new fixture `cleargate-cli/test/scripts/fixtures/state-v1-spri-12.json` is a regression lock. The migration roundtrip test compares the post-migration shape against an embedded expected object (NOT a separate v2 fixture file — the v2 shape is computed in-test from the v1 fixture + the four-default rule). This avoids a "two fixtures must stay in sync" maintenance burden.
- **Idempotency lock** (Scenario 5): re-running `update_state.mjs` on the post-migration v2 file must leave the file byte-equal modulo flag-driven mutations. Test asserts SHA256 equality between two consecutive read-bouncing-write cycles starting from a v2 file.

**STORY-022-03 — template byte-equality lock:**
- All three templates have an existing live-vs-mirror byte-equality assertion in `template-stubs.test.ts:47-55`. Pattern is `expect(live).toBe(mirror)` for each name in `TEMPLATE_NAMES` (verified — that array contains all 7 templates). Post-edit the assertion runs unchanged and must pass.
- The BUG-008 commit `bc56daa` (verified via `git show --stat bc56daa`) modified `readiness-gates.md` in two mirrored locations and the associated test asserts byte-equality the same way (verified `template-stubs.test.ts:47-55` and `template-sync-fields.test.ts:91-99` for templates; `readiness-gates.md` is asserted via its own gate-loader path). The pattern is established. Developer-022-03 mirrors via `cp` after each edit, runs `diff` to verify, then `npm test`.

**No hook snapshot work in M3.** None of the three M3 stories touch `.claude/hooks/*.sh`. The five existing hook lock files at `cleargate-cli/test/snapshots/hooks/*.{cr-008,cr-009,bug-009}.sh` are untouched. Any unexpected diff = QA kicks back.

---

## 4. Sequential-execution contract

M3 is **strictly serial — one Developer at a time, one story at a time**. Per Sprint Plan §2.1 phase-plan and verified by the dependency graph below.

**Order: STORY-022-01 → STORY-022-02 → STORY-022-03.**

Reasons (verified):
1. **022-01 first** because it defines the rubric (seven checks) AND the LD event-type vocabulary that 022-02's `--lane-demote` flag must mirror. If 022-02 lands first with a `--lane-demote` flag whose semantics diverge from 022-01's protocol §24, QA kicks back on the contradiction. 022-01 defines the contract; 022-02 implements one half of it.
2. **022-02 second** because it bumps `SCHEMA_VERSION 1 → 2` and adds the four lane fields. 022-03's sprint_report.md `template_version: 1 → 2` bump is a related-but-disjoint version bump that depends on 022-02's schema being defined first (so the templates' Reporter-readable skeleton has a real schema to validate against, even though that validation is M5/STORY-022-07 work).
3. **022-03 last** because the templates carry the lane fields that 022-02 just added. Templates without 022-02's schema in place would emit dangling fields that init_sprint.mjs cannot fill.

**Per-story file allow-list (no Developer may edit any other file):**

### Developer-A (STORY-022-01) — allowed:
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/agents/architect.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/.claude/agents/architect.md` (live mirror — `cp` from scaffold post-edit; live is gitignored, so cp is a runtime-effect-only step, not a commit step).
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/protocol-section-24.test.ts` (NEW)

### Developer-A — forbidden:
- Anything in `.cleargate/scripts/` (022-02 territory).
- Any template under `.cleargate/templates/` (022-03 territory).
- Any other agent file (`developer.md`, `qa.md`, `reporter.md`).
- Any hook script.

### Developer-B (STORY-022-02) — allowed:
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/constants.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/constants.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/state.schema.json`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/validate_state.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/update_state.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/update_state.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/init_sprint.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_init_sprint_v2.sh` (NEW)
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/state-schema-v2.test.ts` (NEW)
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/fixtures/state-v1-spri-12.json` (NEW)
- The 5 existing v1-fixture test files listed in §2 — modify ONLY their `schema_version: 1` literal to `2` AND inject the four lane fields per story (the cross-fixture regression sweep). No other edit to those files.

### Developer-B — forbidden:
- Any agent file (022-01 / 022-05 territory).
- Protocol file (022-01 territory).
- Templates (022-03 territory).
- Hooks (M1/M2 territory).
- `mcp/` (none of M3 touches MCP).
- The scaffold mirror of `validate_state.mjs` does NOT exist (verified — only `constants.mjs` + `update_state.mjs` are mirrored). Do NOT create a scaffold mirror for `validate_state.mjs`.
- `pre_gate_runner.sh` and `pre_gate_common.sh` (M4 territory).

### Developer-C (STORY-022-03) — allowed:
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/Sprint Plan Template.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/Sprint Plan Template.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/story.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/story.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/sprint_report.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_planning_v2.sh` (extend existing — add lane-related assertions; do NOT rewrite)
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_sprint_report_v2.sh` (NEW)

### Developer-C — forbidden:
- Anything in `.cleargate/scripts/*.mjs` (022-02 territory).
- Any agent file.
- Protocol file.
- Hooks.
- Other templates (`Bug.md`, `CR.md`, `epic.md`, `proposal.md`, `initiative.md`, `sprint_context.md`) — STORY-022-03 §1.2 names exactly three template files.

### Cross-cutting: shared file — none in M3
- The protocol file is touched only by 022-01.
- The state.json schema files are touched only by 022-02.
- The three templates are touched only by 022-03.
- The 5 existing v1-fixture test files (story.test.ts etc.) are touched only by 022-02 in the cross-fixture regression sweep. Developer-C must NOT touch them.

**Result:** zero shared-file collisions across the three Developers AS LONG AS the order is preserved. The serialization is mandatory not because of merge conflicts but because of contract dependency (rubric → schema → templates).

---

## 5. Risk-table cross-reference

M3 surfaces touch the following risks from Sprint Plan §Risks:

- **R-04 (state.json migration lossy → SPRINT-09..13 archive state.json files become unreadable).** **Owned by M3 / STORY-022-02 / Developer-B.** Mitigation per §1 above:
  - Migration is **strictly additive**: `lane: "standard", lane_assigned_by: "migration-default", lane_demoted_at: null, lane_demotion_reason: null` injected on first read; no existing field is renamed, dropped, or re-typed.
  - The cross-fixture regression sweep (§2) explicitly tests that all 5 existing v1 hard-coded state.json fixtures continue to roundtrip after the bump.
  - The `validate_state.mjs` split into `validateState` (strict, post-migration) + `validateShapeIgnoringVersion` (pre-migration) is **non-negotiable**. Without it, the validator runs before migration on every read and rejects every v1 file. QA kicks back if the split is missing.
  - Defaults are documented verbatim: `lane: "standard"`, `lane_assigned_by: "migration-default"`. These are the canonical R-04 mitigation values.
  - SPRINT-10/11/12/13 archive state.json files MUST remain readable post-022-02. Verifiable smoke (M3-close): pick `.cleargate/sprint-runs/SPRINT-12/state.json` (or SPRINT-13), invoke `update_state.mjs` against it (read-only — no transition) and confirm migration log line + post-migration v2 shape + zero data loss on existing fields.
- **R-01 (PROPOSAL-013 not approved → M3 cannot start).** Verified: PROPOSAL-013 frontmatter at `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/wiki/proposals/PROPOSAL-013.md` is approved (per Sprint Plan §2.1: "PROPOSAL-013 awaits Gate 1 approval at sprint kickoff" — and the M3 stories are all `status: Approved` per their frontmatter, e.g. STORY-022-01 L13 `approved: true`). R-01 has fired and resolved. M3 spawns unconditionally.
- **R-05 (hook chain triple-touch on `stamp-and-gate.sh`).** Not active in M3. M3 does not touch any hook script.
- **R-02 (CR-008 Phase B false positives).** Not active in M3. M3 does not flip `pre-edit-gate.sh` mode from warn to enforce. M2's CR-008 ships the warn-mode default; M3 does not change this.
- **R-08 (BUG-008 widens the gate).** Not active in M3. M3 does not touch readiness predicates or `readiness-gates.md`.
- **R-03 (Reporter contract change gates SPRINT-14 close).** Forward-flagged but not active in M3. STORY-022-07 (M5) owns the activation logic; M3 only ships the template skeleton (022-03). The validation does not engage in M3 because (a) `template_version: 2` is set but `close_sprint.mjs` does not yet check it, and (b) no M3 story uses `lane: fast` (verified: SPRINT-14 state.json shows all stories at default lane = standard pre-022-02; post-022-02 all stories carry `lane: "standard"` via migration default).

**No new risks surfaced by M3 plan inspection beyond R-04. R-04's mitigation is fully specified above.**

---

## 6. Acceptance Gherkin reference (canonical M3-close smoke tests)

Each story's acceptance criteria are enumerated in the source files (STORY-022-01 §2.1 = 4 scenarios, STORY-022-02 §2.1 = 5 scenarios, STORY-022-03 §2.1 = 4 scenarios). M3-close validates a curated subset.

**Canonical M3-close smoke (single end-to-end roundtrip — STORY-022-02 §2.2 manual verification + cross-story integration):**

```bash
# 1. Fresh-init a synthetic sprint via init_sprint.mjs
TMPDIR=$(mktemp -d)
export CLEARGATE_REPO_ROOT="$TMPDIR"
mkdir -p "$TMPDIR/.cleargate/delivery/pending-sync"
echo '---
sprint_id: "SPRINT-TEST"
execution_mode: "v2"
---
# SPRINT-TEST' > "$TMPDIR/.cleargate/delivery/pending-sync/SPRINT-TEST.md"
node .cleargate/scripts/init_sprint.mjs SPRINT-TEST --stories STORY-001-01,STORY-001-02

# 2. Verify v2 shape on disk
cat "$TMPDIR/.cleargate/sprint-runs/SPRINT-TEST/state.json"
# Expect: schema_version: 2; both stories carry lane: "standard", lane_assigned_by: "migration-default"

# 3. Set lane via --lane flag
CLEARGATE_STATE_FILE="$TMPDIR/.cleargate/sprint-runs/SPRINT-TEST/state.json" \
  node .cleargate/scripts/update_state.mjs STORY-001-01 --lane fast
# Expect: STORY-001-01.lane = "fast"; lane_assigned_by = "human-override"; STORY-001-02 untouched

# 4. Round-trip — read again, expect schema_version stays 2 (no spurious migration)
CLEARGATE_STATE_FILE="$TMPDIR/.cleargate/sprint-runs/SPRINT-TEST/state.json" \
  node .cleargate/scripts/update_state.mjs STORY-001-01 Bouncing 2>&1 | grep -c 'migration:' && exit 1 || echo "OK: no double-migration"

# 5. Demote
CLEARGATE_STATE_FILE="$TMPDIR/.cleargate/sprint-runs/SPRINT-TEST/state.json" \
  node .cleargate/scripts/update_state.mjs STORY-001-01 --lane-demote "smoke-test demotion"
# Expect: lane = "standard", lane_demoted_at populated, lane_demotion_reason = "smoke-test demotion", qa_bounces=0, arch_bounces=0
```

This single roundtrip exercises Scenarios 1, 3, 4, 5 of STORY-022-02 in one shell session. Scenario 2 (v1 → v2 migration on existing fixture) is covered by the cross-fixture regression sweep AND a dedicated test against `state-v1-spri-12.json`.

**STORY-022-01 canonical smoke:** `grep -A30 '^## 24. Lane Routing' .cleargate/knowledge/cleargate-protocol.md` — visual inspection that the section exists, has the rubric + demotion + forbidden surfaces + LD event registration, and is ≤30 lines. Plus `diff .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` returns empty.

**STORY-022-03 canonical smoke:** read each of the three templates and visually confirm the new sections. Plus `bash .cleargate/scripts/test/test_planning_v2.sh && bash .cleargate/scripts/test/test_sprint_report_v2.sh` exits 0.

### Ambiguities flagged from story bodies (route to orchestrator)

**Three ambiguities surfaced from M3 story-body inspection vs live-code reading. M2 plan §6 surfaced 1 ambiguity; M1 plan §6 surfaced 3. M3 surfaces 3 — symmetric.**

1. **STORY-022-01 §1.2 + §3.2 cite `§14 "Lane Routing"`. §14 is already occupied by `## 14. Multi-Participant Sync` (verified live L518; SPRINT-13 / STORY-010-08; protocol-section-14.test.ts already locks the byte-equality + 9 subsection presence).** Per the architect.md Protocol Numbering Resolver and the FLASHCARD #protocol #section-numbering 2026-04-21, the next free is **§24** (current max = 23 at L946). **Architect picks: §24.** No orchestrator question needed if accepted; flagged for the record per the resolver's "include a note" rule. **STORY text cites §14 — stale, rewritten to §24.**

2. **STORY-022-02 §1.2 cross-file lookup of `expected_bounce_exposure: med|high` for the `--lane fast` rubric §6 contradiction.** Story says: "this story can stub the check as TODO if cross-file lookup is unwieldy and the orchestrator agrees." Inspecting the existing scripts: `init_sprint.mjs:44-57` does a single-field regex read of sprint frontmatter for `execution_mode`. The cross-file pattern is **already established** (init_sprint reads the sprint plan file); a similar regex read could pick up a hypothetical `expected_bounce_exposure` field. **However, the contradiction check needs to read the *story* file's frontmatter, not the *sprint* file** — the rubric is per-story. The story frontmatter at `.cleargate/delivery/pending-sync/STORY-NNN-NN_*.md` has `expected_bounce_exposure: <low|med|high>` per `cleargate-planning/.cleargate/templates/story.md:46`. Doing this read inside `update_state.mjs --lane fast` requires a second `findStoryFile(repoRoot, storyId)` helper plus a frontmatter regex. **Architect call: stub-with-TODO for M3, defer the live check to STORY-022-04 (M4) where `pre_gate_runner.sh` already walks per-story file paths.** Reasons: (a) update_state.mjs today does NOT read any pending-sync file — adding the dependency widens the script's surface in a way that's adjacent to M4's work; (b) the M3 lane is "substrate"; the rubric enforcement is "execution"; (c) STORY-022-02 §1.2 explicitly authorizes the stub. **Confirm with orchestrator that the stub-with-TODO path is the correct M3 call.** If the orchestrator wants the live check in M3, scope creeps by ~30 lines + a new helper + a new test scenario.

3. **STORY-022-01 §1.2 specifies the LD event-type registration in §10 vocabulary. §10 is `## 10. Knowledge Wiki Protocol` (verified L206) — NOT an event-type vocabulary section.** The actual event-type vocabulary lives in (a) `sprint_report.md:10-14` header comment block (touched by STORY-022-03), and (b) protocol §17 `## 17. Mid-Sprint Change Request Triage (v2)` (verified L691) which lists `CR:bug | CR:spec-clarification | CR:scope-change | CR:approach-change`. **Architect picks:** the LD registration goes in §24's body as a self-contained sentence ("`LD: lane-demotion` — emitted by `pre_gate_runner.sh` when a fast-lane story is auto-demoted; counted in Sprint Report §3 alongside CR:* / UR:* tallies"); STORY-022-03 owns the parallel registration in `sprint_report.md` header vocabulary; M3 does NOT amend §10 or §17. **Confirm with orchestrator** that this single-source-of-truth interpretation matches user intent. If the user expects an explicit edit to §10 or §17, scope expands and STORY-022-01's "≤30 lines" length budget on §24 is still preserved (the §10/§17 edits would be 1-2 lines each).

---

## 7. Out of scope for M3 (and forward-flagged constraints for later milestones)

**Explicitly NOT touched in M3:**
- `.cleargate/scripts/pre_gate_runner.sh` and `pre_gate_common.sh` (M4 / STORY-022-04 own).
- `.claude/agents/developer.md` (M4 / STORY-022-05 owns).
- `.claude/agents/qa.md` and `reporter.md` (M5 / STORY-022-07 owns reporter).
- `.cleargate/scripts/close_sprint.mjs` validation logic for §0 AI Continuity Block / §5 Lane Audit / §5 Hotfix Audit / sprint-naming-convention enforcement (M5 / STORY-022-07 owns). M3's edit to close_sprint.mjs is ONLY the migration-call insert at L130 — adds zero validation logic.
- Hotfix lane: `templates/hotfix.md`, `cleargate-cli/src/commands/hotfix.ts`, `wiki/topics/hotfix-ledger.md` (M4 / STORY-022-06 owns).
- M1 surfaces (`stamp-and-gate.sh`, `session-start.sh`, `pre-edit-gate.sh`, `mcp/src/tools/push-item.ts`, `mcp/src/config.ts`, init.ts pin, copy-payload.ts, manifest.ts pin-aware policy) — already shipped, must not regress.
- M2 surfaces (`readiness-predicates.ts`, `readiness-gates.md`, `token-ledger.sh`, `doctor.ts`, `cli.ts` help-text, protocol §22 / §23) — already shipped, must not regress.
- M2.5 surface (BUG-010 detector scoping) — already shipped, must not regress.
- Self-upgrade / version bumps (`cleargate-cli/package.json`, `cleargate-planning/MANIFEST.json`, `mcp/package.json`) — M5 / STORY-014-02 owns. M3 keeps all three at 0.5.0 / 0.5.0 / 0.1.0.
- Wiki / wiki-build / wiki-ingest changes — none in this sprint.
- PROPOSAL-012 / EPIC-020 wiki contradiction layer (per Sprint Plan §2.5 ADR-Conflict Flags: "two schema-bumping epics in one sprint = forbidden").
- Any field rename or removal on the existing v1 state.json schema. The migration is strictly additive per R-04.

**Forward-flagged constraints surfaced from M3 (for later milestone Architect plans):**

1. **M4 / STORY-022-04 constraint surfaced from STORY-022-02:** `update_state.mjs --lane fast` ships with a TODO stub for the `expected_bounce_exposure: med|high` contradiction check. STORY-022-04 (`pre_gate_runner.sh` demotion mechanics) MUST land the live check OR explicitly file a follow-up if `pre_gate_runner.sh` is the wrong host. The stub comment must reference STORY-022-04 by ID so QA can find the trail. M4 architect plan must call this out.

2. **M4 / STORY-022-04 constraint surfaced from STORY-022-01:** §24 of the protocol declares `LD` as an event type emitted by `pre_gate_runner.sh`. STORY-022-04's implementation must emit exactly the `LD` token (uppercase, two letters) into whatever sprint markdown §4 carries event events. Diverging tokens (`Lane-Demotion`, `LDM`, etc.) break the §3 Metrics tally that STORY-022-07 will validate. M4 architect plan must lock the literal token.

3. **M5 / STORY-022-07 constraint surfaced from STORY-022-03:** the new sprint_report.md `template_version: 2` plus the three new §5 Process subsections (`### Lane Audit`, `### Hotfix Audit`, `### Hotfix Trend`) are SKELETONS. STORY-022-07 (Reporter contract) must populate these subsections AND `close_sprint.mjs` must validate their presence ONLY when state.json `schema_version >= 2` AND at least one story has `lane: fast` (per Sprint Plan §2.3 R-03 mitigation). SPRINT-14's own close report (M5) runs all M3 stories at default lane = standard; if STORY-022-08 dogfood (M5) sets `lane: fast` on a synthetic CR, the validation activates. If 022-08 is dropped per R-06 sprint-slippage rule, validation stays dormant for SPRINT-14's close.

4. **M5 / STORY-014-02 constraint surfaced from STORY-022-02:** `state.schema.json` is NOT mirrored in `cleargate-planning/.cleargate/scripts/` (verified — only `constants.mjs` + `update_state.mjs` are mirrored). 014-02's self-upgrade run must NOT attempt to copy `state.schema.json` from the planning payload. If the manifest gains an entry for it, the pin must be `tier: script, overwrite_policy: skip`. Forward-flag to M5 architect plan: review `cleargate-planning/MANIFEST.json` post-022-02; if `state.schema.json` lacks a manifest entry today AND 022-02 ships it as an "upgrade-tracked" file, 014-02's diff validates only the live copy. **Architect picks: state.schema.json stays out of the manifest** — it is a debug schema reference, not a runtime contract; runtime contract is constants.mjs + update_state.mjs. No manifest change in M3.

5. **M5 / STORY-014-02 constraint surfaced from STORY-022-01:** the live `.claude/agents/architect.md` is gitignored; 014-02's self-upgrade `cleargate upgrade` MUST copy from `cleargate-planning/.claude/agents/architect.md` into the live tree. Per the existing scaffold-mirror-by-init contract (FLASHCARD 2026-04-19 #wiki #protocol #mirror), this is already the path. M3 only mutates the scaffold; the runtime effect for the current orchestrator session requires a manual `cp` (per §1 above). M5 architect plan must verify the `cp` step is part of 014-02's upgrade commands OR document that re-running `cleargate init --force` accomplishes the same.

---

## 8. Pre-flight for the Developer

**Developer-A (STORY-022-01) pre-flight:**
1. `cd /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli && npm install && npm run typecheck && npm test` — baseline green on `sprint/SPRINT-14` post-M2.5.
2. Read `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/delivery/pending-sync/PROPOSAL-013_Sprint_Planning_Fast_Track.md` §2.3 to copy the seven-check rubric verbatim.
3. Verify protocol numbering: `grep -n "^## [0-9]" /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md | sort -k2 -t'.' -n | tail -1` should return `946:## 23. Doctor Exit-Code Semantics`. If a different max appears, the M3 plan §1 §-numbering needs rebasing before edit.
4. Confirm `protocol-section-14.test.ts:32-54` is green pre-edit (it tests §14 which is `## 14. Multi-Participant Sync`, NOT lane routing — the test is unrelated to M3 work despite the filename collision).

**Developer-B (STORY-022-02) pre-flight:**
1. `cd /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli && npm run typecheck && npm test` — baseline green.
2. Read `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/update_state.mjs` end-to-end (155 lines). Pay attention to the validate-then-mutate flow at L66-L101 and the atomicWrite pattern at L45-L49.
3. Read `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/validate_state.mjs:34-38` to internalize the strict version equality. The split into `validateShapeIgnoringVersion` is non-negotiable.
4. Read `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/init_sprint.mjs:155-176` for the per-story stub shape.
5. Grep all 5 hard-coded v1 state.json fixtures (per §2 cross-fixture sweep) and decide the rebase strategy. Architect recommends inline-bumping each fixture to v2 + four lane fields, in the same Developer-B commit (one commit covers all M3 022-02 surfaces).
6. FLASHCARD scan: `grep '#schema' .cleargate/FLASHCARD.md` — note the 2026-04-25 `#schema #migrations #drizzle-kit` card (DDL-only generators) and the 2026-04-19 `#schema #manifest #upgrade` card (overwrite_policy union). Neither directly applies but the migration-additive principle is the same.

**Developer-C (STORY-022-03) pre-flight:**
1. `cd /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli && npm run typecheck && npm test` — baseline green.
2. Read each of the three template files top-to-bottom to understand exact insertion points.
3. Read `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/test/test_planning_v2.sh:1-60` to understand the existing assertion harness pattern.
4. Confirm STORY-022-02 is merged before starting (the sprint_report.md `template_version: 2` bump references the schema_version: 2 contract from 022-02). Verify by `grep -n "SCHEMA_VERSION = 2" /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/constants.mjs` returning a hit.
5. FLASHCARD scan: `grep '#scaffold-mirror' .cleargate/FLASHCARD.md` — note the 2026-04-19 `#wiki #protocol #mirror` card (live + shipped diff must return empty). Same principle for templates: `diff .cleargate/templates/<file> cleargate-planning/.cleargate/templates/<file>` returns zero output post-edit.

Pre-commit hook runs typecheck + tests; do not `--no-verify`. Each story's commit message format is specified in its own §4.2 — match verbatim:
- 022-01: `feat(STORY-022-01): SPRINT-14 M3 — Architect Lane Classification rubric + protocol §24`. **Architect note:** the story file says `§14`; rewrite to `§24` per §6 ambiguity #1 above. Use **§24** in the commit message.
- 022-02: `feat(STORY-022-02): SPRINT-14 M3 — state.json schema v1→v2 + migration + lane CLI flags`.
- 022-03: `feat(STORY-022-03): SPRINT-14 M3 — templates lane fields (Sprint Plan + story + sprint report v2)`.

---

## Architect's notes for orchestrator

- **M3 is strictly serial.** Spawn 022-01, wait for QA approval + merge, then 022-02, then 022-03. Three Developers, three commits, three QA gates.
- **Three ambiguities in §6 — two require orchestrator confirmation before the matching Developer spawns:**
  - **Before 022-01:** confirm §24 (not §14) is the protocol section number. Architect picks §24 per Protocol Numbering Resolver. (Self-resolving per the resolver rule, but flagging for visibility.)
  - **Before 022-02:** confirm the `--lane fast` cross-file rubric §6 contradiction check is stub-with-TODO for M3 (Architect's pick). If the orchestrator wants the live check, scope expands ~30 lines + a new helper + a new test.
  - **Before 022-01:** confirm the LD event-type registration goes in §24 body only (Architect's pick), NOT in protocol §10 or §17. Story says §10; §10 is the wiki section, not events.
- **R-04 is M3's hot risk.** STORY-022-02 must split `validateState` into strict + shape-only forms. The cross-fixture regression sweep (§2) is non-negotiable — QA pulls if any of the 5 hard-coded v1 fixtures regress.
- **The live `.claude/agents/architect.md` is gitignored** — verified. STORY-022-01 commits only the scaffold; the live mirror is updated via `cp` post-edit so the running session sees the new rubric. This is consistent with how 014-02 (M5) will copy from scaffold to live.
- **Forward-flags for M4 / M5 are in §7 items 1-5.** M4 owes (a) the live `--lane fast` rubric §6 check and (b) the literal `LD` token emission. M5 owes the validator activation guarded on schema_version + lane=fast usage.
- **No protocol or template number collision risks within M3.** §24 is novel; the three templates' new sections do not clash with anything else in the sprint.
- **M3 sticks to its lane.** No `pre_gate_runner.sh`, no Developer agent contract, no hotfix scaffold, no reporter, no close_sprint validation — those live in M4 + M5. The Reporter at sprint close audits this in the M3 Lane Audit row.
