# Milestone: M5 — Lane Classifier finishing + sprint self-upgrade
## Stories: STORY-022-07, STORY-014-02, STORY-022-08
## Wave: M5 (strict sequential — see §4)

role: architect

## Order

**Strict serial:** `STORY-022-07 → STORY-014-02 → STORY-022-08`. No parallelism. See §4 for the dependency contract.

---

## §1 Per-story file-touch list (verified against current HEAD post-M4 `55f6b53`)

### STORY-022-07 — Reporter Sprint Report v2.1 + close_sprint validation

**Modify (scaffold canonical):**
- `cleargate-planning/.claude/agents/reporter.md` — append a new section titled `## Sprint Report v2.1 — Lane + Hotfix Metrics` after current line 99 (end of "v2-adoption note" block at lines 61-63 + Fallback section at 65-92 + Reporter Rewrite Fallback at 94-98). Insert the v2.1 contract block before `## Guardrails` (currently at line 100). The new block must enumerate (a) all six §3 metric rows verbatim from `.cleargate/templates/sprint_report.md` lines 78-83 (Fast-Track Ratio, Fast-Track Demotion Rate, Hotfix Count (sprint window), Hotfix-to-Story Ratio, Hotfix Cap Breaches, LD events), (b) the §5 Process > Lane Audit table contract per template lines 167-172, (c) the §5 Process > Hotfix Audit table contract per template lines 174-179, (d) the §5 Process > Hotfix Trend narrative contract per template lines 181-188, and (e) the canonical sources: `state.json` lane fields (per `.cleargate/scripts/state.schema.json` StoryEntry definition lines 60-83), sprint markdown §4 LD events (per `pre_gate_runner.sh` line 363 `append_ld_event` writer + protocol §968 `LD` event-type), and `.cleargate/wiki/topics/hotfix-ledger.md` filtered by sprint window (`merged_at` between `started_at` and `closed_at`).
- `cleargate-planning/.cleargate/scripts/close_sprint.mjs` — currently a 251-line file with no schema-version branch. Insert a v2.1 validation phase between Step 2 (line 153 — non-terminal story check) and Step 3 (line 158 — `prefill_report.mjs` invocation). The new phase: (i) early-return legacy-pass when `state.schema_version < 2`; (ii) when `schema_version >= 2` AND `Object.values(state.stories).some(s => s.lane === 'fast')`, run the regex checks per story spec §3.2; (iii) enforce the naming convention `^SPRINT-\d{2,3}$` against `path.basename(sprintDir)` near the existing `if (!fs.existsSync(sprintDir))` block at line 107. Keep the validation phase order: name-convention first, then schema-gate, then content-regex, then existing pipeline.
- `.cleargate/scripts/close_sprint.mjs` (live, post-022-02 line numbers) — currently 257 lines, imports `migrateV1ToV2` at line 34 and auto-migrates v1→v2 at lines 134-137. The Developer must keep that import + auto-migration (live diverges from scaffold by exactly that block) AND add the same v2.1 validation phase between lines 160 (`process.stdout.write('Step 1-2 passed: ...')`) and 165 (`process.stdout.write('Step 3: running prefill_report.mjs...')`). The two files MUST stay byte-equal except for the `migrateV1ToV2` import + call, which is the documented divergence.
- `cleargate-planning/.claude/agents/reporter.md` `cp`-mirrored to `.claude/agents/reporter.md` (gitignored).

**Create:**
- `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` — vitest covering the 6 Gherkin scenarios. Mirror the harness shape of existing `cleargate-cli/test/scripts/test_update_state.test.ts` (uses fixture state.json files + `execSync` against `.cleargate/scripts/`). NOTE: the existing test scripts dir contains `test_update_state.test.ts`, not a `test_close_sprint*.test.ts` — Developer creates a fresh file.
- `cleargate-cli/test/scripts/fixtures/sprint-v1-legacy/state.json` — `schema_version: 1`, one story `Done`. (No REPORT.md needed — legacy-pass path runs without v2 checks.)
- `cleargate-cli/test/scripts/fixtures/sprint-v1-legacy/REPORT.md` — minimal markdown body for atomicWrite path.
- `cleargate-cli/test/scripts/fixtures/sprint-v2-no-fast/state.json` — `schema_version: 2`, all stories `lane: standard`, all `Done`.
- `cleargate-cli/test/scripts/fixtures/sprint-v2-no-fast/REPORT.md` — body without any §3 fast-track rows (proves activation gate disables v2.1 checks).
- `cleargate-cli/test/scripts/fixtures/sprint-v2-fast-missing-audit/state.json` — `schema_version: 2`, one story `lane: fast`, all `Done`.
- `cleargate-cli/test/scripts/fixtures/sprint-v2-fast-missing-audit/REPORT.md` — body missing the §5 Lane Audit heading (regression: must reject).
- `cleargate-cli/test/scripts/fixtures/sprint-v2-fast-complete/state.json` — `schema_version: 2`, one story `lane: fast`, all `Done`.
- `cleargate-cli/test/scripts/fixtures/sprint-v2-fast-complete/REPORT.md` — body containing all six §3 metric rows + §5 Lane/Hotfix Audit + Hotfix Trend.
- `cleargate-cli/test/scripts/fixtures/S-99-bad-name/state.json` + `REPORT.md` — non-conformant directory name to verify naming-convention rejection.

**Schema changes:** None. STORY-022-02 already locked schema_version=2 with lane fields (`.cleargate/scripts/state.schema.json` lines 60-83).

**Test scenarios (from §2.1 of STORY-022-07):** 6 scenarios. See §2 below.

**Reuse (no duplication):**
- `TERMINAL_STATES`, `VALID_STATES` from `.cleargate/scripts/constants.mjs` (lines 32-46).
- `validateState` from `.cleargate/scripts/validate_state.mjs`.
- `migrateV1ToV2` from `.cleargate/scripts/update_state.mjs` line 51 (already imported in live close_sprint.mjs at line 34).
- `atomicWrite` + `atomicWriteString` from current `close_sprint.mjs` lines 56-72 — reuse for any post-validation state mutation.
- Lane source-of-truth field names from `.cleargate/scripts/state.schema.json` StoryEntry: `lane`, `lane_assigned_by`, `lane_demoted_at`, `lane_demotion_reason`.
- LD event row format from `pre_gate_runner.sh` line 363 `append_ld_event` writer (already shipped STORY-022-04).

**Gotchas surfaced from code inspection:**
- The live `close_sprint.mjs` already auto-migrates v1→v2 at lines 134-137 BEFORE strict validation. This means by the time the new v2.1 validation phase runs, `state.schema_version` is always `2` — the legacy-pass branch only fires for **scaffold-mirror** runs (no migration import) OR for fixtures that explicitly skip migration. Developer must still write the legacy-pass branch (it covers the scaffold-mirror code path consumers will use post-`cleargate upgrade`). Test fixture `sprint-v1-legacy/` MUST be invoked against the **scaffold-mirror** copy of close_sprint.mjs OR against the live copy with a flag forcing skip-migration. Recommended: invoke the scaffold-mirror file directly in vitest (`node cleargate-planning/.cleargate/scripts/close_sprint.mjs`) so legacy v1 input survives untouched.
- The naming-convention check at the top of validation: `path.basename(sprintDir)` returns `SPRINT-14` for the live sprint dir but `S-09` for the legacy dir. SPRINT-09 was renamed in sprint kickoff bookkeeping per Sprint Plan Execution Guidelines step 3a. Verify (`ls .cleargate/sprint-runs/`) lists `SPRINT-09`, not `S-09` — failure here is a kickoff bookkeeping miss, not a code bug.
- Reporter section enumeration must list verbatim row names — `Hotfix Count (sprint window)` includes the parenthetical per template line 80 — but the template's regex match uses `/Hotfix Count/` per story §3.2. Both must work. Recommend: regex matches the leading "Hotfix Count" prefix (anchored to start of column), which catches both the verbatim "Hotfix Count" and "Hotfix Count (sprint window)".
- The activation gate uses `Object.values(state.stories).some(s => s.lane === 'fast')`. SPRINT-14's own state.json (post-M4) has all 16 stories at `lane: standard` (verified — current state.json is `schema_version: 1`, so post-migration all stories default to `standard`). Once STORY-022-08 lands and adds `STORY-099-01` with `lane: fast`, the activation gate fires for SPRINT-14's own close. **This is consistent with R-03 mitigation when read carefully:** R-03 says "SPRINT-14 itself runs all stories at `lane: standard`. The dogfood story 022-08 is the *only* `lane: fast` candidate, and its purpose is to populate the audit table." So R-03 *anticipates* that 022-08 flips the gate ON — the mitigation is that activation requires BOTH conditions and 022-08 is the only path that satisfies both. See §6 ambiguity #3.

### STORY-014-02 — Sprint Close Self-Upgrade + Version Bump

**Modify:**
- `cleargate-cli/package.json` line 3 — `"version": "0.5.0"` → `"version": "0.6.0"`.
- `cleargate-planning/MANIFEST.json` line 2 — `"cleargate_version": "0.5.0"` → `"cleargate_version": "0.6.0"`. Note: `MANIFEST.json` is regenerated by `cleargate-cli/scripts/build-manifest.ts` on `npm run build` (line 35 of cleargate-cli/package.json). Either bump manually then `npm run build` to refresh the `generated_at` + sha256 entries, OR bump via the build script. **Decision §6 #1:** manual edit + commit, then immediate `npm run build` so the post-bump sha256 entries are accurate; commit both changes together.
- `mcp/package.json` line 3 — `"version": "0.1.0"` → `"version": "0.2.0"`.
- `cleargate-planning/.claude/hooks/stamp-and-gate.sh` lines 7 (`# cleargate-pin: __CLEARGATE_VERSION__`) and 17 (`CG=(npx -y "@cleargate/cli@__CLEARGATE_VERSION__")`).
- `cleargate-planning/.claude/hooks/session-start.sh` lines 5 + 15 (same pattern).
- `.claude/hooks/stamp-and-gate.sh` (live) — currently `__CLEARGATE_VERSION__` placeholder substituted by `init/copy-payload.ts` line 88 with the live `pinVersion` opt. After bump, re-run `cleargate upgrade` to refresh the live hooks to substitute `0.6.0`.
- `.claude/hooks/session-start.sh` (live) — same.

**Create:** None.

**Schema changes:** None.

**Test scenarios (from §2.1 of STORY-014-02):** 5 scenarios. See §2 below.

**Reuse (no duplication):**
- `cleargate upgrade` CLI verb at `cleargate-cli/src/cli.ts` lines 371-389 — **VERIFIED EXISTS POST-022-06.** This is the existing 3-way merge driver (STORY-009-05). It walks `MANIFEST.json` files[], classifies drift per file, applies overwrite policy. Reads from the installed `@cleargate/cli` package's bundled `cleargate-planning/` payload (`cleargate-cli/templates/` per package.json line 27). For the dogfood meta-repo, the installed package and the source `cleargate-planning/` are the same tree — `cleargate upgrade` consumes the locally-built dist. The Developer must `npm run build` in `cleargate-cli/` after the version bump so the bundled payload reflects post-SPRINT-14 canonical, THEN run `node cleargate-cli/dist/cli.js upgrade` from the repo root.
- `copyPayload` from `cleargate-cli/src/init/copy-payload.ts` lines 64-126 — invoked transitively by upgrade for new files.
- `PIN_PLACEHOLDER` substitution from `cleargate-cli/src/init/copy-payload.ts` line 32 + lines 86-90 — when `cleargate upgrade` re-syncs hooks, it substitutes `__CLEARGATE_VERSION__` with the live `pinVersion` opt. Developer must verify the upgrade-handler invocation passes `pinVersion: '0.6.0'`. **§6 #2:** verify whether `upgradeHandler` accepts a `pinVersion` opt in current `cleargate-cli/src/commands/upgrade.ts` — if not, fall back to manual sed re-stamp on both scaffold + live hooks per Story §3.2 step 3.

**Gotchas surfaced from code inspection:**
- The scaffold hooks store the placeholder literal `__CLEARGATE_VERSION__` (verified via `grep -n "cleargate-pin\|@cleargate/cli@" cleargate-planning/.claude/hooks/*.sh` — see story §1.2 step 4). The pin substitution happens at INSTALL/UPGRADE time, not in the scaffold tree. The story spec §3.2 step 3 sed pattern `'s|@cleargate/cli@0.5.0|@cleargate/cli@0.6.0|g; s|cleargate-pin: 0.5.0|cleargate-pin: 0.6.0|g'` is **WRONG** for the scaffold tree — there is no `0.5.0` literal there. Correct sed for scaffold is a no-op (placeholder stays). Correct sed for **live** hooks targets `0.5.0` → `0.6.0` (live was substituted at last `cleargate init/upgrade` run). **§6 #2** flags this: the re-stamp work happens on the **live** hooks via `cleargate upgrade`'s built-in pin substitution, not via manual sed of the scaffold.
- Live + scaffold hooks are NOT byte-equal — they differ by the pin literal vs placeholder. The DoD line "Live + scaffold mirrors byte-equal post-upgrade" in story §4.2 is incorrect for the hook pair specifically. Reword: live = scaffold-with-placeholder-substituted-to-0.6.0. Hooks are the only `pin-aware` files in MANIFEST.json (lines 91-102 — `overwrite_policy: "pin-aware"`). All other agents/templates/scripts ARE strictly byte-equal between live + scaffold.
- The `cleargate upgrade` driver requires `.cleargate/.install-manifest.json` to exist (`upgrade.ts` line 32 imports `loadInstallSnapshot`). Verified via `ls .cleargate/.install-manifest.json` → MISSING. **§6 #4:** the dogfood repo never installed via `cleargate init`, so it has no install-manifest snapshot. `cleargate upgrade` will fail at snapshot-load. Two paths: (a) run `cleargate init --force` first to bootstrap the snapshot, then `cleargate upgrade`; (b) write a one-shot `node cleargate-cli/scripts/copy-planning-payload.mjs` invocation (already exists per cleargate-cli/package.json line 35) that copies the scaffold without the install-manifest precondition. Architect chooses (b) as the dogfood path — the meta-repo has never used `cleargate init` and shouldn't start now.
- Three version values must match exactly post-commit. Add a one-line vitest assertion: read all three files, `expect(cleargateCli === '0.6.0' && manifest === '0.6.0' && mcp === '0.2.0').toBe(true)`. Co-locate at `cleargate-cli/test/scripts/test_version_bump_alignment.test.ts`. **This protects R-11.**

### STORY-022-08 — Dogfood Fast-Lane End-to-End

**Create:**
- `.cleargate/delivery/pending-sync/STORY-099-01_Dogfood_Lane_Fast_Smoke.md` — synthetic L1 story with frontmatter `lane: "fast"`, `expected_bounce_exposure: "low"`, `complexity_label: "L1"`, `sprint: "SPRINT-14"`, `milestone: "M5"`, `parent_epic_ref: "EPIC-022"`. Body: single Gherkin scenario asking Developer to add a 1-LOC comment to a chosen target file. **Target file: `cleargate-cli/src/cli.ts`** — verified single-file source, easy 1-LOC comment addition near the top (line 1-2), pre-gate scanner will pass on a comment-only diff.
- `.cleargate/sprint-runs/SPRINT-14/dogfood-log.md` — narrative log per story §1.2 Part D. Documents Part A/B/C/D outcomes verbatim. Must include exact commands run, exact state.json mutations observed, exact LD-event row inserted into sprint markdown §4.
- `cleargate-cli/test/scripts/test_dogfood_lane_fast.test.ts` — vitest exercising the Lane Audit row generation logic against a fixture state.json. Since the Reporter agent doesn't currently expose a callable audit-table generator, the test imports `migrateV1ToV2` + builds a fixture state, then asserts the row format that the Reporter's prose contract (per STORY-022-07's §"Sprint Report v2.1 — Lane + Hotfix Metrics" section) prescribes. **Implication:** STORY-022-07 must produce a documented row format inside reporter.md that's testable from prose alone (e.g. `| <story-id> | <files-touched-count> | <loc> | <yes/no> | <blank> | <blank> |`). See §6 ambiguity #5.

**Modify:**
- `cleargate-cli/src/cli.ts` — single 1-LOC comment near line 1 (e.g. `// SPRINT-14 M5 dogfood smoke — STORY-099-01`). The `cleargate hotfix new` import at line 27 stays untouched. Must not introduce typecheck regressions.
- `.cleargate/sprint-runs/SPRINT-14/state.json` — add `STORY-099-01` entry. Pre-Part-A: `{ state: "Ready to Bounce", qa_bounces: 0, arch_bounces: 0, worktree: null, updated_at: "<iso>", notes: "M5 dogfood synthetic story", lane: "fast", lane_assigned_by: "architect", lane_demoted_at: null, lane_demotion_reason: null }`. Post-Part-A: `state` advances to `Architect Passed`. Post-Part-B: `lane: "standard"`, `lane_assigned_by: "human-override"`, `lane_demoted_at` populated, `lane_demotion_reason` populated.
- `.cleargate/delivery/pending-sync/SPRINT-14_Process_v2.md` §4 (events list) — append an `LD` event row naming `STORY-099-01` with the failure reason. **Note:** the Sprint Plan file currently has no §4 events list block. STORY-022-04 specifies that `append_ld_event` writes to "sprint markdown §4". Verify: the sprint plan file's current §-numbering (§1 deliverables, §2 execution strategy, §Risks, §Metrics, etc.). Recommend: `append_ld_event` writes to a new `## §4 Events Log` heading appended to the sprint plan file before §Risks. Developer must verify the actual write target before running Part B. **§6 ambiguity #6.**

**Schema changes:** None.

**Test scenarios (from §2.1 of STORY-022-08):** 4 scenarios. See §2 below.

**Reuse (no duplication):**
- `update_state.mjs --lane <fast|standard>` from `.cleargate/scripts/update_state.mjs` line 133 (STORY-022-02 shipped).
- `update_state.mjs --lane-demote <reason>` from `.cleargate/scripts/update_state.mjs` line 153 (STORY-022-02 shipped).
- `pre_gate_runner.sh qa <worktree> <branch>` line 336 (lane=fast skip-QA logic) and line 363 (`append_ld_event` invocation on scanner fail).
- `cleargate-cli/test/scripts/test_update_state.test.ts` as the harness shape for `test_dogfood_lane_fast.test.ts`.
- `close_sprint.mjs` validation phase from STORY-022-07 — Part D test invocation.
- `cleargate hotfix new` is **NOT** exercised in M5 (per story §1.3 — hotfix dogfood deferred to SPRINT-15).

**Gotchas surfaced from code inspection:**
- Spawning real Developer agents for Parts A and B inside this M5 plan run is expensive (~10k+ tokens per Developer + Architect mini-plan). The story spec §1.2 Part A says "Run the four-agent loop in fast-lane mode" but Part C says "Manually invoke or simulate the Reporter." Inconsistent ambition between Part A (real spawn) and Part C (simulate). **Recommendation in §6 #5:** Part A and B use real Developer spawns (the dogfood IS the loop test); Part C spawns the real Reporter agent (the dogfood IS the Reporter contract test). Total token cost ≤ 80k for the full dogfood, justified by R-03 mitigation: this is the only sprint that exercises the new Reporter contract end-to-end before SPRINT-15+ inherits it.
- `Architect Passed` is one of `VALID_STATES` per `constants.mjs` line 39, but the existing state machine per `STATE_TRANSITIONS` (constants.mjs lines 49-65) requires `Bouncing → QA Passed → Architect Passed`. STORY-022-04's lane=fast skip-QA contract needs to allow `Bouncing → Architect Passed` directly (skipping `QA Passed`). Verify (`grep -n "QA Passed" .cleargate/scripts/pre_gate_runner.sh`) — STORY-022-04 must already handle this. If `update_state.mjs` rejects the direct transition, Part A blocks at state-update. **§6 ambiguity #7.**
- The chosen target file `cleargate-cli/src/cli.ts` is 517 lines and central — a stray edit could break tests. Comment-only edit at line 1 (above the `import` block) is typecheck-safe. Pre-gate scanner per STORY-022-04 specifically allows comment-only diffs in lane=fast.
- Part B's "induced failure" must be reverted before Part D's close_sprint.mjs runs, OR Part B's Developer commit must be left in tree but the file content reverted on disk via a follow-up commit (so git history shows the demotion happened, but tree state at sprint close is clean). Recommend: Part B commits the syntax error, Part B+1 Developer reverts (keeping the LD event log intact). Document in dogfood-log.md.

---

## §2 Test surface per story

### STORY-022-07 — 6 Gherkin scenarios

| # | Gherkin scenario (verbatim line ref STORY-022-07 §2.1) | Test file | Test type |
|---|---|---|---|
| 1 | reporter.md documents v2.1 contract (line 95-100) | `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` | grep assertion on scaffold reporter.md content |
| 2 | close_sprint accepts a v1 report unchanged (legacy-pass) (line 102-108) | same | execSync against scaffold-mirror close_sprint.mjs + `sprint-v1-legacy/` fixture |
| 3 | close_sprint accepts a v2 report with no fast-lane stories (line 110-115) | same | execSync + `sprint-v2-no-fast/` fixture |
| 4 | close_sprint rejects a v2 report missing required sections (line 117-123) | same | execSync + `sprint-v2-fast-missing-audit/` fixture, expect non-zero exit + stderr regex |
| 5 | close_sprint rejects non-conformant sprint-run path (line 125-129) | same | execSync + `S-99-bad-name/` fixture, expect non-zero exit + naming-convention error |
| 6 | Reporter writes Lane Audit + Hotfix Audit on a sprint with a fast-lane story (line 131-138) | same | execSync + `sprint-v2-fast-complete/` fixture, expect zero exit |

### STORY-014-02 — 5 Gherkin scenarios

| # | Gherkin scenario (verbatim line ref STORY-014-02 §2.1) | Test file | Test type |
|---|---|---|---|
| 1 | cleargate upgrade refreshes live scaffold from canonical (line 103-114) | manual + scripted diff | shell verification post-upgrade run; documented in dogfood-log (NOT M5 dogfood — 014-02 close-out script) |
| 2 | Three-way version bump lands atomically (line 116-122) | `cleargate-cli/test/scripts/test_version_bump_alignment.test.ts` (NEW) | vitest reads all 3 files, asserts targets |
| 3 | Pin re-stamp updates both hooks (line 124-130) | manual grep verification | post-`cleargate upgrade` `grep` of live `.claude/hooks/*.sh` for `0.6.0` literal |
| 4 | cleargate --version reports 0.6.0 post-commit (line 132-135) | manual + close-out checklist | `node cleargate-cli/dist/cli.js --version` |
| 5 | R-10 safety net catches accidental clobber (line 137-143) | shell-script verification | pre/post `git status --porcelain` diff filtered to MANIFEST.json files[] paths |

**Note:** STORY-014-02 is largely a close-out procedural step, not a code-test story. Only one new vitest (test_version_bump_alignment.test.ts) is added; the other 4 scenarios are verified via shell scripts in the commit's pre-flight section.

### STORY-022-08 — 4 Gherkin scenarios

| # | Gherkin scenario (verbatim line ref STORY-022-08 §2.1) | Test file | Test type |
|---|---|---|---|
| Part A | fast-lane happy path (line 109-117) | manual + dogfood-log.md | live spawn of Developer + pre_gate_runner; record stdout/state.json delta |
| Part B | induced failure auto-demotes (line 119-129) | manual + dogfood-log.md | live spawn of Developer with induced fail; record stdout/state.json delta + LD row |
| Part C | Reporter v2.1 §3 metric rows populate (line 131-138) | manual (Reporter agent spawn) | spawn Reporter agent against post-Part-B state; verify REPORT.md contents |
| Part D | close_sprint.mjs validation passes for SPRINT-14 (line 140-146) | manual + close-out checklist | `node .cleargate/scripts/close_sprint.mjs SPRINT-14 --assume-ack` against post-Part-C REPORT.md, expect zero exit + sprint_status flips to Completed |
| (unit) | Lane Audit row generation against fixture | `cleargate-cli/test/scripts/test_dogfood_lane_fast.test.ts` | vitest, fixture state.json, asserts the row format reporter.md prescribes |

---

## §3 Snapshot/regression contracts

The existing hook snapshots at `cleargate-cli/test/snapshots/hooks/` (verified contents):
- `pre-edit-gate.cr-008.sh`, `session-start.cr-008.sh`, `session-start.cr-009.sh`, `stamp-and-gate.cr-008.sh`, `stamp-and-gate.cr-009.sh`, `token-ledger.bug-009.sh`, `token-ledger.bug-010.sh`.

Per `cleargate-cli/test/snapshots/hooks-snapshots.test.ts` lines 1-68, **only the `token-ledger` snapshot pair is asserted live-vs-snapshot**: `BUG-010` is the active baseline (line 46-67); `BUG-009` is retained as a historical audit baseline but the live-equality assertion is intentionally skipped (line 7-10). The CR-008/CR-009 snapshots (`stamp-and-gate.cr-008.sh`, `session-start.cr-008.sh`, `session-start.cr-009.sh`, `stamp-and-gate.cr-009.sh`, `pre-edit-gate.cr-008.sh`) are stored on disk but **not asserted against live by any test in this file**.

**Critical finding:** the active live-vs-snapshot lock (`token-ledger`) does NOT depend on the pin literal — `token-ledger.sh` does NOT carry the `__CLEARGATE_VERSION__` placeholder (verified via `grep -L "__CLEARGATE_VERSION__" cleargate-planning/.claude/hooks/*.sh`). So the BUG-010 snapshot is **NOT invalidated** by the 014-02 pin re-stamp.

The CR-008/CR-009 snapshots that DO carry the placeholder are not asserted, so post-014-02 they remain forensically accurate baselines (placeholder still `__CLEARGATE_VERSION__` in scaffold) — the `cleargate upgrade` substitution to `0.6.0` only affects the LIVE `.claude/hooks/*.sh`, which are gitignored and not snapshotted.

**Decision:** **Path B from the prompt — do NOTHING to existing snapshots.** They are scaffold-tree baselines (placeholder retained), not live-tree captures. No `.014-02.sh` snapshots are needed. Snapshot strategy is "scaffold-tree forensic baseline; live tree is gitignored and not snapshotted." The pin re-stamp story §3.2 step 3 sed pattern targets the WRONG tree per §1 gotcha — only the LIVE hooks change literal pins, and live hooks are out-of-tree.

**One-line summary for §6 ambiguity #1:** No snapshot updates required; story §3.2 step 3 sed pattern is misaimed at the scaffold tree which has no version literal. Re-stamp is implicit via `cleargate upgrade`'s pin substitution on the live tree.

---

## §4 Sequential-execution contract (M5-strict)

| Step | Story | Why this order |
|---|---|---|
| 1 | STORY-022-07 | Reporter v2.1 contract + close_sprint validator must exist in **scaffold canonical** (`cleargate-planning/.claude/agents/reporter.md` + `cleargate-planning/.cleargate/scripts/close_sprint.mjs`) BEFORE 014-02 runs the upgrade copy, so the live scaffold post-014-02 reflects the new contract. |
| 2 | STORY-014-02 | Self-upgrade refreshes live `.claude/agents/reporter.md` + `.cleargate/scripts/close_sprint.mjs` from the post-022-07 scaffold. Without this, 022-08's Part C Reporter spawn would consume the OLD live reporter.md and 022-08's Part D close_sprint validation would consume the OLD live close_sprint.mjs (no v2.1 phase). 014-02 is the gate that makes the live tree ready to validate itself. |
| 3 | STORY-022-08 | Dogfood runs against the **post-upgrade live** scaffold. Validates: (a) Reporter agent contract change end-to-end via REAL spawn (not unit test), (b) close_sprint.mjs validation against a REAL fast-lane sprint, (c) lane classifier mechanics (lane=fast → skip QA, demotion → LD event) end-to-end against the post-014-02 dogfood. Doubles as the upgrade-path regression test. |

**Failure mode if order violated:**
- 022-08 before 014-02 → Reporter agent reads old contract; close_sprint validation phase missing; Part C/D both fail.
- 014-02 before 022-07 → upgrade copies pre-v2.1 reporter.md + close_sprint.mjs into live; 022-07's later commit only updates scaffold, not live; live diverges from scaffold; 022-08 still fails.

---

## §5 Risk-table cross-reference (vs Sprint Plan §Risks)

| Risk ID | Risk | Story owning mitigation | Status hook |
|---|---|---|---|
| R-03 | Reporter contract change gates SPRINT-14 own close | STORY-022-07 | Activation gate `schema_version >= 2 AND any lane=fast story`. SPRINT-14 itself flips this ON only after 022-08 lands STORY-099-01. |
| R-04 | state.json migration lossiness | STORY-022-08 (read of SPRINT-10/11/12 archive state.json for §5 Hotfix Trend rolling-4 narrative) | Reporter must tolerate v1 state.json files (no lane fields) when reading historical sprints — the rolling-4 narrative falls back to "0 hotfixes" for any v1 sprint. STORY-022-07 reporter.md addition must call this out. |
| R-10 | Self-upgrade clobbers local data | STORY-014-02 | Pre/post `git status --porcelain` diff confined to MANIFEST.json files[]; tarball backup of `.cleargate/{delivery,wiki,sprint-runs}` + `FLASHCARD.md`. |
| R-11 | Three-place version bump partial | STORY-014-02 | New vitest `test_version_bump_alignment.test.ts` asserts all 3 versions match targets; pre-commit blocks if any one wrong. |
| R-12 | Self-upgrade pin-lag | STORY-014-02 | Per §1 gotcha for 014-02 — the pin re-stamp happens on the LIVE tree via `cleargate upgrade` pin-substitution, not via sed of scaffold. The story §3.2 step-3 sed pattern is misaimed. Correct implementation: run `cleargate upgrade` with `pinVersion: '0.6.0'` (or fall back to manual edit of live `.claude/hooks/*.sh`). |
| R-08 | (carry-over from M2) Gate-criteria over-match | N/A — already shipped BUG-008 `bc56daa` | None — informational |
| R-05 | (carry-over from M1) Hook chain triple-touch | N/A — already shipped CR-008/009/BUG-009 | None — informational |

---

## §6 Story-by-story acceptance Gherkin reference (canonical M5-close smoke test)

| Story | Canonical M5-close test | Sprint-completion signal? |
|---|---|---|
| STORY-022-07 | Scenario 4 (rejects v2 report missing required sections) — proves the validator actually fails-closed; the most likely regression vector. | No (M5 internal). |
| STORY-014-02 | Scenario 4 (`cleargate --version` reports `0.6.0`) — single-shell-command verification. | No (M5 internal). |
| STORY-022-08 | **Part D** (close_sprint.mjs validation passes for SPRINT-14) — single PASS = sprint complete. | **YES — sprint-completion signal.** |

**Sprint completion is signaled by exactly one event:** `node .cleargate/scripts/close_sprint.mjs SPRINT-14 --assume-ack` exits 0, `state.json sprint_status` flips to `Completed`. No other check supersedes this. After this signal, SPRINT-14 is shipped and the orchestrator runs sprint archive bookkeeping (out-of-scope for M5).

---

## §7 Out-of-scope-for-M5

M5 does NOT touch:
- Architect rubric (M3 — STORY-022-01 shipped `112a799`).
- state.json schema or migration (M3 — STORY-022-02 shipped `cf8198e`).
- Templates lane fields (M3 — STORY-022-03 shipped `86bf9af`).
- pre_gate_runner.sh demotion mechanics (M4 — STORY-022-04 shipped `7d7be3b`).
- developer.md lane-aware execution (M4 — STORY-022-05 shipped `c59a057`).
- Hotfix lane CLI / ledger / cap stub (M4 — STORY-022-06 shipped `55f6b53`).
- M1 surfaces (CR-008/009/010, all shipped).
- M2 surfaces (BUG-008/009, STORY-014-01, all shipped).
- M2.5 surface (BUG-010, shipped).

M5 also does NOT:
- Publish `cleargate-cli@0.6.0` to npm (manual user step post-014-02).
- Deploy `mcp@0.2.0` to Coolify (manual user step post-014-02).
- Push items to remote PM tool (sprint archive is human-driven post-close per protocol §4.5 step 7).
- Backport v2.1 sections to historical REPORT.md files (forward-only per STORY-022-07 §1.3).
- Exercise hotfix lane in dogfood (Part D excluded per STORY-022-08 §1.3 — first-pass hotfix runs organically in SPRINT-15+).
- Add new fast-lane fixtures beyond STORY-099-01 (one synthetic story is sufficient for activation-gate firing).

---

## §8 Open ambiguities for orchestrator

1. **MANIFEST.json regen-vs-edit (014-02).** `cleargate-planning/MANIFEST.json` is regenerated by `cleargate-cli/scripts/build-manifest.ts` on `npm run build`. Bumping `cleargate_version` manually then running `npm run build` will overwrite the manual bump unless build-manifest.ts reads the current version from the file. **Architect calls (a):** manually bump line 2 of MANIFEST.json, then run `npm run build` in cleargate-cli/ (which calls `tsx scripts/build-manifest.ts`); verify post-build the version is still `0.6.0` and sha256 entries are refreshed. If build-manifest.ts overwrites version: have Developer either (i) edit build-manifest.ts to preserve version, or (ii) bump version AFTER build-manifest runs. Surface to user only if (i)/(ii) is needed.

2. **Pin re-stamp implementation (014-02 R-12).** Story §3.2 step 3 sed pattern targets `0.5.0 → 0.6.0` literals in scaffold hooks. Verified: scaffold hooks carry `__CLEARGATE_VERSION__` placeholder, NOT a literal version. Sed pattern as written is a no-op on scaffold and only affects live hooks. **Architect calls:** correct implementation is `cleargate upgrade` with `pinVersion: '0.6.0'` (preferred — respects the existing pin-aware machinery) OR manual sed on live hooks only (`.claude/hooks/*.sh`). Recommend confirming with user whether `upgradeHandler` already accepts `pinVersion`; if not, the Developer adds a one-line opt or falls back to manual sed on live tree. Either way, scaffold tree stays placeholder.

3. **R-03 mitigation interpretation (022-07 + 022-08).** R-03 in sprint plan reads as if SPRINT-14 should NOT trip the v2.1 validator at its own close. But STORY-022-08 specifically introduces STORY-099-01 with `lane: fast` precisely to fire the activation gate. Reconciliation: R-03 anticipates this — the mitigation is "validator activates only when BOTH conditions hold; 022-08 is the SOLE gating event for SPRINT-14." This is correct and intentional. **Architect confirms:** SPRINT-14's own close report MUST contain all v2.1 sections (because 022-08 flips the gate), and 022-08's Part D close_sprint validation is the test of that. No code change needed; just an interpretation lock. Surface to user for explicit confirmation before 022-07 spawns.

4. **`cleargate upgrade` precondition for the dogfood meta-repo (014-02).** `upgrade.ts` requires `.cleargate/.install-manifest.json` (verified MISSING in the dogfood meta-repo). Two paths: (a) `cleargate init --force` first to bootstrap the snapshot then upgrade; (b) bypass upgrade entirely and run `node cleargate-cli/scripts/copy-planning-payload.mjs` (the existing prebuild script per cleargate-cli/package.json line 35) to refresh live without snapshot precondition. **Architect picks (b)** — the meta-repo has never used `cleargate init` and shouldn't (its `.cleargate/` is the source of truth, not a consumer copy). The "self-upgrade" semantically means "rebuild the cleargate-cli payload from scaffold canonical, then re-run copy-planning-payload to refresh anywhere live diverges." Surface to user: confirm (b) is acceptable; if user wants real `cleargate upgrade` exercise, fall back to (a) with a one-shot `cleargate init --force` followed by `cleargate upgrade --yes`.

5. **Dogfood spawn cost (022-08 Parts A/B/C).** Story §1.2 Part A says "run the four-agent loop in fast-lane mode" (real spawn) but Part C says "Manually invoke or simulate the Reporter." Architect recommends **REAL spawns for all three parts** — the dogfood IS the contract test for the new Reporter contract from 022-07; simulation defeats the purpose. Total estimated token cost ≤ 80k. Surface to user: confirm willingness to spend 80k on dogfood. If user prefers cheaper dogfood, downgrade Part C to a hand-written REPORT.md against the v2.1 template, but lose the end-to-end Reporter-contract validation.

6. **`append_ld_event` write target (022-08 Part B).** STORY-022-04's `pre_gate_runner.sh` line 363 calls `append_ld_event "${SPRINT_MD}" ...`. The sprint plan file `.cleargate/delivery/pending-sync/SPRINT-14_Process_v2.md` does NOT have a §4 events list heading (it has §1 deliverables, §2 execution strategy, §Risks, §Metrics — no §4). Verify what `append_ld_event` actually does on a target file with no §4 heading: does it auto-create one, append at end-of-file, or fail? Surface to user: Developer must read `pre_gate_runner.sh` lines around 363 OR consult STORY-022-04's commit `7d7be3b` to confirm the write target. If `append_ld_event` requires a pre-existing §4 heading, M5 must add one to the SPRINT-14 sprint plan file before Part B runs.

7. **State-machine direct transition `Bouncing → Architect Passed` (022-08 Part A).** lane=fast skip-QA implies Developer commits → pre_gate scanner passes → state advances to `Architect Passed` without going through `QA Passed`. `STATE_TRANSITIONS` in `constants.mjs` lines 49-65 lists `'Bouncing': ['QA Passed', 'Ready to Bounce', 'Escalated', 'Parking Lot']` — `Architect Passed` is NOT in the allow-list from `Bouncing`. Verify whether STORY-022-04 (`pre_gate_runner.sh`) bypasses `update_state.mjs`'s transition validation OR relies on a state-machine amendment that wasn't surfaced in M4. **Architect surface:** Developer must verify the actual state path lane=fast follows. If `update_state.mjs` rejects `Bouncing → Architect Passed`, Part A blocks with a state-machine error. May need a one-line amendment to `STATE_TRANSITIONS` adding `Architect Passed` to the `Bouncing` allow-list, OR a workaround where lane=fast routes through a synthetic `QA Passed` state set by pre_gate_runner.sh itself.

---

## Plan budget

This plan is 196 lines. Justified by M5 being the sprint-close milestone with three interlocking surfaces (Reporter contract + self-upgrade + dogfood) and seven verified ambiguities that the orchestrator must resolve before spawning Developers. The plan would not be readable at <120 lines.
