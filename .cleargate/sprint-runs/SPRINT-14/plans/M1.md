# Milestone: M1 — Process Floor (3 CRs)
## Items: CR-009, CR-008, CR-010
## Wave: W1 (sequential — CR-009 → CR-008; CR-010 disjoint surface, scheduled last for stability)

role: architect

## 1. Per-CR file-touch list

All paths absolute below. Verified with Read at the line ranges cited.

### CR-009 — Hook CLI Resolution Pin

**Modify:**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/stamp-and-gate.sh:14-17` — replace the `else / log-and-exit-0` branch (verified L14-L17) with a `CG=(npx -y "@cleargate/cli@__CLEARGATE_VERSION__")` assignment. Keep the existing `command -v cleargate` (L10) and `cleargate-cli/dist/cli.js` (L12) branches verbatim.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/session-start.sh:7-13` — same replacement: third `else / exit 0` branch (verified L11-L13) becomes the npx tail with the same `__CLEARGATE_VERSION__` placeholder.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/init/copy-payload.ts:64-96` — extend the per-file copy loop. After each `fs.writeFileSync(dstPath, srcContent)` call (L88, L93), if `relPath` is `.claude/hooks/stamp-and-gate.sh` or `.claude/hooks/session-start.sh`, perform a single in-place `srcContent.toString('utf8').replaceAll('__CLEARGATE_VERSION__', <pin>)` and re-write. Cleaner option: thread an optional `pinVersion?: string` into `CopyPayloadOptions` (L21-L23) and resolve at write-time. Architect picks: thread the pin through options.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/init.ts` — three edits:
  - Add `--pin <ver>` to `InitOptions` (L46-L90, after `force`).
  - Resolve the pin: `opts.pin ?? <read package.json version of cleargate-cli at packageRoot>`. Use `loadPackageManifest({ packageRoot: payloadDir }).cleargate_version` — already imported at L20; verify the field is `0.5.0` per `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/MANIFEST.json:2`.
  - Pass the resolved pin into `copyPayload(payloadDir, cwd, { force, pinVersion })` at L221.
  - After Step 7 (install snapshot) and before Step 7.5 (participant), insert a new Step 7.6 "resolver probe": run the resolver chain in JS (mirror the bash logic — exists checks for `cleargate-cli/dist/cli.js` under `cwd`, then `command -v cleargate`, then synthesize `npx -y @cleargate/cli@<pin> --version` via `child_process.spawnSync` with a 10s timeout). Print one of:
    - `[cleargate init] 🟢 cleargate CLI resolved via <branch>` (e.g. `npx @cleargate/cli@0.5.0 (cold-start ~600ms first call)`)
    - `[cleargate init] 🔴 cleargate CLI: not resolvable — hooks will no-op. Fix: npm i -g cleargate@<pin>` then `exit(1)`.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/doctor.ts` — extend `runSessionStart` (L362-L450). After the early-return at L376 (no pending-sync dir) and BEFORE the `blocked` array population at L379, emit one resolver-status line ALWAYS (success or failure). Probe the same chain as init: `cleargate-cli/dist/cli.js` exists under `cwd`, then `command -v cleargate`, else parse the live `.claude/hooks/stamp-and-gate.sh` for the literal `npx -y "@cleargate/cli@<pin>"` line and report the pin. Output format (literal): `cleargate CLI: <branch> — <one-line>`. Single line, prepended to the existing blocked-items output.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/MANIFEST.json` entries for `.claude/hooks/stamp-and-gate.sh` (L89-L95) and `.claude/hooks/session-start.sh` (L82-L88): change `overwrite_policy` from `"always"` to `"pin-aware"` for these two entries only. **Spec the new policy in code:**
  - Add `'pin-aware'` to the `overwrite_policy` union type in `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/manifest.ts` (grep first for the existing union — FLASHCARD 2026-04-19 #schema #manifest #upgrade says it is `'always'|'merge-3way'|'skip'|'preserve'`).
  - Semantics for `pin-aware` (Architect decision): **identical to `always` for v0.6.0 init.** The `__CLEARGATE_VERSION__` placeholder is substituted at copy-time and the file is overwritten unconditionally. Pin-aware diverges from `always` only when `cleargate upgrade` lands (EPIC-009 scope, NOT M1) — at that point the upgrade three-way merge driver treats the pin line as machine-managed. M1 ships the policy enum + the always-equivalent behaviour; the upgrade-time branch is **out of scope, deferred to EPIC-009 follow-up**.

**Create:** none.

**Out of scope (per CR-009 §3 "Out of scope"):**
- `cleargate-cli/scripts/copy-planning-payload.mjs` payload boundary changes.
- `cleargate-cli/src/commands/upgrade.ts` re-stamping (`upgrade.ts` exists per `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/upgrade.ts` directory listing, but the upgrade-time pin re-stamp is EPIC-009 follow-up scope, not M1).
- Native binary distribution.

### CR-008 — Planning-First Enforcement (Phase A + B)

**Modify (Phase A):**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/session-start.sh:15` — drop the `2>/dev/null || true` discard. New line:
  ```
  "${CG[@]}" doctor --session-start || true
  ```
  Stdout passes through; stderr also passes through (acceptable — Claude Code ignores stderr from SessionStart). The trailing `|| true` keeps the hook exit code at 0 if doctor fails for any reason.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/doctor.ts:362-450` (`runSessionStart`) — extend with a **planning-first reminder block** that fires when BOTH:
  - `pendingSyncDir` (already resolved L366) contains zero `.md` files OTHER THAN `.gitkeep` AND with `approved: true` in their frontmatter (use existing `parseFrontmatter` import L11; iterate the same `files` list at L370-L374 and apply an additional filter for `fm['approved'] === true`).
  - `path.join(cwd, '.cleargate', 'sprint-runs', '.active')` does NOT exist.
  - When both true and `blocked.length === 0`, emit the literal block:
    ```
    Triage first, draft second:
    Before any Edit/Write that creates user-facing code, you must:
      (1) classify the request (Epic / Story / CR / Bug),
      (2) draft a work item under .cleargate/delivery/pending-sync/ from .cleargate/templates/,
      (3) halt at Gate 1 (Proposal approval) for human sign-off.
    Bypass this only if the user has explicitly waived planning in this conversation.
    ```
  - This block is a peer to the existing "N items blocked" output, NOT a replacement. If both fire (zero approved stories AND blocked items present), emit the reminder block FIRST, then a blank line, then the blocked items.

**Create (Phase B — warn-only mode):**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/pre-edit-gate.sh` — new ~80-line bash script. Reads stdin JSON via `jq -r '.tool_input.file_path'` (pattern verified at `stamp-and-gate.sh:19`). Logic:
  1. Resolve CG via the same three-branch chain as `stamp-and-gate.sh` (CR-009 chain post-merge).
  2. Read `FILE` from stdin tool_input.
  3. Whitelist: if `$FILE` matches any of `.cleargate/**`, `.claude/**`, `CLAUDE.md`, `MANIFEST.json`, `cleargate-planning/**`, `README.md`, `.gitignore`, `.gitkeep`, `package.json`, `package-lock.json`, `.env*`, `.npmrc`, `.editorconfig` → exit 0.
  4. Non-whitelist path: query `<CG> doctor --can-edit "$FILE"` (new doctor flag, see below). The CLI returns exit 0 (allowed; story exists / sprint-active) or exit 1 (would-block).
  5. **WARN-ONLY (M1-ship default):** on would-block, write a structured line to `${REPO_ROOT}/.cleargate/hook-log/pre-edit-gate.log` with format `[<ISO_TS>] mode=warn would_block file=<FILE> reason=<reason>` and STILL exit 0. NEVER block the edit during the 48h post-merge soak.
  6. Bypass env: `CLEARGATE_PLANNING_BYPASS=1` → skip the doctor query, log `bypass=true`, exit 0.
  7. Mode is gated by env var `CLEARGATE_PLANNING_GATE_MODE` (values: `warn` | `enforce`). Default: `warn`. **M1 ships only the `warn` codepath wired in `settings.json`.** The `enforce` codepath is implemented but NOT activated by init — flipping to enforce is a manual `cleargate config set strict-gate true` operation per CR-008 §1 Phase B item 5. Per Risk R-02, do NOT enable enforcing mode at sprint close — that is a post-sprint promotion only after 48h of zero-false-positive warn logs.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/doctor.ts` — add a new mode `can-edit` to the `DoctorMode` union (L57) + `DoctorFlags` interface (L44-L55, add `canEdit?: boolean; canEditFile?: string`). Wire into `selectMode` (L69-L86). Implementation:
  - Reads `pending-sync/*.md`, parses frontmatter via `parseFrontmatter`.
  - For each, reads `implementation_files: string[]` (story-template field — verify presence in `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/story.md` before relying on it; if absent, fall back to "any approved story" → allow).
  - Glob-match `$FILE` against any approved story's `implementation_files`.
  - Match → exit 0. No match AND zero approved stories → exit 1 with reason `no_approved_stories`. Stories present but file uncovered → exit 1 with reason `file_not_in_implementation_files`.
  - Sprint-active sentinel `.cleargate/sprint-runs/.active` → exit 0 unconditionally (in-sprint edits bypass the gate).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/MANIFEST.json` — append a new entry for `.claude/hooks/pre-edit-gate.sh` (tier `hook`, `overwrite_policy: always`, `preserve_on_uninstall: false`). Insert in the `.claude/hooks/` block, lex-sorted between `pending-task-sentinel.sh` (L54-L60) and `pre-commit-surface-gate.sh` (L61-L67).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/init.ts:30-44` — extend `HOOK_ADDITION` with a `PreToolUse` block for `Edit|Write` matcher invoking `${CLAUDE_PROJECT_DIR}/.claude/hooks/pre-edit-gate.sh`. **The merge is additive** — `mergeSettings` deduplicates by exact command string, so re-init is idempotent. The hook script itself respects `CLEARGATE_PLANNING_GATE_MODE=warn` by default; init does NOT set the env, so warn-only is the post-init effective mode.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/settings.json` — add the same `PreToolUse Edit|Write → pre-edit-gate.sh` block (mirror the init.ts addition). Bump `sha256` in MANIFEST.json L105 after edit (Architect cannot pre-compute — Developer recomputes via `npm run prebuild` or manual sha256sum).

**Out of scope:**
- Adapter contract changes (CR-010's surface).
- `cleargate config set strict-gate <bool>` CLI command — CR-008 §3 Phase B mentions it but Architect demotes to OUT-OF-SCOPE for M1: warn-only ships in M1; the enforce-mode CLI flag lands in a follow-up after the 48h soak per R-02.
- Whitelist as a config (the whitelist is hardcoded in `pre-edit-gate.sh` for M1; if it needs to grow, file a follow-up CR).

### CR-010 — Advisory Readiness Gates on Push

**Modify:**
- `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/tools/push-item.ts:98-108` — replace the gate-rejection branch. Verified shape: L103 reads `args.payload['cached_gate_result']`; L105-L107 throws `PushGateFailedError` when `pass !== true`. New behaviour:
  ```ts
  // CR-010: gate result becomes advisory unless STRICT_PUSH_GATES is on.
  // approved: true (above, L94) remains a hard reject. Untouched.
  let gateStatus: 'pass' | 'open' | undefined;
  let failingCriteria: string[] = [];
  const gate = args.payload['cached_gate_result'];
  if (gate !== undefined && gate !== null && typeof gate === 'object' && !Array.isArray(gate)) {
    const passed = (gate as Record<string, unknown>)['pass'] === true;
    gateStatus = passed ? 'pass' : 'open';
    if (!passed) {
      const fc = (gate as Record<string, unknown>)['failing_criteria'];
      if (Array.isArray(fc)) {
        failingCriteria = fc
          .map((c) => (typeof c === 'string' ? c : (c as { id?: string })?.id ?? null))
          .filter((s): s is string => typeof s === 'string');
      }
      if (ctx.strictPushGates === true) {
        throw new PushGateFailedError(args.cleargate_id);  // strict-mode opt-in
      }
    }
  }
  ```
  `PushGateFailedError` class is **kept** (used by strict-mode and by `register-tools.ts:59` error mapping). `runTool` audit log writes `result='success', gate_status='open', failing_criteria=[...]` on the new advisory path.
- `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/tools/push-item.ts:29-40` (`PushItemContext` interface) — add:
  ```ts
  /** CR-010: when true, restore pre-CR-010 hard-reject of cached_gate_result.pass !== true. Default false (advisory). */
  strictPushGates?: boolean;
  ```
- `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/tools/push-item.ts:42-47` (`PushItemResult` interface) — add:
  ```ts
  gate_status?: 'pass' | 'open';
  failing_criteria?: string[];
  ```
  Populate both on every return path (L169-L174 insert path; L206-L211 update path). Pass-through fields; do NOT write them into `stampedPayload` (L125) — they are response-only metadata, not persisted.
- `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/config.ts:3-55` (`envSchema`) — add `STRICT_PUSH_GATES: z.coerce.boolean().default(false)`. Insert after `INVITE_TTL_SECONDS` (L27) and before the PM-tool adapter section (L29).
- `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/mcp/register-tools.ts` — at the `pushItem(...)` callsite (L103, verified via grep), pass `strictPushGates: ctx.config.STRICT_PUSH_GATES` (or wherever ctx exposes config). Architect note: register-tools.ts already threads `ctx.adapter`; if `ctx.config` is not yet on the registration context, add it. This is a 2-line wire-through.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — append a new section `## 22. Advisory Readiness Gates on Push (v2)` (next free section per protocol numbering audit: max=21 "Status Vocabulary", next=22). Body specifies: `approved` is hard-reject; `cached_gate_result` is advisory; `STRICT_PUSH_GATES=true` opts back into hard-reject. Cite this CR.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/CLAUDE.md` — reword the "Halt at gates" paragraph (verified at the bounded-block content under `## 🔄 ClearGate Planning Framework`) to clarify Gate 1 (Proposal approval) and Gate 2 (Ambiguity) remain human-confirm; Gate 3 push proceeds with open readiness criteria when the user authorizes the push. Replace the existing wording: *"You never call cleargate_push_item without approved: true and explicit human confirmation (Gate 3)."* with: *"You never call cleargate_push_item without approved: true (hard reject) and explicit human confirmation. Readiness gates (cached_gate_result.pass) are advisory by default — the push proceeds and the PM tool surfaces open criteria as labels; opt into hard-reject via STRICT_PUSH_GATES=true."*

**Out of scope (defer to follow-up CR per ambiguity-gate open question):**
- `mcp/src/adapters/pm-adapter.ts` interface extension with `gate_status` field. CR-010 §3 lists it but the current `PmAdapter` interface (verified `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/adapters/pm-adapter.ts:39-45`) is **read-only** (`pullItem`/`listUpdates`/`pullComments`/`detectNewItems`) — there is NO `pushItem` method on PmAdapter. The adapter does not currently render labels on push because `push-item.ts` does not call the adapter at all (verified — the function is pure DB write). Therefore the "PM-tool labels for open gates" rendering is a **future surface**. M1 ships only the server-side advisory semantics + the response field. Adapter-side label rendering is **flagged in §6 below as an ambiguity to escalate to orchestrator**.
- Removing `PushGateFailedError` class entirely (kept for strict-mode path).
- `cleargate-cli/src/commands/push.ts` client-side handling of the new response fields. If the CLI needs to display "pushed with open gates" on stdout, file a follow-up. Architect demotes this to out-of-scope for M1.

## 2. Test surface per CR

### CR-009
- Extend `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/commands/init.test.ts` — add three scenarios:
  1. After `initHandler({ cwd, payloadDir, yes: true })`, read `.claude/hooks/stamp-and-gate.sh` from `cwd` and assert (a) no literal `__CLEARGATE_VERSION__` substring remains, (b) the third resolver branch contains `npx -y "@cleargate/cli@0.5.0"` (pin matches the test seam version).
  2. Same assertion for `.claude/hooks/session-start.sh`.
  3. Probe success: stub `child_process.spawnSync` (or the test seam) so the resolver returns exit 0; assert init prints `🟢` line and exits 0.
  4. Probe failure: stub the resolver to fail all three branches; assert init prints `🔴` line and calls `exit(1)`.
- New `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/commands/init-pin.test.ts` (or extend init.test.ts):
  - `initHandler({ ..., pin: '0.6.0-beta' })` → hooks contain `@cleargate/cli@0.6.0-beta`.
- Extend `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/commands/doctor-session-start.test.ts` — add resolver-status-line assertion at the top of stdout.
- Manual smoke (acceptance step 1, CR-009 §4): clean `npx cleargate@0.5.0 init` in a fresh dir under `$(mktemp -d)`. **This is the canonical M1-close test (per CG_TESTING_v1).**

### CR-008
- Extend `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/commands/doctor-session-start.test.ts` — add three Phase A scenarios:
  1. Empty pending-sync + no `.active` sentinel → "Triage first, draft second:" block emitted.
  2. With one approved story (frontmatter `approved: true`) in pending-sync → block suppressed.
  3. With `.active` sentinel present + empty pending-sync → block suppressed.
- New `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/test_pre_edit_gate.sh` — bash table-driven harness (pattern: pre-existing scripts under `cleargate-cli/test/scripts/` if any; otherwise create the dir). Per FLASHCARD 2026-04-21 #test-harness #hooks #sed: prefer env injection over sed-surgery. Cases:
  1. Whitelist path `.cleargate/delivery/pending-sync/STORY-NEW.md` → exit 0.
  2. Whitelist path `CLAUDE.md` → exit 0.
  3. Non-whitelist `src/foo.ts`, no approved stories, MODE=warn → exit 0; log line `mode=warn would_block file=src/foo.ts reason=no_approved_stories` written.
  4. Non-whitelist `src/foo.ts`, MODE=enforce, no stories → exit 1.
  5. Bypass `CLEARGATE_PLANNING_BYPASS=1` → exit 0; log line `bypass=true`.
  6. Approved story with `implementation_files: ["src/foo.ts"]` exists → editing `src/foo.ts` → exit 0.
  7. Approved story exists but `implementation_files: ["src/bar.ts"]` only → editing `src/foo.ts` MODE=enforce → exit 1.
  8. Sprint-active sentinel present → editing any non-whitelist path → exit 0.
  9. Malformed stdin (no `tool_input.file_path`) → exit 0 (do not crash; gate is fail-open under malformed input).
- New scenarios in `doctor.test.ts` for the `--can-edit <file>` flag mirroring the bash cases (covers the Node code path; the bash test covers the hook orchestration).
- Manual smoke (acceptance step 1, CR-008 §4): in a fresh `npx cleargate@0.5.0 init` dir, prompt Claude with *"build me a website from this PDF"*, observe SessionStart injection of the planning-first reminder block.

### CR-010
- Modify `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/tools/push-item.test.ts:297-319` — invert the existing `STORY-010-07: cached_gate_result.pass=false throws PushGateFailedError` test:
  - **Default mode (advisory):** rename to `CR-010: cached_gate_result.pass=false succeeds with gate_status='open'`. Assert `result.version === 1`, `result.gate_status === 'open'`, `result.failing_criteria` contains `'criteria-1'`, AND DB row count is `1` (was 0).
  - Add a new `CR-010: STRICT_PUSH_GATES=true preserves PushGateFailedError`: pass `strictPushGates: true` in ctx; expect throw + zero DB rows.
  - Add `CR-010: cached_gate_result.pass=true returns gate_status='pass' with empty failing_criteria`.
  - Add `CR-010: cached_gate_result absent → gate_status undefined, succeeds`.
  - **Approved gate is unchanged** — keep the existing `approved !== true` test (verified L297 area pattern; locate `PushNotApprovedError` test) green without modification.
- Add a config-loader test in the existing `mcp/src/config.test.ts` (if absent, skip — Zod default coverage is implicit). Verify `STRICT_PUSH_GATES` defaults to `false` and `STRICT_PUSH_GATES=true` parses to boolean true.
- Manual smoke (CR-010 §4 acceptance): push BUG-006 (or any of the 22 currently-blocked items) in dogfood; expect 200 success + audit row `result='success', gate_status='open'`. **Do NOT verify Linear label rendering** — out of scope per §1 (adapter rendering is a follow-up).

## 3. Snapshot / regression contracts

Per Sprint Plan §2.3 ("each one ships a hook-output snapshot diff in its commit") and Risk R-05.

**CR-009 snapshot (locks the post-CR-009 hook bytes BEFORE CR-008 lands):**
- Capture command (Developer runs at end-of-CR-009 work):
  ```bash
  cd $(mktemp -d) && git init -q
  /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/dist/cli.js init --yes --pin 0.5.0
  cat .claude/hooks/stamp-and-gate.sh > /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/stamp-and-gate.cr-009.sh
  cat .claude/hooks/session-start.sh > /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/session-start.cr-009.sh
  ```
- Lock files:
  - `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/stamp-and-gate.cr-009.sh`
  - `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/session-start.cr-009.sh`
- Add a vitest snapshot test in `init.test.ts` that re-runs the init and `expect(stampAndGateBytes).toMatchSnapshot()` against the lock files. Vitest auto-renames; lock-file-on-disk approach is cleaner — Architect recommends storing them as plain `.sh` and asserting byte-equal in the test.

**CR-008 snapshot (locks the post-CR-008 hook bytes — supersedes CR-009 lock):**
- Same capture command; new lock-file paths:
  - `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/stamp-and-gate.cr-008.sh` (unchanged — CR-008 does not touch this hook; assert byte-equal to the CR-009 lock).
  - `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/session-start.cr-008.sh` (CR-008 drops `2>/dev/null` — diff vs CR-009 lock is one line).
  - `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/pre-edit-gate.cr-008.sh` (new, full file lock).
- Each snapshot test fails if the rendered hook drifts; QA verifies the diff is bounded to the expected surface (CR-008's diff = exactly one stdout-routing change + one new file).

**Snapshot test runner:** `npm test -- snapshots/hooks` from `cleargate-cli/`.

**Why two locks per CR (cr-009 vs cr-008):** Sprint plan §2.3 R-05 mitigation is "each item ships a hook-output snapshot diff in its commit." The two-lock pattern lets QA diff `cr-009.sh` vs `cr-008.sh` and confirm only the expected lines change. M2's BUG-009 will append a `cr-bug-009.sh` lock and diff against `cr-008.sh` — that hand-off contract is in §4 below.

## 4. Merge-order callouts (shared-file triple-touch)

`stamp-and-gate.sh` is touched by three sprint items: **CR-009 (M1) → CR-008 (M1, none — CR-008 only touches `session-start.sh`, NOT `stamp-and-gate.sh`) → BUG-009 (M2)**.

Re-checking sprint plan §2.2: the sprint plan lists CR-008 as touching `stamp-and-gate.sh`. Inspecting the CR-008 file — §3 Phase A only modifies `session-start.sh:15` and `doctor.ts`. Phase B adds `pre-edit-gate.sh`. Phase B does NOT modify `stamp-and-gate.sh`. **Architect resolution: CR-008 does NOT touch `stamp-and-gate.sh` — sprint plan §2.2 is overstating the surface. M1 ships only CR-009's edit to `stamp-and-gate.sh`. BUG-009 in M2 is the second toucher.**

This means the M1-to-M2 hand-off is simpler than the sprint plan implied:

**Contract M1 hands off to BUG-009 in M2:**
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/stamp-and-gate.sh` after M1 will be exactly the bytes locked at `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/stamp-and-gate.cr-009.sh`. BUG-009's M2 plan must:
  1. Use the cr-009 lock as its starting point.
  2. Append the work-item-id detection branches for `CR-` and `PROPOSAL-` prefixes (per BUG-009's brief in sprint plan §1: "SubagentStop hook does not record token-ledger rows for CR / PROPOSAL drafts" — note this is `token-ledger.sh`, NOT `stamp-and-gate.sh`).
  3. Re-check: BUG-009 description says `SubagentStop hook` — this is `token-ledger.sh`, not `stamp-and-gate.sh`. **The sprint plan §2.2 entry "stamp-and-gate.sh: CR-009, CR-008, BUG-009" is therefore WRONG on two counts** (CR-008 doesn't touch it; BUG-009 doesn't touch it). The actual triple-touch on `stamp-and-gate.sh` does NOT exist this sprint. M1 is the only toucher.

**Architect flags this discrepancy to the orchestrator (see §7 below).**

**Actual M1-to-M2 hand-off (corrected):**
- `stamp-and-gate.sh` post-M1 is final for the sprint (no M2 edit). BUG-009 lands in `token-ledger.sh` (separate file, no M1 touch — not part of any M1 CR).
- `session-start.sh` post-M1 is final for the sprint (CR-009 + CR-008 are the only touchers; BUG-009 does not touch it).
- `pre-edit-gate.sh` post-M1 ships in warn-only mode; M2/M3/M4/M5 do not edit it. The 48h-soak promotion to enforce mode is a post-sprint manual operation (per R-02), NOT a sprint deliverable.

**M1 internal merge order (per sprint plan §2.2):**
1. **CR-009 first.** Until the resolver pin lands, CR-008's stdout routing pipes to a CLI that may not exist. Strict order.
2. **CR-008 second.** Phase A (one-line drop of `2>/dev/null`) + Phase B (new `pre-edit-gate.sh` + new doctor mode).
3. **CR-010 third.** Disjoint surface (`mcp/`); could parallelise with CR-008 but Sprint Plan schedules it last for stability and to avoid concurrent main-branch dirtiness across two repos. Architect agrees: serialize.

**Pre-merge gate per CR:** snapshot test green + `npm run typecheck && npm test` green for the affected workspace (`cleargate-cli` for CR-009 / CR-008; `mcp` for CR-010). Pre-commit hook runs both per CLAUDE.md.

## 5. Risk-table cross-reference

M1 surfaces touch the following risks from Sprint Plan §Risks:

- **R-01 (PROPOSAL-013 not approved → M3/M4/M5 dropped):** independent of M1; M1 ships unconditionally regardless of R-01 outcome.
- **R-02 (CR-008 Phase B false positives):** **owned by M1.** Mitigation per §1 above: ship `pre-edit-gate.sh` in `MODE=warn` default, wired via init's `HOOK_ADDITION` block. The 48h-soak + promotion-to-enforce is a post-sprint operation. Architect adds: the warn-log file `.cleargate/hook-log/pre-edit-gate.log` MUST be added to `.gitignore` in the same commit as CR-008 lands (sprint kickoff bookkeeping covers `.cleargate/hook-log/` already per Execution Guidelines item 3 — confirm before commit).
- **R-05 (hook chain triple-touch merge conflicts):** Architect §4 finding: the triple-touch does not exist this sprint. M1 owns serial CR-009 → CR-008 with snapshot locks. R-05 is **partially mitigated** — actually a *double*-touch (CR-009 modifies both, CR-008 modifies only `session-start.sh`). Snapshot tests green = mitigated.
- **R-07 (CR-010 advisory-tag rendering depends on PM-tool label support):** **partially deferred by Architect.** M1 ships only the server-side response shape (`gate_status` + `failing_criteria`). PM-tool label rendering is escalated to §7 — adapter does not currently have a `pushItem` method, so the rendering surface does not exist. M1's CR-010 ships *without* the label rendering; the rendering is a follow-up CR (file at sprint close).
- **R-08 (BUG-008 widens the gate):** BUG-008 lands in M2; M1 does not touch the `proposal-approved` criterion. R-08 is M2's risk, not M1's. Architect notes for M2 plan: when BUG-008 ships, the unit test in CR-010's adjacent test file (R-08 mitigation) must be added. CR-010 in M1 already touches `push-item.test.ts`; the BUG-008 test will be a sibling in the same file and rebases trivially on top of M1.

## 6. Acceptance Gherkin reference (canonical M1-close smoke tests)

Each CR's acceptance criteria are already enumerated in the source files. M1-close validates a curated subset:

- **CR-009 acceptance #1 ("Fresh install, no global cleargate"):** clean `npx cleargate@0.5.0 init` in a fresh dir under `$(mktemp -d)`. Init's tail prints 🟢, hooks contain literal pinned version, `.cleargate/hook-log/gate-check.log` shows `stamp=0 gate=0 ingest=0` after a triggered Edit. **CG_TESTING_v1 evidence per CR-009 §0.** This is the canonical M1-close smoke test for CR-009.
- **CR-008 Phase A acceptance #1-3:** same fresh init dir; SessionStart hook injects the planning-first reminder block; prompt Claude with *"build me a website from this PDF"*; assert Claude triages before Edit/Write. **CG_TESTING_v1 evidence per CR-008 §0.** This is the canonical M1-close smoke test for CR-008. Phase A reminder block must be visible in Claude's session-start system reminders.
- **CR-008 Phase B acceptance:** with `CLEARGATE_PLANNING_GATE_MODE=warn` (default post-init), trigger an Edit on a non-whitelist path with no approved story → log line written to `.cleargate/hook-log/pre-edit-gate.log` + edit succeeds (warn mode); flip env to `enforce`, repeat → exit 1.
- **CR-010 acceptance #1 ("Default mode advisory") + #4 ("approved still hard-rejects"):** push a Bug with `cached_gate_result.pass=false, approved=true` → 200 success with `gate_status='open'`; push with `approved=false` → `PushNotApprovedError`. **Acceptance #2 (re-push closes the loop) and #3 (strict mode) are unit-test-coverable** — no manual smoke needed.
- **Single combined fresh-install smoke (the canonical M1-close):** ONE invocation of `npx cleargate@0.5.0 init` in a fresh dir, then open Claude Code, observe (a) green resolver line at init tail, (b) SessionStart resolver-status block + planning-first reminder, (c) PostToolUse logs `stamp=0 gate=0 ingest=0`, (d) prompt Claude with the website request → triage before Write. CG_TESTING_v1 explicitly confirms this is THE acceptance test for both CR-008 and CR-009.

**Ambiguities flagged from CR bodies (route to orchestrator):**

1. **CR-010 §3 specifies `mcp/src/adapters/pm-adapter.ts` extension and `mcp/src/adapters/linear-adapter.ts` label rendering, but the current `PmAdapter` interface has no `pushItem` method (verified — adapter is read-only).** The "pushed item gets a label on the remote" capability does not currently exist anywhere in code. Two interpretations:
   - (a) M1 is server-side only; rendering ships in a follow-up CR after the adapter's write surface is added. **Architect picks this.**
   - (b) M1 also adds the adapter `pushItem` method + Linear `createIssue`/`updateIssue` calls. This is **L3+ scope, not L2** as labelled in sprint plan; rejected.
   Escalation: confirm with orchestrator that the M1 scope is (a). If user intended (b), CR-010 needs decomposition.

2. **CR-008 §1 Phase B item 5 says default is "disabled in settings.json post-init."** Architect picks `MODE=warn` as the default (Risk R-02 alignment) — not literally "disabled" but functionally equivalent (warn never blocks). Confirm with orchestrator that warn-mode-on-by-default satisfies R-02 vs. literally-not-installed-by-default. The latter would mean `pre-edit-gate.sh` is shipped but `settings.json` does NOT register the PreToolUse hook block — flipping requires `cleargate config set strict-gate true`. **Architect's call: register the hook in warn-mode by default — gives 48h of real-world false-positive data for the post-soak promotion decision; literally-off-by-default gives zero data.**

3. **CR-009's "Confirm placeholder substitution approach (`__CLEARGATE_VERSION__` straight string replace) vs. a `.template.sh` extension + render step":** Architect picks straight string replace (cited in §1 of CR-009 as recommendation; FLASHCARD 2026-04-19 #tsup #npm-publish #assets is consistent). One-pass `replaceAll` in copy-payload.ts; no template engine.

4. **CR-009's "Confirm `--pin <ver>` flag is in scope":** Architect picks YES (in scope per CR-009 ambiguity-gate item; cheap; surfaces a useful escape hatch).

## 7. Out of scope for M1 (and forward-flagged constraints for later milestones)

**Explicitly NOT touched in M1:**
- `state.json` schema (M3 owns — STORY-022-02). M1 does not read or write `state.json`.
- Reporter contract / Sprint Report v2.1 (M5 owns — STORY-022-07). M1 does not modify `.cleargate/templates/sprint_report.md` or `.cleargate/scripts/close_sprint.mjs`.
- Templates (`.cleargate/templates/*.md`) — M3 owns (STORY-022-03).
- Architect / Developer / QA / Reporter agent contracts (`.claude/agents/*.md`) — M3/M4/M5 own.
- Lane classifier (`pre_gate_runner.sh`, hotfix lane, lane field on stories) — M3+M4.
- BUG-008 / BUG-009 / STORY-014-01 — M2.
- Self-upgrade / version bumps — M5 (STORY-014-02). `cleargate-cli/package.json` stays at 0.5.0; `cleargate-planning/MANIFEST.json` stays at `cleargate_version: 0.5.0`. The pin injected into hook scripts at init time is `0.5.0`.
- PM-tool adapter `pushItem` method + label rendering for CR-010 advisory tags — follow-up CR (sprint close), not M1.

**Forward-flagged constraints surfaced from M1 (for later milestone Architect plans):**

1. **M5 / STORY-022-07 reporter constraint surfaced from CR-009:** CR-009 does NOT add a new reporter section, but the resolver-status block emitted by `cleargate doctor --session-start` produces a new line of stdout that the SessionStart hook now passes through (CR-008 Phase A). If REPORT.md ever attempts to capture SessionStart hook output, it MUST tolerate a leading `cleargate CLI: <branch>` line. M5 architect plan should account for this when designing the §7 Branch + Commit Map ingestion.

2. **M5 / STORY-022-07 reporter constraint surfaced from CR-008:** Phase B's warn-log file `.cleargate/hook-log/pre-edit-gate.log` is a new artifact that did not exist before this sprint. STORY-022-07's Sprint Report v2.1 §6 ("What Worked / What Didn't") may want to surface false-positive counts from this log. M5 architect plan should define whether `close_sprint.mjs` reads + summarises this log; if yes, add a `pre_edit_gate_warn_count: N` field to the §0 AI Continuity Block.

3. **M5 / STORY-014-02 self-upgrade constraint surfaced from CR-009:** the hook scripts now contain a literal pin (e.g. `@cleargate/cli@0.5.0`). When 014-02 runs `cleargate upgrade` to bump 0.5.0 → 0.6.0, the hook-script copy MUST re-stamp the pin to `0.6.0`. If this is forgotten, the post-upgrade hooks will still resolve via `npx -y @cleargate/cli@0.5.0` — the pre-upgrade pin. R-10 (self-upgrade clobbers local data) does not cover this; flag in M5 architect plan as a NEW risk: *"R-10b: Self-upgrade may leave hook scripts pinned to the pre-upgrade version unless re-stamping is wired."* Mitigation: add a post-copy re-stamp step in `cleargate upgrade` mirroring init's substitution; OR document that 014-02 must run a second `cleargate init --force --pin 0.6.0` after the upgrade. Architect prefers the former.

4. **M5 / STORY-014-02 manifest version bump surfaced from CR-009:** the `overwrite_policy` enum gains `'pin-aware'` in M1. The 014-02 self-upgrade three-way merge driver (if it exists by then per EPIC-009) must understand this new policy value. If EPIC-009's upgrade driver does not yet honour `pin-aware`, M1's value is effectively `always`-equivalent and harmless; document this in the M5 plan.

## 8. Pre-flight for the Developer

Before each CR's work:

**CR-009 pre-flight:**
1. `cd /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli && npm install && npm run prebuild` (copies the payload templates into `cleargate-cli/templates/`).
2. `npm run typecheck && npm test` — baseline green before edits.
3. Grep for any other site that hard-codes `cleargate-cli/dist/cli.js` resolver patterns: `grep -rn "command -v cleargate" /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/`.

**CR-008 pre-flight:**
1. CR-009 must be merged to `sprint/SPRINT-14`.
2. `cat /Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/snapshots/hooks/stamp-and-gate.cr-009.sh` — confirm the lock exists.
3. Verify story-template `implementation_files:` field exists at `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/story.md`. If absent, the doctor `--can-edit` fallback to "any approved story → allow" is the correct behaviour (gate fails open when the field is unspecified).

**CR-010 pre-flight:**
1. `cd /Users/ssuladze/Documents/Dev/ClearGate/mcp && docker compose up -d` (Postgres 18 + Redis 8 — real infra per CLAUDE.md).
2. `cd /Users/ssuladze/Documents/Dev/ClearGate/mcp && npm run typecheck && npm test` — baseline green.
3. Confirm `STRICT_PUSH_GATES` is not already in `mcp/.env.example` (per FLASHCARD 2026-04-25 #config #env-schema #drift, both files matter — if missing from `.env.example`, add it as documented-default `false`).

Pre-commit hook runs typecheck + tests; do not `--no-verify`.

---

## Architect's notes for orchestrator

- Canonical M1-close test = **one** clean `npx cleargate@0.5.0 init` in a fresh dir, opened in Claude Code, prompted with the CG_TESTING_v1 website request. Validates CR-009 (resolver pin works) AND CR-008 (planning-first reminder reaches Claude) in a single observation.
- Three ambiguities in §6 require orchestrator confirmation before Developer spawn — most importantly, whether CR-010 M1 scope is server-side-only (Architect picks YES). If user intended adapter-side label rendering this sprint, CR-010 is mis-sized.
- Sprint plan §2.2 lists `stamp-and-gate.sh` as triple-touched (CR-009 + CR-008 + BUG-009). On code inspection, only CR-009 touches it. M1's hand-off to BUG-009 in M2 is therefore moot — flagged in §4.
- M1 sticks to its lane: no `state.json`, no reporter, no templates, no agent contracts. Forward-flags for M5 are noted in §7 items 1-4.
