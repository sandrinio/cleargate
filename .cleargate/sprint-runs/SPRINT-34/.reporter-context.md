# Reporter Context Bundle — SPRINT-34

_Generated: 2026-06-04T07:25:44.719Z_

---

## Sprint Plan Slices

_Source: SPRINT-34_Polyglot_Portability_Hardening.md_

## 1. Consolidated Deliverables
*(Eight CRs. CR-077…082 are EPIC-045 children (SPRINT-66 portability/loop findings F1–F11); CR-075/CR-076 are EPIC-043 hygiene CRs carried from SPRINT-33.)*

## 2. Execution Strategy
*(Written by Architect during Sprint Design Review. Required before sprint start. `execution_mode: v2-parallel` — most CRs are independent; the agent-file and qa.md contenders are serialized below.)*

## State.json Summary

Sprint: SPRINT-34 | Status: Active

CR-077 | Done | standard | qa_bounces=0 arch_bounces=0
CR-079 | Done | standard | qa_bounces=0 arch_bounces=0
CR-080 | Done | fast | qa_bounces=0 arch_bounces=0
CR-078 | Done | fast | qa_bounces=0 arch_bounces=0
CR-081 | Done | standard | qa_bounces=0 arch_bounces=0
CR-082 | Done | standard | qa_bounces=0 arch_bounces=0
CR-075 | Done | standard | qa_bounces=0 arch_bounces=0
CR-076 | Done | standard | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

# Milestone: M1 — Polyglot Portability Foundation (Wave 1)
## CRs: CR-077, CR-079 (CR-080 is fast-lane — NOT planned here)
## Wave: W1 (parallel-capable in v2-parallel; orchestrator runs SERIAL Phase C via kill-switch → CR-077 fully merged, THEN CR-079)

> role: architect
> Sprint goal anchor: "Harden ClearGate so a fresh install runs a sprint end-to-end on an arbitrary (non-node, worktree-isolated) target repo without orchestrator hand-fixes." Both M1 CRs are the *foundation* layer — they make the shipped scaffold runner-neutral and the worktree config-complete, which every later wave (CR-078/081/082) builds on.

---

## Storage-class legend (this meta-repo spans three classes — every file below is tagged)

| Tag | Surface | Where the Developer edits | When it goes live | Test runs where |
|---|---|---|---|---|
| **[Class 1 cli]** | `cleargate-cli/src/**` + `cleargate-cli/test/**` | `story/CR-XXX` branch in the **MAIN `cleargate-cli/` checkout** (NOT a worktree) | on `npm run build` (dist rebuild) — Gate-4 deferred | `tsx --test` from `cleargate-cli/` |
| **[Class 2 canonical-live-deferred]** | `cleargate-planning/.claude/**` (agents/skills) | MAIN outer checkout on `sprint/S-34` | live `/.claude/` re-sync **deferred to Gate-4** (`npm run prebuild` → `cleargate init`) | mirror-parity tests RED until Gate-4 (expected) |
| **[Class 3 tracked-live-on-merge]** | `.cleargate/**` working copy + its `cleargate-planning/.cleargate/**` mirror | MAIN outer checkout on `sprint/S-34`, BYTE-IDENTICAL in both copies | **LIVE ON MERGE** — active for this sprint's own later stories + close | fixture / shell assertions; `CLEARGATE_SKIP_*` seams are break-glass |

---

## Order (strict)

**CR-077 → CR-079.** Forced by the shared file `gate-checks.json` (CR-077 rewrites `qa.typecheck`/`qa.test`/`arch.typecheck` command values; CR-079 ADDS a new `arch.provisioned_config` key + leaves `arch.stray_env_files` in place). Disjoint keys, but the serial loop merges CR-077 first, so **CR-079's Developer rebases mentally onto the POST-077 `gate-checks.json`** — the file CR-079 opens already contains CR-077's repo-derived command values. CR-079 must NOT re-introduce `cd cleargate-cli` literals or touch the typecheck/test keys.

No parallelizable pair inside M1 under the kill-switch — both run on `sprint/S-34` and both touch `gate-checks.json`; serial is mandatory regardless of the v2-parallel topology label.

---

## Worktree-vs-main-checkout decision (per the cross-repo policy — load-bearing)

**The standard SKILL §C.2 `git worktree add .worktrees/CR-XXX` does NOT cleanly hold for either M1 CR. Adapt §C.2 as follows.**

### CR-077 — SPANS Class 1 + Class 2 + Class 3. NO outer worktree.
- The cli detector + its unit test (`cleargate-cli/src/**`, `cleargate-cli/test/**`) are **absent from any outer `.worktrees/` checkout** (cleargate-cli is a gitignored nested repo). A `.worktrees/CR-077` would not even contain them.
- The cli detector test resolves `REPO_ROOT` via `path.resolve(__dirname, '..', '..', ...)` and reads `../.cleargate/scripts/*`; this ONLY resolves when cleargate-cli sits at `<root>/cleargate-cli/`. A `.worktrees/...` depth → `ERR_MODULE_NOT_FOUND`.
- The outer edits (agents canonical, `gate-checks.json`, sprint_context template) are Class 2/3, and the cli detector's F6-regression test (`cleargate init` into a `mktemp -d`, then assert `gate-checks.json` has no `cd cleargate-cli`) needs the **post-init payload to reflect the canonical edits**, which requires `npm run prebuild` to have mirrored canonical → `cleargate-cli/templates/...`. That coupling demands the outer tracked edits live in the **MAIN outer checkout on `sprint/S-34`**, not an isolated worktree.

  **Mechanism for CR-077:**
  - Class 1 cli (detector + test): `git -C cleargate-cli checkout -b story/CR-077 main`; edit + test in the MAIN cli checkout; `tsx --test 'test/**/init-test-stack-detect.red.node.test.ts'`.
  - Class 2 (developer/qa/architect.md canonical) + Class 3 (`gate-checks.json`, sprint_context template): edit in the **MAIN outer checkout on `sprint/S-34`** (so the cli F6-regression test, run after `npm run prebuild`, resolves REPO_ROOT and sees them).
  - **§C.2 adaptation:** SKIP the `git worktree add` step for CR-077. Note in the dispatch: "CR-077 runs on the main sprint/S-34 checkout + a cli story branch; no `.worktrees/CR-077`."

### CR-079 — Class 3 (+ Class 2 SKILL). Outer worktree is OK *for the edits* but the verification test self-creates a worktree.
- All CR-079 edits are Class 3 tracked (`pre_gate_runner.sh`, `config.yml`, `gate-checks.json`, `provision_worktree_config.sh`) + one Class 2 canonical (`SKILL.md`) + `config.example.yml`. None are in `cleargate-cli/`, so an outer `.worktrees/CR-079` WOULD contain them.
- BUT CR-079's §4 verification *itself* does `git worktree add .worktrees/TEST-079 ...` and runs the provisioning script + scan against it. Nesting a test-worktree inside a story-worktree is fragile (worktree-of-a-worktree, and the test's `mktemp`/symlink absolute-path assertions assume repo-root). **Recommendation: run CR-079 in the MAIN outer checkout on `sprint/S-34`** so its self-created `.worktrees/TEST-079` sits at the canonical repo root where the symlink-absolute-path assertion holds. The Class 3 scripts go live on merge into `sprint/S-34` regardless.

  **Mechanism for CR-079:** MAIN outer checkout on `sprint/S-34`; no `.worktrees/CR-079` story-worktree (the test creates + tears down its own `.worktrees/TEST-079`). §C.2 adaptation: skip the per-story worktree; document "CR-079 edits + tests run on the sprint/S-34 main checkout; the §4 test self-provisions `.worktrees/TEST-079`."

---

# ════════════════════════════════════════════════════
# BLUEPRINT — CR-077 (Repo-derived test conventions + gate commands; F3 + F6)
# ════════════════════════════════════════════════════

## 1. Goal linkage
Makes the shipped scaffold *runner-neutral and repo-derived* — strips the meta-repo's own EPIC-028 "node:test mandatory / vitest forbidden" policy and the `cd cleargate-cli && npm run typecheck` gate literal from the shipped payload, so a fresh non-node `cleargate init` (pytest / vitest / go) runs its sprint with the correct runner and zero permanent F6 false-FAILs. This is *the* gating portability fix for the sprint goal.

## 2. Files to touch

| File | Class | Mechanism |
|---|---|---|
| `cleargate-planning/.claude/agents/developer.md` | **[Class 2 canonical-live-deferred]** | main outer `sprint/S-34`; live re-sync Gate-4 |
| `cleargate-planning/.claude/agents/qa.md` | **[Class 2 canonical-live-deferred]** | main outer `sprint/S-34` (077 lands FIRST per §2.2; 081/082 amend later) |
| `cleargate-planning/.claude/agents/architect.md` | **[Class 2 canonical-live-deferred]** | main outer `sprint/S-34` |
| `cleargate-planning/.cleargate/scripts/gate-checks.json` | **[Class 3 tracked-live-on-merge]** | main outer `sprint/S-34` — BYTE-IDENTICAL with live `.cleargate/scripts/gate-checks.json` |
| `.cleargate/scripts/gate-checks.json` (live working copy) | **[Class 3 tracked-live-on-merge]** | same edit, byte-identical; goes LIVE for this sprint's own later gate runs |
| `cleargate-planning/.cleargate/templates/sprint_context.md` | **[Class 3 tracked-live-on-merge]** | main outer `sprint/S-34` |
| `.cleargate/templates/sprint_context.md` (live working copy) | **[Class 3 tracked-live-on-merge]** | byte-identical mirror |
| `cleargate-cli/src/init/copy-payload.ts` (or new adjacent `detect-test-stack.ts`) | **[Class 1 cli]** | `story/CR-077` branch in MAIN cli checkout |
| `cleargate-cli/test/init/init-test-stack-detect.red.node.test.ts` (NEW) | **[Class 1 cli]** | cli branch; `tsx --test` |

> NOTE on `pre_gate_common.sh` header: its comment "Handles Node+TS stacks only (no Python/Rust/Go/Java/Swift detectors)" stays TRUE — the *gate runner* still only runs the configured command and skips when no `package.json`. CR-077 does NOT teach the bash runner to detect stacks; detection happens at `init` (cli, Class 1) and is written INTO `gate-checks.json`. Do not edit `pre_gate_common.sh` for CR-077.

## 3. Schema / config deltas

### 3a. `gate-checks.json` — `qa.typecheck`, `qa.test`, `arch.typecheck` become repo-derived placeholders, not `cd cleargate-cli` literals.

Current (the F6 source):
```json
"qa":   { "typecheck": "cd cleargate-cli && npm run typecheck", "test": "cd cleargate-cli && npm test", ... }
"arch": { "typecheck": "cd cleargate-cli && npm run typecheck", ... }
```

New SHIPPED-payload default (`cleargate-planning/.cleargate/scripts/gate-checks.json`) — empty string so the runner's existing `[[ -n "$typecheck_cmd" && -f package.json ]]` guard skips to INFO (no false-FAIL) until `init` fills it:
```json
"qa":   { "typecheck": "", "test": "", "debug_patterns": [...], "todo_patterns": [...] }
"arch": { "typecheck": "", "new_deps_check": true, "stray_env_files": [".env",".env.local",".env.production"], "file_count_report": true }
```
At `cleargate init`, the detector (§4) OVERWRITES `qa.typecheck`/`qa.test`/`arch.typecheck` with the derived command (e.g. `pytest -q`, `vitest run`, `go test ./...`, or for a node target the project's own `npm run typecheck`/`npm test`).

> The meta-repo's OWN live `.cleargate/scripts/gate-checks.json` is filled with the meta-repo's real commands — NOT `cd cleargate-cli` (that path is meta-repo-internal and breaks the abstraction). Set the live copy to:
> ```json
> "qa":   { "typecheck": "npm run typecheck --workspace=cleargate-cli", "test": "npm test --workspace=cleargate-cli", ... }
> "arch": { "typecheck": "npm run typecheck --workspace=cleargate-cli", ... }
> ```
> These mirror the already-correct `config.yml` `gates.*` values (lines 24-26) — reuse those exact strings; do not invent new ones.

### 3b. `sprint_context.md` — add a structured `## Test Stack` block (the runtime authority agents read as OVERRIDING their defaults).

Insert AFTER `## Locked Versions` and BEFORE `## Cross-Cutting Rules`:
```markdown
## Test Stack

Repo-derived test conventions. Written best-effort by `cleargate init` (detector); the
orchestrator may correct any field per-sprint. Agents read this block as OVERRIDING their
built-in defaults — Developer/QA/Architect use these values, not any hardcoded runner.

| Field | Value |
|-------|-------|
| Backend runner    | _(e.g. `pytest -q` · `go test ./...` · `npm test`)_ |
| Frontend runner   | _(e.g. `vitest run` — blank if single-stack)_ |
| Typecheck command | _(e.g. `mypy .` · `tsc --noEmit` · blank if none)_ |
| Red-test naming   | _(e.g. `*.red.test.tsx` · `test_*_red.py` · `*.red.node.test.ts`)_ |

_If unresolved at init: leave the table stubbed. The pre-gate scan emits a one-line
"test_stack unresolved — populate sprint_context.md §Test Stack" advisory and treats the
typecheck/test gate as advisory (not FAIL) until populated. (§0.5 backstop decision.)_
```

> Polyglot capture (Chyro = pytest backend + vitest frontend) is why this is a TABLE with separate Backend/Frontend runner rows, not a single `runner:` field — a single detected runner cannot represent a polyglot target.

### 3c. Agent-text deltas (the EVICTION — strip EPIC-028 policy, parameterize off §Test Stack)

- **`developer.md:82-91`** — replace the "## Inner-loop test runner" block. Remove: "`node:test` … single, mandatory runner across all ClearGate packages (EPIC-028)"; "vitest is fully eliminated … blocked by the `check:no-vitest` pre-commit guard"; the `*.node.test.ts` naming line; the `mcp/`+`cleargate-cli/` `tsx --test …` run command line. Replace with: "Use the project's test runner and red-test naming as declared in `sprint_context.md` §Test Stack. Run the backend runner (and frontend runner if present) before committing." Keep the generic mocking-pattern paragraph (DI seams / `mock.fn()`) — that is runner-agnostic guidance, NOT EPIC-028 policy.
  - `developer.md:126-127` (the "files you must not edit" list — `**/*.red.test.ts` / `**/*.red.node.test.ts`): GENERALIZE to "QA-Red-authored red-test files (naming per `sprint_context.md` §Test Stack)". Do NOT delete the bullet; it protects red-test immutability regardless of naming.
- **`qa.md:43, 47, 52`** — replace literal `*.red.node.test.ts` with "the red-test naming declared in `sprint_context.md` §Test Stack". `qa.md:98` (the runtime-lane skip clause referencing `cleargate-cli/`/`mcp/`) and `qa.md:116` (`prep_qa_context.mjs` heuristic referencing `cleargate-cli/src/commands/`) describe the META-REPO's own packages — these are NOT EPIC-028 policy leaks; **leave them** (they are this repo's own runtime-lane heuristics). See §7 Risk for the grep-scope reconciliation.
- **`architect.md:110, 116`** — TPV step: replace "list of `*.red.node.test.ts` files" and "Test files end in `*.red.node.test.ts`" with "the red-test naming declared in `sprint_context.md` §Test Stack". `architect.md:64` (the merged-story grep example referencing `cleargate-cli|src|.cleargate/scripts`) is a META-REPO path example in the adjacent-implementation-check — leave it (not a policy leak).

## 4. New code to create — init-time stack detector [Class 1 cli]

**Where it hooks in:** `copy-payload.ts` already lists files and copies the payload (SKIP_FILES at `cleargate-cli/src/init/copy-payload.ts:54`; copy loop at `:122`). Add a post-copy step (new module `cleargate-cli/src/init/detect-test-stack.ts`, called from the init orchestration AFTER the payload lands, so the freshly-copied `gate-checks.json` + `sprint_context.md` template are present to rewrite).

**Signature (sketch — do NOT implement here):**
```
export interface TestStack {
  backendRunner: string;     // '' if undetected
  frontendRunner: string;    // '' if single-stack
  typecheckCmd: string;      // '' if none
  redTestNaming: string;     // '' if undetected
  resolved: boolean;         // false → emit the "unresolved" advisory sentinel
}
export function detectTestStack(targetDir: string): TestStack;
export function applyTestStack(targetDir: string, stack: TestStack): void;
  // writes the §Test Stack block into <targetDir>/.cleargate/sprint-runs/... template
  // AND rewrites <targetDir>/.cleargate/scripts/gate-checks.json qa.typecheck/qa.test/arch.typecheck.
```

**Detection rules (inputs → outputs):**
- `pyproject.toml` OR `setup.cfg`/`pytest.ini` present → backendRunner `pytest -q`, redTestNaming `test_*.py` (red convention `test_*_red.py`), typecheckCmd `mypy .` only if `[tool.mypy]`/`mypy` in deps else `''`.
- `go.mod` present → backendRunner `go test ./...`, redTestNaming `*_red_test.go`, typecheckCmd `go vet ./...`.
- `package.json` present with `vitest` in deps → frontendRunner `vitest run` (or backendRunner if no python/go), redTestNaming `*.red.test.ts(x)`.
- `package.json` with `"test"` script but no vitest → backendRunner `npm test`, typecheckCmd `npm run typecheck` if that script exists.
- **Polyglot:** pyproject AND package.json/vitest → backendRunner `pytest -q` + frontendRunner `vitest run` (BOTH captured — the Chyro case).
- **None detected** → `resolved: false`; write the stub + advisory sentinel; gate-checks command stays `''` (skip-to-INFO, not a wrong runner). Per §0.5 fail-loud-at-gate decision: do NOT default to node:test.

**Absolute-path safety:** operate on `path.resolve(targetDir, ...)`; never assume cwd.

## 5. Test shape (QA-Red `*.red.*`)

### 5a. Detector unit test — `cleargate-cli/test/init/init-test-stack-detect.red.node.test.ts` [Class 1 cli; runs `tsx --test`]
Per CR-077 §4. Assert (each via a `mktemp -d` fixture or a `test/fixtures/test-stack/{pytest,vitest,polyglot,go,none}` dir):
1. pytest fixture (`pyproject.toml`) → `stack.backendRunner === 'pytest -q'`, `typecheckCmd` derived, NOT node:test.
2. vitest fixture (`package.json` w/ vitest dep) → `frontendRunner === 'vitest run'`.
3. polyglot fixture (pyproject + vitest package.json) → BOTH `backendRunner` (pytest) AND `frontendRunner` (vitest) populated.
4. go fixture (`go.mod`) → `backendRunner === 'go test ./...'`.
5. undetectable fixture (empty dir) → `resolved === false`, runners `''` (sentinel — NOT a node:test default).
6. `applyTestStack` against a copied `gate-checks.json` fixture → resulting `arch.typecheck` does NOT contain `cd cleargate-cli`; contains the derived command (or `''` for unresolved).

### 5b. Eviction grep — OUTER shell assertion [Class 3; expressed as a `cleargate-cli/test/scripts/cr077-eviction.red.node.test.ts` that shells out, OR a standalone bash test under `.cleargate/scripts/test/`]
**SCOPE-NARROWED grep (see §7 Risk — the CR §4 grep as literally written hits 9 files and CANNOT return zero without breaking the wiki/devops/reporter agents).** QA-Red asserts ZERO matches for the EPIC-028-POLICY tokens on the THREE §3-scoped files only:
```
grep -rniE "node:test|vitest|check:no-vitest|tsx --test|node\.test\.ts" \
  cleargate-planning/.claude/agents/developer.md \
  cleargate-planning/.claude/agents/qa.md \
  cleargate-planning/.claude/agents/architect.md \
  cleargate-planning/.cleargate/scripts/gate-checks.json \
  cleargate-planning/.cleargate/templates/sprint_context.md
# → expect ZERO matches (policy tokens evicted from the three scoped agents + gate config + template)
```
The bare token `cleargate-cli` is DROPPED from the eviction grep — it legitimately appears in devops/reporter/wiki agents as a meta-repo path and in qa.md:98/116 + architect.md:64 as meta-repo runtime-lane heuristics. Asserting zero `cleargate-cli` repo-wide would force deleting correct framework content. The gate-checks.json line is in-scope because its `cd cleargate-cli` literal IS the F6 leak.

### 5c. F6 regression — OUTER shell assertion [Class 1 cli test that does `cleargate init` into `mktemp -d`]
Per CR-077 §4: `cleargate init` into a non-node `mktemp -d` (no `cleargate-cli/`, no `package.json`) → assert the written `gate-checks.json` `arch.typecheck`/`qa.typecheck` do NOT contain `cd cleargate-cli`; the gate either runs the detected command or (for the empty fixture) records INFO-skip / the "test_stack unresolved" advisory — NEVER exit-127. **Requires `npm run prebuild` first** so the cli `templates/` payload reflects the canonical edits (note as a test-precondition).

## 6. Red-test naming (the irony — confirmed)
This very CR parameterizes red-test naming, but the meta-repo's OWN stack is node:test. So CR-077's own QA-Red tests are `*.red.node.test.ts` (e.g. `init-test-stack-detect.red.node.test.ts`). **Confirmed: the meta-repo's §Test Stack carries `*.red.node.test.ts`; these stories' red tests follow the meta-repo's own convention, not the parameterized target convention.** No contradiction — the parameterization is about what the SHIPPED scaffold tells a TARGET, not about how the meta-repo writes its own tests.

## 7. Risks (top 3)
1. **EPIC-028-strip scope (§2.5 ADR flag — HIGH).** Strip the node:test/vitest-forbidden policy from the SHIPPED payload (`cleargate-planning/.claude/agents/{developer,qa,architect}.md`) ONLY. PRESERVE the meta-repo's own node:test discipline: it lives in the meta-repo's LIVE `/.claude/agents/*.md` (gitignored, re-synced Gate-4) AND in the meta-repo's own `gate-checks.json`/`config.yml` commands + `check:no-vitest` pre-commit guard. The strip is NOT a reversal of EPIC-028 — it relocates an internal convention into the meta-repo's own DATA. Verify: the meta-repo's own `npm test` in `cleargate-cli/` stays green; `check:no-vitest` survives. Until the Gate-4 live re-sync, the live `/.claude/` keeps the OLD node:test-asserting agents — which is CORRECT for the meta-repo and means this sprint's own later stories still see node:test discipline.
2. **The §4 eviction grep is OVER-BROAD as literally written (spec-internal inconsistency, reconciled here, NOT a blocker).** It greps `cleargate-planning/.claude/agents` (the whole dir) for bare `cleargate-cli`, which matches devops.md (`npm run prebuild`, ledger lib path), reporter.md (ledger require path), and the three wiki subagents (`repo:` derivation) — all LEGITIMATE meta-repo self-references that must survive. The plan (§5b) narrows the verification to the THREE §3-Modify agents + policy tokens, matching CR §3 Execution Sandbox (which lists exactly those three agents). Developer/QA follow §5b's narrowed grep, not the literal §4 grep. This reconciliation is within the CR's stated scope — no orchestrator escalation needed.
3. **Cross-repo dist staleness (Gate-4).** The cli detector lands as source on `story/CR-077`; `tsx --test` runs source so it stays green. But the orchestrator's own `cleargate init`/`gate check`/`wiki ingest` run `dist/cli.js` — the detector is NOT live for those until `npm run build` in `cleargate-cli/`. Defer dist rebuild to Gate-4 (DoD item). The F6-regression test in §5c needs `npm run prebuild` (payload mirror) as a precondition, distinct from the `npm run build` (dist) deferral.

## 8. DoD checklist (CR-077)
- [ ] Detector module + `cleargate-cli/test/init/init-test-stack-detect.red.node.test.ts` green via `tsx --test` (Class 1, cli `story/CR-077` branch).
- [ ] developer.md / qa.md / architect.md EPIC-028 policy stripped + parameterized off §Test Stack (Class 2 canonical, main `sprint/S-34`).
- [ ] `gate-checks.json` shipped default = empty command strings; meta-repo live copy = `npm run typecheck/test --workspace=cleargate-cli` (Class 3, byte-identical canonical+live).
- [ ] `sprint_context.md` template + live mirror carry the `## Test Stack` block (Class 3, byte-identical).
- [ ] Narrowed eviction grep (§5b) returns ZERO matches on the three scoped agents + gate-checks + template.
- [ ] F6 regression: `cleargate init` into a non-node `mktemp -d` produces a `gate-checks.json` with no `cd cleargate-cli` (requires `npm run prebuild` first).
- [ ] Meta-repo self-check: `npm test` in `cleargate-cli/` green; `check:no-vitest` intact; the meta-repo's resolved test_stack still node:test.
- [ ] **Gate-4 deferred:** (a) `npm run prebuild` (mirror canonical→cli payload), THEN (b) `cleargate init` to re-sync live `/.claude/` + `/.cleargate/` (order load-bearing, BUG-024 class). (c) `npm run build` in `cleargate-cli/` to make the detector live in `dist/cli.js`. DevOps merges the cli `story/CR-077` branch → cleargate-cli `main` `--no-ff` LOCAL ONLY (no push/publish).

---

# ════════════════════════════════════════════════════
# BLUEPRINT — CR-079 (Worktree config provisioning + stray-env exemption; F4 + F7)
# ════════════════════════════════════════════════════

## 1. Goal linkage
Makes a fresh story worktree config-COMPLETE: `git worktree add` brings only tracked files, so a target whose build/tests read a gitignored `.env` (Chyro's `config.py` resolves `_ENV_FILE` via `parents[3]`) cannot run in-worktree. CR-079 provisions the configured gitignored config roots into each worktree (symlink default) AND exempts them from the `stray_env_files` scan — eliminating the F4 manual-symlink hand-fix and the F7 false-flag. Directly serves "runs a sprint end-to-end on a worktree-isolated target without orchestrator hand-fixes."

## 2. Files to touch

| File | Class | Mechanism |
|---|---|---|
| `.cleargate/scripts/pre_gate_runner.sh` | **[Class 3 tracked-live-on-merge]** | main outer `sprint/S-34` — goes LIVE for this sprint's own later gate runs |
| `cleargate-planning/.cleargate/scripts/pre_gate_runner.sh` (canonical mirror) | **[Class 3 tracked-live-on-merge]** | byte-identical |
| `.cleargate/scripts/gate-checks.json` | **[Class 3 tracked-live-on-merge]** | main `sprint/S-34` — **POST-077 baseline** (CR-077 already merged; do not touch its typecheck/test keys) |
| `cleargate-planning/.cleargate/scripts/gate-checks.json` (canonical mirror) | **[Class 3 tracked-live-on-merge]** | byte-identical |
| `.cleargate/config.yml` | **[Class 3 tracked-live-on-merge]** | main `sprint/S-34` — add `worktree.provision_config` |
| `.claude/skills/sprint-execution/SKILL.md` (live working copy edited via canonical) | **[Class 2 canonical-live-deferred]** | edit canonical; live re-sync Gate-4 |
| `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` (canonical) | **[Class 2 canonical-live-deferred]** | main `sprint/S-34`; live re-sync Gate-4 |
| `cleargate-planning/.cleargate/config.example.yml` | **[Class 3 tracked-live-on-merge]** | document the new key in full reference |
| `.cleargate/scripts/provision_worktree_config.sh` (NEW) | **[Class 3 tracked-live-on-merge]** | main `sprint/S-34` — LIVE ON MERGE |
| `cleargate-planning/.cleargate/scripts/provision_worktree_config.sh` (canonical mirror, NEW) | **[Class 3 tracked-live-on-merge]** | byte-identical |

> NOTE: CR-079's §3 Execution Sandbox lists the exemption list in `pre_gate_runner.sh` + a `config.yml` key, but the existing `stray_env_files` list lives in `gate-checks.json:12`, read via `read_config_field "arch.stray_env_files"`. The provisioned-config exemption is most consistent placed as a SIBLING `gate-checks.json` key (`arch.provisioned_config`), read by the same `read_config_field` helper — see §3. The `config.yml` `worktree.provision_config` key drives the *provisioning script*; `gate-checks.json arch.provisioned_config` drives the *scan exemption*. Keep ONE source: have `provision_worktree_config.sh` read `config.yml`, and seed `gate-checks.json arch.provisioned_config` from the same default (`[".env"]`). If keeping a single source is impractical in shell, the SIMPLER reuse is: the scan reads the SAME `config.yml worktree.provision_config` list it provisioned (one key, two readers). Recommend the single-`config.yml`-key approach; flag in §7.

## 3. Schema / config deltas

### 3a. `config.yml` — new `worktree` section
```yaml
worktree:
  # Gitignored config roots provisioned into each new story worktree (CR-079).
  # Symlinked by default (worktree-root/<root> -> absolute repo-root/<root>); copy via mode.
  provision_config:
    - .env
  provision_mode: symlink   # symlink (default) | copy
```
Meta-repo default `[".env"]`. Document the identical block (with a `.env.local` example) in `cleargate-planning/.cleargate/config.example.yml`.

### 3b. `gate-checks.json` — exemption reader (POST-077 baseline)
The scan must exempt provisioned roots. Recommended single-source: the scan reads `worktree.provision_config` from `config.yml` (NOT a new gate-checks key) so provisioning and exemption share one list. If that is shell-awkward (config.yml is YAML; `read_config_field` is JSON-only), the fallback is a new `gate-checks.json` key seeded from the same default:
```json
"arch": { ..., "stray_env_files": [".env",".env.local",".env.production"], "provisioned_config": [".env"] }
```
This is ADDITIVE to the POST-077 `gate-checks.json` — CR-079 adds `arch.provisioned_config` and leaves CR-077's `qa.typecheck`/`qa.test`/`arch.typecheck` values UNTOUCHED.

### 3c. `pre_gate_runner.sh` stray-env exemption (lines ~249-271)
Between reading `stray_patterns` and the `for pat in ... if [[ -f "${WORKTREE}/${pat}" ]]` loop, read the provisioned-config list and SKIP any pattern present in it before recording FAIL:
```bash
# read provisioned exemptions (config.yml worktree.provision_config OR gate-checks arch.provisioned_config)
# for each stray match: if pattern ∈ provisioned list → continue (exempt), do not set stray_found=1
```
The exemption is SCOPED to the configured list — a non-provisioned `.env.bak` still FAILs (negative control, §5).

### 3d. SKILL §C.2 (canonical, `:244-256`) — document the provisioning step
After the `git worktree add .worktrees/STORY-NNN-NN ...` block, add:
```markdown
After creating the worktree, provision configured gitignored config into it:

    bash .cleargate/scripts/provision_worktree_config.sh .worktrees/STORY-NNN-NN

This symlinks (or copies, per `config.yml worktree.provision_mode`) the roots in
`config.yml worktree.provision_config` (default `[.env]`) so the target's build/tests
load their config in-worktree. The same list is exempt from the stray-env scan.
```
> Mirror this against the M1 worktree-policy note above: for THIS sprint's own CR-079 the §C.2 worktree step is skipped (the §4 test self-creates `.worktrees/TEST-079`), but the SKILL text documents the general-target behavior.

## 4. New code to create — `provision_worktree_config.sh` [Class 3, LIVE ON MERGE]

**Args:** `provision_worktree_config.sh <worktree-path> [--mode symlink|copy]`. Default mode from `config.yml worktree.provision_mode`, overridable by flag.

**Behavior (sketch — do NOT implement here):**
- Resolve repo root absolutely (`git rev-parse --show-toplevel` from the script's own dir, NOT cwd — doubled-cwd hazard cf. F5).
- Read `worktree.provision_config` from `config.yml` (default `[".env"]` if absent). YAML read: a small `node -e` parse OR a grep-based list extractor; reuse the meta-repo's existing yaml-read idiom if one exists (grep first).
- For each root: if `<repo-root>/<root>` exists and `<worktree-path>/<root>` does NOT:
  - symlink mode: `ln -s "<ABSOLUTE repo-root>/<root>" "<worktree-path>/<root>"` — **target MUST be absolute** (relative symlink breaks under the worktree's doubled depth, F5).
  - copy mode: `cp "<repo-root>/<root>" "<worktree-path>/<root>"`.
- Idempotent (skip if already provisioned). No-op (exit 0, INFO) if the source root is absent — provisioning a non-existent `.env` is not an error.
- Teardown is implicit: `git worktree remove` drops the worktree dir + the symlink inside it; assert NO dangling symlink at the REPO ROOT (the symlink lives INSIDE the worktree, pointing OUT — removing the worktree removes the link).

## 5. Test shape (QA-Red `*.red.*`) — OUTER shell assertions

Per CR-079 §4. Express as a bash test harness under `.cleargate/scripts/test/` (mirrors the existing `test_prep_qa_context.sh` pattern) OR a `cleargate-cli/test/scripts/cr079-provision.red.node.test.ts` that shells out. Recommend the bash harness (Class 3, no cli dependency). Assertions:
1. **Provision (symlink):** `git worktree add .worktrees/TEST-079 -b test/079 HEAD && bash .cleargate/scripts/provision_worktree_config.sh .worktrees/TEST-079` → `.worktrees/TEST-079/.env` exists AND `readlink .worktrees/TEST-079/.env` is the ABSOLUTE repo-root `.env` path.
2. **Scan PASS (exempted):** run `pre_gate_runner.sh` (arch mode) over the worktree path with the provisioned `.env` present → records `stray_env_files PASS` (NOT FAIL).
3. **Negative control:** drop a non-provisioned `.env.bak` into the worktree → scan still FAILs on it (exemption is the configured list, not a blanket `.env*` mute). NOTE: `.env.bak` must be in `stray_env_files` for this to fire — current list is `[.env, .env.local, .env.production]`. Either add `.env.bak` to the test's config fixture OR use `.env.local` (in the list, NOT provisioned) as the negative control. Recommend: negative control uses `.env.local` (already a scanned pattern, not in `provision_config` default) — cleaner than mutating the list.
4. **Teardown:** `git worktree remove .worktrees/TEST-079` → no dangling symlink at the repo root (`test ! -L ./.env` AND the original repo-root `.env` is untouched, still a real file).

> The `*.red.*` test must FAIL against the clean baseline: before the script exists, assertion 1 fails (no script → no `.env` provisioned); before the exemption branch, assertion 2 fails (scan FAILs on the provisioned `.env`).

## 6. Red-test naming (confirmed)
Same as CR-077: meta-repo stack is node:test, so if expressed as a node test the file is `cr079-provision.red.node.test.ts`; if a bash harness, it follows the meta-repo's `test_*.sh` script-test convention under `.cleargate/scripts/test/`. The parameterization CR-077 ships does not change how THIS repo names its own red tests.

## 7. Risks (top 3)
1. **Two-source drift between provisioning and exemption (MED).** If the provisioned list (`config.yml worktree.provision_config`) and the scan-exemption list diverge, a provisioned `.env` could still false-flag (F7 unfixed) OR a non-provisioned file could be wrongly exempted. MITIGATION: ONE source — both the script and the scan read `config.yml worktree.provision_config`. Avoid duplicating the list into `gate-checks.json` unless the YAML-from-bash read is genuinely impractical; if duplicated, the QA-Red test MUST assert the two lists match. Flag for Developer: prefer single-source.
2. **Symlink absolute-path / doubled-cwd (F5 class, MED).** A relative symlink target breaks under the worktree's nested depth. The script MUST use an absolute repo-root path resolved from `git rev-parse --show-toplevel` (NOT `$PWD`, NOT a `../..` relative). QA-Red assertion 1 verifies `readlink` is absolute. Copy mode sidesteps this but loses live-sync with source `.env`.
3. **POST-077 gate-checks.json baseline (LOW, but call it out).** CR-079's Developer opens a `gate-checks.json` that ALREADY contains CR-077's repo-derived command values (serial loop merged 077 first). CR-079 must ADD `arch.provisioned_config` (or read config.yml) and MUST NOT revert/re-touch `qa.typecheck`/`qa.test`/`arch.typecheck`. Rebase mentally onto the post-077 file; a stale mental model that re-adds `cd cleargate-cli` would regress F6.

## 8. DoD checklist (CR-079)
- [ ] `provision_worktree_config.sh` created (Class 3, byte-identical canonical + live); symlink-default/copy-opt-in; absolute-path-safe.
- [ ] `pre_gate_runner.sh` exempts provisioned roots before recording `stray_env_files FAIL` (Class 3, byte-identical canonical + live).
- [ ] `config.yml worktree.provision_config` + `provision_mode` added; documented in `config.example.yml`.
- [ ] SKILL §C.2 documents the provisioning step + key (Class 2 canonical).
- [ ] QA-Red bash harness: provision-symlink + scan-PASS + negative-control (`.env.local`) + teardown-no-dangling-symlink all pass.
- [ ] Single-source verified (provisioning list === exemption list) OR two-list-match asserted.
- [ ] POST-077 baseline preserved: `gate-checks.json` `qa.typecheck`/`qa.test`/`arch.typecheck` UNCHANGED from CR-077's values.
- [ ] **Gate-4 deferred:** live `/.claude/` re-sync (`npm run prebuild` → `cleargate init`) for the SKILL.md Class 2 edit. The Class 3 scripts (`provision_worktree_config.sh`, `pre_gate_runner.sh`, `config.yml`, `gate-checks.json`) go LIVE ON MERGE into `sprint/S-34` — verify they're fixture-tested before merge since this sprint's own later stories will use them. DevOps commits to the OUTER repo on `sprint/S-34`.

---

## Cross-story risks (M1)
1. **`gate-checks.json` shared, disjoint keys, serial-ordered.** CR-077 = `qa.typecheck`/`qa.test`/`arch.typecheck` (command values). CR-079 = `arch.provisioned_config` (or none, if reading config.yml) + leaves `arch.stray_env_files` intact. Under the serial loop CR-077 MERGES before CR-079 starts, so CR-079 edits the post-077 file — NO re-baseline race, but CR-079's Developer must NOT touch the typecheck/test keys (§7.3 above).
2. **Both go LIVE ON MERGE for this sprint's own gate runs (Class 3).** After CR-077 merges, the meta-repo's own subsequent stories (and CR-079's own QA gate) run against the NEW `gate-checks.json` command values — so CR-077's live-copy commands (`npm run typecheck/test --workspace=cleargate-cli`) MUST be correct, else this sprint's later stories false-FAIL their gate. After CR-079 merges, `provision_worktree_config.sh` + the stray-env exemption are live for any later worktree (M2/M3) — they must be fixture-verified before merge (CLEARGATE_SKIP_* break-glass is the only escape).
3. **Live `/.claude/` stays on the OLD agents/SKILL all sprint (Class 2).** CR-077's agent strip + CR-079's SKILL §C.2 edit are canonical-only until Gate-4. The orchestrator keeps running the OLD loaded loop — which is CORRECT for the meta-repo (node:test discipline + the existing worktree step). Mirror-parity tests (live vs canonical) go RED until Gate-4 — EXPECTED deferred drift, not a regression. Do not "fix" the live drift mid-sprint.
4. **Reuse — `read_config_field` (CR-079).** `arch.provisioned_config` (if used) reads via the EXISTING `read_config_field` helper (`.cleargate/scripts/pre_gate_common.sh:12`) — no new JSON parser. If reading `config.yml`, reuse any existing yaml-read idiom (grep first; do not author a parallel parser).
5. **Reuse — `config.yml gates.*` (CR-077).** The meta-repo's correct typecheck/test commands ALREADY exist at `config.yml:24-26` (`npm run typecheck/test --workspace=cleargate-cli`). CR-077's live `gate-checks.json` copy reuses those exact strings — do NOT invent new command spellings or revive `cd cleargate-cli`.

## Adjacent Implementations (Reuse — no duplication)
- `read_config_field` from `.cleargate/scripts/pre_gate_common.sh:12` — JSON dot-path reader; CR-079 exemption reads through it (no new parser).
- `record_result` / `write_report_header` from `.cleargate/scripts/pre_gate_common.sh` — CR-079's PASS/FAIL recording reuses these (do not hand-roll).
- `config.yml gates.{typecheck,test}` (`.cleargate/config.yml:24-26`) — the meta-repo's already-correct command strings; CR-077's live gate-checks copy reuses verbatim.
- The `test_prep_qa_context.sh` bash-test-harness pattern under `.cleargate/scripts/test/` — CR-079's §4 verification reuses this harness shape (mktemp fixture + worktree + assertions).
- `copy-payload.ts:54` (SKIP_FILES) / `:122` (copy loop) — CR-077's detector hooks in AFTER this copy, reusing the existing init payload-copy flow rather than re-authoring it.

## Open decisions for orchestrator
1. **CR-079 exemption source — `config.yml` single-key vs a new `gate-checks.json arch.provisioned_config`.** Architect RECOMMENDS single-source via `config.yml worktree.provision_config` (one list, two readers) to eliminate the two-source drift risk (§7.1). The CR §3 left this open ("a new `config.yml` key, or a fixed convention" was answered as config.yml; but the existing scan reads `gate-checks.json`). This is a within-scope implementation choice — Architect's recommendation stands UNLESS the orchestrator directs otherwise; not a blocker. Surfaced for visibility, not sign-off.
2. **None blocking.** Both CRs are 🟢 (owner accepted all §0.5 recommendations 2026-06-03). The §4 eviction-grep over-breadth (CR-077) is reconciled within the CR's own §3 scope (§7.2) — NOT escalated. No true-blocker per the five-case rule.

---

## Footer
- **Protocol §-numbering:** Audited — current max protocol section is `## 23. Parallel-Wave Execution Contract` (max = 23). Neither M1 CR appends a `cleargate-protocol.md` section, so no §(max+1) emission and no stale-§ rewrite is needed. No story prose cites a `§N` protocol reference requiring audit.
- **CR-037 npm version check:** N/A — neither CR-077 nor CR-079 adds any npm dependency (CR-077 detector + CR-079 provisioning script use only Node stdlib / bash / git). No `npm view` check required.
- **Flashcards consulted:** grepped `#test-harness #gate #worktree #section-numbering #schema` — relevant hits noted (gate-block count guards, evalSection positional, awk range self-termination); none alters M1 since neither CR adds a readiness-gate block or a positional gate section. `#section-numbering` card (close_sprint Step 2.5 stale label) is unrelated to M1 scope.
- **Lane:** Both CR-077 (high bounce) and CR-079 (med bounce) are `standard` — confirmed: CR-077 touches forbidden surface class (agent payload + gate config) and is multi-file; CR-079 touches worktree/gate-scan mechanics. Neither is fast-eligible (CR-080 is the only M1 fast-lane, planned separately).

### M3.md

role: architect

# Milestone: M3 — QA-loop hardening + deferred-verification close gate
## Stories: CR-081, CR-082
## Wave: W3 (STRICTLY SERIAL — both edit qa.md)

## Order

**STRICT: CR-081 → CR-082.** Both edit `cleargate-planning/.claude/agents/qa.md`. The serial loop merges CR-081 fully (QA-Red → Dev → QA-Verify → merge) before CR-082's Developer opens the file, so CR-082's worktree branches from the POST-081 qa.md. **Not parallelizable.** Beyond the shared qa.md, the CRs are otherwise file-disjoint (CR-081 owns SKILL.md/architect.md/qa_red_lint.mjs/pre_gate_runner.sh; CR-082 owns close_sprint.mjs/story.md + its node:test).

**Live-on-merge sequencing consequence (critical):** the moment CR-081 merges, `qa_red_lint.mjs` is wired into `pre_gate_runner.sh` and goes LIVE (Class 3). The orchestrator's OWN pre-gate scans for CR-082's story (the §C.3.5 TPV scan and §C.6 post-flight scan) will invoke `qa_red_lint` against CR-082's `*.red.node.test.ts`. Therefore qa_red_lint MUST clean-pass CR-082's own close-gate node test (no Pydantic `Literal`, no `queryByText` → no R-enum/R-query match → exit 0). This is a hard, plan-level requirement, not advisory — a false-flag here would self-block the milestone. See CR-081 Risk #1.

---

## Per-story blueprint

### CR-081 — semantic QA-Red fixture lint + accept red-now-green as the green test

- **Goal linkage:** Stops two SPRINT-66 fixture-class defects (invalid enum literal; duplicate-text query) from burning a full Developer dispatch, and stops QA-Verify false-FAILing a red-now-green test — directly serving "run a sprint end-to-end without orchestrator hand-fixes."

- **Cross-story coupling:** Shares ONLY `qa.md` with CR-082. CR-081 edits qa.md's **Mode VERIFY / acceptance-trace + coverage-check** region (the *meaning* of "green test satisfied"). CR-082 edits qa.md's **verdict-vocabulary / output-shape** region (`QA: PASS | FAIL` line). These are DISJOINT line regions (see Cross-story risks) — serial merge required for clean rebase, but low collision. The composed verdict semantics are spelled out in Cross-story risks.

- **Files to touch (Class-tagged, with edit region in CURRENT post-077 files):**

  | File | Class | Edit region (current line nums) | Edit |
  |---|---|---|---|
  | `cleargate-planning/.claude/agents/qa.md` | **Class 2** (canonical-live-deferred) | Mode RED bullet 6 `:47` (wiring-soundness note); Workflow step 4 `:131-133`; coverage-check is in Lane playbook `:110` (`runtime` lane "every Gherkin scenario has a passing test") + Workflow `:131-133` | Add the **red-now-green clause** to Mode VERIFY acceptance-trace (Workflow step 4, `:131-133`) and mirror a one-liner where coverage is asserted. New clause text below in Schema/contract deltas. |
  | `cleargate-planning/.claude/agents/architect.md` | **Class 2** | Mode: TPV `:108-126`, after `:118` ("You DO NOT verify test logic correctness") | Add ONE note: "A separate pre-gate `qa_red_lint` (CR-081) catches semantic fixture bugs; TPV stays wiring-only — do not fold semantic checks into TPV." No behavior change to TPV. |
  | `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` | **Class 2** | §C.3.5 `:304-339`, between the scan invocation `:308-312` and the conditional-dispatch block `:314` | Add the `qa_red_lint` step: it runs as part of `pre_gate_runner.sh arch`; a lint flag routes back to QA-Red via `arch_bounces` (`:333-337` pattern), identical to a TPV wiring gap. Document that the lint is a pre-gate check, not a separate dispatch. |
  | `cleargate-planning/.cleargate/scripts/qa_red_lint.mjs` | **Class 3** (tracked-live-on-merge) **NEW** | new file | R-enum + R-query rule engine (see Schema/contract deltas). Pure pattern-match, no test execution. |
  | `cleargate-planning/.cleargate/scripts/pre_gate_runner.sh` | **Class 3** | `run_arch()` `:204-315`, add as **check #5** after the file-count block `:302-314`; wire flag into `OVERALL_EXIT` | Invoke `qa_red_lint.mjs` against the worktree's QA-Red test files; on flag set `OVERALL_EXIT=1` + `record_result "$REPORT_FILE" "qa_red_lint" "FAIL" ...`; on clean `record_result ... "PASS"`. Gate behind `read_config_field "arch.qa_red_lint" "$CONFIG_FILE"` (config default `true`). |

  **Class 3 mirror obligation:** every edit to `.cleargate/scripts/qa_red_lint.mjs` and `.cleargate/scripts/pre_gate_runner.sh` MUST be byte-identical in BOTH the canonical `cleargate-planning/.cleargate/scripts/` AND the live `.cleargate/scripts/` (the live copy goes active for THIS sprint on merge). The Developer writes both; QA-Verify diffs them (`diff -q`). The `arch.qa_red_lint` key must be added to BOTH `gate-checks.json` copies (live `.cleargate/scripts/gate-checks.json` + the `cleargate-planning` mirror).

  > **Note — live `.cleargate/scripts/` IS the one the orchestrator runs.** Unlike Class-2 `.md` (orchestrator runs OLD agents all sprint, re-sync deferred to Gate-4), the live `.cleargate/scripts/pre_gate_runner.sh` is invoked by the orchestrator's own §C.3.5/§C.6 scans the moment CR-081 merges. Class-3 = live-on-merge.

- **Schema / contract deltas:**

  **`qa_red_lint.mjs` rule signatures** (structured for growth — R-enum + R-query first ship):
  - **Invocation:** `node .cleargate/scripts/qa_red_lint.mjs <worktree-or-fixture-dir>`. Globs QA-Red test files under the dir (red-test naming per `sprint_context.md` §Test Stack; meta-repo default `*.red.node.test.ts`; also accept `*.red.*` test-file forms for polyglot targets). Exit 0 = clean; exit non-zero = ≥1 rule flagged, with a per-flag message to stderr (file:line + rule-id + recommended fix).
  - **R-enum (constructed-fixture enum/Literal validity):** when a fixture constructs a typed model with a literal/enum field whose argument is a string/enum literal, and the surrounding source declares the allowed set (e.g. `Literal["technical-dark","business-warm","minimal-light"]` or a TS string-union / enum), flag if the literal is NOT a member of the declared set. Catches `DeckSpec(theme="dark")`. Pattern-matchable: scan for `Literal[...]` / union-type / enum declarations in scope, then for constructor-call kwargs/props whose value is a bare string literal not in the set. Conservative: only flag when BOTH the declared set AND the out-of-set literal are statically visible (no flag when the set can't be resolved — avoids false positives).
  - **R-query (query-by-text single-match):** flag `queryByText(<s>)` / `getByText(<s>)` whose target string `<s>` is duplicated across ≥2 fixture rows rendered in the same render block. Recommend `queryAllByText(<s>)[0]` / `getByTestId(...)`. Pattern-matchable: collect literal strings passed to `getByText`/`queryByText`, cross-check against the same string appearing ≥2× in the fixture data / render input within the test body.
  - **Growth contract:** rule registry is a list of `{id, description, scan(fileText) -> [{line, message}]}`; adding R-N later appends to the registry. Ship R-enum + R-query ONLY; do not gold-plate.
  - **No-false-flag guarantee:** a test file with no `Literal[...]`/union-with-out-of-set-literal AND no duplicate-string `getByText`/`queryByText` → zero flags → exit 0. This is what protects CR-082's close-gate node test (plain node:test, `assert`-based, no Pydantic/RTL patterns).

  **`qa.md` red-now-green clause** (Class-2 canonical text to add at Workflow step 4 `:131-133`, mirrored at the coverage assertion):
  > "A QA-Red test file that was failing at baseline and now PASSES against the Developer's commit fully satisfies the green-test / acceptance-coverage requirement. Do NOT require a separate green-path file distinct from the now-passing red file — a duplicate is redundant under reuse-over-recreate. Map each Gherkin scenario to the now-passing red test; absence of a second file is NOT a MISSING entry and must not produce a FAIL."

  **`architect.md` TPV note** (Class-2, after `:118`):
  > "Semantic fixture correctness (invalid enum literals, unsatisfiable duplicate-text queries) is caught by the separate `qa_red_lint` pre-gate step (CR-081), NOT by TPV. TPV stays wiring-only — never fold semantic-correctness checks here."

- **Test scenarios (from §4 Verification Protocol — QA-Red writes these as `*.red.*`):**
  1. **R-enum positive:** a regression fixture mirroring `DeckSpec(theme="dark")` against `Literal["technical-dark","business-warm","minimal-light"]` → `qa_red_lint.mjs <dir>` exits non-zero with an R-enum message naming the bad literal.
  2. **R-query positive:** a render fixture where the same text appears on two rows under a `queryByText`/`getByText` call → exits non-zero, R-query message recommends `queryAllByText(...)[0]`/`getByTestId(...)`.
  3. **Negative (no false positive):** a fixture with a valid literal + a uniquely-matched query → exit 0.
  4. **Negative — non-applicable file (anti-false-flag):** a plain node:test file (assert-based, no Literal/queryByText — i.e. shaped like CR-082's own close-gate test) → exit 0. This scenario directly proves the live-on-merge safety property.
  5. **Wiring grep:** `grep -n "qa_red_lint" .cleargate/scripts/pre_gate_runner.sh` returns a hit inside `run_arch()`; the flag path sets `OVERALL_EXIT=1` (routes back via `arch_bounces`, mirroring SKILL.md `:333-337`).
  6. **qa.md red-now-green grep:** `grep -nE "red-now-green|now PASSES|failing at baseline" cleargate-planning/.claude/agents/qa.md` returns the new clause.

  **Test location + runner:** a bash harness `cleargate-planning/.cleargate/scripts/test/test_qa_red_lint.red.sh` (mirrored live at `.cleargate/scripts/test/test_qa_red_lint.red.sh`) — consistent with the existing pre-gate test convention (`test_pre_gate_lane_aware.sh`, `cr079_provision.red.sh`) and the fact qa_red_lint is invoked via bash from the runner. The harness creates `mktemp -d` fixtures for scenarios 1-4, runs `node qa_red_lint.mjs <fixture>`, asserts exit codes + grep of stderr messages; scenarios 5-6 are grep assertions. RED state: the harness fails until `qa_red_lint.mjs` exists and the runner wiring + qa.md clause land. **Do NOT run via the degraded cli npm-test suite** — this harness is standalone `bash`.

  > Naming: meta-repo §Test Stack is unfilled in sprint-context.md → default applies. For this CR the proven local form is bash (`*.red.sh`, matching `cr079_provision.red.sh`). If the orchestrator fills §Test Stack with node:test, an equivalent `*.red.node.test.ts` wrapper that shells out to qa_red_lint is acceptable — but the bash harness is the recommended path here.

- **Reuse (no duplication):**
  - `record_result`, `read_config_field`, `print_summary` from `.cleargate/scripts/pre_gate_common.sh:48/12/60` — use these in the runner wiring; do NOT inline new report-writing.
  - `OVERALL_EXIT` aggregator (already in `pre_gate_runner.sh:73`, set `=1` at `:90/:125/:148/:194/:215/:245/:297`) — set `OVERALL_EXIT=1` on qa_red_lint flag; do NOT invent a parallel exit variable. The existing tail (`:362-399`) already routes `OVERALL_EXIT!=0` to QA via `arch_bounces` for standard lane — no new routing code needed.
  - The `arch.<field>` config-read pattern (`read_config_field "arch.new_deps_check"` at `:223`) — gate qa_red_lint behind `arch.qa_red_lint` the same way.

- **Gotchas surfaced from code inspection (file:line citations):**
  - `pre_gate_runner.sh:333` summary uses `grep -c '^\[PASS\]' ... || echo 0` (in `print_summary`, `pre_gate_common.sh:64-66`) — the **exact** double-count hazard carded 2026-06-03 (`#test-harness #bash`: `grep -c` prints 0 AND exits 1, so `|| echo 0` appends → `"0\n0"`). The qa_red_lint flag check in `run_arch()` must NOT use `X=$(grep -c ... || echo 0)` for any count it then numerically compares — use `grep -q` or `grep -c | head -1`.
  - `pre_gate_runner.sh:210` (`run_arch` typecheck) runs `cd "$WORKTREE" && eval ... > /dev/null 2>&1` UN-subshelled — the cwd-leak carded 2026-06-03 (`#pre-gate #cwd-leak`). The qa_red_lint invocation in `run_arch()` MUST pass an ABSOLUTE path to `qa_red_lint.mjs` and an absolute worktree-dir arg (`WORKTREE` is already abspath-normalized at `:47`), and must NOT leave the script `cd`'d into a subdir. Prefer `node "${SCRIPT_DIR}/qa_red_lint.mjs" "${WORKTREE}"` with no `cd`.
  - `pre_gate_runner.sh:47` normalizes `WORKTREE` to absolute — rely on this; pass `"${WORKTREE}"` directly to the lint.
  - qa.md current `:143` still reads `QA: PASS | FAIL` (post-077 did NOT parameterize the verdict line — 077 only touched red-test naming at `:43/:47/:52`). CR-081 does NOT edit `:143` (that's CR-082's region). CR-081 stays in the Workflow/coverage region `:110/:131-133`.

---

### CR-082 — track deferred heavy verifications + enforce at close gate

- **Goal linkage:** Prevents a story reaching Done/merged on static-scope QA while its real heavy acceptance (e.g. a ~500-700MB docker build smoke) is deferred-and-unrun — the F11 defect where a broken Dockerfile rode the sprint branch. Adds a deterministic close gate so deferred verifications are enforced before human ack.

- **Cross-story coupling:** Shares ONLY `qa.md` with CR-081 (DISJOINT region — CR-082 edits the output-shape/verdict-vocabulary block `:140-152`; CR-081 edits the Workflow/coverage block `:110/:131-133`). CR-082's Developer opens the POST-081 qa.md (serial). The semantic composition of the two verdict rules is the load-bearing cross-story risk — see Cross-story risks.

- **Files to touch (Class-tagged, with edit region in CURRENT files):**

  | File | Class | Edit region (current line nums) | Edit |
  |---|---|---|---|
  | `.cleargate/templates/story.md` (+ `cleargate-planning/.cleargate/templates/story.md` mirror) | **Class 3** | frontmatter block `:57-95`, add as sibling to `db_write_set:` (`:70`) | Add optional `deferred_verification:` list field + one-line doc comment (shape below). Backward-compatible: absent/empty = no obligation. |
  | `.claude/agents/qa.md` → canonical `cleargate-planning/.claude/agents/qa.md` | **Class 2** | Output shape `:140-152` (`QA: PASS | FAIL` at `:143`) + Guardrails | Add `PASS-PENDING-SMOKE` to the verdict line; add the rule "a deferred-acceptance scenario (declared `deferred_verification`, unrun) yields `PASS-PENDING-SMOKE`, NOT `PASS`." |
  | `.cleargate/scripts/close_sprint.mjs` (+ `cleargate-planning/.cleargate/scripts/close_sprint.mjs` mirror) | **Class 3** | NEW **Step 2.9**, inserted after Step 2.8 closes (`:701`) and before Step 3 (`:703`); env-seam docblock added near `:41-53` | Add the deferred-verification close gate + env seams (below). Models on Step 2.7 (`:568-626`) / 2.8 (`:628-701`). |
  | `gate-checks.json` (both copies) | — | n/a for 082 | (no change for 082) |

  **Class 3 mirror obligation:** `close_sprint.mjs` + `story.md` edits MUST be byte-identical in canonical `cleargate-planning/.cleargate/...` AND live `.cleargate/...`. The live `close_sprint.mjs` goes active on merge and runs at SPRINT-34's OWN Gate-4 close — see the CRITICAL section below.

- **Schema / contract deltas:**

  **`deferred_verification:` story.md frontmatter** (sibling to `db_write_set:` at `:70`):
  ```yaml
  deferred_verification: []  # optional. List of heavy checks the story owes, run post-merge.
                             # Each: { description: <human string>, command: <shell, run from repo root>, blocks: close }
                             # blocks defaults to `close` (gate sprint-close); `merge` is the stricter opt-in.
                             # Absent/empty = no obligation (the common case; fully backward-compatible).
  ```

  **Deferred-verify RESULT artifact** `.cleargate/sprint-runs/<id>/deferred-verify/<STORY-ID>.json` (written at run-time by whoever runs the command — orchestrator or a `cleargate verify deferred` wrapper; NOT a tracked source file):
  ```json
  { "story": "STORY-NNN-NN", "command": "<shell>", "ran_at": "<ISO-8601>", "exit_code": 0, "status": "green" }
  ```
  `status ∈ green | red | unrun`. Gate reads `status === "green"` (and `exit_code === 0`) as satisfied.

  **`close_sprint.mjs` Step 2.9 — Deferred-Verification Close Gate** (insert at `:701`→`:703`):
  - **Resolve story files:** scan `delivery/pending-sync/` + `delivery/archive/` for files whose frontmatter `sprint_cleargate_id` matches this sprint (reuse the frontmatter-read helper pattern already in close_sprint.mjs — `setFrontmatterStatusAtomic`/`parseFrontmatter` at `:450-453`; cite `delivery` resolution at `:478/:536`). For each, read `deferred_verification:` (default `[]` if absent).
  - **No-op rule (BACKWARD-COMPAT — load-bearing):** if NO story declares any `deferred_verification` entry, the gate prints `Step 2.9 passed: no deferred verifications declared.` and continues — a SILENT NO-OP. SPRINT-34's own stories (CR-077..082, CR-075/076) declare ZERO such fields → SPRINT-34's close stays green.
  - **Enforce rule:** for each declared entry with `blocks: close`, require a matching `deferred-verify/<STORY-ID>.json` with `status === "green"`. Missing file OR `status !== "green"` OR `exit_code !== 0` → print the failing story+command and `process.exit(1)` (block close), exactly as Step 2.7 (`:623`) / Step 2.8 (`:697`) do.
  - **Env test seams** (mirror `:41-52` docblock + Step 2.7/2.8 implementation):
    - `CLEARGATE_SKIP_DEFERRED_VERIFY_CHECK=1` — bypass Step 2.9 entirely (mirrors `CLEARGATE_SKIP_WORKTREE_CHECK` `:576`).
    - `CLEARGATE_FORCE_DEFERRED_VERIFY=<json>` — inject the declared-entries + result map without reading real story files or result artifacts (mirrors `CLEARGATE_FORCE_WORKTREE_PATHS` `:582`). Shape: a JSON map `{ "<STORY-ID>": { declared: [{command,blocks}], result: "green"|"red"|"unrun"|null } }`. Lets the node:test exercise FAIL / PASS / no-op paths without heavy runs.
    - Reuse `CLEARGATE_SPRINT_DIR` (`:170`) and `CLEARGATE_REPO_ROOT` (`:101`) for path overrides — do NOT add new path-override seams.
  - **Fail-open on unreadable state:** if story-file scan throws (delivery dir absent in a tmpdir test), behave like Step 2.7's `worktreeListAvailable=false` (`:611`) — print `Step 2.9 skipped: ... unavailable (non-fatal).` and continue. Never crash the close pipeline.

  **`qa.md` PASS-PENDING-SMOKE verdict** (Class-2, at `:143` + a Guardrails rule):
  - Output line becomes: `QA: PASS | PASS-PENDING-SMOKE | FAIL`.
  - Rule: "If the story declares a `deferred_verification` whose heavy acceptance scenario was NOT run in this QA pass (out of static scope), and every OTHER scenario passes, return `PASS-PENDING-SMOKE` (story merges to the sprint branch but is NOT fully Done; close gate enforces the deferred check). A normal red-now-green or fully-covered story returns full `PASS`."

- **Test scenarios (from §4 — QA-Red writes the close-gate node:test):**
  1. **declared + NO result file → FAIL:** a sprint fixture with a story declaring `deferred_verification` and no matching green result → Step 2.9 exits non-zero / halts.
  2. **declared + green result → PASS:** same with a `status: green` result injected → gate passes.
  3. **none declared → silent no-op:** a sprint with zero `deferred_verification` declarations → gate prints the no-op line, continues, close stays green (THE SPRINT-34-own-close case).
  4. **declared + red/unrun result → FAIL:** result present but `status: red` (or `unrun`) → gate halts.
  5. **`qa.md` grep:** `grep -n "PASS-PENDING-SMOKE" cleargate-planning/.claude/agents/qa.md` returns the verdict in the output-shape section.
  6. **`story.md` grep:** `grep -n "deferred_verification" .cleargate/templates/story.md` returns the new field.

  **Test location + runner:** `.cleargate/scripts/close_sprint.deferred-verify.red.node.test.ts` (canonical mirror `cleargate-planning/.cleargate/scripts/close_sprint.deferred-verify.red.node.test.ts`), run via `node --test --import tsx/esm <file>` — EXACTLY as CR-082 §4 mandates. It uses the `CLEARGATE_FORCE_DEFERRED_VERIFY` + `CLEARGATE_SPRINT_DIR` + `CLEARGATE_SKIP_WORKTREE_CHECK=1`/`CLEARGATE_SKIP_MERGE_CHECK=1` seams to drive scenarios 1-4 against `close_sprint.mjs` without real git/heavy runs (mirror how the existing close-pipeline tests inject state). Scenarios 5-6 are grep assertions (may live in the same node:test via `child_process` or a sibling bash assertion). RED state: fails until Step 2.9 + the seams land. **Standalone node:test — NOT the cli npm-test suite.**

  > This is the file qa_red_lint scans during CR-082's OWN pre-gate (CR-081 is already live). It contains only `node:test` + `assert` + env-seam strings — no Pydantic `Literal`, no `queryByText` → qa_red_lint MUST exit 0 on it (CR-081 scenario 4 proves this property; verify it holds here).

- **Reuse (no duplication):**
  - Step 2.7 (`close_sprint.mjs:568-626`) + Step 2.8 (`:628-701`) are the block-on-state-with-env-seam TEMPLATE — Step 2.9 is cut from this mold (same `process.exit(1)` on block, same `CLEARGATE_SKIP_*` / `CLEARGATE_FORCE_*` seam pair). Do NOT author a new gate framework.
  - Frontmatter read: reuse the existing `parseFrontmatter`/`setFrontmatterStatusAtomic` path (`:450-453`) and delivery-dir resolution (`:478/:536/:897-899`); do NOT add a new YAML parser.
  - `CLEARGATE_SPRINT_DIR` (`:170`) + `CLEARGATE_REPO_ROOT` (`:101`) for path overrides — already exist, reuse.

- **Gotchas surfaced from code inspection (file:line citations):**
  - **Step 2.9 placement is load-bearing:** insert AFTER Step 2.8's closing brace (`close_sprint.mjs:701`) and BEFORE `Step 3: prefill_report.mjs` (`:703`). Placing it before Step 2.7/2.8 would let the deferred gate run before the worktree/merge gates — wrong order; the deferred gate is the LAST pre-report gate.
  - close_sprint.mjs's flashcarded WS8(e) dist assertion (carded 2026-06-01 `#close-pipeline`) fires only when dist absent — unrelated to Step 2.9 but confirms close gates `process.exit(1)` on block and clean-no-op on healthy state; Step 2.9 follows the same convention.
  - `sprintId` is `args[0]` (`:166`); `sprintDir` resolves via `CLEARGATE_SPRINT_DIR` else `sprint-runs/<sprintId>` (`:170-172`). The `deferred-verify/` result dir is `path.join(sprintDir, 'deferred-verify')` — reuse `sprintDir`, do not re-derive.
  - The env-seam docblock at `:41-53` is the canonical place to document the two new seams — add them there alongside `CLEARGATE_SKIP_MERGE_CHECK` for discoverability (the close-pipeline test greps this block).

---

## Cross-story risks

1. **qa.md serial-merge collision (LOW but real).** CR-081 edits the Workflow/coverage region (`:110`, `:131-133`); CR-082 edits the output-shape/verdict region (`:140-152`). DISJOINT line regions → a clean serial rebase. BUT the Developer for CR-082 MUST branch from the POST-081 qa.md (the serial loop guarantees this). If somehow CR-082's worktree predates the 081 merge, the verdict-line edit could land on a stale file and drop 081's coverage clause. **Mitigation: enforce CR-081 merged-to-sprint-branch before CR-082's worktree is cut** (the serial loop already does this; the orchestrator must not parallelize).

2. **Composed verdict semantics (THE load-bearing semantic interaction — 081 × 082 MUST compose coherently).** CR-081 says "a red test turned green = full PASS." CR-082 adds "a story with an unrun declared `deferred_verification` = PASS-PENDING-SMOKE, not PASS." These compose as a DECISION ORDER, which the composed qa.md MUST state explicitly:
   - **(a)** Does the story declare a `deferred_verification` entry that was NOT run in this QA pass? → `PASS-PENDING-SMOKE` (082 wins; the deferred heavy check is still owed; close gate enforces it).
   - **(b)** Else, are all Gherkin scenarios covered by passing tests — INCLUDING red-now-green tests counting as their own green (081)? → full `PASS`.
   - **(c)** Else → `FAIL`.
   The key non-contradiction: 081's "red-now-green = PASS" applies to the STATIC scenarios; 082's PASS-PENDING-SMOKE is gated on an UNRUN DEFERRED scenario, a different axis. A story can be red-now-green on its static scenarios AND still be PASS-PENDING-SMOKE because a separate heavy scenario is deferred. **CR-082's Developer MUST write the verdict region so the deferred-check (082, step a) is evaluated BEFORE the full-PASS branch (081, step b)** — i.e. PASS-PENDING-SMOKE shadows PASS when a deferred entry is unrun. The composed output line: `QA: PASS | PASS-PENDING-SMOKE | FAIL`. A Developer working only on 082 might overwrite 081's red-now-green clause when editing the nearby verdict line — flag: the two clauses live in adjacent-but-distinct subsections; preserve 081's Workflow-step-4 clause verbatim.

3. **Live-on-merge self-block (qa_red_lint × CR-082's own tests).** Once CR-081 merges, qa_red_lint runs in the orchestrator's pre-gate for CR-082's story. CR-082's `*.red.node.test.ts` must NOT trip R-enum/R-query (it won't — no Literal, no queryByText). **Mitigation: CR-081 scenario 4 (negative non-applicable file) explicitly proves exit 0 on a plain node:test; the Developer must include a fixture shaped like CR-082's test in that scenario.** If qa_red_lint were to false-flag, CR-082's story would route back to QA-Red on a phantom wiring gap and stall the milestone.

4. **CR-082 close gate blocking SPRINT-34's OWN close (sprint-ending if mishandled).** Covered in the CRITICAL section below — the no-op-when-none-declared rule is the entire mitigation.

---

## ⚠️ CRITICAL — CR-082 close gate is LIVE at SPRINT-34's own Gate-4

`close_sprint.mjs` is Class 3 (live-on-merge). Once CR-082 merges, its Step 2.9 deferred-verification gate is ACTIVE for SPRINT-34's own close. SPRINT-34's stories (CR-077..082, CR-075/076) declare ZERO `deferred_verification` fields. The plan REQUIRES, non-negotiably:

- **(a) Frontmatter scan + silent no-op:** Step 2.9 scans every SPRINT-34 story's frontmatter for `deferred_verification:`. With none declared, it prints `Step 2.9 passed: no deferred verifications declared.` and continues. NO block, NO friction.
- **(b) Env test seam:** `CLEARGATE_FORCE_DEFERRED_VERIFY` + `CLEARGATE_SKIP_DEFERRED_VERIFY_CHECK` make the gate testable WITHOUT real heavy runs (mirroring `CLEARGATE_FORCE_WORKTREE_PATHS` `:582` / `CLEARGATE_SKIP_WORKTREE_CHECK` `:576`). The node:test drives FAIL/PASS/no-op via the force seam.
- **(c) Explicit no-deferred verification:** the close-gate node:test's scenario 3 (none-declared → silent no-op) is the regression that proves SPRINT-34's own close stays green. After CR-082 merges, the orchestrator MUST re-verify SPRINT-34 close is green (dry-run `close_sprint.mjs` Step 2.9 path with no force seam → no-op).

A gate that blocked SPRINT-34's own close on a phantom deferred verification would be a sprint-ending defect. The no-op-when-none-declared rule (CR §1 "Absent/empty = no obligation, fully backward-compatible") is the binding mitigation.

---

## Open decisions for orchestrator

1. **§Test Stack is unfilled in `sprint-context.md`.** The meta-repo default (node:test, `*.red.node.test.ts`) applies. CR-081's lint test is recommended as a bash harness (`test_qa_red_lint.red.sh`, matching the existing pre-gate test convention); CR-082's close-gate test is node:test per the CR's explicit §4 mandate. If the orchestrator wants a uniform stack, fill §Test Stack — but the recommendation above is internally consistent and matches both CRs' verification commands. No blocker.
2. **`cleargate verify deferred <story>` wrapper.** CR-082 mentions an OPTIONAL wrapper to write the result artifact. NOT in scope for M3 (the gate only READS the artifact; the orchestrator writes it manually or via the wrapper as a follow-up). The gate ships; the wrapper is deferred. Confirm this scoping or flag a CR.
3. **`gate-checks.json` `arch.qa_red_lint` default value.** Plan specifies default `true` (lint runs by default in every init'd target). Confirm — a target could opt out via `false`. (Recommend `true`; the whole point is to ship it stock.)

---

## DoD (per CR)

**CR-081 DoD:**
- [ ] `qa_red_lint.mjs` created (canonical + live mirror, byte-identical); R-enum + R-query rules pass scenarios 1-4.
- [ ] `pre_gate_runner.sh` `run_arch()` invokes qa_red_lint, sets `OVERALL_EXIT=1` on flag, uses `record_result` (canonical + live mirror); `arch.qa_red_lint` key added to both `gate-checks.json` copies.
- [ ] qa.md red-now-green clause + architect.md TPV-stays-wiring note (canonical Class-2 edits).
- [ ] SKILL.md §C.3.5 documents the qa_red_lint step.
- [ ] Bash harness `test_qa_red_lint.red.sh` green (canonical + live).
- [ ] `git diff --name-only` lists only the §3 scaffold paths (no `mcp/`/`admin/`/DB).
- [ ] **Gate-4 deferred (Class 2):** live `/.claude/**` re-sync for qa.md + architect.md + SKILL.md via `cleargate init`/hand-port; `npm run prebuild` mirrors canonical into `cleargate-cli/templates/`. Mirror-parity tests RED until Gate-4 = EXPECTED (Class-2 deferred re-sync).

**CR-082 DoD:**
- [ ] `deferred_verification:` field added to story.md (canonical + live mirror).
- [ ] `PASS-PENDING-SMOKE` verdict + deferred→pending rule in qa.md (canonical Class-2), composed coherently with CR-081's red-now-green clause per Cross-story risk #2.
- [ ] `close_sprint.mjs` Step 2.9 gate + env seams (canonical + live mirror, byte-identical); silent no-op when none declared.
- [ ] `close_sprint.deferred-verify.red.node.test.ts` green: declared+no-result→FAIL; declared+green→PASS; none-declared→no-op; declared+red→FAIL.
- [ ] qa_red_lint (now LIVE from CR-081) exits 0 on CR-082's own `*.red.node.test.ts` (no R-enum/R-query match) — verified, no phantom wiring gap.
- [ ] **SPRINT-34's own close re-verified green AFTER CR-082 merges** (Step 2.9 no-op path, no force seam). This is mandatory — close_sprint.mjs is now live.
- [ ] **Gate-4 deferred (Class 2):** live `/.claude/**` re-sync for qa.md via `cleargate init`/hand-port; `npm run prebuild` mirrors canonical.

---

## Adjacent Implementation Check

Grepped current sprint surfaces. No prior-merged SPRINT-34 story exports a reusable module CR-081/082 would duplicate — they extend stock helpers (`pre_gate_common.sh` `record_result`/`read_config_field`/`print_summary`; close_sprint.mjs Step 2.7/2.8 gate mold; `OVERALL_EXIT` aggregator). All citations are to pre-existing infrastructure, listed as Reuse above. CR-077 (already merged) parameterized red-test naming to `sprint_context.md §Test Stack` — both CRs honor this (qa.md edits do NOT reintroduce a hardcoded `*.red.node.test.ts` literal in the parameterized regions `:43/:47/:52`; the new CR-081/082 clauses reference the §Test Stack convention or the generic red-test concept).

---

## Pre-Spec Dep Version Check (CR-037)

No new dependencies in M3. CR-081/082 add zero packages (qa_red_lint.mjs uses node builtins `fs`/`path`/`process`; the node:test uses `node:test` + `node:assert` + `tsx/esm`, already present). `npm view` not invoked — no packages to check. Version check N/A (zero new deps).

### M4.md

role: architect

# Milestone: M4 — Carried EPIC-043 cli hygiene CRs
## Stories: CR-075 (cli standalone test suite), CR-076 (trim published npm package)
## Wave: W4 — SERIAL (Phase C loop). CR-075 lands first, CR-076 second. Ordering is REQUIRED (shared `npm pack` assertion in changelog-format).

> Sprint goal (sprint-context.md): "ship the two carried hygiene CRs" alongside EPIC-045 portability. M4 is independent of M1–M3 EPIC-045 surfaces — both CRs live ENTIRELY in `cleargate-cli/`.
> Both CRs `lane: standard` in `state.json` (`migration-default`). Neither is fast-eligible (CR-075 touches the test-harness config = test-runner surface; CR-076 touches `package.json`+`tsup.config.ts` = build/publish surface). Standard lane stays.

---

## Order

1. **CR-075 FIRST** — establishes the tiered `npm test` / `test:integration` split, removes both `--workspace=cleargate-cli` shells (changelog-format + license-contract), adds `.nvmrc`, settles the ratchet question. This LANDS the standalone/in-package `npm pack` form of the changelog assertion.
2. **CR-076 SECOND** — config surgery on the SAME tarball that CR-075's changelog assertion now inspects in-package; CR-076 re-tightens that one assertion for the trimmed (no-`*.map`, single-payload) manifest.
3. **NOT parallelizable.** Both edit `test/changelog-format.node.test.ts` line ~167-173. Serial merge to cli `main` keeps the assertion edit clean (see §"changelog-format coordination").

---

## Cross-repo mechanism (authoritative — both CRs are cli-only)

`cleargate-cli/` is a SEPARATE gitignored git repo at `<root>/cleargate-cli/` (currently `main`, ahead 3 = M1–M3 EPIC-045 stories).

- **Branch in the MAIN cli checkout — NOT a worktree:** `git -C cleargate-cli checkout -b story/CR-075 main` (then `story/CR-076` off the post-CR-075 `main`). A `.worktrees/` depth breaks the `REPO_ROOT = path.resolve(__dirname,'..','..')` idiom (79 test files use it) → `ERR_MODULE_NOT_FOUND`. Main checkout only.
- **Tests run from `cleargate-cli/`** via `npm test` / `npx tsx --test 'test/<file>'`.
- **DevOps merges `story/CR-XXX` → cli `main` `--no-ff`, LOCAL ONLY.** Never `git push`, never `npm publish` — release is the owner's separate action.
- **dist rebuild + full publish-build verification DEFERRED to Gate-4.** `tsx --test` runs source; `npm pack --dry-run` reflects `package.json files[]` immediately (no build needed) so CR-076's manifest assertion is verifiable in-branch. BUT the tsup `sourcemap` change only takes effect after a build — the "no `*.map` in dist" half of CR-076 needs `npm run build && npm pack --dry-run` to fully confirm. Defer that build-confirm to Gate-4; note it in the DoD.

---

## ⚠️ ENVIRONMENT CONSTRAINT — CR-075 acceptance is Node-25-residual, NOT fully-green

The orchestrator runs in the MONOREPO layout on **Node 25** (target = Node 24 LTS; `engines.node` = `>=24.0.0`, this `.nvmrc` will pin 24). The CR's ideal acceptance ("fresh standalone clone, Node 24, `npm test` green") CANNOT be reproduced in-place. **Concrete in-environment acceptance instead:**

After tiering, the DEFAULT `npm test` (monorepo, Node 25) drops from **86 failures** to **ONLY the residual Node-25 harness failures** — empirically the `require is not defined` class (verified: `test/wiki/ingest.node.test.ts` subtest 2 throws `require is not defined`) plus any unconverted-vitest `it`/`.toBe` cases. The CR defers these per §0.5 (`.nvmrc`=24 + non-blocking follow-up). The ~82 infra/monorepo-coupled failures MOVE to `npm run test:integration`.

Acceptance is met when ALL THREE hold:
1. Default `npm test` (Node 25, monorepo) shows ONLY Node-25-harness residuals red — zero infra-connection failures, zero `--workspace=` failures, zero exit-127 gate failures, zero live↔canonical-parity failures in the DEFAULT tier.
2. `grep -rn "workspace=cleargate-cli" cleargate-cli/test` returns NOTHING.
3. `npm run test:integration` EXISTS, is documented as the infra/monorepo tier, and (run in the monorepo with infra) covers the moved tests.

Developer + QA: **do NOT chase a fully-green Node-25 `npm test`** — it is impossible without Node 24. The residual Node-25 reds in the default tier are EXPECTED and accepted.

---

## Per-story blueprint

### CR-075 — make the cli test suite standalone-runnable (tiering)

- **Cross-story coupling:** owns the FIRST edit to `test/changelog-format.node.test.ts:167-173` (the `npm pack --workspace=` assertion). CR-076 re-edits the same lines. Land CR-075's in-package form first.

- **Schema changes:** none. No DB, no migration, no frontmatter.

- **Tier mechanism (PICK ONE — RECOMMENDED: header-tag + glob split):**

  A per-file header tag is the ONLY system that scales — there are **79** files using the outer-resolve `path.resolve(__dirname,'..','..')` idiom and a hand-list of "82 files" is unmaintainable. Use a **file-header opt-in tag** read by the test script glob, NOT a hand-enumerated exclude list.

  **Mechanism:**
  1. Add a one-line header marker to each integration-tier file (top of file, after the license/doc block):
     ```
     // @cleargate-tier: integration
     ```
  2. Rename integration-tier files to the suffix `*.integration.node.test.ts` OR keep the name and filter by the header. **Recommend the SUFFIX rename** — it is glob-native, needs no custom runner, and the existing `npm test` glob already supports negation. Header tag is the audit/grep aid; the SUFFIX is what the runner keys on.
  3. `package.json` scripts:
     ```
     "test":             "tsx --test ... 'test/**/*.node.test.ts' '!test/**/*.integration.node.test.ts' '!test/fixtures/**'",
     "test:integration": "tsx --test ... 'test/**/*.integration.node.test.ts'",
     ```
     (preserve the existing `--test-concurrency=1 --experimental-test-module-mocks --test-reporter=spec` flags verbatim on both.)

  **Decision: SUFFIX rename `*.integration.node.test.ts` is the tier mechanism.** Justification: glob-native (no custom runner to maintain/test), self-documenting in `ls`, and the default `test` glob already uses negation patterns. The `// @cleargate-tier: integration` header is ADDED too as a grep-able provenance marker but is advisory — the suffix is load-bearing.

- **Files that MOVE to the integration tier (rename to `*.integration.node.test.ts`).** Categories verified empirically against the cli checkout 2026-06-04 (`tsx --test` runs, not the stale CR list):

  | Category | Selection rule (Developer applies the rule, does not hand-pick) | Confirmed examples |
  |---|---|---|
  | **Infra (live PG18/Redis8)** | file imports `pg` / opens `new Client`/`new Pool` / hits `:5432`/`:6379`, OR is in the CR's named infra set | `test/commands/bootstrap-root.node.test.ts`, `test/auth/acquire*`, `test/auth/file-store*`, `test/admin-api/client*`, `test/lib/membership*`, `scrubPassword`, `handle-validation` |
  | **Monorepo-coupling (outer paths)** | file references `../.cleargate/scripts`, `cleargate-planning`, or resolves the OUTER repo via `path.resolve(__dirname,'..','..')` AND fails standalone | the 79 outer-resolve users that need the monorepo layout — incl. `test/agents/qa-content`, `test/lib/agent-developer-section`, `test/scaffold/canonical-live-parity.red`, `test/scaffold/sprint-execution-mirror`, `test/commands/init*`, `test/e2e/dogfood-install`, `test/integration/foreign-repo` |
  | **Workspace= shell** | the two `npm pack --workspace=` users — see fix below; they STAY in default tier after the `--workspace=` removal (in-package `npm pack` works standalone) | `test/changelog-format.node.test.ts`, **`test/lib/license-contract.node.test.ts`** (CR named only changelog — license-contract is the SECOND user, flag it) |
  | **Gate monorepo-path (exit-127 class)** | `test/commands/gate-v2.node.test.ts` — verified 5/7 fail; failures are unit-seam/env, real gate logic untested-by-these | move to integration OR fix the seam (see Gotcha) — recommend integration tier |
  | **Live↔canonical parity** | files asserting `/.claude/**` ↔ `cleargate-planning/.claude/**` mirror parity — these need the monorepo's live instance | `test/scaffold/canonical-live-parity.red` (3 fail, also red from THIS sprint's CR-077/081/082 Gate-4-deferred drift), `test/agents/qa-content`, `test/lib/agent-developer-section`, `test/scripts/template-stubs`, `test/scripts/template-sync-fields` |

  **Developer applies the RULE per category** (grep signature → tier-suffix rename + header tag), runs `npm test`, and any file STILL failing for an infra/monorepo reason gets moved. Iterate until the default tier shows only Node-25 residuals. This is a mechanical loop, not 82 hand-decisions.

- **`*.red.node.test.ts` triage (57 red files found):** these are TDD-discipline naming (CR-043 immutability), NOT "deliberately red in-flight." Most are SHIPPED stories' red tests that now pass. Tier them by the SAME category rule above — a `*.red.node.test.ts` that references outer paths goes to integration and becomes `*.red.integration.node.test.ts` (preserve the `.red` segment for CR-043 naming). Do NOT exclude red files wholesale. Specific stale-vs-live calls:
  - `test/scripts/{admin,cli,mcp}-vitest-conversion.red.node.test.ts` + `codemod-vitest-to-node-test.red` — these import/reference vitest-era fixtures. If their fixtures import `'vitest'` they fail under the no-vitest repo → move to integration (they exercise the conversion codemod against monorepo fixtures). Do NOT delete — EPIC-028 conversion provenance.
  - `test/scaffold/canonical-live-parity.red` — integration (live↔canonical).
  - `test/scripts/token-ledger-resolver.red`, `test/hooks/red-gate.node.test.ts` — grep for outer-path/infra signature; tier accordingly. `red-gate` references `../.cleargate/scripts` → integration.

- **`--workspace=cleargate-cli` removal (BOTH files — CR named only changelog):**
  - `test/changelog-format.node.test.ts:167-173`: replace
    `execSync('npm pack --workspace=cleargate-cli --dry-run 2>&1', { cwd: repoRoot, ... })`
    with `execSync('npm pack --dry-run 2>&1', { cwd: cliDir, ... })` — note `cwd: cliDir` (the package root, already computed at line 122 as `path.resolve(__dirname, '..')`), NOT `repoRoot`. In-package `npm pack` is workspace-flag-free and standalone-correct.
  - **`test/lib/license-contract.node.test.ts`** (SECOND user — Developer MUST fix this too): same `--workspace=` → in-package `npm pack` from the package root. The grep gate (`grep -rn "workspace=cleargate-cli" cleargate-cli/test` → nothing) FAILS if license-contract is missed.

- **`.nvmrc`:** new file `cleargate-cli/.nvmrc` containing `24` (single line). Plus a CI-matrix note in the CR's follow-up (the Node-25 harness drift is filed non-blocking per §0.5).

- **Ratchet re-baseline — OPEN DECISION (see §Open decisions): the ratchet is STALE, not just unbaselined.** Verified: `test_ratchet.mjs` lives ONLY at the OUTER `.cleargate/scripts/test_ratchet.mjs` (NOT in `cleargate-cli/`), still spawns **vitest** (`'vitest', 'run', '--reporter=json'` lines 78-83 — vitest was REMOVED from the repo in EPIC-028), and reads `test-baseline.json` at `REPO_ROOT/test-baseline.json` which **does not exist anywhere**. There is no live pre-commit wiring to it in `cleargate-cli/` (`.husky/`, `.git/hooks/`, `package.json` scripts — none reference it). The CR's "re-baseline IF present" is satisfied by: the baseline is ABSENT and the ratchet is vitest-era dead code. **Recommended Developer action:** do NOT resurrect the vitest ratchet. Either (a) leave it untouched (it is an outer-repo artifact, out of the cli package's `files[]`, harmless), or (b) if the orchestrator wants a live node:test ratchet, that is a SEPARATE follow-up CR, not in M4 scope. **Decide at dispatch** (Open decision #1).

- **Reuse (no duplication):** `cliDir = path.resolve(__dirname,'..')` already computed in changelog-format.node.test.ts:122 — reuse it for the `cwd`. No new helper needed.

- **Gotchas surfaced from code inspection:**
  - `test_ratchet.mjs:78-83` — spawns vitest, which is gone. `test_ratchet.mjs:30` — baseline at outer `REPO_ROOT/test-baseline.json`, absent. Stale machinery; see Open decision #1.
  - `gate-v2.node.test.ts` failures (5/7) are unit-SEAM failures using injected `spawnFn`/`exitFn`/`runScriptPath:'/fake/run_script.sh'` (lines 141-159, 209-229) — NOT real gate-logic bugs. The exit-127 the CR cites is the OLD shape; this file already uses seams. Moving it to integration is the clean call; alternatively the seam-default may need a Node-25 fix — prefer the tier move (don't expand scope).
  - `test/lib/license-contract.node.test.ts` is the UNDOCUMENTED second `--workspace=` user. The CR §Existing-Surfaces missed it. Must be fixed or the grep gate fails.
  - 79 files use the `path.resolve(__dirname,'..','..')` outer-resolve idiom — this is why a hand-list is untenable and the suffix-rename rule is mandatory.

---

### CR-076 — trim the published npm package (drop sourcemaps + de-dup payload)

- **Cross-story coupling:** SECOND edit to `test/changelog-format.node.test.ts` (the in-package `npm pack` assertion CR-075 just landed) — re-tighten it for the trimmed manifest (no `*.map`, single payload). Lands after CR-075 merges to cli `main`.

- **Schema changes:** none. Build/publish config only.

- **⚠️ READ-PATH PROOF (hard prerequisite — and the CR's hypothesis is INVERTED):**

  The CR §0.5 assumed the installed CLI reads `dist/templates/` via `__dirname`. **It does NOT.** Verified against `src/commands/init.ts:140-145`:
  ```
  export function resolveDefaultPayloadDir(): string {
    const thisFile = fileURLToPath(import.meta.url);
    const pkgRoot = path.resolve(path.dirname(thisFile), '..');   // dist/cli.js → dirname=dist/ → up one = PACKAGE ROOT
    return path.join(pkgRoot, 'templates', 'cleargate-planning'); // → <pkgRoot>/templates/cleargate-planning  (ROOT templates!)
  }
  ```
  In the published package the bin is `dist/cli.js`, so `__dirname = dist/`, and `pkgRoot = dist/.. = package root`. `cleargate init` reads **ROOT `templates/cleargate-planning`** — NOT `dist/templates/`. Independently confirmed: the wiki synthesis loaders (`src/wiki/synthesis/active-sprint.ts:60-61`, `product-state.ts:82-83`, `open-gates.ts:62-63`, `roadmap.ts:99-100`) ALL resolve `path.resolve(__dirname,'..','templates','synthesis')` = ROOT `templates/synthesis` too. A repo-wide grep for a literal `dist/templates` RUNTIME read returns NOTHING (only tsup's onSuccess writer + comments).

  **Conclusion (inverts the CR):**
  - ROOT `templates/` is the LIVE read path. It MUST STAY in `files[]`.
  - `dist/templates/` (written by tsup `onSuccess`, tsup.config.ts:43-49) is the UNUSED duplicate. The redundancy is eliminated by **stopping the `onSuccess` template copy** (so `dist/` no longer carries a second payload), NOT by dropping `templates` from `files[]`.
  - **Developer MUST re-run this proof before touching anything** (grep `resolveDefaultPayloadDir`, confirm `__dirname` math) and record it in the dev report. The CR's "drop `templates` from `files[]`" instruction is WRONG for this codebase — do the inverse.

- **Config surgery (corrected for the inverted read-path):**
  1. **Sourcemaps:** `tsup.config.ts:37` `sourcemap: true` → set `false` for the publish build (drops `dist/cli.js.map` 15.2MB + `dist/cli.cjs.map` 15.3MB ≈ 30MB). Simplest + most robust: `sourcemap: false` unconditionally (local debugging rarely needs CLI sourcemaps; the CR accepts this). Alternative — keep emit, exclude via `.npmignore` (`*.map`) — is more fragile (`.npmignore` overrides `files[]` semantics). **Recommend `sourcemap: false`.**
  2. **De-dup payload:** remove the `onSuccess` template-copy block (tsup.config.ts:40-50, the `srcTemplates → dstTemplates` copy). KEEP the MANIFEST.json copy (tsup.config.ts:52-62) — `dist/MANIFEST.json` may be read separately; verify with grep before removing, but default = keep it (out of scope, low risk). Leave `files[]` listing BOTH `dist` and `templates` — both are needed: `dist` for the bin + `dist/MANIFEST.json`, `templates` for the LIVE init/synthesis read path. **Do NOT drop `templates` from `files[]`.**
  3. **Net effect:** `dist/*.map` gone (~30MB), `dist/templates/cleargate-planning/**` gone (the dup), root `templates/` retained (live path). Payload ships ONCE (root `templates/`), no maps.

- **Test shape (CR-076 QA-Red — standard lane, a real Red test applies here):**
  1. **`npm pack --dry-run` manifest assertion** (new or extend changelog-format): from `cwd: cliDir`, assert the dry-run output has ZERO lines matching `\.map$` AND `templates/cleargate-planning` appears under exactly ONE root prefix (`templates/`, not also `dist/templates/`). NOTE: `npm pack --dry-run` reflects `files[]` IMMEDIATELY but the `*.map` absence and `dist/templates` absence only show after `npm run build` (they're build outputs). So this assertion is two-phase: the `files[]`/`templates` half is verifiable in-branch; the `dist/*.map` + `dist/templates` half needs a build → DEFER that half's full confirm to Gate-4 (note in DoD). The Red test should `npm run build && npm pack --dry-run` if a build is acceptable in-CI; otherwise assert the `files[]` shape now + defer the dist-output assertion.
  2. **`cleargate init` smoke** (init-still-works): in a scratch dir, run init (or call `resolveDefaultPayloadDir()` + `copyPayload`) and assert `.claude/` + `.cleargate/` scaffold lands and `.cleargate/.install-manifest.json` is written — proves the ROOT-`templates/` read path still resolves after the dist-dup removal.

- **Reuse (no duplication):** `copyPayload()` from `src/init/copy-payload.ts:111` and `resolveDefaultPayloadDir()` from `src/commands/init.ts:140` for the init smoke. `cliDir` from changelog-format:122 for `npm pack` cwd.

- **Gotchas surfaced from code inspection:**
  - **The CR hypothesis is INVERTED** (read-path proof above). Dropping `templates` from `files[]` — the CR's literal instruction — would BREAK `cleargate init` in the field. The correct move is dropping the `dist/templates` dup via tsup `onSuccess` + `sourcemap:false`. This is the #1 risk; the proof prerequisite gates it.
  - `tsup.config.ts:52-62` also copies `MANIFEST.json` into `dist/` — KEEP (separate concern). Only remove the `templates` copy (40-50).
  - `npm pack --dry-run` reflects `files[]` without a build; the dist-output changes (`*.map`, `dist/templates`) need a build to appear in the manifest — hence the two-phase verification + Gate-4 deferral for the build half.

---

## changelog-format coordination (CR-075 → CR-076 — clean serial merge)

ONE assertion (`test/changelog-format.node.test.ts:167-173`, scenario "Tarball includes CHANGELOG") is touched by BOTH CRs. Sequence:

1. **CR-075 edits lines 168-171:** `'npm pack --workspace=cleargate-cli --dry-run 2>&1'` → `'npm pack --dry-run 2>&1'`, and `{ cwd: repoRoot, ... }` → `{ cwd: cliDir, ... }`. The assertion body (`assert.match(output, /CHANGELOG\.md/)`) is UNCHANGED. Merge CR-075 to cli `main`.
2. **CR-076 then edits the SAME scenario** (or adds an adjacent scenario in the same `describe`): after the trim, the dry-run output must ALSO satisfy "no `*.map`" + "payload once." CR-076 either (a) extends the existing scenario's assertions, or (b) adds a sibling `test('Scenario: tarball excludes sourcemaps and ships payload once', ...)` in the same `describe` block — **recommend (b)**, a NEW sibling test, so CR-076 does not re-touch CR-075's exact lines (zero merge conflict on the in-package-pack line).

**Who edits what:** CR-075 owns lines 168-171 (the `--workspace=`→in-package conversion) and the parallel fix in `license-contract.node.test.ts`. CR-076 owns a NEW sibling test in the same `describe` (no overlap with CR-075's lines) + the `tsup.config.ts` + (verify-only) read-path. Serial merge order CR-075→CR-076 with CR-076 ADDING rather than MUTATING keeps the diff conflict-free.

---

## Test shape summary

- **CR-075:** primarily a refactor — the "test" is the suite itself going 86 → Node-25-residual-only in the default tier, PLUS the grep gate (`workspace=cleargate-cli` → nothing) PLUS `test:integration` existing + covering the moved files. No new green-field QA-Red Red test is strictly required; if a standard QA-Red runs, the Red assertion is "default `npm test` has no `--workspace=`/infra/exit-127/parity failures" — which is the acceptance itself. **Standard lane.**
- **CR-076:** a real Red test applies — `npm pack --dry-run` manifest assertion (no `*.map`, single payload) + the `cleargate init` smoke. **Standard lane.**

---

## Cross-story risks

1. **CR-076 read-path inversion (HIGHEST).** The CR literally says "drop `templates` from `files[]` and ship only `dist`." This codebase reads ROOT `templates/` at init+synthesis time (init.ts:140-145, synthesis loaders). Following the CR verbatim breaks `cleargate init`. Mitigation = the mandatory read-path proof prerequisite + the corrected instruction (drop the `dist/templates` dup via tsup `onSuccess`, keep root `templates/` in `files[]`). The Developer must record the proof in the dev report BEFORE editing `tsup.config.ts`/`files[]`.
2. **Second `--workspace=` user (license-contract).** CR-075's grep gate fails if `test/lib/license-contract.node.test.ts` is not also converted. The CR only named changelog-format. Both must change.
3. **changelog-format double-edit.** If CR-076 MUTATES CR-075's in-package-pack line instead of ADDING a sibling test, the serial merge conflicts. Mitigation = CR-076 adds a new sibling test in the same `describe`.
4. **Ratchet false-confidence / masking a real regression.** The ratchet is stale (vitest + missing baseline). If the Developer "re-baselines" by resurrecting it, a Node-25-residual count could be frozen as the new floor and mask a future REAL regression. Mitigation = do NOT resurrect the vitest ratchet in M4 (Open decision #1); tier acceptance is by-category grep + the residual-only rule, not a numeric baseline.
5. **Node-25 residual mistaken for a tiering miss.** QA might see the `require is not defined` reds in the default tier and reject the split as incomplete. Mitigation = the explicit Node-25-residual acceptance (these reds are EXPECTED and accepted; `.nvmrc`=24 + follow-up).
6. **Gate-4 build-confirm gap.** CR-076's `dist/*.map`-absence + `dist/templates`-absence only appear after `npm run build`. If Gate-4 skips the build-confirm, a publish could still ship maps. Mitigation = explicit DoD line: Gate-4 runs `npm run build && npm pack --dry-run` and confirms zero `*.map` + single payload before any (owner-run) publish.

---

## Open decisions for orchestrator

1. **Ratchet disposition (CR-075).** The `test_ratchet.mjs` is stale (outer-repo-only, spawns removed vitest, no baseline file). Options: (a) **RECOMMENDED** — leave it untouched, treat "re-baseline IF present" as satisfied (baseline ABSENT, ratchet is dead outer-repo code, out of the cli `files[]`); acceptance for CR-075 is by-category grep + residual-only rule, no numeric baseline. (b) File a SEPARATE follow-up CR to port a node:test-native ratchet — NOT M4 scope. Confirm (a) at dispatch, or pass (b) as a new CR. Do NOT instruct the Developer to resurrect the vitest ratchet.
2. **`gate-v2.node.test.ts` — tier vs seam-fix.** 5/7 fail on unit-seam/env, not gate logic. RECOMMENDED = move to integration tier (clean, no scope creep). Alternative = fix the seam default for Node 25 (expands CR-075 scope). Confirm tier-move is acceptable, or authorize the seam fix.
3. **`*.map` strategy (CR-076).** RECOMMENDED = `tsup sourcemap:false`. Alternative = keep emit + `.npmignore *.map`. Confirm `sourcemap:false` (simpler, the CR §0.5 accepts dropping maps from the package; "build may still emit locally" can be a dev-only env flag follow-up if desired).

---

## DoD (M4)

- [ ] CR-075: default `npm test` (Node 25, monorepo) shows ONLY Node-25-harness residuals — zero `--workspace=`, zero infra-connection, zero exit-127 gate, zero live↔canonical-parity failures in the DEFAULT tier.
- [ ] CR-075: `grep -rn "workspace=cleargate-cli" cleargate-cli/test` returns nothing (BOTH changelog-format AND license-contract converted).
- [ ] CR-075: `npm run test:integration` exists, documented as the infra/monorepo tier, and (run with infra in monorepo) covers the moved files.
- [ ] CR-075: `cleargate-cli/.nvmrc` = `24` added; Node-25 harness drift filed as non-blocking follow-up.
- [ ] CR-075: ratchet disposition recorded per Open decision #1 (recommended: untouched).
- [ ] CR-076: read-path proof recorded in dev report BEFORE config edits (ROOT `templates/` is live; `dist/templates` is the dup).
- [ ] CR-076: `tsup sourcemap:false`; tsup `onSuccess` template-copy removed; `files[]` retains BOTH `dist` + `templates` (NOT dropped).
- [ ] CR-076: `npm pack --dry-run` (post-build) shows zero `*.map`, payload once; `cleargate init` smoke scaffolds correctly + writes `.install-manifest.json`.
- [ ] changelog-format coordination honored: CR-075 converts the in-package pack line; CR-076 ADDS a sibling test (no mutation of CR-075's line).
- [ ] DevOps merges `story/CR-075` then `story/CR-076` → cli `main` `--no-ff`, LOCAL ONLY (no push, no publish).
- [ ] **Gate-4 deferral:** `npm run build && npm pack --dry-run` confirms zero `*.map` + single payload (the build-output half of CR-076) — DEFERRED to Gate-4, run before any owner publish.

---

## Footer

- **Dep version check (CR-037):** N/A — neither CR adds a package. `engines.node >=24.0.0` (package.json:42-44) and the `.nvmrc`=24 align; existing pins (tsup ^8, tsx ^4.21.0, typescript ^5.8.0) unchanged.
- **Protocol numbering audit:** N/A — neither CR appends a `cleargate-protocol.md` section.
- **Flashcards recorded (3):** `#npm-publish #payload #init` (init reads ROOT templates, dist/templates is the droppable dup — CR-076 inverted); `#test #monorepo #ratchet` (test_ratchet.mjs stale: outer-only, vitest, no baseline); `#test #workspace` (TWO `--workspace=` users, CR named one).
- **Empirical verification (cli checkout `main`, Node 25, 2026-06-04):** changelog-format 3/4 (workspace= fails); gate-v2 2/7; wiki/ingest 17/18 (`require is not defined`); canonical-live-parity.red 9/12; license-contract 3/4 (workspace=). 225 `*.node.test.ts` files, 57 `*.red.node.test.ts`, 79 outer-resolve-idiom users.

## Git Log Digest

Git log unavailable for sprint/S-34 (no commits or branch not found).

## Token Ledger Digest

**Total:** 317,811,139 tokens (input: 91,833 / output: 1,236,476 / cache_read: 306,283,452)

**Per-agent breakdown:**
- architect: 173,943,579 (30 dispatches)
- developer: 51,824,829 (10 dispatches)
- qa: 51,546,848 (11 dispatches)
- reporter: 8,336,231 (1 dispatches)
- devops: 32,159,652 (8 dispatches)

**Anomalies:**
- SPRINT-34: 9.1× higher than median story cost

**CR-035 two-line split (pre-computed for Reporter §3):**
sprint_work_tokens: 309,474,908
sprint_total_tokens: 317,811,139
reporter_pass_tokens: null


## Flashcard Slice

No flashcard entries found in sprint window (no date window available).

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
