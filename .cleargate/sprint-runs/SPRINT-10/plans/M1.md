# Milestone: M1 — Safety Gates + Infra
## Stories: STORY-014-01, STORY-014-02, STORY-014-03, STORY-014-04, STORY-014-05
## Wave: W1a (parallel) + W1b (sequential)

`role: architect`

## Order

**W1a (parallel, 3 concurrent worktrees):** 014-01, 014-02, 014-04 — disjoint file surfaces.
**Serialize inside W1a for shared protocol file:** 014-01 lands §20 append first; 014-02 rebases its §2 amendment on top. Enforce by merging 014-01 → sprint branch before opening 014-02's PR.
**W1b (sequential):** 014-03 → 014-05. Both edit `.claude/hooks/pending-task-sentinel.sh`. 03 lands the flashcard-gate block; 05 rebases and wraps `REPO_ROOT` with the `ORCHESTRATOR_PROJECT_DIR` override.

R1 mitigation (common to 01, 03, 04): each gate ships with a `SKIP_*` env bypass (`SKIP_SURFACE_GATE`, `SKIP_FLASHCARD_GATE`, `SKIP_TEST_RATCHET`). Bypasses stay enabled through M1 close, disabled post-milestone by orchestrator action (tracked in sprint §4 Execution Log, not a story).

Pre-commit dispatcher rule: 01 ships first pre-commit hook → writes dispatcher stub `.claude/hooks/pre-commit.sh` that chains `pre-commit-*.sh` in lexical order. 04 adds `pre-commit-test-ratchet.sh` into the same directory; no dispatcher edit required. Claude Code's `.claude/settings.json` has no pre-commit event — these are git hooks invoked via `.git/hooks/pre-commit` symlink (scaffold installs the symlink on `cleargate init`; for dogfood, 01's Developer installs it by hand and documents in commit body).

## Per-story blueprint

### STORY-014-01 — File-Surface Diff Gate

- **Files to create (live + `cleargate-planning/` mirror):**
  - `.cleargate/scripts/file_surface_diff.sh` — parses §3.1 pipe-table of the active story, diffs vs `git diff --cached --name-only`.
  - `.claude/hooks/pre-commit-surface-gate.sh` — wrapper that resolves active-story path, invokes `file_surface_diff.sh`, exits its exit code under v2, prints-warn-exit-0 under v1.
  - `.claude/hooks/pre-commit.sh` — dispatcher: `for h in "$(dirname "$0")"/pre-commit-*.sh; do "$h" || exit $?; done`.
  - `.cleargate/scripts/surface-whitelist.txt` — line-per-pattern; seed with `cleargate-planning/MANIFEST.json`, `.cleargate/hook-log/*`, `.cleargate/sprint-runs/**/token-ledger.jsonl`, `.cleargate/sprint-runs/**/.pending-task-*.json`, `.cleargate/sprint-runs/**/state.json`.
  - `.cleargate/scripts/test/test_file_surface.sh` — 4-scenario Gherkin test per story §2.1.
- **Files to modify:**
  - `.cleargate/knowledge/cleargate-protocol.md` — append `## 20. File-Surface Contract (v2)` (next free § confirmed; last shipped header is `## 19. Execution Mode Routing (v2)` at line 762). Mirror to `cleargate-planning/`.
  - `.cleargate/templates/story.md` — in §3.1 heading block (line 112 `### 3.1 Context & Files`) add one-line note: "under v2 this table is a pre-commit gate input (protocol §20)."
- **Schema changes:** none.
- **Test scenarios (from §2.1):**
  1. Off-surface staged file blocks commit with non-zero exit + stderr list.
  2. Staged files match surface → exit 0.
  3. Whitelist line admits generated file (test with `cleargate-planning/MANIFEST.json`).
  4. v1 mode prints warning, exits 0, does not block.
- **Reuse:** active-sprint resolution via `.cleargate/sprint-runs/.active` sentinel (same pattern as `pending-task-sentinel.sh:31`). Current-story resolution via `state.json` stories map — scan for first non-terminal state; fallback to most-recently-updated story. Use portable bash `arr=(); while IFS= read -r x; do arr+=("$x"); done < <(cmd)` pattern (flashcard `#bash #macos #portability` 2026-04-21) — no `mapfile`.
- **Gotchas surfaced:**
  - `.git/hooks/pre-commit` symlink not installed in dogfood repo today. Developer must `ln -sf ../../.claude/hooks/pre-commit.sh .git/hooks/pre-commit` during Scenario 1 verification and flashcard the install-once step for future worktrees.
  - Story-file §3.1 parser must scan between `### 3.1` and next `###` header; third pipe column contains the path in backticks — strip backticks before comparison. Multiple paths in a single cell separated by `, ` must split.
  - Worktree paths: developer runs pre-commit from `.worktrees/STORY-014-01/`. Whitelist must match both repo-root-relative and absolute-worktree-path variants.

### STORY-014-02 — Gate-2 Story-File Assertion

- **Files to create (live + mirror):**
  - `.cleargate/scripts/assert_story_files.mjs` — args `<sprint-file-path>`. Parses `## 1. Consolidated Deliverables` table for `STORY-\d+-\d+` IDs, verifies `pending-sync/STORY-<id>_*.md` exists via `fs.readdirSync` + prefix match. Exit 0 on full match, non-zero with `{ missing: [], present: [] }` on stderr otherwise.
  - `.cleargate/scripts/test/test_assert_story_files.sh` — 4-scenario Gherkin test.
- **Files to modify:**
  - `.cleargate/scripts/init_sprint.mjs` — at `init_sprint.mjs:74` the file currently hardcodes `execution_mode: 'v1'` in the created state.json. Read the sprint-file frontmatter (at `.cleargate/delivery/pending-sync/SPRINT-<id>_*.md` or archive equivalent) to determine execution mode before writing state.json. If `execution_mode: v2`, spawn `node assert_story_files.mjs <sprint-file>` via `child_process.spawnSync`; on non-zero exit, bubble stderr and `process.exit(1)` BEFORE the `fs.writeFileSync` call at line 84. If v1, call the script but only warn on failure. Set the `execution_mode` field in state.json from the sprint frontmatter instead of hardcoding `'v1'`.
  - `.cleargate/knowledge/cleargate-protocol.md` §2 Gate 2 (line 96–107 block) — append bullet after the v2 enforcement-rule paragraph: "Additionally for v2 sprints, `cleargate sprint init` asserts every story in §Consolidated Deliverables has a `pending-sync/STORY-*.md` file; missing files block init with an enumerated stderr list."
- **Schema changes:** state.json — `execution_mode` now read from sprint file frontmatter (previously hardcoded). No key-shape change; constants.mjs unaffected.
- **Test scenarios:**
  1. v2 init with missing story files → exit non-zero + missing list on stderr + state.json NOT created.
  2. v2 init with all story files present → exit 0 + state.json created.
  3. v1 init with missing story file → state.json created + stderr has `WARN: missing story files: …`.
  4. Standalone CLI `node assert_story_files.mjs <sprint-file>` returns same result independent of init_sprint.
- **Reuse:** frontmatter read — do NOT add a new parser. Use existing `parseFrontmatter` from `cleargate-cli/src/lib/parse-frontmatter.ts` IF accessible via Node import from `.cleargate/scripts/`; otherwise inline minimal regex `^---\n([\s\S]*?)\n---` + `yaml.load` (flashcard `#yaml #frontmatter` 2026-04-19 mandates js-yaml CORE_SCHEMA — but `.cleargate/scripts/` has no js-yaml dep today; use regex `^execution_mode:\s*["']?(v[12])` for the single field). Rule: single-field regex only; do NOT hand-roll a full YAML parser.
- **Gotchas:**
  - `## 1. Consolidated Deliverables` heading text in SPRINT-10 is actually `## 1. Consolidated Deliverables` — confirmed at `SPRINT-10_Execution_v2_Polish.md:30`. Regex must be anchored to `^## 1\.? Consolidated Deliverables` to avoid catching a section name drift in other sprints.
  - Story-ID extraction regex: `/STORY-\d+-\d+/g` within the Deliverables section; dedupe before file-existence check.
  - `fs.readdirSync` on `pending-sync/` is fine — no glob dep (out-of-scope note in story §1.2 confirms).
  - `CLEARGATE_REPO_ROOT` env override exists on init_sprint.mjs (line 20–22); `assert_story_files.mjs` MUST honor same env for test isolation (flashcard `#test-harness #scripts #env` 2026-04-21).

### STORY-014-03 — Flashcard Gate Enforcement

- **Files to modify (live + mirror):**
  - `.claude/hooks/pending-task-sentinel.sh` — before the `Write the sentinel atomically` block at line 72, insert flashcard-gate check: resolve `SPRINT_DIR`, glob `SPRINT_DIR/reports/STORY-*-dev.md` + `STORY-*-qa.md` (note: today these live in `.cleargate/sprint-runs/<id>/` not a `reports/` subdir — confirm at sprint runtime; plan assumes flat dir per SPRINT-09 layout and reads `STORY-*-{dev,qa}.md` at `SPRINT_DIR/`), find most-recent by `stat -f %m` (macOS) / `stat -c %Y` (Linux) — use `ls -t` since both shells accept it. Parse `flashcards_flagged:` YAML block (use awk between `flashcards_flagged:` line and next top-level key or EOF). For each `- "<card>"`, compute `HASH=$(printf '%s' "$card" | shasum -a 1 | cut -c1-12)`. Check `SPRINT_DIR/.processed-<HASH>` existence. If any unprocessed AND execution_mode is v2, exit 1 with stderr naming cards + hint `touch .cleargate/sprint-runs/<id>/.processed-<hash>`. Under v1, print warning and continue. Execution-mode read: cheap heuristic — `grep "execution_mode" <sprint-file>` or read from active sprint's state.json (`jq -r .execution_mode state.json`). Use state.json — it's already in SPRINT_DIR.
  - `.cleargate/knowledge/cleargate-protocol.md` §18 — after §18.5 (line 758), append `### §18.6 PreToolUse hook enforcement (v2)` documenting the hash-marker convention, the SHA-1 first-12-char hash, the `.processed-<hash>` sentinel convention, and the `SKIP_FLASHCARD_GATE=1` bypass.
  - Mirror both changes to `cleargate-planning/`.
- **Files to create:**
  - `.cleargate/scripts/test/test_flashcard_enforcement.sh` — 4-scenario Gherkin test (distinct from existing `test_flashcard_gate.sh` which only validates protocol+agent-spec text).
- **Schema changes:** none. New on-disk artifact: `.processed-<hash>` marker files in sprint dir (gitignored via existing `.cleargate/sprint-runs/` gitignore rule — verify).
- **Test scenarios:**
  1. v2 Task spawn blocked when unprocessed flagged card exists; stderr names card + touch-command hint.
  2. v2 Task spawn proceeds after `.processed-<hash>` marker touched.
  3. v1 advisory: warning to stderr, exit 0.
  4. Empty `flashcards_flagged: []` is a no-op (no error, no warning).
- **Reuse:** existing sentinel path resolution (`pending-task-sentinel.sh:51–59`). Existing jq-on-stdin pattern for tool_name check (line 36–40). Hash computation: use `shasum -a 1` (portable macOS + Linux per flashcard `#bash #macos #portability`); avoid `openssl sha1` (not always present).
- **Gotchas:**
  - SPRINT-09 dev/qa reports live directly at `SPRINT-09/STORY-013-NN-{dev,qa}.md` — NOT in a `reports/` subdir. Story text says `reports/` but the de facto layout is flat. Plan uses flat layout; flag to orchestrator as open decision if SPRINT-10 introduces `reports/` subdir.
  - Hook exits must remain exit-0 safe for PreToolUse under v1 (flashcard `#hooks #protocol` 2026-04-19: Claude Code hooks schema). Under v2 the hook MUST exit non-zero to BLOCK the Task — but the existing hook top comment says "never blocks the tool call (exit 0 always)". That contract changes under v2. Update the top-level comment to reflect conditional-block.
  - `flashcards_flagged:` parsing must tolerate both YAML list forms: `[]` (empty inline) and `- "text"` (block form); must also handle bare unquoted entries since the `#flashcard` SKILL format uses `·` which is UTF-8 multibyte — use `LC_ALL=C` only where safe (never for grepping multibyte content).
  - Hash stability: same card string → same hash across runs (story DoD requirement); prove with a hardcoded expected hash in test_flashcard_enforcement.sh (e.g. `2026-04-22 · #test · example` → known 12-char hex).

### STORY-014-04 — Test-Failure Ratchet

- **Files to create (live + mirror):**
  - `.cleargate/scripts/test_ratchet.mjs` — three subcommands: `update-baseline`, `check` (default), `list-regressions`. Spawns `npx vitest run --reporter=json` in `cleargate-cli/`, parses stdout JSON, extracts `numPassedTests` / `numFailedTests`, writes atomically via tmp+rename.
  - `.claude/hooks/pre-commit-test-ratchet.sh` — invokes script with `check`; respects `SKIP_TEST_RATCHET=1`; 120s timeout via `timeout 120 node …` (or `gtimeout` on macOS — document fallback).
  - `test-baseline.json` at repo root — shape `{ total, passed, failed, skipped, updated_at, failing_tests: [] }`. Key order frozen (story §2.2 demands stable format).
- **Files to modify:** none (pre-commit dispatcher comes from 014-01; 014-04 only adds a sibling `pre-commit-*.sh`).
- **Schema changes:** new `test-baseline.json` file, committed (not gitignored).
- **Test scenarios:**
  1. Current passed >= baseline passed → exit 0 + delta summary "+N tests passing".
  2. Current passed < baseline passed → exit non-zero + "regression: -N tests" stderr.
  3. `update-baseline` mode overwrites baseline file atomically.
  4. `list-regressions` diffs failing-tests sets; emits only NEW failing tests.
  5. `SKIP_TEST_RATCHET=1` → prints bypass warning + exit 0 without running suite.
- **Reuse:** atomic tmp+rename pattern from `init_sprint.mjs:83–85`. `process.env.CLEARGATE_REPO_ROOT` override for test isolation.
- **Gotchas:**
  - R3 mitigation: Developer regenerates baseline as FIRST implementation step. Do NOT copy 829 from the story draft — re-run `npm test --workspace=cleargate-cli` at sprint start and commit that count. Sprint file §Risks R3 makes this explicit.
  - Vitest JSON reporter: vitest 2.x writes JSON to stdout only with `--reporter=json` AND `--outputFile=-` OR `--reporter=json` redirected. Validate the actual output shape before parsing — vitest's JSON is nested under `testResults[].assertionResults[]`. Use top-level `numPassedTests` / `numFailedTests` / `numTotalTests` keys.
  - `SKIP_TEST_RATCHET=1` is the only bypass; do NOT add `--no-verify` instructions to commit messages (CLAUDE.md rule: never skip hooks).
  - Pre-existing test suite is SPRINT-10's cleargate-cli. Running it requires `.worktrees/STORY-014-04/cleargate-cli` to have deps installed — `npm ci --workspace=cleargate-cli` from worktree root. Flashcard `#docker #workspace` 2026-04-20 warns `--workspace` skips siblings in builder; for local dev from worktree, plain `npm ci` is safer.
  - R7: measure latency on first commit. If ratchet adds >30s to commit, flag to orchestrator — Developer does not refactor; QA observes and reports.

### STORY-014-05 — Cross-Project Ledger Routing

- **Files to modify (live + mirror):**
  - `.claude/hooks/pending-task-sentinel.sh:27` — change `REPO_ROOT="/Users/ssuladze/Documents/Dev/ClearGate"` to `REPO_ROOT="${ORCHESTRATOR_PROJECT_DIR:-/Users/ssuladze/Documents/Dev/ClearGate}"`. The scaffold mirror at `cleargate-planning/.claude/hooks/pending-task-sentinel.sh:7` already uses `REPO_ROOT="${CLAUDE_PROJECT_DIR}"`; change to `REPO_ROOT="${ORCHESTRATOR_PROJECT_DIR:-${CLAUDE_PROJECT_DIR}}"`.
  - `.claude/hooks/token-ledger.sh:59` — same substitution. Scaffold mirror: same precedence chain.
  - `cleargate-planning/CLAUDE.md` — inside the `<!-- CLEARGATE:START -->...<!-- CLEARGATE:END -->` block, add a new subsection (after "Support infrastructure" paragraph at line 39): "**Cross-project orchestration.** When running an orchestrator from one project's repo against another project's sprint tree, export `ORCHESTRATOR_PROJECT_DIR=/absolute/path/to/target/repo` in the shell before launching the session. Overrides `CLAUDE_PROJECT_DIR`; sentinel + ledger writes route into the target's `.cleargate/sprint-runs/` tree." Mirror to live `CLAUDE.md` only IF the live file has a CLEARGATE block — current dogfood repo `/Users/ssuladze/.../ClearGate/CLAUDE.md` is NOT wrapped in CLEARGATE markers (it's the source-of-truth project instructions); the scaffold-only mirror lands in `cleargate-planning/CLAUDE.md`.
- **Files to create:**
  - `.cleargate/scripts/test/test_cross_project_routing.sh` — 4-scenario Gherkin test with two tmpdir projects.
- **Schema changes:** none.
- **Test scenarios:**
  1. `ORCHESTRATOR_PROJECT_DIR=/tmp/other-repo` + `.active` in target → sentinel writes to target tree.
  2. Same env + SubagentStop → ledger row writes to target tree.
  3. Unset env → behavior unchanged (hook's own REPO_ROOT).
  4. Env set but target has no `.active` → off-sprint bucket in TARGET (not hook-repo).
- **Reuse:** existing `.active` sentinel read pattern at `pending-task-sentinel.sh:53` and `token-ledger.sh:79`. Existing off-sprint fallback at `token-ledger.sh:89`.
- **Gotchas:**
  - Rebase onto 014-03. Both stories touch `pending-task-sentinel.sh`. 014-05 must land the `REPO_ROOT` wrapper at the top (line ~27) and 014-03's flashcard-gate block (inserted by 03) must still see `$REPO_ROOT` resolved correctly — verify with `bash -n` and a smoke run.
  - `CLAUDE_PROJECT_DIR` vs `ORCHESTRATOR_PROJECT_DIR` precedence: story §1.2 says ORCHESTRATOR wins. Precedence chain in scaffold: `${ORCHESTRATOR_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-/fallback}}`. Do NOT invert.
  - R4 DoD smoke: spawn a fake subagent dispatch with `ORCHESTRATOR_PROJECT_DIR=/tmp/cg-test-target` set, confirm `/tmp/cg-test-target/.cleargate/sprint-runs/.../` receives the sentinel. Clean up the tmp tree in test teardown.
  - Live `/Users/ssuladze/Documents/Dev/ClearGate/CLAUDE.md` is the project's own instructions file, NOT a scaffold-wrapped CLAUDE.md. No live-mirror change; only scaffold gets the new subsection.

## Cross-story risks

- **014-01 ↔ 014-04 pre-commit dispatcher:** 01 creates `.claude/hooks/pre-commit.sh` dispatcher; 04 must drop `pre-commit-test-ratchet.sh` into the same directory without editing the dispatcher. Merge order 01 → 04 prevents 04 from having to write a stub dispatcher that 01 then overwrites. Developer of 04 assumes 01's dispatcher exists; if 01 lands partial (dispatcher missing), 04 blocks on W1a merge.
- **014-03 ↔ 014-05 sentinel hook:** 05 rebases on 03. If 03's flashcard-gate block uses `$REPO_ROOT` before 05 has landed the env-override wrapper, the hook resolves the local hardcoded path — still correct under v2 dogfood (same repo). But after 05 lands, the block transparently picks up `$ORCHESTRATOR_PROJECT_DIR`. Verify: `grep -n '\$REPO_ROOT' .claude/hooks/pending-task-sentinel.sh` post-05 to ensure no leftover hardcoded paths.
- **014-01 ↔ 014-02 protocol file:** 01 appends `## 20`, 02 amends `## 2`. §§ never collide but same file. 01 merges first; 02 rebases. Developer of 02 MUST re-run the three-surface diff after rebase to detect any conflict markers.
- **014-01 §3.1 parser vs story.md template note:** 01's template edit adds a parenthetical ("this table is a pre-commit gate input under v2"). Any SPRINT-10 story authored AFTER 01 lands must have clean §3.1 tables. SPRINT-10's own stories (including 014-01..05) have the `Mirrors` row in §3.1 which is NOT a file path — the parser MUST skip rows where the "Value" cell doesn't look like a path (no `.` or `/`). Failing this, the parser rejects legitimate §3.1 tables on the first commit.
- **014-03 layout assumption (`reports/` vs flat):** If SPRINT-10 adopts a `reports/` subdir for dev/qa reports, 03's glob must match. Plan assumes flat; orchestrator must confirm at Developer dispatch.
- **014-02 execution_mode read from sprint file:** init_sprint.mjs today hardcodes `'v1'` in state.json (line 74). 02 MUST flip that to read from the sprint frontmatter. Downstream stories (014-03 and 014-05) read `execution_mode` from state.json — if 02 doesn't flip the hardcode, SPRINT-10's state.json will say v1 and every new v2 gate falls through to advisory.

## Open decisions for orchestrator

1. **Dev/QA report layout for SPRINT-10:** flat (`<sprint-dir>/STORY-*-dev.md`) or nested (`<sprint-dir>/reports/STORY-*-dev.md`)? Plan assumes flat per SPRINT-09 precedent; story 014-03 text says `reports/`. Orchestrator confirms at first Developer dispatch; if nested, 03's Developer adjusts the glob.
2. **Pre-commit hook install in dogfood repo:** `.git/hooks/pre-commit` symlink to `.claude/hooks/pre-commit.sh` is not present today. 014-01's Developer installs it by hand during verification and logs the install step to sprint §4 Execution Log. Is `cleargate init` to install the symlink automatically in-scope for a follow-up story (EPIC-014 §6 out-of-scope note implies no), or should 014-01 add it as a side effect?
3. **Baseline number for 014-04:** story §4.2 DoD says "829 passed". Per R3, Developer regenerates. If the regenerated count differs materially (e.g. 820 or 835), does the orchestrator want a heads-up before the Developer commits the new baseline? Plan answer: yes — Developer reports regenerated count in first response line after `STORY=014-04`.
4. **Live `CLAUDE.md` mirror for 014-05:** live `/Users/ssuladze/.../ClearGate/CLAUDE.md` is the meta-repo's own project instructions (not scaffold-wrapped). 014-05 mirrors the new subsection only into `cleargate-planning/CLAUDE.md`. Confirm — or does orchestrator want the meta-repo's own instructions updated too? Plan defaults to scaffold-only to preserve meta-repo editorial authority.
5. **Test-ratchet scope for SPRINT-10:** locked to `cleargate-cli/` per EPIC-014 §Q4. MCP + admin follow-up is SPRINT-11. 014-04 Developer must NOT extend the ratchet to other workspaces even if opportunistic.
