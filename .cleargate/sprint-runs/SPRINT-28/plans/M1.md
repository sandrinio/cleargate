# Milestone: M1 — Wave 1 Foundations (parallel-safe)

**Stories in scope:** STORY-066-01, STORY-067-01, STORY-028-04, STORY-010-02
**Wave:** W1 (all four parallel; no intra-wave ordering required)
**Lane confirmation:** All four are `lane: standard` per state.json + SDR §2.4. Confirmed.
**Out of scope here:** BUG-004 (fast lane — skips per-milestone Architect per skill §B). Waves 2 + 3 covered by separate milestone plans.

## Goal-tie (one sentence per story)

- **STORY-066-01** ships `parent-rollup.ts` — the pure library that Thread A (CR-066 parent reconciliation) depends on; without it Step 2.6c block-mode cannot be wired by -066-02.
- **STORY-067-01** ships `migrate-status-to-completed.mjs` + `.migration-lock` — the Phase A primitive Thread B (CR-067 vocab unification) executes against the live archive in -067-02.
- **STORY-028-04** ships the vitest→node:test codemod — the tool Thread C (EPIC-028 vitest elimination) invokes per-package in -028-05/-06/-07; without it the three per-package conversions have no mechanical engine.
- **STORY-010-02** ships the four MCP pull/list/comments/detect endpoints + generic `PmAdapter` interface — Thread D closes EPIC-010 (sitting since 2026-04-19), the final deliverable for that epic. **STATUS-DISCOVERY: see §"Open decisions for orchestrator" below — code already merged in mcp/ on 2026-04-20.**

## Dispatch order

All four stories are parallel-safe — disjoint primary file surfaces:
- STORY-066-01: `cleargate-cli/src/lib/parent-rollup.ts` (NEW) + `cleargate-cli/test/lifecycle/parent-rollup.node.test.ts` (NEW) + ONE re-export line in `cleargate-cli/src/lib/lifecycle-reconcile.ts`.
- STORY-067-01: `cleargate-cli/scripts/migrate-status-to-completed.mjs` (NEW) + sibling `.node.test.ts` + ONE lock-check insert in `cleargate-cli/src/commands/push.ts`.
- STORY-028-04: `cleargate-cli/scripts/codemod-vitest-to-node-test.mjs` (NEW) + sibling `.node.test.ts` + 12 fixture files + ONE `ts-morph` devDep add in `cleargate-cli/package.json`.
- STORY-010-02: `mcp/src/adapters/*` + `mcp/src/tools/cleargate-*.ts` + `mcp/src/mcp/register-tools.ts` (mcp/ workspace only — fully disjoint from cleargate-cli/).

No story in M1 blocks another in M1.

---

## Per-story blueprint

### STORY-066-01 — `parent-rollup.ts` Library + Tests

**Cross-story coupling within M1:** None. The re-export edit in `lifecycle-reconcile.ts` is purely additive; STORY-067-01 / -028-04 / -010-02 do not touch this file.

**Predecessor for:** STORY-066-02 (Wave 2 close_sprint Step 2.6c wiring).

**Schema changes:** None (no migration, no frontmatter delta).

**Files to touch:**
| Action | Path | Notes |
|---|---|---|
| CREATE | `cleargate-cli/src/lib/parent-rollup.ts` | Pure library; exports `rollUpParentStatus`, `walkActiveParents`, `RollupResult`. |
| CREATE | `cleargate-cli/test/lifecycle/parent-rollup.node.test.ts` | 5 verdict scenarios + 1 cycle-detection scenario. Use `node:test` + `node:assert/strict`. |
| CREATE | `cleargate-cli/test/fixtures/parent-rollup/{full,partial,zero,sub-epic,already-terminal}/` | 5 sub-dirs; each contains a synthetic `pending-sync/` + `archive/` skeleton sufficient for one Gherkin scenario. Each `.md` file needs only a frontmatter block (no body required). |
| MODIFY | `cleargate-cli/src/lib/lifecycle-reconcile.ts` | Add **one** export line near the other exports: `export { rollUpParentStatus, walkActiveParents, type RollupResult } from './parent-rollup.js';`. Do NOT modify lines 27-36 (ARTIFACT_TERMINAL_STATUSES) or lines 47/51 (VERB_STATUS_MAP expected[]). |

**Test scenarios (from §2.1 Gherkin — Developer MUST cover all 5 + the cycle-detection error path = 6 tests minimum):**
1. Full coverage — all 6 children terminal → verdict `auto-flip`, proposed_status `Completed`, coverage `full`, terminal_children.length === 6.
2. Partial coverage — 7 archived Done + 1 pending Approved → verdict `halt-partial`, halt_reason prefix `"EPIC-FX2: 7/8 children terminal"`, pending_children includes the Approved one.
3. Zero children — no children in archive or pending-sync → verdict `halt-zero-children`, halt_reason ends `"0 children drafted; not reconcilable — decompose or abandon"`.
4. Sub-epic recursion + DEFERRED exclusion — sub_epics: [SUB-A auto-flip, SUB-B Completed, SUB-C DEFERRED] → verdict `auto-flip`, coverage `full`, SUB-C excluded from denominator.
5. Already-terminal parent — status:Completed → verdict `no-op`, proposed_status null.
6. **Error path (mandated by §4.1):** sub_epics cycle (A→B→A) → throws `Error('parent-rollup: sub_epics cycle detected at <parent>')`.

**Reuse (no duplication):**
- Import `ARTIFACT_TERMINAL_STATUSES` from `./lifecycle-reconcile.js` (line 27). The set is the tolerant 8-element variant during the CR-067 migration window — DO NOT redefine it locally. STORY-067-03 (Wave 3) tightens it later; -066-01 reads through.
- Import `parseFrontmatter` from `cleargate-cli/src/wiki/parse-frontmatter.ts` (the actual exporter — verified by grep). NOTE: the story body §1.6 cites `frontmatter-yaml.ts` for `parseFrontmatter`; that file exports `serializeFrontmatter` only. Use `wiki/parse-frontmatter.ts` for the parse helper.
- Follow the predicate shape at `lifecycle-reconcile.ts:437` (`if (ARTIFACT_TERMINAL_STATUSES.has(status)) continue;`) for the terminal-skip branch.

**Gotchas surfaced from code inspection:**
- `cleargate-cli/src/wiki/parse-frontmatter.ts` is the parse helper; `cleargate-cli/src/lib/frontmatter-yaml.ts` only serializes. Story body §1.6 + §3.2 cite `frontmatter-yaml.ts` for `parseFrontmatter()` — this is wrong. Developer must import `parseFrontmatter` from `../wiki/parse-frontmatter.js`. (Source: `grep -rln parseFrontmatter cleargate-cli/src/` returns `src/wiki/parse-frontmatter.ts` + 9 consumers.)
- The terminal-status constant is named `ARTIFACT_TERMINAL_STATUSES` (verified at `cleargate-cli/src/lib/lifecycle-reconcile.ts:27`). The story's context_source notes this correction; Developer must use the verified name.
- For child enumeration, the story §3.2 step (2b) requires matching `archive/<child-id>-*.md` by parent-id PREFIX (e.g. `EPIC-016` → glob `archive/STORY-016-*.md`) AND by `parent_cleargate_id`/`parent_epic_ref` frontmatter equality. Both are required because BUG/CR children don't follow the prefix pattern (e.g. `archive/BUG-004*.md` would not match `EPIC-NNN`-prefix glob). Developer must implement both lookups; tests should cover at least one parent with a non-prefix-matching child.

**Risks (≤3):**
1. Test fixture frontmatter byte drift — if fixtures are authored via parseFrontmatter+serializeFrontmatter roundtrip, a one-blank-line strip can occur (FLASHCARD 2026-04-24 `#frontmatter #write-back`). Mitigation: hand-author fixture `.md` files as raw text; do NOT generate via the parse/serialize roundtrip.
2. Glob escaping on `pending-sync/EPIC-*.md` — `*` must not match `EPIC-016_SubEpic-NNN.md` if sub-epics live as siblings in pending-sync/. Mitigation: filter results by frontmatter `parent_epic_ref` after readdir; the glob is just a coarse pre-filter.
3. Cycle detection in walkActiveParents — `visited: Set<string>` must be passed through recursion, NOT scoped per-call. A naive per-call visited set lets B→A→B repeat with separate sets.

---

### STORY-067-01 — Migration Script + Tests + `.migration-lock` Primitive

**Cross-story coupling within M1:** None. Disjoint surfaces from -066-01 / -028-04 / -010-02.

**Predecessor for:** STORY-067-02 (Wave 2 archive migration application).

**Schema changes:** None on data; creates the conventional lock-file path `.cleargate/.migration-lock` (treated as transient — never committed; lifecycle owned by the script).

**Files to touch:**
| Action | Path | Notes |
|---|---|---|
| CREATE | `cleargate-cli/scripts/migrate-status-to-completed.mjs` | Pure `.mjs` script; argv-parsed; no exports. Follows sibling shape at `cleargate-cli/scripts/copy-planning-payload.mjs`. |
| CREATE | `cleargate-cli/scripts/migrate-status-to-completed.node.test.ts` | 6 fixture scenarios + 1 lock-respecting test (= 7 tests min). Builds tmpdir fixtures per test; invokes script via `spawnSync(process.execPath, [scriptPath, '--apply', '--delivery-root', tmpDir])`. |
| MODIFY | `cleargate-cli/src/commands/push.ts` | Insert ONE lock-check block at the top of `pushHandler` (story §3.2 step 2 specifies the location). Exit code 75 on lock-present; message: `Error: CR-067 migration in progress (.migration-lock held); retry in 30s`. |

**Test scenarios (from §2.1 Gherkin — 6 required):**
1. Rewrite `Done → Completed` — fixture pending-sync/STORY-FX1.md with `status: Done` → reads `status: Completed` after run; body bytes (after closing `---`) unchanged.
2. Rewrite `Verified → Completed` — archive/BUG-FX2.md with `status: Verified` → reads `status: Completed`.
3. Leave `Completed` unchanged — archive/STORY-FX3.md with `status: Completed` → file is byte-identical (no tmp+rename triggered — script must short-circuit when no substitution would fire).
4. Idempotency — run --apply twice; second run reports `Rewrote: 0 files`; every file byte-identical to post-first-run state.
5. Flag non-terminal stale (`Approved`, `Triaged`) without rewriting — files left alone; summary lists them under `Flagged for human review`.
6. Lock respected by concurrent push — script holds `.cleargate/.migration-lock`; `cleargate push <fixture>` exits 75 with message containing `CR-067 migration in progress`.

**Reuse (no duplication):**
- Atomic-write pattern: copy idiom from `cleargate-cli/src/commands/push.ts:411` `writeAtomic(filePath, content)` — `fs.writeFile(tmp, content); fs.rename(tmp, final)`. Do NOT call `writeAtomic` directly across packages (it's not exported); replicate the 2-line idiom inline.
- Argv-parsing + script shape: model after `cleargate-cli/scripts/copy-planning-payload.mjs` (sibling prebuild script).
- Do NOT call `parseFrontmatter` / `serializeFrontmatter` — per story §1.2 step 4 + FLASHCARD 2026-04-24 `#frontmatter #write-back`, raw-bytes regex-replace inside the first `---\n...\n---` block is mandatory to preserve byte-fidelity. Roundtrip strips one leading blank line.

**Gotchas surfaced from code inspection:**
- `push.ts` imports `parseFrontmatter` from `'../wiki/parse-frontmatter.js'` (line 33) and `serializeFrontmatter` from `'../lib/frontmatter-yaml.js'` (line 34). The lock-check insert must land BEFORE either of these is invoked. The earliest safe insertion point is the top of `pushHandler` (line 74) before the `await fs.readFile(...)` call near line ~200 (the frontmatter read). Developer: place the lock check immediately after the path validation block, before any file I/O. Exit code 75 = `EX_TEMPFAIL` on BSD-derived systems.
- `cleargate-cli/scripts/` extension is `.mjs` (verified by ls); test file must be `.node.test.ts` per Cross-Cutting Rule 1 (no two-runner state). Running `.node.test.ts` next to a `.mjs` script is fine — tsx loads both.
- The `.migration-lock` file is at `.cleargate/.migration-lock` (one level up from `<delivery-root>`, NOT inside it). Story §3.2 step 2 shows `path.join(opts.deliveryRoot, '..', '.migration-lock')` — this is correct relative to the default `--delivery-root .cleargate/delivery`. Developer: do not place the lock inside `delivery/` or it will appear as a tracked artifact in the migration's own commit.
- Stale-lock reclaim: story §3.2 step 2 says "if exists → check PID alive → reclaim or abort". `process.kill(pid, 0)` returns without error if PID alive, throws ESRCH if dead. Reclaim on ESRCH; abort otherwise.

**Risks (≤3):**
1. Regex matches `status:` inside a YAML quoted-multi-line body field accidentally — mitigated by §1.2 requirement "only rewrites within the first `---\n...\n---` block". Developer must slice the file at the second `---` and ONLY substitute within the head slice.
2. Quote-variant coverage — `status: "Done"`, `status: 'Done'`, `status: Done` all valid YAML. Regex per §3.2 step 4 handles all three (`["']?` optional). Test fixture 7 (per story §1.5 risk row) covers single-quoted — Developer: add ONE extra fixture beyond the 6 Gherkin if single-quoted case is not already represented. (Story §4.1 says 6 min; Developer may emit 7.)
3. `proper-lockfile` is NOT in `cleargate-cli/package.json` (verified by grep). Story §1.2 step 1 says "via `proper-lockfile` if available; else `fs.mkdirSync` exclusive flag". Default to `fs.writeFileSync(lockPath, String(process.pid), { flag: 'wx' })` — exclusive-write fails if file exists. Do NOT add `proper-lockfile` as a dep in this story (separate concern).

---

### STORY-028-04 — Vitest → node:test Codemod Tool

**Cross-story coupling within M1:** None. Disjoint surfaces from -066-01 / -067-01 / -010-02 (no shared files within M1).

**Predecessor for:** STORY-028-05, -06, -07 (per-package conversions, Wave 2).

**Schema changes:** None on data; adds **ONE new devDep** (`ts-morph`) to `cleargate-cli/package.json`.

**Files to touch:**
| Action | Path | Notes |
|---|---|---|
| CREATE | `cleargate-cli/scripts/codemod-vitest-to-node-test.mjs` | `.mjs` script; argv-parsed; uses `ts-morph` Project API. |
| CREATE | `cleargate-cli/scripts/codemod-vitest-to-node-test.node.test.ts` | 6 golden-fixture scenarios + idempotency + exit-code assertions (≥9 tests). |
| CREATE | `cleargate-cli/test/fixtures/codemod-vitest/{plain,matchers,hooks,async,spec-rename,vi-mock-flag}/{input.test.ts,expected.node.test.ts}` | 6 sub-dirs × 2 files = 12 fixture files. `spec-rename/input.spec.ts` is the exception (named `.spec.ts` to test the rename path). |
| MODIFY | `cleargate-cli/package.json` | Add `ts-morph` to `devDependencies`. **MUST** verify version via `npm view ts-morph version` before pinning (story §4.2 DoD + CR-037 pre-spec rule). Pin to caret of current registry version. |

**Test scenarios (from §2.1 Gherkin — 6 + idempotency + exit-code):**
1. AUTO-CONVERTIBLE plain `describe/it/expect/beforeAll` — output byte-identical (after prettier) to expected.node.test.ts; input.test.ts renamed; exit code 0.
2. All 8 matchers map correctly — `toBe`/`toEqual`/`toThrow`/`toBeUndefined`/`toBeNull`/`toBeTruthy`/`toBeFalsy` per §1.2 mapping table; each converts to documented `assert.*`.
3. `vi.mock` flags for manual fix — file NOT renamed; bytes unchanged; manual-fix report lists file with `vi.mock` api; exit code 1.
4. `.spec.ts` files convert and rename to `.node.test.ts` — input.spec.ts gone; only `.node.test.ts` remains.
5. Idempotency on already-converted — `.node.test.ts` using only node:test API is unchanged and not double-renamed.
6. Target collision — both `input.test.ts` AND `input.node.test.ts` exist → input.test.ts left alone; manual-fix report lists "target file already exists"; exit code 1.

**Reuse (no duplication):**
- Script shape: model after `cleargate-cli/scripts/backfill-sprint-reports.mjs` + `cleargate-cli/scripts/copy-planning-payload.mjs` (argv parsing, top-level await, no exports).
- Output reference shapes: existing 53 `*.node.test.ts` files in `cleargate-cli/test/` (per story §1.6) provide ground-truth examples. Pick 2-3 of these as inspiration for fixture `expected.node.test.ts` formatting (especially import-line layout).
- Prettier: invoke via `require('prettier').format(text, prettierConfig)` AFTER `sourceFile.saveSync()` and BEFORE the rename — story §1.5 risk row mandates this to avoid trailing-comma / quote-style drift breaking fixture equality.

**Gotchas surfaced from code inspection:**
- `ts-morph` is NOT in cleargate-cli/package.json devDeps (verified by grep). Adding it is a clean addition. Per CR-037, run `npm view ts-morph version` first; pin to caret of the returned version. **Do not** trust training-data memory for the version.
- Test runner: tests are `.node.test.ts` from birth (Cross-Cutting Rule 1). Tests invoke the codemod via `spawnSync(process.execPath, [scriptPath, '--apply', '--root', tmpDir])`; do NOT import the script module directly because it's `.mjs` and has top-level argv side-effects.
- Exit-code contract: §1.2 step 9 says exit 1 if ANY MANUAL-FIX-REQUIRED files; the per-package stories (-028-05/-06/-07) gate on this at preflight per §EPIC-028 §5. Developer's tests MUST assert exit code via `result.status` from spawnSync (not just stdout content).
- Glob exclusion: story §3.2 step 2 says "skip node_modules + dist". Add `cleargate-cli/examples/` to the skip list per FLASHCARD `#fixtures #sprint-22` (intentionally-failing Red examples in `cleargate-cli/examples/`). Wave 2's -028-06 walks `cleargate-cli/test/` only, so this skip is defensive but cheap — add it now.
- AST-text replacement vs raw-text: `ts-morph` `Identifier.replaceWithText('test')` is safe for the `it→test` rename ONLY when the identifier is a CallExpression callee — never within string literals. Use `sourceFile.getDescendantsOfKind(SyntaxKind.Identifier)` filtered by `parent.kind === CallExpression && parent.expression === identifier`.
- `expect(...).matcher(...)` rewrite: ts-morph `CallExpression.replaceWithText(newCode)` replaces the whole call expression. The replacement string must be syntactically valid TypeScript (otherwise next save throws). For `toBe`/`toEqual`/etc the swap is mechanical; for unrecognized matchers, classify the file as MANUAL-FIX-REQUIRED and bail BEFORE saving (the partial in-memory edit is discarded on `project.removeSourceFile(file)`).

**Risks (≤3):**
1. Fixture format drift between `expected.node.test.ts` (hand-authored) and codemod output (prettier-formatted). Mitigation per story §1.5: prettier-format BOTH sides before `assert.strictEqual`. Fixture files must commit AS THEY APPEAR after prettier.
2. `vi.hoisted()` / `vi.stubGlobal` / `vi.useFakeTimers` not in §1.2's flagged-api list explicitly — story §1.2 step 3 mentions them as MANUAL-FIX-REQUIRED triggers. Developer: ensure the AST scan flags ANY property access on a `vi` identifier (not just the 6 named members in §1.2 step 3); use `PropertyAccessExpression.getExpression().getText() === 'vi'`.
3. ts-morph TypeScript version mismatch with the repo's TS 5.8 — pin `ts-morph` to a version compatible with TS 5.8+. The latest ts-morph (verify via `npm view ts-morph peerDependencies`) bundles TS 5.x. If preview shows it pins to TS 4.x peer, fall back to the latest TS-5-compatible release.

---

### STORY-010-02 — Four New MCP Endpoints + Generic `PmAdapter` Interface

**Cross-story coupling within M1:** None for code edits. Per SDR §2.2, `mcp/src/adapters/README.md` is shared with STORY-067-03 (Wave 3 appends a Status Vocabulary Mapping section) and `mcp/package.json` may be shared with -028-05 + -028-08. Wave 1 dispatch order is correct: -010-02 first (additive), Wave 2/3 follow.

**Predecessor for:** STORY-067-03 (appends to the README -010-02 creates) — Wave 3.

**Schema changes:** None (no DB migration; the endpoint handlers read existing `items` table via existing Drizzle schema at `mcp/src/db/schema.js`).

**⚠️ STATUS DISCOVERY — see §"Open decisions for orchestrator" below. Codebase grep shows all primary deliverables already exist on disk and were committed in mcp/ as `315af63 feat(epic-010): STORY-010-02 MCP pull/list/comments/detect tools + PmAdapter` (commit found via `git log -- src/adapters/pm-adapter.ts`). The story's `pending-sync/` frontmatter still reads `status: Draft` — this is a stale-frontmatter case, not a green-field implementation task.**

**Files (verified state on disk, not "to touch"):**
| Path | Disk state | Notes |
|---|---|---|
| `mcp/src/adapters/pm-adapter.ts` | EXISTS | Interface defined: `PmAdapter`, `PmToolName`, `RemoteItem`, `RemoteUpdateRef`, `RemoteComment`. Matches story §1.2 spec verbatim. |
| `mcp/src/adapters/linear-adapter.ts` | EXISTS | `LinearAdapter` class implementing `PmAdapter`; DI-friendly constructor; `mapIssue` + `mapComment` helpers; `isNotFoundError` returns null on Linear 404. |
| `mcp/src/adapters/index.ts` | EXISTS | Barrel export + `buildAdapter(config)` factory; falls back to a `noopAdapter` when `LINEAR_API_KEY` is absent. |
| `mcp/src/adapters/README.md` | EXISTS | R1 validation findings for Linear / Jira / GitHub Projects + adapter-author checklist. |
| `mcp/src/tools/cleargate-pull-item.ts` | EXISTS | Returns `{ error: 'not_found' }` on null (per story §2.1 scenario 2). |
| `mcp/src/tools/cleargate-list-remote-updates.ts` | EXISTS | Lightweight ref shape. |
| `mcp/src/tools/cleargate-pull-comments.ts` | EXISTS | Read-only. |
| `mcp/src/tools/cleargate-detect-new-items.ts` | EXISTS | Server-side dedup against `items.remote_id` via Drizzle. |
| `mcp/src/mcp/register-tools.ts` | MODIFIED (already) | Lines 231 / 249 / 267 / 301 register the 4 tools. Verified by grep. |
| `mcp/src/tools/*.test.ts` | EXISTS | Test files present for all 4 endpoints + adapter (`pm-adapter.test.ts`, `linear-adapter.test.ts`). |
| `mcp/test/fixtures/linear/` | UNKNOWN — story §3 lists this; spot-check via `ls`. May or may not exist. |

**Test scenarios (per §2.1 Gherkin — 8 scenarios). If story is re-routed to "verify-only" path, Developer's job becomes:**
1. Run `npm run typecheck` + `npm test` in `mcp/` — confirm green.
2. Spot-check `register-tools.ts` lists all 4 tools.
3. Confirm test files cover all 8 Gherkin scenarios; if any are missing, write the gap-fill tests.
4. Confirm `mcp/src/adapters/index.ts` exports `PmAdapter` (verified: line 1 `export * from './pm-adapter.js'`).

**Reuse (no duplication):**
- All adapter + endpoint surfaces already exist; no new infrastructure to add.
- `buildAdapter` factory at `mcp/src/adapters/index.ts:16` provides DI for tests — Developer tests must use this pattern (NOT direct `LinearClient` instantiation) per story §1.2 risk + the FLASHCARD 2026-04-18 `#cli #vitest #vi-mock-hoisting`.
- Existing `runTool(ctx, '<name>', payload, fn)` helper at `mcp/src/mcp/register-tools.ts` wraps the auth + error normalisation — all 4 endpoints already use it (verified at lines 240, 258, 276, 311).

**Gotchas surfaced from code inspection:**
- **Story is paper-stale.** The implementation shipped 2026-04-20 in mcp/ commit `315af63`. The pending-sync file has `status: Draft, approved: true` — the approval predates the merge but the status flip never happened. Per CR-066 reconciler logic (once -066-02 ships), this story will auto-flip when -028-01 dogfood runs. Surface this to orchestrator (see §"Open decisions" below).
- Story §3 "Files touched" lists `mcp/src/endpoints/` as the target directory — **this directory does not exist**. The actual endpoint files live at `mcp/src/tools/cleargate-*.ts`. The story spec is wrong; the actual disk layout is the authoritative one.
- Story §1.2 specifies `PmAdapter.pullItem(remoteId): Promise<RemoteItem>` (non-null). The shipped interface at `pm-adapter.ts:41` is `Promise<RemoteItem | null>` — null is returned on not-found instead of throwing. This is BETTER than the spec (avoids 404-as-exception), and the endpoint handler at `cleargate-pull-item.ts:28` translates null → `{ error: 'not_found' }`. No conflict, but Developer/QA should note that the on-disk contract is more permissive than the story prose.
- Story §1.2 specifies `PmToolName = "linear" | "jira" | "github-projects"`. Shipped at `pm-adapter.ts:11` includes a 4th: `'no-adapter-configured'` (the noop stub literal). Acceptable extension; matches the `index.ts:28` stub usage.

**Risks (≤3):**
1. **Mis-dispatch risk** — if orchestrator dispatches a Developer to this story without reading this plan, the Developer will discover the work is done, then either (a) waste a turn re-writing what exists, or (b) flag the situation. Mitigation: orchestrator reads §"Open decisions" below and re-routes to "audit + flip-to-Completed" before any Developer spawn.
2. Test coverage gap — story §4.1 requires 8 endpoint unit tests + 4 integration tests + 1 auth test (13 total). On disk: 4 endpoint test files exist. Developer/QA verification step must enumerate Gherkin scenarios against existing tests; gap-fill if any scenario unmapped.
3. `mcp/test/fixtures/linear/` directory existence not verified at plan time. If absent and integration tests require recorded fixtures, this is the only net-new artifact. Developer/QA: `ls mcp/test/fixtures/linear/` early; if missing, escalate before drafting fixtures (recording requires a Linear sandbox).

---

## Cross-story risks (within M1)

- **None on shared files within M1.** Verified per §"Dispatch order" — the four stories' primary edit surfaces do not overlap. The only shared concerns are:
  - `cleargate-cli/package.json` is touched ONLY by -028-04 in this milestone (adds `ts-morph`). -067-01 and -066-01 do not touch package.json. -010-02 lives in `mcp/` — different package.
  - `cleargate-cli/src/lib/lifecycle-reconcile.ts` is touched ONLY by -066-01 in M1 (one re-export line). -067-03's tightening of ARTIFACT_TERMINAL_STATUSES is Wave 3 — not in M1.
  - `mcp/src/adapters/README.md` is touched ONLY by -010-02 in M1 (already exists on disk; nothing to do). -067-03's append is Wave 3 — not in M1.
- **Test fixture isolation.** -066-01's fixtures live under `cleargate-cli/test/fixtures/parent-rollup/`; -028-04's under `cleargate-cli/test/fixtures/codemod-vitest/`. Disjoint trees. Developers must not cross-reference each other's fixtures.
- **No order required.** Any sequence of dispatch within M1 is safe. Orchestrator may launch all four in one wave or stagger as throughput allows.

## Open decisions for orchestrator

1. **STORY-010-02 work appears already shipped (mcp/ commit `315af63`, 2026-04-20).** Pending-sync frontmatter still reads `status: Draft`. Three routing options for orchestrator:
   - **(a) Re-dispatch as verify-only Developer task** — Developer runs typecheck + tests in mcp/, confirms the 8 Gherkin scenarios are covered, and (if green) commits a status-flip of the pending-sync frontmatter to `status: Completed` + moves to `archive/`.
   - **(b) Skip the Developer entirely** — treat as already-done; let STORY-028-01 dogfood reconciler (Wave 3) auto-flip the status when CR-066's `--parents` flag runs against EPIC-010.
   - **(c) Re-dispatch to Architect** to update the story body to match the on-disk reality before any other action.
   - **Recommendation:** Option (a). The pending-sync prose has at least three drift points from the on-disk implementation (`endpoints/` vs `tools/`, nullable `pullItem` return, extra `PmToolName` literal). A Developer verification pass closes the audit cleanly AND surfaces any missing test coverage before SPRINT-28 close. Option (b) leaves the audit ambiguous; option (c) is wasted Architect turn given the implementation is correct.

2. **STORY-010-02 fixture directory `mcp/test/fixtures/linear/`** — not verified at plan time. Whichever option above is chosen, the Developer/Verifier must `ls` this directory early; absence may trigger a sub-CR for recorded-fixture authoring.

3. **STORY-028-04's open question** (story §1.4): "EPIC-028 §5 scenario 8 (CI pre-commit hook fails when vitest reappears) — owned by -028-04 or -028-08?" Story author recommends -028-08 (docs). **Architect concurs**: the hook is repo-policy enforcement, not codemod logic; -028-08 owns the docs/agent prompt + the pre-commit grep line. Confirm at orchestrator dispatch — no code change in -028-04 for this scenario.

4. **`ts-morph` version pin** (STORY-028-04): CR-037 mandates `npm view ts-morph version` BEFORE pinning. Orchestrator's dispatch prompt to Developer should include the literal command and require the Developer to record the resolved version in their commit message footer (per CR-037 §rule 3).

---

## Sprint-context updates (Architect post-plan)

No `## Mid-Sprint Amendments` entries from this milestone. No CR:scope-change or CR:approach-change occurred during M1 planning. The Adjacent Implementations table in `sprint-context.md` will populate as Wave 1 stories merge (orchestrator/Architect appends per CR-017 §11).

---

## Verification cheatsheet (paste-ready for Developer dispatch briefs)

- STORY-066-01: Test file MUST be `parent-rollup.node.test.ts`. Import `parseFrontmatter` from `../wiki/parse-frontmatter.js`, NOT `frontmatter-yaml.ts`. Import `ARTIFACT_TERMINAL_STATUSES` from `./lifecycle-reconcile.js`. 6 tests minimum (5 Gherkin + 1 cycle).
- STORY-067-01: Test file MUST be `migrate-status-to-completed.node.test.ts`. Lock-check insert in `push.ts` lands at top of `pushHandler` (line ~74), exit code 75. Raw-bytes regex, NEVER parseFrontmatter+serializeFrontmatter roundtrip. 7 tests minimum (6 Gherkin + lock-respect).
- STORY-028-04: `npm view ts-morph version` before pinning. 6 fixture pairs under `test/fixtures/codemod-vitest/`. Prettier-format both sides of `assert.strictEqual`. Tests assert exit code via `spawnSync` `.status`. 9 tests minimum (6 + idempotency + 2 exit-code).
- STORY-010-02: **Halt before dispatch.** Orchestrator decides routing per §"Open decisions" #1 above. If Option (a), Developer runs `cd mcp && npm run typecheck && npm test`, confirms register-tools.ts lines 231/249/267/301 register the 4 tools, then flips story frontmatter `status: Completed` and archives. If Option (b), no Developer dispatch; STORY-028-01 handles.
