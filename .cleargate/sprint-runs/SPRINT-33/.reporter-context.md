# Reporter Context Bundle — SPRINT-33

_Generated: 2026-06-01T15:34:58.501Z_

---

## Sprint Plan Slices

_Source: SPRINT-33_Framework_Hygiene_And_Gate_Correctness.md_

## 1. Consolidated Deliverables

## 2. Execution Strategy (SDR-validated)

## State.json Summary

Sprint: SPRINT-33 | Status: Active

CR-070 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-043-01 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-043-02 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-043-03 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-043-04 | Done | standard | qa_bounces=0 arch_bounces=3
STORY-043-05 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-043-06 | Done | standard | qa_bounces=0 arch_bounces=0
STORY-043-07 | Done | standard | qa_bounces=0 arch_bounces=1
STORY-043-08 | Done | standard | qa_bounces=0 arch_bounces=1
STORY-043-09 | Done | standard | qa_bounces=0 arch_bounces=1
STORY-043-10 | Done | standard | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

# Milestone: M1 — Gate Correctness
## Stories: CR-070 (closure), STORY-043-01, STORY-043-02, STORY-043-03, STORY-043-04, STORY-043-05
## Wave: W1 (SERIAL — execution_mode: v2)

> Sprint goal: make every gate fire on the right signal, retire stale scaffold debt, add a sprint-end consolidation pass. M1 is the integrity core — gate false-negatives → 0; flashcard gate enforces again. All paths Read/grep-verified against the working tree on 2026-06-01. SDR §2 (in the sprint plan) is binding on merge order, shared surfaces, and the deferred live-loop re-sync.

## Order

Strict serial within M1, this exact sequence:

1. **CR-070 closure** — thin. Substance already shipped (commit `b87f6ac0`). First only for ledger cleanliness; its blocking dependency for 043-01 is **already satisfied in-tree** (`init_sprint.mjs` no longer writes `execution_mode`; schema is v3).
2. **STORY-043-01** — sentinel fail-closed. Consumes the post-CR-070 world.
3. **STORY-043-02** — predicate heading-text anchoring. *(parallel_eligible: y, but serialized under v2)*
4. **STORY-043-04** — register `hotfix` type. *(parallel_eligible: y, but serialized under v2)*
5. **STORY-043-03** — template de-number. **MUST land AFTER 043-02** (heading-text anchoring before de-numbering; both fix the same root bug from opposite sides, no file collision — different files).
6. **STORY-043-05** — close hardening + reporter v2 + flashcard curation. Independent within M1.

**Parallelizable in principle (but serialized this sprint):** 043-02 ‖ 043-04 touch disjoint files (`readiness-predicates.ts` vs `work-item-type.ts` + `readiness-gates.md`). The only hard ordering edge is **043-02 → 043-03**.

**No shared-file collisions inside M1.** SKILL.md / qa.md three-way and two-way collisions are all M2/M3/M4 (043-06/-08/-09/-10) — not this milestone. DevOps does not need rebase-chaining for M1.

---

## Per-story blueprint

### CR-070 (thin closure)

- **Goal:** Close the CR by adding its one missing deliverable (a non-Red unit test for `isAdvisory()`) and verifying the already-merged in-tree state via grep gates. Advances goal: completes the execution_mode collapse so the gate-semantics invariant has exactly one downgrade lever.
- **Cross-story coupling:** Unblocks 043-01 on paper. **Dependency already holds** — do NOT re-implement any CR-070 substance.
- **CRITICAL FINDING — the dispatch asks for `cleargate-cli/test/util/gate-mode.test.ts`, but `cleargate-cli/test/util/gate-mode.red.node.test.ts` already exists** (QA-Red authored for STORY-070-01) and already covers exactly the four `isAdvisory()` cases the dispatch enumerates ('1' true; unset/'0'/'true'/'yes'/'' false). That Red file is **sealed/immutable** per CR-043 naming.
  - **Decision for the Developer:** add a *new, mutable, green-lane* unit test at `cleargate-cli/test/util/gate-mode.node.test.ts` (note: `.node.test.ts`, NOT `.red.node.test.ts` — the Red file is immutable and is QA-Red's, not the Developer's). It re-asserts the same four+ truthiness cases as an owned regression test so CR-070 §3 line 164 ("`cleargate-cli/test/util/gate-mode.test.ts` — new file. Cover both `CLEARGATE_ADVISORY=1` and unset") is satisfied by a node:test file the Developer owns. Use `tsx --test`; mirror the `withEnv` save/restore pattern from the existing Red file so no env leaks across cases.
  - This is NOT a TDD Red→Green cycle — `gate-mode.ts` already exists and passes. This is a closure regression test. Skip TPV for this story (no QA-Red Red authoring step is needed for an already-green module).
- **Files to touch:**
  - `cleargate-cli/test/util/gate-mode.node.test.ts` — **new file** (the deliverable). NOT scaffold-mirrored (test files are excluded from the payload by `check:no-execution-mode-vocabulary`'s `--exclude-dir=test`).
- **Schema / type / signature deltas:** none. `isAdvisory(): boolean` is unchanged (`cleargate-cli/src/util/gate-mode.ts`). Schema is already v3.
- **Test shape for QA-Verify (no Red needed):** node:test, `describe`/`it`, `assert.strictEqual`. Six assertions minimum: `isAdvisory()===true` for `'1'`; `===false` for unset, `'0'`, `'true'`, `'yes'`, `''`. Save+restore `process.env['CLEARGATE_ADVISORY']` per case.
- **In-tree verification grep gates (CR-070 §4 — run as part of acceptance, do NOT re-fix):**
  - Test 1: `grep -m1 '^execution_mode:' '.cleargate/templates/Sprint Plan Template.md'` → zero matches.
  - Test 2: `.cleargate/scripts/state.schema.json` → `properties.execution_mode === undefined` AND `properties.schema_version.const === 3` (verified: line 5 description states v3; field absent).
  - Test 5: `cd cleargate-cli && npm run check:no-execution-mode-vocabulary` → exit 0 (verified wired at `package.json:56`).
- **Gotchas (file:line):**
  - `cleargate-cli/test/util/gate-mode.red.node.test.ts:1` — the Red file already exists and is immutable. Developer MUST NOT edit it and MUST NOT name the new file `*.red.node.test.ts`.
  - FLASHCARD `.cleargate/FLASHCARD.md:4` — CR-070 pending-sync file is a STALE artifact describing done work; do not act on its §3 "Modify" list beyond the one missing test. The schema bump, `gate-mode.ts`, enforcement §15, unconditional scripts, and the npm check are all already in main.
  - After closure: move the CR file `pending-sync/ → archive/`. (Conversational-orchestrator action at Gate-4, not the Developer's commit.)

### STORY-043-01 (flashcard sentinel fail-closed)

- **Goal:** Make `pending-task-sentinel.sh` block (`exit 1`) by default when unprocessed flagged flashcards exist; remove the inert `execution_mode` read; `CLEARGATE_ADVISORY=1` is the SOLE downgrade. Advances goal: repairs WS8(a) "gate that does not gate."
- **Cross-story coupling:** Consumes CR-070's post-strip world. Touches no `state.schema.json` / script line CR-070 owns — only the *consumer read* in this one hook. Disjoint from 043-02/-03/-04/-05.
- **Files to touch (BINDING per orchestrator decision 2026-06-01, owner-approved — supersedes the prior "edit live + accept mid-sprint change" directive):**
  - `cleargate-planning/.claude/hooks/pending-task-sentinel.sh` (CANONICAL, tracked, in-worktree) — **the SINGLE edited tracked copy and the source of truth.** This is BOTH the Developer's edit target AND the tests' target. The Developer edits ONLY this file for the hook change.
  - `.cleargate/scripts/test/test_flashcard_enforcement.sh` — rework the test harness scenarios AND **re-point the hook-under-test to the in-worktree canonical sibling** (see Test-target resolution directive below).
  - `.cleargate/scripts/test/test_flashcard_fail_closed.red.sh` — the sealed Red shell test (QA-Red authors it) MUST also resolve the hook-under-test to the in-worktree canonical path, NOT `${GIT_ROOT}/.claude/hooks/…`.
  - **NOT edited in this diff (deferred):**
    - `cleargate-cli/templates/cleargate-planning/.claude/hooks/pending-task-sentinel.sh` (payload) — **do NOT hand-edit.** Regenerated from canonical by `npm run prebuild` (DevOps, post-merge). Payload byte-parity is a post-merge `prebuild` concern, not a within-story diff obligation.
    - `.claude/hooks/pending-task-sentinel.sh` (LIVE, main-repo, gitignored) — **NOT edited mid-sprint.** This is the hook the orchestrator's own dispatch loop runs (live-loop hazard). Live re-sync via `cleargate init` is DEFERRED to Gate-4 doc-refresh. Do NOT require live==canonical inside the story diff.
- **Test-target resolution directive (BINDING — QA-Red + Developer both follow; supersedes harness line 37):**
  - **Mechanism (a), RECOMMENDED and adopted:** both `test_flashcard_enforcement.sh` and `test_flashcard_fail_closed.red.sh` compute the hook-under-test path **relative to the test script's own location**, landing on the sibling canonical hook WITHIN the same checkout — NOT via `_find_git_root`/`${GIT_ROOT}`. Concretely, replace harness line 37 `LIVE_HOOK="${GIT_ROOT}/.claude/hooks/pending-task-sentinel.sh"` with a script-relative resolve: from `SCRIPT_DIR` (line 18, `.cleargate/scripts/test/`) walk up to the checkout root (`SCRIPT_DIR/../../..`, already `REPO_ROOT` at line 19) and target `${REPO_ROOT}/cleargate-planning/.claude/hooks/pending-task-sentinel.sh`. **Delete/retire the `_find_git_root` walk-PAST-worktree logic (lines 21-37) for hook resolution** — it currently resolves to the MAIN-repo LIVE gitignored hook, which is the wrong target for a self-hosting hook change tested inside a worktree.
  - **Mechanism (b), allowed fallback:** accept a `PENDING_TASK_SENTINEL_HOOK` env override. Default to today's `${GIT_ROOT}/.claude/hooks/…` for normal CI on the main checkout; QA sets `PENDING_TASK_SENTINEL_HOOK=${REPO_ROOT}/cleargate-planning/.claude/hooks/pending-task-sentinel.sh` for THIS story. Use (b) only if (a) breaks normal-CI runs on the main checkout where canonical and live legitimately differ.
  - **Adopt (a).** Rationale: the worktree contains the tracked canonical sibling but NO `cleargate-cli/` payload and a gitignored `/.claude/`, so a script-relative resolve is unambiguous and needs no env plumbing. Verified: harness `SCRIPT_DIR`/`REPO_ROOT` already exist (lines 18-19); canonical sibling present (`diff`-verified 2026-06-01); `cleargate-cli/` payload + `/.claude/` are gitignored in the worktree.
- **Schema / signature deltas (exact, verified lines — apply to the CANONICAL hook):**
  - **Delete** `pending-task-sentinel.sh:54-58` — the `EXEC_MODE="v1"` init and `jq -r '.execution_mode // "v1"'` read (lines 55, 57, 58).
  - **Line 134-135:** drop the `(mode=%s)` suffix and the `"${EXEC_MODE}"` printf arg from the `[%s] flashcard-gate: %d unprocessed card(s) found` log line.
  - **Lines 136-157:** replace `if [[ "${EXEC_MODE}" == "v2" ]] … else (v1 advisory) …` with: `if [[ "${CLEARGATE_ADVISORY:-0}" == "1" ]]` → print `FLASHCARD GATE WARNING` (drop the `(v1 advisory)` parenthetical at line 149) and fall through (continue to sentinel write); `else` → print `FLASHCARD GATE BLOCKED` and `exit 1`. The per-card `card:` / `mark processed:` loop is shared by both arms (today's two loops are identical — factor or duplicate).
  - **Preserve unchanged:** the `SKIP_FLASHCARD_GATE=1` early bypass (line 53 guard), the `_off-sprint` short-circuit (lines 43, 53), the `flashcards_flagged` parsing loop, and the `{ … } 2>> "${HOOK_LOG}"` token-ledger block below the gate.
  - **Final bypass precedence:** `SKIP_FLASHCARD_GATE=1` > `_off-sprint` > `CLEARGATE_ADVISORY=1` (warn) > default (block, exit 1).
- **Test shape for QA-Red** — `*.red.node.test.ts` is the wrong harness here; this is a **shell-integration** gate. The story's own test is `.cleargate/scripts/test/test_flashcard_enforcement.sh` (bash, not node:test); the sealed Red shell test is `.cleargate/scripts/test/test_flashcard_fail_closed.red.sh`. **Both resolve the hook-under-test to the in-worktree canonical path per the Test-target resolution directive above (mechanism a).** QA-Red authors the failing scenarios:
  - S1 (block by default): synthetic `state.json` with NO `execution_mode`, one unprocessed flagged card → assert `exit 1` + `FLASHCARD GATE BLOCKED` on stderr. **Fails against clean baseline because the current canonical hook always takes the v1-advisory arm (exit 0).**
  - S2 (processed marker): `.processed-<hash>` present → `exit 0` + sentinel written.
  - S3 (advisory): `CLEARGATE_ADVISORY=1` + unprocessed card → `exit 0` + `FLASHCARD GATE WARNING`.
  - S4 (empty list): `flashcards_flagged: []` → `exit 0`, no gate output.
  - `mk_sprint` must drop the `execution_mode` field from the synthetic state.json; add an `invoke_hook_advisory` helper (exports `CLEARGATE_ADVISORY=1`).
  - There is no DB here — real-infra rule is N/A; just real bash + temp dirs.
- **Reuse (no duplication):** the existing flashcard-parsing loop and all stderr/log format strings — reuse verbatim. No net-new abstraction. `gate-mode.ts:isAdvisory()` is the TS sibling of this hook's `CLEARGATE_ADVISORY` check — consistent lever, but the hook reads the env directly (it is bash, not TS).
- **Gotchas (file:line):**
  - **LIVE-LOOP HAZARD (CR-024 class) — RESOLVED by worktree isolation (orchestrator decision 2026-06-01, owner-approved).** The MAIN-repo LIVE hook `.claude/hooks/pending-task-sentinel.sh` IS the hook this sprint's own dispatch loop runs, AND it is what the harness targeted today via `_find_git_root`→`${GIT_ROOT}` (verified: harness line 37 + invocation line 133). The decision removes the hazard at the root: **the Developer edits the in-worktree CANONICAL hook ONLY, and the tests target that same in-worktree canonical sibling (mechanism a) — neither the Developer's edit nor the tests touch the MAIN-repo LIVE hook.** Therefore the orchestrator's own loop is NOT perturbed mid-sprint; there is no need to edit the live copy and no need for `CLEARGATE_ADVISORY=1` break-glass for THIS story. Live re-sync (`cleargate init`) is DEFERRED to Gate-4 doc-refresh; payload regeneration is a post-merge `prebuild` step (DevOps).
  - **SCAFFOLD DEBT (candidate follow-up, do NOT expand scope now):** the flashcard-enforcement tests targeting the live/gitignored hook via `${GIT_ROOT}`/`_find_git_root` is scaffold debt that breaks self-hosting hook changes tested inside a worktree — this story re-points to canonical, but a durable fix (env-override default + harness convention) for ALL hook self-test harnesses is a candidate follow-up item.
  - `.cleargate/FLASHCARD.md:4` confirms `execution_mode` is already stripped from `state.json` — so the inert read at line 57 already returns the `// "v1"` default every time. This is the dead read being removed.
  - Do NOT touch `state.schema.json` in this diff (CR-070 owns it; DoD asserts zero schema edit here).
- **Acceptance trace + DoD:**
  - Gherkin S1 (unprocessed blocks) → `test_flashcard_enforcement.sh` S1 asserts `exit 1` + `BLOCKED` + names card + `.processed-<hash>` hint (against the in-worktree canonical hook).
  - S2 (processed proceeds) → harness S2 asserts `exit 0` + sentinel written.
  - S3 (advisory warns) → harness S3 asserts `CLEARGATE_ADVISORY=1` → `exit 0` + `WARNING`.
  - S4 (Error path: stale execution_mode ignored) → harness variant with `state.json` carrying `"execution_mode":"v1"` + unprocessed card, no env → still `exit 1`. QA-Verify greps the CANONICAL hook for `execution_mode`/`EXEC_MODE` → zero matches.
  - **DoD byte-parity (BINDING, narrowed):** within THIS story's diff, verify `cleargate-planning/.claude/hooks/pending-task-sentinel.sh` (canonical) is the SINGLE edited tracked copy. **Do NOT require live==canonical inside the story diff** — payload byte-parity is a post-merge `npm run prebuild` concern (DevOps); live parity is a Gate-4 `cleargate init` doc-refresh item. QA-Verify confirms: (1) canonical is the only tracked hook touched (`git diff --name-only` shows no `cleargate-cli/templates/...` hook and no live hook); (2) both test scripts resolve to the in-worktree canonical path (grep the scripts for `cleargate-planning/.claude/hooks` resolution, and for ABSENCE of `${GIT_ROOT}/.claude/hooks` as the hook target).

### STORY-043-02 (readiness predicate heading-text anchoring)

- **Goal:** Anchor the Existing-Surfaces / Why-not-simpler heading predicates on heading TEXT (number- and H-level-tolerant), so template numbering can never silently fail `gate check`. Advances goal: durable predicate-side fix for the recurring self-inflicted heading bug (FLASHCARD 41).
- **Cross-story coupling:** **Logical dependency, NOT a file collision, with 043-03.** 043-02 fixes the predicate side (`readiness-predicates.ts`); 043-03 fixes the template side (de-numbering). Different files — no merge conflict. 043-02 lands first so 043-03's de-numbering becomes cosmetic-but-still-correct against the already-tolerant predicate.
- **Files to touch:**
  - `cleargate-cli/src/lib/readiness-predicates.ts` — the single source file.
  - `cleargate-cli/test/lib/readiness-predicates.node.test.ts` — extend with anchoring cases.
  - *(NOT scaffold-mirrored — `cleargate-cli/src/**` is product code, not the planning scaffold payload.)*
- **Schema / signature deltas (exact, verified lines):**
  - **New helper** `headingTitleOf(line: string): string | null`: trim; if not starting with `#`, return `null`; strip leading `#`-run + spaces; strip optional numeric prefix `^\d+(\.\d+)*\.?\s+`; return remaining trimmed. (`## 3.5 Existing Surfaces` → `Existing Surfaces`; `### Why not simpler?` → `Why not simpler?`.) Recognized titles for the heading-needle branch: `Existing Surfaces`, `Why not simpler?`, `Technical Grounding`, `Reproduction Protocol`.
  - **`evalBodyContains` (function at `readiness-predicates.ts:345`):** currently does `body.indexOf(needle)` (verified: line ~359 `while ((pos = body.indexOf(needle, pos)) !== -1)`). Add a needle-is-heading branch: if `headingTitleOf(needle) !== null`, scan body lines, compute `headingTitleOf` per line, match when a line's title equals the needle's title; feed the existing `present`/negated/`detail` shape (occurrence count + `§` section context). If needle is NOT a heading → fall through to today's literal `indexOf` scan UNCHANGED (no regression for `body contains 'STORY-'`, `'Scenario:'`).
  - **`evalExistingSurfacesVerified` (function at `readiness-predicates.ts:708`):** replace the locator at **line 720** `if (part.startsWith('## Existing Surfaces'))` with a check that the part's first line's `headingTitleOf` equals `"Existing Surfaces"`. Everything downstream (PATH_RE at ~L740, sentinel handling) is UNCHANGED.
  - Numeric-prefix tolerance forms: `N `, `N. `, `N.N `, `N.N.N ` (e.g. `0.5`, `3.5`, `3.6`).
  - **Closed-set parser (`parsePredicate`) and the 7 predicate shapes are UNCHANGED** — only two evaluators' heading resolution changes. The positional `section(N)` counters (`implementation-files-declared`/`dod-declared`/Bug `section(2)`) are untouched.
- **Test shape for QA-Red** — `cleargate-cli/test/lib/readiness-predicates.node.test.ts` (node:test, `tsx --test`). New cases that fail against clean baseline (baseline `indexOf`/`startsWith` reject numbered headings):
  1. `body contains '## Existing Surfaces'` against a body with `## 3.5 Existing Surfaces` → expect PASS (baseline FAILS).
  2. `body contains '## Why not simpler?'` against `### Why not simpler?` → expect PASS (baseline FAILS).
  3. Plain-substring needle (`body contains 'Scenario:'`) against a body lacking it → expect FAIL with literal behaviour (regression guard — must still work).
  4. `existing-surfaces-verified` against a doc whose only reuse section is `## 3.5 Existing Surfaces` citing a real path → locator selects it and verifies (baseline routes to absent section, returns not-applicable PASS — assert it actually *finds and verifies* the section, e.g. a bad path in a numbered section now FAILS).
  - Plus the false-positive guard: `## Existing Surfaces` text appearing in a PROSE line (not a heading) must NOT match (heading-title anchoring requires a real heading line).
- **Reuse (no duplication):** one shared `headingTitleOf` helper consumed by BOTH evaluators — one definition, not two divergent regexes (DoD-enforced). Reuse the existing `body.split(/^(?=## )/m)` part-splitting (line ~716) and the `evalSection` lookahead pattern (referenced at L464).
- **Gotchas (file:line):**
  - `readiness-predicates.ts:719` comment currently says "match literal `## Existing Surfaces` (not numbered heading)" per CR-033 — this comment must be updated/removed when the locator changes, or it lies about the new behaviour.
  - FLASHCARD `.cleargate/FLASHCARD.md:44,151,153,254` — the heading-bug history. Card 254 warns the `section` evaluator splits on `/^(?=## )/m` and `rawParts[0]` is section-1 when body starts with `##` — `headingTitleOf` on a section's *first line* must handle a preamble-free split.
  - **AND-semantics regression risk (FLASHCARD 132):** changing how an enforcing evaluator resolves headings can break ALL existing items of a type. QA-Verify MUST run the FULL gate against real parent files on disk (e.g. EPIC-043 with un-numbered headings — must still pass), not just isolated predicate unit tests. This is the §2.2 manual step "Confirm an existing 🟢 Epic still passes."
- **Acceptance trace:**
  - Gherkin (numbered passes) → unit test 1.
  - (releveled passes) → unit test 2.
  - (existing-surfaces-verified routes to numbered section) → unit test 4.
  - (plain substring stays literal / no false positive) → unit test 3 + prose-line guard.
  - (non-heading Error path unchanged) → `body contains 'Scenario:'` literal-FAIL test.
  - Manual: author Epic from `templates/epic.md` with numbered headings, `gate check` → reuse-audit + simplest-form + existing-surfaces-verified all pass with no hand-fix; full suite green + `typecheck` clean.

### STORY-043-04 (register hotfix WorkItemType)

- **Goal:** Add `'hotfix'` to the `WorkItemType` union + detection maps + a `readiness-gates.md` block so `gate check HOTFIX-*.md` returns pass/fail instead of erroring on unknown type. Advances goal: repairs WS8(d).
- **Cross-story coupling:** Disjoint files from all other M1 stories. parallel_eligible (serialized this sprint). Relies on `readiness-predicates.ts` vocabulary but does NOT edit it.
- **Files to touch (SCAFFOLD-MIRRORED for the gate block):**
  - `cleargate-cli/src/lib/work-item-type.ts` — four map edits (NOT scaffold-mirrored; product code).
  - `.cleargate/knowledge/readiness-gates.md` — new hotfix gate block.
  - `cleargate-planning/.cleargate/knowledge/readiness-gates.md` (canonical) — byte-identical hotfix block. *(payload `cleargate-cli/templates/cleargate-planning/.cleargate/knowledge/readiness-gates.md` is auto-regenerated by `prebuild` from canonical — do not hand-edit, but confirm parity post-build.)*
  - `cleargate-cli/test/lib/work-item-type.node.test.ts` — extend.
- **Schema / type deltas (exact, verified lines):**
  - `work-item-type.ts:8` — `WorkItemType` union: append `| 'hotfix'`.
  - `work-item-type.ts:14-21` (`FM_KEY_MAP`) — append `{ key: 'hotfix_id', type: 'hotfix' }`. First-match-wins; `hotfix_id` is unique, append position immaterial.
  - `work-item-type.ts:27-35` (`PREFIX_MAP`) — append `{ prefix: 'HOTFIX-', type: 'hotfix' }`.
  - `work-item-type.ts:73-81` (`WORK_ITEM_TRANSITIONS`, typed `Record<WorkItemType, string[]>`) — append `hotfix: ['ready-for-merge']`. **TypeScript will not compile without this entry** once `'hotfix'` joins the union — `typecheck` is the enforcement.
  - `readiness-gates.md` new block (model on the bug block at lines 160-172, which uses `section(2) has ≥3 declared-item`, `frontmatter(.).severity != null`, `body does not contain marker 'TBD'`, `frontmatter(.).context_source != null`). Hotfix block, transition `ready-for-merge`, criteria using ONLY existing vocabulary:
    - `anomaly-populated` → `"section(1) has ≥1 listed-item"`
    - `files-touched-declared` → `"section(2) has ≥1 declared-item"`
    - `verification-steps-nonempty` → `"section(3) has ≥1 unchecked-checkbox"`
    - `severity-set` → `"frontmatter(.).severity != null"`
    - `no-tbds` → `"body does not contain marker 'TBD'"`
  - **The ≤2-file/≤30-LOC cap is NOT a machine gate** — the section-count predicate supports only `>=`/`==`/`>` (`readiness-predicates.ts:16,88-91,516-521`), no `<=`. Decision (recorded in story §3.2): enforce at DevOps-merge/review time. Developer does NOT add a `<=` op.
- **Test shape for QA-Red** — `cleargate-cli/test/lib/work-item-type.node.test.ts` (node:test). Cases that fail against clean baseline (no `hotfix` in maps):
  1. `detectWorkItemTypeFromFm({ hotfix_id: 'HOTFIX-001' })` → `'hotfix'` (baseline returns `null`).
  2. `detectWorkItemType('HOTFIX-001-Slug.md')` → `'hotfix'` (baseline `null`).
  3. `detectWorkItemTypeFromFm({})` → `null` (regression guard — unknown stays null, no throw).
  4. `WORK_ITEM_TRANSITIONS.hotfix` is defined and equals `['ready-for-merge']` (compile-enforced; assert at runtime too).
- **Reuse (no duplication):** add entries to the four EXISTING maps; copy the bug gate block's shape. No new module, command, or predicate op.
- **Gotchas (file:line):**
  - `readiness-gates.md` blocks are YAML lists — `yaml.load()` returns an array; the loader unwraps `[0]` (FLASHCARD 253). Match the existing block indentation exactly or the parse breaks.
  - The hotfix template emits `hotfix_id` + `severity` frontmatter (`.cleargate/templates/hotfix.md:31-43`). Confirm `severity` is the actual key name before wiring `severity-set`.
  - `detectWorkItemType` matches by `basename.includes(prefix)` (line ~62, verified) — `HOTFIX-` will not collide with another prefix (`FIX` is not a registered prefix).
  - AND-semantics (FLASHCARD 132): a NEW gate block only affects hotfix-type files — it cannot break other types (each block is type-scoped). Low regression risk, but QA-Verify still smoke-checks one real hotfix file end-to-end.
- **Acceptance trace:**
  - Gherkin (gate check resolves + evaluates) → smoke `gate check` on a `HOTFIX-999-Smoke.md` returns pass/fail, not unknown-type.
  - (detection from fm key + filename prefix) → unit tests 1 + 2.
  - (unknown-type Error path preserved) → unit test 3.
  - DoD: `typecheck` clean (proves transitions entry present); `diff` canonical ↔ working `readiness-gates.md` hotfix block identical.

### STORY-043-03 (template de-number + context_source + Bug repro + proposal purge)

- **Goal:** Make the shipped epic/story/CR/Bug templates pass their own readiness predicates out of the box. Advances goal: WS6 + WS8(b)(c) — stop false-negative gate blocks on freshly authored items.
- **Cross-story coupling:** **MUST land AFTER 043-02.** Different files (templates vs `readiness-predicates.ts`) — no merge conflict, but ordering keeps acceptance clean: once 043-02's heading-text anchoring is in, de-numbering is cosmetic-but-correct; if 043-03 landed first against the un-fixed predicate, the de-numbering is the *only* thing making the gate pass — still correct, just brittle. Land 043-02 first.
- **Files to touch (SCAFFOLD-MIRRORED — canonical templates):**
  - `.cleargate/templates/epic.md` — de-number `## 3.5`→`## Existing Surfaces`, `## 3.6`→`## Why not simpler?`; purge proposal refs (line 41 `context_source` default, line 128 prose, `§3.5`/`§3.6` gate-box cross-refs).
  - `.cleargate/templates/story.md` — de-number `### 1.6`→`## Existing Surfaces`, `### 1.7`→`## Why not simpler?` AND **relocate them to AFTER `## 4. Quality Gates`** (so positional `implementation-files-declared`=§3 / `dod-declared`=§4 do not shift — see FLASHCARD 44); purge proposal refs (lines 6, 64, `§1.6`/`§1.7` cross-refs).
  - `.cleargate/templates/CR.md` — de-number `## 2.5`→`## Existing Surfaces`; add `context_source` frontmatter + `## Context Source` footer box; update `§2.5` gate-box cross-ref.
  - `.cleargate/templates/Bug.md` — add `context_source` frontmatter + footer box; convert §2 Reproduction sample from ordered `1./2./3.` to `- ` bullets (`countDeclaredItems` scores ordinals 0); de-number/anchor the leading `## 0.5 Open Questions` so positional `section(2)` resolves to Reproduction (verified on BUG-034).
  - `cleargate-planning/.cleargate/templates/CR.md` + `cleargate-planning/.cleargate/templates/Bug.md` (canonical mirrors) — byte-identical. *(epic.md/story.md canonical mirrors: confirm whether they exist under `cleargate-planning/.cleargate/templates/` and mirror them too if so — the story §3.1 only names CR/Bug canonical, but ALL edited templates with a canonical sibling must be mirrored. Developer: `ls cleargate-planning/.cleargate/templates/` first and mirror every edited file that has a sibling there. payload auto-regenerated by `prebuild`.)*
- **Schema / content deltas (exact):**
  - Default `context_source` value everywhere: `"approved Epic / verified codebase grounding + recorded direct approval"` (replaces `"PROPOSAL-{ID}.md"`).
  - Proposal prose: `"Populate this strictly from the approved proposal.md"`, `"Sourced from approved proposal.md"`, `"from the approved proposal"` → wording referencing approved Epic + verified codebase grounding + recorded direct approval.
  - CR/Bug `## Context Source` footer box: mirror epic.md's footer-box shape.
- **Test shape for QA-Red** — there is NO dedicated node:test in this story's §3.1 (it edits markdown templates, verified via `gate check` on scratch copies). QA-Red options:
  - (a) author a `*.red.node.test.ts` that reads each template file and asserts the post-fix invariants (heading literal text, `context_source` present in CR/Bug frontmatter, Bug §2 uses `- ` bullets, zero `PROPOSAL-{ID}.md`/`approved proposal.md` matches in epic/story) — these FAIL against the current numbered/proposal-ref templates. Path: `cleargate-cli/test/lib/template-gate-correctness.red.node.test.ts` (reads `../../../.cleargate/templates/*.md` via repo-root-relative path — see FLASHCARD 146 worktree-path caveat: use `import.meta.url`-relative resolution, not a hardcoded root).
  - (b) the manual `gate check` scratch-file verification (§2.2) as the primary acceptance.
  - **Recommend (a)** — a doc-lint Red test gives QA-Verify a re-runnable green signal and guards regression. node:test only; no DB.
- **Reuse (no duplication):** edits markdown only; relies on (does not edit) `readiness-predicates.ts:720` and `countDeclaredItems`. After 043-02 merges, the predicate already tolerates numbers, so this story's de-numbering + the predicate fix are belt-and-suspenders — that is intended (WS6 picks de-numbering as the template-side half).
- **Gotchas (file:line):**
  - **story.md heading relocation is the trap (FLASHCARD 44).** Leaving `## Existing Surfaces`/`## Why not simpler?` inside the `## 3`/`## 4` numbered run shifts the H2 ordinal and breaks `implementation-files-declared`=section3 / `dod-declared`=section4. They MUST move to AFTER `## 4. Quality Gates`. QA-Verify asserts both positional predicates still resolve to §3/§4 on a scratch story.
  - **Bug.md double fix:** both the ordered-list→bullets conversion AND the `## 0.5 Open Questions` de-number are required; either alone leaves `repro-steps-deterministic` failing (bug block needs `section(2) has ≥3 declared-item` — note ≥**3**, so the repro sample must keep ≥3 bullets).
  - BUG-033 (CR.md/Bug.md missing `context_source` → `discovery-checked` fails for every CR/Bug) and BUG-034 (Bug positional index) are the verified motivating failures — this story un-falses them.
  - **Sequence guard:** if 043-03 is dispatched before 043-02 merges, the scratch-Epic `gate check` still passes (de-numbering alone satisfies the literal-substring predicate) — so the ordering is for cleanliness, not a hard block. But DO honor 043-02→043-03 per SDR.
- **Acceptance trace:**
  - Gherkin (Epic passes reuse+right-size) → scratch Epic `gate check` reuse-audit-recorded + simplest-form-justified pass.
  - (Story keeps positional predicates) → scratch Story: headings after §4; implementation-files-declared=§3, dod-declared=§4.
  - (CR+Bug satisfy discovery-checked) → both frontmatters carry non-empty `context_source`; both have footer box; discovery-checked passes.
  - (Bug repro deterministic) → §2 uses `- ` bullets (≥3), section(2)=Reproduction, repro-steps-deterministic passes.
  - (canonical byte-identical) → `diff -q` CR.md/Bug.md working ↔ canonical = empty.
  - (no proposal refs Error) → `grep -nE "PROPOSAL-\{ID\}\.md|approved proposal\.md|from the approved proposal" .cleargate/templates/epic.md .cleargate/templates/story.md` → zero matches.

### STORY-043-05 (sprint-close hardening + reporter v2 + flashcard curation)

- **Goal:** Make the close cascade fail-closed when `dist/` is stale, run the cascade exactly once on `--assume-ack`, re-sync Reporter to the v2 seven-section template, and add a review-driven flashcard-archival step. Advances goal: seals close-stage integrity holes (WS8(e)/(f) + WS2).
- **Cross-story coupling:** Independent within M1. Does NOT touch `execution_mode` lines — its `close_sprint.mjs` hunks (dist-assertion before Step 2.6, `--assume-ack` de-dup) are disjoint from CR-070's already-merged migrator. Disjoint from 043-01/-02/-03/-04 files. **Keep atomic — do NOT split** (close_sprint.mjs + reporter.md + flashcard SKILL.md are one close-stage story by design, per SDR §2.3).
- **Files to touch (LIVE-LOOP for reporter.md + flashcard SKILL.md):**
  - `.cleargate/scripts/close_sprint.mjs` — dist assertion + cascade de-dup + flashcard-archival surfacing. *(Script, not scaffold-mirrored to `cleargate-planning/.cleargate/scripts/`? — verify: `.cleargate/scripts/` IS mirrored to `cleargate-planning/.cleargate/scripts/` per FLASHCARD 227 patterns; Developer must `ls cleargate-planning/.cleargate/scripts/close_sprint.mjs` and mirror if present.)*
  - `.claude/agents/reporter.md` (live) — v2 seven-section re-sync + flashcard-archival duty.
  - `cleargate-planning/.claude/agents/reporter.md` (canonical) — byte-identical. *(payload auto-mirrored by `prebuild`.)*
  - `.claude/skills/flashcard/SKILL.md` (live) — document `.cleargate/FLASHCARD-archive.md` + review-driven curation. Mirror to `cleargate-planning/.claude/skills/flashcard/SKILL.md` canonical.
  - **reporter.md is currently byte-identical live ↔ canonical (verified `diff -q` clean).**
- **Schema / signature deltas (exact, verified lines):**
  - **WS8(e) dist assertion:** add before Step 2.6 (lifecycle reconciliation starts at `close_sprint.mjs:301`, the skip-path "CLI binary not found … (non-fatal)" is at `:354`, mirrored at `:405` (2.6b), `:454` (2.6c), `:512` (2.6d), and Step 2.8). Resolve `cleargate-cli/dist/cli.js` from `REPO_ROOT`; if absent → `process.stderr.write` build instruction + `process.exit(1)` BEFORE Step 2.6. **Keep the `CLEARGATE_SKIP_LIFECYCLE_CHECK=1` / `CLEARGATE_SKIP_FLASHCARD_CLEANUP=1` test seams intact** (lines 37, 56) — the dist assertion fails closed only on *unintended* staleness, NOT on the deliberate skip-env path.
  - **WS8(e) cascade de-dup:** `assumeAck` is set at `:168` (`args.includes('--assume-ack') || reportBodyStdin`). Trace the `--assume-ack` branch and ensure the lifecycle/orphan/merge cascade runs exactly once; delete the redundant second invocation.
  - **WS8(f) reporter v2:** `template_version: 1` → `2` (line ~113); "all six sections (§§1-6)" (line ~49) and "All six sections required" (line ~260) → "seven (§§1-7)" / "All seven sections required"; add **§4 Observe**, re-number Lessons → §5, Self-Assessment → §6, Change Log → §7 in the §6-synthesize block (lines ~101-111).
  - **WS2 flashcard curation:** Reporter scans `.cleargate/FLASHCARD.md`, classifies cards as superseded / resolved / duplicate (one-line reason each), emits a candidate list in the report + Gate-4 Brief. close_sprint.mjs presents the list; on human approval, approved cards append to `.cleargate/FLASHCARD-archive.md` (greppable, NEVER deleted) and are marked `[S]`/`[R]` in the hot file per SKILL Rule 7. **No card auto-evicted by age** (Epic §6 Q1 rejected clock-based rotation). Wire alongside the existing Step 6.7 `--flashcard-cleanup` scan.
- **Test shape for QA-Red** — node:test (`*.red.node.test.ts`). The story explicitly resolves sprint dir from `REPO_ROOT` with a `CLEARGATE_SPRINT_DIR` env override for isolation (FLASHCARD 227) — use it. Red cases failing against clean baseline:
  1. dist-gate: spawn `close_sprint.mjs <test-sprint>` with `dist/cli.js` path moved/absent → assert non-zero exit + "dist not built"/build-instruction on stderr, BEFORE Step 2.6 markers. (Baseline: continues with "non-fatal" skip, exit 0.)
  2. single-cascade: run with `--assume-ack` + present dist → parse stdout, assert `Step 2.6` (and 2.6b/2.8) markers appear EXACTLY once. (Baseline: duplicated.)
  3. reporter doc-lint: read `.claude/agents/reporter.md` → assert `template_version: 2`, ≥1 "seven sections"/"§§1-7" match, "six sections" no longer asserts the requirement, §4 Observe/§5 Lessons/§6 Self-Assessment/§7 Change Log all named. (Baseline FAILS — currently v1/six.)
  4. mirror parity: `diff` live `reporter.md` ↔ canonical and flashcard SKILL ↔ canonical → identical.
  5. flashcard curation: against a fixture `FLASHCARD.md`, dry-run the candidate surfacing → candidates print with reasons; nothing written to `FLASHCARD-archive.md` without approval (append-only, never delete).
  - **Real-infra:** no DB needed; close_sprint.mjs spawning + temp sprint dirs + fixture FLASHCARD files. Use `CLEARGATE_SKIP_*` seams to isolate the gates under test from the ones not under test.
- **Reuse (no duplication):** `close_sprint.mjs` already runs the cascade + a Step 6.7 flashcard-cleanup scan; `reporter.md` already flags flashcard candidates; `SKILL.md` already documents `[S]`/`[R]` markers (Rules 6-8 at `SKILL.md:59-65`). This story narrows/reconciles — no new module/command. Reuse the existing `suggest_improvements.mjs --flashcard-cleanup` scan as the candidate source.
- **Gotchas (file:line):**
  - **LIVE-LOOP HAZARD (CR-024 class) — reporter.md + flashcard SKILL.md + close_sprint.mjs are all on THIS sprint's own close path.** Editing the live `close_sprint.mjs` to fail-closed on stale dist could break THIS sprint's own Gate-4 close if `cleargate-cli/dist/` is stale at that moment. **Mitigation: build `cleargate-cli/dist` (`npm run build` in cleargate-cli/) before this sprint's own close, OR the assertion will (correctly) tell you to.** Per SDR §2.3, formal `cleargate init` live re-sync of reporter.md/SKILL.md is DEFERRED to Gate-4 doc-refresh — the Developer edits canonical + payload (+ the live files in-place since they are byte-identical copies), but does NOT run `cleargate init` mid-sprint (would re-sync the whole live tree and could perturb the running loop). State this in the dispatch.
  - **Atomicity (SDR §2.3):** do not split this story. All three surfaces are close-stage and interlock (close_sprint.mjs presents what reporter.md surfaces, governed by SKILL.md rules).
  - `close_sprint.mjs:14` — sprint_status flips to "Completed" on Reporter success + ack; the dist assertion MUST sit ABOVE this so a stale-dist run never reaches the flip with un-run lifecycle/merge gates (the WS8(e) Error-path scenario).
  - The flashcard hot/cold split: `.cleargate/FLASHCARD-archive.md` is a NEW file (greppable cold archive) — never delete from `.cleargate/FLASHCARD.md`, only mark `[S]`/`[R]` and append the approved batch to the archive.
- **Acceptance trace:**
  - Gherkin (close fails closed, dist absent) → Red test 1: non-zero exit + build message before Step 2.6; gates NOT silently skipped.
  - (cascade once on --assume-ack) → Red test 2: single Step-2.6/2.8 markers.
  - (reporter v2 seven-section) → Red test 3: template_version 2, seven sections, §4-§7 named; canonical byte-identical.
  - (flashcard curation at Gate 4) → Red test 5: candidates with reasons, approval-gated append, no clock eviction, none deleted.
  - (stale-dist Error path) → Red test 1 variant: hardened close raises fail-closed assertion, does NOT reach sprint_status flip.

---

## Cross-story risks

1. **One LIVE-LOOP story remains (043-05); 043-01's hazard is now neutralized by worktree isolation (orchestrator decision 2026-06-01).** 043-01 (`pending-task-sentinel.sh`) edits the **in-worktree CANONICAL hook ONLY**, and its tests target that same in-worktree canonical sibling — neither touches the MAIN-repo LIVE hook the orchestrator runs, so there is no mid-sprint loop perturbation and no `CLEARGATE_ADVISORY=1` break-glass needed for 043-01. 043-05 (`close_sprint.mjs` + `reporter.md` + flashcard `SKILL.md`) still touches files the running orchestrator uses: Developer edits canonical (+ live in-place for the byte-identical copies), but the **formal `cleargate init` live re-sync is DEFERRED to Gate-4 doc-refresh** per SDR §2.3 — do NOT run `cleargate init` mid-sprint. Break-glass if 043-05's dist gate trips this sprint's own close: `npm run build` in `cleargate-cli/`. For BOTH stories, payload byte-parity is a post-merge `npm run prebuild` (DevOps) concern, not a within-story diff obligation.
2. **043-02 → 043-03 ordering is load-bearing for acceptance, not for merge (different files).** Heading-text anchoring (043-02) lands before template de-numbering (043-03). No file collision, so no rebase needed — but dispatch them in order so 043-03's scratch-`gate check` runs against the already-tolerant predicate.
3. **AND-semantics regression (FLASHCARD 132) on 043-02.** Changing an enforcing evaluator's heading resolution can break every existing item of a type. QA-Verify MUST run the full gate against real on-disk parents (EPIC-043 with un-numbered headings must still pass), not just isolated unit tests.
4. **CR-070 deliverable mismatch.** The dispatch asks for `gate-mode.test.ts`, but `gate-mode.red.node.test.ts` (immutable, QA-Red's) already covers it. The Developer authors a NEW owned `gate-mode.node.test.ts` (no `.red.` infix) — do NOT touch the sealed Red file. This is closure regression, not a TDD cycle; skip TPV.
5. **story.md heading relocation (043-03).** The de-numbered `## Existing Surfaces`/`## Why not simpler?` MUST move AFTER `## 4. Quality Gates` or the positional section-count predicates (implementation-files-declared=§3, dod-declared=§4) shift and break — FLASHCARD 44 has bitten EPIC-031/32/33 + STORY-033-01. Bug.md repro must keep ≥3 bullets (`section(2) has ≥3 declared-item`).
6. **Scaffold-mirror obligation (BUG-024 class).** 043-03 (templates canonical), 043-04 (`readiness-gates.md` canonical), 043-05 (`reporter.md` + flashcard `SKILL.md` canonical + `close_sprint.mjs` if mirrored) carry post-merge mirror obligations. **043-01 NARROWED (orchestrator decision 2026-06-01):** the Developer edits ONLY the canonical hook within the story diff; payload is regenerated by `npm run prebuild` post-merge (DevOps) and live re-syncs at Gate-4 — do NOT require all-three byte-parity inside the 043-01 diff. DevOps runs `npm run prebuild` + `diff -q` mirror-parity on every scaffold-mirrored merge. 043-02 and CR-070 touch only `cleargate-cli/src|test` (product code) — no scaffold mirror.
7. **Self-hosting hook self-test scaffold debt (candidate follow-up — do NOT expand scope now).** The flashcard-enforcement tests resolved the hook via `${GIT_ROOT}`/`_find_git_root`, walking PAST the worktree `.git` to the MAIN-repo LIVE (gitignored) hook — wrong target for a hook change tested inside a worktree. 043-01 re-points its two test scripts to the in-worktree canonical sibling; a durable convention (env-override default + shared resolver) for ALL hook self-test harnesses is a candidate follow-up item, not in scope this sprint.

## Open decisions for orchestrator

1. **CR-070 test-file naming.** Plan directs a NEW owned `cleargate-cli/test/util/gate-mode.node.test.ts` (the sealed `gate-mode.red.node.test.ts` already covers the cases). Confirm this satisfies "CR-070 §3 line 164" for closure, or whether you want the CR archived on the strength of the existing Red test alone (in which case CR-070 has zero net new deliverables and is a pure paperwork close). Architect recommendation: author the owned regression test — it is cheap and gives a non-immutable green signal.
2. **epic.md/story.md canonical mirror existence (043-03).** Story §3.1 names only CR.md/Bug.md canonical mirrors. Developer must `ls cleargate-planning/.cleargate/templates/` and mirror every edited template that has a canonical sibling. If epic.md/story.md have NO canonical sibling there, no mirror is needed for them — flag the actual state at dispatch rather than assuming.
3. **close_sprint.mjs canonical mirror (043-05).** Confirm whether `.cleargate/scripts/close_sprint.mjs` has a `cleargate-planning/.cleargate/scripts/` canonical sibling (FLASHCARD 227 implies scripts are mirrored). If yes, 043-05 mirrors it; if no, the live/working copy is the only target. Developer verifies at dispatch.

---

_Version check: no dependency versions declared or changed in M1 (no `package.json` additions). `npm view` Pre-Spec Dep Version Check N/A._

### M3.md

# Milestone: M3 "Efficiency"
## Stories: STORY-043-07, STORY-043-08, STORY-043-09
## Wave: M3 — serial within M3 for SKILL.md serialization; -07/-09 disjoint and parallel-eligible

> role: architect — M3 implementation plan. Authoritative for Developers; supersedes story §3.1 where they conflict (per memory `feedback_dispatch_vs_milestone_plan_precedence`). Preflight verified against `sprint-context.md` (Sprint Goal: every gate fires on the right signal, retire stale scaffold debt, add consolidation — without touching the adversarial core) and SPRINT-33 plan §2 (SDR-validated). All file:line citations below are grep/Read-verified 2026-06-01.

---

## Order (CONFIRMED — proposed serial order accepted, with one refinement)

**Confirmed serial order: 043-07 → 043-08 → 043-09.**

- **043-07** is fully disjoint (only `cleargate-cli/src/commands/wiki-ingest.ts` + a colocated test). It touches no scaffold file and no SKILL.md. It can land anytime; placed first because it is the cleanest and unblocks nothing/blocked-by-nothing.
- **043-08 → 043-09** keeps the SKILL.md edits serial and in-order. Both edit `.claude/skills/sprint-execution/SKILL.md` but in **textually disjoint regions** (08: §C.3.5 ~L286 + §C.6 ~L369-383 dispatch gating; 09: the `write_dispatch.sh`-call prose at SKILL L87/181/260/293/314/346/406/578). 043-10 (M4) is the 3rd SKILL.md editor (Phase D.5, inserted between §6 ~L542 and §7 ~L559). DevOps serializes 08 → 09 → 10 and rebases each later story on the prior.

**Parallelizable note:** 043-07 ‖ 043-09 are file-disjoint and BOTH `parallel_eligible: y` — they may run concurrently if the orchestrator chooses a 2-wide M3 wave. 043-08 is `parallel_eligible: n` (live dispatch-count change, high exposure) and must be the SKILL.md anchor that lands before 043-09 amends the same file. If running -07 ‖ -09 in parallel, still serialize 08 before 09's merge.

**Mirror-sync timing (binding, from SDR §2.3):** the three-way mirror (canonical → payload via `npm run prebuild` → live via `cleargate init`) for SKILL.md runs ONCE after the LAST of 08/09/10 lands — NOT per-story. The **live `/.claude` re-sync is DEFERRED to Gate-4 doc-refresh**; re-syncing mid-sprint would alter THIS sprint's own running loop. Developers edit canonical + payload-via-prebuild only; the live tree stays on the pre-sprint loop until close.

---

## Cross-repo realities (binding for every M3 story)

1. **`cleargate-cli/` is a separate gitignored repo** at `<root>/cleargate-cli/`. Stories editing `cleargate-cli/src/**` (043-07, 043-09) run on a feature branch in the **MAIN cleargate-cli checkout — NOT a git worktree**. Worktrees break `../.cleargate` path resolution → ~738 spurious test load-failures. Tests run from `cleargate-cli/` resolving the outer repo via `REPO_ROOT = path.resolve(__dirname, '..', '..'[, '..'])`.
2. **`.claude/**` (live) is gitignored and Gate-4-deferred.** Stories editing agents/skills (043-08) edit the **CANONICAL** copy `cleargate-planning/.claude/...`. Live re-sync via `cleargate init` is a Gate-4 item, not a per-story blocker. The main-repo live tree lacking the edit post-commit is EXPECTED (FLASHCARD 2026-05-29 #qa #worktree #mirror #agents).
3. **`.cleargate/scripts/**` is TRACKED** in BOTH live (`.cleargate/scripts/`) and the canonical mirror (`cleargate-planning/.cleargate/scripts/`). `write_dispatch.sh` (043-09) → edit BOTH byte-identical; changes go ACTIVE on merge (these are not Gate-4-deferred — they are tracked source). **Verified 2026-06-01: the two `write_dispatch.sh` copies are currently byte-identical** — keep them so.
4. **`dist/cli.js` rebuild is Gate-4-deferred.** 043-07 (wiki-ingest) and 043-09 (cli.ts) verify via SOURCE `npm test` (`tsx --test`), NOT via the built CLI. 043-09's DoD line "rebuild dist so --help reflects hidden flags" is a Gate-4 re-confirm, not a per-story blocker — assert the `hidden:true` flags and the stub-label removal at the SOURCE level in tests.
5. **SKILL.md is touched by 043-08 + 043-09 (+ 043-10 in M4).** Serialize 08 → 09 → 10; rebase each. Disjoint text regions, one file.

---

## Per-story blueprint

### STORY-043-07 — Incremental Wiki Synthesis Recompile

**Goal (1 line):** Narrow per-edit wiki ingest to recompile only the changed item's synthesis partition (and zero pages on a stamp-only edit), with a byte-parity floor against full rebuild. **Advances sprint goal:** cuts redundant four-page rewrites (token/IO cost) while the full-rebuild correctness floor holds — efficiency without weakening the gate.

**Files to touch — exact paths, repo, obligations:**
- `cleargate-cli/src/commands/wiki-ingest.ts` — separate gitignored repo, MAIN checkout (no worktree). The ONLY production file. No canonical/payload/live mirror (CLI source, not scaffold). No dist rebuild needed for acceptance (Gate-4-deferred).
- NEW test: `cleargate-cli/test/wiki/wiki-ingest-synthesis-parity.node.test.ts` — **MUST live under `test/wiki/`, NOT colocated in `src/`.** The CLI `npm test` glob is `test/**/*.node.test.ts` (package.json:50); a `*.node.test.ts` under `src/` is silently NOT run (FLASHCARD 2026-05-29 #test-glob #cli #wiki). Story §3.1's "colocated `src/commands/wiki-ingest.*.node.test.ts`" wording is WRONG — override it; place the test in `test/wiki/`.

**The change (concrete):**
1. **Add `BUCKET_SYNTHESIS_MAP`** as a module-level const co-located with `recompileSynthesis` (wiki-ingest.ts), derived (read-only) from the four recipe filters:
   - `sprints` → `['active-sprint', 'product-state', 'roadmap']`
   - `epics` → `['product-state', 'roadmap']`
   - `proposals` → `['open-gates', 'product-state']`
   - `stories` → `['product-state']`
   - `crs` → `['product-state']`
   - `bugs` → `['product-state']`
   - `initiatives`, `topics`, unknown → `[]` (recompile NONE)
   Derivation sources (consult, do NOT modify): `src/wiki/synthesis/active-sprint.ts:16` (`i.bucket === 'sprints'`), `open-gates.ts:26` (proposals + ready/blocked), `product-state.ts:36` (`['epics','stories','sprints','proposals','crs','bugs']`), `roadmap.ts:25-26` (sprints + epics).
2. **Extract `writePage(page, items, wikiRoot, templateDir)`** — a tiny helper that switches on the page name and runs the existing `compileActiveSprint`/`compileOpenGates`/`compileProductState`/`compileRoadmap` + `fs.writeFileSync`. Both the incremental path and the full-rebuild path call this SAME helper so per-page output is provably byte-identical regardless of path.
3. **Change the signature** `recompileSynthesis(wikiRoot, cwd, templateDir, opts?)` where `opts = { bucket?: string; stampOnly?: boolean }`:
   - If `opts?.stampOnly === true` → return immediately (zero writes).
   - Compute `pages = opts?.bucket ? (BUCKET_SYNTHESIS_MAP[opts.bucket] ?? []) : ALL_FOUR`. If `opts` is absent (the full-rebuild call site, `wiki build`) → `pages = ALL_FOUR` (correctness floor unchanged).
   - If `pages` is empty → return (zero writes).
   - Run the `scanRawItems`/`loadWikiConfig` scan ONCE (the corpus state must be whole for the pages that DO recompile — current body, wiki-ingest.ts:663-673), then call `writePage` only for pages in `pages`.
4. **Compute `bucket` + `stampOnly` at the Step-8 call site (wiki-ingest.ts:313-314).** The handler already derives `bucketInfo` via `deriveBucket`/`deriveBucketFromReportPath` at L132-137 — reuse `bucketInfo.bucket` (do NOT re-derive). For `stampOnly`: compare the freshly-built `pageBody` (already computed at L301 via `buildPageBody`) + `wikiPage.status` against the prior wiki page body+status read at L282-294 (`existingPageBody`). If the rendered body is unchanged AND status is unchanged → `stampOnly = true`. The stamp-field set (frontmatter keys that never feed synthesis): `pushed_at`, `pushed_by`, `last_synced_*`, `last_pulled_*`, `last_remote_update`, `draft_tokens`, `updated_at`, `updated_at_version`, `server_pushed_at_version`, `cached_gate_result.last_gate_check`. **Fail-safe:** if the prior page can't be read or the body parse fails → `stampOnly = false` (recompile the partition), NEVER skip.
5. **Remove the `// (all four — M3 over-recompiles)` comment at L313** and the `// Recompile all four synthesis pages` comment at L661; replace with one line describing the incremental behavior.

**Test shape for QA-Red (`test/wiki/wiki-ingest-synthesis-parity.node.test.ts`):**
- **Unit — `BUCKET_SYNTHESIS_MAP`:** each bucket maps to the exact set above; `initiatives`/`topics`/unknown → `[]`.
- **Unit — stamp-only detection:** stamp-field-only delta → `stampOnly=true`; body or status delta → `false`; unreadable prior page → `false` (fail-safe).
- **Integration — per-edit partition:** build a fixture wiki; edit an epic body → only `product-state.md` + `roadmap.md` rewritten (assert mtime AND content; `active-sprint.md`/`open-gates.md` mtime unchanged); edit a sprint → those three rewrite, `open-gates.md` does not; stamp-only edit → zero of four mtimes change.
- **Integration (PARITY FLOOR — the load-bearing test):** ingest a fixed corpus (sprints + epics + stories + bugs) one item at a time incrementally, snapshot the four synthesis files, then run a full `cleargate wiki build` on the same corpus and assert **byte-equality per page**. On mismatch, throw `Error("wiki synthesis drift: <page>")`. This is the correctness floor — `cleargate wiki build` output MUST be byte-identical before and after this change.
- Red scenarios for the CLI command path: drive via the handler import or `process.execPath + dist` is NOT needed here — test the exported function logic + a fixture-repo ingest at source level (`tsx --test`).

**Risks / gotchas:**
- **Correctness floor is non-negotiable.** The full-rebuild path (`wiki build`) must produce byte-identical output. Guarantee this by having BOTH paths share `writePage` and by NOT passing `opts` from the full-rebuild call site (so it defaults to ALL_FOUR). If the parity test goes red, the incremental change is REJECTED until parity is restored — do not weaken the test.
- `buildIndex` has two code paths (normal + empty-delivery early-return) — NOT in scope here (this story does not touch index), but the corpus scan at L663-673 already guards `fs.existsSync(deliveryRoot)`; keep that guard.
- Do NOT touch the synthesis recipes (`src/wiki/synthesis/*.ts`), the PostToolUse hook, `ingest_buckets` semantics, or Steps 6/7/9.

**Acceptance trace:** §2.1 Gherkin scenarios 1-4 → covered by the integration partition test (scenarios 1-2), the parity test (scenario 3), and the drift-Error assertion (scenario 4). §4.2 DoD lines 1-7 → BUCKET map + stampOnly + comment removal + parity test + byte-identical floor + green typecheck/test + no cross-WS file change.

---

### STORY-043-08 — Conditional Architect Re-Entries (TPV + Post-Flight Fire on Signal)

**Goal (1 line):** Make the two live Architect re-entries (§C.3.5 TPV, §C.6 post-flight) fire only on a `pre_gate_runner.sh` flag, dropping a clean standard-lane story from 6 dispatches to ≤5 — with a non-removable safeguard that ANY flag still dispatches the live Architect. **Advances sprint goal:** cuts a re-paid 16KB `architect.md` dispatch on the proven-clean path (token cost) without weakening the adversarial core — the scan still runs unconditionally and any flag re-arms the full check.

**Files to touch — exact paths, repo, canonical/live obligations:**
- `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` (CANONICAL) — edit §C.3.5 (~L286) and §C.6 (~L369-383). This is the tracked source.
- `cleargate-planning/.claude/agents/architect.md` (CANONICAL) — edit `## Mode: TPV` block (~L108) AND add the conditional contract for the post-flight pass.
- **Do NOT edit the live `.claude/skills/...` or `.claude/agents/architect.md`** — live re-sync (`cleargate init`) is Gate-4-deferred (SDR §2.3 live-loop safety: re-syncing mid-sprint alters THIS sprint's running loop). **Verified 2026-06-01: live↔canonical SKILL.md and architect.md are currently byte-identical** (the SPRINT-32 FLASHCARD #47 divergence has been reconciled). Edit canonical; payload mirror is regenerated by `npm run prebuild`; live stays untouched until Gate-4.
- `prebuild` propagates canonical → `cleargate-cli/templates/cleargate-planning/.claude/...` (gitignored payload; verify parity via `diff`, do NOT `git add` — FLASHCARD 2026-05-15 #mirror #parity #prebuild).
- `.cleargate/scripts/pre_gate_runner.sh:199-287` (`run_arch()`) — READ-ONLY reference. Consumed as-is; exit 0 = clean, 1 = flagged, 2 = scan couldn't run.

**The change (concrete):**
1. **§C.3.5 TPV Gate (SKILL.md ~L286-310):** Wrap the existing `Mode: TPV` dispatch in a scan-result guard. The current block unconditionally runs `write_dispatch.sh ... architect` (L293) then injects the TPV prompt. Insert a precondition: run the wiring/pre-gate scan first; on a **clean** result proceed straight to §C.4 Spawn Developer (NO Architect agent); on a **flagged** result spawn the live Architect `Mode: TPV` exactly as today and route per `APPROVED`/`BLOCKED-WIRING-GAP`. Keep the `--arch-bounce` routing intact for the flagged path.
2. **§C.6 Architect Pass (SKILL.md ~L369-383):** The `pre_gate_runner.sh arch` invocation already runs first (L378). Today L383 reads "If pre-gate passes, spawn Architect for post-flight review." **Flip L383** so a CLEAN scan (exit 0, no recorded flags) proceeds directly to §C.7 Story Merge WITHOUT spawning the post-flight Architect. The existing L381 behavior (new deps / structural issues → return to Developer for mechanical failures; 3+ → escalate) is preserved for the non-zero path. Structural/surface flags spawn the live Architect post-flight as today.
3. **Safeguard line (mandatory, non-removable — must land VERBATIM in BOTH files):** add at both §C.3.5 and §C.6:
   > **Safeguard (non-removable):** ANY pre-gate flag — demotion, `arch_bounce` signal, surface drift, new-deps, structural issue, OR exit-2 (scan-could-not-run) — MUST still dispatch the live Architect. Treat exit 2 as a flag (fail toward dispatching, never toward skipping). This optimization removes the Architect ONLY on a proven-clean scan; it never removes the Architect from a flagged path.
4. **`architect.md` `## Mode: TPV` (~L108-110):** the current L110 reads "Dispatched between QA-Red and Developer for standard-lane stories under v2 (fast lane skips)." Add the scan-flag qualifier: dispatched only when the pre-gate/wiring scan flags a problem; a clean scan skips TPV. **Add a new sub-section for the post-flight contract** — architect.md TODAY has NO standalone post-flight block (the only post-flight reference is the "What you are NOT" line at :218 "post-flight is QA's job"). State that the Architect post-flight pass is spawned only on a non-zero/flagged `pre_gate_runner.sh arch` result, and embed the same safeguard sentence verbatim.

**Test shape for QA-Red:** This is a prose/doc-trace story (no runtime code) — Red tests are **grep/diff assertions** under `cleargate-cli/test/` (the only repo with a node:test harness for scaffold-parity checks) OR doc-walkthrough assertions. Concretely:
- `cleargate-cli/test/scaffold/skill-md-conditional-architect.node.test.ts` (or nearest existing scaffold-parity test dir) asserting: (a) §C.3.5 prose conditions the live TPV dispatch on a scan flag (grep for the guard sentence); (b) §C.6 prose conditions the post-flight dispatch on a non-zero/flagged scan; (c) the verbatim safeguard sentence appears in BOTH `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` AND `cleargate-planning/.claude/agents/architect.md`; (d) mirror parity — `diff` of canonical vs payload is empty after prebuild. (Live↔canonical diff is the Gate-4 check, NOT a per-story Red test — live is intentionally un-synced this sprint.)
- Dispatch-count walkthrough: a clean standard-lane story → ≤5 dispatches; a flagged story → 6.

**Risks / gotchas (HIGH exposure — adversarial-core invariant):**
- **The safeguard is the adversarial-core guard.** ADR-conflict flag (SDR §2.5): the five-agent split is locked; 043-08 narrows WHEN the Architect re-enters but the safeguard preserves the check on every risky path. The safeguard prose MUST land **verbatim and identical** in both SKILL.md and architect.md — QA-Verify greps both files for the exact sentence. If it is missing or weakened in either file, the change is REJECTED.
- **Edit by anchor text, never line number.** Although live↔canonical are currently byte-identical, edit canonical by matching the heading/sentence text (FLASHCARD 2026-05-29 #mirror #parity #architect-md) — line numbers shift as you insert.
- **SKILL.md three-way serialization:** this is the FIRST of 08→09→10 to touch SKILL.md. 043-09 amends the `write_dispatch.sh`-call prose (disjoint lines L87/181/260/293/314/346/406/578); note that §C.3.5 at L293 contains a `write_dispatch.sh ... architect` call that 043-09 reframes as fallback — 08 edits the dispatch-GATING logic around it, 09 edits the marker-call PROSE. Coordinate: 08 lands first; 09 rebases. Flag to DevOps.
- **Do NOT touch** `pre_gate_runner.sh` logic/exit codes, the `lane: fast` skip, or `execution_mode: v1` advisory path.
- **Live-loop safety:** do not run `cleargate init` mid-sprint. The running orchestrator keeps the unconditional dispatches until Gate-4 — that is correct and intended.

**Acceptance trace:** §2.1 scenarios 1-4 → clean-skip (1) + flagged-safeguard (2) covered by dispatch-count walkthrough + grep of the gated prose; safeguard-breach Error (3) + canonical/live-drift Error (4) covered by the parity test (drift) and the verbatim-safeguard grep (breach). §4.2 DoD lines 1-7 → gated §C.3.5/§C.6, verbatim safeguard in both files, architect.md conditional contract, canonical+payload byte-identical (live deferred), fast/v1 unchanged, 043-10 merge-order flagged, walkthrough counts.

---

### STORY-043-09 — CLI Surface Hygiene

**Goal (1 line):** Hide hook-only plumbing commands from `--help` (keep callable), drop the false "(stub …)" label, delete the orphan `triage-classifier.ts` + its red test, and make `write_dispatch.sh` fallback-only behind the auto-marker. **Advances sprint goal:** retires stale/duplicated scaffold debt and shrinks the perceived CLI surface to the real one — scar-tissue GC.

**Files to touch — exact paths, repo, obligations:**
- `cleargate-cli/src/cli.ts` — separate gitignored repo, MAIN checkout. Add `{ hidden: true }` to eight plumbing command definitions; fix the stub label at L418. Verified at SOURCE; dist rebuild is Gate-4-deferred (assert in tests against the source command tree, not `node dist/cli.js`).
- `cleargate-cli/src/lib/triage-classifier.ts` — DELETE. **Verified 2026-06-01: zero `src/` callers** (only its own header self-reference at :2; sole consumer is its red test). Safe to delete.
- `cleargate-cli/test/lib/triage-classifier.red.node.test.ts` — DELETE (the red test that imports the orphan).
- `.cleargate/scripts/write_dispatch.sh` — TRACKED in BOTH live (`.cleargate/scripts/`) and canonical (`cleargate-planning/.cleargate/scripts/`). **Edit BOTH byte-identical.** Verified 2026-06-01 the two copies are currently identical — keep them so. These go ACTIVE on merge (not Gate-4-deferred — tracked source).
- `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` (CANONICAL) — reframe the `write_dispatch.sh` manual-call prose at L87/181/260/293/314/346/406/578 from "mandatory pre-spawn step" to "fallback only". Same canonical-edit + Gate-4-deferred-live rule as 043-08; mirror via prebuild. **Rebase on 043-08** (shared file).

**The change (concrete):**
1. **Hide eight plumbing commands (`hidden: true`).** Commander's chained `.command(name)` form takes opts as the 2nd arg: `.command('stamp <file>', { hidden: true })`. Targets (verified positions):
   - `stamp <file>` — top-level `.command('stamp <file>')` ~L168.
   - `stamp-tokens <file>` — top-level ~L454.
   - `state update <story-id> <new-state>` — `state` subcommand ~L431.
   - `state validate <sprint-id>` — `state` subcommand ~L443.
   - `wiki ingest <file>` — `wiki` subcommand ~L189.
   - `gate qa <worktree> <branch>` — `gate` subcommand ~L290.
   - `gate arch <worktree> <branch>` — `gate` subcommand ~L298.
   - `sprint reconcile-lifecycle <sprint-id>` — `sprint` subcommand ~L428.
   `hidden:true` affects `--help` listing ONLY; the `.action()` handler is untouched so direct invocation still works. For commands declared with explicit description text (e.g. `gate qa`/`gate arch` use `.command(name).description(...)`), pass `{ hidden: true }` as the 2nd arg of `.command()` — NOT into `.description()`.
2. **Fix the stub label (cli.ts:418).** Current: `.description('mark a story complete and clean up its worktree (stub — requires complete_story.mjs)')`. Drop the parenthetical → `.description('mark a story complete and clean up its worktree')`. The resulting string MUST contain no occurrence of the substring `stub`. Leave `.option('--sprint ...')` and `.action(...)` intact.
3. **Delete the orphan + audit siblings.** Re-run `grep -rn "triage-classifier\|triageClassifier" cleargate-cli/src` immediately before delete to re-confirm zero live callers (already confirmed); delete `src/lib/triage-classifier.ts` + `test/lib/triage-classifier.red.node.test.ts`. Then sweep `src/lib/*.ts` for any other module imported by zero `src/` files; delete ONLY zero-caller ones with a green suite after removal. **Record the audit verdict (deleted list or "no other orphans found") in the merge commit body.**
4. **`write_dispatch.sh` fallback-only guard.** Add an early-exit BEFORE the write block (after sprint resolution, ~after L80 in the live copy). Detection: glob `${SPRINT_DIR}/.dispatch-*.json` for a marker whose payload `session_id` matches `${CLAUDE_SESSION_ID}` AND whose `writer` field starts with `pre-tool-use-task.sh` (the auto-hook's writer tag — verified `pre-tool-use-task.sh:120` writes `writer: "pre-tool-use-task.sh@cleargate-${CG_VERSION}"`, distinct from this script's own `write_dispatch.sh@...` writer). If such a same-session auto-marker exists → log "fallback: auto-marker present, no-op" and `exit 0` WITHOUT writing. Only write when the auto-hook left no same-session marker. This makes the manual call a true fallback that never duplicates the auto-marker. **macOS bash 3.2:** no `declare -A` / `mapfile` — iterate the glob with a `for` loop + `jq -r '.session_id, .writer'` per file (FLASHCARD 2026-05-29 #bash #macos #portability).
5. **SKILL.md prose reframe** at the eight `write_dispatch.sh` call sites: describe the manual call as a fallback for spawns the auto-hook can't attribute, not a mandatory pre-spawn step. (The script header already documents the CR-026 fallback intent — align the SKILL prose to it.)

**Test shape for QA-Red:**
- `cleargate-cli/test/commands/cli-surface-hygiene.node.test.ts` (under `test/`, NOT `src/`): (a) walk the Commander program tree and assert each of the eight plumbing commands has `hidden === true`; (b) assert each still resolves to a registered action (callability preserved); (c) assert the `story complete` description contains no `stub` substring; (d) assert `fs.existsSync('src/lib/triage-classifier.ts') === false`.
- `cleargate-cli/test/scripts/write-dispatch-fallback.node.test.ts`: spawn `write_dispatch.sh` via `spawnSync(node-less bash, [scriptPath, 'STORY-X', 'developer'])` (it is a bash script — invoke via `spawnSync('bash', [scriptPath, ...])`, NOT `wrapScript`, per FLASHCARD 2026-05-04 #red-test #scripts #env). Use `CLEARGATE_SPRINT_DIR`/`ORCHESTRATOR_PROJECT_DIR` + `CLAUDE_SESSION_ID` env overrides for fixture isolation. Assert: (1) with a same-session auto-marker (writer=pre-tool-use-task.sh) pre-seeded → exits 0 and writes NO second `.dispatch-*.json`; (2) with no auto-marker → writes exactly one marker.
- Full `cleargate-cli` suite green after the orphan delete (typecheck + test).

**Risks / gotchas:**
- **Orphan-DELETE confirmation (the load-bearing risk).** Before deleting `triage-classifier.ts`, the grep-for-importers is MANDATORY (FLASHCARD 2026-05-15 #mcp #scope-bleed-guard: grep ALL importers before deleting an exported symbol). Already confirmed zero `src/` callers 2026-06-01, but re-run at implementation time — a mid-sprint story could have added a caller. The sibling-orphan sweep deletes ONLY zero-caller modules with a green suite afterward.
- **Callability invariant (§2.1 scenario 3 Error path).** `hidden:true` must affect `--help` ONLY. If any hidden command stops dispatching, REVERT the hidden change for that command (a hook would break). Test (b) above guards this.
- **`write_dispatch.sh` byte-identity.** Edit live AND canonical identically; verify `diff -q` is empty after the edit. A drift here ships a buggy tracked script.
- **SKILL.md rebase on 043-08.** 09's prose edits at L293 sit inside the §C.3.5 block 08 also edits — rebase 09 onto 08's merged SKILL.md and re-confirm the disjoint hunks apply cleanly. Flag to DevOps.
- **`session_id` matching in the guard:** the auto-hook may write `session_id` from a transcript-fallback when `CLAUDE_SESSION_ID` is unset (write_dispatch.sh:84-103 shows the same fallback chain). Match on `CLAUDE_SESSION_ID` when present; when absent, fall back to most-recent same-PID/same-window marker (don't over-match across sessions). Fail toward writing (a duplicate marker is harmless; a missing marker breaks attribution) — i.e. if unsure whether the existing marker is this spawn's, WRITE.

**Acceptance trace:** §2.1 scenario 1 (hidden+callable+no-stub+orphan-gone+green) → cli-surface test (a-d) + full suite. Scenario 2 (fallback idempotent) → write-dispatch-fallback test (1-2). Scenario 3 (callability Error path) → cli-surface test (b). §4.2 DoD lines 1-6 → hidden flags, no-stub, orphan+test deleted + audit verdict in commit, write_dispatch fallback-only, green typecheck/test, dist rebuilt (Gate-4 re-confirm).

---

## Cross-story risks

1. **SKILL.md is a three-story shared surface (08 → 09 → 10), and 043-09 was added to that chain after the prepopulated SDR table (SDR §2.2 correction).** 08 edits the §C.3.5/§C.6 dispatch-gating logic; 09 edits the `write_dispatch.sh`-call PROSE at L87/181/260/293/314/346/406/578 — these regions OVERLAP at §C.3.5 (L293 has a `write_dispatch.sh ... architect` call inside the block 08 gates). They are textually separable but adjacent. **Mitigation:** serialize 08 → 09; 09 rebases onto 08's merged SKILL.md and re-applies its prose hunks; DevOps does not auto-merge concurrently. 043-10 (M4) rebases on both.
2. **Mirror-sync once, at the end.** Do NOT run `npm run prebuild` + `cleargate init` per-story for the three SKILL.md editors. Live `/.claude` re-sync is Gate-4-deferred; payload prebuild can run per-merge for parity-diff but the LIVE tree stays on the pre-sprint loop (SDR §2.3 live-loop safety). A Developer who re-syncs live mid-sprint alters THIS sprint's own execution loop — forbidden.
3. **The 043-08 safeguard sentence is a cross-file invariant.** It must be byte-identical in `SKILL.md` AND `architect.md`. If a later edit (043-09 SKILL.md prose, or 043-10) touches the §C.6 region, re-confirm the safeguard sentence survives verbatim.
4. **043-07's parity floor is independent** — it shares no file with 08/09. Its only cross-story note: it touches `wiki-ingest.ts`, NOT the `wiki ingest` CLI command definition in `cli.ts` that 043-09 hides. No collision (different files, different concerns).
5. **`write_dispatch.sh` (043-09) and SKILL.md `write_dispatch.sh`-call prose (043-09) are the SAME story** — both in 043-09's scope. But the SKILL.md half rebases on 043-08; the script half (tracked, live+canonical) is independent. Land them in one commit but verify both mirror obligations (script byte-identity + SKILL canonical/payload parity).

## Open decisions for orchestrator

1. **M3 wave width.** 043-07 ‖ 043-09 are file-disjoint and both `parallel_eligible: y` — orchestrator MAY run them as a 2-wide wave, but 043-08 (`parallel_eligible: n`) must still land in SKILL.md before 043-09's SKILL.md prose hunk merges. Recommended simplest path: serial 07 → 08 → 09 (the proposed order); parallelizing 07‖09 saves one wave but adds a merge-ordering constraint (08 before 09 regardless). Orchestrator's call.
2. **Sibling-orphan sweep scope (043-09 req).** The story mandates a sweep of `src/lib/*.ts` for other zero-caller modules and deletion of any found. This is open-ended deletion — recommend the Developer records the audit verdict in the commit body and deletes ONLY modules with a clean grep + green suite; if the sweep surfaces a non-obvious candidate (e.g. a module referenced only by a test or by `dist`), the Developer should report it rather than delete unilaterally. No split needed.

## Stories I'd split: NONE.

All three are single-concern and correctly sized (043-07 L3, 043-08 L3, 043-09 L2). 043-09 spans three files but they are one coherent "CLI surface hygiene" concern with a shared acceptance theme; splitting would fragment the surface-area story for no gain.

---

_Version check (CR-037): no new dependencies declared in any M3 story — npm registry check N/A (all changes are in-place edits/deletions to existing code + scaffold prose)._

## Git Log Digest

Git log unavailable for sprint/S-33 (no commits or branch not found).

## Token Ledger Digest

**Total:** 307,946,609 tokens (input: 71,625 / output: 1,424,477 / cache_read: 301,461,587)

**Per-agent breakdown:**
- architect: 66,721,774 (21 dispatches)
- developer: 48,669,252 (19 dispatches)
- qa: 126,267,776 (26 dispatches)
- reporter: 41,880,285 (1 dispatches)
- devops: 24,407,522 (10 dispatches)

**CR-035 two-line split (pre-computed for Reporter §3):**
sprint_work_tokens: 266,066,324
sprint_total_tokens: 307,946,609
reporter_pass_tokens: null


## Flashcard Slice

No flashcard entries found in sprint window [2026-06-02 → 2026-06-13].

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
