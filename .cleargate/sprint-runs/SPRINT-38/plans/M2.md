role: architect

# Milestone: M2 — P2 Break-Glass Reconciliation & Doc Coherence (EPIC-051)
## Stories: STORY-051-08, STORY-051-09
## Wave: wave4 (051-08, solo/serial) → wave5 (051-09, solo/serial)

Sprint goal (acceptance anchor): "Make every 'always enforced' ClearGate gate actually block
again after the CR-070/CR-074 execution_mode retirement — no dead vocabulary, no silent no-ops,
and a guard so canonical↔live drift cannot recur."

M0 (01·02·03·04) and M1 (05·06·07) are merged to `sprint/S-38` (`63a2c5ec`). M2 is the final
third: 08 closes the break-glass overclaim (the last "silent no-op" — an instruction with no
mechanism), 09 closes the coherence layer (the last "dead vocabulary" — stale gate numbers,
phantom paths, an orphaned RED test, a mis-keyed hook).

**Landed baseline this plan is written against** (verified this session, not the pre-sprint tree):
outer `sprint/S-38` @ `63a2c5ec`; cli `sprint/S-38` @ `5864bb7`. Every line number below was
re-read today — several story-file citations are stale and are corrected inline (§Grounding notes).

> **All four Open Decisions are RESOLVED by the orchestrator (2026-07-27).** The per-story
> blueprints below are already updated in place; the verbatim rulings are recorded in
> `## Mid-Sprint Amendment (orchestrator, 2026-07-27)` at the end of this file. Dev/QA dispatches
> read the blueprints; the amendment is the audit trail.

---

## Order

Strict, load-bearing, non-negotiable:

1. **wave4 = STORY-051-08 alone. MUST fully merge to `sprint/S-38` before wave5's worktree is cut.**
   Three independent reasons: (a) both stories edit root + canonical `CLAUDE.md`; (b) 08 **EDITS**
   `close_sprint.deferred-verify.red.node.test.ts` and 09 **DELETES** it — git handles
   edit-then-delete cleanly, delete-then-edit raises a rebase conflict on a vanished target;
   (c) post-amendment, both stories now edit `cleargate-enforcement.md` (disjoint sections —
   see Cross-story risk 3).
2. **wave5 = STORY-051-09 alone.** `parallel_eligible: n` (L3 coherence sweep) → own serial wave
   by rule. Lands last in the sprint.

Parallelizable pairs: **none in M2.** Both stories are `parallel: false`. M2 is a two-step chain.

Merge order for M2: **08 → 09.** This is the tail of the sprint-wide `CLAUDE.md` bounded-block
edit chain **05 → 07 → 08 → 09** (05 and 07 landed; each story owns a *distinct paragraph*).

---

## Per-story blueprint

### STORY-051-08 — Narrow break-glass semantics: scope `CLEARGATE_ADVISORY`, guard `--assume-ack`

**(d) Goal linkage.** Converts the sprint's last pure-instruction gate into a mechanism: today
"never pass `--assume-ack` autonomously" is prose with zero enforcement (a textbook silent no-op),
and enforcement §15 claims `CLEARGATE_ADVISORY` softens "all gate failures in the CLI" when it
reaches exactly two CLI sites. After 08, the close flag physically refuses without a CI token and
the break-glass doc states the true reach — the "no silent no-ops" clause, applied to the
enforcement levers themselves.

**(a) Exact files across tiers** (all paths verified today; ⊕ = added by the 2026-07-27 amendment)

| Tier | Files |
|---|---|
| Canonical (edit in worktree, tracked) | `cleargate-planning/.cleargate/scripts/close_sprint.mjs` — guard after `:177`; **plus the `:639` comment fix, see G8-3** · `cleargate-planning/.cleargate/knowledge/cleargate-enforcement.md` — §15 advisory scope at **`:544`**, §12.3 Flag Reservation at **`:459-461`** (story cites `:540` / `:455-457` — **stale**, see G8-4; ⊕ AD#5: 08 owns the *append* only — the `Gate-3 breach` gate-number token on `:461` is 09's), ⊕ §14.1 `:503-504` + ⊕ §14.2 `:511-512` (Steps 2.7/2.8 "Advisory (v1)" bullets — AD#2) · `cleargate-planning/CLAUDE.md` — `:36` (Sprint-close-is-Gate-4-class) and `:63` (Guardrails bullet) · `cleargate-planning/.cleargate/scripts/close_sprint.deferred-verify.red.node.test.ts` — add `CLEARGATE_CI_ACK: '1',` inside `baseEnv()` at `:82-95` |
| Live-root (edit in worktree, tracked, DIFF-FIRST) | `.cleargate/scripts/close_sprint.mjs` · `.cleargate/knowledge/cleargate-enforcement.md` · `.cleargate/scripts/close_sprint.deferred-verify.red.node.test.ts` · root `CLAUDE.md` — `:158` (inside bounded block, mirrors canonical `:36`), `:185` (inside block, mirrors canonical `:63`), `:126` (**OUTSIDE** the bounded block — meta-repo-only, see G8-7) |
| cli (edit + commit in MAIN cli checkout `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli`, branch `sprint/S-38`) | `cleargate-cli/package.json` — prefix `test`, `test:node`, `test:integration`, `test:file`, `test:node:file` with `CLEARGATE_CI_ACK=1` (all five verified present) · **NEW** `cleargate-cli/test/scripts/close-sprint-assume-ack-guard.node.test.ts` (relocated per AD#1) · ⊕ `cleargate-cli/src/cli.ts:347` — `--assume-ack` option help string · ⊕ `cleargate-cli/src/commands/sprint.ts:788` — "sprint not closed" message string (AD#3; **string literals only, no control flow**) |
| Delivery docs (tracked) | ⊕ `.cleargate/delivery/pending-sync/STORY-051-08_Reconcile_Break_Glass_Semantics.md` — strike the stale "Held at 🟡 …" blockquote under the Ambiguity Gate's `Current Status: 🟢` line, and self-amend §3.1 with the AD#1/#2/#3 file-surface justification (file-surface contract §9) |
| DEFERRED to Gate-4 (NOT per story) | `npm run prebuild` payload regen + `cleargate-planning/MANIFEST.json` · cli `dist` rebuild · live `/.claude/**` hand-sync (08 touches **no** `/.claude/**` file) |
| MUST NOT touch | protocol `cleargate-protocol.md` (that is 09's spine) · enforcement **§6.2 `:294-295`** and **§12 heading `:448` / §12.1 `:452`** (reassigned to 09 by AD#2) · enforcement `:248`, `:329`, `:388`, `:398`, `:414` and the `(source: protocol §NN)` annotations (carry-over CR, AD#2) · any new `CLEARGATE_ADVISORY` honor site (Q1 = NARROW, hard-locked) · any **non-string** line of `cli.ts` / `sprint.ts` (no option semantics, no control flow, no new arg forwarding) |

**(b) Integrity delta (what actually changes behavior).**

Baseline verified today: `close_sprint.mjs` has **zero** reads of `process.env.CLEARGATE_ADVISORY`
(grep count = 0) and **zero** guards on `--assume-ack`. `isAdvisory()` (`gate-mode.ts:14`) has
exactly **one** CLI call site — `sprint.ts:1715`, the `sprint preflight` gate. `init_sprint.mjs:140`
reads the raw env for the sprint-init story-file assertion. `pending-task-sentinel.sh:129` reads it
for the flashcard gate. Those three are the complete honor set.

After 08:
- **Guard.** Directly after `const assumeAck = ...` (`close_sprint.mjs:177`), before the `sprintDir`
  resolution at `:179`, a fail-fast block: `args.includes('--assume-ack') && process.env.CLEARGATE_CI_ACK !== '1'`
  → stderr line naming `CLEARGATE_CI_ACK` + `process.exit(2)`. Exit 2 matches the existing `usage()`
  convention at `:123`. Gate on `args.includes('--assume-ack')` **specifically**, not on the
  `assumeAck` variable — `assumeAck` is also true for `--report-body-stdin` (`:176-177`), which
  §1.3 explicitly exempts.
- **§15 narrowed** (`enforcement.md:544`). "all gate failures in the CLI (`cleargate sprint
  preflight`, `cleargate sprint init`, etc.)" is replaced by the RATIFIED enumeration
  (STORY-051-08 §1.4 NEW-question human decision, EPIC-051 Q1): it downgrades gate failures at the
  `cleargate sprint preflight` gate (`sprint.ts` `isAdvisory()`, `gate-mode.ts:14`) **and** the
  `cleargate sprint init` story-file assertion (`init_sprint.mjs:140`), with a note on the
  `pending-task-sentinel.sh:129` hook honor-site — and explicitly does **not** soften the
  file-surface, decomposition, lifecycle-init/reconciliation, or sprint-close gates, and is not a
  universal enforcement-strength knob. **Do not write a bare "preflight-only" sentence** — that is
  a fresh underclaim; the §1.4 ratification supersedes Q1's shorthand. This question is CLOSED; do
  not re-open it.
- **§12.3 extended** (`enforcement.md:459-461`). Append to the Flag Reservation paragraph:
  `close_sprint.mjs` mechanically refuses `--assume-ack` (exit 2) unless `CLEARGATE_CI_ACK=1`;
  **the token is never set unprompted by an agent — it is set for a single invocation only after an
  explicit human close authorization (Gate 4), or by CI** (AD#3 wording; use this formulation
  verbatim in all four doc sites, not "the orchestrator MUST NOT set it").
- ⊕ **§14.1 / §14.2 de-vocabularized** (`enforcement.md:503-504`, `:511-512`, AD#2). The
  "**Enforcement (v2):** … / **Advisory (v1):** …" bullet pairs for Steps 2.7 and 2.8 collapse to a
  single always-enforced statement each (halts the close, exit 1, with the named resolution), and
  the "(advisory in v1)" warning strings are removed. Grounding: `close_sprint.mjs` has zero
  `CLEARGATE_ADVISORY` reads, so no advisory path exists for either step — these bullets contradict
  the §15 sentence 08 lands in the same commit. Keep the "**Test seams:**" bullets unchanged.
- **CLAUDE.md guardrails** (4 lines across 2 files). Append one clause after "Never pass
  `--assume-ack` autonomously" / "Never pass --assume-ack yourself": `close_sprint.mjs` now refuses
  `--assume-ack` unless `CLEARGATE_CI_ACK=1`; that token is never set unprompted by an agent — only
  for a single invocation after an explicit human close authorization, or by CI. Grounding confirms
  **neither** `CLAUDE.md` carries a `CLEARGATE_ADVISORY` "all CLI gates" claim — no advisory
  narrowing belongs in `CLAUDE.md`, only the guard clause.
- ⊕ **Two CLI strings completed** (AD#3). `cli.ts:347` option help — append the token requirement to
  `'skip the "waiting for Reporter" gate and flip state to Completed directly'`.
  `sprint.ts:788` — the template literal must keep the leading substring `sprint not closed`
  intact (two tests match on it — see G8-8) and gain the token requirement. Text-only; the option
  registration, the `opts.assumeAck === true` branch at `sprint.ts:426-427`, and every arg array
  stay byte-identical.
- **Harness token.** All five cli test scripts get `CLEARGATE_CI_ACK=1`. Blast-radius audited:
  the five files that genuinely `spawnSync` close_sprint with `--assume-ack`
  (`close-sprint-reconcile.integration`, `close-sprint-step-2-6c.red`,
  `close-sprint-hardening-043-05.red`, `test_close_sprint_v21.integration`,
  `close-sprint-backsync`) **all** spread `...process.env`, so the parent token propagates. The
  other five `--assume-ack` matches (`reporter-content`, `sprint-unit`, `sprint-archive`,
  `sprint-archive-stamp`, `readme-qa-doc-truth-043-06.red`) do **not** spawn close_sprint — they
  assert on injected-`execFn` arg arrays or spawn `diff`. **Zero per-file edits needed in
  `cleargate-cli/test/**`.**
- **Standalone red test.** `close_sprint.deferred-verify.red.node.test.ts` self-runs outside the
  cli harness (`node --test --import tsx <file>`); its `runClose()` at `:103-128` spawns with
  `--assume-ack` at `:110` through `baseEnv()` at `:82`. Add `CLEARGATE_CI_ACK: '1',` to that
  object in **both** copies (canonical + live) or its Scenarios 1-4 all flip to exit 2.

**(c) Test shape (the QA-Red target).**

**Target file (RESOLVED, AD#1):** `cleargate-cli/test/scripts/close-sprint-assume-ack-guard.node.test.ts`.
Naming follows the sibling close-sprint suites already in `test/scripts/`. It is globbed by
`npm test` (`run-default-tests.mjs:23` → `test/**/*.node.test.ts`, minus `*.integration.*`), and it
does **not** ship in the npm payload. It spawns the **live-root** `REPO_ROOT/.cleargate/scripts/close_sprint.mjs`
(`REPO_ROOT = resolve(__dirname,'..','..','..')` — the `test_close_sprint_v21.integration` anchor
pattern), so the live-copy guard must land in the same commit family as the test.

Four guard cases, per §4.1 — each **fails against the clean baseline** because `close_sprint.mjs`
has no guard at all today (grep-verified: zero `CLEARGATE_CI_ACK` occurrences repo-wide):

1. **Refuse without token.** Closable tmpdir sprint + all skip seams; child env built from
   `...process.env` with `delete env.CLEARGATE_CI_ACK`; spawn with `--assume-ack`.
   Assert `exitCode === 2` and `/CLEARGATE_CI_ACK/.test(stderr)`.
   *Baseline*: exits 0 (or 1 on a fixture issue), never 2 → RED.
2. **Allow with token.** Same fixture, `CLEARGATE_CI_ACK: '1'`; assert exit 0 and `sprint_status`
   flipped to `Completed`.
   *Baseline*: passes trivially — this is the **regression** leg, not a red leg. Keep it; it is the
   proof the guard is not over-broad.
3. **No-flag path unaffected.** Token unset, **no** `--assume-ack`; assert exit 0 and stdout
   contains `Review the report, then confirm close by re-running with --assume-ack`
   (verbatim string at `close_sprint.mjs:1024`).
   *Baseline*: passes — second regression leg (proves the guard is placed on the flag, not the run).
4. **Non-`'1'` values refused.** Loop `['0','true','']` with `--assume-ack`; assert exit 2 each time.
   *Baseline*: exits 0 → RED.

Skip-env recipe (copy verbatim from `close_sprint.deferred-verify.red.node.test.ts:82-95`; **do not
import it** — 09 deletes that file one wave later): `CLEARGATE_SPRINT_DIR`, `CLEARGATE_REPO_ROOT`,
`CLEARGATE_SKIP_LIFECYCLE_CHECK`, `CLEARGATE_SKIP_WORKTREE_CHECK`, `CLEARGATE_SKIP_MERGE_CHECK`,
`CLEARGATE_SKIP_PARENT_ROLLUP`, `CLEARGATE_SKIP_SPRINT_TRENDS`, `CLEARGATE_SKIP_SKILL_CANDIDATES`,
`CLEARGATE_SKIP_FLASHCARD_CLEANUP`, `CLEARGATE_SKIP_BUNDLE_CHECK`.

Doc legs (Gherkin scenarios 4 + 5) are grep assertions, cheapest as extra `it()` blocks in the same
file — all RED on baseline:
- `grep "all gate failures in the CLI"` → 0 hits in enforcement.md (both tiers).
- §15 names both honor sites (`sprint preflight` **and** the `sprint init` story-file assertion).
- §12.3 + both `CLAUDE.md` copies contain `CLEARGATE_CI_ACK`.
- ⊕ **AD#2 leg:** enforcement §14 region contains zero `Advisory (v1)` / `Enforcement (v2)` /
  `advisory in v1` tokens (baseline: 4 lines → RED).
- ⊕ **AD#3 leg:** `cli.ts` `--assume-ack` help string and the `sprint.ts` `sprint not closed`
  message each contain `CLEARGATE_CI_ACK` (baseline: neither does → RED). Read the source strings;
  do not spawn the CLI for this.

**(e) Risks + first-bounce hints.**

- **G8-1 — RESOLVED (AD#1).** The guard test relocates to
  `cleargate-cli/test/scripts/close-sprint-assume-ack-guard.node.test.ts`. Retained rationale for
  the in-commit §3.1 self-amendment: the spec'd `cleargate-planning/.cleargate/scripts/…` path is
  un-globbed by `run-default-tests.mjs:23` (nothing globs `.cleargate/scripts/`) and
  `copy-planning-payload.mjs` applies **no** file filter, so prebuild would ship a `.node.test.ts`
  into the npm payload — a dead gate, and the exact defect STORY-051-09 R9 deletes. Same correction
  the orchestrator accepted for 05 in M1.
- **G8-2 — RESOLVED (AD#3).** `cleargate sprint close <id> --assume-ack` IS a real caller
  (`cli.ts:347` declares the option; `sprint.ts:426-427` pushes the flag). It now exits 2 without
  the token — **as ratified**. 08 folds in the two now-incomplete strings (`cli.ts:347`,
  `sprint.ts:788`) so the CLI tells the operator what it needs. **SPRINT-38's own Gate-4 close runs
  under this rule**: the human authorizes, the orchestrator sets `CLEARGATE_CI_ACK=1` for that
  single invocation.
- **G8-3 (stale comment that would contradict 08's own §15 text).** `close_sprint.mjs:639` reads:
  `// On miss: list unmerged commits + exit 1 (always enforced; \`CLEARGATE_ADVISORY=1\` warns +
  continues).` VERIFIED FALSE — zero `process.env.CLEARGATE_ADVISORY` reads in the file. This is the
  code-comment twin of the §14.2 bullet AD#2 assigns to 08; fix both in the same commit. Drop the
  advisory clause, keep "always enforced".
- **G8-4 (stale line numbers — three).** Correct before editing: enforcement §15 advisory sentence is
  at **`:544`**, not `:540`; §12.3 Flag Reservation heading `:459`, body `:461`, not `:455-457`
  (those lines are §12.2 Steps A/B); `isAdvisory()` call site is `sprint.ts:1715`, not `:1681`
  (shifted by 05's docstring sweep). `close_sprint.mjs:173-177`, `usage()` exit 2 at `:123`,
  `init_sprint.mjs:140`, `pending-task-sentinel.sh:129`, `gate-mode.ts:14`, canonical
  `CLAUDE.md:36/:63`, root `CLAUDE.md:126/:158/:185`, `baseEnv():82`, `enforcement.md:503-504/:511-512`,
  `cli.ts:347`, and `sprint.ts:788` are all **confirmed accurate**.
- **G8-5 (self-defeating test — pinned by the story, restate it).** The harness prefix sets
  `CLEARGATE_CI_ACK=1` for every `test/**` child. The refuse-cases (1 and 4) MUST build a child env
  that does not carry it — `const env = { ...process.env, ...skips }; delete env.CLEARGATE_CI_ACK;`.
  A spread-only env silently green-washes the refusal branch.
- **G8-6 (dual-copy sync is now machine-checked — new this sprint).** Two independent checkers will
  red a canonical-only edit: (a) `cleargate-cli/test/scaffold/canonical-live-parity.red.integration.node.test.ts`
  asserts canonical `.cleargate/scripts/*` byte-identical to live `.cleargate/scripts/*`; (b) 06's
  `checkCanonicalDrift` walks `.claude`, `.cleargate/{scripts,knowledge,templates}`
  (`drift-check.ts:37-40`) and flags `missing-in-live` / `content-mismatch` → `cleargate doctor`
  exit 1. Every canonical edit in 08 lands inside that walked set. Sync both copies in the same
  commit.
- **G8-7 (root `CLAUDE.md:126` is outside the bounded block).** The `CLEARGATE:START` marker is at
  `:129`; `:126` sits in the meta-repo's own "Guardrails for the conversational agent (me)" section.
  It is a legitimate edit (this repo's own operating rule) but it is **not** mirrored to canonical,
  **not** shipped, and **not** rewritten by `cleargate init`. Do not try to sync it anywhere. Lines
  `:158` and `:185` are inside the block and DO mirror canonical `:36` / `:63`.
- ⊕ **G8-8 (AD#3 first-bounce hint — two existing assertions match on the message prefix).**
  `test/commands/sprint-archive-stamp.integration.node.test.ts:552` and
  `test/commands/sprint-archive.node.test.ts:440` both assert
  `String(errOut).includes('sprint not closed')`. **Append** the token requirement to the end of
  `sprint.ts:788`'s template literal; do not reword the leading `sprint not closed —` prefix, or both
  tests red. No test asserts on the `cli.ts:347` help string (grep-verified), so that one is free.
- **DOGFOOD SYNC (dominant sprint hazard, FLASHCARD 2026-07-17 `#dogfood #sync`).** Verified today:
  `close_sprint.mjs`, `cleargate-enforcement.md`, and `close_sprint.deferred-verify.red.node.test.ts`
  are **byte-identical** canonical↔live right now. Still `diff` before writing and hand-port only the
  intended lines. Never blind-`cp`.
- **Payload is derived.** Never hand-edit `cleargate-cli/templates/cleargate-planning/**` or
  `MANIFEST.json`. QA-Verify must not red 08 on stale payload state (FLASHCARD 2026-07-18
  `#dogfood #test-harness`).

**Reuse (no duplication) — verified exports/patterns, do not re-implement:**
- `isAdvisory()` (`cleargate-cli/src/util/gate-mode.ts:14`) — the exact-`'1'` truthiness the guard
  mirrors. **Read it; do not import it** (close_sprint.mjs is scaffold `.mjs`, cli is a separate repo).
- `usage()` `process.exit(2)` (`close_sprint.mjs:123`) — the reserved-flag/invocation-misuse exit
  convention the guard reuses.
- `baseEnv()` skip-seam recipe (`close_sprint.deferred-verify.red.node.test.ts:82-95`) — copy the
  object literal into the new test; 09 deletes the source file.
- `test_close_sprint_v21.integration.node.test.ts` — the `REPO_ROOT`-anchored `spawnSync` +
  `...process.env` close-spawn shape for a cli-side test hitting the live scaffold script.
- Canonical enforcement.md "always enforced (STORY-070-01: `execution_mode` retired)" annotations
  (`:27, :211, :254, :266, :280`) — the house wording for always-enforced claims; §13's closing line
  `:491` ("This gate is **always enforced** (STORY-070-01: single always-enforced behavior).") is the
  exact sentence shape the rewritten §14.1/§14.2 bullets should mirror.

---

### STORY-051-09 — Fix doc contradictions, gate-numbering canon & phantom references

**(d) Goal linkage.** The coherence layer of the restoration: after M0/M1 made the gates block
again, 09 ensures no agent reading the protocol, either `CLAUDE.md`, the enforcement doc, a rubric,
or a script comment is routed by a stale three-gate flow, a path that does not exist, a module
deleted two sprints ago, or an orphaned RED test asserting against a shipped feature — the "no dead
vocabulary" clause applied to structure rather than to `v1`/`v2` strings.

**(a) Exact files across tiers** (all paths + line numbers verified today; ⊕ = added by the
2026-07-27 amendment)

| Tier | Files |
|---|---|
| Canonical (edit in worktree, tracked) | `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — R3 §9 flow `:204-206`; R4 §10.2 `:254`, §10.8 `:348-349`, §10.9 `:369`, §12.6 `:512`; R5 §6 `:161`, §8 `:183`; ⊕ **R12** §4 Gate-3 `:119` `§<N>` → `§13` (AD#4) · ⊕ `cleargate-planning/.cleargate/knowledge/cleargate-enforcement.md` — **R13** §6.2 `:294-295` v1/v2 bullets, **R14** §12 heading `:448` + §12.1 `:452` Gate-3.5/Gate-3-class → four-gate spine (AD#2), ⊕ **R14 cont.** §-index row `:20` **3rd column only** + `:461` **`Gate-3 breach` token only** (AD#5) · `cleargate-planning/.cleargate/knowledge/mid-sprint-triage-rubric.md` — R6 `:13`, `:155` · `cleargate-planning/.cleargate/templates/Bug.md` — R7 `<instructions>` anchors `:17`, `:18`, `:19` · `cleargate-planning/.cleargate/scripts/launch_wave.mjs` — R8 comments `:227`, `:250`, `:276`, `:307-308` · `cleargate-planning/.claude/hooks/pre-tool-use-autonomy.sh` — R10 `:53` · `cleargate-planning/CLAUDE.md` — R2 `:28` |
| Canonical DELETE (`git rm`) | `cleargate-planning/.cleargate/scripts/close_sprint.deferred-verify.red.node.test.ts` |
| Live-root (edit in worktree, tracked, DIFF-FIRST) | `.cleargate/knowledge/cleargate-protocol.md` · ⊕ `.cleargate/knowledge/cleargate-enforcement.md` · `.cleargate/knowledge/mid-sprint-triage-rubric.md` · `.cleargate/templates/Bug.md` · `.cleargate/scripts/launch_wave.mjs` · root `CLAUDE.md` — R2 `:150` |
| Live-root DELETE (`git rm`) | `.cleargate/scripts/close_sprint.deferred-verify.red.node.test.ts` |
| Live `/.claude` (gitignored, NOT staged) | `.claude/hooks/pre-tool-use-autonomy.sh` — hand-sync at Gate-4. Verified byte-identical to canonical today, so a clean one-line hand-port. |
| cli (edit + commit in MAIN cli checkout, branch `sprint/S-38`) | **NEW** `cleargate-cli/test/scaffold/enforcement-doc-coherence.node.test.ts` |
| Delivery docs (tracked) | ⊕ `.cleargate/delivery/pending-sync/STORY-051-09_Doc_Consistency_And_Phantom_Refs.md` — self-amend §3.1 to add `cleargate-enforcement.md` + the protocol `:119` fix, with the AD#2/AD#4 justification in-commit (file-surface contract §9) |
| DEFERRED to Gate-4 (NOT per story) | `npm run prebuild` payload regen (mirrors all seven canonical edits **and** drops the deleted orphan from `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/`) + `MANIFEST.json` · cli `dist` rebuild · live `/.claude` sync |
| MUST NOT touch | protocol §4 `:98-128` **structure** (the spine being adopted — no renumber; the `:119` edit is the `§<N>` token ONLY, not the `v1`/`v2` vocab on that line) · protocol §12.4 and `CLAUDE.md` `v2` clauses (05's territory, already swept) · enforcement **§15 `:544`, §12.3 `:459-463`, §14.1 `:503-504`, §14.2 `:511-512`** (08's, landed in wave4 — leave them), ⊕ **EXCEPT the `Gate-3 breach` gate-number token on `:461`, released to 09/R14 by AD#5 — numbering only; every other byte of §12.3, including 08's appended `CLEARGATE_CI_ACK` sentence at `:463`, stays byte-identical** · enforcement `:248`, `:329`, `:388`, `:398`, `:414` and the `(source: protocol §NN)` annotations (carry-over CR, AD#2 — **including the `(source: protocol §27)` suffix on the `:448` heading and the identical `protocol §27` 2nd-column cell of the `:20` §-index row 09 is otherwise editing (AD#5)**) · ⊕ `.claude/skills/sprint-execution/SKILL.md:625` "Gate-3-class action" (carry-over CR, AD#5 — **not** wave5) · any **non-comment** line of `launch_wave.mjs` (incl. the `:310-315` banner string — see G9-3) · live `/.claude/agents/{qa,devops,reporter}.md` (drifted, Gate-4-deferred — see G9-4) |

**(b) Integrity delta (what stops being wrong).**

Verified baseline — every defect 09 claims is **present today, at the cited line**:
- Protocol three-gate tokens: `:254` ("halts Gate 1 (Initiative approval) and Gate 3 (Push)"),
  `:348`, `:349`, `:369` ("at Gate 1 or Gate 3"), `:512` ("Gate 2 (Ambiguity)"). ✔
- Protocol §9 ASCII flow `:204-206` — still mandates an approved Initiative and halts at "Gate 2". ✔
- `.cleargate/plans/` phantom at `:161` and `:183`. ✔ (Real destination confirmed at §4 `:111`.)
- ⊕ Protocol §4 Gate-3 `:119` ends "See `cleargate-enforcement.md` §<N> for full enforcement spec" —
  literal unresolved placeholder. ✔
- ⊕ Enforcement §6.2 `:294` ("Under v2: off-surface files cause a non-zero exit — the commit is
  blocked.") / `:295` ("Under v1: the hook prints a warning but exits 0 (advisory only)."). ✔
- ⊕ Enforcement §12 heading `:448` ("Gate 3.5 — Sprint Close Acknowledgement") and §12.1 `:452`
  ("Sprint close is a **Gate-3-class action** … (§4 Gate 3)"). ✔
- `triage-classifier.ts` phantom at rubric `:13` and `:155`, in **both** tiers (byte-identical). ✔
- Bug.md broken anchors: `:17` `§0.5 Open Questions`, `:18` `§2 Impact (edge conditions)`,
  `:19` `§2 Impact`. Body headings are `### Open Questions` `:68`, `## 2. Reproduction Protocol`
  `:81`, `## 4. Execution Sandbox (Suspected Blast Radius)` `:95`. ✔
- `launch_wave.mjs` header `:11` vs stale function comments `:227`, `:250`, `:276`, `:307-308`. ✔
- `pre-tool-use-autonomy.sh:53` reads `jq -r '.agent // "unknown"'` while `write_dispatch.sh` writes
  `agent_type` and `token-ledger.sh:191` reads `.agent_type`. ✔
- Orphan RED test present in all three tiers (canonical / live-root / payload), 9900 bytes each. ✔

After 09: zero three-gate tokens repo-wide in the two `CLAUDE.md` files + protocol; the four-gate
spine (`§4` Gate 1 Brief / Gate 2 Sprint-Ready / Gate 3 Execution / Gate 4 Close-Ack, `:98-128`) is
the only numbering model **including in `cleargate-enforcement.md` §12**; both phantom paths resolve
to `.cleargate/delivery/pending-sync/`; the `§<N>` placeholder resolves to `§13`; no reference to a
deleted module; the file-surface hook is documented as unconditional (matching landed 01); every
Bug.md Brief anchor resolves to a verbatim body heading; `launch_wave.mjs` header and function
comments agree with **no executable-line diff**; the autonomy hook logs the real agent instead of a
constant `"unknown"`; the orphan test is gone from all three tiers.

Apply the R2/R3/R4 re-map with exactly one mapping, everywhere:
*Initiative approval → intake step **before** Gate 1 · Ambiguity resolution → **Gate 1 (Brief)** ·
Push → the action fired when Gate 1 goes green (not a numbered gate) · Sprint-Ready / Execution /
Close stay Gate 2 / 3 / 4.* Never emit the token `Gate 2 (Ambiguity)`.

⊕ **R13 (enforcement §6.2 `:294-295`).** Collapse the two "Under v2 / Under v1" bullets into one:
off-surface files cause a non-zero exit and the commit is blocked (always enforced);
`SKIP_SURFACE_GATE=1` is the sole bypass. Grounding: STORY-051-01 (landed, `086788ea`) removed
`detect_execution_mode()` from `file_surface_diff.sh` — there is no v1 path. Leave the third bullet
(`SKIP_SURFACE_GATE=1`) and §6.2a (STORY-051-02's retirement note) untouched.

⊕ **R14 (enforcement §12 `:448`, `:452`).** Sprint close is **Gate 4**, per protocol §4 `:121-125`
("Gate 4 — Close-Ack" … "Violation is a Gate-4 breach") and enforcement §14's own heading ("Sprint
Close Gate (Gate 4)"). Retitle the §12 heading from "Gate 3.5 — Sprint Close Acknowledgement" to the
Gate-4 framing and rewrite §12.1's "Gate-3-class action … (§4 Gate 3)" to the Gate-4 equivalent,
keeping the posture analogy to `cleargate_push_item` (which is a **Gate-1-green** push after 09's own
re-map — say "the same posture as the Gate-1-green `cleargate_push_item` approval", not "§4 Gate 3").
**Leave the `(source: protocol §27)` suffix on `:448` alone** — phantom-annotation cleanup is the
carry-over CR, not this story.

**(c) Test shape (the QA-Red target).**

**One** new file: `cleargate-cli/test/scaffold/enforcement-doc-coherence.node.test.ts`. Follow the
landed `test/scaffold/ratchet-retired.node.test.ts` shape exactly — `// @cleargate-tier: scaffold`
marker, `describe`/`it`, `REPO_ROOT = process.env.CLEARGATE_META_ROOT ?? resolve(__dirname,'..','..','..')`.

**The `CLEARGATE_META_ROOT` override is mandatory**, not optional: the default resolution points at
the outer-meta **MAIN** checkout, which does not carry the story worktree's edits, so the suite is
red pre-merge without it (FLASHCARD 2026-07-18 `#test-harness #cross-repo`).

**Assertion target is canonical + live-outer — NOT the payload (G9-1).** Six clusters:

1. **Gate-token absence.** `grep -rn "Gate 1 (Initiative approval)\|Gate 2 (Ambiguity\|Gate 3 (Push)"`
   over root `CLAUDE.md`, `cleargate-planning/CLAUDE.md`, and both protocol copies → 0 hits.
   *Baseline*: 5 protocol hits (`:254, :348, :349, :512` + the `:369` variant) + 2 `CLAUDE.md` hits
   (root `:150`, canonical `:28`) → **RED**.
2. **Phantom paths / module / placeholder.** `.cleargate/plans` → 0 in both protocol copies;
   `triage-classifier` → 0 in both rubric copies; ⊕ `§<N>` → 0 in both protocol copies, and §4
   `:119` cites `§13`.
   *Baseline*: 2 + 2 + 1 hits → **RED**.
3. **Bug.md anchor resolution.** Parse the `<instructions>` POST-WRITE BRIEF list; for each line
   matching `^\s+- (\S+.*?)\s+←\s+(.+)$`, resolve the right-hand side against the body's headings.
   **Whitelist the two non-heading anchors** — `Prior work ← cleargate-wiki-query result …` (added
   by 07) and `Ambiguity ← bottom-of-doc ambiguity gate block` — or the test fails on correct
   content (G9-2). Also assert literal absence of `§0.5` and `§2 Impact`.
   *Baseline*: 3 unresolvable anchors → **RED**.
4. **Orphan absence.** `fs.existsSync` false for the canonical + live-outer copies. Payload copy:
   assert **only if** `templates/cleargate-planning/` exists AND gate it behind a comment explaining
   the deferred-prebuild model — or omit it (preferred; the payload leg cannot be green pre-merge).
   *Baseline*: both exist → **RED**.
5. **Autonomy-hook attribution (the only behavioral leg).** Build a tmpdir fixture:
   `.cleargate/sprint-runs/<id>/.active` sentinel + `state.json` with `sprint_status: "Active"` +
   `.dispatch-<ts>.json` carrying `agent_type: "developer"`; invoke the **canonical**
   `pre-tool-use-autonomy.sh` with `CLAUDE_PROJECT_DIR` pointed at the tmpdir and an
   `AskUserQuestion` payload on stdin; assert the appended
   `.cleargate/hook-log/autonomy-warnings.log` row's tab-separated agent column is `developer`.
   Real shell, no mocks. Hook always `exit 0` (soft mode) — assert on the log, not the exit code.
   *Baseline*: logs `unknown` → **RED**.
6. ⊕ **Enforcement-doc coherence (AD#2 legs), both tiers.** `Under v1` and `Under v2` → 0 hits in the
   §6.2 region; `Gate 3.5` and `Gate-3-class` → 0 hits in the whole file. **Scope the assertions to
   these exact tokens** — a blanket `\bv1\b|\bv2\b` scan reds on the five carry-over lines
   (`:248, :329, :388, :398, :414`) that AD#2 explicitly defers.
   *Baseline*: 2 + 2 hits → **RED**.

`launch_wave.mjs`'s "comment-only diff" DoD is not mechanically testable from a fresh checkout
(no baseline to compare against inside the test). Verify it as `git diff --unified=0
cleargate-planning/.cleargate/scripts/launch_wave.mjs | grep '^[+-]' | grep -v '^[+-][+-]'` showing
only `//`/` *`-prefixed lines — a §2.2 manual step, not a node:test assertion. Do not invent a
snapshot fixture for it.

**(e) Risks + first-bounce hints.**

- **G9-1 (HIGHEST bounce risk — the story's test target is wrong against landed convention).**
  §3.1 says the new test asserts "all against the payload scaffold copies (self-contained in the cli
  repo)" and §4.1 test #3 asserts "canonical/live-root/payload are byte-identical". Both contradict
  the convention M0 established and the orchestrator accepted: `copy-planning-payload.mjs` hardcodes
  its source to the outer-**MAIN** `cleargate-planning/` with no worktree or env override, so the
  payload reflects a story's edits only **after merge + the once-per-wave prebuild**. A
  payload-asserting test is structurally red at story time. Precedent: STORY-051-02's
  `ratchet-retired.node.test.ts` was written payload-first, hit exactly this, and its landed header
  now documents the correction ("this test intentionally does NOT assert against the npm payload
  mirror"). **Assert canonical + live-outer; drop or conditionally-skip the payload leg.** Also
  reuse — do not duplicate —
  `cleargate-cli/test/scaffold/canonical-live-parity.red.integration.node.test.ts`, which already
  guards canonical↔live parity for `.cleargate/scripts/*` (note: it is an `*.integration.*` file, so
  it runs under `npm run test:integration`, not the default `npm test`).
- **G9-2 (Bug.md anchor test over-reaches on correct content).** After 07, the anchor list has
  **six** entries, two of which legitimately do not name a body heading (`Prior work ←
  cleargate-wiki-query result`, `Ambiguity ← bottom-of-doc ambiguity gate block`). A naive
  "every anchor resolves to a heading" assertion reds on correct content. Whitelist those two
  explicitly and assert the whitelist by name so the intent is auditable.
- **G9-3 (wrong-direction reconciliation on `launch_wave.mjs`).** Keep the header `:11` and the
  direct-run banner. **The banner text at `:310-315` is a string literal inside `process.stdout.write(...)`
  — executable, not comment.** It already states the settled contract ("Every wave runs through this
  launcher via the Workflow tool (/workflows)."); changing it would both violate the comment-only DoD
  and soften the CR-074-settled §23 claim. Only `:227`, `:250`, `:276`, `:307-308` move.
- **G9-4 (do not chase the live `/.claude` agent drift).** Verified today: `.claude/agents/qa.md`,
  `devops.md`, `reporter.md` **DRIFT** from canonical (05's Gate-4-deferred sync);
  `.claude/hooks/pre-tool-use-autonomy.sh`, `token-ledger.sh`, and `skills/sprint-execution/SKILL.md`
  are **SAME**. 09 owns exactly one `/.claude` file (the autonomy hook) and must leave the three
  drifted agents alone. A bare `cleargate doctor` in this repo will keep reporting that drift through
  wave5 — it is 06's intended real-world proof, resolved at Gate-4. **QA-Verify must not red 09 on it.**
- **G9-5 — RESOLVED (AD#4).** Protocol §4 Gate-3 `:119`'s literal `§<N>` placeholder resolves to
  `cleargate-enforcement.md` **§13 "Sprint Execution Gate (Gate 3)"** (`:463`). Edit the `§<N>` token
  ONLY; the `v1`/`v2` vocabulary on that same line is 05's already-swept territory and the sentence's
  "always enforced" framing stays.
- ⊕ **G9-6 (AD#2 scope discipline — do not over-scrub `cleargate-enforcement.md`).** 09 owns exactly
  four line regions in that file: `:294`, `:295`, `:448`, `:452`. Five other `v1`/`v2` lines
  (`:248, :329, :388, :398, :414`) and every `(source: protocol §NN)` annotation are the named
  carry-over CR — **leave them**, including the `(source: protocol §27)` suffix on the very heading
  at `:448` that 09 is retitling. A Developer who "cleans while in there" widens the surface and
  trips the file-surface gate. The cluster-6 test tokens are scoped accordingly.
- ⊕ **G9-7 (enforcement.md line anchors survive 08).** 08's wave4 edits to that file are all at
  `:459+` (§12.3, §14.1, §14.2, §15) — **below** 09's `:294-295`, `:448`, and `:452`. 08 appends
  prose, so none of 09's three anchors shift. Re-`grep` anyway rather than trusting the numbers
  after the rebase.
- **Rebase discipline.** 09's worktree must be cut **after** 08 merges. 08 appends a clause to
  `CLAUDE.md` lines that sit *below* 09's targets (canonical `:36`/`:63` vs 09's `:28`; root
  `:158`/`:185` vs 09's `:150`), so 09's line anchors survive 08 unchanged — but re-`grep` rather
  than trusting the numbers.
- **DOGFOOD SYNC.** Verified byte-identical canonical↔live today for protocol, enforcement, rubric,
  Bug.md, launch_wave, and the autonomy hook. `diff` before writing anyway; hand-port only intended
  lines. The two `git rm` deletions must hit both tracked tiers in one commit or G8-6's parity
  checkers red.

**Reuse (no duplication) — verified, clone these:**
- `cleargate-cli/test/scaffold/ratchet-retired.node.test.ts` — the scaffold-assertion test template:
  `@cleargate-tier` marker, `CLEARGATE_META_ROOT` override, canonical+live-outer targeting, the
  explicit "not the payload" scope note. Clone its structure wholesale.
- `cleargate-cli/test/scaffold/canonical-live-parity.red.integration.node.test.ts` — existing
  canonical↔live parity guard for `.cleargate/scripts/*`. Reference it; do not re-implement parity.
- `token-ledger.sh:191` (`jq -r '.agent_type // empty'`) — the already-correct sibling read R10 copies.
- `write_dispatch.sh:20` — proves the dispatch payload key is `agent_type` (validated set:
  `developer|architect|qa|reporter|devops|cleargate-wiki-contradict`).
- Protocol §4 `:98-128` — the spine. Quote its gate names verbatim in every re-mapped reference so
  wording does not fork.
- ⊕ Enforcement §13 `:491` and §14 heading `:495` — the landed always-enforced sentence shape and the
  Gate-4 naming R13/R14 align to. Do not invent new phrasing.
- SKILL.md §C.10 (STORY-043-09) — the landed precedent wording for a de-classifier'd triage
  reference; mirror it into the rubric doc.

---

## Cross-story risks

1. **`close_sprint.deferred-verify.red.node.test.ts` — edit (08) then delete (09). Order-critical.**
   08 adds one line — `CLEARGATE_CI_ACK: '1',` — inside `baseEnv()` at `:82-95`, in the canonical and
   live-root copies. 09 `git rm`s canonical + live-root (payload drops at prebuild). Git resolves
   edit→delete cleanly; delete→edit would leave 08 with a vanished target. **Do not "optimize" by
   having 09 delete it early and 08 skip the edit** — 08 must be independently green at its own merge
   point (its §2.2 lists a standalone run of that file), and reversing the order inverts the wave
   dependency. If a Developer or QA questions editing a doomed file, that is the answer.

2. **`CLAUDE.md` (root + canonical) serial chain 05 → 07 → 08 → 09, four distinct paragraphs.**
   05 re-injected the whole bounded block; 07 rewrote the duplicate-check + Ambiguity-Gate paragraphs
   (both landed and verified present today); 08 appends the `--assume-ack` guard clause to the
   sprint-close guardrails (canonical `:36`/`:63`, root `:158`/`:185`, plus meta-only root `:126`);
   09 re-maps the gate-halt sentence (canonical `:28`, root `:150`). No two overlap. The wave4→wave5
   barrier is what keeps it mechanical — cut 09's worktree only after 08 merges.

3. **`cleargate-enforcement.md` is now a THREE-story shared surface: 02 (M0, landed), 08 (wave4),
   09 (wave5).** Post-amendment ownership, strictly disjoint:

   | Region | Owner | Wave |
   |---|---|---|
   | §6.2a retirement note | STORY-051-02 | M0 (landed) |
   | ⊕ §-index row `:20`, **3rd column only** (AD#5) | **STORY-051-09** | wave5 |
   | §6.2 `:294-295` v1/v2 bullets | **STORY-051-09** | wave5 |
   | §12 heading `:448` + §12.1 `:452` gate naming | **STORY-051-09** | wave5 |
   | ⊕ `:461`, **`Gate-3 breach` gate-number token only** (AD#5) | **STORY-051-09** | wave5 |
   | §12.3 `:459-463` Flag Reservation, **minus the `:461` gate-number token** | **STORY-051-08** | wave4 |
   | §14.1 `:503-504` + §14.2 `:511-512` | **STORY-051-08** | wave4 |
   | §15 `:544` advisory scope | **STORY-051-08** | wave4 |
   | `:248, :329, :388, :398, :414` + `(source: protocol §NN)`, incl. the `:20` 2nd column | **carry-over CR** | not this sprint |

   ⊕ **Post-AD#5 the "08 owns everything `:459+`" shorthand no longer holds.** 09's regions are `:20`,
   `:294-295`, `:448`, `:452`, and the single gate-number token on `:461`; 08 keeps the remainder of
   §12.3 — including its landed `CLEARGATE_CI_ACK` sentence at `:463` — plus §14.1/§14.2/§15. The
   `:461` split is intra-line and token-scoped: 09 rewrites `Gate-3 breach` to the Gate-4 equivalent
   and changes nothing else on that line. The wave4→wave5 barrier orders them and 08's appends do not
   shift 09's anchors (G9-7). Neither story may touch the other's rows or the carry-over rows.

4. **`cleargate-protocol.md` — 05 (M1, landed) and 09 (M2).** 05 edited only §4 `:119` + `:864`;
   §4 was correctly **not** renumbered. 09 re-maps §6/§8/§9/§10/§12 and (per AD#4) fixes the `§<N>`
   token on `:119` — the *same line* 05 touched, but a different token (05 did the `v1`/`v2` vocab;
   09 does the cross-ref). 05 has merged, so this is a sequential edit, not a conflict. The M1→M2
   barrier orders them; a rebase is mechanical.

5. **08 no longer adds a `.node.test.ts` to a scaffold dir (AD#1 resolved).** With the guard test
   relocated to `cleargate-cli/test/scripts/`, the sprint ships zero new payload-bound test files and
   09's R9 rationale ("a `.node.test.ts` must not ship in the payload") holds without qualification.
   After wave5's prebuild, `find cleargate-cli/templates/cleargate-planning -name '*.node.test.ts'`
   returns nothing — a clean, checkable end state.

6. **Payload mirror + `MANIFEST.json` are shared by 08 and 09 but are NOT a per-story collision
   surface.** Derived artifacts, regenerated once by `npm run prebuild` from the outer-MAIN tree after
   each wave merges. No story hand-edits them; no story commits per-branch prebuild output (MANIFEST
   checksum collisions — FLASHCARD 2026-07-18 `#dogfood #sync #build`). Note that
   `readme-qa-doc-truth-043-06.red.node.test.ts` S6 (`diff -q` canonical vs payload `qa.md`) stays red
   until the M1 prebuild runs — a known deferred-prebuild artifact, not an M2 regression.

7. **cli MAIN-checkout index.** M2 has no intra-wave cli race (one story per wave), but both stories
   commit into the same gitignored cli repo on branch `sprint/S-38`, serialized by the wave barrier.
   08 touches `package.json`, `src/cli.ts`, `src/commands/sprint.ts`, and one new test; 09 touches one
   new test. File-disjoint. Stage explicit paths; never `git add -A` in the cli tree.

---

## Wave execution notes

**Worktree base.** Both stories branch off `sprint/S-38` (`63a2c5ec`). Outer-repo scaffold work is
worktree-isolated; cli work lands on a branch in the cli MAIN checkout
(`/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli`), never an outer worktree. 09's `/.claude`
hook edit is gitignored — hand-port only, do not stage.

**wave4-before-wave5 barrier (hard).** STORY-051-08 must reach a merged state on `sprint/S-38` before
wave5's worktree is cut. Three enforcement reasons (edit-before-delete; `CLAUDE.md` chain;
`cleargate-enforcement.md` three-way ownership) — Cross-story risks 1, 2, 3. Non-negotiable.

**§Prebuild (once per wave, deferred).** After wave4 merges, run `npm run prebuild` from
`cleargate-cli/` ONCE; commit payload changes in the cli repo and the `MANIFEST.json` change in the
outer repo. Repeat after wave5 merges — that second run is what drops the orphan test from the
payload and satisfies 09's third-tier absence claim. Do NOT run or commit prebuild output on a story
branch.

**§Gate-4 sync backlog (carried, not M2's to fix).** Outstanding at wave4 start: live
`.claude/agents/{qa,devops,reporter}.md` drifted (05); cli `dist` not rebuilt; payload behind M1
canonical. All Gate-4 items. QA-Verify must not red 08 or 09 on any of them.

**§Gate-4 close procedure for SPRINT-38 itself (new, AD#3).** After 08 merges, `close_sprint.mjs`
refuses `--assume-ack` without `CLEARGATE_CI_ACK=1`. The sprint's own close therefore runs as:
`close_sprint.mjs SPRINT-38` with no flags → surface the prompt verbatim → **halt for explicit human
close authorization** → then the orchestrator sets `CLEARGATE_CI_ACK=1` for that single invocation
and re-runs with `--assume-ack`. The token is never set unprompted and is never exported into the
shell session. Note the closer runs the *live* `.cleargate/scripts/close_sprint.mjs`, which carries
the guard as of wave4.

**Sync discipline (every edit).** `diff` canonical↔live BEFORE overwriting. M2 baseline verified
2026-07-27: **every** M2 canonical↔live pair is byte-IDENTICAL (`close_sprint.mjs`,
`cleargate-enforcement.md`, `close_sprint.deferred-verify.red.node.test.ts`, `cleargate-protocol.md`,
`mid-sprint-triage-rubric.md`, `Bug.md`, `launch_wave.mjs`, `pre-tool-use-autonomy.sh`). Direction is
canonical→live. Never blind-`cp`. [FLASHCARD 2026-07-17 `#dogfood #sync`]

**Protocol numbering resolver (run this session).** Highest numbered protocol heading =
**`## 23. Execution Contract`** (`:843`). Next free = **§24**. Neither M2 story appends a new protocol
section, so no new number is emitted. 09 edits only existing sections (§4 `:119` token, §6, §8, §9,
§10, §12) — all confirmed present. Note the corpus gap: protocol jumps §14 → §21, so **§15-§20 do not
exist**; every `(source: protocol §15)`…`(source: protocol §27)` annotation in
`cleargate-enforcement.md` is a dangling cross-reference — routed to the carry-over CR by AD#2, NOT
to 09.

---

## Open decisions for orchestrator

**None outstanding.** All four decisions raised in the original dispatch of this plan were resolved
by the orchestrator on 2026-07-27 and are recorded verbatim below; the blueprints above are updated
in place to match. No further Architect escalation is pending for M2.

**Named carry-over (for the Reporter at sprint close):**
> **CR candidate: "enforcement.md residual v1/v2 vocabulary + phantom source annotations."**
> Scope: `cleargate-enforcement.md` lines `:248` ("both v1 and v2"), `:329` (§7 heading "(v2)"),
> `:388` (§10.2 heading "(v1)"), `:398` ("does NOT block in v1"), `:414` (§10.5 "v2 Escalation Path"),
> plus every `(source: protocol §15)`…`(source: protocol §27)` annotation, all of which point at
> protocol sections that do not exist (protocol jumps §14 → §21). Deferred out of SPRINT-38 by
> orchestrator ruling AD#2 as cosmetic — none of these contradict landed behavior, unlike the four
> clusters folded into 08 and 09. Note that `check-no-exec-mode-vocab.mjs`'s `EXCLUDE_PATTERN`
> contains `cleargate-enforcement\.md`, so the sprint's own vocabulary gate is permanently blind to
> this file; the CR should decide whether to drop that exclusion after the cleanup.

---

## Mid-Sprint Amendment (orchestrator, 2026-07-27)

_Append-only. Recorded verbatim by the Architect on orchestrator ruling; earlier plan content above
was updated in place so Dev/QA dispatches read one consistent plan. Never rewrite this section._

`2026-07-27 · SPRINT-38 M2 · orchestrator rulings on Architect Open Decisions #1-#4 (M2.md)`

**AD#1 — APPROVED as recommended.** 08's guard test relocates to
`cleargate-cli/test/scripts/close-sprint-assume-ack-guard.node.test.ts`. Rationale to record: the
spec'd `.cleargate/scripts/` path is un-globbed by `run-default-tests.mjs:23` and would ship into the
npm payload via the unfiltered `copy-planning-payload.mjs` — a dead gate, and the exact defect 09's
R9 deletes. Same correction the orchestrator accepted for 05 in M1. 08's §3.1 is self-amended
in-commit with this justification (file-surface contract §9).

**AD#2 — PARTIAL. Fold only the actively-false clusters; carry the cosmetic residue.**
- `cleargate-enforcement.md:503-504` + `:511-512` (Steps 2.7/2.8 "advisory under v1") → **assign to
  08**. They contradict the §15 sentence 08 lands, in a file 08 already owns.
- `:294-295` (file-surface hook advisory under v1) → **assign to 09**. Contradicted by landed 01.
- Enforcement §12's "Gate 3.5 / Gate-3-class" naming for sprint close → **assign to 09**. It is a
  literal four-gate-spine violation and R1 claims repo-wide coverage; 09 is already in the file for
  `:294-295`.
- Remaining cosmetic v1/v2 mentions (`:248`, `:329`, `:388`, `:398`, `:414`) and the
  `(source: protocol §15-§27)` phantom annotations → **NOT this sprint.** Record them in the plan as a
  named carry-over CR candidate ("CR: enforcement.md residual v1/v2 vocabulary + phantom source
  annotations") so the Reporter picks them up at close.
- Both stories self-amend their §3.1 to add `cleargate-enforcement.md` (09) / extend it (08) with the
  justification in-commit.

**AD#3 — RESOLVED (b)+(c), no halt needed.** The guard ships as ratified in Q2 — `--assume-ack`
refuses unless `CLEARGATE_CI_ACK=1`. 08's doc wording must state: the token is **never set unprompted
by an agent**; it is set for a single invocation only after an explicit human close authorization
(Gate 4), or by CI. That is code-truthful, preserves the mechanical teeth, and matches the standing
operator preference (human gives the go; orchestrator does the typing). Additionally **fold into 08**
the two now-incomplete CLI strings you flagged: `cli.ts:347` option help text and `sprint.ts:788`'s
"run `cleargate sprint close … --assume-ack` first" message — both must mention the
`CLEARGATE_CI_ACK=1` requirement. Text-only edits, no behavior change; add both to 08's §3.1 and drop
them from its MUST-NOT-touch list. Note SPRINT-38's own Gate-4 close will run under this rule.

**AD#4 — APPROVED.** Protocol §4 `:119`'s `§<N>` placeholder resolves to `cleargate-enforcement.md`
§13 (Sprint Execution Gate / Gate 3) → **assign to 09**, same defect class as R5.

**Also confirmed:** the stale "Held at 🟡" blockquote strike in STORY-051-08's Ambiguity Gate stays
assigned to **08**, as the Architect recommended.

**Architect application notes (what changed above, for auditability):**
- 08 file list: guard-test path relocated (AD#1); `enforcement.md:503-504`/`:511-512` added (AD#2);
  `cli.ts:347` + `sprint.ts:788` added and removed from MUST-NOT-touch (AD#3); the STORY-051-08
  delivery doc added for the §3.1 self-amendment + Ambiguity-Gate blockquote strike.
- 08 integrity delta: new §14.1/§14.2 bullet; §12.3 and CLAUDE.md wording switched to the AD#3
  formulation ("never set unprompted by an agent … single invocation after explicit human close
  authorization, or by CI").
- 08 test shape: target path fixed; two new RED doc legs (AD#2 §14 token absence, AD#3 CLI-string
  token presence). New gotcha **G8-8** — `sprint.ts:788` must keep the `sprint not closed` prefix
  intact; `sprint-archive-stamp.integration.node.test.ts:552` and `sprint-archive.node.test.ts:440`
  both `includes()` it. No test asserts on the `cli.ts:347` help string.
- 09 file list: `cleargate-enforcement.md` added to canonical + live-root (AD#2); protocol `:119`
  `§<N>` fix added (AD#4); the STORY-051-09 delivery doc added for the §3.1 self-amendment;
  MUST-NOT-touch rewritten to fence 08's regions and the carry-over lines.
- 09 requirements: new **R12** (`§<N>` → `§13`), **R13** (§6.2 `:294-295`), **R14** (§12 `:448`/`:452`
  Gate-4 renaming), each with verified grounding.
- 09 test shape: new cluster 6 (enforcement-doc coherence), token-scoped so it does not red on the
  five carry-over lines. New gotchas **G9-6** (do not over-scrub enforcement.md) and **G9-7**
  (08's appends do not shift 09's `:294-295`/`:448`/`:452` anchors).
- Cross-story risk 3 rewritten as a seven-row `cleargate-enforcement.md` ownership table; risk 4
  updated (09 now co-edits protocol `:119`); risk 5 rewritten (AD#1 removes the 08↔09 contradiction);
  risk 7 updated for 08's two new cli source files. Order §1 gains reason (c).
- Wave execution notes: new "§Gate-4 close procedure for SPRINT-38 itself" (AD#3); protocol-resolver
  note routes the phantom `(source: protocol §NN)` annotations to the carry-over CR.
- Open decisions section replaced with "none outstanding" + the named carry-over CR candidate.
- **Verified before recording:** `sprint.ts:788` is the sole definition site of the "sprint not
  closed" message and only two tests match it, both on the `sprint not closed` substring (safe under
  an append); `cli.ts:347` has zero asserting tests; enforcement `:294-295`, `:448`, `:452`,
  `:503-504`, `:511-512` all confirmed present at those exact lines in both canonical and live copies
  (byte-identical); enforcement §13 confirmed at `:463` as the `§<N>` referent.

`2026-07-27 · SPRINT-38 M2 · orchestrator ruling on the two wave-5 scope additions raised at wave-4 post-flight`

**AD#5 — APPROVED (both wave-5 scope additions).** Raised by the Architect in the wave-4 post-flight
pass and approved in writing by the orchestrator at that time. The contemporaneous record is
`.cleargate/sprint-runs/SPRINT-38/reports/STORY-051-08-arch.md`, heading
`## Wave-5 scope additions raised — both APPROVED by the orchestrator` (committed in `91bb87f1`).
Both rulings were passed **verbatim** into the wave-5 QA-Red and Developer dispatch prompts, which is
why the RED test asserts them and the implementation lands them. Recorded in this plan late: the
omission was an orchestrator record-keeping gap, **not** a scope expansion by any agent and **not** a
Developer defect. Nothing already landed is reverted; no RED assertion is trimmed. Verbatim from the
report:

> - (a) `cleargate-enforcement.md:20` §-index row: retitle the 3rd column only (drop "Gate 3.5"),
>   leave `protocol §27`. Without it, 09's whole-file `Gate 3.5` assertion reds against itself.
> - (b) `:461` "Gate-3 breach" gate-number token released from 08's region to 09's R14 —
>   numbering only, no other §12.3 change.
> - Informational: `sprint-execution/SKILL.md:625` "Gate-3-class action" → carry-over CR, not wave 5.

**Both (a) and (b) assign to STORY-051-09, folded into R14.** Scope is strictly the two gate-number
tokens — nothing else on either line:
- **`:20`** — 3rd column text only. The 2nd column `protocol §27` phantom annotation stays untouched
  (carry-over CR, AD#2), exactly as with the `(source: protocol §27)` suffix on the `:448` heading.
- **`:461`** — the literal `Gate-3 breach` token only, rewritten to the Gate-4 equivalent. Every other
  byte of §12.3 stays as 08 landed it, **including 08's appended `CLEARGATE_CI_ACK` sentence, which
  stays byte-identical**. 08 does not reopen; 09 edits the token in place after the wave4→wave5
  barrier.
- **`sprint-execution/SKILL.md:625`** is **NOT** wave-5 scope. It joins the named carry-over CR
  ("enforcement.md residual v1/v2 vocabulary + phantom source annotations") — Reporter picks it up at
  close alongside the `:248/:329/:388/:398/:414` residue.

Grounding (Read/Grepped 2026-07-27 post-08-merge; canonical and live-root byte-identical):
`:20` = `| §12 | protocol §27 | Gate 3.5 — Sprint Close Acknowledgement (CR-019) |`. `:461` = the Flag
Reservation body sentence ending "… is a Gate-3 breach equivalent to calling `cleargate_push_item`
without `approved: true`." `:463` = 08's landed `CLEARGATE_CI_ACK` sentence; §12.3 therefore spans
`:459-463` post-08 and §13 has shifted from `:463` to `:465` (re-`grep`, per G9-7). Whole-file counts:
`Gate 3.5` = 2 hits (`:20`, `:448`), `Gate-3-class` = 1 (`:452`), `Gate-3 breach` = 1 (`:461`).
**Necessity:** without (a), R14's whole-file `Gate 3.5` → 0-hits assertion (test cluster 6) is
unsatisfiable — `:20` is the second occurrence and it sits in 09's own target file. Without (b), the
whole-file scope leaves a live `Gate-3 breach` gate number thirteen lines below a §12 heading 09 has
just retitled to Gate 4 — the exact self-contradiction R14 exists to remove.

**Architect application notes (AD#5 — what changed above, for auditability):**
- 09 canonical file table: **R14 cont.** added — `:20` (3rd column) + `:461` (gate-number token).
  The live-root row already names `cleargate-enforcement.md` generically; no change needed there.
- 09 MUST-NOT-touch: §12.3 range corrected to `:459-463` and fenced **except** the `:461` gate-number
  token; the `(source: protocol §NN)` fence extended to the `:20` 2nd-column cell;
  `sprint-execution/SKILL.md:625` added as an explicit carry-over fence.
- Cross-story risk 3 ownership table: two new 09 rows (`:20` 3rd column, `:461` gate-number token);
  08's §12.3 row re-ranged to `:459-463` **minus** the released token; carry-over row extended to the
  `:20` 2nd column; the "08's regions are all `:459+`" shorthand replaced with the accurate split.
- 08 file table: §12.3 cell annotated — 08 owns the append, not the `:461` gate-number token.
- Grounding notes: the STORY-051-08 §12.3 anchor correction annotated with the AD#5 reassignment.
- **Unchanged on purpose:** R13, R12/AD#4, all six test clusters, every 08 deliverable already merged,
  and AD#1–AD#4 above.

---

## Grounding notes (verification provenance — everything below Read/Grepped 2026-07-27)

**Story-file citations that are STALE against landed code (correct before dispatch):**
- STORY-051-08 §3.1: enforcement §15 advisory sentence is `:544`, not `:540`.
- STORY-051-08 §3.1: §12.3 Flag Reservation is `:459` (heading) / `:461` (body), not `:455-457`
  (those are §12.2 Steps A/B). ⊕ AD#5: post-08 the section spans `:459-463`, and the `Gate-3 breach`
  gate-number token on `:461` belongs to **09** (R14), not wholly to 08 — 08 owns the `:463` append.
- STORY-051-08 §Existing Surfaces: `isAdvisory()` call site is `sprint.ts:1715`, not `:1681`
  (shifted by 05's docstring sweep).
- STORY-051-08 §3.1: guard-test path is un-globbed and payload-shipping — **corrected by AD#1**.
- STORY-051-09 §3.1/§4.1: new test's payload-assertion target contradicts the landed M0 convention
  (G9-1).
- STORY-051-08 Ambiguity Gate: `Current Status: 🟢` while the trailing blockquote still says
  "Held at 🟡 … the recorded human decision must be updated to match before this goes Green."
  The ratification IS recorded (§1.4 "RATIFIED (Gate 1, 2026-07-17)"), so the blockquote is stale
  self-contradicting residue. **Assigned to STORY-051-08** (orchestrator-confirmed) — it is 08's own
  document, the fix is deleting one paragraph, and leaving it means the sprint that gave the
  Ambiguity Gate machine teeth ships a self-contradicting gate block. All five `- [x]` boxes are
  checked, so the predicate itself passes; this is prose integrity, not a gate failure.

**Confirmed accurate in the story files (no correction needed):**
- `close_sprint.mjs`: arg parse `:173-177` (`assumeAck` at `:177`), `usage()` exit 2 at `:123`,
  no-flag prompt string at `:1024`.
- `close_sprint.deferred-verify.red.node.test.ts`: `baseEnv()` `:82`, `...process.env` `:84`,
  `runClose()` `:103-128`, `--assume-ack` spawn `:110`. Six scenarios; Sc5/Sc6 read
  `REPO_ROOT/cleargate-planning/...` which is already broken in the canonical copy
  (`REPO_ROOT = __dirname/../..` = `cleargate-planning/`) — extra evidence the orphan is stale.
- `gate-mode.ts:14` `isAdvisory()`; `init_sprint.mjs:140`; `pending-task-sentinel.sh:129`.
- Canonical `CLAUDE.md:28/:36/:63`; root `CLAUDE.md:126/:150/:158/:185`; bounded block `:129`-`:188`.
- Protocol: §4 `:98-128`, Gate-3 `:119` (incl. the `§<N>` token), §6 `:161`, §8 `:183`, §9 flow
  `:204-206`, §10.2 `:254`, §10.8 `:348-349`, §10.9 `:369`, §12.6 `:512`. Max numbered heading =
  §23 `:843`.
- Enforcement: §6.2 `:294-295`, §12 heading `:448`, §12.1 `:452`, §12.3 `:459-461`, §13 `:463`
  (+ always-enforced sentence `:491`), §14 heading `:495`, §14.1 `:503-504`, §14.2 `:511-512`,
  §15 `:538-553` (advisory sentence `:544`).
- Rubric `:13`, `:155`. Bug.md anchors `:17/:18/:19`; body headings `:68/:81/:95`; 07's
  `## Prior work` landed at `:108` (6th `##`, before `## Context Source` `:117` — index-safe as
  planned). `launch_wave.mjs` `:11`, `:227`, `:250`, `:276`, `:307-308`, banner `:310-315`.
  `pre-tool-use-autonomy.sh:53`; `token-ledger.sh:191`; `write_dispatch.sh:20`.
- `cli.ts:347` option registration; `sprint.ts:426-427` flag forwarding; `sprint.ts:788` message.

**New facts established this session:**
- `close_sprint.mjs` contains **zero** `process.env.CLEARGATE_ADVISORY` reads — the `:639` comment
  claiming otherwise is false (G8-3), as are enforcement `:504` and `:512` (AD#2 → 08).
- `run-default-tests.mjs:23-27` globs `test/**/*.node.test.ts` minus `*.integration.*` and
  `test/fixtures/**`; nothing globs `.cleargate/scripts/`.
- `copy-planning-payload.mjs` applies **no** include/exclude filter — `.node.test.ts` ships.
- Exactly one `*.node.test.ts` exists under canonical scaffold and under the payload today: the
  orphan 09 deletes. Post-AD#1, the sprint adds none.
- `drift-check.ts:37-40` walks `.claude`, `.cleargate/{scripts,knowledge,templates}` — both M2
  stories' canonical edits fall inside 06's new drift guard.
- `--assume-ack` blast radius: 10 cli test files match the string; **5 genuinely spawn** close_sprint
  and **all 5** spread `...process.env` → the `package.json` prefix suffices, zero per-file edits.
  Of the 5 non-spawners, two assert on the `sprint not closed` message substring (G8-8).
- All five target `package.json` scripts exist: `test`, `test:node`, `test:integration`, `test:file`,
  `test:node:file`. 05 landed `check:no-execution-mode-vocabulary → scripts/check-no-exec-mode-vocab.mjs`
  (the M1 near-duplicate flag was resolved by consolidation); that script's `EXCLUDE_PATTERN`
  contains `cleargate-enforcement\.md`, so the vocab gate cannot see the AD#2 clusters — they must be
  covered by the two stories' own tests.
- Live `/.claude` drift snapshot: `agents/{qa,devops,reporter}.md` DRIFT; `hooks/pre-tool-use-autonomy.sh`,
  `hooks/token-ledger.sh`, `skills/sprint-execution/SKILL.md` SAME.

Version check: **N/A** — neither M2 story declares a dependency package (`db_write_set: []`, no
`package.json` `dependencies`/`devDependencies` delta; 08's `package.json` edit is script-prefix only).
No `npm view` required.

Constraints honored: node:test only (`*.node.test.ts`, `tsx --test`) — no vitest; no reintroduction of
`execution_mode` / `v1` / `v2` / `CLEARGATE_EXEC_MODE` / `CLEARGATE_PARALLEL_WAVES`; `CLEARGATE_ADVISORY=1`
remains the only enforcement-strength lever and gains **no** new honor site; canonical is source of
truth with diff-before-overwrite to live and deferred once-per-wave payload regeneration; cli edits
land in the separate `cleargate-cli` checkout, not an outer worktree.
