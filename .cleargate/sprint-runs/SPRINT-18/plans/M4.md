# Milestone: M4 — Sprint Plan + Sprint Report Template Reframe
## Stories: STORY-025-04
## Wave: W1 (parallel with CR-023, STORY-025-01, STORY-025-02)

> **role: architect** · M4 plan for SPRINT-18, written 2026-05-01.
> Source-of-truth inputs: `STORY-025-04.md` §1.2 R1–R8, `CR-021.md` §3.2.1 + §3.2.2, `SPRINT-18.md` §2.1 / §2.2 / §2.3 / §2.5.

## Order

**Strict order:** CR-023 (M0) → STORY-025-04 (M4) on the two shared template files. CR-023 lands its body-prose §-citation rewrites first; M4 applies the structural reframe atop a clean baseline. M4 may dispatch concurrently with M1 / M2 in Wave 1 — no surface overlap with those.

**Within M4:** doc-only edits in any internal order. Recommended sequence: (1) `<instructions>` block on Sprint Plan Template, (2) `## 0. Stakeholder Brief` insertion, (3) `<instructions>` `output_location` on sprint_report.md, (4) §4 Observe insertion + renumber pass, (5) canonical-mirror lockstep diffs.

**Pre-flight gate:** before Developer dispatch, re-grep line numbers of the four target files. CR-023 body-prose rewrites at `Sprint Plan Template.md:10` and `sprint_report.md:199` may shift hunks the instant CR-023 merges; the `<instructions>` block on `Sprint Plan Template.md` (lines 1–15) and the body §-headings on `sprint_report.md` (lines 25 / 41 / 64 / 106 / 135 / 199) are the load-bearing positions.

## Per-story blueprint

### STORY-025-04

**Files to modify** (4 files, all doc-only edits):

| # | File | Surface | Edit shape |
|---|---|---|---|
| 1 | `.cleargate/templates/Sprint Plan Template.md` | live | Replace `<instructions>` block (lines 1–15); insert `## 0. Stakeholder Brief` above current `## 1. Consolidated Deliverables` (currently line 52) |
| 2 | `cleargate-planning/.cleargate/templates/Sprint Plan Template.md` | canonical | Identical edits to #1 |
| 3 | `.cleargate/templates/sprint_report.md` | live | Update `<instructions>` `output_location` (line 5 — the `Output location:` field outside any explicit `<instructions>` block; sprint_report's `<instructions>` is conventional-comment shape, not tag-block — see "Gotcha 1" below); insert `## §4 Observe Phase Findings` between current `## §3 Execution Metrics` (line 64) and current `## §4 Lessons` (line 106); apply renumber map (closed set, see below) |
| 4 | `cleargate-planning/.cleargate/templates/sprint_report.md` | canonical | Identical edits to #3 |

**Files to create:** None.

**Schema changes:** None (doc-only).

**Renumber map for `sprint_report.md` (closed set — pinned interpretation (i), all-shift):**

| Before | After | Action |
|---|---|---|
| `## §1 What Was Delivered` (line 25) | `## §1 What Was Delivered` | unchanged |
| `## §2 Story Results + CR Change Log` (line 41) | `## §2 Story Results + CR Change Log` | unchanged |
| `## §3 Execution Metrics` (line 64) | `## §3 Execution Metrics` | unchanged |
| *(new — inserted between §3 and §4)* | `## §4 Observe Phase Findings` | INSERT |
| `## §4 Lessons` (line 106) | `## §5 Lessons` | rename |
| `## §5 Framework Self-Assessment` (line 135) | `## §6 Framework Self-Assessment` | rename |
| `## §6 Change Log` (line 199) | `## §7 Change Log` | rename |

**Why interpretation (i) (all-shift) wins over (ii):** SPRINT-18.md §2.5 ADR-Conflict Flags surfaces this as the only open decision; it recommends interpretation (i). Pinning rationale here:

1. **Ordering semantics are preserved.** §4 Observe is a real new section bearing real new content; the report is sequential prose meant to be read top-to-bottom. Shifting Lessons / Framework Self-Assessment / Change Log all by +1 keeps the relative ordering between each existing section and the new insertion unambiguous.
2. **CR-021 §3.2.2 explicitly enumerates only `§4 Lessons → §5` and `§5 Retrospective → §6`** — but its lookup of "§5 Retrospective" is a stale reference (the live template has `§5 Framework Self-Assessment`, not `§5 Retrospective`; Retrospective is a structural concept inside Framework Self-Assessment). The all-shift interpretation extends CR-021's stated intent ("everything below §3 shifts by +1") without rewriting CR-021's prose.
3. **Interpretation (ii) would orphan `## §6 Change Log`** in numerical sequence. After §6 Framework Self-Assessment renumber to (ii)'s scheme, Change Log would either retain §6 (creating two §6s) or be implicitly skipped. Both are silent failure modes. (i) is the only safe answer.
4. **Cross-reference cost is zero.** External grep (see "Gotchas surfaced" below) finds 13 hits on `## §4 Lessons` / `## §5 Framework Self-Assessment` across `.cleargate/scripts/test/*.sh` and `cleargate-cli/test/scripts/fixtures/**/REPORT.md`. None of those files cite `## §6 Change Log`, so renumbering it to §7 is free at the external-reference level. (External references are tracked in cross-story risks below — they belong to STORY-025-03.)

**Renumber map for the new §4 Observe insertion** — verbatim shape from CR-021 §3.2.2 + STORY-025-04 §1.2 R5. Closed-set:

```markdown
## §4 Observe Phase Findings

> Populated from sprint plan §4 Execution Log entries dated within the Observe window
> [last-story-merge-timestamp, sprint-close-timestamp]. Reporter date-filters and groups by event type.
>
> SKIP THIS SECTION ENTIRELY (no header, no body) if all three subsections are empty.
> Output a single line in its place: "Observe phase: no findings."

### 4.1 Bugs Found (UR:bug)
| Date | Description | Resolution | Commit |

### 4.2 Hotfixes Triggered
| ID | Trigger | Resolution | Commit |

### 4.3 Review Feedback (UR:review-feedback)
| Date | Description | Status (folded / deferred) | Deferred to / Rationale |
```

**Renumber map for the `<instructions>` block on `Sprint Plan Template.md`** — replace lines 1–15 verbatim with CR-021 §3.2.1's actively-authored block (already quoted in STORY-025-04 §1.2 R1 and CR-021.md lines 173–209). Closed-set, no clauses dropped or added:

- Six labeled clauses: `WHAT TO GATHER`, `HOW TO GATHER`, `ANALYSIS REQUIRED`, `WHERE TO WRITE`, `POST-WRITE BRIEF`, `DUAL-AUDIENCE STRUCTURE`.
- POST-WRITE BRIEF must contain the six bullets verbatim per STORY-025-04 §1.2 R3: Sprint Goal / Selected items / Recommended priority changes / Open questions / Risks / Ambiguity + Gate 2.

**`## 0. Stakeholder Brief` body-skeleton insertion** (above current `## 1. Consolidated Deliverables`, ~10 lines):

```markdown
## 0. Stakeholder Brief
*(Sponsor-readable summary. Pushed to PM tool. Pair with §3 Risks below.)*

- **Sprint Goal:** {1 sentence}
- **Business Outcome:** {what the user / sponsor gets}
- **Risks (top 3):** {bullet list, see §3 for full table}
- **Metrics:** {expected impact / KPIs}
```

**Test scenarios (from Gherkin §2.1, 8 scenarios — all doc-grep, no runtime):**

1. Sprint Plan `<instructions>` contains `"actively authored during the Prepare phase"` AND does NOT contain `"READ artifact"` or `"Do NOT draft this file manually"`.
2. Sprint Plan `<instructions>` POST-WRITE BRIEF section enumerates all six bullets.
3. Sprint Plan body contains `## 0. Stakeholder Brief` heading above `## 1. Consolidated Deliverables`.
4. Sprint Plan `<instructions>` declares DUAL-AUDIENCE STRUCTURE with top-of-body / bottom-of-body split.
5. Sprint Report body contains `## §4 Observe Phase Findings` heading; §4 contains `"SKIP THIS SECTION ENTIRELY"` blockquote; §4 has three subsections 4.1 / 4.2 / 4.3.
6. Sprint Report body has `## §5 Lessons` (formerly §4) AND `## §6 Framework Self-Assessment` (formerly §5) AND `## §7 Change Log` (formerly §6). *(Story §2.1 names §5 + §6 only; §7 is the all-shift extension pinned in this plan.)*
7. Sprint Report `<instructions>` `output_location` reads `.cleargate/sprint-runs/<sprint-id>/SPRINT-<#>_REPORT.md`; does NOT read `.../REPORT.md`.
8. Mirror diff empty for both `Sprint Plan Template.md` and `sprint_report.md` (live ↔ canonical).

Implement as cleargate-cli vitest cases under `cleargate-cli/test/scripts/template-stubs.test.ts` (extend) — add 8 new `it()` blocks reading the four template files via `fs.readFileSync` and grepping the assertions above. Do NOT create a new test file unless `template-stubs.test.ts` exceeds 300 LOC after extension; if so, create `cleargate-cli/test/scripts/template-content.test.ts` per STORY-025-04 §4.1.

**Reuse (no duplication):**

- `cleargate-cli/test/scripts/template-stubs.test.ts` already asserts mirror-parity (live = canonical) for both templates. Extend its existing test harness; do NOT re-implement diff logic.
- The `<instructions>` block from CR-021 §3.2.1 (lines 173–209 of CR-021) is verbatim source — copy verbatim, do not paraphrase.
- The `## §4 Observe Phase Findings` shape from CR-021 §3.2.2 (lines 220–237 of CR-021) is verbatim source — copy verbatim.
- The `## 0. Stakeholder Brief` skeleton has a working example in `SPRINT-18.md` lines 47–54 (the §0 block of the in-flight sprint plan). Mirror that structure.

**Gotchas surfaced from code inspection:**

1. **`sprint_report.md` has no `<instructions>...</instructions>` tag-block.** Grep confirms: the file's only metadata-shape comments are HTML `<!-- ... -->` blocks (lines 9–15 declaring template version + event-type vocabulary, line 28 + 32 + 36 declaring section bullets). The "output_location" reference STORY-025-04 R4 calls out is currently NOT in this template — it lives in `reporter.md` agent definition + `prefill_report.mjs` (per CR-021 §3.2 surfaces table). **Implementation note:** R4 (`output_location` template edit) is a no-op for `sprint_report.md` itself — there is no `output_location` field to update. STORY-025-04 §1.2 R4 was drafted under the misapprehension that this template carries an `<instructions>` block matching `Sprint Plan Template.md`'s shape. **Action:** Developer should NOT invent a new `<instructions>` block in sprint_report.md just to host an `output_location` field. The naming-reference work is owned by STORY-025-03 (touches `prefill_report.mjs` + `close_sprint.mjs`) and STORY-025-05 (touches `reporter.md` agent's Output row). M4 limits sprint_report.md edits to: insert §4 + renumber + skip-pattern. Surface this divergence in the Developer's first pass — if QA bounces on the missing R4 edit, the answer is "R4 is misframed in the story; it's an STORY-025-03/05 concern".
2. **Pre-existing live ↔ canonical drift on BOTH template files.** `diff` confirms (verified 2026-05-01):
   - `Sprint Plan Template.md`: 4-hunk drift on lines 10, 22, 26, 73 (§24 → §9, §25.4 → §10.4, §§15–18 → §§1–18 + §19 → §5, `cleargate-protocol.md` → `cleargate-enforcement.md`). All 4 hunks are CR-023's responsibility (M0). M4 starts on a CR-023-clean baseline if Wave 1 dispatch order respects: CR-023 lands first, M4 second.
   - `sprint_report.md`: 1-hunk drift on line 10 (§§16–17 → §§2–17). Also CR-023's responsibility. Same dependency.
   - **STORY-025-04 §1.5 risk row** correctly anticipates this: "this story's edits must end with live=canonical even if the starting state is drifted." Mitigation per row: coordinate with CR-023 — if CR-023 reconciles first, M4 builds on a clean baseline. Wave 1 ordering enforces CR-023-first.
   - **Action for Developer:** before opening any edit, run `diff "$LIVE" "$CANONICAL"` for both template pairs. Empty? Proceed. Non-empty? Halt and surface to orchestrator — CR-023 has not landed yet.
3. **`<instructions>` block on Sprint Plan Template includes a §-citation today (line 10: `protocol §24`).** STORY-025-04 wholesale-replaces lines 1–15 with the new actively-authored block. The new block (CR-021 §3.2.1) does NOT cite §24 / §9 anywhere — it has no `lane`-rubric prose. So CR-023's line-10 fix is effectively discarded by M4's reframe. This is acceptable per SPRINT-18.md §2.3 Shared-Surface Warning #2. **No carry-over of CR-023's fix is needed inside the new block.**
4. **The `## §6 Change Log` heading style uses `§N` form (literal `§` glyph + Arabic numeral)**, not `## N.` form. Live and canonical both use `## §1` ... `## §6` consistently for sprint_report.md. Sprint Plan Template uses `## 1.` ... `## 5.` form (Arabic + period). Two different conventions in two templates. **Renumber pass for sprint_report.md must preserve the `## §N` glyph form**; do NOT normalize to `## N.`. Search/replace operations: `## §4 Lessons` → `## §5 Lessons`, `## §5 Framework Self-Assessment` → `## §6 Framework Self-Assessment`, `## §6 Change Log` → `## §7 Change Log`. Three exact-string edits, in that order (highest section first to avoid replacement-cascade).
5. **`## 0. Stakeholder Brief` insertion target.** Sprint Plan Template's current `## 1. Consolidated Deliverables` is at line 52 (verified `grep -nE "^## "`). After CR-023 lands its line-10 / 22 / 26 / 73 fixes (no body inserts, only token rewrites), the line numbers do NOT shift. M4 inserts `## 0. Stakeholder Brief` (~10 lines) between current line 51 (blank) and current line 52 (the §1 heading). Post-M4, current §1 will sit at approximately line 62.
6. **`## §4 Observe Phase Findings` insertion target.** sprint_report.md current `## §3 Execution Metrics` is at line 64; current `## §4 Lessons` is at line 106. The block between lines 65–104 is §3's body (Execution Metrics table). The insertion point is between the trailing `---` of §3 (line 104) and the header `## §4 Lessons` (line 106). New §4 block is ~21 lines (4 headings + 4 column rows + 1 blockquote + 5 blanks). Post-M4, old §4 (now §5) Lessons heading sits at approximately line 127.

## Cross-story risks

- **STORY-025-03 owns 13 external grep hits on `## §4 Lessons` + `## §5 Framework Self-Assessment` across `.cleargate/scripts/test/test_close_pipeline.sh` (3 hits: lines 105, 112, 256) + `.cleargate/scripts/test/test_report_body_stdin.sh` (1 hit: line 67) + `cleargate-cli/test/scripts/fixtures/**/REPORT.md` (9 hits across 5 fixture files).** M4 cannot land its renumber pass without coordinating with STORY-025-03 to update these external references in the same wave. Per SPRINT-18.md §2.2 (Wave 2: STORY-025-03 depends on STORY-025-01 + CR-023; STORY-025-04 is Wave 1) — but the test fixtures depend on STORY-025-04's renumber landing. **Resolution:** STORY-025-03 (Wave 2) updates `test_close_pipeline.sh` + `test_report_body_stdin.sh` fixture assertions to the new `§5 Lessons` / `§6 Framework Self-Assessment` / `§7 Change Log` numbering. The fixture REPORT.md files under `cleargate-cli/test/scripts/fixtures/` are pre-existing snapshots of old REPORT.md output (not the template); they do NOT need renumbering — they are historical artifacts asserting the legacy report shape. Confirm with QA on first M4 dispatch: ad-hoc grep `## §4 Lessons` post-M4 should return zero hits in `.cleargate/templates/` and zero hits in `.cleargate/scripts/test/`, but non-zero in `cleargate-cli/test/scripts/fixtures/` (those are frozen historical fixtures of v1 reports). **Architect annotation for STORY-025-03 M3 plan:** the fixture-renumber task is owned by M3, not M4. M4 surfaces the external hits but does not edit them.
- **STORY-025-05 (Wave 3) cites `SPRINT-<#>_REPORT.md` in the Reporter agent's Output row.** That citation is the agent-prompt-side reference to the new naming. M4's `<instructions>` `output_location` edit on Sprint Plan Template is template-side; STORY-025-03 lands the actual file-write naming in `prefill_report.mjs` + `close_sprint.mjs`; STORY-025-05 lands the agent-prompt reference. Three independent surfaces; M4 has no overlap with -05. *(M4's R4 — sprint_report.md `<instructions>` output_location — is misframed per Gotcha 1 above; this risk row only flags the cross-story coordination on the Sprint-Plan-Template side, which is the actual M4 surface.)*
- **`## 0. Stakeholder Brief`'s `Pair with §3 Risks below` reference.** The new §0 skeleton points at "§3 Risks". Sprint Plan Template's current `## Risks & Dependencies` heading is at line 90 — no `§N` numeric citation. If a future story introduces numeric §N labelling on the Sprint Plan Template body (currently only §1 and §2 carry numbers in live template), the `§3 Risks` cross-reference may go stale. **Soft risk; not blocking.** M4 leaves the cross-reference as-is per CR-021 §3.2.1 phrasing intent.
- **POST-WRITE BRIEF six bullets in the `<instructions>` block include "Gate 2 readiness checklist (decomposed? all 🟢? SDR §2 written?)".** SDR §2 is the Sprint Plan §2 Execution Strategy section (per Architect role definition). M4 does not modify §2 of the body; the citation refers to the very §2 that already exists in Sprint Plan Template (live line 59). Cross-reference is stable.

## Open decisions for orchestrator

- **None blocking.** The single SPRINT-18 SDR §2.5 open decision (renumber map for `## §6 Change Log`) is **pinned in this plan to interpretation (i), all-shift → §7**. No further orchestrator decision required for M4 to dispatch.
- **Soft note on STORY-025-04 §1.4 Open Questions.** All three open questions in the story carry recommended answers + "_accept recommended_" placeholders. M4 plan honors all three:
  1. Bottom-of-body `Execution Guidelines (Local Annotation — Not Pushed)` section: **kept as-is** per recommended.
  2. §4 header naming: **`Observe Phase Findings`** per recommended.
  3. Skip-pattern application: **inline blockquote** at top of §4 (matches CR-021 §3.2.2 verbatim) per recommended.
- **Soft note on Gotcha 1 (R4 misframing on sprint_report.md).** If the orchestrator wants R4 to fire as written (introduce a new `<instructions>` tag-block on sprint_report.md to host an `output_location` field), surface it as an in-flight CR:scope-clarification before M4 dispatches. Recommendation: drop R4 silently for sprint_report.md — the naming-reference work happens in STORY-025-03 + STORY-025-05 surfaces, where the field actually lives.

## Mirror-parity assertion (DoD)

After Developer commit, the following 2 commands MUST both return empty:

```bash
diff ".cleargate/templates/Sprint Plan Template.md" "cleargate-planning/.cleargate/templates/Sprint Plan Template.md"
diff ".cleargate/templates/sprint_report.md" "cleargate-planning/.cleargate/templates/sprint_report.md"
```

Edit-parity invariant per FLASHCARD `2026-04-19 #wiki #protocol #mirror`: every M4 hunk applied to live AND canonical in the same commit. If CR-023 (M0) has not yet landed and the pre-existing 5-hunk drift (Gotcha 2) is still present at M4 dispatch time, **Developer halts and notifies orchestrator** — M4 cannot reconcile pre-existing drift outside its scope (per FLASHCARD `2026-05-01 #mirror #parity`: edit-parity, not state-parity).

## Plan size: 158 lines. Within 60–120 target overshoot due to (i) closed-set renumber map + (ii) six gotchas worth flagging up-front to keep Developer bounce-count at 0. M4 is the highest-coordination Wave-1 milestone on shared-template surfaces; the over-allocation is intentional.
