# Reporter Context Bundle — SPRINT-19

_Generated: 2026-05-02T09:19:35.098Z_

---

## Sprint Plan Slices

_Source: SPRINT-19_Gate_4_Close_QA_Context_Initiative.md_

## 1. Consolidated Deliverables

## 2. Execution Strategy

## 5. Metrics & Metadata

## State.json Summary

Sprint: SPRINT-19 | Status: Active

BUG-024 | Done | standard | qa_bounces=0 arch_bounces=0
CR-022 | Done | standard | qa_bounces=0 arch_bounces=0
CR-024 | Done | standard | qa_bounces=0 arch_bounces=0
CR-025 | Done | standard | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

# Milestone: M1 — `lib/report-filename.mjs` shared helper extraction
## Stories: CR-022 M0
## Wave: W1 (parallel — disjoint with BUG-024 / CR-024 S1 / CR-025 S1)

> role: architect

## Order

Single story. No intra-milestone ordering. Wave 1 runs parallel to BUG-024, CR-024 S1 (M2), CR-025 S1 (M3) over disjoint surfaces.

**Wave 2 dependency lock:** M1 MUST land before any of M6 (CR-022 M3, Steps 6.5/6.6/6.7), M7 (CR-022 M5, cap raise), M11 (CR-022 M4, Step 8), M12 (CR-022 M6, prose updates) dispatch. Wave 2 architect plans cite the frozen API below verbatim. Sprint plan §2.2 codifies this: M1 → M6 / M1 → M7.

## Per-story blueprint

### CR-022 M0 (sprint plan milestone label: M1)

- **Cross-story coupling:**
  - **M6 (CR-022 M3, Wave 2)** consumes the helper from `suggest_improvements.mjs` when adding §6.6 Skill Candidates and §6.7 FLASHCARD Cleanup blocks (CR-022 §3 M3). M6's architect plan will cite the frozen signature below.
  - **M7 (CR-022 M5, Wave 2)** raises `MAX_BUNDLE_BYTES` in `prep_reporter_context.mjs`. M1 already wires the helper into that file (read-side only — `prep_reporter_context.mjs` only references the report path in §7 template pointer prose; current code does not read or write the report file). The two edits land in disjoint regions (helper import + one path resolution call vs. constant raise). Sprint plan §2.2 row 3: M1 → M7.
  - Sprint plan §2.3 first bullet: "M1 freezes the helper signature (`reportFilename(sprintDirPath, sprintId, opts) → string`) before Wave 1 dev dispatch; M6 architect plan cites the frozen signature." This plan freezes that signature in the next subsection.

- **Schema changes:** None. Helper is pure path computation + read-only filesystem probe.

- **Frozen helper API (verbatim — Wave 2 plans cite this block):**

  File: `.cleargate/scripts/lib/report-filename.mjs` (live + canonical mirror).

  Module shape: ESM. Named export only — no default.

  ```js
  /**
   * Compute the report filename for a given sprint directory + sprint ID.
   *
   * New naming (SPRINT-18+): SPRINT-<#>_REPORT.md where <#> is the numeric
   *   portion of the sprint-id (e.g. "18" for "SPRINT-18").
   * No-numeric-portion ids (e.g. "SPRINT-TEST") → plain REPORT.md.
   *
   * Backwards-compat read-fallback: when opts.forRead === true AND the new-name
   *   file is absent BUT legacy REPORT.md exists, return the legacy path.
   *   Covers SPRINT-01..17 archives written before STORY-025-03's naming change.
   *   MUST NOT rename or rewrite those pre-existing files.
   *
   * @param {string} sprintDirPath  absolute path to the sprint directory
   * @param {string} sprintId       e.g. "SPRINT-18" or "SPRINT-TEST"
   * @param {{ forRead?: boolean }} [opts]
   * @returns {string} absolute path to the report file
   */
  export function reportFilename(sprintDirPath, sprintId, opts) { ... }
  ```

  **Behavior contract (locked — Wave 2 must not alter):**
  1. `sprintId.match(/^SPRINT-(\d+)$/)` — if no match, return `path.join(sprintDirPath, 'REPORT.md')` regardless of `opts`.
  2. Match → compute `newName = path.join(sprintDirPath, ‌`SPRINT-${captured}_REPORT.md`)`.
  3. If `opts?.forRead === true` AND `!fs.existsSync(newName)` AND `fs.existsSync(legacyName)` (where `legacyName = path.join(sprintDirPath, 'REPORT.md')`), return `legacyName`. Otherwise return `newName`.
  4. **No throw paths.** Helper never throws — it returns a path, callers decide whether to `fs.readFileSync` and handle ENOENT themselves. This matches the existing inline implementation in `close_sprint.mjs:98-118` (extracted verbatim).
  5. **No env-var reads inside the helper.** Sprint dir resolution stays in callers (each consumer owns `CLEARGATE_SPRINT_DIR` resolution). Helper is pure given its arguments + the filesystem read at line 3.

  **Imports the helper depends on:** `node:path`, `node:fs` only. No third-party deps.

- **Test scenarios (from CR-022 §4.3 Gherkin "naming consistent across all close-pipeline scripts" + CR-022 DoD §4.2 "Backwards-compat: SPRINT-18 archived SPRINT-18_REPORT.md continues to work"):**
  1. `reportFilename(dir, 'SPRINT-18')` returns `<dir>/SPRINT-18_REPORT.md` (write-side default; no fs probe).
  2. `reportFilename(dir, 'SPRINT-18', { forRead: true })` with new-name file present returns `<dir>/SPRINT-18_REPORT.md`.
  3. `reportFilename(dir, 'SPRINT-15', { forRead: true })` with only legacy `REPORT.md` present returns `<dir>/REPORT.md`.
  4. `reportFilename(dir, 'SPRINT-15', { forRead: true })` with neither file present returns `<dir>/SPRINT-15_REPORT.md` (new-name path; caller will hit ENOENT).
  5. `reportFilename(dir, 'SPRINT-15')` (no opts) with only legacy `REPORT.md` present returns `<dir>/SPRINT-15_REPORT.md` — read-fallback is opt-in via `forRead`, never default.
  6. `reportFilename(dir, 'SPRINT-TEST')` returns `<dir>/REPORT.md` (no numeric portion).
  7. `reportFilename(dir, 'SPRINT-TEST', { forRead: true })` returns `<dir>/REPORT.md` (no-numeric path ignores `forRead`).
  8. `reportFilename(dir, 'SPRINT-19', { forRead: true })` with both files present (`SPRINT-19_REPORT.md` + `REPORT.md`) returns `<dir>/SPRINT-19_REPORT.md` — new-name wins when present.
  9. **Caller-integration: `suggest_improvements.mjs` consumes helper.** Run the script against a fixture sprint dir containing `SPRINT-19_REPORT.md`; assert it reads the file and produces `improvement-suggestions.md` (zero behavior change vs current `path.join(sprintDir, 'REPORT.md')` line 136 — but now resolves the new name first).
  10. **Caller-integration: legacy fixture.** Run `suggest_improvements.mjs` against a fixture dir with only legacy `REPORT.md` (e.g. SPRINT-15-shaped); assert it falls back and produces output.

- **Reuse (no duplication):**
  - The current inline `reportFilename()` at `.cleargate/scripts/close_sprint.mjs:98-118` is the source of truth for the extraction. Move it verbatim to `lib/report-filename.mjs`; replace the inline definition with a named import. Net LOC delta in `close_sprint.mjs`: −21 (function body) +1 (import) = −20.
  - Existing lib pattern: `.cleargate/scripts/lib/ledger-digest.mjs` (named exports, no default, ESM, used by `prep_reporter_context.mjs:47` + `count_tokens.mjs:42`). New helper follows the same shape.
  - Test fixture pattern: `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts:392-490` already covers Scenarios 7/8/14 (the runtime end-to-end versions of helper test cases 1, 3, 6). Those existing tests stay green post-refactor (the inline helper becomes an imported helper; runtime behavior unchanged).

- **Test fixture location decision:** `cleargate-cli/test/scripts/report-filename.test.ts` (NEW).
  - Rationale: `cleargate-cli/test/scripts/` is the established home for `.cleargate/scripts/*.mjs` unit tests under vitest (see `test_close_sprint_v21.test.ts`, `close-sprint-reconcile.test.ts`, `test_update_state.test.ts`). The CLI package owns the vitest harness; `.cleargate/scripts/test/` is bash-only (`test_close_pipeline.sh` etc.) and not the right home for a unit test of a pure path helper.
  - Pattern to copy: `cleargate-cli/test/lib/sha256.test.ts` (small pure-helper vitest with named imports). The new file imports from the project-relative path `../../../.cleargate/scripts/lib/report-filename.mjs` — verify this resolution against the existing `test_close_sprint_v21.test.ts:40` pattern (it uses `spawnSync` against a path resolved from `process.cwd()`; the new test imports the module directly because the helper is pure).
  - Test scenario count: 8 unit tests (cases 1-8 above) in `report-filename.test.ts`. Caller-integration cases 9-10 stay in `test_close_sprint_v21.test.ts` (already covered by Scenarios 7+8 — no new scenarios needed; existing scenarios continue to exercise the helper through `suggest_improvements.mjs` invocation in the close-pipeline run).

- **Caller refactor scope (zero behavior change):**
  1. **`.cleargate/scripts/close_sprint.mjs`:** Delete inline `reportFilename` function body lines 98-118; add `import { reportFilename } from './lib/report-filename.mjs';` near the top imports (file currently does not import anything from `./lib/`; add the import next to existing `node:` imports). All existing call sites at `close_sprint.mjs:252, 417` keep their current invocation shape — they already pass `(sprintDir, sprintId, { forRead: true })`.
  2. **`.cleargate/scripts/suggest_improvements.mjs`:** Replace `const reportFile = path.join(sprintDir, 'REPORT.md');` at line 136 with `const reportFile = reportFilename(sprintDir, sprintId, { forRead: true });`. Add `import { reportFilename } from './lib/report-filename.mjs';` to imports (joins existing `node:fs`, `node:path`, `node:url`, `node:crypto` imports at lines 24-27). Update the error message at line 140 to reference the resolved path (already does — `${reportFile}` interpolates the resolved name; legacy fallback path will appear correctly when applicable).
  3. **`.cleargate/scripts/prep_reporter_context.mjs`:** Audit-only. The script does NOT currently read or write the report file (its only REPORT mention is the §7 "REPORT Template" pointer at lines 441-443, which references the template path under `.cleargate/templates/`, not the sprint's report). **Decision: do NOT add the import to `prep_reporter_context.mjs` in M1.** Wave 2 M7 (cap raise) does not need it either. If a future story needs to surface the resolved report path inside the bundle, it can add the import then. Recording this decision here so M7's architect plan does not duplicate the audit.
  4. **`.cleargate/scripts/prefill_report.mjs`:** Audit-only. Grep returned no `REPORT` matches on the script body. CR-022 §3 M0 lists this file as a candidate consumer ("Re-use from suggest_improvements.mjs + prep_reporter_context.mjs + prefill_report.mjs") but the file does not currently construct a report filename — it writes agent prefill stubs to other paths. **Decision: do NOT touch prefill_report.mjs in M1.** If a future audit surfaces a hidden hardcoded path, file as a follow-on.

- **Mirror parity (per-edit invariant — FLASHCARD `2026-04-19 #wiki #protocol #mirror`, `2026-05-01 #mirror #parity`):**
  - Every file edit in M1 lands in BOTH `.cleargate/scripts/...` (live) AND `cleargate-planning/.cleargate/scripts/...` (canonical) in the same commit. Files affected:
    | Live path | Canonical mirror path |
    |---|---|
    | `.cleargate/scripts/lib/report-filename.mjs` (NEW) | `cleargate-planning/.cleargate/scripts/lib/report-filename.mjs` (NEW) |
    | `.cleargate/scripts/close_sprint.mjs` | `cleargate-planning/.cleargate/scripts/close_sprint.mjs` |
    | `.cleargate/scripts/suggest_improvements.mjs` | `cleargate-planning/.cleargate/scripts/suggest_improvements.mjs` |
  - Canonical mirror currently lacks a `lib/` subdirectory. Developer creates `cleargate-planning/.cleargate/scripts/lib/` as part of the same commit.
  - Verification: `diff -q .cleargate/scripts/lib/report-filename.mjs cleargate-planning/.cleargate/scripts/lib/report-filename.mjs` returns empty post-commit; same diff for the two refactored callers. Per-edit parity, not whole-file reconciliation — do NOT pick up any pre-existing canonical drift in unrelated files.
  - **Test fixture file lives in `cleargate-cli/test/` only — no canonical mirror.** The `cleargate-cli/` tree is the npm package; it is not mirrored under `cleargate-planning/`.

- **Gotchas surfaced from code inspection (file:line citations):**
  1. **`close_sprint.mjs:98-118` inline implementation is the canonical source.** The function body is ~21 lines including the JSDoc; extract verbatim into `lib/report-filename.mjs` (preserve the JSDoc word-for-word — Wave 2 plans rely on the documented contract).
  2. **`suggest_improvements.mjs:136` is the only place in that file that builds a REPORT path.** Only one call site to refactor. The script currently calls `path.join(sprintDir, 'REPORT.md')` with NO sprint-id awareness — it would silently miss SPRINT-18+ reports today, which is exactly the SPRINT-18 lesson CR-022 M0 fixes (CR §0.5: "suggest_improvements.mjs missed the SPRINT-<#>_REPORT.md naming sweep"). Refactor MUST pass `sprintId` (already in scope at line 126) + `{ forRead: true }`.
  3. **`prep_reporter_context.mjs` REPORT references at lines 441-443 are template-path mentions, not sprint-report paths.** The §7 bundle section emits a literal sentence about the report template under `.cleargate/templates/`. Do NOT confuse the two. Triple-checked via `grep -n REPORT prep_reporter_context.mjs`.
  4. **`close_sprint.mjs` does not currently import from `./lib/`** — adding the import introduces the first lib-import in that file. Insert the import alongside other `node:` imports near line ~10. Verify by reading the import block at edit time; do not let auto-formatters reorder.
  5. **No `.cleargate/scripts/test/` bash test exists for `reportFilename` today.** New unit tests land in `cleargate-cli/test/scripts/report-filename.test.ts` (vitest), per the test-fixture-location decision above. The existing bash fixture `test_close_pipeline.sh` exercises the helper indirectly through `close_sprint.mjs` end-to-end runs and continues to pass (zero behavior change).
  6. **Test isolation env var (FLASHCARD `2026-04-21 #test-harness #scripts #env`):** `close_sprint.mjs:172`, `suggest_improvements.mjs:127`, `prep_reporter_context.mjs:54` all resolve `sprintDir` via `process.env.CLEARGATE_SPRINT_DIR ?? <REPO_ROOT>/.cleargate/sprint-runs/<id>`. The helper is pure given `sprintDirPath` — it never reads that env var. Tests pass `sprintDirPath` directly, so the env override only matters for the callers, not the helper. Document this in the helper's JSDoc to forestall confusion.

- **Pre-commit gates (FLASHCARD `#typecheck` parity):**
  - `.mjs` files are not under TypeScript compilation. `npm run typecheck` in `cleargate-cli/` covers the new `report-filename.test.ts` file (it must typecheck against vitest's types). Run `npm test --workspace cleargate-cli -- --run` to catch the new test file.
  - The two refactored `.mjs` files are exercised by existing test_close_sprint_v21.test.ts scenarios 7/8/14 + the new caller-integration cases 9-10 above.

## Cross-story risks

1. **Wave 2 plans MUST cite the frozen API block above verbatim.** If a Wave 2 milestone plan invents a different signature or behavior assumption (e.g. "helper throws on missing file"), reject at architect review. The contract above is locked: helper never throws, never reads env vars, return paths are deterministic given args + filesystem state.

2. **M6 (CR-022 M3, Wave 2) appends new sections to `improvement-suggestions.md`.** That output file is written by `suggest_improvements.mjs` — same script we refactor in M1. Risk: if M1's caller refactor accidentally changes the output file's path or write sequence (it shouldn't — only the read path changes), M6 breaks. Mitigation: M1's caller refactor is read-side only at `suggest_improvements.mjs:136`; the output write at `suggestionsFile` (line 137) is untouched. Verify via diff at edit time.

3. **M7 (CR-022 M5) lands in `prep_reporter_context.mjs` Wave 2.** M1 explicitly does NOT touch that file (audit-only — see Caller refactor scope #3). M7's architect plan should re-confirm via grep that no `REPORT.md` literal exists in the file post-M1, then proceed with the constant raise in disjoint scope.

4. **Mirror divergence is the silent-failure mode.** FLASHCARD `2026-05-01 #mirror #parity`: pre-existing canonical drift in CLAUDE.md is unrelated and out of scope. Developer agent MUST NOT use the M1 commit to "clean up" any mirror drift discovered in other files — per-edit invariant only.

5. **STORY-025-03's existing test scenarios (7/8/13/14) remain authoritative for runtime behavior.** Post-refactor, those scenarios MUST pass without modification. If a Developer needs to edit those scenarios to make them pass, the refactor changed behavior — kick back to QA.

## Open decisions for orchestrator

None. All four CR-022 §0.5 open questions reference Wave 2 milestones (sprint_trends stub depth, bundle cap value, archive flag, merge verify-only) — none affect M1's helper extraction. The frozen API block above resolves the only M1-relevant ambiguity (signature, no-throw contract, opt-in legacy fallback).

## DoD (per CR-022 §4.2 + sprint plan §2.2 freeze requirement)

- [ ] `.cleargate/scripts/lib/report-filename.mjs` (NEW, live + canonical mirror) byte-identical post-edit; named export `reportFilename` matches frozen signature.
- [ ] `.cleargate/scripts/close_sprint.mjs` (live + canonical) — inline helper deleted, import added, byte-identical post-edit between live and canonical.
- [ ] `.cleargate/scripts/suggest_improvements.mjs` (live + canonical) — line 136 consumes helper with `{ forRead: true }`, byte-identical post-edit.
- [ ] `cleargate-cli/test/scripts/report-filename.test.ts` (NEW) — 8 unit test scenarios (cases 1-8) all pass under vitest.
- [ ] Existing `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` Scenarios 7/8/13/14 stay green (zero behavior change validated).
- [ ] `cleargate-cli/` typecheck clean: `npm run typecheck --workspace cleargate-cli`.
- [ ] `cleargate-cli/` tests green: `npm test --workspace cleargate-cli -- --run`.
- [ ] Mirror parity verified: `diff -q` returns empty for the three live↔canonical pairs.
- [ ] Single commit per CR-022 §4.2 DoD: `feat(CR-022): CR-022-M0 extract lib/report-filename.mjs shared helper`.

---

role: architect

### M2.md

# Milestone: M2 — `prep_qa_context.mjs` script + frozen QA context-bundle schema
## Stories: CR-024 Story 1
## Wave: W1 (parallel — disjoint surface from M0 BUG-024, M1 lib/report-filename.mjs, M3 templates/initiative.md)

> Front-half of CR-024. Ships the script + the **frozen JSON schema** that M8 (Wave 2, CR-024 S2) will cite when it edits `.claude/agents/developer.md` (STATUS=done extension) + `.claude/agents/qa.md` (lane playbook + pack-first directive). **M2 must NOT touch either agent file** — sprint plan §2.2 line 103 reserves them exclusively for M8. Wave 1 disjointness depends on this.

## Order

Single-story milestone. No internal ordering. Parallelizable with M0 / M1 / M3 (no shared files — confirmed against sprint plan §2.2 file-touch table lines 91-108: `prep_qa_context.mjs` is a new file owned only by M2).

## Per-story blueprint

### CR-024 Story 1 — `prep_qa_context.mjs` script + frozen JSON schema

- **Cross-story coupling:**
  - **Schema is a hard freeze for M8.** M8 (CR-024 S2) edits `developer.md` STATUS=done schema and `qa.md` lane playbook around the bundle this script emits. The schema below is the contract. M8's Architect plan will cite it verbatim. Any divergence between M2's emitted bundle and M8's prose breaks the QA pack-first contract.
  - **No agent-file touches.** `developer.md` + `qa.md` belong to M8 (sprint plan §2.2 line 103). M2 emits the schema and the script that produces conformant output; M8 wires the agents to consume it.
  - **No `close_sprint.mjs` invocation.** The Reporter pattern (STORY-025-01) wired `prep_reporter_context.mjs` into `close_sprint.mjs` Step 3.5. M2 does **not** wire `prep_qa_context.mjs` anywhere — the orchestrator invokes it directly per QA spawn. Confirmed against CR-024 §1 line 75: "Orchestrator's QA spawn prompt collapses to ~5 lines: worktree path + commit SHA + 'read `.qa-context-<story>.md` and run the playbook.'"

- **Schema changes (verbatim — this is the freeze for M8):**

  Output file: `.cleargate/sprint-runs/<sprint-id>/.qa-context-<story-id>.md` (markdown bundle with embedded JSON code blocks for machine-readable fields, mirroring `prep_reporter_context.mjs` line 47 `parseJsonl` pattern + bundle prose pattern).

  **Top-level markdown sections (8, ordered, all required — missing inputs degrade to one-liners per CR-024 §4 line 125 R4 resilience):**

  1. **`## Worktree + Commit`** — fields: `worktree_path`, `branch`, `head_sha`, `dev_status_block_present` (bool — true iff dev's STATUS=done block was found in commit message body or the dispatch payload).

  2. **`## Spec Sources`** — fields: `story_path`, `plan_path`, `spec_pointers` (list of `{section, path, line_range}` — currently a single entry: the milestone plan's per-story blueprint header).

  3. **`## Baseline`** — fields: `main_head_sha`, `baseline_failures` (list of `{file, count}`; populated from `.cleargate/sprint-runs/<sprint-id>/.baseline-failures.json` if present, else `[]` with a `baseline_unavailable: true` flag and a one-liner `_Baseline cache stale or absent — recompute via `cleargate gate test` on main._`). **R4 fallback:** baseline cache absence is a soft-degrade, not a hard error.

  4. **`## Adjacent Files`** — fields: `touched_files` (from `git diff --name-only main..HEAD`), `adjacent_test_files` (test files in same directory as touched source files; both `*.test.ts` neighbors and bash `test_*.sh` neighbors), `mirror_pairs` (live↔canonical pair detection — for any touched path under `.cleargate/`, `.claude/`, or `cleargate-planning/.cleargate/`, list the paired path).

  5. **`## Cross-Story Map`** — fields: `concurrent_stories` (list of `{story_id, branch, head_sha, shared_files}`); enumerated by reading `state.json` for stories whose `state` is `Active|InQA|InReview` AND whose worktree branches' `git diff --name-only main..<branch>` overlaps `touched_files` from §4.

  6. **`## Flashcard Slice`** — fields: `tags_inferred` (list — derived from touched-file path roots: `cleargate-cli/src/commands/` → `#cli #commander`; `mcp/src/auth/` → `#auth`; etc. — table below in Source-of-data), `entries` (list of full FLASHCARD lines matching any inferred tag).

  7. **`## Lane`** — fields: `lane` (one of `fast|standard|runtime`), `lane_source` (`state.json` | `default` if absent). Sourced from `state.json.stories[<story-id>].lane`. Default `standard` per developer.md line 78 contract. **Note:** `runtime` is a new lane introduced by CR-024; absent from current state.json values (only `fast`/`standard` exist today). M8 prose will define when QA classifies a story as `runtime`; M2 simply passes through whatever the field contains, and emits `lane_source: not-yet-runtime-aware` if the field is `standard` AND the story touches CLI command files (heuristic for QA hint).

  8. **`## Dev Handoff`** — verbatim copy of the dev's STATUS=done structured block (from commit message body or dispatch payload). If absent or in legacy free-form shape, emit `_SCHEMA_INCOMPLETE — context limited; old-format STATUS=done found, no `r_coverage`/`plan_deviations`/`adjacent_files`._` per CR-024 §4 line 130 backwards-compat assertion.

  **Frozen JSON shape (embedded as a single fenced ` ```json` block at the top of the markdown bundle for machine-readable consumers; the prose sections below repeat the same fields for human/LLM consumption — pattern matches `prep_reporter_context.mjs` line 226 ledger-digest embedding):**

  ```json
  {
    "schema_version": 1,
    "story_id": "STORY-NNN-NN",
    "sprint_id": "SPRINT-NN",
    "generated_at": "ISO-8601",
    "worktree": {
      "path": "string",
      "branch": "string",
      "head_sha": "string",
      "dev_status_block_present": "boolean"
    },
    "spec_sources": {
      "story_path": "string|null",
      "plan_path": "string|null",
      "spec_pointers": [{"section": "string", "path": "string", "line_range": "string"}]
    },
    "baseline": {
      "main_head_sha": "string",
      "baseline_unavailable": "boolean",
      "failures": [{"file": "string", "count": "integer"}]
    },
    "adjacent": {
      "touched_files": ["string"],
      "adjacent_test_files": ["string"],
      "mirror_pairs": [{"touched": "string", "mirror": "string"}]
    },
    "cross_story_map": [
      {"story_id": "string", "branch": "string", "head_sha": "string", "shared_files": ["string"]}
    ],
    "flashcard_slice": {
      "tags_inferred": ["string"],
      "entries": ["string"]
    },
    "lane": {
      "value": "fast|standard|runtime",
      "source": "state.json|default|not-yet-runtime-aware"
    },
    "dev_handoff": {
      "format": "structured|legacy|absent",
      "status": "done|blocked|null",
      "commit": "string|null",
      "typecheck": "pass|fail|null",
      "tests": "string|null",
      "files_changed": ["string"],
      "notes": "string|null",
      "r_coverage": [{"r_id": "string", "covered": "boolean", "deferred": "boolean", "clarified": "boolean"}],
      "plan_deviations": [{"what": "string", "why": "string", "orchestrator_confirmed": "boolean"}],
      "adjacent_files": ["string"],
      "flashcards_flagged": ["string"]
    }
  }
  ```

  **Schema-version contract:** `"schema_version": 1` is permanent. M8 reads exactly v1. Any future field additions emit `"schema_version": 2` and require a coordinated agent-file update — out of scope for SPRINT-19.

  **Bundle target ≤20KB.** CR-024 §0 line 49 metric. Enforced via `Buffer.byteLength()` check at end of `main()`; if exceeded, write the file anyway (R4 — partial bundles worse than oversized) and stderr-warn with the kb count, mirroring `prep_reporter_context.mjs` lines 452-457.

- **Test scenarios (from CR-024 §4 Verification Protocol — every assertion must have a test):**

  Test file: `.cleargate/scripts/test/test_prep_qa_context.sh` (NEW). Pattern mirrors `test_prep_reporter_context.sh` lines 1-251 (3-scenario bash harness, `mktemp -d` fixtures, `CLEARGATE_SPRINT_DIR` + new `CLEARGATE_PENDING_SYNC_DIR` env overrides, macOS bash 3.2 portable — confirmed FLASHCARD line 63 portability constraint).

  1. **Scenario 1 — happy path against real STORY-025-01 fixture replica.** Set up `tmpdir` with `state.json` (Active story `STORY-AAA-01`, lane=standard), `token-ledger.jsonl` (3 rows), a synthetic `.git` fake-or-real with one commit on a branch named `story/STORY-AAA-01`, a synthetic story file, and a milestone plan. Invoke `node prep_qa_context.mjs STORY-AAA-01 <worktree-path> --output <tmp>/.qa-context-STORY-AAA-01.md`. Assert: exit 0, output file present, size ≤20KB (20480 bytes), all 8 section headers present, JSON code block parses to `schema_version: 1`.

  2. **Scenario 2 — missing story file degrades to one-liner.** Same fixture as Scenario 1, but no story file in `pending-sync` or `archive`. Assert: exit 0, output present, `## Spec Sources` section contains `_Story file not found for STORY-AAA-01._`, no other section impacted.

  3. **Scenario 3 — missing baseline cache → `baseline_unavailable: true`.** Same as Scenario 1, but no `.baseline-failures.json` present. Assert: exit 0, JSON `baseline.baseline_unavailable === true`, prose `## Baseline` includes the recompute one-liner.

  4. **Scenario 4 — bundle size cap warning on oversized fixture.** Synthesize a fixture where `git diff --name-only main..HEAD` returns 500+ paths (write a script-side stub or create that many real files in tmpdir). Assert: exit 0 (still writes), stderr contains "exceeds 20KB target", bundle is still written.

  5. **Scenario 5 — legacy STATUS=done falls through to `format: legacy`.** Pass a dev handoff payload that lacks `r_coverage`/`plan_deviations`/`adjacent_files`. Assert: JSON `dev_handoff.format === "legacy"`, prose `## Dev Handoff` contains `_SCHEMA_INCOMPLETE — context limited_`, exit 0.

  6. **Scenario 6 — usage error on missing args.** `node prep_qa_context.mjs` (no args). Assert: exit 2, stderr contains `Usage:`. Mirrors `prep_reporter_context.mjs` line 384 `process.exit(2)`.

  Six scenarios; CR-024 §4 lines 122-126 lists 4 acceptance assertions, the test file expands them to 6 to cover both R4 fallbacks (story-missing + baseline-missing as separate scenarios) plus the cap warning and usage-error exit codes.

- **Reuse (no duplication — Adjacent Implementation Check, sprint plan §2.2 + git log of merged Wave 1 stories):**

  - **`atomicWrite(filePath, content)`** from `.cleargate/scripts/prep_reporter_context.mjs` lines 72-76. Identical pattern. **Action:** copy the function inline (it is 5 LOC); do NOT extract to `lib/` for one consumer — extraction would duplicate the duplication-anti-pattern from CR-021. If M1's helper-extraction pattern is ratified by sprint close, CR-027 can consolidate.
  - **`parseFrontmatter(content)`** from `.cleargate/scripts/prep_reporter_context.mjs` lines 80-92. Identical pattern. Same inline-copy guidance.
  - **`resolveSprintDir`/`resolvePendingSyncDir`/`resolveArchiveDir`** from `.cleargate/scripts/prep_reporter_context.mjs` lines 54-68. Identical env-override pattern. Inline-copy. **Confirmed against FLASHCARD line 61** (`#test-harness #scripts #env`): the env overrides are the contract for test isolation; do NOT skip them.
  - **`stableHash`** from `.cleargate/scripts/suggest_improvements.mjs` line 41. Not needed — QA bundle has no idempotency requirement (QA reads it once per spawn; orchestrator overwrites between spawns).
  - **`parseJsonl`** from `.cleargate/scripts/lib/ledger-digest.mjs`. Not needed — M2 does not consume the ledger; it consumes `state.json` and `git` output.
  - **`constants.mjs`** (`VALID_STATES`, `TERMINAL_STATES`, `SCHEMA_VERSION`) from `.cleargate/scripts/constants.mjs`. Use `VALID_STATES` to filter the cross-story map to in-flight stories (Active|InQA|InReview).

- **CLI shape (frozen — M8 spawn prompts will cite it):**

  ```
  node .cleargate/scripts/prep_qa_context.mjs <story-id> <worktree-path> [--output <path>] [--dev-handoff-json <path>]
  ```

  - **Positional 1:** `<story-id>` — e.g. `STORY-025-04`. Required.
  - **Positional 2:** `<worktree-path>` — absolute path to the dev's worktree (e.g. `.worktrees/STORY-025-04/`). Required. The script `git -C "${worktree_path}" rev-parse HEAD` for `head_sha`, `git -C "${worktree_path}" symbolic-ref HEAD` for branch.
  - **`--output <path>`** — optional. Default: `<sprint-dir>/.qa-context-<story-id>.md`. Sprint dir resolved via `CLEARGATE_SPRINT_DIR` env (test isolation) or auto-detect by reading `.cleargate/sprint-runs/.active`.
  - **`--dev-handoff-json <path>`** — optional. If present, the file's JSON content is merged into `dev_handoff` field. If absent, the script attempts to parse the dev's commit-message body for the STATUS=done block (legacy free-form fallback). If neither succeeds, `dev_handoff.format = "absent"`.

  **Exit codes:** `0` success (including all R4 soft-degrades); `1` reserved for hard errors (worktree path doesn't exist, git command fails); `2` usage error. Mirrors `prep_reporter_context.mjs` exit-code table at lines 32-36.

- **Source-of-data table (every schema field maps to a verifiable source):**

  | Schema field | Source | Notes |
  |---|---|---|
  | `story_id` | argv[2] | verbatim |
  | `sprint_id` | `.cleargate/sprint-runs/.active` (or `CLEARGATE_SPRINT_DIR` basename) | matches `prep_reporter_context.mjs` resolution pattern |
  | `worktree.path` | argv[3] | verbatim |
  | `worktree.branch` | `git -C <worktree> symbolic-ref HEAD` | strip `refs/heads/` prefix |
  | `worktree.head_sha` | `git -C <worktree> rev-parse HEAD` | full 40-char SHA |
  | `worktree.dev_status_block_present` | regex `/STATUS:\s*(done|blocked)/` against commit-msg body OR `--dev-handoff-json` content | bool |
  | `spec_sources.story_path` | glob `.cleargate/delivery/{pending-sync,archive}/<story-id>_*.md` | first hit; null if none |
  | `spec_sources.plan_path` | derive milestone from sprint plan §1 table; resolve to `.cleargate/sprint-runs/<sprint-id>/plans/M<N>.md` | null if not in any milestone (fast-lane stories) |
  | `spec_sources.spec_pointers` | parse plan_path for `### <story-id>` heading; emit `{section: "Per-story blueprint", path: plan_path, line_range: "<start>-<end>"}` | empty list if plan absent |
  | `baseline.main_head_sha` | `git -C <worktree> rev-parse main` | string |
  | `baseline.baseline_unavailable` | `!fs.existsSync('<sprint-dir>/.baseline-failures.json')` | bool |
  | `baseline.failures` | parse `.baseline-failures.json` if present | shape: `[{file, count}]`; this file does NOT exist today and is forward-compat — currently always empty + `baseline_unavailable: true`. CR-026/CR-027 territory. M2 ships the read path; cache population is out of scope. |
  | `adjacent.touched_files` | `git -C <worktree> diff --name-only main..HEAD` | list |
  | `adjacent.adjacent_test_files` | for each touched_file, glob `<dirname(f)>/*.test.{ts,sh}` and `<dirname(f)>/test_*.sh` | dedupe |
  | `adjacent.mirror_pairs` | path-rewrite map: `.cleargate/scripts/X` ↔ `cleargate-planning/.cleargate/scripts/X`; `.claude/agents/X` ↔ `cleargate-planning/.claude/agents/X`; `.cleargate/templates/X` ↔ `cleargate-planning/.cleargate/templates/X`. Per-edit, not state-parity (FLASHCARD line 8 `#mirror #parity`) — so for each touched file, emit the canonical pair only if the touched file's mirror exists at the paired path. |
  | `cross_story_map[]` | read `state.json.stories`, filter by `state ∈ VALID_STATES.in_flight` (Active|InQA|InReview); for each, `git -C <repo> diff --name-only main..<branch>` and intersect with `adjacent.touched_files`; emit only stories with non-empty intersection | branch derived as `story/<story-id>` |
  | `flashcard_slice.tags_inferred` | path-prefix table: `cleargate-cli/src/commands/` → `#cli #commander`; `cleargate-cli/src/auth/` → `#auth`; `mcp/src/` → `#mcp #fastify`; `mcp/src/auth/` → `#auth`; `mcp/src/db/` → `#schema #migrations`; `.cleargate/scripts/` → `#scripts #test-harness`; `.claude/agents/` → `#agents`; `.cleargate/knowledge/` → `#protocol #wiki`; default → `#general` | unioned across touched_files |
  | `flashcard_slice.entries` | `grep -E '<tag1>\|<tag2>' .cleargate/FLASHCARD.md` | full line; max 20 entries (cap to honor 20KB budget) |
  | `lane.value` | `state.json.stories[<story-id>].lane`; default `"standard"` | passthrough; M2 does not interpret |
  | `lane.source` | `"state.json"` if field present; `"default"` if missing; `"not-yet-runtime-aware"` if `standard` AND any touched_file matches `cleargate-cli/src/commands/` (heuristic to flag for QA M8 playbook) | informational only |
  | `dev_handoff.*` | parse `--dev-handoff-json` argument if present; else regex-extract from commit-msg body of `worktree.head_sha` | structured if all 3 new fields present, legacy if STATUS+COMMIT but no `r_coverage`, absent otherwise |

- **Mirror-parity callout:**

  **The script ships in TWO locations** (FLASHCARD line 98 `#wiki #protocol #mirror` + line 8 `#mirror #parity`):

  1. **Live:** `.cleargate/scripts/prep_qa_context.mjs`
  2. **Canonical:** `cleargate-planning/.cleargate/scripts/prep_qa_context.mjs`

  Per-edit parity: after writing the live file, immediately write the byte-identical canonical mirror. Confirm via `diff -q .cleargate/scripts/prep_qa_context.mjs cleargate-planning/.cleargate/scripts/prep_qa_context.mjs` returning empty. **Test file (`test_prep_qa_context.sh`) likewise mirrors** to `cleargate-planning/.cleargate/scripts/test/test_prep_qa_context.sh`. Confirmed against the live↔canonical diff at `.cleargate/scripts/test/` vs `cleargate-planning/.cleargate/scripts/test/` (per the dir listings above, canonical mirror has 4 of the 16 live test files — pre-existing divergence; per FLASHCARD line 8 do NOT reconcile pre-existing divergence as a side effect, only ensure THIS new file lands in both).

- **Gotchas surfaced from code inspection:**

  1. **`prep_reporter_context.mjs` lines 54-68 establish the env-override convention** (`CLEARGATE_SPRINT_DIR`, `CLEARGATE_PENDING_SYNC_DIR`). The QA script MUST honor both — the test harness depends on them (see `test_prep_reporter_context.sh` lines 138-140). Skipping these env overrides will make the test untestable in `mktemp -d` isolation.
  2. **`prep_reporter_context.mjs` line 50 anchors `REPO_ROOT = path.resolve(__dirname, '..', '..')`** — i.e., scripts assume they live two levels deep under repo root. The canonical mirror at `cleargate-planning/.cleargate/scripts/` has the SAME relative depth (two dirs up = `cleargate-planning/`, which is NOT the repo root). **This is fine for the live script** but the canonical mirror's `REPO_ROOT` resolves to `cleargate-planning/`, which is empty of `.cleargate/sprint-runs/`. **Action:** the canonical mirror is a static-ship artifact (installed by `cleargate init`), not invoked from `cleargate-planning/`. The test harness invokes only the LIVE script. Do not add canonical-aware path logic.
  3. **`developer.md` line 40** documents STATUS=done as `STATUS: done | blocked` — current schema. M2's `dev_handoff` field must accept this format AND the M8-extended format. The `format: structured|legacy|absent` enum encodes that backwards-compat per CR-024 §4 line 130.
  4. **`state.json` schema_version 2** (sprint-runs/SPRINT-18/state.json line 2). The `lane` field exists at `state.json.stories[<id>].lane` (line 14). `runtime` is NOT yet a valid value — only `fast`/`standard` exist. M2 emits `runtime` only if found verbatim in state.json; otherwise it passes through `standard`/`fast` and lets M8 wire QA dispatch. **No state.json schema change in M2.**
  5. **`.cleargate/sprint-runs/<sprint-id>/.active` sentinel** — `prep_reporter_context.mjs` does NOT use this; it takes sprint-id as positional argv[1]. M2 mirrors that pattern: the sprint-id is derived from `worktree-path/.git/HEAD` branch name OR the `CLEARGATE_SPRINT_DIR` env var basename, NOT from `.active`. This matches sprint plan §2 cross-project orchestration concerns.
  6. **`git -C <worktree> diff --name-only main..HEAD`** — if `main` is not present in the worktree (worktrees inherit refs from primary clone, but a fresh worktree might not have fetched), this fails. Wrap in try/catch and fall back to empty `touched_files` with a stderr warning. R4 resilience.
  7. **macOS bash 3.2 portability** (FLASHCARD line 63). The test harness must NOT use `mapfile`/`readarray`. Use `arr=(); while IFS= read -r x; do arr+=("$x"); done < <(cmd)`.
  8. **20KB target is tight.** `prep_reporter_context.mjs` produces 138KB on a real sprint (SPRINT-18 close, 1.7× the 80KB target). M2's QA pack budget is **4× tighter**. Mitigations: cap `flashcard_slice.entries` at 20 lines; cap `cross_story_map` at 5 stories; truncate `git log` output (M2 does not run git log, only diff --name-only); each section that overflows logs a stderr warning; partial-bundle-better-than-no-bundle still applies.

## Cross-story risks

- **M8 (Wave 2) cannot start until M2's schema is frozen and the script is merged.** Sprint plan §2.2 lists M8 as Wave 2 (gated on Wave 1 completion). The frozen JSON shape under `## Schema changes (verbatim)` above is the M8 contract. **If M2 ships a different shape than what is documented here, M8's Architect plan will reference a stale schema and the Developer for M8 will produce agent prose that cannot consume the bundle.** Mitigation: M2's Developer commit message MUST include the JSON schema verbatim as a fenced code block (so M8's Architect can grep the merged commit for the contract).
- **M3 (CR-025 S1 templates rewrite) is in the same wave but does NOT touch `.cleargate/scripts/`.** Confirmed disjoint via sprint plan §2.2 lines 91-108. No risk.
- **M1 (CR-022 M0 helper extraction) does NOT touch `prep_qa_context.mjs`.** Sprint plan §2.2 line 96 explicitly lists M1's surface as `lib/report-filename.mjs` + consumer updates to `close_sprint.mjs`. M2's script is a fresh file under `.cleargate/scripts/` with no overlap.
- **BUG-024 (M0) is investigation-only** (sprint plan §2 line 79). Zero code surface. No risk.
- **`cleargate-planning/.cleargate/scripts/test/` is missing 12 test files** that exist in the live `.cleargate/scripts/test/` (per dir listings). Per FLASHCARD line 8, M2 does NOT reconcile this pre-existing divergence — only ships its own new test file in both locations. Pre-existing canonical drift is out of scope for SPRINT-19.
- **`runtime` lane is introduced semantically by CR-024 but not registered as a state.json valid value.** M2 passes it through; M8 introduces the agent-side dispatch. **Risk:** if a future story sets `lane: runtime` in state.json before SPRINT-20 lands a state.schema.json update, `validate_state.mjs` may reject it. **Mitigation:** flag in §6 of REPORT.md as a follow-up; out of scope for M2.

## Open decisions for orchestrator

1. **Should `--baseline-failures-json <path>` be a flag in M2, or read from a fixed path (`<sprint-dir>/.baseline-failures.json`)?** This plan picks fixed-path for simplicity (matches `state.json` convention). If the orchestrator wants to pass a custom baseline cache per QA spawn, M2 must add the flag. **Recommendation:** fixed-path. Confirm before Developer dispatch.
2. **Is the canonical mirror (`cleargate-planning/.cleargate/scripts/prep_qa_context.mjs`) shipped as part of M2's commit, or as a separate `cleargate init` scaffold step?** Sprint plan §1 line 81 ("test-pattern" mirror obligations from past sprints) implies same-commit mirroring. **Recommendation:** same commit — both files in one `feat(CR-024): STORY-CR-024-S1 prep_qa_context.mjs + frozen schema` commit. No separate canonical-only commit.
3. **Bundle file naming: `.qa-context-<story-id>.md` (this plan) vs `<story-id>.qa-context.md`?** This plan uses dotfile-prefixed (matches `.reporter-context.md` convention from STORY-025-01). Confirm — M8's prose will cite the exact filename.

## DoD (drawn from CR-024 §4 + sprint plan §0 metric)

- [ ] `node .cleargate/scripts/prep_qa_context.mjs <story-id> <worktree-path>` runs against a real SPRINT-18 archive story fixture (e.g. `STORY-025-01`) and produces ≤20KB JSON-embedded markdown bundle.
- [ ] Schema documented in the script's top-of-file comment AS A VERBATIM JSON BLOCK (M8's Architect must be able to copy-paste from `head -100 prep_qa_context.mjs`).
- [ ] Both mirrors land (live `.cleargate/scripts/prep_qa_context.mjs` + canonical `cleargate-planning/.cleargate/scripts/prep_qa_context.mjs`) in the same commit; `diff -q` between them is empty.
- [ ] Test file `.cleargate/scripts/test/test_prep_qa_context.sh` ships, also mirrored to `cleargate-planning/.cleargate/scripts/test/test_prep_qa_context.sh`, all 6 scenarios pass.
- [ ] Pre-gate scanner clean; `cleargate gate typecheck` not applicable (no TS surface) — confirm via Developer pre-commit pass.
- [ ] `cleargate gate test` passes (the bash harness counts as a gated test surface; the test runner that picks it up is the script-test sweep, not vitest).
- [ ] Schema-version contract: top-of-file comment block declares `// schema_version: 1 — frozen for SPRINT-19 M8`.
- [ ] Hand-off note in commit body: `M8 (CR-024 S2) consumes this schema. M2 freezes; M8 wires.`

## Hand-off for M8 (Wave 2)

M2 freezes the JSON schema. M8 (CR-024 S2) Architect plan MUST:

1. Read `.cleargate/scripts/prep_qa_context.mjs` lines 1-100 (top-of-file comment block) for the verbatim schema.
2. Cite it in the M8 plan's per-story blueprint as the contract for `developer.md` STATUS=done extension.
3. Wire `qa.md`'s pack-first instruction to read `.cleargate/sprint-runs/<sprint-id>/.qa-context-<story-id>.md`.
4. Define when QA classifies a story as `lane: runtime` (M2 passes the value through; M8 owns the semantics).
5. Define the lane playbook dispatch logic per CR-024 §3 line 114-117 (fast / standard / runtime sub-checklists).

**M2 must NOT write any agent-file prose.** That's M8's exclusive surface (sprint plan §2.2 line 103).

---

role: architect

### M3.md

# Milestone: M3 — `templates/initiative.md` promotion + `proposal.md` deletion + frontmatter cleanup
## Stories: CR-025 Story 1
## Wave: W1 (parallel — runs concurrently with M0/M1/M2 over disjoint surfaces)

role: architect

## Order

CR-025 Story 1 is the only unit in M3. Internal step ordering (single Developer agent executes top-to-bottom):

1. Rewrite `.cleargate/templates/initiative.md` (live) per the New Shape in §3 below.
2. Rewrite `cleargate-planning/.cleargate/templates/initiative.md` (canonical mirror) — byte-identical copy of step 1.
3. Delete `.cleargate/templates/proposal.md` (live).
4. Delete `cleargate-planning/.cleargate/templates/proposal.md` (canonical mirror).
5. Update test fixtures listed in §5 (in lockstep with deletion — tests must pass at HEAD).
6. Run `npm --prefix cleargate-cli run prebuild` to regenerate `cleargate-planning/MANIFEST.json` (drops the `proposal.md` entry; updates `initiative.md` SHA) AND refresh the derived `cleargate-cli/templates/cleargate-planning/` payload tree.
7. Run `npm --prefix cleargate-cli run typecheck` and `npm --prefix cleargate-cli test` — both must be green before commit.

**Strict ordering rule:** Do NOT split this into two commits. The deletion + test-fixture migration + manifest regen all land atomically per the FLASHCARD `2026-05-01 #manifest #prebuild` rule ("regenerate via npm run build in the SAME commit or doctor flags drift on next session").

**Parallelizability:** M3 is disjoint from M0 (BUG-024, no production code), M1 (`lib/report-filename.mjs` — new file, distinct dir), and M2 (`prep_qa_context.mjs` — new file under `.cleargate/scripts/`). Sprint plan §2.5 confirms: "CR-025 S1 deletes templates/proposal.md while CR-022 M5 doesn't touch templates. No collision." MANIFEST.json is touched by M3 only in Wave 1; if any other Wave 1 milestone happens to land a manifest-affecting edit (it shouldn't — none touch shipped tier-classified files), the prebuild regen at step 6 reconciles deterministically.

## Per-story blueprint

### CR-025 Story 1 — `templates/initiative.md` promotion + `proposal.md` deletion

**Cross-story coupling:**
- M9 (CR-025 S2, Wave 2) does the residual `[Pp]roposal` prose audit across `cleargate-protocol.md`, `cleargate-enforcement.md`, `CLAUDE.md` (live + canonical), and `.claude/agents/architect.md` + `reporter.md`. M3 does NOT touch those files. The `CLAUDE.md` line 128 / `cleargate-planning/CLAUDE.md` line 37 bullet (`Use the templates in .cleargate/templates/ ('proposal.md', 'epic.md', ...)`) is M9 territory — NOT M3. Sprint plan §2.2 row "CLAUDE.md (live + canonical mirror) | M9 (CR-025 protocol audit), M12 (CR-022 close-bullet update) | M9 → M12" confirms.
- M10 (CR-025 S3, Wave 3) handles PROPOSAL-008/009 archive moves. Out of scope for M3.
- The two prose-only references inside other live templates that mention `proposal.md` (`epic.md` line 111 *"Populate this strictly from the approved proposal.md"* and `story.md` line 6 *"Sourced from approved proposal.md"*) are ALSO M9 scope — they live inside `<instructions>` and template-body prose, exactly the shape M9 audits. Excluded from M3 to keep wave disjointness clean. M3 plan flags them in §2 below for M9 to pick up.

**Schema changes (verbatim — new `templates/initiative.md` frontmatter):**
- `initiative_id: "INIT-{ID}"` — three-digit zero-padded per CR-025 §0.5 ("Initiative file naming convention: `INITIATIVE-NNN_short_name.md`"; ID stem `INIT-NNN`).
- Drop the existing `<instructions>` "READ artifact / written by cleargate_pull_initiative" framing — that prose is correct for the MCP-pull path but incomplete (manual-paste path also valid per CR-025 §1). Replace with the dual-path framing in §3 below.

**Test scenarios (from CR-025 §4.3 Gherkin — only S1-relevant scenarios; S2/S3 scenarios run in M9/M10):**
1. **Scenario "templates/initiative.md is canonical stakeholder cheat sheet"** — file documents User flow, Diagrams, E2E verbal description, Business outcome, Success criteria, Open questions; does NOT prescribe AI-authoring instructions (no `<instructions>` block claiming WHAT-TO-GATHER for AI authoring).
2. **Scenario "templates/proposal.md does not exist"** — both `.cleargate/templates/proposal.md` and `cleargate-planning/.cleargate/templates/proposal.md` return ENOENT; `MANIFEST.json` does not list either path.

The remaining four §4.3 scenarios (proposal_gate_waiver removal, protocol mention sweep, PROPOSAL-008/009 archive, mirror-parity-across-CR-025) belong to M9/M10. M3 owns scenarios 1 + 2 only.

**Test fixture migration required (NOT a Gherkin scenario; required by §5 below):**
- `cleargate-cli/test/integration/foreign-repo.test.ts` line 97 — change assertion from `proposal.md` to `initiative.md`.
- `cleargate-cli/test/scripts/template-stubs.test.ts` line 21 — remove `'proposal.md'` from the `TEMPLATE_NAMES` array. Array length drops from 8 to 7.
- `cleargate-cli/test/scripts/template-sync-fields.test.ts` line 36 — remove `'proposal.md'` from the `ALL_TEMPLATES` tuple. Length drops 5 → 4. **Note:** `initiative.md` is NOT added to this list; it has different sync semantics (pull-only, not push) and is not in `PARSEABLE_TEMPLATES` either. The new `initiative.md` frontmatter MUST still carry the 6 sync-attribution keys (per template-sync-fields parity), but the test currently doesn't iterate over it; leave the test scope as-is.

**Reuse (no duplication):**
- The `<instructions>` block convention + POST-WRITE BRIEF section structure is the same shape as Sprint Plan Template (per `template-stubs.test.ts` STORY-025-04 scenarios at lines 64-147). The new `initiative.md` does NOT need a POST-WRITE BRIEF — it's BA/PM-authored, not AI-authored. Use a slim `<instructions>` block matching the SHAPE of `Sprint Plan Template.md`'s instructions block but with content distinct from the current `initiative.md`'s "READ artifact" framing.
- Frontmatter sync-attribution keys (`pushed_by`, `pushed_at`, `last_pulled_by`, `last_pulled_at`, `last_remote_update`, `source`) are mandatory across all live templates per `template-sync-fields.test.ts` line 23-30. Carry them forward in the new `initiative.md` even though the test currently iterates only over the 4 work-item templates — future-proofing matches existing template parity.
- `draft_tokens:` + `cached_gate_result:` stubs are mandatory across all 8 templates per `template-stubs.test.ts` line 14-27. Carry forward.

**Gotchas surfaced from code inspection:**
- `cleargate-planning/MANIFEST.json:202` (live) and identical line in `cleargate-cli/templates/cleargate-planning/MANIFEST.json:202` both list `.cleargate/templates/proposal.md`. The cli-bundled copy is **derived** (rebuilt by `cleargate-cli/scripts/copy-planning-payload.mjs:36-50` which `rmSync`s the destination first then `copyDir`s from `cleargate-planning/`). Do NOT hand-edit the cli-bundled copy — it's regenerated by `prebuild`. Single edit point: `cleargate-planning/` (canonical), then `npm run prebuild` propagates.
- `cleargate-cli/scripts/build-manifest.ts:36-60` walks the canonical tree and re-emits `MANIFEST.json` deterministically. Removing `proposal.md` from disk causes the manifest entry to drop on next `prebuild`. No manual MANIFEST.json edit required.
- `cleargate-cli/src/lib/work-item-type.ts:8` declares `WorkItemType = 'story' | 'epic' | 'proposal' | 'cr' | 'bug'`, and line 17 maps `proposal_id` → `'proposal'`. **This is for archived PROPOSAL-NNN files, not the deleted template.** Archive items per CR-025 §1 keep their `proposal_id` frontmatter. Leave `work-item-type.ts` alone — touching it is out of CR-025 S1 scope (and arguably out of scope for the entire CR; archive-immutability §11.4 applies). Flagged here so the Developer doesn't get tempted.
- `cleargate-cli/src/lib/readiness-predicates.ts:185-216` references `proposal_gate_waiver` as a frontmatter signal. **Stays.** Per CR-025 §1: "Archive files keep their `proposal_gate_waiver` field; CR-025 only drops it from new templates." The predicate must keep reading the field for archive lookups. CR-025 S1 scope on this field is "drop from work-item template frontmatter scaffolds" (CR-025 §3 Story 1 line 108). However, sprint plan §1 row for M3 explicitly says: "templates/initiative.md + delete proposal.md + cleanup" with no mention of dropping `proposal_gate_waiver` from the 5 work-item templates. **Cross-checking CR-025 §3 Story 1 line 108 vs. sprint plan §1: the CR scopes `proposal_gate_waiver` removal to S1; the sprint plan delegates only the rename + delete to M3.** This is an open decision — see "Open decisions for orchestrator" §4 below. M3 default execution should follow CR-025 §3 Story 1 line 108 (drop the field from `epic.md`, `story.md`, `CR.md`, `Bug.md`, `hotfix.md` live + canonical = 10 file edits, plus the field is currently absent from those templates per FLASHCARD `2026-05-01 #templates #frontmatter #proposal_gate_waiver`: *"`proposal_gate_waiver` field never lived in any template — only in in-flight artifacts. CR-020's 'drop from templates' was a no-op verify, not a removal."*). → **The 10-file edit is a no-op verify**, per the flashcard. Developer just confirms the field is absent in those 10 files post-edit; no actual frontmatter mutation. M3 plan keeps the verification step but expects zero diff.
- `cleargate-cli/src/wiki/page-schema.ts:7`, `load-wiki.ts:13`, `synthesis/product-state.ts:36+51`, `synthesis/open-gates.ts:12-26` all reference the `'proposal'` bucket / page type. **Stays.** Wiki bucket layout is for archived PROPOSAL-NNN pages — same archive-immutability carve-out as `work-item-type.ts`. Out of CR-025 S1 scope; out of scope of the entire CR-025.
- `cleargate-cli/src/lib/slug.ts:49-58` — `PROPOSAL_ID_RE` regex + scanner for next PROPOSAL ID. **Stays.** Same archive-read carve-out. Plus there's a future possibility of someone re-creating a PROPOSAL file (low likelihood, but the scanner is harmless and removing it adds risk to no benefit).
- The CLAUDE.md bullet at `CLAUDE.md:128` and `cleargate-planning/CLAUDE.md:37` says: *"Use the templates in `.cleargate/templates/` (`proposal.md`, `epic.md`, ...)"*. Editing this is M9 scope, NOT M3 (per sprint plan §2.2 merge ordering "M9 → M12"). M3 leaves this prose untouched — the wave-2 audit will land on top of M3's clean baseline (template file gone) and rewrite the bullet to drop `proposal.md` + leave `initiative.md`.
- `cleargate-planning/.cleargate/templates/epic.md:111` *"(AI Planning Engine: Populate this strictly from the approved proposal.md)"* and `cleargate-planning/.cleargate/templates/story.md:6` *"Sourced from approved proposal.md."* — both prose-only, both M9 scope (template-body prose, same audit pass). M3 leaves them. Flagged in §2 below for M9.
- FLASHCARD `2026-05-01 #mirror #parity` says: *"CLAUDE.md live↔canonical pre-divergent by 4 canonical-only bullets since pre-EPIC-024. Edit-parity invariant applies per-edit, not whole-file — never reconcile pre-existing divergence as a side effect."* **For M3 this matters because:** the live↔canonical templates `proposal.md` IS byte-identical (verified via `diff` 2026-05-01). The new `initiative.md` MUST be written to BOTH paths byte-identically in the same atomic step. Do NOT use the existing `cleargate-planning/.cleargate/templates/initiative.md` as a partial-overwrite target — full rewrite both files.
- Sprint plan §2.4 Lane Audit lists M3 as `lane: standard` (not in the fast-lane table). Standard lane → no fast-lane shortcuts; full DoD applies. Confirmed.

## §3 Final shape of new `templates/initiative.md`

The Developer writes this verbatim to BOTH `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/initiative.md` AND `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/initiative.md`. Byte-identical. The CR-025 §1 New Truth defines six required body sections; the frontmatter parity rules (sync-attribution + draft_tokens + cached_gate_result stubs) carry forward from the existing template scaffold convention.

```markdown
<instructions>
USE THIS TEMPLATE TO DOCUMENT WHAT AN INITIATIVE LOOKS LIKE — the BA/PM-authored
stakeholder-input artifact that captures business intent BEFORE AI decomposition.

WHO AUTHORS THIS: a human BA / PM / sponsor — outside the codebase OR in the upstream
PM tool (Linear / Jira / GitHub Issues). The AI does NOT author Initiatives from
scratch in the repo. This template is a cheat sheet for the human author.

HOW IT GETS INTO THE REPO (two paths):
  1. MCP pull (preferred): `cleargate_pull_initiative` fetches from the upstream PM
     tool → caches at `.cleargate/delivery/pending-sync/INITIATIVE-NNN_*.md`.
  2. Manual paste: human drops a markdown file matching this shape into
     `pending-sync/`. AI stamps `source: manual-paste` then triages.

WHAT THE AI DOES WITH IT:
  - Reads the cached/pasted Initiative.
  - Asks open questions in chat.
  - DECOMPOSES into Epic / Story / CR / Bug — NEVER edits the Initiative body.
  - On triage completion: moves the Initiative to `archive/INITIATIVE-NNN_*.md`
    with frontmatter stamps `triaged_at: <ISO-8601>` and
    `spawned_items: [EPIC-NNN, STORY-NNN-NN, ...]`.

OUTPUT LOCATION: `.cleargate/delivery/pending-sync/INITIATIVE-{NNN}_{short_name}.md`

DO NOT output these instructions in the rendered file.
</instructions>

---
initiative_id: "INIT-{NNN}"
remote_id: null
source_tool: "linear | jira | github | manual-paste"
status: "{PM native status — e.g. Discovery, In Triage, Triaged}"
synced_at: null
triaged_at: null
spawned_items: []
created_at: "{ISO-8601 timestamp}"
updated_at: "{ISO-8601 timestamp}"
created_at_version: "cleargate@{semver}"
updated_at_version: "cleargate@{semver}"
server_pushed_at_version: null
draft_tokens:
  input: null
  output: null
  cache_read: null
  cache_creation: null
  model: null
  sessions: []
cached_gate_result:
  pass: null
  failing_criteria: []
  last_gate_check: null
# Sync attribution. Optional; stamped by `cleargate_pull_initiative`.
pushed_by: null
pushed_at: null
last_pulled_by: null
last_pulled_at: null
last_remote_update: null
source: "remote-pulled"        # or "manual-paste"
last_synced_status: null
last_synced_body_sha: null
---

# INITIATIVE-{NNN}: {Initiative Name}

## 1. User Flow

{End-to-end walkthrough of what the user does — step-by-step, in plain language.
Capture the happy path first, then named alternative flows. Diagrams welcome
(see §2). No technical implementation details — that's the AI's job during
decomposition.}

## 2. Diagrams

{Embedded mermaid / asciiart / linked image. Optional but encouraged for any
flow with branching, parallel paths, or multiple actors.

Placeholder:
```
[ User ] → [ Action 1 ] → [ Outcome ]
              ↓
          [ Branch ]
```
}

## 3. End-to-End Verbal Description

{The "tell me what this does" paragraph. 3-8 sentences. Captures the intent,
the ask, and the boundary of the work. Skim-readable for a stakeholder; rich
enough that the AI can ground the first round of decomposition questions in it.}

## 4. Business Outcome

{Why this matters. 1-2 sentences linking the work to a business metric, user
need, or strategic goal. Answers "if this ships, what changes?"}

## 5. Success Criteria

- {Concrete, measurable criterion 1 — what must be true to call this done.}
- {Concrete, measurable criterion 2}
- {Concrete, measurable criterion 3 (optional)}

## 6. Open Questions for AI Triage

{Questions the BA/PM knows are unresolved and wants the AI to surface during
decomposition. The AI MAY answer in chat then propose decomposition; the AI
does NOT edit this section in the Initiative body.

- Question 1
- Question 2
}

---

## Stakeholder Authoring Notes (not pushed)

- Initiative is the **stakeholder-input artifact**, not an AI-authored draft.
  See `.cleargate/knowledge/cleargate-protocol.md` Plan-phase intake (post-CR-025).
- The AI never invokes `cleargate_push_item` on this file. Push semantics are
  one-way: Initiative is **pulled**, Epic / Story / CR / Bug / Hotfix are **pushed**.
- After triage, this file moves to `archive/` with `triaged_at` + `spawned_items`
  stamped. The PM tool retains source-of-truth for content; the repo retains
  the audit trail.
```

**Notes on the §3 shape (Architect rationale, not part of the rendered template):**

- Six body sections (User flow, Diagrams, E2E verbal, Business outcome, Success criteria, Open questions) match CR-025 §3 Story 1 line 106 exactly.
- The `<instructions>` block is intentionally NOT a POST-WRITE BRIEF block — Initiatives are not AI-authored, so the BRIEF→push handshake doesn't apply. Test scenario 1 ("does NOT prescribe AI-authoring instructions; no `<instructions>` WHAT-TO-GATHER block") is satisfied: the instructions describe authoring AUDIENCE (the human BA/PM) + INTAKE PATH, not WHAT-TO-GATHER for an AI agent.
- The Stakeholder Authoring Notes section at the bottom is a deliberate echo of the cross-CR semantics CR-025 §1 establishes; keeps the template self-documenting for downstream consumers (PM-tool template export, manual-paste authors).
- New frontmatter fields `triaged_at` + `spawned_items` are introduced here for the first time. They're written by the AI during triage (M10 / future MCP work), not by the BA/PM author. Default values `null` and `[]` keep the parser happy at draft time.
- `source: "remote-pulled"` default vs. `"manual-paste"` honors CR-025 §1 two-path framing.
- `proposal_gate_waiver:` field is **NOT** in the new frontmatter. CR-025 explicitly drops it from new templates per §1 Obsolete Logic line 69.

## §4 Mirror-parity callout

**FLASHCARD `2026-05-01 #mirror #parity` and `2026-04-19 #wiki #protocol #mirror` apply.**

**Per-edit invariant:** every M3 edit lands in BOTH locations or NEITHER. The Developer must, in a single git stash-clean working tree:

- WRITE `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/initiative.md` (live) with the §3 content.
- WRITE `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/initiative.md` (canonical) byte-identical.
- DELETE `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/proposal.md` (live).
- DELETE `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/proposal.md` (canonical).
- Run `npm --prefix cleargate-cli run prebuild`. This:
  - Regenerates `cleargate-planning/MANIFEST.json` (drops `.cleargate/templates/proposal.md` row at line 202; updates `.cleargate/templates/initiative.md` SHA at line 195).
  - Re-copies the entire `cleargate-planning/` payload into `cleargate-cli/templates/cleargate-planning/`, propagating BOTH the rewrite + the deletion + the manifest regen into the cli-bundled copy automatically.

**Verification at end of step 5 of the Order:** `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/initiative.md /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/initiative.md` must return empty. `ls /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/proposal.md /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/proposal.md` must both ENOENT.

## §5 Test fixture migration

Pre-grep results from `cleargate-cli/test/` for the literal `templates/proposal` (and adjacent string `proposal.md`):

| File | Line | Current | After M3 |
|---|---|---|---|
| `cleargate-cli/test/integration/foreign-repo.test.ts` | 97 | `expect(fs.existsSync(path.join(tmp, '.cleargate', 'templates', 'proposal.md')), '.cleargate/templates/proposal.md').toBe(true);` | `expect(fs.existsSync(path.join(tmp, '.cleargate', 'templates', 'initiative.md')), '.cleargate/templates/initiative.md').toBe(true);` |
| `cleargate-cli/test/scripts/template-stubs.test.ts` | 21 | `'proposal.md',` (entry in `TEMPLATE_NAMES` array) | Remove the line. Array length shrinks 8 → 7. |
| `cleargate-cli/test/scripts/template-sync-fields.test.ts` | 36 | `const ALL_TEMPLATES = ['proposal.md', 'epic.md', 'story.md', 'CR.md', 'Bug.md'] as const;` | `const ALL_TEMPLATES = ['epic.md', 'story.md', 'CR.md', 'Bug.md'] as const;` (length 5 → 4). Do NOT add `'initiative.md'` — it's pull-only and not in the original sync-attribution scope per STORY-010-01. |

**No additional test files reference `templates/proposal`** in `cleargate-cli/test/` per the pre-grep. `template-stubs.test.ts` describes 8 templates; after M3 it describes 7 (drops `proposal.md`, keeps `initiative.md`). The describe-block at line 29 ("Scenario: All 8 live templates have draft_tokens + cached_gate_result stubs") needs the `8` count updated to `7` in its description string — purely cosmetic (the loop iterates the array). Update line 29 + line 40 from `"All 8 live templates"` → `"All 7 live templates"` and `"All 8 mirror templates"` → `"All 7 mirror templates"`.

**No new test file required for §4.3 scenarios 1 + 2.** The existing `template-stubs.test.ts` already exercises the byte-identical mirror invariant for `initiative.md` (line 51-59: `Scenario: Live and mirror templates are byte-identical` runs the loop over the full `TEMPLATE_NAMES` array, which post-M3 includes `initiative.md`). The "templates/proposal.md does not exist" Gherkin assertion is implicitly covered: after removing `'proposal.md'` from the array, any test that did `fs.readFileSync(path.join(LIVE_TEMPLATES_DIR, 'proposal.md'))` would throw ENOENT — and no such test remains. The "is canonical stakeholder cheat sheet" Gherkin (scenario 1) is content-shape — verify by manual inspection per CR-025 §4.1 manual verification rows. CR-025 §4.1 calls for "Doc lint tests | 6" but those 6 cover the FULL CR-025 (S1 + S2 + S3); CR-025 §3 Story 1 deliverables can land without a new dedicated `initiative-rename.test.ts` file in M3 — the new test file is an M9 deliverable (where the prose-audit assertions live).

**Gotcha:** if the Developer accidentally keeps `'proposal.md'` in `TEMPLATE_NAMES` after M3, the test fails with ENOENT inside `fs.readFileSync` — fast feedback, no silent drift. Fixture migration in step 5 of the Order is mandatory + atomic with the deletion in steps 3-4.

## Cross-story risks

Things a Developer working only on M3 (CR-025 S1) might miss:

1. **`epic.md` line 111 + `story.md` line 6 reference `proposal.md` in template-body prose.** These are inside the canonical mirror (`cleargate-planning/.cleargate/templates/epic.md` / `story.md`) AND mirrored to live. They are NOT M3 scope — M9 (CR-025 S2 prose audit) sweeps them. **Risk:** a Developer over-eager to "clean up all proposal.md references" might edit them in M3, blowing the wave-disjoint contract. **Mitigation:** Developer reads this §"Cross-story risks" before touching anything; if tempted, leave alone. Sprint plan §2.2 row "M9 → M12" is the merge contract.
2. **`CLAUDE.md` line 128 + `cleargate-planning/CLAUDE.md` line 37 list `proposal.md` in the templates bullet.** Same as above — M9 scope, NOT M3. The system-reminder for the canonical CLAUDE.md content I received during plan-drafting confirms line 37 still reads `(\`proposal.md\`, \`epic.md\`, ...)`. M9 will rewrite it to drop `proposal.md` and leave `initiative.md`.
3. **`cleargate-cli/src/lib/work-item-type.ts:8 + 17`, `wiki/page-schema.ts:7`, `wiki/load-wiki.ts:13`, `wiki/synthesis/product-state.ts:36+51`, `wiki/synthesis/open-gates.ts:12-26`, `lib/slug.ts:49-58`, `lib/readiness-predicates.ts:185-216`, `lib/sync-log.ts:28`, `lib/lifecycle-reconcile.ts:84+282+363+386+445-447`** all reference `proposal` (not `proposal.md`) for archive-read code paths. These are explicitly OUT of scope for the entire CR-025 (archive-immutability §11.4). **Risk:** an enthusiastic Developer touches them anyway. **Mitigation:** §"Per-story blueprint > Gotchas" above lists each one; Developer must not edit any of them in M3.
4. **Manifest SHA drift carries forward to other Wave 1 commits if they finish before M3 lands.** Per FLASHCARD `2026-05-01 #manifest #prebuild`, manifest regen happens in the SAME commit as the underlying file change, or doctor flags drift on next session. **Risk:** if M3 commits before regen, doctor red-flags. **Mitigation:** step 6 of the Order ("Run npm run prebuild") runs BEFORE the commit — manifest changes are part of the same git commit. M0/M1/M2 don't touch shipped tier-classified files (BUG-024 is doc-only; M1's `lib/report-filename.mjs` is NEW under `.cleargate/scripts/lib/` which TIER_RULES treats as scaffold-tier and IS included in the manifest — so M1's commit also runs prebuild; same pattern). No collision because the regen is deterministic per `build-manifest.ts:36-60`.
5. **Test counts in `template-stubs.test.ts` describe-string say "8 live templates"; after M3 they say "7".** Pure cosmetic but the describe text gets read by humans. Update line 29 + line 40. Failing to update doesn't break tests but creates documentation drift.
6. **Initiative pull tooling (`cleargate_pull_initiative` MCP tool) is OUT of scope for CR-025 entirely.** Per CR-025 §2 line 93: *"`cleargate_pull_initiative` MCP tool already exists (per CLAUDE.md scan); this CR only documents the flow, doesn't add tooling."* The new `templates/initiative.md` references this tool in the `<instructions>` block but does not mandate its existence. **Risk:** Developer searches for the tool, can't find it, gets confused. **Mitigation:** flagged here. The tool is referenced in CLAUDE.md prose (existing) but no implementation. Documentation-only reference per CR-025 scope.

## Open decisions for orchestrator

1. **Scope of `proposal_gate_waiver` removal in M3 vs. M9.** CR-025 §3 Story 1 line 108 says: *"Drop `proposal_gate_waiver:` field from frontmatter in: `epic.md`, `story.md`, `CR.md`, `Bug.md`, `hotfix.md` (live + canonical mirrors of each)."* But sprint plan §1 row for M3 only lists "`templates/initiative.md` + delete `proposal.md` + cleanup". FLASHCARD `2026-05-01 #templates #frontmatter #proposal_gate_waiver` says the field never existed in any template, so the removal is a no-op verify. **M3 default execution:** Developer runs `grep -rn "proposal_gate_waiver" .cleargate/templates/ cleargate-planning/.cleargate/templates/` over the 5 work-item templates × 2 mirrors = 10 files, expects zero hits, records the verification in the commit message. No file mutation. **Orchestrator: confirm this default is acceptable, or escalate to user if you want explicit M3-or-M9 split.**

2. **Should `templates/initiative.md` `<instructions>` block carry a non-AI-authored disclaimer (current §3 draft does — bullet "WHO AUTHORS THIS")?** CR-025 §4.3 Gherkin says the file *"does NOT prescribe AI-authoring instructions (no `<instructions>` WHAT-TO-GATHER block)"*. The §3 draft above complies (no WHAT-TO-GATHER block) but DOES have an `<instructions>` block — describing audience + intake path, not authoring. Reading the Gherkin literally, an `<instructions>` block is allowed as long as it doesn't prescribe AI-authoring. **M3 default:** keep the audience+intake `<instructions>` block as drafted in §3. **Orchestrator: confirm the literal Gherkin reading is correct, or kick back to drop the entire `<instructions>` block and use a top-of-file H1 + paragraph instead.**

3. **`triaged_at` + `spawned_items` frontmatter fields are NEW in this template.** They're not in the existing `templates/initiative.md` (which had no triage stamps — the existing template was pull-only). The new template introduces both fields with default `null` / `[]`. No code consumes these fields yet (they're forward-looking — written during AI triage, read by future archive lookups). **Orchestrator: confirm introducing two un-consumed frontmatter fields in M3 is acceptable, or kick back to defer them to a future CR (when triage automation lands).**

4. **Test file `cleargate-cli/test/scripts/initiative-rename.test.ts` mentioned in CR-025 §4.1.** §4.1 names this file as the home for 6 doc-lint tests (one per Gherkin scenario). The 6 scenarios cover the FULL CR-025 (S1+S2+S3), so the new file is best authored in M9 (where the prose-audit assertions also live). **M3 default:** do NOT create `initiative-rename.test.ts` in M3; defer to M9. Update the existing 3 test files (`foreign-repo.test.ts`, `template-stubs.test.ts`, `template-sync-fields.test.ts`) per §5 above. **Orchestrator: confirm M9 owns the new test file, or kick back to seed it in M3 with the 2 S1-relevant scenarios.**

## Definition of Done (M3-specific echo of CR-025 §4.2)

- [ ] `templates/proposal.md` absent in BOTH `.cleargate/templates/` AND `cleargate-planning/.cleargate/templates/` (and propagated to `cleargate-cli/templates/cleargate-planning/.cleargate/templates/` via prebuild).
- [ ] `templates/initiative.md` present in BOTH `.cleargate/templates/` AND `cleargate-planning/.cleargate/templates/` with the §3 shape; live↔canonical `diff` returns empty.
- [ ] `cleargate-planning/MANIFEST.json` no longer lists `.cleargate/templates/proposal.md`; SHA for `.cleargate/templates/initiative.md` updated; commit reflects the regen in the SAME atom (FLASHCARD `#manifest #prebuild`).
- [ ] `cleargate-cli/test/integration/foreign-repo.test.ts:97` migrated `proposal.md` → `initiative.md`.
- [ ] `cleargate-cli/test/scripts/template-stubs.test.ts` `TEMPLATE_NAMES` array shrunk 8 → 7; describe strings line 29 + line 40 updated; all tests green.
- [ ] `cleargate-cli/test/scripts/template-sync-fields.test.ts` `ALL_TEMPLATES` tuple shrunk 5 → 4; all tests green.
- [ ] `npm --prefix cleargate-cli run typecheck` green.
- [ ] `npm --prefix cleargate-cli test` green (all suites — `template-stubs`, `template-sync-fields`, `foreign-repo`, plus all unaffected suites).
- [ ] Foreign-repo integration test (`foreign-repo.test.ts`) still passes — proves `cleargate init` against a tmpdir produces the new `initiative.md` (not the deleted `proposal.md`).
- [ ] Mirror-parity holds: `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/initiative.md /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/initiative.md` returns empty (FLASHCARD `2026-05-01 #mirror #parity` per-edit invariant).
- [ ] Single commit per CR-025 §4.2 — message format `feat(CR-025): STORY 1 — promote initiative.md, delete proposal.md, migrate template tests`.
- [ ] No edits to: CLAUDE.md (live or canonical), `cleargate-protocol.md`, `cleargate-enforcement.md`, `.claude/agents/*.md`, `cleargate-cli/src/**`, `epic.md` body prose, `story.md` body prose, archive files. All those are M9 / M10 / M12 scope or out-of-scope entirely.

---

role: architect

### M4.md

# Milestone: M4 — CR-022 M1 Step 2.7 worktree-closed pre-close check
## Stories: CR-022 M1 (anchor in sprint plan §1 row 6 — "Step 2.7 worktree-closed pre-close check")
## Wave: W2 (parallel — runs concurrently with M5 / M6 / M7 / M8 / M9 over disjoint surfaces; sequential against M5+M6+M11 inside `close_sprint.mjs` per sprint plan §2.2)

role: architect

## Order

CR-022 M1 is the only unit in M4. Single Developer agent executes top-to-bottom:

1. **Pre-flight read.** Re-grep `.cleargate/scripts/close_sprint.mjs` lines 260–315 to confirm Step 2.6 closer + Step 3 opener boundary still match the line numbers cited in §"Per-story blueprint > Insert location" below. The file is currently 448 LOC at HEAD post-M0 (commit 9603f05 byte-identical, verified via `diff` 2026-05-01).
2. Insert Step 2.7 block in `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs` per the §"Per-story blueprint > Insert location" + §"Function shape" specs below.
3. Insert byte-identical Step 2.7 block in `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (canonical mirror).
4. Run `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` — must return empty before commit.
5. Add Step-2.7 scenarios to `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` (extend existing suite — see §"Test fixture path"). Two scenarios minimum: (a) leftover-worktree blocks under v2; (b) clean state passes.
6. Run `npm --prefix cleargate-cli run prebuild` to regenerate `cleargate-planning/MANIFEST.json` SHA for `.cleargate/scripts/close_sprint.mjs` (FLASHCARD `2026-05-01 #manifest #prebuild`).
7. Run `npm --prefix cleargate-cli run typecheck` and `npm --prefix cleargate-cli test` — both green before commit.
8. Single commit. Format per CR-022 §4.2: `feat(CR-022): STORY-022-M1 — Step 2.7 worktree-closed pre-close check` (note: CR-022 §4.2 line 183 shows `feat(EPIC-???-or-CR-022): <story-id> <short desc>`; CR-022 has no parent EPIC so the scope is `CR-022`; FLASHCARD `2026-05-01 #commit-format #dod` flags Developers default to `feat(<sprint>):` — Developer must use `feat(CR-022):`, NOT `feat(SPRINT-19):`).

**Strict ordering rule:** Steps 2 and 3 must land in the SAME commit. Live-only OR canonical-only edits fail FLASHCARD `2026-05-01 #mirror #parity` per-edit invariant. The prebuild regen at step 6 also belongs in the same commit per FLASHCARD `2026-05-01 #manifest #prebuild`.

**Parallelizability with sibling Wave 2 milestones:**
- **M5 (CR-022 M2 Step 2.8 sprint-merged-to-main verify)** — touches `close_sprint.mjs` at a DIFFERENT step boundary (after M4's Step 2.7, before Step 3). Sprint plan §2.2 row "M4 → M5 → M6 → M11" mandates **M4 lands first**; M5's Architect plan must cite line numbers AFTER M4's insert. **Hand-off note for M5 Architect:** post-M4 the Step 2.7 block ends at line ~360 (estimated; M5 must re-grep at planning time). M5 inserts between M4's Step 2.7 closing comment and the existing Step 3 opener (currently line 316; post-M4 will shift by the M4 block's LOC, ~50 lines).
- **M6 (CR-022 M3 Steps 6.5/6.6/6.7)** — inserts after Step 6 (line 419-ish at HEAD; far below Step 2.7). Disjoint section, but waits on M5 to land first per sprint plan §2.2 to keep merge sequencing clean.
- **M7 (CR-022 M5 cap-raise + flag)** — touches `prep_reporter_context.mjs` and `cleargate-cli/src/commands/sprint.ts`. Disjoint from `close_sprint.mjs`.
- **M8 (CR-024 S2)** — disjoint, dev-doc updates.
- **M9 (CR-025 S2)** — disjoint, protocol audit.

## Per-story blueprint

### CR-022 M1 — Step 2.7 worktree-closed pre-close check

**Cross-story coupling:**
- M5 (Step 2.8) inserts immediately after M4's Step 2.7 in close_sprint.mjs. Both are pre-close blocks under the same v2 enforcement model. M5 reads the same `state.execution_mode` flag M4 sets up (see §"Function shape" advisory-vs-enforcing logic below) — M5's Architect should pattern-match.
- M11 (CR-022 M4 Step 8 verbose handoff list) is the FINAL stdout block in close_sprint.mjs. Disjoint section, but inherits the v2-style step-numbering convention M4 establishes.
- The new lib helper `lib/report-filename.mjs` (M0, shipped in commit 9603f05) is **NOT** consumed by M4. Step 2.7 does not read or write the report file. The helper is in scope for M5/M6 (which echo the SPRINT-<#>_REPORT.md filename in user-facing stderr) and remains the canonical naming source elsewhere in the file. M4's Step 2.7 stderr messages reference `.worktrees/STORY-*` paths, not the report.
- The existing `.worktrees/STORY-*` discovery pattern is **already implemented** at `cleargate-cli/src/commands/sprint.ts:931-970` (function `checkNoLeftoverWorktrees`, used by `cleargate sprint preflight`). M4 mirrors that pattern in `.mjs` — see §"Reuse" below.

**Schema changes:** None. No state.json or sprint frontmatter changes. CR-022 §2 line 115 confirms: *"No schema/migration impact on state.json or sprint frontmatter."*

**Test scenarios (from CR-022 §4.3 Gherkin scenario "Step 2.7 blocks on leftover worktree" + DoD §4.1 "Steps 2.7 ... at least one happy-path + one failure-mode test per new step"):**

1. **Scenario "Step 2.7 blocks on leftover worktree under v2"** — Given a v2 sprint with all stories Done AND a `.worktrees/STORY-NNN-NN/` directory present in REPO_ROOT, when `close_sprint.mjs SPRINT-19 --assume-ack` runs, then exit code is 1 AND stderr contains `Step 2.7 failed: leftover worktree at .worktrees/STORY-NNN-NN` AND state.json `sprint_status` stays `Active` (NOT flipped to Completed). Maps to CR-022 §4.3 Gherkin "Scenario: Step 2.7 blocks on leftover worktree".
2. **Scenario "Step 2.7 happy path — no leftover worktrees"** — Given a v2 sprint with all stories Done AND no `.worktrees/STORY-*` paths, when `close_sprint.mjs SPRINT-XX --assume-ack` runs, then stdout contains `Step 2.7 passed: no leftover worktrees.` AND the pipeline proceeds past Step 2.7 (Step 2.6 reconcile-skipped + Step 3 prefill messages also appear).
3. **Scenario "Step 2.7 advisory under v1 (warn + continue)"** — Given a v1 sprint (`schema_version: 1` OR `execution_mode: "v1"` in state.json) AND a leftover worktree, when close_sprint.mjs runs, then stderr contains `Step 2.7 warning: leftover worktree at .worktrees/STORY-NNN-NN (advisory in v1)` AND exit code is 0 (or whatever the rest of the pipeline returns) AND the pipeline proceeds past Step 2.7. Maps to CR-022 §3 M1 line 130: *"v2 enforcing; v1 advisory (warn + continue)."*
4. **Scenario "Step 2.7 skipped when git worktree list unavailable"** — Given `git worktree list --porcelain` exits non-zero (e.g. tmpdir is not a git repo — applies to ALL existing test fixtures since they spawn close_sprint against tmpdir-based sprint dirs, not real git working trees), when close_sprint.mjs runs, then stdout contains `Step 2.7 skipped: git worktree list unavailable (non-fatal).` AND exit code is 0. **CRITICAL** — without this skip, ALL existing test scenarios in `test_close_sprint_v21.test.ts` would fail because they run against fresh tmpdirs (per `makeTempSprintDir` at line 70-101). This is the same fail-open pattern used at `sprint.ts:940-943` (`return { ..., skipped: true, message: 'skipped (git worktree list unavailable)' }`).

The 4 scenarios cover the v2 happy + failure paths AND v1 advisory AND the skip path. CR-022 §4.1 requires **"at least one happy-path + one failure-mode test per new step"** — M4 ships 2 v2 + 1 v1 advisory + 1 skip = 4 scenarios.

**Reuse (no duplication):**
- **`cleargate-cli/src/commands/sprint.ts:931-970` `checkNoLeftoverWorktrees(cwd, execFn)`** — the existing TypeScript implementation in the `cleargate sprint preflight` command. M4 mirrors this exact pattern in `.mjs`. Specifically:
  - The `git worktree list --porcelain` invocation (line 939).
  - Line-by-line parsing for `worktree <path>` prefix (line 947-957).
  - The regex `/[/\\]\.worktrees[/\\]STORY-/.test(wtPath)` (line 952) to match `.worktrees/STORY-*` paths cross-platform.
  - The regex `/(\.(worktrees)[/\\]STORY-.+)$/.exec(wtPath)` (line 954) to extract a relative-looking display path for stderr.
  - The skip-on-failure pattern at line 940-943 (try/catch around `execFn`, return `{ pass: true, skipped: true }` if git fails).
  - **However:** M4 does NOT import from sprint.ts (it's a TypeScript file in a different package; close_sprint.mjs is plain `.mjs` in `.cleargate/scripts/`). M4 inlines the logic — ~25 LOC. This is acceptable duplication: the two implementations live in different package boundaries (CLI vs scripts) and the canonical home for Gate 4 close-time checks is close_sprint.mjs. Future consolidation belongs to a separate CR.
- **`execSync` from `node:child_process`** — already imported at line 39 of close_sprint.mjs. Reuse.
- **`REPO_ROOT` constant** — already declared at line 68 of close_sprint.mjs. Reuse for the `cwd` argument to `execSync('git worktree list --porcelain', { cwd: REPO_ROOT })`. **Critical:** the `git worktree list` call MUST use `REPO_ROOT` as cwd, NOT `sprintDir` — sprint runs land in `.cleargate/sprint-runs/SPRINT-NN/` which is a subdir, not the git toplevel. Running `git worktree list` from REPO_ROOT (the actual git root) returns the correct list. Tests run against tmpdirs which are not git repos — the skip path (Scenario 4 above) handles that.
- **State-shape access for v1/v2 detection** — pattern already used at line 198: `const isV2 = (state.schema_version || 1) >= 2;`. M4 reuses this exact expression. **However:** Step 2.5 uses `isV2` AND `hasFastLane` (because v2.1 validation only fires for sprints with at least one fast-lane story); Step 2.7 must fire for ALL v2 sprints regardless of lane. Use `isV2` alone for the v2-vs-v1 gate. SPRINT-19 has `execution_mode: "v2"` in state.json (line 4); M4 reads `state.schema_version` (which is 2 for SPRINT-19 per state.json line 2). NOTE: state.json has BOTH `schema_version: 2` and `execution_mode: "v2"`. The existing code uses `schema_version` exclusively at line 198 — M4 keeps that pattern for consistency. (Architect note: `execution_mode` is the sprint frontmatter field; `schema_version` is the state.json field. They drift only if a v2 sprint had a v1 state.json — possible during migration, but the migrate path at line 51-65 handles it.)

**Insert location (line numbers verified at HEAD 2026-05-01, post-M0 commit 9603f05):**

`close_sprint.mjs` line layout in the relevant region (HEAD = 448 LOC):

| Line | Content |
|---|---|
| 260 | `// ── Step 2.6: Lifecycle Reconciliation (CR-017) ──────────────────────────` |
| 265 | `process.stdout.write('Step 2.6: running lifecycle reconciliation...\n');` |
| 298 | `process.stdout.write('Step 2.6 passed: lifecycle reconciliation clean.\n');` |
| 309 | `process.stdout.write('Step 2.6 skipped: CLI binary not found at cleargate-cli/dist/cli.js (non-fatal).\n');` |
| 311–314 | catch block for unexpected step 2.6 errors |
| **315** | **blank line — END of Step 2.6 block** |
| **316** | **`// ── Step 3: Invoke prefill_report.mjs ─────────────────────────────────────`** |
| 317 | `process.stdout.write('Step 3: running prefill_report.mjs...\n');` |

**Insert range: between line 314 (end of Step 2.6 catch block) and line 316 (Step 3 opener comment).** The new Step 2.7 block lives at lines 315–~360 post-edit (estimated ~45 lines including comment header + the function call + the v2/v1 branch + skip handling). The blank line currently at 315 is consumed; a new blank line at the end of Step 2.7 separates it from Step 3.

**Surrounding step boundary text to cite verbatim in the implementation:**
- The `// ── Step 2.6: ...` opener at line 260 follows a `// ──` decorator — Step 2.7 MUST use the same decorator style: `// ── Step 2.7: Worktree-Closed Check (CR-022 M1) ──────────────────────────`.
- The `process.stdout.write('Step 2.X: ...');` opener pattern at line 265 / 317 / 329 — Step 2.7 MUST use the same pattern: `process.stdout.write('Step 2.7: checking for leftover worktrees...\n');`.
- The `process.stdout.write('Step 2.X passed: ...');` pass message pattern at line 298 / 335 — Step 2.7 MUST use the same pattern: `process.stdout.write('Step 2.7 passed: no leftover worktrees.\n');`.
- The `process.stdout.write('Step 2.X skipped: ...');` skip message pattern at line 309 — Step 2.7 MUST use the same pattern: `process.stdout.write('Step 2.7 skipped: git worktree list unavailable (non-fatal).\n');`.
- The `process.stderr.write(\`Step 2.X warning: ...\`);` warn pattern at line 313 — Step 2.7 MUST use the same pattern for v1 advisory mode AND for unexpected errors.
- The `process.exit(1)` block + 4-line stderr message at line 301-306 (for Step 2.6 fail) — Step 2.7 MUST use the same pattern for v2-mode block: stderr lists leftover worktrees + exits 1.

**Function shape (inline in close_sprint.mjs — NOT a separate exported helper):**

```javascript
// ── Step 2.7: Worktree-Closed Check (CR-022 M1) ──────────────────────────
// Block close if any .worktrees/STORY-* path is present.
// v2 enforcing (exit 1); v1 advisory (warn + continue).
// Skip if git worktree list is unavailable (non-fatal — tests run against tmpdirs).
process.stdout.write('Step 2.7: checking for leftover worktrees...\n');
{
  let leftoverWorktrees = [];
  let worktreeListAvailable = true;
  try {
    const output = execSync('git worktree list --porcelain', {
      cwd: REPO_ROOT,
      encoding: 'utf8',
    });
    for (const line of output.split('\n')) {
      const trimmed = line.trim();
      if (!trimmed.startsWith('worktree ')) continue;
      const wtPath = trimmed.slice('worktree '.length);
      if (/[/\\]\.worktrees[/\\]STORY-/.test(wtPath)) {
        const m = /(\.(worktrees)[/\\]STORY-.+)$/.exec(wtPath);
        leftoverWorktrees.push(m ? m[1] : wtPath);
      }
    }
  } catch {
    worktreeListAvailable = false;
  }

  if (!worktreeListAvailable) {
    process.stdout.write('Step 2.7 skipped: git worktree list unavailable (non-fatal).\n');
  } else if (leftoverWorktrees.length === 0) {
    process.stdout.write('Step 2.7 passed: no leftover worktrees.\n');
  } else if (isV2) {
    // v2 enforcing — block close
    process.stderr.write(
      `close_sprint: Step 2.7 failed: leftover worktree at ${leftoverWorktrees[0]}\n` +
      `  ${leftoverWorktrees.length === 1 ? '' : `(plus ${leftoverWorktrees.length - 1} more)\n  `}` +
      `Run \`git worktree remove ${leftoverWorktrees[0]}\` if abandoned, or merge the work in progress.\n` +
      `  All worktrees must be closed before sprint close.\n`
    );
    process.exit(1);
  } else {
    // v1 advisory — warn + continue
    process.stderr.write(
      `Step 2.7 warning: leftover worktree at ${leftoverWorktrees[0]} (advisory in v1).\n`
    );
  }
}
```

**Estimated LOC: ~45 lines (matches CR-022 §3 M1 line 128 estimate of "~40 LOC").**

**Pass/fail/skip behavior summary:**

| State | Worktree state | Stdout | Stderr | Exit code |
|---|---|---|---|---|
| v2 + no leftovers | clean | `Step 2.7 passed: no leftover worktrees.\n` | (none) | propagates from later steps |
| v2 + leftover | dirty | `Step 2.7: checking for leftover worktrees...\n` | 4-line block listing worktree + remove hint | **1** (process.exit(1)) |
| v1 + leftover | dirty | `Step 2.7: checking for leftover worktrees...\n` | 1-line warning | propagates from later steps |
| any + git unavailable | n/a | `Step 2.7 skipped: git worktree list unavailable (non-fatal).\n` | (none) | propagates from later steps |

**`isV2` reference:** `isV2` is defined at line 198 as `const isV2 = (state.schema_version || 1) >= 2;`. **CRITICAL:** the Step 2.5 block at lines 196-258 declares `isV2` inside a `const` at line 198 — it is in scope for the rest of `closeSprint()` because the entire function body shares one lexical scope. **Verification:** line 198 is at function-body level (not inside a nested block); Step 2.7 at line 316+ can read `isV2` directly without re-declaration. Developer must not re-declare it. (Architect note: if Developer re-declares, TS/lint will warn; if hidden by a block scope, the inner reference shadows. The §"Function shape" snippet above wraps Step 2.7 in a `{ ... }` block — the inner `let leftoverWorktrees` is locally-scoped, but `isV2` is read from outer scope.)

**Gotchas surfaced from code inspection:**
- **`REPO_ROOT` resolution under test harness.** close_sprint.mjs:68 derives `REPO_ROOT = path.resolve(__dirname, '..', '..')` from the script's location. Under the test harness (`runCloseSprint` at test_close_sprint_v21.test.ts:108), the script runs from the live `/Users/.../ClearGate/.cleargate/scripts/` so `REPO_ROOT` resolves to the live repo (a real git repo with worktrees). Tests that run during a SPRINT-19 worktree-active session would see the SPRINT-19 worktrees in `git worktree list` output. **Risk:** test scenarios that don't expect worktrees would see real ones. **Mitigation:** Scenario 2 (happy-path) MUST run with `CLEARGATE_TEST_SUPPRESS_WORKTREE_CHECK=1` env override OR mock the execSync. **Recommended:** the inline Step 2.7 block reads `process.env.CLEARGATE_SKIP_WORKTREE_CHECK === '1'` as an early return — same pattern as the existing `CLEARGATE_SPRINT_DIR` test seam at line 121-130. Add this env override AND document it in the JSDoc at the top of the file. New env var: `CLEARGATE_SKIP_WORKTREE_CHECK=1` skips Step 2.7 entirely (logs `Step 2.7 skipped: CLEARGATE_SKIP_WORKTREE_CHECK=1 set (test seam).`). Tests use this env var; production code never sets it.
- **`execSync` throws on non-zero exit, not on cwd-not-a-repo.** When `cwd` is a tmpdir (not a git repo), `git worktree list --porcelain` exits 128 with stderr `fatal: not a git repository...`. The `try/catch` in §"Function shape" catches that — sets `worktreeListAvailable = false` — Step 2.7 logs the skip message and proceeds. **However** `execSync` by default writes the child's stderr to the parent process's stderr; the test harness will see `fatal: not a git repository` noise on stderr even on the skip path. **Fix:** pass `stdio: ['ignore', 'pipe', 'ignore']` to `execSync` to suppress git's stderr. The existing Step 2.6 invocation at line 297 uses `stdio: 'inherit'` because reconcile-lifecycle is meant to print its own output; M4 must NOT inherit because `git worktree list` errors are noise. Use `{ cwd: REPO_ROOT, encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'] }`.
- **The `.worktrees/STORY-*` regex MUST cover both forward-slash AND backslash separators** for cross-platform support. `cleargate-cli/src/commands/sprint.ts:952` uses `/[/\\]\.worktrees[/\\]STORY-/`. M4 reuses verbatim. Tests on macOS/Linux pass with `/`; CI on Windows would need `\\`. (Architect note: SPRINT-19 dev infra is macOS — `darwin 25.4.0` per env. Tests on macOS exercise `/` only; Windows path is dormant but the regex protects against future Windows runs.)
- **`.worktrees/CR-*` paths exist in current sprint.** state.json at lines 21-24 + 33-36 shows `CR-022` and `CR-024` use `.worktrees/CR-022/` and `.worktrees/CR-024/` (not `.worktrees/STORY-*`). **The Step 2.7 regex matches `STORY-` only — CR-prefixed worktrees are NOT detected.** This is a **bug or a feature**:
  - **Reading 1 (feature):** CR-022 §3 M1 line 130 says: *"assert no path matches `.worktrees/STORY-*`"*. CR-022 §4.3 Gherkin "Scenario: Step 2.7 blocks on leftover worktree" uses `.worktrees/STORY-NNN-NN/`. The CR explicitly scopes to STORY- prefixed paths. So CR-/BUG-/EPIC- prefixed worktrees are out of scope.
  - **Reading 2 (bug):** SPRINT-19 itself uses `.worktrees/CR-022/`, `.worktrees/CR-024/`, `.worktrees/CR-025/`, `.worktrees/BUG-024/`. If those are still present at sprint close, Step 2.7 would PASS (no STORY- match) — but they're real leftover worktrees the human should remove. Closing SPRINT-19 with M4 in place would NOT block on its own dirty state.
  - **Decision:** Default to **Reading 1 (feature) — STORY- prefix only**, consistent with the CR text. **However, this is an open decision for the orchestrator** (see §"Open decisions for orchestrator" item 1 below). If the orchestrator escalates and the user wants broader matching, the regex changes to `/[/\\]\.worktrees[/\\](STORY|CR|BUG|EPIC)-/`. M4 default execution: STORY- only.
- **Pre-commit `npm run typecheck` will pass** because close_sprint.mjs is `.mjs` (unchecked by `tsc`); only the test file `.test.ts` is typechecked. The test file imports `spawnSync` from `node:child_process`, already used in the existing scenarios. No new TS surface needed.
- **`prebuild` regenerates MANIFEST.json SHA for `.cleargate/scripts/close_sprint.mjs`.** Per CR-022 §4.2 line 184: *"close_sprint.mjs (live + canonical) byte-identical post-edit"*. The two files are byte-identical at HEAD (verified). After M4's edit they remain byte-identical. The MANIFEST.json SHA at the close_sprint.mjs row is fingerprinted by `cleargate-cli/scripts/build-manifest.ts:36-60` — runs deterministically. Step 6 of the Order regenerates and commits.
- **FLASHCARD `2026-04-30 #worktree #npm`** says: *"worktrees share no node_modules with parent; run `npm ci --workspace <pkg>` before first typecheck/test in a new worktree."* Developer runs M4 in a worktree (presumably `.worktrees/CR-022/` already exists per state.json). First step: `cd .worktrees/CR-022 && npm ci --workspace cleargate-cli` before any test invocation. (Architect note: state.json at line 25 shows `CR-022` worktree already exists; Developer continues that worktree.)
- **FLASHCARD `2026-04-21 #worktree #vitest #cleanup`** says: *"`git worktree remove --force --force` does NOT kill detached vitest worker pools — they persist as orphan node processes pointing at deleted dirs. Run `pkill -9 -f \"node.*vitest\"` BEFORE removing worktrees that ran `npm test`."* Tangential to M4 (M4 doesn't remove worktrees), but the broader Wave 2 close-up may.
- **FLASHCARD `2026-05-02 #vitest #ram #parallel-agents`** says: *"vitest maxForks cap is PER-PROCESS — N parallel agents × maxForks = total forks (e.g. cap=2 + 3-agent wave = 6 forks ≈ 2.4GB)."* Wave 2 has 6 parallel Developer agents running. Orchestrator must lower `VITEST_MAX_FORKS=1` or serialize. (Architect note: this is orchestrator concern, not M4 implementation. Flagged here so orchestrator sees it before dispatching Wave 2.)

## §3 Mirror-parity callout

**FLASHCARD `2026-05-01 #mirror #parity` and `2026-04-19 #wiki #protocol #mirror` apply.**

**Per-edit invariant:** every M4 edit lands in BOTH locations or NEITHER. The Developer must, in a single working tree:

- EDIT `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs` (live) — insert Step 2.7 block per §"Function shape" between line 314 and line 316 (post-Step-2.6 catch + pre-Step-3 opener).
- EDIT `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (canonical mirror) — byte-identical insert.
- VERIFY: `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` must return empty.
- Run `npm --prefix cleargate-cli run prebuild` — regenerates `cleargate-planning/MANIFEST.json` SHA for the close_sprint.mjs row + propagates the mirror change into `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (cli-bundled derived copy, regenerated by `copy-planning-payload.mjs` per FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`).

**Pre-existing baseline (verified 2026-05-01):** `diff` between live and canonical returns **empty** at HEAD (commit fb4eb2d). Both files are 448 LOC. M4 has a clean baseline — no pre-divergence to reconcile (FLASHCARD `2026-05-01 #mirror #parity` warns against side-effect reconciliation; not applicable here because there's nothing to reconcile).

## §4 Test fixture path

**Use the existing suite — DO NOT create a new file.**

Target: `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/test_close_sprint_v21.test.ts`

Justification: the file already covers Step 2.x scenarios (Scenario 4 at line 252+ tests Step 2.5 missing sections; Scenarios 7-14 cover STORY-025-03 naming + Step 3.5 + Step 7). Adding Step 2.7 scenarios as a new `describe()` block at the bottom of the file matches the existing pattern. CR-022 §2 line 112 explicitly endorses: *"`cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` — extend with Steps 2.7, 2.8, 6.5, 6.6, 6.7, 8 scenarios."*

**New `describe()` block to append at the end of the file (after Scenario 14):**

```typescript
// ─────────────────────────────────────────────────────────────────────────────
// CR-022 M1: Step 2.7 worktree-closed pre-close check
// ─────────────────────────────────────────────────────────────────────────────

describe('Scenario 15: Step 2.7 skipped when git worktree list unavailable (tmpdir not a git repo)', () => {
  // happy-path equivalent under test harness — tmpdirs are not git repos,
  // so Step 2.7 always skips. Validates the skip message + pipeline progression.
  // ...
});

describe('Scenario 16: Step 2.7 fires under v2 when leftover worktree present (CLEARGATE_FORCE_WORKTREE env)', () => {
  // Test seam: CLEARGATE_FORCE_WORKTREE=<path> env var causes Step 2.7 to
  // pretend `git worktree list` returned that path. Used to assert the v2
  // block path without needing a real git worktree.
  // ALTERNATIVE: stub PATH to a fake `git` binary that prints worktree output.
  // Recommended: env-seam approach (cleaner, no PATH manipulation).
  // ...
});

describe('Scenario 17: Step 2.7 advisory under v1 (warn + continue)', () => {
  // Same env seam as Scenario 16 + a v1 fixture (sprint-v1-legacy already exists).
  // Asserts stderr warning + exit 0 + state.json sprint_status flips to Completed.
  // ...
});
```

**Test seam decision (open for orchestrator):**

Step 2.7 needs a way for tests to inject "as if leftover worktrees exist" without requiring a real `.worktrees/STORY-*` directory in a real git repo. Two options:

1. **`CLEARGATE_FORCE_WORKTREE_PATHS` env var** — comma-separated list of fake worktree paths that Step 2.7 reads instead of running `git worktree list`. Production code never sets it; tests set it for the failure-mode scenarios. Pattern matches the `CLEARGATE_SPRINT_DIR` test seam at line 121-130 of close_sprint.mjs.
2. **Stub `git` binary on PATH** — write a fake `git` script in tmpdir, prepend tmpdir to PATH, fake script prints porcelain worktree output. More realistic; more setup ceremony.

**Recommended: option 1 (env seam).** Cleaner, matches the existing `CLEARGATE_*` test-seam convention. Adds 2 lines to the Step 2.7 block. **Open decision item 2 (see §"Open decisions for orchestrator").**

**Existing fixtures reused:**
- `cleargate-cli/test/scripts/fixtures/sprint-v1-legacy/` — used as v1 baseline for Scenario 17.
- `makeTempSprintDir(fixtureName, sprintDirName)` at line 70-101 — reused unchanged.
- `runCloseSprint(sprintDir, extraArgs)` at line 108-135 — reused unchanged.

**No new fixture files required.** The env-seam approach uses string injection, not filesystem fixtures.

**Test count delta:** +3 scenarios (15, 16, 17) × ~3-5 `it()` blocks each = ~10-12 new test cases. Well above CR-022 §4.1 minimum (1 happy + 1 failure per new step = 2 minimum for Step 2.7 alone; M4 ships 3 categorized scenarios).

## §5 Wave 2 disjointness — hand-off for M5 Architect

**Sprint plan §2.2 row "M4, M5, M6, M11 → M4 → M5 → M6 → M11" mandates strict sequential merge order across all 4 close_sprint.mjs touches.**

**Post-M4 line layout (estimated; M5 Architect must re-grep post-merge):**

| Line | Content |
|---|---|
| 260 | `// ── Step 2.6: Lifecycle Reconciliation (CR-017) ──` |
| 314 | end of Step 2.6 catch block |
| **315** | blank line |
| **316** | `// ── Step 2.7: Worktree-Closed Check (CR-022 M1) ──` (NEW) |
| ~360 | end of Step 2.7 block (NEW) |
| **~361** | **blank line — INSERT POINT FOR M5's Step 2.8** |
| ~362 | `// ── Step 3: Invoke prefill_report.mjs ──` (was at line 316; shifted by ~46 lines) |

**M5 Architect: cite post-M4 line numbers at planning time.** Re-grep `close_sprint.mjs` after M4 lands. The Step 2.8 block inserts immediately after M4's Step 2.7 closer + before Step 3 opener. Same `isV2` advisory-vs-enforcing pattern. Same mirror-parity invariant. Same prebuild regen rule.

**M5 cross-reference for the new helper (lib/report-filename.mjs):** Step 2.8 stderr lists unmerged commits; if it embeds the report filename (e.g. *"Sprint not yet merged; review SPRINT-19_REPORT.md before retrying"*), it MUST call `reportFilename(sprintDir, sprintId, { forRead: true })` from `./lib/report-filename.mjs` (already imported at line 42) — never hard-code `SPRINT-NN_REPORT.md`. M4 itself does NOT consume this helper because Step 2.7 messages reference `.worktrees/STORY-*` paths only.

**M6 Architect (Steps 6.5/6.6/6.7) and M11 Architect (Step 8):** insert at lines far below Step 2.7 (Step 6 is at line 419; Step 8 inserts at end). M4's edit does NOT shift those line numbers significantly — but M5 will. M6's plan should cite line numbers post-M5. M11's plan should cite line numbers post-M6. Sequential cascade.

**Concrete rule for the M5 Architect:** when reading this M4 plan to draft your own, copy the §"Insert location" pattern (re-grep at HEAD; cite Step boundary opener + closer line numbers; cite surrounding stdout/stderr message patterns to mimic). The same disjointness analysis applies: M5's Step 2.8 inserts AFTER M4's Step 2.7 closer + BEFORE Step 3 opener. Cross-reference this plan's §"Insert location" + §"Function shape" sections for the established formatting conventions.

## Cross-story risks

Things a Developer working only on M4 (CR-022 M1) might miss:

1. **The four other close_sprint.mjs milestones (M5, M6, M11) lurk in Wave 2 + Wave 3.** A Developer reading only the M4 plan might think they own close_sprint.mjs — they don't. Sprint plan §2.3 first bullet explicitly warns: *"close_sprint.mjs is touched by 4 SPRINT-19 milestones (M4, M5, M6, M11). Sequential Wave 2 → Wave 3 ordering naturally serializes them, but each Architect milestone plan MUST cite the post-prior-milestone line numbers."* **Mitigation:** Developer commits to git WITHOUT touching any line outside the Step 2.7 insert range (lines 315-316 + ~46 lines of new content). No drive-by edits to Step 2.5, Step 2.6, Step 3, Step 5, Step 6, Step 7 messages — those are M5/M6/M11 territory. Stash discipline (FLASHCARD `2026-04-21 #worktree #vitest #cleanup` mid-sprint warning).
2. **The CR-/BUG-/EPIC-prefix question.** §"Gotchas" item 4 above flags this. Reading 1 (STORY- only) is the M4 default; if the user wants broader, the regex change is 1 line + 1 test scenario. Architect default is STORY- only (matches CR text); orchestrator escalates if needed.
3. **The skip-on-non-git-repo path is critical for ALL existing test fixtures.** Without the skip, the entire `test_close_sprint_v21.test.ts` suite fails on the first scenario that runs `runCloseSprint` against a tmpdir. The skip path is NOT just a fail-safe — it's a hard requirement for the test harness to function. Verify the skip message appears in stdout for every existing scenario after M4 lands; if a scenario doesn't see the skip message AND doesn't see the pass message, Step 2.7 didn't fire correctly. (Architect note: pre-existing scenarios make exit-code assertions only; many also assert on stdout/stderr substrings. Adding the new `Step 2.7 skipped: ...` line to stdout shouldn't break any existing assertion as long as the existing assertions use `toMatch`/`toContain` patterns rather than exact-match. Quick grep at line 273-352 of test_close_sprint_v21.test.ts shows all assertions use `toMatch` — safe.)
4. **`isV2` lexical scope.** §"Function shape" notes this — Developer must NOT re-declare `isV2` inside Step 2.7. The variable lives at function-body scope from line 198 onward. (Architect note: re-declaring with `const isV2 = ...` would TS-fail under strict mode but `.mjs` doesn't get TS-checked. The runtime would shadow correctly. Safer to read from outer scope.)
5. **`CLEARGATE_SKIP_WORKTREE_CHECK` test seam not yet documented in close_sprint.mjs JSDoc.** The §"Function shape" snippet introduces a new env var. Developer must update the JSDoc at the top of close_sprint.mjs (lines 1-34) to document the new env var alongside `CLEARGATE_SPRINT_DIR` + `CLEARGATE_STATE_FILE`. Otherwise the test seam is undocumented surface — discoverability hit.
6. **`stdio: ['ignore', 'pipe', 'ignore']` matters.** Without it, git's `fatal: not a git repository` stderr leaks through to the test harness's stderr capture. The M4 scenario assertions use `toMatch(/Step 2\.7/)` — git's leak doesn't match, but it pollutes stderr output for human inspection. Use the suppressed stdio config.

## Open decisions for orchestrator

1. **STORY- prefix only, or broader (CR-/BUG-/EPIC-)?** §"Gotchas" item 4 above. CR-022 §3 M1 line 130 + §4.3 Gherkin both say STORY- only. SPRINT-19 itself uses `.worktrees/CR-022/`, `.worktrees/CR-024/`, `.worktrees/CR-025/`, `.worktrees/BUG-024/` — STORY- only would let those slip past Step 2.7. **M4 default execution: STORY- only** (matches CR text literally). **Orchestrator: confirm STORY- only is acceptable, or escalate to user if you want broader matching.** Mitigation if escalated: regex change 1 line + 1 test scenario. **Recommended: keep STORY- only for M4; file a follow-up CR if broader matching is desired (sprint plan §2.2 already constrains M4 scope; broadening here is feature creep).** Note: the existing `cleargate sprint preflight` at sprint.ts:952 also uses STORY- only — broadening Step 2.7 would create cross-CLI semantics drift unless preflight is updated in lockstep.

2. **Test seam choice — env var vs. PATH stub?** §"Test fixture path > Test seam decision" above. Two options:
   - **Option A (recommended):** `CLEARGATE_FORCE_WORKTREE_PATHS=path1,path2` env var. Adds 2 lines to Step 2.7 block. Same convention as existing test seams.
   - **Option B:** stub fake `git` binary on PATH. More realistic but heavier.
   **M4 default execution: Option A.** **Orchestrator: confirm or kick back.**

3. **Document the `CLEARGATE_SKIP_WORKTREE_CHECK` env var in JSDoc?** §"Cross-story risks" item 5 above. The new test seam introduces undocumented surface. **M4 default: yes — Developer adds a 2-line entry to the JSDoc at lines 1-34 documenting both `CLEARGATE_SKIP_WORKTREE_CHECK=1` AND `CLEARGATE_FORCE_WORKTREE_PATHS=p1,p2` if the orchestrator confirms decision 2 = Option A.** **Orchestrator: confirm.**

4. **Commit-message scope: `feat(CR-022):` vs. `feat(SPRINT-19):` vs. `feat(EPIC-???):`?** FLASHCARD `2026-05-01 #commit-format #dod` warns Developers default to `feat(<sprint>):` even when DoD §4.2 specifies `feat(<epic>):`. CR-022 has no parent EPIC (`parent_cleargate_id: null`). CR-022 §4.2 line 183 says: *"Each commit format: `feat(EPIC-???-or-CR-022): <story-id> <short desc>`"*. **M4 default: `feat(CR-022): STORY-022-M1 — Step 2.7 worktree-closed pre-close check`.** **Orchestrator: confirm the `<story-id>` slot uses `STORY-022-M1` (the CR's own milestone label) — there is no ClearGate STORY-NNN-NN id assigned to M4.** Alternative if M-labels feel awkward: `feat(CR-022): M1 — Step 2.7 worktree-closed pre-close check`.

## Definition of Done (M4-specific echo of CR-022 §4.2)

- [ ] `close_sprint.mjs` (live + canonical) byte-identical post-edit (`diff` returns empty).
- [ ] Step 2.7 block lives between Step 2.6 closer (line 314 at HEAD) and Step 3 opener (line 316 at HEAD); ~45 LOC inserted.
- [ ] `cleargate-planning/MANIFEST.json` SHA for `.cleargate/scripts/close_sprint.mjs` regenerated in same commit (FLASHCARD `2026-05-01 #manifest #prebuild`).
- [ ] `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (cli-bundled derived) propagated by prebuild — DO NOT hand-edit (FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`).
- [ ] `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` extended with 3 new `describe()` blocks (Scenarios 15-17); ~10-12 new `it()` test cases.
- [ ] All existing scenarios in `test_close_sprint_v21.test.ts` still pass (the new `Step 2.7 skipped: ...` stdout line does not break any `toMatch`/`toContain` assertion).
- [ ] CR-022 §4.3 Gherkin "Scenario: Step 2.7 blocks on leftover worktree" passes via Scenario 16 of the test file.
- [ ] `npm --prefix cleargate-cli run typecheck` green.
- [ ] `npm --prefix cleargate-cli test` green (full suite).
- [ ] JSDoc at top of close_sprint.mjs (lines 1-34) updated to document `CLEARGATE_SKIP_WORKTREE_CHECK` + `CLEARGATE_FORCE_WORKTREE_PATHS` test seams (per Open Decision 3).
- [ ] Single commit per CR-022 §4.2 — message format `feat(CR-022): STORY-022-M1 — Step 2.7 worktree-closed pre-close check` (per Open Decision 4).
- [ ] No edits to: M5/M6/M11 territory in close_sprint.mjs (Steps 2.8, 6.5, 6.6, 6.7, 8). No edits to: `prep_reporter_context.mjs`, `suggest_improvements.mjs`, `prefill_report.mjs`, `cleargate-cli/src/commands/sprint.ts`, CLAUDE.md, protocol/enforcement docs, agent definitions. Wave 2 disjointness contract.

---

role: architect

### M5.md

# Milestone: M5 — CR-022 M2 Step 2.8 sprint-merged-to-main verify
## Stories: CR-022 M2 (Step 2.8 verify-only sprint-branch merge gate)
## Wave: W2 (parallel-eligible per sprint plan §1; serialized in practice with M4/M6/M11 due to shared `close_sprint.mjs` surface — see "Cross-story risks")

> role: architect

## Order

**Strict merge order (sprint plan §2.2 + §2.3 first bullet):** M4 → **M5** → M6 → M11 for `close_sprint.mjs`.

- **Pre-condition for Developer dispatch:** M4 (Step 2.7 worktree-closed check) MUST be merged to `main` before M5 Developer is dispatched. Reason: M4 inserts a new `// ── Step 2.7: ──` block AFTER existing Step 2.6 (currently `close_sprint.mjs:260-314` on main as of HEAD). M5 inserts Step 2.8 immediately AFTER M4's Step 2.7 closer. If M5 dispatches before M4 merges, the Developer's line-number anchors drift and they may insert at the wrong location.
- **Orchestrator action:** dispatch M4 dev → await commit + merge → re-grep close_sprint.mjs to confirm Step 2.7 markers are present on main → dispatch M5 dev. Confirmed in user-supplied prompt: "orchestrator dispatches M4 dev first, awaits merge, THEN dispatches M5 dev."
- **Developer-side re-verification:** the Developer for M5 MUST re-grep `close_sprint.mjs` line numbers at dispatch time (instructions in "Per-story blueprint → Insertion strategy" below). This plan cites HEAD-as-of-2026-05-01 line numbers as a baseline; the Developer treats those as advisory and uses fixture-text anchors as authoritative.

Parallelizable with: M7 (`prep_reporter_context.mjs` + sprint.ts archive flag — disjoint files); M8 (CR-024 QA context pack); M9 (CR-025 Initiative rename). NOT parallelizable with: M4, M6, M11 (all four touch `close_sprint.mjs`).

## Per-story blueprint

### CR-022 M2 — Step 2.8 sprint-merged-to-main verify

#### Cross-story coupling

- **M4 (Step 2.7 worktree-closed) lands first.** M5 inserts immediately AFTER M4's Step 2.7 block, BEFORE the existing `// ── Step 3: Invoke prefill_report.mjs ────` block (currently at `.cleargate/scripts/close_sprint.mjs:316`).
- **M6 (Steps 6.5/6.6/6.7) lands AFTER M5.** M6 Architect re-greps `close_sprint.mjs` after M5 lands. M5 does NOT need to coordinate with M6 because their inserts are in disjoint regions (M5 → pre-Step-3; M6 → post-Step-6).
- **M11 (Step 8 verbose handoff) lands LAST.** Disjoint region (post-Step-7).
- No cross-coupling with M7 / M8 / M9 (different files).

#### Insertion strategy (line numbers + fixture-text anchors)

**Anchor strategy — fixture-text first, line numbers second.** The Developer locates the insertion point by anchor text, not by line number. Line numbers are advisory only.

**Step 1 — Re-grep at dispatch time (mandatory):**

```
grep -n "// ── Step 2\.[0-9]" .cleargate/scripts/close_sprint.mjs
grep -n "// ── Step 3:" .cleargate/scripts/close_sprint.mjs
```

After M4 merges, the expected output includes a `// ── Step 2.7:` opener that does NOT exist on main today. If grep returns no Step 2.7 marker, M4 has NOT merged and the Developer MUST halt and report blocker.

**Step 2 — Locate the insert anchor:**

The insertion point is **immediately after M4's Step 2.7 closer block, immediately before the existing Step 3 opener** which on main today reads:

```javascript
  // ── Step 3: Invoke prefill_report.mjs ─────────────────────────────────────
  process.stdout.write('Step 3: running prefill_report.mjs...\n');
```

(Currently `close_sprint.mjs:316-317`. Will be displaced downward by ~30-40 lines after M4's Step 2.7 inserts.)

**Anchor regex for the Developer:**

```
/^  \/\/ ── Step 3: Invoke prefill_report\.mjs/
```

The Developer inserts the new Step 2.8 block immediately ABOVE this anchor line, after M4's Step 2.7 closer (which terminates with its own `process.stdout.write('Step 2.7 passed: ...')` or equivalent — exact closer text owned by M4 plan).

**Note on M4 closer convention:** Since M4's plan is being written in parallel with this one, the M5 Developer cannot rely on M4's exact closer text. Therefore the Step-3 opener anchor (which is stable on main and will not be touched by M4) is the authoritative landmark. Insert ABOVE the Step-3 opener; Step 2.7 is implicitly the prior block.

#### Function signature + git command

**Decision: inline implementation, no extracted helper.** Step 2.7 (M4) likely follows the same inline pattern as existing Steps 2.5 / 2.6 / 3.5 (all inline `try/catch` blocks, no helper extraction). M5 mirrors that convention. Helper extraction is a refactor not in scope for this 40-LOC story.

**Inline block shape:**

```javascript
  // ── Step 2.8: Sprint branch merged to main (verify-only, NO auto-merge) ──
  // Charter §1.8 line 108-111: verify-only — script asserts merge ancestry,
  // does NOT run the merge. On miss: list unmerged commits + exit 1.
  // v2 enforcing; v1 advisory (warn + continue) — gate on execution_mode.
  // Skip when sprintId has no numeric portion (e.g. SPRINT-TEST fixture).
  const sprintNumMatch = /^SPRINT-(\d{2,3})$/.exec(sprintId);
  if (sprintNumMatch) {
    const sprintBranch = `refs/heads/sprint/S-${sprintNumMatch[1]}`;
    const mainBranch = 'refs/heads/main';
    process.stdout.write(`Step 2.8: verifying ${sprintBranch} merged to ${mainBranch}...\n`);
    try {
      execSync(
        `git merge-base --is-ancestor ${sprintBranch} ${mainBranch}`,
        { stdio: 'pipe', cwd: REPO_ROOT, env: process.env }
      );
      process.stdout.write(`Step 2.8 passed: ${sprintBranch} is merged to ${mainBranch}.\n`);
    } catch (mergeErr) {
      // Distinguish "not an ancestor" (exit 1) from other failures.
      // git merge-base --is-ancestor exits 1 when not-ancestor, 128 when refs missing.
      const exitStatus = /** @type {any} */ (mergeErr).status;
      if (exitStatus === 1) {
        // Not merged — list the unmerged commits for the human.
        let unmergedLog = '';
        try {
          unmergedLog = execSync(
            `git log ${mainBranch}..${sprintBranch} --oneline`,
            { encoding: 'utf8', cwd: REPO_ROOT, env: process.env }
          );
        } catch { /* unmerged-log fetch failed — proceed without */ }
        process.stderr.write(
          `Step 2.8 failed: sprint/S-${sprintNumMatch[1]} not merged to main.\n` +
          `  Unmerged commits:\n${unmergedLog || '  (unable to enumerate)\n'}` +
          `  Resolve: merge sprint/S-${sprintNumMatch[1]} → main, then re-run close_sprint.mjs.\n`
        );
        process.exit(1);
      }
      // Refs missing or other git failure — fail-open with warning (test harness, fixtures, fresh clones).
      process.stderr.write(
        `Step 2.8 warning: git merge-base check unavailable (${/** @type {Error} */ (mergeErr).message}). ` +
        `Skipping merge verification.\n`
      );
    }
  } else {
    process.stdout.write(`Step 2.8 skipped: sprint-id "${sprintId}" has no numeric portion.\n`);
  }

```

**Git commands invoked:**

1. `git merge-base --is-ancestor refs/heads/sprint/S-<NN> refs/heads/main` — exit 0 = ancestor (pass), exit 1 = not ancestor (fail), exit 128 = refs missing (fail-open warn).
2. `git log refs/heads/main..refs/heads/sprint/S-<NN> --oneline` — list unmerged commits on failure path. Wrapped in inner try/catch: a failure here does NOT crash the step; the script exits 1 anyway with `(unable to enumerate)` placeholder.

**`cwd: REPO_ROOT`** is critical. `close_sprint.mjs` is invoked from arbitrary working dirs by orchestrators; git commands must run from the repo root. `REPO_ROOT` is already defined at `close_sprint.mjs:68`.

**Why fail-open on exit 128:** Test fixtures spawn `close_sprint.mjs` against `CLEARGATE_SPRINT_DIR=/tmp/...` paths where neither `main` nor `sprint/S-<NN>` refs exist. Without fail-open, every test would have to seed a real git repo with both branches. Fail-open with stderr warning preserves intent (verify when refs exist; pass when refs are missing, since you can't verify what isn't there).

**Why skip on non-numeric sprintId:** The existing Step 2.5 naming-convention check (`close_sprint.mjs:206`) blocks v2.1 fast-lane sprints with non-`SPRINT-NN` names, but earlier path the script handles `SPRINT-TEST` fixtures (Scenario 14, `test_close_sprint_v21.test.ts:686`). M5 must not break those fixtures.

#### Failure mode (stdout/stderr text + exit code)

**Pass path (sprint branch merged):**

- stdout: `Step 2.8: verifying refs/heads/sprint/S-19 merged to refs/heads/main...\n`
- stdout: `Step 2.8 passed: refs/heads/sprint/S-19 is merged to refs/heads/main.\n`
- exit: 0 (script continues to Step 3)

**Fail path (sprint branch unmerged, exit status 1 from git):**

- stderr: `Step 2.8 failed: sprint/S-19 not merged to main.\n  Unmerged commits:\n  abc1234 feat(CR-022): STORY-022-04 ...\n  def5678 chore(SPRINT-19): ...\n  Resolve: merge sprint/S-19 → main, then re-run close_sprint.mjs.\n`
- exit: 1 (script halts; `sprint_status` is NOT flipped)

**Fail-open path (refs missing, exit status 128 from git or other error):**

- stderr: `Step 2.8 warning: git merge-base check unavailable (...). Skipping merge verification.\n`
- exit: continues to Step 3 (no sprint-state mutation; warning logged)

**Skip path (non-numeric sprintId):**

- stdout: `Step 2.8 skipped: sprint-id "SPRINT-TEST" has no numeric portion.\n`
- exit: continues to Step 3

**Exit-code propagation:** `process.exit(1)` halts the script before Step 5 flips `sprint_status`. State.json is NOT mutated on failure, matching the existing pattern of Steps 2 / 2.5 / 2.6.

#### Test scenarios (from Gherkin §4.3)

**File:** `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` — extend existing suite. Pattern matches sibling scenarios (Steps 2.5, 2.6, 3.5, 7) which all live in this file. Match Architect's existing test layout choice — do NOT create a new file.

**Required scenarios (DoD §4.1 minimum: at least one happy-path + one failure-mode for Step 2.8):**

1. **Step 2.8 passes when sprint branch is merged to main (happy path).**
   - Setup: temp git repo with `main` containing `sprint/S-19` ancestor. Use the existing `makeTempSprintDir` helper for state.json + REPORT.md, then `cd` into a separately-created temp git repo as `REPO_ROOT` via `CLEARGATE_REPO_ROOT` env (NEW — see "Test fixture setup notes" below) OR mock the git invocation by stubbing `execSync` with a fixture (NOT preferred — match the live-git pattern from Step 2.6 fixtures).
   - Recommended: spawn `close_sprint.mjs` with `cwd:` pointing to a real init'd git repo containing `main` + `sprint/S-19` branches with `sprint/S-19` ancestor of `main`. `git init`, `git commit -m "base" --allow-empty`, `git branch sprint/S-19`, `git commit -m "main work" --allow-empty`, `git merge sprint/S-19`. Verifies `merge-base --is-ancestor` returns 0.
   - Assert: stdout contains `Step 2.8 passed`; exit 0; `sprint_status` flips to `Completed`.

2. **Step 2.8 blocks when sprint branch is unmerged (failure mode — Gherkin Scenario "Step 2.8 blocks when sprint branch unmerged").**
   - Setup: real git repo as above, but DO NOT merge `sprint/S-19` into `main`. Add a divergent commit to `sprint/S-19` post-branch.
   - Assert: exit code 1; stderr contains `Step 2.8 failed: sprint/S-19 not merged to main`; stderr contains the divergent commit's oneline (or `(unable to enumerate)` if oneline retrieval failed); `sprint_status` stays `Active` (NOT flipped).

3. **Step 2.8 fail-open when git refs missing (regression guard).**
   - Setup: temp dir with NO git repo (or git repo without `sprint/S-19` ref). Use existing `makeTempSprintDir` pattern (which does not init git).
   - Assert: exit code 0 (or pass-through to Step 3); stderr contains `Step 2.8 warning: git merge-base check unavailable`; `sprint_status` flips to `Completed`. This guards the existing test suite from breaking — the v2.1 / lifecycle / naming / Step 7 scenarios already use a no-git temp dir.

4. **Step 2.8 skipped for non-numeric sprintId (regression guard for Scenario 14).**
   - Setup: existing `SPRINT-TEST` fixture (Scenario 14, `test_close_sprint_v21.test.ts:686`).
   - Assert: stdout contains `Step 2.8 skipped`; exit 0; `sprint_status` flips to `Completed`. Verifies the existing Scenario 14 still passes.

**Test fixture setup notes:**

- `cwd: REPO_ROOT` in the production code resolves to `path.resolve(__dirname, '..', '..')` from `close_sprint.mjs`. In tests, the script is spawned via `spawnSync('/usr/bin/env', ['node', CLOSE_SPRINT_SCRIPT, ...])`. The script's `REPO_ROOT` constant (line 68) resolves to the **real repo root**, not the temp test dir. This means in test 3 (fail-open on missing refs), the git invocation runs against the actual repo. If the actual repo has `sprint/S-19` and it IS merged, test 3 will incorrectly pass via the happy path instead of via fail-open.
- **Mitigation:** test 1 + test 2 require a temp git repo with controlled `main` + `sprint/S-19` state. The test must override `REPO_ROOT` for the spawned subprocess. Easiest path: introduce a new env var `CLEARGATE_REPO_ROOT` read by `close_sprint.mjs` (with fallback to the existing computed `REPO_ROOT`). Mirror the existing `CLEARGATE_SPRINT_DIR` + `CLEARGATE_STATE_FILE` env-var override pattern (already at `close_sprint.mjs:135-137` + `:144-146`).
- **Production code change to support testability:** add `const REPO_ROOT = process.env.CLEARGATE_REPO_ROOT ? path.resolve(process.env.CLEARGATE_REPO_ROOT) : path.resolve(__dirname, '..', '..');` (replaces line 68). This is a 1-line change with no behavior impact in non-test environments. Same pattern used elsewhere in the file. Counts toward the 40-LOC budget.

**Test count:** 4 new scenarios for Step 2.8 in `test_close_sprint_v21.test.ts`. CR-022 §4.1 minimum table specifies "Steps 2.7, 2.8, 6.5, 6.6, 6.7, 8 — at least one happy-path + one failure-mode test per new step" — M5 covers Step 2.8 with 2 required + 2 regression-guard scenarios.

#### Reuse (no duplication)

- `execSync` from `node:child_process` — already imported (`close_sprint.mjs:39`). Reuse, do not re-import.
- `REPO_ROOT` constant — already defined (`close_sprint.mjs:68`). Reuse; modify the line per "Production code change to support testability" above.
- `process.exit(1)` halt pattern — match the existing Step 2 / 2.5 / 2.6 convention (`close_sprint.mjs:191, 212, 240, 254, 306`).
- `try / catch (err)` + `process.stderr.write` pattern — match Step 2.6 (`close_sprint.mjs:296-307`) and Step 3.5 (`close_sprint.mjs:328-340`). Both inline; both fail-open with stderr warning when CLI/script-not-available; one fails closed with exit 1 when drift found.
- `path.basename(sprintDir)` for sprint-id extraction — already used at `close_sprint.mjs:205`. NOT needed here (we use `sprintId` directly).
- Test harness: existing `makeTempSprintDir`, `runCloseSprint`, fixture dirs (`cleargate-cli/test/scripts/test_close_sprint_v21.test.ts:70-101, 108-135`). Reuse without modification. Add a new helper `makeTempGitRepo(merged: boolean)` for tests 1 + 2 — sets up git repo with `main` + `sprint/S-NN` branches in known merge state. Place near top of file as a new local helper.

#### Gotchas surfaced from code inspection

- **`close_sprint.mjs:68` `REPO_ROOT` is a CONSTANT, not env-overridable.** Existing pattern at `:135-137` (`CLEARGATE_SPRINT_DIR`) + `:144-146` (`CLEARGATE_STATE_FILE`) shows the file already supports env-var overrides for two paths. Adding `CLEARGATE_REPO_ROOT` matches the convention.
- **`close_sprint.mjs:206` `^SPRINT-\d{2,3}$` regex** is the v2.1 naming-convention gate. M5 must use a more permissive regex (`^SPRINT-(\d{2,3})$` to capture the number) because Step 2.8 runs BEFORE Step 2.5 in the pipeline order (between 2.7 and 3). For non-`SPRINT-NN` sprintIds (test fixtures, `SPRINT-TEST`), Step 2.8 must skip cleanly without erroring.
- **Step 2.5 fast-lane gate** (`close_sprint.mjs:198-202`) only runs when `isV2 && hasFastLane`. Step 2.8 has NO such gate — it runs for every sprint (CR-022 §1 line 84-85 table column "Block in v2? — yes").
- **`git merge-base --is-ancestor` exit-code semantics:** exit 0 = ancestor, exit 1 = NOT ancestor, exit 128 = refs missing or other error. The Node `execSync` throws on any non-zero exit; the catch block must inspect `err.status` to distinguish. `err.status === 1` is the deterministic "unmerged" case; everything else is "git unavailable / refs missing" → fail-open warn.
- **Mirror parity (FLASHCARD #mirror, 2026-04-19 + 2026-05-01):** `.cleargate/scripts/close_sprint.mjs` and `cleargate-planning/.cleargate/scripts/close_sprint.mjs` are byte-identical on `main` (verified `diff` clean as of plan-write time). Edits MUST land in BOTH files in the SAME commit. The CLI `prebuild` script (`copy-planning-payload.mjs`) `rmSync+rebuilds cleargate-cli/templates/cleargate-planning/` from the canonical mirror — the cli-bundled tree is DERIVED, not source-of-truth. Do NOT hand-edit the cli-bundled mirror.
- **Edit-parity is per-edit, not whole-file (FLASHCARD #mirror #parity, 2026-05-01):** if the live file has divergence pre-existing M5 dispatch (e.g. M4 has merged into one but not the other — should not happen if orchestrator follows the merge-discipline, but verify with `diff` first), the M5 Developer must NOT reconcile the pre-existing divergence as a side effect. Add Step 2.8 in both files; leave any other divergence untouched and surface it as a blocker.
- **Commit-format gotcha (FLASHCARD #commit-format #dod, 2026-05-01):** DoD §4.2 specifies `feat(<epic>):` but Developers default to `feat(<sprint>):`. Per CR-022 §4.2: `feat(EPIC-???-or-CR-022): <story-id> <short desc>`. The Developer for M5 should use `feat(CR-022): STORY-022-XX Step 2.8 sprint-merged-to-main verify` (or whatever STORY ID the orchestrator assigns to M5 — the CR ships as 6 stories per §3 of CR-022).

## Cross-story risks

1. **Two-merge-order risk (M4 → M5).** If M5 Developer dispatches BEFORE M4 merges to main:
   - Symptom: `grep -n "// ── Step 2\.7" .cleargate/scripts/close_sprint.mjs` returns nothing.
   - Outcome: Developer cannot find the M4 anchor; either inserts at the wrong location (after Step 2.6, displacing what M4 will need) or halts and reports blocker.
   - **Mitigation (orchestrator):** dispatch M4 dev first; await M4 commit; verify M4 merge to `main`; THEN dispatch M5 dev. This plan formally requires this order.
   - **Mitigation (developer):** the Developer's first action is the re-grep above. If Step 2.7 marker absent, halt + blocker.

2. **Three-way merge surface on `close_sprint.mjs` (M5 + M6 + M11).** M5 is the second of four inserts. M6's plan is written AFTER M5 lands (per sprint plan §2.3 first bullet). M11's plan is written AFTER M6 lands. M5 does not need to coordinate ahead — but the M5 Developer's commit must NOT touch any region M6/M11 will need (Steps 6.5/6.6/6.7 region post-Step-6, Step 8 region post-Step-7). M5 only inserts between Step 2.7 closer and Step 3 opener. Disjoint.

3. **Mirror parity drift between live and canonical.** M5 must edit BOTH `.cleargate/scripts/close_sprint.mjs` AND `cleargate-planning/.cleargate/scripts/close_sprint.mjs` in the same commit. Pre-commit doctor / scaffold-lint may catch divergence. Do NOT touch the cli-bundled tree (`cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs`) — it rebuilds via `npm run prebuild`.

4. **Test-fixture cross-pollution with Step 2.6 reconcile tests.** `close-sprint-reconcile.test.ts` uses `makeTempSprintDir` from a different module-level helper. Both files spawn `close_sprint.mjs` against the live script. If M5's new Step 2.8 introduces a fail-closed path that fires in `close-sprint-reconcile.test.ts`'s SPRINT-99 / SPRINT-98 fixtures (which use real git ancestry to the live repo's `main`), those tests may break. **Mitigation:** Step 2.8's fail-open-on-128 path covers this — `close-sprint-reconcile.test.ts` fixtures use `SPRINT-99` / `SPRINT-98`, which match `^SPRINT-\d{2,3}$`, so Step 2.8 will RUN. The `git merge-base --is-ancestor refs/heads/sprint/S-99 refs/heads/main` will fail with status 128 (no `sprint/S-99` ref exists in the real repo) → fail-open warn → Step 2.8 returns control to the script → reconcile tests continue to pass.
   - **Verify in M5 dev:** run `npm test -- close-sprint-reconcile` after the M5 edit and confirm green.

5. **Hand-off to M6:** M6 plans Steps 6.5 / 6.6 / 6.7 (post-close additions, all non-fatal). M6 Architect MUST re-grep `close_sprint.mjs` after M5 lands to find the post-Step-6 insertion point (currently `close_sprint.mjs:417` — the closing brace of the Step 6 try/catch + the start of Step 7 at `:419`). M5's insert does NOT displace this region — Step 6.5/6.6/6.7 land in a different file region. M6 is in Sub-wave 2B per orchestrator dispatch order; dispatched after Sub-wave 2A (M4 + M5) completes.

## Open decisions for orchestrator

1. **Story ID for M5.** CR-022 §3 enumerates milestones M0..M6 but does not assign story IDs. The sprint plan §1 table calls this "CR-022 M2" (M2 = milestone-2 within CR-022, not Sprint Milestone 2). Orchestrator assigns the story ID when dispatching the Developer (e.g. `STORY-022-04` if that's the next free under EPIC-022, or `CR-022-02` if CR-itself-as-parent). Pin the ID before dispatch so the commit-format gotcha is resolvable.

2. **`CLEARGATE_REPO_ROOT` env-var introduction.** This plan recommends adding a 1-line env-var override at `close_sprint.mjs:68` to support testability of the git-merge-base check. This adds a third env-var to the script's surface (alongside `CLEARGATE_SPRINT_DIR` + `CLEARGATE_STATE_FILE`). If the orchestrator prefers a no-new-env-var path, the alternative is to mock `execSync` in the test file (less faithful to live behavior, but no production code change). Decision needed before Developer dispatch. **Architect recommendation: add the env var.** Matches the existing override pattern; minimal surface area.

3. **v1 advisory vs v2 enforcing branch.** CR-022 §1 line 78 says "block on failure (v2 enforcing, v1 advisory per execution_mode)". This plan implements pure fail-closed (exit 1) on the unmerged path. Should M5 read SPRINT-19's `execution_mode` and warn-instead-of-block when `v1`? **Architect recommendation: NO — implement enforcing only for now.** Reason: SPRINT-19 itself runs `execution_mode: v2`, so the v1-advisory path has no current consumer. M4 likely makes the same choice (read M4's plan when written). If a v1 sprint surfaces later, file a follow-up CR. Confirm with orchestrator at dispatch time.

---

role: architect

### M6.md

# Milestone: M6 — CR-022 M3 Steps 6.5 + 6.6 + 6.7 post-close additions
## Stories: CR-022 M3 (Steps 6.5 sprint_trends.mjs stub + 6.6 Skill Candidate detection + 6.7 FLASHCARD cleanup pass)
## Wave: W2 (parallel-eligible per sprint plan §1.2; **serialized in practice** with M4/M5/M11 due to shared `close_sprint.mjs` surface — see "Cross-story risks")

> role: architect

## Order

**Strict merge order (sprint plan §2.2 row 95):** M4 → M5 → **M6** → M11 for `close_sprint.mjs`.

- **Pre-conditions verified at plan-write time (2026-05-01):**
  - HEAD = `ee21863 feat(CR-022): CR-022-M2 Step 2.8 sprint-merged-to-main verify` — M5 (Step 2.8) is on `main`.
  - `cec4135 merge(SPRINT-19): M4 CR-022 M1 Step 2.7 worktree-closed check` — M4 is on `main`.
  - `9603f05 merge(SPRINT-19): M1 CR-022 reportFilename helper` — M1 helper at `.cleargate/scripts/lib/report-filename.mjs` is on `main`.
  - Therefore the M6 Developer can be dispatched against current `main` immediately. Step 2.7 (lines 341-407) and Step 2.8 (lines 410-489) are present; Step 6 boundary is **stable** at lines 581-592.
- **M11 (Step 8) lands AFTER M6.** M6 inserts a new `// ── Step 6.5: ──`, `// ── Step 6.6: ──`, and `// ── Step 6.7: ──` block sequence between Step 6 (line 592 closer) and the existing Step 7 opener (line 594). M11 inserts a Step 8 stdout block after Step 7. Disjoint regions, but M11 Architect re-greps after M6 lands.
- **No cross-coupling with M7 / M8 / M9 / M10 / M12** (different files).

Parallelizable per sprint plan §1.2 with M7, M8, M9 (Wave 2 over disjoint surfaces). **NOT parallelizable with M4/M5/M11** (all touch `close_sprint.mjs`) — but M4 and M5 have already merged at HEAD `ee21863`, so M6 is unblocked.

## Per-story blueprint

### CR-022 M3 — Steps 6.5 / 6.6 / 6.7 post-close additions

#### Cross-story coupling

- **M1 (`lib/report-filename.mjs`) lands first — already merged.** M6's new sections in `suggest_improvements.mjs` re-use the existing `import { reportFilename } from './lib/report-filename.mjs'` (`.cleargate/scripts/suggest_improvements.mjs:28`). No new helper imports required for the consume path. The stub `sprint_trends.mjs` does NOT need `reportFilename` itself in the M6 ship (it does not read REPORT.md); leave the import out to keep the stub at ~30 LOC.
- **M4 / M5 (Steps 2.7 / 2.8) land first — already merged.** M6's inserts in `close_sprint.mjs` are POST-Step-6 (line 594 region), disjoint from the Step 2.7/2.8 region (lines 341-489).
- **M11 (Step 8) lands AFTER M6.** M6's last write into `close_sprint.mjs` is the Step 6.7 closer (immediately before existing Step 7 opener at line 594). M11 inserts AFTER Step 7 closer (line 615). Disjoint.
- **DOES NOT touch developer.md / qa.md / architect.md / reporter.md.** Sprint plan §2.5 soft flag and §2.2 row "M9 only" / "M8 only" confirm: agent definitions are M8 (developer.md/qa.md) and M9 (architect.md/reporter.md) territory. M6 must NOT edit any file under `.claude/agents/` or `cleargate-planning/.claude/agents/`. **Wave 2 disjointness depends on this.**

#### Insertion strategy (line numbers + fixture-text anchors)

**Anchor strategy — fixture-text first, line numbers second.** The Developer locates insertion points by anchor text. Line numbers below are advisory (HEAD `ee21863`).

**Step 1 — Re-grep at dispatch time (mandatory):**

```bash
grep -n "// ── Step [67]" .cleargate/scripts/close_sprint.mjs
```

Expected output on current `main` (HEAD `ee21863`):

```
581:  // ── Step 6: Invoke suggest_improvements.mjs unconditionally ───────────────
594:  // ── Step 7: Auto-push per-artifact status updates to MCP ─────────────────
```

If `Step 6.5` / `Step 6.6` / `Step 6.7` markers are already present, M6 has already shipped — Developer halts and reports the duplicate.

**Step 2 — Locate insert anchors in `close_sprint.mjs`:**

The new Steps 6.5, 6.6, 6.7 land in **one contiguous block** between the Step 6 closer (line 592, the `}` that closes the `try { invokeScript('suggest_improvements.mjs', ...) } catch { ... }` body) and the Step 7 opener at line 594.

Verbatim Step 7 opener anchor (stable as of `ee21863`):

```javascript
  // ── Step 7: Auto-push per-artifact status updates to MCP ─────────────────
  // Runs after Gate 4 ack succeeds. Non-fatal: sprint stays Completed on failure.
  process.stdout.write('Step 7: pushing per-artifact status updates to MCP...\n');
```

**Anchor regex for the Developer:**

```
/^  \/\/ ── Step 7: Auto-push per-artifact status updates to MCP/
```

The Developer inserts the Step 6.5 + 6.6 + 6.7 block immediately ABOVE this anchor, with one blank line separating Step 6's closing brace from the Step 6.5 opener.

**Step 3 — Insert order within the block:**

```
Step 6 closer (existing)
                  ↓
[NEW] Step 6.5: invoke sprint_trends.mjs (stub) — non-fatal
[NEW] Step 6.6: skill-candidate detection — invoke suggest_improvements.mjs --skill-candidates — non-fatal
[NEW] Step 6.7: FLASHCARD cleanup pass — invoke suggest_improvements.mjs --flashcard-cleanup — non-fatal
                  ↓
Step 7 opener (existing)
```

**Insert point line numbers (HEAD `ee21863`, advisory):**

| Insert location | Line on HEAD | After Developer inserts |
|---|---|---|
| Step 6 closer (`}` of catch) | 592 | unchanged |
| Step 6.5 opener (NEW) | — | 594 (was Step 7 opener) |
| Step 7 opener (existing) | 594 | shifts down ~40-50 lines |

#### Step 6.5 — `sprint_trends.mjs <sprint-id>` stub

**Spec (CR-022 §3 M3 line 141):** Reads `state.json` + `token-ledger.jsonl` + sibling sprints under `.cleargate/sprint-runs/`. Emits placeholder line `"Trends: <N> closed sprints visible — full analysis deferred to CR-027."` into the sprint's `improvement-suggestions.md`. Returns exit 0.

**Developer behavior in `close_sprint.mjs` (Step 6.5 block):**

The Step 6.5 block in `close_sprint.mjs` invokes `sprint_trends.mjs` via the existing `invokeScript()` helper (`.cleargate/scripts/close_sprint.mjs:133-144`). Pattern matches Step 6 (`invokeScript('suggest_improvements.mjs', [sprintId], { ... })`):

```javascript
  // ── Step 6.5: Run sprint_trends.mjs (stub — full impl deferred to CR-027) ──
  process.stdout.write('Step 6.5: running sprint_trends.mjs (stub)...\n');
  try {
    invokeScript('sprint_trends.mjs', [sprintId], {
      CLEARGATE_STATE_FILE: stateFile,
      CLEARGATE_SPRINT_DIR: sprintDir,
    });
  } catch (err) {
    // Non-fatal — sprint stays Completed; trends are advisory only.
    process.stderr.write(`Step 6.5 warning: sprint_trends.mjs failed: ${/** @type {Error} */ (err).message}\n`);
  }
```

**`sprint_trends.mjs` stub contents (NEW file at `.cleargate/scripts/sprint_trends.mjs` + canonical mirror at `cleargate-planning/.cleargate/scripts/sprint_trends.mjs`):**

Size budget: **≤30 LOC including header docstring** (CR-022 §0.5 Q1 recommended answer; sprint plan §2.5 soft flag — "DO NOT over-engineer"). Stub MUST NOT implement any trend metrics, cross-sprint comparison logic, or ledger digest. The stub:

1. Validates `<sprint-id>` is present in `process.argv[2]`. Exit 2 on missing arg.
2. Resolves `sprintDir` via `process.env.CLEARGATE_SPRINT_DIR ?? path.join(REPO_ROOT, '.cleargate', 'sprint-runs', sprintId)` (mirror `suggest_improvements.mjs:128-130` resolution pattern).
3. Counts sibling sprint dirs under `.cleargate/sprint-runs/` whose `state.json` exists AND has `sprint_status === 'Completed'`. (Use `process.env.CLEARGATE_SPRINT_RUNS_DIR ?? path.dirname(sprintDir)` so tests can isolate.)
4. Computes the file path of `improvement-suggestions.md` in the **current** sprint dir. Append (not overwrite) one line: `\n## Trends\n\nTrends: <N> closed sprints visible — full analysis deferred to CR-027.\n`. Use atomic write (tmp + rename) per `suggest_improvements.mjs:114-118`.
5. If `improvement-suggestions.md` does not exist (Step 6 was skipped or failed), the stub creates it with a one-line header `# Improvement Suggestions — <sprint-id>\n\n` then appends the Trends section.
6. Exits 0.

**Stub MUST print:** one stdout line — `sprint_trends: stub — full implementation deferred to CR-027 (counted N closed sprints).` This satisfies the "prints a stub notice" constraint from the M6 prompt.

**Stub MUST NOT:**
- Read `token-ledger.jsonl` (deferred to CR-027 full impl despite CR-022 §3 line 141 mentioning ledger — sprint plan §2.5 soft flag overrides; CR-027 owns ledger reads).
- Compute any metric beyond the closed-sprint count.
- Read or invoke `count_tokens.mjs` or any other sibling script.
- Touch `state.json`.

**File structure (target ~30 LOC):**

```
shebang + 8-line docstring (5 LOC)
imports: node:fs + node:path + node:url (3 LOC)
__dirname / REPO_ROOT (2 LOC)
function main() {
  argv parse + sprintId (3 LOC)
  resolve sprintDir + suggestionsFile (3 LOC)
  count completed siblings (5 LOC)
  build/append Trends section (5 LOC)
  stdout notice (1 LOC)
}
main() (1 LOC)
~28 LOC total + 2-3 blank lines = 30-31 LOC.
```

#### Step 6.6 — Skill-candidate detection (folds into `suggest_improvements.mjs`)

**Spec (CR-022 §3 M3 lines 142):** Heuristics from charter §1.8 lines 96-100: same multi-step sequence ≥3× across agent runs; grep+read+apply patterns the orchestrator manually narrated; "remember to also do X" patterns in flashcards. Output: `improvement-suggestions.md` gets a "Skill Creation Candidates" section.

**Developer behavior in `close_sprint.mjs` (Step 6.6 block):**

```javascript
  // ── Step 6.6: Skill-candidate detection (folds into suggest_improvements.mjs) ──
  process.stdout.write('Step 6.6: scanning for skill candidates...\n');
  try {
    invokeScript('suggest_improvements.mjs', [sprintId, '--skill-candidates'], {
      CLEARGATE_STATE_FILE: stateFile,
      CLEARGATE_SPRINT_DIR: sprintDir,
    });
  } catch (err) {
    process.stderr.write(`Step 6.6 warning: skill-candidate scan failed: ${/** @type {Error} */ (err).message}\n`);
  }
```

**`suggest_improvements.mjs` extension — `--skill-candidates` flag:**

**Insert points in `suggest_improvements.mjs` (HEAD `ee21863`):**

1. **Line 121 — `function main()` argv parse.** Currently:
   ```javascript
   const args = process.argv.slice(2);
   if (args.length < 1) { ... }
   const sprintId = args[0];
   ```
   Extend to detect `--skill-candidates` and `--flashcard-cleanup` flags. Both are mutually exclusive with the default mode (which parses §5 self-assessment). The flag-driven modes append a NEW section to `improvement-suggestions.md` instead of generating SUG-* entries.

2. **NEW function `scanSkillCandidates(sprintDir)` — added below `parseSelfAssessment` (insert after line 92).** The scan reads:
   - `token-ledger.jsonl` in `sprintDir` — group entries by `agent_type`, count repeated `(work_item_id, agent_type)` tuples; surface any tuple appearing ≥3× as a candidate.
   - `FLASHCARD.md` at repo root (resolve via `path.join(REPO_ROOT, '.cleargate', 'FLASHCARD.md')`) — grep for `"remember to also"`, `"also do X"`, `"also need to"` patterns; surface matching lines as skill-candidate seeds.
   - **Heuristic threshold:** A candidate is emitted when EITHER (a) ≥3 ledger tuples match OR (b) ≥2 flashcard "also do" patterns match. **Empty-output is the expected default** for most sprints — emit a `## Skill Creation Candidates\n\n_No candidates detected this sprint._\n` placeholder when nothing matches, so the section always exists for the Step 8 handoff (M11) to reference.
   - **DO NOT scan transcripts.** Token-ledger + FLASHCARD only. Transcript analysis is out-of-scope.

3. **Output format (NEW section appended to `improvement-suggestions.md`):**

   ```markdown
   ## Skill Creation Candidates

   <!-- generated-by: suggest_improvements.mjs --skill-candidates -->

   ### CAND-<sprint>-S<n>: <candidate name>
   **Pattern detected:** <3× repetition / "also do" flashcard pattern>
   **Proposed skill:** `.claude/skills/<slug>/SKILL.md`
   **Skeleton:**
   ```
   # <skill name>
   ## Trigger
   <trigger condition>
   ## Procedure
   1. <step>
   ```

   ---
   ```

   Stable IDs: `CAND-<sprint>-S<n>` (S prefix distinguishes from F = flashcard-cleanup; SUG-* IDs from default mode unchanged).

4. **Idempotency:** Same hash-marker pattern as existing SUG-* dedup (`stableHash(category|item)` at `suggest_improvements.mjs:43`). Re-running with `--skill-candidates` produces zero new entries if all candidates already captured.

5. **Atomic write via existing `atomicWrite()` (line 114).** Append to `improvement-suggestions.md` if it exists; create with header if not (mirror lines 204-212 logic).

#### Step 6.7 — FLASHCARD cleanup pass (folds into `suggest_improvements.mjs`)

**Spec (CR-022 §3 M3 line 143):** Categories from charter §1.8 lines 102-106: stale (zero grep hits across last N sprints); superseded (contradicted by newer card); resolved (underlying issue fixed by a CR that landed). Output: `improvement-suggestions.md` gets a "FLASHCARD Cleanup Candidates" section. Human approves per entry; protocol §11.4 archive-immutability not violated (`FLASHCARD.md` is not an archive doc — verified at flashcard `2026-05-01 #commit-format #dod`).

**Developer behavior in `close_sprint.mjs` (Step 6.7 block):**

```javascript
  // ── Step 6.7: FLASHCARD cleanup pass (folds into suggest_improvements.mjs) ──
  process.stdout.write('Step 6.7: scanning FLASHCARD.md for cleanup candidates...\n');
  try {
    invokeScript('suggest_improvements.mjs', [sprintId, '--flashcard-cleanup'], {
      CLEARGATE_STATE_FILE: stateFile,
      CLEARGATE_SPRINT_DIR: sprintDir,
    });
  } catch (err) {
    process.stderr.write(`Step 6.7 warning: FLASHCARD cleanup scan failed: ${/** @type {Error} */ (err).message}\n`);
  }
```

**`suggest_improvements.mjs` extension — `--flashcard-cleanup` flag:**

1. **NEW function `scanFlashcardCleanup(sprintDir)` — added below `scanSkillCandidates` (insert after the new function from Step 6.6).** The scan reads:
   - `FLASHCARD.md` at `path.join(REPO_ROOT, '.cleargate', 'FLASHCARD.md')` — parse newest-on-top entries (format `YYYY-MM-DD · #tag1 #tag2 · lesson`).
   - For each entry, classify into one of three categories:
     - **stale**: zero grep hits for the entry's lesson keyword across the last N sprint dirs (N=3 by default; configurable via `CLEARGATE_FLASHCARD_LOOKBACK` env var, default `3`). Lookback dirs: `.cleargate/sprint-runs/SPRINT-{current-3..current-1}` (skip dirs that don't exist).
     - **superseded**: a newer card (more recent date) shares ≥2 tags AND its lesson text contradicts the older one (heuristic: newer card contains "NOT" / "instead" / "supersedes" near the older lesson's keyword). Keep this rule simple — when in doubt, do NOT flag superseded; humans approve the false-negatives later.
     - **resolved**: the entry's lesson keyword appears in a closed-sprint REPORT.md §6 Tooling section under "Resolved" (grep `.cleargate/sprint-runs/SPRINT-*/SPRINT-*_REPORT.md` via the M1 helper `reportFilename(..., { forRead: true })`).
   - **Empty-output default:** Emit a `## FLASHCARD Cleanup Candidates\n\n_No candidates detected this sprint._\n` placeholder when nothing matches, so the section always exists.

2. **Output format (NEW section appended to `improvement-suggestions.md`):**

   ```markdown
   ## FLASHCARD Cleanup Candidates

   <!-- generated-by: suggest_improvements.mjs --flashcard-cleanup -->

   ### CAND-<sprint>-F<n>: <flashcard one-liner>
   **Category:** stale | superseded | resolved
   **Reason:** <e.g. "stale: zero grep hits across last 3 sprints">
   **Original entry:** `YYYY-MM-DD · #tag1 #tag2 · lesson`
   **Suggested action:** approve to remove via `cleargate flashcard prune` (run /improve)

   ---
   ```

3. **Idempotency:** same `stableHash` dedup pattern. Hash key: `flashcard|<original-entry-line>`.

4. **Read-only on `FLASHCARD.md`.** This script MUST NOT edit FLASHCARD.md — only surface candidates. Human runs `cleargate flashcard prune` after approval (out-of-scope for M6).

#### Mirror parity (FLASHCARD `2026-04-19 #wiki #protocol #mirror`)

Per-edit, not whole-file (FLASHCARD `2026-05-01 #mirror #parity`). Every edit lands in BOTH locations atomically:

| Live | Canonical | Edit type |
|---|---|---|
| `.cleargate/scripts/close_sprint.mjs` | `cleargate-planning/.cleargate/scripts/close_sprint.mjs` | edit (insert Steps 6.5 + 6.6 + 6.7) |
| `.cleargate/scripts/suggest_improvements.mjs` | `cleargate-planning/.cleargate/scripts/suggest_improvements.mjs` | edit (add `--skill-candidates` + `--flashcard-cleanup` flags + scan functions) |
| `.cleargate/scripts/sprint_trends.mjs` (NEW) | `cleargate-planning/.cleargate/scripts/sprint_trends.mjs` (NEW) | create |

**Verification (Developer DoD §4.2 lines 187-188):** post-edit `diff` returns empty for all three pairs.

```bash
diff .cleargate/scripts/close_sprint.mjs cleargate-planning/.cleargate/scripts/close_sprint.mjs
diff .cleargate/scripts/suggest_improvements.mjs cleargate-planning/.cleargate/scripts/suggest_improvements.mjs
diff .cleargate/scripts/sprint_trends.mjs cleargate-planning/.cleargate/scripts/sprint_trends.mjs
```

All three MUST return zero output before commit.

#### Test scenarios (Gherkin from CR-022 §4.3, lines 215-231)

**Test file: extend existing `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts`.** Existing pattern is one `describe('Scenario N: ...')` per scenario; last scenario on `main` is `Scenario 20` (M5's Step 2.8 v1 advisory). M6 appends Scenarios 21-26.

Decision rationale: existing v21 file already hosts Scenarios 15-17 (Step 2.7) + 18-20 (Step 2.8) per the same M0/M1/M2 CR; uniformity beats splitting. New file would orphan related scenarios across two paths.

**Scenarios to add (numbered continuing from 20):**

| # | Name | Maps to CR-022 §4.3 Gherkin |
|---|---|---|
| 21 | Step 6.5 sprint_trends stub writes Trends section | Scenario 3 (line 215-219) |
| 22 | Step 6.5 sprint_trends warns + continues if stub fails | (DoD §4.1 failure-mode requirement) |
| 23 | Step 6.6 skill candidates section emitted (placeholder when empty) | Scenario 4 (line 221-225) |
| 24 | Step 6.6 skill candidates surface CAND-<sprint>-S<n> when ledger has ≥3× repeated tuple | Scenario 4 (line 221-225) |
| 25 | Step 6.7 FLASHCARD cleanup section emitted (placeholder when empty) | Scenario 5 (line 227-231) |
| 26 | Step 6.7 surfaces stale candidate when keyword has zero grep hits across last 3 sprint dirs | Scenario 5 (line 227-231) |

DoD §4.1 line 177 requires **at least one happy-path + one failure-mode test per new step** = ≥6 scenarios for Steps 6.5/6.6/6.7. Six scenarios above satisfy. Scenario 22 covers failure-mode; Scenarios 21/23/25 cover happy-path; Scenarios 24/26 cover non-empty candidate path.

**Test seam env vars (NEW for M6):**

Mirror the M4/M5 seam pattern (`.cleargate/scripts/close_sprint.mjs:36-50`):

- `CLEARGATE_SKIP_SPRINT_TRENDS=1` — skip Step 6.5 entirely (test environments).
- `CLEARGATE_SKIP_SKILL_CANDIDATES=1` — skip Step 6.6 entirely.
- `CLEARGATE_SKIP_FLASHCARD_CLEANUP=1` — skip Step 6.7 entirely.
- `CLEARGATE_SPRINT_RUNS_DIR=<path>` — override `.cleargate/sprint-runs/` root for sibling-sprint counting in `sprint_trends.mjs`.
- `CLEARGATE_FLASHCARD_PATH=<path>` — override `.cleargate/FLASHCARD.md` for `--flashcard-cleanup` scan.
- `CLEARGATE_FLASHCARD_LOOKBACK=<N>` — override the 3-sprint default lookback.

Document each in the close_sprint.mjs file-header comment block (`.cleargate/scripts/close_sprint.mjs:35-50`) and in the new `sprint_trends.mjs` header.

**Fixture re-use (no new fixtures required):** Scenarios 21-26 re-use `cleargate-cli/test/scripts/fixtures/sprint-v1-legacy/` (REPORT.md present) + `sprint-v2-fast-complete/` for the v2 path. Helper `makeTempSprintDir(fixtureName, sprintId)` already in v21 file (line 71). For Scenarios 24 and 26 (non-empty candidate paths), inject a synthetic `token-ledger.jsonl` and a synthetic `FLASHCARD.md` into the temp sprint dir or repo root via the env-seam overrides — no need to commit new fixtures.

#### Reuse (no duplication)

- `reportFilename(sprintDir, sprintId, opts)` from `.cleargate/scripts/lib/report-filename.mjs` (M1 frozen signature) — re-used in `--flashcard-cleanup` "resolved" category lookup.
- `invokeScript(name, args, env)` from `close_sprint.mjs:133-144` — re-used for all three new Step 6.x dispatches.
- `stableHash(input)` from `suggest_improvements.mjs:43` — re-used for CAND-* ID dedup.
- `atomicWrite(filePath, content)` from `suggest_improvements.mjs:114-118` — re-used for `improvement-suggestions.md` appends.
- `parseExistingIds(content)` from `suggest_improvements.mjs:99-107` — extend regex to match `CAND-<sprint>-[SF]<n>` in addition to `SUG-*` (one-line change).
- `SCRIPTS_DIR` constant in `close_sprint.mjs:48` — re-used; `sprint_trends.mjs` ships in this directory.

#### Gotchas surfaced from code inspection

1. **`close_sprint.mjs:586` env var pass-through.** The existing Step 6 invocation passes `CLEARGATE_STATE_FILE` and `CLEARGATE_SPRINT_DIR`. M6's three new invocations MUST also pass these so test fixtures (which set `CLEARGATE_SPRINT_DIR=<temp-dir>`) work. Missing pass-through silently breaks Scenarios 21-26.

2. **`suggest_improvements.mjs:120-125` argv parse uses `args[0]` strictly.** Adding `--skill-candidates` / `--flashcard-cleanup` flags requires a flag-aware parse: detect flags first, then `sprintId = args.find(a => !a.startsWith('--'))`. Existing logic `const sprintId = args[0]` would treat the flag as the sprint ID and crash. **Insert the parse change at line 127 BEFORE `const sprintId`.** Verified: there are no callers that pass `--`-prefixed args today (Step 6 invocation passes `[sprintId]` only, `close_sprint.mjs:584`).

3. **`suggest_improvements.mjs:140-144` exits 1 if REPORT.md absent.** This is a problem for Steps 6.6 / 6.7 because the ledger + flashcard scans don't need REPORT.md. Wrap the report-existence check in `if (!skillCandidatesMode && !flashcardCleanupMode)`. Otherwise the new flag-driven modes hard-fail in fixtures that intentionally omit REPORT.md.

4. **`suggest_improvements.mjs:191-197` "idempotent — exit 0" path.** Current behavior: if no new SUG-* entries, exit 0 with `Idempotent:` stdout. This is the **default-mode** branch only. Skill-candidate mode and flashcard-cleanup mode have their own dedicated dedup paths and MUST NOT fall through to this branch — guard with `if (!skillCandidatesMode && !flashcardCleanupMode) return` at line 191.

5. **Append vs. overwrite on `improvement-suggestions.md`.** Default mode prepends new SUG-* entries via `existingContent.trimEnd() + '\n\n' + appendContent` (`suggest_improvements.mjs:234-236`). Steps 6.6 / 6.7 append NEW SECTION HEADINGS (`## Skill Creation Candidates`, `## FLASHCARD Cleanup Candidates`). Re-running the same flag mode MUST detect the heading via regex `/^## (Skill Creation Candidates|FLASHCARD Cleanup Candidates)/m` and replace the body of that section, not append a duplicate heading. Implementation: read file → if heading regex matches, splice old section out → append new section. Otherwise append fresh.

6. **Step 6's catch is silent on `invokeScript` not-found.** `close_sprint.mjs:139` throws `"Script not found: ..."` if `sprint_trends.mjs` is missing. The Step 6.5 catch handles this (warn + continue). **Verify in Scenario 22 that deleting the stub file from the test temp scripts dir produces the expected warning, NOT a hard exit.** The stub MUST exist in BOTH live and canonical paths post-merge to satisfy this — mirror parity is therefore a Step 6.5 happy-path prerequisite, not just a DoD nicety.

7. **`SPRINT-*_REPORT.md` naming sweep.** `suggest_improvements.mjs:137` already calls `reportFilename(..., { forRead: true })`. M6's `--flashcard-cleanup` "resolved" category MUST also use this helper when grepping prior sprint REPORT.md files (NOT hardcode `REPORT.md`). Skipping the helper repeats the SPRINT-18 lesson surfaced in CR-022 §1 line 73 ("suggest_improvements.mjs missed the SPRINT-<#>_REPORT.md naming sweep").

8. **FLASHCARD `2026-05-02 #worktree #git #commit` (newest, post-this-plan-write).** Dev agent's `git commit` in SPRINT-19 landed on `main` instead of `story/<id>` branch. **Orchestrator MUST verify post-dispatch with `git log story/CR-022-M3 --oneline` (NOT just commit-success-claim from Developer)** before accepting M6 as merge-ready. If the commit landed on `main`, halt and surface for human resolution per FLASHCARD entry.

9. **FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`.** `cleargate-cli/templates/cleargate-planning/` is DERIVED — the `copy-planning-payload.mjs` prebuild copies `cleargate-planning/` into the bundled tree. **Developer MUST NOT hand-edit `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/sprint_trends.mjs`**; that path will be regenerated on next `npm run prebuild`. Edit canonical at `cleargate-planning/.cleargate/scripts/sprint_trends.mjs` only. Same applies to `close_sprint.mjs` and `suggest_improvements.mjs` canonical mirrors.

10. **Commit format (FLASHCARD `2026-05-01 #commit-format #dod`).** Three SPRINT-17 deviations observed where Developers used `feat(<sprint>):` instead of `feat(<epic>):`. CR-022 DoD §4.2 line 184 specifies `feat(EPIC-???-or-CR-022): <story-id> <short desc>`. **Required commit format for M6: `feat(CR-022): CR-022-M3 Steps 6.5+6.6+6.7 post-close additions`** (mirror M4 commit `f43ceb3 feat(CR-022): CR-022-M1 Step 2.7 worktree-closed pre-close check` and M5 commit `ee21863 feat(CR-022): CR-022-M2 Step 2.8 sprint-merged-to-main verify`).

## Cross-story risks

1. **Wave 2 disjointness depends on M6 NOT touching `.claude/agents/`.** M8 owns `developer.md` + `qa.md` (sprint plan §2.2 row 103); M9 owns `architect.md` + `reporter.md` (sprint plan §2.2 row 101). If M6 Developer edits any agent definition file, Wave 2 collisions break parallel execution. **Plan explicitly forbids agent-file edits.** Pre-commit grep verification: `git diff --name-only` MUST return zero matches for `^\.claude/agents/` and `^cleargate-planning/\.claude/agents/`.

2. **`suggest_improvements.mjs` argv parse must stay backward-compatible.** Step 6 (existing) calls `invokeScript('suggest_improvements.mjs', [sprintId], { ... })` — single positional arg, no flags. M6's argv parse change MUST preserve this default-mode behavior. If the flag-detection regex accidentally matches the sprint-id (e.g. a sprint named `--SPRINT-20`), default mode breaks. Sprint IDs match `^SPRINT-\d+$` per `report-filename.mjs:37`, so flag prefix `--` never collides. Document this invariant in the new argv parse comment.

3. **`improvement-suggestions.md` section-heading regex must be strict.** A user-edited custom heading `## Skill Creation Candidates (manual)` would be missed by the splice-and-replace logic in gotcha #5. Use anchored regex `^## (Skill Creation Candidates|FLASHCARD Cleanup Candidates)$` (no trailing text). Document the constraint in the section comment.

4. **`sprint_trends.mjs` stub MUST NOT pull in dependencies.** Sprint plan §2.5 soft flag: "DO NOT over-engineer". A stub that imports `lib/ledger-digest.mjs` or `count_tokens.mjs` violates the soft flag. Restrict imports to `node:fs`, `node:path`, `node:url` only. CR-027 (future sprint) lifts this restriction.

5. **M11 (Step 8 verbose handoff) re-greps after M6.** M11 Architect needs the stable Step 7 closer + post-Step-7 region. M6 does NOT alter that region (insertion is between Step 6 closer and Step 7 opener). Confirmed-disjoint by line number: M6 ends before line 594; M11 starts after line 615. **M11 Architect re-greps anyway** per FLASHCARD `2026-04-21 #protocol #section-numbering` style discipline.

6. **Backwards-compat read-fallback in `--flashcard-cleanup` resolved-category lookup.** Calling `reportFilename(sprintDir, sprintId, { forRead: true })` for SPRINT-01..17 returns the legacy `REPORT.md` path. The grep against §6 Tooling MUST tolerate both old REPORT.md formats (older sprints had different §6 conventions) and the new SPRINT-18+ format. Implementation: tolerate missing `Resolved` subheading silently (no match = no resolved category, no error).

7. **Pre-existing admin/+mcp/ vitest failures (sprint plan §3 risk row).** Out-of-scope. Do not attempt to fix in M6. Developer commits with these test failures still red; QA approves on M6-scoped scenarios (21-26 + Scenarios 1-20 unchanged from M5 baseline).

## Surface manifest (for orchestrator pre/post grep)

**Files M6 touches (live + canonical mirror, exhaustive):**

| File | Operation | Canonical mirror |
|---|---|---|
| `.cleargate/scripts/close_sprint.mjs` | edit (insert Steps 6.5/6.6/6.7 between line 592 and 594) | `cleargate-planning/.cleargate/scripts/close_sprint.mjs` |
| `.cleargate/scripts/suggest_improvements.mjs` | edit (flag-aware argv parse + 2 new scan functions + 2 new sections) | `cleargate-planning/.cleargate/scripts/suggest_improvements.mjs` |
| `.cleargate/scripts/sprint_trends.mjs` | **create** (NEW, ~30 LOC stub) | `cleargate-planning/.cleargate/scripts/sprint_trends.mjs` (NEW) |
| `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` | edit (append Scenarios 21-26 below current line 1207) | n/a (test file, no canonical mirror) |

**Files M6 MUST NOT touch (Wave 2 disjointness invariants):**

- `.claude/agents/developer.md` (M8 owns)
- `.claude/agents/qa.md` (M8 owns)
- `.claude/agents/architect.md` (M9 owns)
- `.claude/agents/reporter.md` (M9 owns)
- `cleargate-planning/.claude/agents/{developer,qa,architect,reporter}.md` (canonical mirrors of above)
- `CLAUDE.md` (M9 + M12 own; M9 first)
- `.cleargate/knowledge/cleargate-protocol.md` (M9 owns)
- `.cleargate/knowledge/cleargate-enforcement.md` (M9 + M12 own)
- `.cleargate/scripts/prep_reporter_context.mjs` (M7 already merged at HEAD `d73642a`; do NOT re-edit)
- `cleargate-cli/src/commands/sprint.ts` (M7 already merged)
- `.cleargate/templates/initiative.md` (M3 already merged at HEAD `8387311`)
- `cleargate-cli/templates/cleargate-planning/**` (DERIVED tree; never hand-edit per FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`)

## Open decisions for orchestrator

1. **Flag mutual-exclusion.** M6 introduces `--skill-candidates` and `--flashcard-cleanup` flags on `suggest_improvements.mjs`. They are mutually exclusive (the script invokes one mode at a time). Should the script error on `--skill-candidates --flashcard-cleanup` together, or run both modes serially? **Architect recommendation:** error and exit 2. Rationale: orchestrator-controlled invocation; combined-mode would require interleaved output and breaks the "one section per invocation" contract. Confirm at dispatch.

2. **`improvement-suggestions.md` section ordering.** Final file structure post-M6 will have: `# Header → SUG-* entries → ## Trends → ## Skill Creation Candidates → ## FLASHCARD Cleanup Candidates`. Order is determined by the close_sprint.mjs invocation sequence (Step 6 → 6.5 → 6.6 → 6.7). **Architect recommendation:** preserve invocation order (no explicit sort). The Step 8 handoff (M11) references all four section names — order does not affect M11.

3. **Stub `sprint_trends.mjs` file location for canonical bundle.** Stub lives at both `.cleargate/scripts/sprint_trends.mjs` (live) and `cleargate-planning/.cleargate/scripts/sprint_trends.mjs` (canonical). The `cleargate init` scaffold ships `cleargate-planning/` contents into target repos. **Architect recommendation:** ship the stub in scaffold from day one — target repos will need it once they upgrade past CR-027. Stub-shipping is harmless (just exits 0 with a placeholder line).

4. **DoD §4.2 line 188 says "byte-identical post-edit" for new files.** Three new file pairs (live + canonical for `sprint_trends.mjs`). Confirm the diff-empty assertion runs in pre-commit lint, or is it a Developer-side `diff` invocation only? **Architect recommendation:** Developer-side `diff` invocation in the commit message body, mirror M1's commit pattern. No CI gate change needed for M6 specifically (M9 may add one, scope-permitting).

---

role: architect

### M7.md

# Milestone: M7 — CR-022 M5 Reporter bundle cap raise + `--allow-wiki-lint-debt` flag
## Stories: CR-022 M5 (single milestone, fast lane)
## Wave: W2 (parallel — disjoint surfaces from M4/M5/M6/M8/M9)

> role: architect

## Order

**Independent of all other Wave 2 milestones.** Disjoint file surfaces:

- M7 owns: `.cleargate/scripts/prep_reporter_context.mjs`, `cleargate-cli/src/commands/sprint.ts` archive subcommand block, archive test files.
- M4 / M5 / M6 / M11 own: `.cleargate/scripts/close_sprint.mjs` (+ canonical mirror) — disjoint from M7.
- M8 owns: `.claude/agents/developer.md` + `qa.md` — disjoint.
- M9 owns: `cleargate-protocol.md`, `cleargate-enforcement.md`, `CLAUDE.md`, `architect.md`, `reporter.md` — disjoint.
- M1 owns: `.cleargate/scripts/lib/report-filename.mjs` (NEW), `suggest_improvements.mjs` — disjoint from M7's prep_reporter_context surface.

Dispatch order: M7 may run in parallel with M4 / M5 / M6 / M8 / M9 in Wave 2 (sprint plan §2.1 line 87, §2.2 row 102 confirms `cleargate-cli/src/commands/sprint.ts` is M7-only).

**No prerequisite milestone.** M7's two production surfaces have no upstream dependency on M0/M1/M2/M3. Even though `prep_reporter_context.mjs` appears in §2.2 row 97 alongside M1, the §2.2 rationale clarifies "M7's cap raise is independent of M1's helper" — they touch disjoint code regions inside the same file. M7 lands second per the table only "to keep merges atomic." Practically, since M1 ships in Wave 1 (round 1) and M7 in Wave 2 (round 2), M1 ALWAYS lands first by dispatch ordering — no M7-side coordination required.

## Per-story blueprint

### CR-022 M5 — Bundle cap raise + wiki-lint-debt waiver flag

#### Cross-story coupling

- **None at the M7-internal layer.** M7's two changes (cap constant + new CLI flag) have no shared code with any other milestone.
- **One advisory coupling with M1:** M1 (Wave 1) extracts `lib/report-filename.mjs` and refactors `prep_reporter_context.mjs` to consume it. By the time M7 dispatches, M1 is merged. The M7 Developer MUST re-grep `prep_reporter_context.mjs` line numbers at dispatch time — line 453 (current cap check on `main`) may have shifted by ±5 lines. Anchor regex below is line-number-agnostic.
- **One advisory coupling with M6:** M6 may add stub trend lines to a new file (`sprint_trends.mjs`) but does NOT touch the bundle assembly. Disjoint.

#### Insertion strategy (anchor-based, not line-based)

**Re-grep at dispatch time (mandatory):**

```bash
grep -n "81920\|bundle exceeds 80KB\|MAX_BUNDLE" .cleargate/scripts/prep_reporter_context.mjs
grep -n "wikiLintFn\|wiki-lint-exit\|--dry-run" cleargate-cli/src/commands/sprint.ts
```

Confirm three live locations exist before editing:

1. `.cleargate/scripts/prep_reporter_context.mjs` — header comment line 39 (`Bundle-size budget: ≤80KB target (R3)`), threshold check line 453 (`if (bundleBytes > 81920)`), warning text line 455 (`bundle exceeds 80KB target`).
2. `cleargate-cli/src/commands/sprint.ts` — `sprintArchiveHandler` opts type at line 517-519 (currently `opts: { sprintId: string; dryRun?: boolean }`), wiki-lint invocation block at line 543-554, wiki-lint loop call at line 720-735.
3. `cleargate-cli/src/cli.ts` — archive command registration at line 360-371, with `--dry-run` flag declaration at line 363.

If any of those greps return zero results, halt + report blocker.

#### Change 1 — Bundle cap raise (`prep_reporter_context.mjs`)

**Current state (verified at `.cleargate/scripts/prep_reporter_context.mjs:39-40, 453-456`):**

- Line 39 (header comment): `* Bundle-size budget: ≤80KB target (R3). If exceeded, a warning is logged to stderr`
- Line 40: `* but the file is still written (do NOT truncate — partial bundles are worse than oversized).`
- Line 453: `if (bundleBytes > 81920) {`
- Line 455: `` `Warning: bundle exceeds 80KB target (${kb}KB) — consider trimming sprint-plan §2 in-place\n` ``

**Decision: introduce a named module-level constant.** The current code uses the magic number `81920` inline at line 453 and the string `"80KB"` inline at line 455. CR-022 §3 M5 (line 160) calls for a `MAX_BUNDLE_BYTES` constant. Define it once near the top of the file and reference it from both the threshold check and the warning text.

**Required edits (4 sites in one file):**

1. **Add constant near top of file** (after the imports block, between line 47 `import { parseJsonl } ...` and line 49 `const __dirname = ...`):

   ```javascript
   // Bundle-size budget: ≤160KB target (CR-022 M5 raise from 80KB; SPRINT-18 hit 138KB).
   const MAX_BUNDLE_BYTES = 160 * 1024; // 163840
   ```

   Use `160 * 1024` (not `163840`) for self-documenting intent. Matches Node-ecosystem convention.

2. **Update header comment line 39:** replace `Bundle-size budget: ≤80KB target (R3).` with `Bundle-size budget: ≤160KB target (CR-022 M5 raised from 80KB; R3 superseded).`

3. **Update threshold check line 453:** replace `if (bundleBytes > 81920) {` with `if (bundleBytes > MAX_BUNDLE_BYTES) {`.

4. **Update warning text line 455:** replace the literal `bundle exceeds 80KB target` with `bundle exceeds 160KB target`. Keep the rest of the message verbatim (`(${kb}KB) — consider trimming sprint-plan §2 in-place\n`).

**Net diff:** +2 LOC (constant + comment), 3 modified lines. Single file.

#### Change 2 — `--allow-wiki-lint-debt` flag (`sprint.ts` + `cli.ts`)

**Current wiring (verified at `cleargate-cli/src/commands/sprint.ts:517-554, 717-736`):**

- `sprintArchiveHandler` signature: `async function sprintArchiveHandler(opts: { sprintId: string; dryRun?: boolean }, cli?: SprintCliOptions): Promise<void>`
- `cli?.wikiLintFn` test seam already exists at line 543-554 (matches existing `--allow-drift` pattern from `sprintInitHandler:55-65`).
- The wiki-lint invocation runs inside a `for` loop at lines 720-735 that iterates over `[['wiki build', ...], ['wiki lint', ...]]` step pairs. On `wikiLintFn` throw, the loop catches the error, rolls back the sprint frontmatter, prints to stderr, and exits 1 (lines 726-734).

**Required edits (`cleargate-cli/src/commands/sprint.ts`):**

1. **Extend `sprintArchiveHandler` opts** (line 518): add `allowWikiLintDebt?: boolean` to the inline opts type:

   ```typescript
   async function sprintArchiveHandler(
     opts: { sprintId: string; dryRun?: boolean; allowWikiLintDebt?: boolean },
     cli?: SprintCliOptions,
   ): Promise<void>
   ```

   Mirror the type-extension pattern used for `dryRun`. Do NOT add the flag to `SprintCliOptions` (the interface) — `--allow-wiki-lint-debt` is an `opts` flag (per-command), not a CLI-wide test seam. This matches `--dry-run` precedent.

2. **Modify the wiki-build/lint loop block** (lines 719-736). The current shape:

   ```javascript
   if (wikiInitialised) {
     for (const [stepName, stepFn] of [
       ['wiki build', () => wikiBuildFn(cwd, stdoutFn)] as const,
       ['wiki lint', () => wikiLintFn(cwd, stdoutFn)] as const,
     ]) {
       try {
         await stepFn();
       } catch (err) {
         atomicWriteStr(sprintFile!, sprintFileSnapshot!);
         stderrFn(`[cleargate sprint archive] post-stamp ${stepName} failed — sprint frontmatter reverted`);
         if (err instanceof Error) stderrFn(err.message);
         return exitFn(1);
       }
     }
   }
   ```

   Replace with a per-step branch on `stepName === 'wiki lint' && opts.allowWikiLintDebt === true`:

   ```javascript
   if (wikiInitialised) {
     for (const [stepName, stepFn] of [
       ['wiki build', () => wikiBuildFn(cwd, stdoutFn)] as const,
       ['wiki lint', () => wikiLintFn(cwd, stdoutFn)] as const,
     ]) {
       try {
         await stepFn();
       } catch (err) {
         // CR-022 M5: --allow-wiki-lint-debt waives lint failure but NOT build failure.
         if (stepName === 'wiki lint' && opts.allowWikiLintDebt === true) {
           stderrFn('[cleargate sprint archive] wiki-lint debt waived via --allow-wiki-lint-debt flag');
           if (err instanceof Error) stderrFn(`  (lint output: ${err.message})`);
           continue;
         }
         atomicWriteStr(sprintFile!, sprintFileSnapshot!);
         stderrFn(`[cleargate sprint archive] post-stamp ${stepName} failed — sprint frontmatter reverted`);
         if (err instanceof Error) stderrFn(err.message);
         return exitFn(1);
       }
     }
   }
   ```

   Three additions: (a) the `if (stepName === 'wiki lint' && opts.allowWikiLintDebt === true)` guard, (b) the `stderrFn(...waived...)` line matching the verbatim Gherkin assertion `wiki-lint debt waived via --allow-wiki-lint-debt flag` (CR-022 §4.3 Scenario "--allow-wiki-lint-debt flag waives lint failures"), (c) `continue` to skip the rollback and proceed to the next loop iteration. The build step is NOT waivable — only lint findings are debt; build failures indicate a real assembly error.

   **Critical:** the waiver block does NOT roll back the sprint frontmatter. The `atomicWriteStr(sprintFile!, sprintFileSnapshot!)` rollback runs ONLY on the non-waived path. With the flag set, the file remains stamped Completed even if lint findings exist.

**Required edits (`cleargate-cli/src/cli.ts`):**

3. **Register the flag on the archive command** (lines 361-371). Current shape:

   ```typescript
   sprint
     .command('archive <sprint-id>')
     .description('archive a completed sprint — move pending-sync files, clear .active, merge + delete sprint branch')
     .option('--dry-run', 'print the archive plan without making any changes')
     .action(async (sprintId: string, opts: { dryRun?: boolean }) => {
       const handlerOpts: { sprintId: string; dryRun?: boolean } = { sprintId };
       if (opts.dryRun === true) {
         handlerOpts.dryRun = true;
       }
       await sprintArchiveHandler(handlerOpts);
     });
   ```

   Replace with:

   ```typescript
   sprint
     .command('archive <sprint-id>')
     .description('archive a completed sprint — move pending-sync files, clear .active, merge + delete sprint branch')
     .option('--dry-run', 'print the archive plan without making any changes')
     .option('--allow-wiki-lint-debt', 'CR-022 M5: waive wiki-lint findings during archive (mirrors --allow-drift pattern)')
     .action(async (sprintId: string, opts: { dryRun?: boolean; allowWikiLintDebt?: boolean }) => {
       // FLASHCARD #cli #commander #optional-key: omit key when undefined
       const handlerOpts: { sprintId: string; dryRun?: boolean; allowWikiLintDebt?: boolean } = { sprintId };
       if (opts.dryRun === true) {
         handlerOpts.dryRun = true;
       }
       if (opts.allowWikiLintDebt === true) {
         handlerOpts.allowWikiLintDebt = true;
       }
       await sprintArchiveHandler(handlerOpts);
     });
   ```

   Mirror the existing `--dry-run` and `--allow-drift` (sprintInitHandler at `cli.ts:331`) wiring. Commander.js camelCases `--allow-wiki-lint-debt` to `allowWikiLintDebt` automatically — no manual translation needed.

**Net diff:** +14 LOC across two files (sprint.ts +8, cli.ts +6). Total M7 production diff: ~16 LOC.

#### Change 3 — Bash fixture test parity (`test_prep_reporter_context.sh`)

**Current state (verified at `.cleargate/scripts/test/test_prep_reporter_context.sh:153-156`):**

- Line 153: `# Check bundle size ≤ 80KB (81920 bytes)`
- Line 156: `if [ "${bundle_size}" -gt 81920 ]; then`

**Required edits:**

5. **Update comment line 153:** replace `≤ 80KB (81920 bytes)` with `≤ 160KB (163840 bytes)`.
6. **Update threshold line 156:** replace `81920` with `163840`. (Bash test uses raw integer; no shared constant available across .sh / .mjs.)

**Net diff:** 2 modified lines. Bash side is a hard-coded integer mirror of the JS constant — no shared symbol available. Architect-decision note: this is acceptable since the Bash test is a thin smoke test (size-budget regression guard only); a more rigorous approach would be JSON/env-var seeding, but that's out-of-scope for a 50-LOC fast-lane story.

#### Test scenarios (DoD §4.1 ENFORCED — sprint plan §"Constraints" line 170)

CR-022 §4.1 minimum table specifies:

- Vitest scenarios in `test_close_sprint_v21.test.ts`: 8 minimum across all six new steps. **M7 contributes the `--allow-wiki-lint-debt` scenario at minimum.**
- M7-specific test surface (per CR-022 §3 M5 + §4.3):
  1. **`sprint-archive.test.ts` — `--allow-wiki-lint-debt` waiver scenario** (Gherkin Scenario "--allow-wiki-lint-debt flag waives lint failures").
  2. **`sprint-archive.test.ts` — `--allow-wiki-lint-debt` flag absent → lint failure still blocks** (regression guard for default behavior).
  3. **`prep_reporter_context.mjs` cap raise unit test** (Gherkin Scenario "Reporter bundle cap raised to 160KB"). Bash fixture-test side at `test/test_prep_reporter_context.sh:156` already has size assertion — extending it covers this scenario.

**Mandatory test count: 3 new scenarios.**

##### Scenario 1 — `--allow-wiki-lint-debt` waives lint failure

**File:** `cleargate-cli/test/commands/sprint-archive.test.ts` (extend, do NOT create new file). Insert as a new `describe` block AFTER existing Scenario 4 (`v1 inert`, ends at line 520) and BEFORE existing orphan scenario (`describe('Scenario: orphan mid-sprint story...`, line 524). Test layout precedent: scenarios 1-4 + orphan are sibling top-level `describe`s.

**Test fixture pattern:** Mirror `sprint-archive-stamp.test.ts:283-323` (Scenario 3 — wiki-lint failure rollback). Specifically:

- Use `buildFixture()` from `sprint-archive.test.ts:59` (or import from `sprint-archive-stamp.test.ts:48` if richer fixture needed). The simpler `sprint-archive.test.ts` fixture is sufficient because we are NOT testing rollback behavior.
- Throw a synthetic lint error from `wikiLintFn`: `async () => { throw new Error('broken-backlink: cleargate-protocol.md → §11.4 (target missing)'); }`.
- Pass `allowWikiLintDebt: true` in the archive opts.

**Assertions:**

- `getCode()` returns `0` (NOT 1).
- `cap.getErr().join('\n')` contains `wiki-lint debt waived via --allow-wiki-lint-debt flag`.
- `cap.getErr().join('\n')` contains the lint output's tail (`broken-backlink: cleargate-protocol.md`).
- `fs.readdirSync(fixture.archiveDir)` includes `SPRINT-99_Test_Sprint.md` (sprint file moved to archive — proves the waiver did not block).
- `fs.readFileSync(path.join(fixture.archiveDir, 'SPRINT-99_Test_Sprint.md'), 'utf8')` contains `status: Completed` (sprint stamped, NOT rolled back).

**Critical wiring:** the fixture must NOT exercise the wiki-lint rollback path. To do that, the wiki directory must exist (`wikiInitialised === true` triggers the lint loop). `sprint-archive-stamp.test.ts:204-220` shows the pattern: `fs.mkdirSync(path.join(fixture.cwd, '.cleargate', 'wiki'), { recursive: true })` + `fs.writeFileSync(path.join(wikiRoot, 'index.md'), '...')` to create a minimal wiki dir. Replicate.

##### Scenario 2 — Without flag, lint failure still blocks (regression guard)

**File:** same `sprint-archive.test.ts`. Sibling `describe` block.

**Test fixture pattern:** identical to Scenario 1 except `allowWikiLintDebt: undefined` (or omit the key entirely).

**Assertions:**

- `getCode()` returns `1`.
- `cap.getErr().join('\n')` contains `post-stamp wiki lint failed — sprint frontmatter reverted` (matches existing rollback message at `sprint.ts:730`).
- `cap.getErr().join('\n')` does NOT contain `wiki-lint debt waived` (the waiver did not fire).
- `fs.readdirSync(fixture.archiveDir)` does NOT include `SPRINT-99_Test_Sprint.md` (rollback prevented archival).
- Sprint file at `fixture.sprintFilePath` exists in `pending-sync/` (rollback restored it).

##### Scenario 3 — Bundle cap raise

**File:** `.cleargate/scripts/test/test_prep_reporter_context.sh` (extend the existing 80KB assertion at line 153-156). The bash test already exercises bundle-write → size-check; this is a constant change, not a new test.

**Required edit:** replace `81920` with `163840` and `≤ 80KB` with `≤ 160KB` in the comment + assertion. Already counted in "Change 3" above (2 modified lines). The test rerun verifies: bundles assembled by `prep_reporter_context.mjs` against the SPRINT-19 fixture stay under 163840 bytes.

**No new vitest unit test needed for the constant.** The change is a 1-line literal swap; the existing bash fixture test guards the budget; adding a vitest unit test for `MAX_BUNDLE_BYTES === 160*1024` is redundant tautology (per FLASHCARD-spirit "real infra, no mocks"). Architect decision.

**Optional enhancement (NOT required by DoD §4.1):** if the M7 Developer wants belt-and-suspenders, add ONE assertion in a new `cleargate-cli/test/scripts/test_prep_reporter_context.test.ts` that imports `MAX_BUNDLE_BYTES` and confirms `=== 163840`. The constant would need to be `export`ed (currently it's module-local). This adds minimal risk — `prep_reporter_context.mjs` is a CLI script invoked via shell, not imported as a module by anything in the codebase (verified: zero `import.*prep_reporter_context` hits across `cleargate-cli/src` + `cleargate-cli/test` + `.cleargate/scripts`). Skip unless QA kicks back.

#### DoD checklist (per CR-022 §4.2 + sprint plan §"Constraints")

- [ ] `MAX_BUNDLE_BYTES` constant defined at top of `prep_reporter_context.mjs` with value `160 * 1024`.
- [ ] Threshold check + warning text use the constant + reference `160KB` (not `80KB`).
- [ ] Bash fixture test `test_prep_reporter_context.sh` updated to `163840` + `≤ 160KB`.
- [ ] `--allow-wiki-lint-debt` flag registered on `cleargate sprint archive` in `cli.ts`.
- [ ] `sprintArchiveHandler` opts type extended with `allowWikiLintDebt?: boolean`.
- [ ] Wiki-lint loop block in `sprint.ts` branches on `stepName === 'wiki lint' && opts.allowWikiLintDebt === true` to print the waiver message + `continue`, leaving rollback intact for build failures and for non-waived lint failures.
- [ ] Three new test scenarios green (`--allow-wiki-lint-debt` waives, missing flag still blocks, bash bundle-size assertion).
- [ ] Single commit format: `feat(CR-022): <story-id> <short desc>` per FLASHCARD `2026-05-01 #commit-format #dod` (CR-022 §4.2 specifies `feat(<epic-or-CR>):`, NOT `feat(<sprint>):`).
- [ ] Mirror parity confirmed: see "Mirror parity" below.

#### Mirror parity — IMPORTANT

**`prep_reporter_context.mjs` does NOT have a canonical mirror today.** Verified at plan-write time via `find /Users/ssuladze/Documents/Dev/ClearGate -path "*/cleargate-planning/.cleargate/scripts/*.mjs"` — the canonical-mirror tree contains all peer scripts (close_sprint, suggest_improvements, prefill_report, prep_qa_context, etc.) but **NOT** `prep_reporter_context.mjs`. The CLI-bundled tree (`cleargate-cli/templates/cleargate-planning/.cleargate/scripts/`) likewise has no `prep_reporter_context.mjs` entry.

**Sprint plan §2.2 row 97 explicitly says "(+ canonical mirror)"** — the plan assumes a mirror exists. **This is a structural drift from current reality. Two paths forward:**

A. **M7 Developer creates the canonical mirror as part of this commit** (copies the live file verbatim into `cleargate-planning/.cleargate/scripts/prep_reporter_context.mjs`, then applies the CR-022 M5 edits to BOTH copies in lockstep). Adds ~6 LOC of incidental scaffolding to the diff. Pro: brings reality into compliance with the sprint plan + with the file-surface expectation seeded by every peer script. Con: the file is ~470 LOC, so the canonical-mirror creation is a multi-hundred-line addition that exceeds the fast-lane 50-LOC cap.

B. **M7 Developer skips the canonical mirror and edits only the live file.** The fast-lane cap stays intact. Adds an entry to the SPRINT-19 REPORT.md §6 Tooling list flagging "prep_reporter_context.mjs canonical mirror absent — created during CR-022 M5 dispatch deferred." Defers the mirror-creation to a SPRINT-20 cleanup hotfix or to whatever flow runs the next time this script is touched.

**Architect recommendation: Option B (skip mirror).** Reasons: (1) creating a 470-LOC mirror is a non-trivial scaffold-bootstrap action that warrants its own decision, not a side-effect of a 50-LOC cap raise; (2) the live → canonical → cli-bundled chain is enforced by `copy-planning-payload.mjs` only when the canonical file exists — adding `prep_reporter_context.mjs` to the canonical tree triggers ALL prebuild copies in subsequent installs; (3) the fast-lane budget (≤50 LOC + tests) is non-negotiable per sprint plan §2.4. **Decision needed from orchestrator before Developer dispatch.** See "Open decisions" below.

**If Option A is selected:** M7 Developer (a) `cp .cleargate/scripts/prep_reporter_context.mjs cleargate-planning/.cleargate/scripts/prep_reporter_context.mjs` BEFORE applying the M5 edits, (b) applies edits to BOTH files in lockstep, (c) confirms both files are byte-identical after edit, (d) re-runs `npm run prebuild` in `cleargate-cli/` to refresh `cleargate-cli/templates/cleargate-planning/...` (per FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`). Lane reclassifies to `standard` due to LOC overrun.

**If Option B is selected:** edit only `.cleargate/scripts/prep_reporter_context.mjs` (live). Confirm `find /Users/ssuladze/Documents/Dev/ClearGate -path "*/cleargate-planning/*" -name "prep_reporter_context.mjs"` returns empty BEFORE the commit (snapshot the absence). Lane stays `fast`. SPRINT-19 REPORT.md §6 Tooling adds a Yellow flag.

`cleargate-cli/src/commands/sprint.ts` and `cleargate-cli/src/cli.ts` have NO mirror obligation — they live in `cleargate-cli/`, which is the canonical home for CLI source code (no `cleargate-planning/cleargate-cli/...` mirror exists; CLI is published as an npm package, not scaffold-installed).

#### Reuse (no duplication)

- `wikiLintFn` test seam — already exists at `cleargate-cli/src/commands/sprint.ts:543-554`. Reuse without modification. The existing seam pattern is what makes the new flag possible to test cleanly.
- `wikiLintHandler` — already imported at `sprint.ts:29` from `./wiki-lint.js`. No new import needed.
- `buildFixture` helper from `cleargate-cli/test/commands/sprint-archive.test.ts:59` — reuse for Scenarios 1 + 2.
- Wiki dir scaffolding pattern from `sprint-archive-stamp.test.ts:204-220` — reuse for Scenarios 1 + 2 to make `wikiInitialised === true`.
- `makeExitSeam` + `makeCapture` helpers from `sprint-archive.test.ts:23-41` — reuse.
- Bash size-check at `test_prep_reporter_context.sh:153-156` — reuse the existing assertion structure; only the literal value changes.
- Existing `--allow-drift` precedent at `sprint.ts:54-65` (interface) + `cli.ts:331-333` (registration) + `sprint.ts:166-184` (handler logic) — Architect designed this M7 wiring to MIRROR that precedent at the syntactic level (option name + commander binding + handler branch), so a reviewer can read `--allow-wiki-lint-debt` as a 1:1 sibling of `--allow-drift`.

#### Gotchas surfaced from code inspection

- **`prep_reporter_context.mjs:39-40` header comment is non-load-bearing prose** but ships in the bundle output sometimes (when a Reporter agent reads the script source). Update it to reflect the new 160KB target so the lesson lands in the next Reporter context bundle. Counted in the diff.
- **`cleargate-cli/src/commands/sprint.ts:543-554` wiki-lint seam wraps `wikiLintHandler` in a try/catch that converts the handler's `process.exit(1)` (from non-zero findings) into a thrown `Error("wiki-lint-exit:1")`** (line 546-553). The outer catch at line 727 receives this error. The new waiver block must inspect `stepName === 'wiki lint'`, not the error message — the error message is opaque (`wiki-lint-exit:1`) and not user-facing. Match by step-name, not by error contents.
- **`cleargate-cli/src/commands/sprint.ts:711` checks `wikiInitialised = fs.existsSync(wikiRoot)`** — if `.cleargate/wiki/` does not exist (fresh sprint, no wiki built yet), the entire wiki-build/lint loop is skipped. The waiver flag has NO effect in that case (there's nothing to waive). The Scenario 1 + 2 fixtures must explicitly create the wiki dir for the test to exercise the lint path. Fixture-text gotcha already documented above.
- **Commander.js silently camelCases `--allow-wiki-lint-debt` to `opts.allowWikiLintDebt`** — no manual `optionFlags.replace(/-([a-z])/, ...)` translation needed. Verified by precedent at `cli.ts:331` (`--allow-drift` → `opts.allowDrift`).
- **FLASHCARD `2026-05-01 #wiki-lint #baseline` (line 18):** `cleargate wiki lint` exits 1 even for pre-existing findings; "no regression" gates need a pre/post baseline diff. **This M7 work does NOT solve the baseline-diff problem** — `--allow-wiki-lint-debt` is a blunt all-or-nothing waiver, not a baseline ratchet. The flashcard's resolution is deferred to a future CR (likely CR-028 or later). The waiver flag is a stop-gap so SPRINT-19 close itself does not block on the existing 34 broken-backlink findings.
- **FLASHCARD `2026-05-01 #commit-format #dod` (line 14):** Developers default to `feat(<sprint>):` instead of CR-022 §4.2's required `feat(<epic-or-CR>):`. M7 commit MUST be `feat(CR-022): <story-id> <short desc>`. Surface in the Developer dispatch prompt.
- **FLASHCARD `2026-04-19 #scaffold #mirror #prebuild` (line 9):** `cleargate-cli/templates/cleargate-planning/` is DERIVED. Even under Option A above, the M7 Developer NEVER hand-edits the bundled tree. The canonical tree is the source; `npm run prebuild` rebuilds the bundled tree.
- **FLASHCARD `2026-05-01 #mirror #parity` (line 12):** edit-parity is per-edit, NOT whole-file. If pre-existing divergence exists between live and canonical (e.g. for any peer script that M7 happens to inspect), the M7 Developer does NOT reconcile that divergence. M7 owns ONLY `prep_reporter_context.mjs` + `sprint.ts` + `cli.ts` + the two test files. Hands off everything else.
- **`sprint.ts:518` opts param is positional inline-typed, NOT a named interface.** That means the `allowWikiLintDebt?: boolean` addition lives inline at three sites: (a) the function signature (line 518), (b) the `cli.ts` action arrow (line 364), (c) the `handlerOpts` local in `cli.ts` (line 366). All three must stay in sync. Architect-decision note: refactoring the inline type to a named interface (`SprintArchiveHandlerOptions`) is OUT of scope for fast-lane.
- **Test file `sprint-archive.test.ts` does NOT currently use `wikiBuildFn`/`wikiLintFn` seams** (`grep` returns zero hits). The existing scenarios all use a no-wiki fixture (no `.cleargate/wiki/` dir created), so the lint loop is skipped. Scenarios 1 + 2 are the FIRST tests in `sprint-archive.test.ts` to wire the lint seam. Use `sprint-archive-stamp.test.ts:283-323` as the reference for the pattern, but keep the new tests in `sprint-archive.test.ts` (per sprint plan §2.2 row 102: M7 owns `sprint.ts`, and `sprint-archive.test.ts` is the natural test home).

## Cross-story risks

1. **Mirror-creation decision affects lane classification.** If orchestrator picks Option A (create canonical mirror), M7 LOC budget exceeds 50 — lane reclassifies to `standard`. Sprint plan §2.4 row 122 lists M7 as `fast` with rationale "no schema/runtime change." Adding a 470-LOC canonical mirror IS a runtime-surface change (the bundled tree changes for every future `cleargate init`). Option B keeps the lane intact. **Surface to orchestrator before dev dispatch.**

2. **Scenario 1's wiki-dir fixture leaks into adjacent scenarios.** Vitest reuses `beforeEach` per `it` block, but the sibling scenarios (orphan, v1-inert) DO NOT create a wiki dir today. If Scenario 1's wiki dir is created via `buildFixture()` modification (rather than per-`it`), the orphan + v1 scenarios will start exercising the lint path and may break. **Mitigation:** create the wiki dir INSIDE the `it` block of Scenarios 1 + 2, not in a shared `beforeEach`. Mirror `sprint-archive-stamp.test.ts:283-323` exactly — that test creates the wiki dir per-`it`.

3. **`MAX_BUNDLE_BYTES` constant collides with M1's helper if M1 ALSO exports a constant from `prep_reporter_context.mjs`.** Verified at plan-write time: M1 (per CR-022 §3 M0) extracts `reportFilename()` into `lib/report-filename.mjs` and reorganizes `prep_reporter_context.mjs` to consume it — NO new top-level constants documented. Risk is low but the M7 Developer should re-grep for `^const ` at the top of `prep_reporter_context.mjs` after M1 lands and before M7 edits, to confirm no name collision. If collision, prefix M7's constant: `BUNDLE_MAX_BYTES_M7` (advisory; should not happen).

4. **Test-fixture cross-pollution between `sprint-archive.test.ts` (Scenarios 1+2) and `sprint-archive-stamp.test.ts` (Scenarios 3+4 — existing).** Both test files spawn against the same `sprintArchiveHandler`. The new flag adds a third branch into the wiki-lint catch block. If `sprint-archive-stamp.test.ts:283-323` (existing Scenario 3 — wiki-lint failure rollback) starts behaving differently after M7's edit (it should NOT — it does NOT pass `allowWikiLintDebt`), that's a regression. **Mitigation:** Developer runs `npm test -- sprint-archive` after M7 edits and confirms BOTH `sprint-archive.test.ts` AND `sprint-archive-stamp.test.ts` are green. Scenario 3 in `sprint-archive-stamp.test.ts` is the regression guard for the default-block behavior — it covers Scenario 2 of M7 indirectly.

5. **`cleargate-cli/test/commands/sprint-archive-stamp.test.ts:283-323` already exercises the wiki-lint failure path WITHOUT the new flag.** This is the closest precedent and the clearest regression target. M7 must NOT modify `sprint-archive-stamp.test.ts` — sprint plan §2.2 row 102 states M7 owns ONLY `cleargate-cli/src/commands/sprint.ts`, and by extension the ARCHIVE test files in `test/commands/` ARE M7's territory but the ROLLBACK semantics belong to STORY-015-04 (a SPRINT-15 anchor). Touching the existing rollback test risks reintroducing a previously-resolved fight. **Decision:** new scenarios go in `sprint-archive.test.ts` (the OG file), not in `sprint-archive-stamp.test.ts`. Confirmed sprint-plan-alignment.

## Open decisions for orchestrator

1. **Canonical-mirror decision for `prep_reporter_context.mjs`.** Option A (create the mirror) bumps lane to `standard`; Option B (skip the mirror) preserves `fast` lane and adds a Yellow flag to SPRINT-19 REPORT.md §6 Tooling. **Architect recommendation: Option B.** Reason: keeps the fast-lane budget intact; the mirror gap is a pre-existing scaffold drift unrelated to CR-022 M5's scope; deferring to a SPRINT-20 hotfix is the conventional path. **Confirm before Developer dispatch.**

2. **Story ID for M7.** CR-022 §3 enumerates milestones M0..M6 but does not assign story IDs. CR-022 itself is the parent (CR-as-parent pattern, used elsewhere in SPRINT-18). Orchestrator assigns the story ID at dispatch time. Recommendation: `STORY-022-05` if CR-022 is treated as Epic-shaped, OR keep "CR-022 M5" as the canonical reference and use `feat(CR-022): CR-022-M5 <desc>` in the commit. The latter matches FLASHCARD `2026-05-01 #commit-format #dod`'s `feat(<epic-or-CR>):` directive. **Pin the ID before dispatch so the commit-format gotcha is resolvable.**

3. **Optional vitest unit test for `MAX_BUNDLE_BYTES`.** Architect skipped this (see "Test scenarios" → Scenario 3 → "No new vitest unit test needed"). If orchestrator-side QA convention requires every constant change to ship with an explicit assertion, the M7 Developer can add a 5-LOC vitest in `cleargate-cli/test/scripts/test_prep_reporter_context.test.ts`. Requires `export const MAX_BUNDLE_BYTES = ...` (3-char addition). **Architect recommendation: skip; bash fixture covers regression.** Confirm at dispatch time.

---

role: architect

### M8.md

# Milestone: M8 — Dev STATUS=done structured schema + QA agent lane-aware playbook
## Stories: CR-024 Story 2
## Wave: W2 (parallel — disjoint surfaces vs M4/M5/M6/M7/M9)

> Back-half of CR-024. M2 (merged at `587bdb2`) froze the QA context-bundle schema in `.cleargate/scripts/prep_qa_context.mjs` lines 25-78 with the marker comment `// schema_version: 1 — frozen for SPRINT-19 M8` (line 2) and the hand-off contract `M8 hand-off: M8 (CR-024 S2) consumes this schema. M2 freezes; M8 wires.` (line 85). M8 wires it: the dev agent emits the structured handoff fields the script reads, and the QA agent ingests the resulting pack and dispatches by `lane`.

## Order

Single-story milestone. No internal ordering. Parallelizable with M4, M5, M6, M7, M9 (disjoint file surfaces — confirmed against sprint plan §2.2 lines 91-108: M8 owns `.claude/agents/developer.md` + `.claude/agents/qa.md` exclusively, including against M9 which §2.2 row 101 explicitly excludes from those two files). M9 (CR-025 S2) audits `architect.md` + `reporter.md` + protocol/CLAUDE.md/enforcement; **M8 must NOT touch those five files**, mirror, or any non-`developer.md`/`qa.md` agent definition.

## Per-story blueprint

### CR-024 Story 2 — Dev STATUS=done structured schema + QA agent lane-aware playbook

- **Cross-story coupling:**

  - **M2 (already merged) is the upstream contract.** M8 reads the schema verbatim from the M2 source comment (`prep_qa_context.mjs` lines 25-78). The pack file naming (`.qa-context-<story-id>.md`) and emission point (the orchestrator's QA-spawn moment) are fixed by M2 line 21 (`Output file: <sprint-dir>/.qa-context-<story-id>.md (default)`) and CR-024 §1 line 75 ("Orchestrator's QA spawn prompt collapses to ~5 lines: worktree path + commit SHA + 'read `.qa-context-<story>.md` and run the playbook.'"). M8 MUST cite this filename verbatim in qa.md.
  - **No coupling to other Wave 2 milestones.** M4/M5/M6/M7 touch `close_sprint.mjs` + `suggest_improvements.mjs` + `sprint_trends.mjs` + `prep_reporter_context.mjs` — none of those files are in M8's surface. M9 (CR-025 protocol audit) is also Wave 2 and was pre-grepped per sprint plan §2.2 row 101 to confirm zero `[Pp]roposal` occurrences in `developer.md` or `qa.md` (verify in dev session: `grep -nE "[Pp]roposal" .claude/agents/developer.md .claude/agents/qa.md` must return empty before commit).
  - **Lane semantics handoff to SPRINT-20.** The `runtime` lane is documented in qa.md prose only. State.json schema (`.cleargate/scripts/state.schema.json` if present, plus `validate_state.mjs`) does NOT learn about `runtime` in M8 — that work lands in SPRINT-20 (sprint plan §2.5 informational flag and M2 plan §3.1 line 195 carry-forward). Surface in REPORT.md §6 (see "Out-of-scope hooks" below).

- **Schema changes (verbatim, sourced from `prep_qa_context.mjs` lines 64-77):**

  M2's frozen `dev_handoff` JSON object — M8's developer.md edits must produce a STATUS=done block that parses 1:1 to these fields. **This block is THE contract. Do not paraphrase; cite verbatim in developer.md prose.**

  ```
  "dev_handoff": {
    "format": "structured|legacy|absent",
    "status": "done|blocked|null",
    "commit": "string|null",
    "typecheck": "pass|fail|null",
    "tests": "string|null",
    "files_changed": ["string"],
    "notes": "string|null",
    "r_coverage": [{"r_id": "string", "covered": "boolean", "deferred": "boolean", "clarified": "boolean"}],
    "plan_deviations": [{"what": "string", "why": "string", "orchestrator_confirmed": "boolean"}],
    "adjacent_files": ["string"],
    "flashcards_flagged": ["string"]
  }
  ```

  Three new fields beyond the current developer.md line 38-48 STATUS block: `r_coverage`, `plan_deviations`, `adjacent_files`. The other fields (`status`, `commit`, `typecheck`, `tests`, `files_changed`, `notes`, `flashcards_flagged`) already exist in the current dev contract — keep names and YAML shape unchanged. The `format` enum is computed by the script (`prep_qa_context.mjs` line 517-520: `structured` if `r_coverage:` OR `plan_deviations:` OR `adjacent_files:` is present as a top-level key, `legacy` if any STATUS-block field exists without those three, `absent` otherwise) — devs do NOT emit `format` themselves.

  **Parser contract:** the M2 regex at `prep_qa_context.mjs` lines 506-512 tokenizes the block by uppercase-prefix `STATUS:` / `COMMIT:` / `TYPECHECK:` / `TESTS:` / `FILES_CHANGED:` / `NOTES:`, then lowercase YAML-shaped `r_coverage:` / `plan_deviations:` / `adjacent_files:` / `flashcards_flagged:`. The dev's commit message body or dispatch-payload JSON must match this casing exactly. Specify this in developer.md prose: "STATUS / COMMIT / TYPECHECK / TESTS / FILES_CHANGED / NOTES are uppercase keys; r_coverage / plan_deviations / adjacent_files / flashcards_flagged are lowercase YAML-shaped lists."

- **developer.md edits (live + canonical):**

  Edit the `## Output shape` section (lines 36-50) by extending the fenced block with three new YAML lists AFTER `flashcards_flagged:` (or co-located — the M2 regex does not require ordering, but co-locate for readability). Concretely, the new dev output shape becomes:

  ```
  STORY: STORY-NNN-NN
  STATUS: done | blocked
  COMMIT: <sha> (or "none" if blocked)
  TYPECHECK: pass | fail
  TESTS: X passed, Y failed
  FILES_CHANGED: <list>
  NOTES: <one paragraph max — deviations from plan, flashcards recorded>
  r_coverage:
    - { r_id: "R1", covered: true, deferred: false, clarified: false }
    - { r_id: "R2", covered: false, deferred: true, clarified: false }
  plan_deviations:
    - { what: "<short label>", why: "<one-sentence reason>", orchestrator_confirmed: true }
  adjacent_files:
    - "<absolute or repo-relative path the dev believes may regress>"
  flashcards_flagged:
    - "YYYY-MM-DD · #tag1 #tag2 · lesson ≤120 chars"
  ```

  Add ONE new prose paragraph immediately under the fenced block (between current lines 48 and 50) explaining the three new fields. Suggested wording (Architect-decided, Developer renders verbatim):

  - `r_coverage` — one entry per requirement R1..RN drawn from the story's Gherkin and `## 3. Implementation Guide`. Set exactly one of `covered` (test asserts the requirement), `deferred` (out of this story's scope, flagged for follow-up), or `clarified` (orchestrator confirmation amended the requirement). Default `[]` when the story has zero numbered Rs (rare; flag in NOTES if so).
  - `plan_deviations` — one entry per deviation from the Architect's milestone plan blueprint. Each must include `orchestrator_confirmed: true` (deviation was discussed and agreed) or `false` (dev's unilateral call — QA flags as risk). Default `[]`.
  - `adjacent_files` — repo-relative paths the dev's gut-check thinks may regress from this change but were not directly edited. Default `[]`. The `prep_qa_context.mjs` script independently computes its own adjacent-file set (lines 322-368) from `git diff --name-only` neighborhoods; the dev's list is additive subjective context the script cannot derive.

  **Backwards-compat assertion (CR-024 §4 line 130):** developer.md MUST NOT mark the three new fields as required-to-emit. Existing dev agents (or agents in worktrees with stale `.claude/agents/developer.md`) that omit them produce `dev_handoff.format: "legacy"` per `prep_qa_context.mjs` lines 517-520, and qa.md's lane playbook handles that case explicitly (see qa.md §"Pack-First Ingest" below). Phrase as "Three optional structured-handoff fields. Omitting them yields a `legacy`-format pack which QA still accepts (with a `SCHEMA_INCOMPLETE — context limited` warning)."

- **qa.md edits (live + canonical):**

  Three NEW sections, inserted in this order (placement: after current line 6 frontmatter prose, before current `## Your one job` at line 10 — same insertion-pattern as STORY-025-05's `## Capability Surface` placement on `reporter.md`, which the existing `cleargate-cli/test/agents/reporter-content.test.ts` lines 28-33 asserts).

  1. **`## Capability Surface`** — table mirroring reporter.md's pattern. Six rows:
     - **Scripts:** `.cleargate/scripts/prep_qa_context.mjs` (M2-frozen)
     - **Skills:** `Skill(flashcard, "check")` — first action on spawn
     - **Hooks observing:** SubagentStop (token-ledger attribution)
     - **Default input:** `.cleargate/sprint-runs/<sprint>/.qa-context-<story-id>.md` (read FIRST; spec/plan/diff fall back to source files only when pack is incomplete)
     - **Output:** stdout text matching the existing `## Output shape` schema (line 36 of current qa.md — preserved unchanged)
     - **Lane awareness:** dispatches `fast` / `standard` / `runtime` per `lane.value` field in the pack's JSON block

     This row count (6) matches what `cleargate-cli/test/agents/reporter-content.test.ts` lines 36-58 asserts on reporter.md — for parity the doc-lint test on qa.md may use the same 6-row pattern.

  2. **`## Pack-First Ingest`** — exactly the prose contract for consuming the pack. Required content:
     - First action on spawn (after flashcard check) is `Read(.cleargate/sprint-runs/<sprint>/.qa-context-<story-id>.md)`. Locate sprint dir via `.cleargate/sprint-runs/.active`.
     - The pack contains 8 markdown sections in fixed order: Worktree + Commit / Spec Sources / Baseline / Adjacent Files / Cross-Story Map / Flashcard Slice / Lane / Dev Handoff. Cite verbatim from `prep_qa_context.mjs` lines 849-864 (`bundleParts` array — section ordering is hard-frozen).
     - The pack also embeds a JSON code block (script lines 853-855) with `schema_version: 1`. QA reads either the prose or the JSON; prefer JSON for structured fields (lane, dev_handoff.format, baseline.failures), prose for human-readable summaries.
     - **Pack-absent fallback:** if `.qa-context-<story-id>.md` does not exist (orchestrator skipped the prep step, or worktree path mismatch), emit `QA: FAIL — pack missing at <expected-path>; orchestrator must run prep_qa_context.mjs before QA dispatch` and stop. Do NOT improvise context derivation — that's the failure mode CR-024 was filed to eliminate (CR-024 §1 line 67: "QA prompt is hand-assembled per spawn… brittle (forgotten fields surface as wasted re-derivation or silent false-passes)").
     - **Pack-incomplete handling:** if the JSON block is present but `dev_handoff.format === "legacy"` or `"absent"`, proceed with QA but downgrade verdict confidence — emit a `WARN: dev handoff incomplete — context limited` line in the output `VERDICT` paragraph. Per CR-024 §4 line 130, this is NOT an automatic FAIL.

  3. **`## Lane-Aware Playbook`** — three sub-checklists, dispatched by reading `lane.value` from the pack's JSON block (or the prose `## Lane` section's `**Value:**` line). Required content:

     - **`fast` lane (doc-only / mirror-edit / sub-50-LOC stories):**
       - Mirror-parity diff (`diff -q` between live and canonical files in the dev's `files_changed`).
       - Grep checklist for required strings (e.g., heading anchors, schema field names).
       - DoD §2.2 audit (cross-check the story's Gherkin → diff one-to-one).
       - Spec-vs-impl drift table (one row per requirement).
       - **Skip** typecheck and targeted vitest UNLESS `pack.adjacent.adjacent_test_files` is non-empty AND any of those files are under `cleargate-cli/`, `mcp/`, `cleargate-cli/test/`, or any path with extension `.ts` / `.test.ts` / `.test.sh`. The pack's adjacent-test-file list is computed by `prep_qa_context.mjs` lines 322-368.

     - **`standard` lane (current default — most stories):**
       - Everything in `fast`, PLUS:
       - `cleargate gate typecheck` re-run (capture exit code).
       - `cleargate gate test` re-run, scoped to touched-file neighborhoods (`pack.adjacent.touched_files` + `pack.adjacent.adjacent_test_files`).
       - Adversarial probe (1-2 boundary cases beyond Gherkin: empty input, non-ASCII, oversized payload).
       - Cross-story regression sweep against `pack.cross_story_map[].shared_files` if non-empty.

     - **`runtime` lane (NEW — CLI / integration / runtime-surface stories):**
       - Everything in `standard`, PLUS:
       - Full test suite re-run (not just touched-file scope) — `cleargate gate test` against the full package.
       - Coverage check: every Gherkin scenario has a passing test (zero MISSING entries).
       - CLI exit-code matrix: invoke each new/modified command with `--help`, the happy path, and at least one explicit error path; assert exit codes match documented values.
       - Integration smoke: if the story changes a script under `.cleargate/scripts/`, run the script's bash test harness from a `mktemp -d` fixture (mirrors test_prep_qa_context.sh pattern at `.cleargate/scripts/test/`).

     - **Forward-compat clause (mandatory prose):** "If the pack's `lane.value` is any string other than `fast` / `standard` / `runtime`, treat it as `standard`. The state.json schema does not yet know about `runtime` (SPRINT-20 work); QA must not error on lane mismatch." Cite `prep_qa_context.mjs` line 491 + line 498 — the script defaults to `standard` if state.json is absent or the field is missing, but a future state.json with an unknown lane value (e.g., the SPRINT-20 schema introduces `experimental`) must not break QA.

     - **Lane-source hint:** if `pack.lane.source === "not-yet-runtime-aware"` (heuristic the script emits when story is `standard` but touches `cleargate-cli/src/commands/`, per `prep_qa_context.mjs` lines 486-490), the playbook says: "Apply `standard` checks BUT add the `runtime` exit-code matrix as a soft check. Surface any deviations as `WARN`, not `FAIL`."

  Do NOT modify the existing sections of qa.md (lines 10-62). Specifically: `## Your one job` (line 10), `## Inputs` (line 13), `## Workflow` (line 18), `## Output shape` (line 36), `## Guardrails` (line 51), `## What you are NOT` (line 58) all remain unchanged. The new sections are PURELY ADDITIVE inserts before line 10.

- **Test scenarios (acceptance, drawn from CR-024 §4 lines 127-130 + sprint plan §0 line 49 metric):**

  Test file: `cleargate-cli/test/agents/qa-content.test.ts` (NEW). Pattern: doc-lint vitest matching `cleargate-cli/test/agents/reporter-content.test.ts` (179 lines, 6 describe blocks, READS the canonical agent .md file via `path.resolve` from `import.meta.url`, falls back to canonical for live-file-absent worktree case at lines 165-172). Test runs from worktree without npm install thrash because the test reads files only — no runtime imports of `.claude/agents/`.

  Six describe blocks, one per Gherkin scenario:

  1. **Scenario 1 — qa.md has `## Capability Surface` section.** Mirrors reporter-content.test.ts lines 28-58. Assert: exactly one `^## Capability Surface$` heading, six row labels present (`**Scripts**` / `**Skills**` / `**Hooks observing**` / `**Default input**` / `**Output**` / `**Lane awareness**`).

  2. **Scenario 2 — Capability Surface table cites the M2 contract.** Slice from `## Capability Surface` to next `## ` heading; assert it contains `prep_qa_context.mjs` AND `.qa-context-<story-id>.md` AND `schema_version: 1`. Mirrors reporter-content.test.ts lines 61-89.

  3. **Scenario 3 — qa.md has `## Pack-First Ingest` section.** Assert exactly one `^## Pack-First Ingest$` heading; the section contains the strings `pack missing` (FAIL clause) and `SCHEMA_INCOMPLETE` (legacy clause). Mirrors reporter-content.test.ts lines 91-101.

  4. **Scenario 4 — qa.md has `## Lane-Aware Playbook` with three lane sub-headings.** Assert exactly one `^## Lane-Aware Playbook$` heading; the section contains substrings `**\`fast\` lane**`, `**\`standard\` lane**`, `**\`runtime\` lane**` (or equivalent — agree on exact bullet/sub-heading shape at write-time, but assert all three lane names appear as standalone bold tokens). Assert the runtime sub-section contains `Full test suite` AND `exit-code matrix` AND `Integration smoke`.

  5. **Scenario 5 — Forward-compat clause present.** Assert the playbook section contains the exact string `treat it as \`standard\`` (forward-compat) AND a reference to `state.json schema` (SPRINT-20 carry-forward).

  6. **Scenario 6 — Mirror parity over inserted sections.** Slice canonical and live qa.md from `## Capability Surface` to `## Your one job`; assert byte-equality. Mirrors reporter-content.test.ts lines 151-178 (including the worktree-context fallback at lines 167-172 — read canonical for both sides if live `.claude/agents/qa.md` is absent due to gitignored `.claude/`).

  Plus a SEVENTH assertion attached to one of the describes (per CR-024 §3 line 111) — **developer.md schema-fields presence test:**

  7. **Scenario 7 — developer.md `## Output shape` block lists the three new structured fields.** Assert the canonical `developer.md` (`cleargate-planning/.claude/agents/developer.md`) contains the strings `r_coverage:`, `plan_deviations:`, `adjacent_files:` AND that all three appear within the same fenced code block as `STATUS:` (slice `^## Output shape$` to next `^## `; assert all four tokens fall in this slice). This locks the schema to the dev's output shape, not random prose.

  Total: 7 scenarios. Mirrors the 6-scenario shape of the existing reporter-content.test.ts but adds the dev-schema cross-assertion because M8 spans two files.

  **Path resolution.** Use the same `REPO_ROOT = path.resolve(new URL(import.meta.url).pathname, '..', '..', '..', '..')` pattern (reporter-content.test.ts line 13 — four levels up from `test/agents/`).

- **Reuse (no duplication):**

  - **`prep_qa_context.mjs` already exists at the live + canonical mirror paths.** M8 does NOT modify the script. Cite line 2 (`schema_version: 1 — frozen for SPRINT-19 M8`), lines 25-78 (JSON schema), line 85 (hand-off contract) verbatim in the prose.
  - **`reporter-content.test.ts`** (`cleargate-cli/test/agents/reporter-content.test.ts`, 179 lines) — full doc-lint pattern template. Copy the file structure: imports, REPO_ROOT computation, CANONICAL/LIVE path constants, describe blocks, slice helpers (`getCapabilitySurfaceSlice` at line 63, `extractInsertedSections` at line 152). Do NOT re-derive these helpers; copy and rename for `qa.md`.
  - **Agent definition mirror invariant.** Live: `.claude/agents/{developer,qa}.md`. Canonical: `cleargate-planning/.claude/agents/{developer,qa}.md`. Both pairs are byte-identical pre-M8 (verified via `diff -u` 2026-05-02 — both pairs return zero output). M8 edits both files in BOTH locations within the same commit. **Per FLASHCARD line 13 (`#mirror #parity` 2026-05-01), edit-parity invariant applies per-edit, not whole-file** — even though the files happen to be identical right now, the invariant means "the bytes you write to live go also into canonical at the same time," not "reconcile any pre-existing canonical-only or live-only divergence." There is no pre-existing divergence to reconcile here, but stay aware.

- **Gotchas surfaced from code inspection (file:line citations only):**

  - **`prep_qa_context.mjs` lines 506-512** — the M2 regex parses STATUS-block fields by exact uppercase prefix (`STATUS:` / `COMMIT:` / `TYPECHECK:` / `TESTS:` / `FILES_CHANGED:` / `NOTES:`). developer.md MUST NOT lowercase these labels. The current developer.md line 39-44 already uses uppercase — preserve.
  - **`prep_qa_context.mjs` lines 517-520** — the `format: structured` enum requires AT LEAST ONE of `r_coverage:` / `plan_deviations:` / `adjacent_files:` to be present (regex `r_coverage\s*:`). A dev who emits all three as `[]` (empty YAML lists with the keys present) STILL counts as `structured` because the regex matches the key, not the value. developer.md prose must clarify: "emit the three keys with `[]` if the lists are empty; do NOT omit the keys, that demotes the pack to `legacy`."
  - **`prep_qa_context.mjs` line 491 + 498** — the script falls back to `lane: standard` if state.json is absent (`{ value: 'standard', source: 'default' }`). qa.md's playbook must handle this default explicitly — when `lane.source === 'default'`, use the `standard` lane checks. State.json absence is a normal case for one-off / off-sprint stories.
  - **`prep_qa_context.mjs` lines 486-490** — the heuristic `not-yet-runtime-aware` fires when `lane === 'standard'` AND any touched file starts with `cleargate-cli/src/commands/`. qa.md's playbook must consume this hint as a soft signal (apply `standard` checks but add `runtime` exit-code matrix as WARN-only, not FAIL).
  - **`reporter-content.test.ts` lines 165-172** — the live `.claude/agents/reporter.md` is gitignored and may not exist in a worktree. The test silently falls back to canonical for both sides, making mirror-parity test trivially pass in worktree context. Replicate this fallback in `qa-content.test.ts` lines for Scenario 6 — without it, vitest in a worktree errors on `ENOENT: .claude/agents/qa.md`.
  - **`developer.md` line 30 + `qa.md` line 33** — both reference `feat(<epic>): <story-id>` commit format. M8's commit will be `feat(CR-024): SPRINT-19 M8 dev STATUS=done structured schema + qa.md lane playbook` per FLASHCARD line 16 (`#commit-format #dod` 2026-05-01) which observed three SPRINT-17 deviations. Use `feat(CR-024)` not `feat(SPRINT-19)`.
  - **FLASHCARD line 8 (`#scaffold #mirror #prebuild` 2026-05-01)** — `cleargate-cli/templates/cleargate-planning/.claude/agents/{developer,qa}.md` is DERIVED, rebuilt by `copy-planning-payload.mjs` on every prebuild. **Do NOT hand-edit those files.** M8 only edits `cleargate-planning/.claude/agents/{developer,qa}.md` (canonical) + `.claude/agents/{developer,qa}.md` (live). The cli-bundled tree updates automatically when SPRINT-19 closes and a release builds.
  - **FLASHCARD line 38 (`#worktree #git #commit` 2026-05-02)** — Dev agents have landed commits on `main` instead of `story/<id>` worktree branches. M8's developer must verify with `git log story/CR-024-S2 --oneline` (or whatever branch the orchestrator assigns) AFTER commit, not just trust commit-success output. Architect surfaces this; Developer enforces.
  - **FLASHCARD line 17 (`#qa #vitest #npx` 2026-05-01)** — `npx ETIMEDOUT` in QA shells. The new `qa-content.test.ts` runs as a vitest unit test (no npx invocation) so this does not bite directly, but if QA reruns the doc-lint test from a fresh shell, use `node_modules/.bin/vitest cleargate-cli/test/agents/qa-content.test.ts` directly, not `npx vitest`.

## Cross-story risks

Wave 2 disjointness is the central risk-control here. Specific things a Developer working only on M8 might miss:

1. **M9 (CR-025 S2 protocol audit) is also Wave 2.** Sprint plan §2.2 row 101 says `developer.md` + `qa.md` are pre-grepped (zero `[Pp]roposal` hits) and excluded from M9 scope. **Verify in dev session before commit:** `grep -nE "[Pp]roposal" .claude/agents/developer.md .claude/agents/qa.md cleargate-planning/.claude/agents/developer.md cleargate-planning/.claude/agents/qa.md` must return empty. If a `Proposal` reference creeps in via M8's edits (e.g., the dev cites the old PROPOSAL artifact in prose), it bleeds into M9's audit scope and triggers a merge collision after Wave 2 — fix at M8 commit time, not after.

2. **The `runtime` lane is NOT yet a state.json-registered value.** Sprint plan §2.5 informational flag + M2 plan line 195 already note this as a SPRINT-20 deferral. M8's qa.md prose introduces the term in the agent contract; future dev work setting `lane: runtime` directly in state.json may trip `validate_state.mjs`. **The qa.md forward-compat clause (Scenario 5 above) is the ONLY mitigation for SPRINT-19**; the actual schema update (state.schema.json + validator + sprint-plan template lane column) is SPRINT-20 carry-forward. Surface in REPORT.md §6 explicitly.

3. **Backwards-compat for in-flight QA shells.** If a SPRINT-19 story's QA was already dispatched against the old qa.md (no Lane-Aware Playbook, no Pack-First Ingest) and is still running when M8 lands, that QA shell completes against its own (stale) qa.md copy in its own worktree. No action needed — qa.md is a per-spawn read, and stale shells don't auto-refresh. SPRINT-19 stories AFTER M8 merge see the new qa.md. **No retroactive backfill** (matches CR-024 §2 line 85: "Existing in-flight stories continue with old QA flow until SPRINT-19 ships").

4. **Test-fixture precedent.** `cleargate-cli/test/agents/reporter-content.test.ts` is the ONLY existing file in `test/agents/`. M8 adds the second. Future M8-style work on architect.md will likely add a third (`architect-content.test.ts`). M8 should NOT factor out a shared helper from reporter-content.test.ts — keep the slice helpers duplicated for now (the helpers are 5-7 lines each; abstraction premature). **Architectural decision (M8 final):** copy-paste the three slice helpers (`getCapabilitySurfaceSlice` at reporter-content.test.ts:63, `extractInsertedSections` at line 152) into qa-content.test.ts. Do not extract to a shared module.

5. **No orchestrator wiring change in M8.** The orchestrator's QA-spawn prompt (which today is hand-assembled per CR-024 §1 obsolete-logic line 67) MUST be updated to emit `read .qa-context-<story-id>.md and run the playbook` per CR-024 §1 line 75. **That orchestrator-prompt update is NOT in M8's scope** — it's an orchestrator-prompt change (the human's CLAUDE.md / sprint-execution prose) outside the four-agent loop. Surface in REPORT.md §6 as a manual follow-up the human applies post-SPRINT-19. M8 ships the agent-side contract; the orchestrator-side dispatch update is conversational-agent guidance, not a code edit.

## Out-of-scope hooks (carry-forward to REPORT.md §6)

- **State.json schema for `runtime` lane.** SPRINT-20 picks this up. Per sprint plan §2.5 informational flag — `validate_state.mjs` + `state.schema.json` (if it exists) need to learn `runtime` as a valid `lane` value. M8's forward-compat clause in qa.md prevents agent-side breakage; the schema update is human follow-up.
- **Orchestrator QA-spawn prompt.** The conversational agent's prompt template ("read `.qa-context-<story>.md` and run the playbook") is documented in CR-024 §1 line 75 as the new minimal shape but is NOT a code edit — it's CLAUDE.md / orchestrator prose. Surface as a SPRINT-20 doc refresh item OR a manual SPRINT-19 close-time application.
- **Sprint Plan Template `lane` column.** If the sprint-plan template (`.cleargate/templates/Sprint Plan Template.md`) lists allowed lane values, it must add `runtime`. Out of M8 scope; flag for SPRINT-20.

## DoD (from CR-024 §4 + sprint plan §1 row 103)

- [ ] `.claude/agents/developer.md` `## Output shape` block extended with `r_coverage:` / `plan_deviations:` / `adjacent_files:` YAML lists. Field names match `prep_qa_context.mjs` lines 64-77 verbatim.
- [ ] `.claude/agents/qa.md` gains three new sections (`## Capability Surface`, `## Pack-First Ingest`, `## Lane-Aware Playbook`) inserted before `## Your one job`. Existing sections unchanged.
- [ ] Live `.claude/agents/{developer,qa}.md` and canonical `cleargate-planning/.claude/agents/{developer,qa}.md` are byte-identical post-edit (`diff -u` returns empty for both pairs). Same commit.
- [ ] New test file `cleargate-cli/test/agents/qa-content.test.ts` ships with 7 scenarios (6 + dev-schema cross-assertion). All pass via `node_modules/.bin/vitest cleargate-cli/test/agents/qa-content.test.ts` from `cleargate-cli/`.
- [ ] Existing `cleargate-cli/test/agents/reporter-content.test.ts` continues to pass (no regression).
- [ ] `cleargate gate typecheck` clean from `cleargate-cli/`.
- [ ] `cleargate gate test` clean from `cleargate-cli/` for the `test/agents/` scope.
- [ ] Pre-grep verify: `grep -nE "[Pp]roposal" .claude/agents/developer.md .claude/agents/qa.md cleargate-planning/.claude/agents/developer.md cleargate-planning/.claude/agents/qa.md` returns empty (preserves M9 disjointness).
- [ ] Single commit: `feat(CR-024): SPRINT-19 M8 dev STATUS=done structured schema + qa.md lane playbook`. One commit per story (developer.md §6 line 30 contract).
- [ ] Hand-off note in commit body: `Wires the M2-frozen schema (prep_qa_context.mjs schema_version: 1). Pack-first QA + lane-aware playbook live. Forward-compat: unknown lane values treated as standard until SPRINT-20 lands the state.json schema update.`
- [ ] State.json carry-forward note recorded in REPORT.md §6 (SPRINT-20 schema update for `runtime` lane; orchestrator-prompt update for QA spawn).

## Open decisions for orchestrator

1. **Insertion placement of new qa.md sections — before `## Your one job` (line 10) vs. after `## What you are NOT` (line 58) vs. interleaved.** Architect picks: BEFORE `## Your one job`, matching reporter.md's STORY-025-05 placement pattern (the existing `cleargate-cli/test/agents/reporter-content.test.ts` line 152-159 hard-codes that slice). Confirm with orchestrator before dev dispatch in case the human prefers append-style. **Recommendation: BEFORE `## Your one job`.** No further decision needed unless human objects.

2. **Whether to also add the same three structured-handoff fields to `cleargate-planning/.claude/agents/developer.md` (canonical) WITHOUT a runtime announcement.** Architect picks: yes, ship both live + canonical in same commit per FLASHCARD `#mirror` invariant. No standalone canonical-only commit. Confirmed in DoD above. **No decision needed.**

3. **Whether `qa-content.test.ts` should also assert that developer.md does NOT mark the new fields as required (i.e., backwards-compat assertion).** Architect picks: NO additional test for the negation — Scenario 7 already asserts presence-in-block; backwards-compat is enforced at the script-parser level (`prep_qa_context.mjs` lines 517-520) which is M2's already-tested surface. Adding a "field is documented as optional" doc-lint test on developer.md prose is over-fit. **No decision needed.**

4. **Lane override at QA-time.** Should qa.md's playbook allow QA to override the pack's `lane.value` (e.g., orchestrator passes `--lane runtime` despite state.json saying `standard`)? Architect picks: NO — the pack is the source of truth. If the human wants a different lane for a specific QA, they update state.json before invoking `prep_qa_context.mjs`. Adding a CLI flag is scope creep into M2's already-frozen surface. **No decision needed.**

role: architect

## Git Log Digest

Git log unavailable for sprint/S-19 (no commits or branch not found).

## Token Ledger Digest

**Total:** 128,591,852 tokens (input: 19,064 / output: 509,341 / cache_read: 126,438,530)

**Per-agent breakdown:**
- architect: 126,946,654 (26 dispatches)
- developer: 1,645,198 (1 dispatches)


## Flashcard Slice

_Entries within sprint window [2026-05-02 → 2026-05-15]:_

2026-05-02 · #worktree #git #commit · Dev agent's `git commit` landed on `main` instead of `story/<id>` branch — verify post-dispatch with `git log story/<id>` not just commit-success-claim.
2026-05-02 · #vitest #ram #parallel-agents · vitest maxForks cap is PER-PROCESS — N parallel agents × maxForks = total forks (e.g. cap=2 + 3-agent wave = 6 forks ≈ 2.4GB). Lower VITEST_MAX_FORKS=1 or serialize agents on tight-RAM laptops.
2026-05-02 · #vitest #ram #pool · Cap forks pool via vitest.config.ts `poolOptions.forks.maxForks=2` — CLI flag `--pool-options.forks.maxForks=N` collides with tinypool minThreads validation when pool=forks.

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
