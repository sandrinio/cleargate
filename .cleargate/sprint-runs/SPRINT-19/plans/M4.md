# Milestone: M4 — CR-022 M1 Step 2.7 worktree-closed pre-close check
## Stories: CR-022 M1 (anchor in sprint plan §1 row 6 — "Step 2.7 worktree-closed pre-close check")
## Wave: W2 (parallel — runs concurrently with M5 / M6 / M7 / M8 / M9 over disjoint surfaces; sequential against M5+M6+M11 inside `close_sprint.mjs` per sprint plan §2.2)

role: architect

## Order

CR-022 M1 is the only unit in M4. Single Developer agent executes top-to-bottom:

1. **Pre-flight read.** Re-grep `.cleargate/scripts/close_sprint.mjs` lines 260–315 to confirm Step 2.6 closer + Step 3 opener boundary still match the line numbers cited in §"Per-story blueprint > Insert location" below. The file is currently 448 LOC at HEAD post-M0 (commit 9603f05 byte-identical, verified via `diff` 2026-05-01).
2. Insert Step 2.7 block in `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs` per the §"Per-story blueprint > Insert location" + §"Function shape" specs below.
3. Insert byte-identical Step 2.7 block in `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (canonical mirror).
4. Run `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` — must return empty before commit.
5. Add Step-2.7 scenarios to `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` (extend existing suite — see §"Test fixture path"). Two scenarios minimum: (a) leftover-worktree blocks under v2; (b) clean state passes.
6. Run `npm --prefix cleargate-cli run prebuild` to regenerate `cleargate-planning/MANIFEST.json` SHA for `.cleargate/scripts/close_sprint.mjs` (FLASHCARD `2026-05-01 #manifest #prebuild`).
7. Run `npm --prefix cleargate-cli run typecheck` and `npm --prefix cleargate-cli test` — both green before commit.
8. Single commit. Format per CR-022 §4.2: `feat(CR-022): STORY-022-M1 — Step 2.7 worktree-closed pre-close check` (note: CR-022 §4.2 line 183 shows `feat(EPIC-???-or-CR-022): <story-id> <short desc>`; CR-022 has no parent EPIC so the scope is `CR-022`; FLASHCARD `2026-05-01 #commit-format #dod` flags Developers default to `feat(<sprint>):` — Developer must use `feat(CR-022):`, NOT `feat(SPRINT-19):`).

**Strict ordering rule:** Steps 2 and 3 must land in the SAME commit. Live-only OR canonical-only edits fail FLASHCARD `2026-05-01 #mirror #parity` per-edit invariant. The prebuild regen at step 6 also belongs in the same commit per FLASHCARD `2026-05-01 #manifest #prebuild`.

**Parallelizability with sibling Wave 2 milestones:**
- **M5 (CR-022 M2 Step 2.8 sprint-merged-to-main verify)** — touches `close_sprint.mjs` at a DIFFERENT step boundary (after M4's Step 2.7, before Step 3). Sprint plan §2.2 row "M4 → M5 → M6 → M11" mandates **M4 lands first**; M5's Architect plan must cite line numbers AFTER M4's insert. **Hand-off note for M5 Architect:** post-M4 the Step 2.7 block ends at line ~360 (estimated; M5 must re-grep at planning time). M5 inserts between M4's Step 2.7 closing comment and the existing Step 3 opener (currently line 316; post-M4 will shift by the M4 block's LOC, ~50 lines).
- **M6 (CR-022 M3 Steps 6.5/6.6/6.7)** — inserts after Step 6 (line 419-ish at HEAD; far below Step 2.7). Disjoint section, but waits on M5 to land first per sprint plan §2.2 to keep merge sequencing clean.
- **M7 (CR-022 M5 cap-raise + flag)** — touches `prep_reporter_context.mjs` and `cleargate-cli/src/commands/sprint.ts`. Disjoint from `close_sprint.mjs`.
- **M8 (CR-024 S2)** — disjoint, dev-doc updates.
- **M9 (CR-025 S2)** — disjoint, protocol audit.

## Per-story blueprint

### CR-022 M1 — Step 2.7 worktree-closed pre-close check

**Cross-story coupling:**
- M5 (Step 2.8) inserts immediately after M4's Step 2.7 in close_sprint.mjs. Both are pre-close blocks under the same v2 enforcement model. M5 reads the same `state.execution_mode` flag M4 sets up (see §"Function shape" advisory-vs-enforcing logic below) — M5's Architect should pattern-match.
- M11 (CR-022 M4 Step 8 verbose handoff list) is the FINAL stdout block in close_sprint.mjs. Disjoint section, but inherits the v2-style step-numbering convention M4 establishes.
- The new lib helper `lib/report-filename.mjs` (M0, shipped in commit 9603f05) is **NOT** consumed by M4. Step 2.7 does not read or write the report file. The helper is in scope for M5/M6 (which echo the SPRINT-<#>_REPORT.md filename in user-facing stderr) and remains the canonical naming source elsewhere in the file. M4's Step 2.7 stderr messages reference `.worktrees/STORY-*` paths, not the report.
- The existing `.worktrees/STORY-*` discovery pattern is **already implemented** at `cleargate-cli/src/commands/sprint.ts:931-970` (function `checkNoLeftoverWorktrees`, used by `cleargate sprint preflight`). M4 mirrors that pattern in `.mjs` — see §"Reuse" below.

**Schema changes:** None. No state.json or sprint frontmatter changes. CR-022 §2 line 115 confirms: *"No schema/migration impact on state.json or sprint frontmatter."*

**Test scenarios (from CR-022 §4.3 Gherkin scenario "Step 2.7 blocks on leftover worktree" + DoD §4.1 "Steps 2.7 ... at least one happy-path + one failure-mode test per new step"):**

1. **Scenario "Step 2.7 blocks on leftover worktree under v2"** — Given a v2 sprint with all stories Done AND a `.worktrees/STORY-NNN-NN/` directory present in REPO_ROOT, when `close_sprint.mjs SPRINT-19 --assume-ack` runs, then exit code is 1 AND stderr contains `Step 2.7 failed: leftover worktree at .worktrees/STORY-NNN-NN` AND state.json `sprint_status` stays `Active` (NOT flipped to Completed). Maps to CR-022 §4.3 Gherkin "Scenario: Step 2.7 blocks on leftover worktree".
2. **Scenario "Step 2.7 happy path — no leftover worktrees"** — Given a v2 sprint with all stories Done AND no `.worktrees/STORY-*` paths, when `close_sprint.mjs SPRINT-XX --assume-ack` runs, then stdout contains `Step 2.7 passed: no leftover worktrees.` AND the pipeline proceeds past Step 2.7 (Step 2.6 reconcile-skipped + Step 3 prefill messages also appear).
3. **Scenario "Step 2.7 advisory under v1 (warn + continue)"** — Given a v1 sprint (`schema_version: 1` OR `execution_mode: "v1"` in state.json) AND a leftover worktree, when close_sprint.mjs runs, then stderr contains `Step 2.7 warning: leftover worktree at .worktrees/STORY-NNN-NN (advisory in v1)` AND exit code is 0 (or whatever the rest of the pipeline returns) AND the pipeline proceeds past Step 2.7. Maps to CR-022 §3 M1 line 130: *"v2 enforcing; v1 advisory (warn + continue)."*
4. **Scenario "Step 2.7 skipped when git worktree list unavailable"** — Given `git worktree list --porcelain` exits non-zero (e.g. tmpdir is not a git repo — applies to ALL existing test fixtures since they spawn close_sprint against tmpdir-based sprint dirs, not real git working trees), when close_sprint.mjs runs, then stdout contains `Step 2.7 skipped: git worktree list unavailable (non-fatal).` AND exit code is 0. **CRITICAL** — without this skip, ALL existing test scenarios in `test_close_sprint_v21.test.ts` would fail because they run against fresh tmpdirs (per `makeTempSprintDir` at line 70-101). This is the same fail-open pattern used at `sprint.ts:940-943` (`return { ..., skipped: true, message: 'skipped (git worktree list unavailable)' }`).

The 4 scenarios cover the v2 happy + failure paths AND v1 advisory AND the skip path. CR-022 §4.1 requires **"at least one happy-path + one failure-mode test per new step"** — M4 ships 2 v2 + 1 v1 advisory + 1 skip = 4 scenarios.

**Reuse (no duplication):**
- **`cleargate-cli/src/commands/sprint.ts:931-970` `checkNoLeftoverWorktrees(cwd, execFn)`** — the existing TypeScript implementation in the `cleargate sprint preflight` command. M4 mirrors this exact pattern in `.mjs`. Specifically:
  - The `git worktree list --porcelain` invocation (line 939).
  - Line-by-line parsing for `worktree <path>` prefix (line 947-957).
  - The regex `/[/\\]\.worktrees[/\\]STORY-/.test(wtPath)` (line 952) to match `.worktrees/STORY-*` paths cross-platform.
  - The regex `/(\.(worktrees)[/\\]STORY-.+)$/.exec(wtPath)` (line 954) to extract a relative-looking display path for stderr.
  - The skip-on-failure pattern at line 940-943 (try/catch around `execFn`, return `{ pass: true, skipped: true }` if git fails).
  - **However:** M4 does NOT import from sprint.ts (it's a TypeScript file in a different package; close_sprint.mjs is plain `.mjs` in `.cleargate/scripts/`). M4 inlines the logic — ~25 LOC. This is acceptable duplication: the two implementations live in different package boundaries (CLI vs scripts) and the canonical home for Gate 4 close-time checks is close_sprint.mjs. Future consolidation belongs to a separate CR.
- **`execSync` from `node:child_process`** — already imported at line 39 of close_sprint.mjs. Reuse.
- **`REPO_ROOT` constant** — already declared at line 68 of close_sprint.mjs. Reuse for the `cwd` argument to `execSync('git worktree list --porcelain', { cwd: REPO_ROOT })`. **Critical:** the `git worktree list` call MUST use `REPO_ROOT` as cwd, NOT `sprintDir` — sprint runs land in `.cleargate/sprint-runs/SPRINT-NN/` which is a subdir, not the git toplevel. Running `git worktree list` from REPO_ROOT (the actual git root) returns the correct list. Tests run against tmpdirs which are not git repos — the skip path (Scenario 4 above) handles that.
- **State-shape access for v1/v2 detection** — pattern already used at line 198: `const isV2 = (state.schema_version || 1) >= 2;`. M4 reuses this exact expression. **However:** Step 2.5 uses `isV2` AND `hasFastLane` (because v2.1 validation only fires for sprints with at least one fast-lane story); Step 2.7 must fire for ALL v2 sprints regardless of lane. Use `isV2` alone for the v2-vs-v1 gate. SPRINT-19 has `execution_mode: "v2"` in state.json (line 4); M4 reads `state.schema_version` (which is 2 for SPRINT-19 per state.json line 2). NOTE: state.json has BOTH `schema_version: 2` and `execution_mode: "v2"`. The existing code uses `schema_version` exclusively at line 198 — M4 keeps that pattern for consistency. (Architect note: `execution_mode` is the sprint frontmatter field; `schema_version` is the state.json field. They drift only if a v2 sprint had a v1 state.json — possible during migration, but the migrate path at line 51-65 handles it.)

**Insert location (line numbers verified at HEAD 2026-05-01, post-M0 commit 9603f05):**

`close_sprint.mjs` line layout in the relevant region (HEAD = 448 LOC):

| Line | Content |
|---|---|
| 260 | `// ── Step 2.6: Lifecycle Reconciliation (CR-017) ──────────────────────────` |
| 265 | `process.stdout.write('Step 2.6: running lifecycle reconciliation...\n');` |
| 298 | `process.stdout.write('Step 2.6 passed: lifecycle reconciliation clean.\n');` |
| 309 | `process.stdout.write('Step 2.6 skipped: CLI binary not found at cleargate-cli/dist/cli.js (non-fatal).\n');` |
| 311–314 | catch block for unexpected step 2.6 errors |
| **315** | **blank line — END of Step 2.6 block** |
| **316** | **`// ── Step 3: Invoke prefill_report.mjs ─────────────────────────────────────`** |
| 317 | `process.stdout.write('Step 3: running prefill_report.mjs...\n');` |

**Insert range: between line 314 (end of Step 2.6 catch block) and line 316 (Step 3 opener comment).** The new Step 2.7 block lives at lines 315–~360 post-edit (estimated ~45 lines including comment header + the function call + the v2/v1 branch + skip handling). The blank line currently at 315 is consumed; a new blank line at the end of Step 2.7 separates it from Step 3.

**Surrounding step boundary text to cite verbatim in the implementation:**
- The `// ── Step 2.6: ...` opener at line 260 follows a `// ──` decorator — Step 2.7 MUST use the same decorator style: `// ── Step 2.7: Worktree-Closed Check (CR-022 M1) ──────────────────────────`.
- The `process.stdout.write('Step 2.X: ...');` opener pattern at line 265 / 317 / 329 — Step 2.7 MUST use the same pattern: `process.stdout.write('Step 2.7: checking for leftover worktrees...\n');`.
- The `process.stdout.write('Step 2.X passed: ...');` pass message pattern at line 298 / 335 — Step 2.7 MUST use the same pattern: `process.stdout.write('Step 2.7 passed: no leftover worktrees.\n');`.
- The `process.stdout.write('Step 2.X skipped: ...');` skip message pattern at line 309 — Step 2.7 MUST use the same pattern: `process.stdout.write('Step 2.7 skipped: git worktree list unavailable (non-fatal).\n');`.
- The `process.stderr.write(\`Step 2.X warning: ...\`);` warn pattern at line 313 — Step 2.7 MUST use the same pattern for v1 advisory mode AND for unexpected errors.
- The `process.exit(1)` block + 4-line stderr message at line 301-306 (for Step 2.6 fail) — Step 2.7 MUST use the same pattern for v2-mode block: stderr lists leftover worktrees + exits 1.

**Function shape (inline in close_sprint.mjs — NOT a separate exported helper):**

```javascript
// ── Step 2.7: Worktree-Closed Check (CR-022 M1) ──────────────────────────
// Block close if any .worktrees/STORY-* path is present.
// v2 enforcing (exit 1); v1 advisory (warn + continue).
// Skip if git worktree list is unavailable (non-fatal — tests run against tmpdirs).
process.stdout.write('Step 2.7: checking for leftover worktrees...\n');
{
  let leftoverWorktrees = [];
  let worktreeListAvailable = true;
  try {
    const output = execSync('git worktree list --porcelain', {
      cwd: REPO_ROOT,
      encoding: 'utf8',
    });
    for (const line of output.split('\n')) {
      const trimmed = line.trim();
      if (!trimmed.startsWith('worktree ')) continue;
      const wtPath = trimmed.slice('worktree '.length);
      if (/[/\\]\.worktrees[/\\]STORY-/.test(wtPath)) {
        const m = /(\.(worktrees)[/\\]STORY-.+)$/.exec(wtPath);
        leftoverWorktrees.push(m ? m[1] : wtPath);
      }
    }
  } catch {
    worktreeListAvailable = false;
  }

  if (!worktreeListAvailable) {
    process.stdout.write('Step 2.7 skipped: git worktree list unavailable (non-fatal).\n');
  } else if (leftoverWorktrees.length === 0) {
    process.stdout.write('Step 2.7 passed: no leftover worktrees.\n');
  } else if (isV2) {
    // v2 enforcing — block close
    process.stderr.write(
      `close_sprint: Step 2.7 failed: leftover worktree at ${leftoverWorktrees[0]}\n` +
      `  ${leftoverWorktrees.length === 1 ? '' : `(plus ${leftoverWorktrees.length - 1} more)\n  `}` +
      `Run \`git worktree remove ${leftoverWorktrees[0]}\` if abandoned, or merge the work in progress.\n` +
      `  All worktrees must be closed before sprint close.\n`
    );
    process.exit(1);
  } else {
    // v1 advisory — warn + continue
    process.stderr.write(
      `Step 2.7 warning: leftover worktree at ${leftoverWorktrees[0]} (advisory in v1).\n`
    );
  }
}
```

**Estimated LOC: ~45 lines (matches CR-022 §3 M1 line 128 estimate of "~40 LOC").**

**Pass/fail/skip behavior summary:**

| State | Worktree state | Stdout | Stderr | Exit code |
|---|---|---|---|---|
| v2 + no leftovers | clean | `Step 2.7 passed: no leftover worktrees.\n` | (none) | propagates from later steps |
| v2 + leftover | dirty | `Step 2.7: checking for leftover worktrees...\n` | 4-line block listing worktree + remove hint | **1** (process.exit(1)) |
| v1 + leftover | dirty | `Step 2.7: checking for leftover worktrees...\n` | 1-line warning | propagates from later steps |
| any + git unavailable | n/a | `Step 2.7 skipped: git worktree list unavailable (non-fatal).\n` | (none) | propagates from later steps |

**`isV2` reference:** `isV2` is defined at line 198 as `const isV2 = (state.schema_version || 1) >= 2;`. **CRITICAL:** the Step 2.5 block at lines 196-258 declares `isV2` inside a `const` at line 198 — it is in scope for the rest of `closeSprint()` because the entire function body shares one lexical scope. **Verification:** line 198 is at function-body level (not inside a nested block); Step 2.7 at line 316+ can read `isV2` directly without re-declaration. Developer must not re-declare it. (Architect note: if Developer re-declares, TS/lint will warn; if hidden by a block scope, the inner reference shadows. The §"Function shape" snippet above wraps Step 2.7 in a `{ ... }` block — the inner `let leftoverWorktrees` is locally-scoped, but `isV2` is read from outer scope.)

**Gotchas surfaced from code inspection:**
- **`REPO_ROOT` resolution under test harness.** close_sprint.mjs:68 derives `REPO_ROOT = path.resolve(__dirname, '..', '..')` from the script's location. Under the test harness (`runCloseSprint` at test_close_sprint_v21.test.ts:108), the script runs from the live `/Users/.../ClearGate/.cleargate/scripts/` so `REPO_ROOT` resolves to the live repo (a real git repo with worktrees). Tests that run during a SPRINT-19 worktree-active session would see the SPRINT-19 worktrees in `git worktree list` output. **Risk:** test scenarios that don't expect worktrees would see real ones. **Mitigation:** Scenario 2 (happy-path) MUST run with `CLEARGATE_TEST_SUPPRESS_WORKTREE_CHECK=1` env override OR mock the execSync. **Recommended:** the inline Step 2.7 block reads `process.env.CLEARGATE_SKIP_WORKTREE_CHECK === '1'` as an early return — same pattern as the existing `CLEARGATE_SPRINT_DIR` test seam at line 121-130. Add this env override AND document it in the JSDoc at the top of the file. New env var: `CLEARGATE_SKIP_WORKTREE_CHECK=1` skips Step 2.7 entirely (logs `Step 2.7 skipped: CLEARGATE_SKIP_WORKTREE_CHECK=1 set (test seam).`). Tests use this env var; production code never sets it.
- **`execSync` throws on non-zero exit, not on cwd-not-a-repo.** When `cwd` is a tmpdir (not a git repo), `git worktree list --porcelain` exits 128 with stderr `fatal: not a git repository...`. The `try/catch` in §"Function shape" catches that — sets `worktreeListAvailable = false` — Step 2.7 logs the skip message and proceeds. **However** `execSync` by default writes the child's stderr to the parent process's stderr; the test harness will see `fatal: not a git repository` noise on stderr even on the skip path. **Fix:** pass `stdio: ['ignore', 'pipe', 'ignore']` to `execSync` to suppress git's stderr. The existing Step 2.6 invocation at line 297 uses `stdio: 'inherit'` because reconcile-lifecycle is meant to print its own output; M4 must NOT inherit because `git worktree list` errors are noise. Use `{ cwd: REPO_ROOT, encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'] }`.
- **The `.worktrees/STORY-*` regex MUST cover both forward-slash AND backslash separators** for cross-platform support. `cleargate-cli/src/commands/sprint.ts:952` uses `/[/\\]\.worktrees[/\\]STORY-/`. M4 reuses verbatim. Tests on macOS/Linux pass with `/`; CI on Windows would need `\\`. (Architect note: SPRINT-19 dev infra is macOS — `darwin 25.4.0` per env. Tests on macOS exercise `/` only; Windows path is dormant but the regex protects against future Windows runs.)
- **`.worktrees/CR-*` paths exist in current sprint.** state.json at lines 21-24 + 33-36 shows `CR-022` and `CR-024` use `.worktrees/CR-022/` and `.worktrees/CR-024/` (not `.worktrees/STORY-*`). **The Step 2.7 regex matches `STORY-` only — CR-prefixed worktrees are NOT detected.** This is a **bug or a feature**:
  - **Reading 1 (feature):** CR-022 §3 M1 line 130 says: *"assert no path matches `.worktrees/STORY-*`"*. CR-022 §4.3 Gherkin "Scenario: Step 2.7 blocks on leftover worktree" uses `.worktrees/STORY-NNN-NN/`. The CR explicitly scopes to STORY- prefixed paths. So CR-/BUG-/EPIC- prefixed worktrees are out of scope.
  - **Reading 2 (bug):** SPRINT-19 itself uses `.worktrees/CR-022/`, `.worktrees/CR-024/`, `.worktrees/CR-025/`, `.worktrees/BUG-024/`. If those are still present at sprint close, Step 2.7 would PASS (no STORY- match) — but they're real leftover worktrees the human should remove. Closing SPRINT-19 with M4 in place would NOT block on its own dirty state.
  - **Decision:** Default to **Reading 1 (feature) — STORY- prefix only**, consistent with the CR text. **However, this is an open decision for the orchestrator** (see §"Open decisions for orchestrator" item 1 below). If the orchestrator escalates and the user wants broader matching, the regex changes to `/[/\\]\.worktrees[/\\](STORY|CR|BUG|EPIC)-/`. M4 default execution: STORY- only.
- **Pre-commit `npm run typecheck` will pass** because close_sprint.mjs is `.mjs` (unchecked by `tsc`); only the test file `.test.ts` is typechecked. The test file imports `spawnSync` from `node:child_process`, already used in the existing scenarios. No new TS surface needed.
- **`prebuild` regenerates MANIFEST.json SHA for `.cleargate/scripts/close_sprint.mjs`.** Per CR-022 §4.2 line 184: *"close_sprint.mjs (live + canonical) byte-identical post-edit"*. The two files are byte-identical at HEAD (verified). After M4's edit they remain byte-identical. The MANIFEST.json SHA at the close_sprint.mjs row is fingerprinted by `cleargate-cli/scripts/build-manifest.ts:36-60` — runs deterministically. Step 6 of the Order regenerates and commits.
- **FLASHCARD `2026-04-30 #worktree #npm`** says: *"worktrees share no node_modules with parent; run `npm ci --workspace <pkg>` before first typecheck/test in a new worktree."* Developer runs M4 in a worktree (presumably `.worktrees/CR-022/` already exists per state.json). First step: `cd .worktrees/CR-022 && npm ci --workspace cleargate-cli` before any test invocation. (Architect note: state.json at line 25 shows `CR-022` worktree already exists; Developer continues that worktree.)
- **FLASHCARD `2026-04-21 #worktree #vitest #cleanup`** says: *"`git worktree remove --force --force` does NOT kill detached vitest worker pools — they persist as orphan node processes pointing at deleted dirs. Run `pkill -9 -f \"node.*vitest\"` BEFORE removing worktrees that ran `npm test`."* Tangential to M4 (M4 doesn't remove worktrees), but the broader Wave 2 close-up may.
- **FLASHCARD `2026-05-02 #vitest #ram #parallel-agents`** says: *"vitest maxForks cap is PER-PROCESS — N parallel agents × maxForks = total forks (e.g. cap=2 + 3-agent wave = 6 forks ≈ 2.4GB)."* Wave 2 has 6 parallel Developer agents running. Orchestrator must lower `VITEST_MAX_FORKS=1` or serialize. (Architect note: this is orchestrator concern, not M4 implementation. Flagged here so orchestrator sees it before dispatching Wave 2.)

## §3 Mirror-parity callout

**FLASHCARD `2026-05-01 #mirror #parity` and `2026-04-19 #wiki #protocol #mirror` apply.**

**Per-edit invariant:** every M4 edit lands in BOTH locations or NEITHER. The Developer must, in a single working tree:

- EDIT `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs` (live) — insert Step 2.7 block per §"Function shape" between line 314 and line 316 (post-Step-2.6 catch + pre-Step-3 opener).
- EDIT `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (canonical mirror) — byte-identical insert.
- VERIFY: `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/close_sprint.mjs /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/close_sprint.mjs` must return empty.
- Run `npm --prefix cleargate-cli run prebuild` — regenerates `cleargate-planning/MANIFEST.json` SHA for the close_sprint.mjs row + propagates the mirror change into `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (cli-bundled derived copy, regenerated by `copy-planning-payload.mjs` per FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`).

**Pre-existing baseline (verified 2026-05-01):** `diff` between live and canonical returns **empty** at HEAD (commit fb4eb2d). Both files are 448 LOC. M4 has a clean baseline — no pre-divergence to reconcile (FLASHCARD `2026-05-01 #mirror #parity` warns against side-effect reconciliation; not applicable here because there's nothing to reconcile).

## §4 Test fixture path

**Use the existing suite — DO NOT create a new file.**

Target: `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/test/scripts/test_close_sprint_v21.test.ts`

Justification: the file already covers Step 2.x scenarios (Scenario 4 at line 252+ tests Step 2.5 missing sections; Scenarios 7-14 cover STORY-025-03 naming + Step 3.5 + Step 7). Adding Step 2.7 scenarios as a new `describe()` block at the bottom of the file matches the existing pattern. CR-022 §2 line 112 explicitly endorses: *"`cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` — extend with Steps 2.7, 2.8, 6.5, 6.6, 6.7, 8 scenarios."*

**New `describe()` block to append at the end of the file (after Scenario 14):**

```typescript
// ─────────────────────────────────────────────────────────────────────────────
// CR-022 M1: Step 2.7 worktree-closed pre-close check
// ─────────────────────────────────────────────────────────────────────────────

describe('Scenario 15: Step 2.7 skipped when git worktree list unavailable (tmpdir not a git repo)', () => {
  // happy-path equivalent under test harness — tmpdirs are not git repos,
  // so Step 2.7 always skips. Validates the skip message + pipeline progression.
  // ...
});

describe('Scenario 16: Step 2.7 fires under v2 when leftover worktree present (CLEARGATE_FORCE_WORKTREE env)', () => {
  // Test seam: CLEARGATE_FORCE_WORKTREE=<path> env var causes Step 2.7 to
  // pretend `git worktree list` returned that path. Used to assert the v2
  // block path without needing a real git worktree.
  // ALTERNATIVE: stub PATH to a fake `git` binary that prints worktree output.
  // Recommended: env-seam approach (cleaner, no PATH manipulation).
  // ...
});

describe('Scenario 17: Step 2.7 advisory under v1 (warn + continue)', () => {
  // Same env seam as Scenario 16 + a v1 fixture (sprint-v1-legacy already exists).
  // Asserts stderr warning + exit 0 + state.json sprint_status flips to Completed.
  // ...
});
```

**Test seam decision (open for orchestrator):**

Step 2.7 needs a way for tests to inject "as if leftover worktrees exist" without requiring a real `.worktrees/STORY-*` directory in a real git repo. Two options:

1. **`CLEARGATE_FORCE_WORKTREE_PATHS` env var** — comma-separated list of fake worktree paths that Step 2.7 reads instead of running `git worktree list`. Production code never sets it; tests set it for the failure-mode scenarios. Pattern matches the `CLEARGATE_SPRINT_DIR` test seam at line 121-130 of close_sprint.mjs.
2. **Stub `git` binary on PATH** — write a fake `git` script in tmpdir, prepend tmpdir to PATH, fake script prints porcelain worktree output. More realistic; more setup ceremony.

**Recommended: option 1 (env seam).** Cleaner, matches the existing `CLEARGATE_*` test-seam convention. Adds 2 lines to the Step 2.7 block. **Open decision item 2 (see §"Open decisions for orchestrator").**

**Existing fixtures reused:**
- `cleargate-cli/test/scripts/fixtures/sprint-v1-legacy/` — used as v1 baseline for Scenario 17.
- `makeTempSprintDir(fixtureName, sprintDirName)` at line 70-101 — reused unchanged.
- `runCloseSprint(sprintDir, extraArgs)` at line 108-135 — reused unchanged.

**No new fixture files required.** The env-seam approach uses string injection, not filesystem fixtures.

**Test count delta:** +3 scenarios (15, 16, 17) × ~3-5 `it()` blocks each = ~10-12 new test cases. Well above CR-022 §4.1 minimum (1 happy + 1 failure per new step = 2 minimum for Step 2.7 alone; M4 ships 3 categorized scenarios).

## §5 Wave 2 disjointness — hand-off for M5 Architect

**Sprint plan §2.2 row "M4, M5, M6, M11 → M4 → M5 → M6 → M11" mandates strict sequential merge order across all 4 close_sprint.mjs touches.**

**Post-M4 line layout (estimated; M5 Architect must re-grep post-merge):**

| Line | Content |
|---|---|
| 260 | `// ── Step 2.6: Lifecycle Reconciliation (CR-017) ──` |
| 314 | end of Step 2.6 catch block |
| **315** | blank line |
| **316** | `// ── Step 2.7: Worktree-Closed Check (CR-022 M1) ──` (NEW) |
| ~360 | end of Step 2.7 block (NEW) |
| **~361** | **blank line — INSERT POINT FOR M5's Step 2.8** |
| ~362 | `// ── Step 3: Invoke prefill_report.mjs ──` (was at line 316; shifted by ~46 lines) |

**M5 Architect: cite post-M4 line numbers at planning time.** Re-grep `close_sprint.mjs` after M4 lands. The Step 2.8 block inserts immediately after M4's Step 2.7 closer + before Step 3 opener. Same `isV2` advisory-vs-enforcing pattern. Same mirror-parity invariant. Same prebuild regen rule.

**M5 cross-reference for the new helper (lib/report-filename.mjs):** Step 2.8 stderr lists unmerged commits; if it embeds the report filename (e.g. *"Sprint not yet merged; review SPRINT-19_REPORT.md before retrying"*), it MUST call `reportFilename(sprintDir, sprintId, { forRead: true })` from `./lib/report-filename.mjs` (already imported at line 42) — never hard-code `SPRINT-NN_REPORT.md`. M4 itself does NOT consume this helper because Step 2.7 messages reference `.worktrees/STORY-*` paths only.

**M6 Architect (Steps 6.5/6.6/6.7) and M11 Architect (Step 8):** insert at lines far below Step 2.7 (Step 6 is at line 419; Step 8 inserts at end). M4's edit does NOT shift those line numbers significantly — but M5 will. M6's plan should cite line numbers post-M5. M11's plan should cite line numbers post-M6. Sequential cascade.

**Concrete rule for the M5 Architect:** when reading this M4 plan to draft your own, copy the §"Insert location" pattern (re-grep at HEAD; cite Step boundary opener + closer line numbers; cite surrounding stdout/stderr message patterns to mimic). The same disjointness analysis applies: M5's Step 2.8 inserts AFTER M4's Step 2.7 closer + BEFORE Step 3 opener. Cross-reference this plan's §"Insert location" + §"Function shape" sections for the established formatting conventions.

## Cross-story risks

Things a Developer working only on M4 (CR-022 M1) might miss:

1. **The four other close_sprint.mjs milestones (M5, M6, M11) lurk in Wave 2 + Wave 3.** A Developer reading only the M4 plan might think they own close_sprint.mjs — they don't. Sprint plan §2.3 first bullet explicitly warns: *"close_sprint.mjs is touched by 4 SPRINT-19 milestones (M4, M5, M6, M11). Sequential Wave 2 → Wave 3 ordering naturally serializes them, but each Architect milestone plan MUST cite the post-prior-milestone line numbers."* **Mitigation:** Developer commits to git WITHOUT touching any line outside the Step 2.7 insert range (lines 315-316 + ~46 lines of new content). No drive-by edits to Step 2.5, Step 2.6, Step 3, Step 5, Step 6, Step 7 messages — those are M5/M6/M11 territory. Stash discipline (FLASHCARD `2026-04-21 #worktree #vitest #cleanup` mid-sprint warning).
2. **The CR-/BUG-/EPIC-prefix question.** §"Gotchas" item 4 above flags this. Reading 1 (STORY- only) is the M4 default; if the user wants broader, the regex change is 1 line + 1 test scenario. Architect default is STORY- only (matches CR text); orchestrator escalates if needed.
3. **The skip-on-non-git-repo path is critical for ALL existing test fixtures.** Without the skip, the entire `test_close_sprint_v21.test.ts` suite fails on the first scenario that runs `runCloseSprint` against a tmpdir. The skip path is NOT just a fail-safe — it's a hard requirement for the test harness to function. Verify the skip message appears in stdout for every existing scenario after M4 lands; if a scenario doesn't see the skip message AND doesn't see the pass message, Step 2.7 didn't fire correctly. (Architect note: pre-existing scenarios make exit-code assertions only; many also assert on stdout/stderr substrings. Adding the new `Step 2.7 skipped: ...` line to stdout shouldn't break any existing assertion as long as the existing assertions use `toMatch`/`toContain` patterns rather than exact-match. Quick grep at line 273-352 of test_close_sprint_v21.test.ts shows all assertions use `toMatch` — safe.)
4. **`isV2` lexical scope.** §"Function shape" notes this — Developer must NOT re-declare `isV2` inside Step 2.7. The variable lives at function-body scope from line 198 onward. (Architect note: re-declaring with `const isV2 = ...` would TS-fail under strict mode but `.mjs` doesn't get TS-checked. The runtime would shadow correctly. Safer to read from outer scope.)
5. **`CLEARGATE_SKIP_WORKTREE_CHECK` test seam not yet documented in close_sprint.mjs JSDoc.** The §"Function shape" snippet introduces a new env var. Developer must update the JSDoc at the top of close_sprint.mjs (lines 1-34) to document the new env var alongside `CLEARGATE_SPRINT_DIR` + `CLEARGATE_STATE_FILE`. Otherwise the test seam is undocumented surface — discoverability hit.
6. **`stdio: ['ignore', 'pipe', 'ignore']` matters.** Without it, git's `fatal: not a git repository` stderr leaks through to the test harness's stderr capture. The M4 scenario assertions use `toMatch(/Step 2\.7/)` — git's leak doesn't match, but it pollutes stderr output for human inspection. Use the suppressed stdio config.

## Open decisions for orchestrator

1. **STORY- prefix only, or broader (CR-/BUG-/EPIC-)?** §"Gotchas" item 4 above. CR-022 §3 M1 line 130 + §4.3 Gherkin both say STORY- only. SPRINT-19 itself uses `.worktrees/CR-022/`, `.worktrees/CR-024/`, `.worktrees/CR-025/`, `.worktrees/BUG-024/` — STORY- only would let those slip past Step 2.7. **M4 default execution: STORY- only** (matches CR text literally). **Orchestrator: confirm STORY- only is acceptable, or escalate to user if you want broader matching.** Mitigation if escalated: regex change 1 line + 1 test scenario. **Recommended: keep STORY- only for M4; file a follow-up CR if broader matching is desired (sprint plan §2.2 already constrains M4 scope; broadening here is feature creep).** Note: the existing `cleargate sprint preflight` at sprint.ts:952 also uses STORY- only — broadening Step 2.7 would create cross-CLI semantics drift unless preflight is updated in lockstep.

2. **Test seam choice — env var vs. PATH stub?** §"Test fixture path > Test seam decision" above. Two options:
   - **Option A (recommended):** `CLEARGATE_FORCE_WORKTREE_PATHS=path1,path2` env var. Adds 2 lines to Step 2.7 block. Same convention as existing test seams.
   - **Option B:** stub fake `git` binary on PATH. More realistic but heavier.
   **M4 default execution: Option A.** **Orchestrator: confirm or kick back.**

3. **Document the `CLEARGATE_SKIP_WORKTREE_CHECK` env var in JSDoc?** §"Cross-story risks" item 5 above. The new test seam introduces undocumented surface. **M4 default: yes — Developer adds a 2-line entry to the JSDoc at lines 1-34 documenting both `CLEARGATE_SKIP_WORKTREE_CHECK=1` AND `CLEARGATE_FORCE_WORKTREE_PATHS=p1,p2` if the orchestrator confirms decision 2 = Option A.** **Orchestrator: confirm.**

4. **Commit-message scope: `feat(CR-022):` vs. `feat(SPRINT-19):` vs. `feat(EPIC-???):`?** FLASHCARD `2026-05-01 #commit-format #dod` warns Developers default to `feat(<sprint>):` even when DoD §4.2 specifies `feat(<epic>):`. CR-022 has no parent EPIC (`parent_cleargate_id: null`). CR-022 §4.2 line 183 says: *"Each commit format: `feat(EPIC-???-or-CR-022): <story-id> <short desc>`"*. **M4 default: `feat(CR-022): STORY-022-M1 — Step 2.7 worktree-closed pre-close check`.** **Orchestrator: confirm the `<story-id>` slot uses `STORY-022-M1` (the CR's own milestone label) — there is no ClearGate STORY-NNN-NN id assigned to M4.** Alternative if M-labels feel awkward: `feat(CR-022): M1 — Step 2.7 worktree-closed pre-close check`.

## Definition of Done (M4-specific echo of CR-022 §4.2)

- [ ] `close_sprint.mjs` (live + canonical) byte-identical post-edit (`diff` returns empty).
- [ ] Step 2.7 block lives between Step 2.6 closer (line 314 at HEAD) and Step 3 opener (line 316 at HEAD); ~45 LOC inserted.
- [ ] `cleargate-planning/MANIFEST.json` SHA for `.cleargate/scripts/close_sprint.mjs` regenerated in same commit (FLASHCARD `2026-05-01 #manifest #prebuild`).
- [ ] `cleargate-cli/templates/cleargate-planning/.cleargate/scripts/close_sprint.mjs` (cli-bundled derived) propagated by prebuild — DO NOT hand-edit (FLASHCARD `2026-05-01 #scaffold #mirror #prebuild`).
- [ ] `cleargate-cli/test/scripts/test_close_sprint_v21.test.ts` extended with 3 new `describe()` blocks (Scenarios 15-17); ~10-12 new `it()` test cases.
- [ ] All existing scenarios in `test_close_sprint_v21.test.ts` still pass (the new `Step 2.7 skipped: ...` stdout line does not break any `toMatch`/`toContain` assertion).
- [ ] CR-022 §4.3 Gherkin "Scenario: Step 2.7 blocks on leftover worktree" passes via Scenario 16 of the test file.
- [ ] `npm --prefix cleargate-cli run typecheck` green.
- [ ] `npm --prefix cleargate-cli test` green (full suite).
- [ ] JSDoc at top of close_sprint.mjs (lines 1-34) updated to document `CLEARGATE_SKIP_WORKTREE_CHECK` + `CLEARGATE_FORCE_WORKTREE_PATHS` test seams (per Open Decision 3).
- [ ] Single commit per CR-022 §4.2 — message format `feat(CR-022): STORY-022-M1 — Step 2.7 worktree-closed pre-close check` (per Open Decision 4).
- [ ] No edits to: M5/M6/M11 territory in close_sprint.mjs (Steps 2.8, 6.5, 6.6, 6.7, 8). No edits to: `prep_reporter_context.mjs`, `suggest_improvements.mjs`, `prefill_report.mjs`, `cleargate-cli/src/commands/sprint.ts`, CLAUDE.md, protocol/enforcement docs, agent definitions. Wave 2 disjointness contract.

---

role: architect
