# Milestone M1 — Execution Phase v2 Infrastructure

**V-Bounce HEAD SHA pinned:** `2b8477ab65e39e594ee8b6d8cf13a210498eaded` (github.com/sandrinio/V-Bounce-Engine@main, fetched 2026-04-21). Every §4.2 reference below cites this SHA.

**M1 goal:** land worktree+branch contract, state.json schema, run_script+pre-gate wrappers, sprint-context template. No v2 behavior wired — scaffold only.

**Execution mode reminder:** SPRINT-09 itself runs under `execution_mode: v1` (SPRINT-09 frontmatter line 20). No story may invoke `cleargate sprint init`, `cleargate state update`, or `git worktree add` during its own execution. These exist only as artefacts in `.cleargate/scripts/` + `cleargate-planning/` mirror. First real exercise is SPRINT-10.

## Stories: STORY-013-01, STORY-013-02, STORY-013-03, STORY-013-04

## Wave: W1 (sequential — SPRINT-09 is on v1, no parallel worktrees)

Under v2, 01→04 could parallelize after 01 merges (02/03/04 touch disjoint paths). Under v1 we serialize end-to-end: one Developer, one commit at a time on `main`.

## Order

Strict: **01 → 02 → 03 → 04**.

- **01 first** — protocol §10 is referenced by every subsequent story's DoD ("gated behind execution_mode: v2").
- **02 before 03** — `run_script.sh` preflight greps for `.cleargate/sprint-runs/<id>/state.json`; that path must be defined by 02 first.
- **03 before 04** — sprint_context template references the pre-gate scanner output path; 03 defines `<worktree>/.cleargate/reports/`.
- **04 last** — patches `.claude/agents/architect.md`, which 01 also patches (developer.md patch in 01, architect.md patch in 04). Serializing avoids both stories touching the same files on `main`.

## Per-story blueprint

### STORY-013-01 — Worktree + Branch Hierarchy

**Files to create:** none.

**Files to modify:**
- `.cleargate/knowledge/cleargate-protocol.md` — append new `## 15. Worktree Lifecycle (v2)` section (existing numbered §§ run 1–14, line 511 is §14; take §15). Sections: §15.1 Branch hierarchy, §15.2 Worktree commands (verbatim V-Bounce SKILL.md `Worktree Commands` block with s/vbounce/cleargate/ and path s/.vbounce/.cleargate\/sprint-runs\/<id>/), §15.3 MCP rule ("nested `mcp/` is off-limits for worktree add — edit inside outer worktree"), §15.4 Local state.json wins in-flight; MCP is post-facto audit (EPIC-013 Q7 answer), §15.5 v1/v2 gating ("under v1 informational; under v2 mandatory").
- `.claude/agents/developer.md` — append `## Worktree Contract` after current `## What you are NOT` (line 56). Three rules: (a) check `pwd` equals assigned worktree before any edit; (b) never mix stories in one worktree; (c) never `git worktree add` inside `mcp/`.
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — mirror the §15 append. `diff` must be empty vs live after.
- `cleargate-planning/.claude/agents/developer.md` — mirror the Worktree Contract append.
- `/.gitignore` (repo root, file lines 1–37) — add after line 36 (before `node_modules/`): `# Story worktrees — ephemeral per-story git worktrees (v2 execution)` + `.worktrees/`.
- `cleargate-planning/.gitignore` — currently only has 4 entries (file verified 12 lines); append same `.worktrees/` block. Planning mirror ships to downstream users via `cleargate init`.
- `.cleargate/FLASHCARD.md` — prepend one line at top of log section (below format description line 5): `2026-<date> · #worktree #mcp · never git worktree add inside nested mcp/ repo — edit mcp/ inside the outer worktree; nested-repo worktrees are a git footgun.`
- `cleargate-planning/MANIFEST.json` — no change (developer.md is already tracked; protocol.md already tracked; .gitignore is not in manifest).

**Schema changes:** none.

**Test scenarios (from §2.1, all 5 required):**
1. Sprint branch cut from main — manual walkthrough. `git checkout -b sprint/S-FAKE main`; verify `git branch -a` includes `sprint/S-FAKE`.
2. Story worktree creation — `git worktree add .worktrees/STORY-013-FAKE -b story/STORY-013-FAKE sprint/S-FAKE`; `git worktree list` verification.
3. `.worktrees/` gitignored — populate `.worktrees/STORY-013-FAKE/` with a committed+uncommitted file, run `git status` at repo root, expect zero entries under `.worktrees/`.
4. Merge + worktree removal — `git merge story/STORY-013-FAKE --no-ff` on sprint branch, `git worktree remove`, `git branch -d`, verify cleanup.
5. mcp/ refusal documented — grep `developer.md` for "never git worktree add inside mcp/"; grep `FLASHCARD.md` for `#worktree #mcp`.

All walkthroughs use `STORY-013-FAKE` — do not create actual worktrees inside SPRINT-09's own execution (we're on v1). Clean up with `git worktree prune` after.

**Reuse (no duplication):** none — first story to touch worktree commands. Read V-Bounce SKILL.md §§ "Git Worktree Strategy" + "Worktree Commands" at pinned SHA; port verbatim with namespace rename.

**Gotchas from code inspection:**
- `cleargate-planning/.gitignore` is tiny (4 entries, all `.cleargate/.foo`) — don't blindly copy root `.gitignore` content; only append `.worktrees/` rule.
- `.claude/agents/developer.md` current last line is 59 (`# Not the Reporter — one-paragraph notes max.`) — Worktree Contract goes as a fresh `## Worktree Contract` after `## What you are NOT`. Existing `subagent_type: developer` frontmatter at top (lines 1–6) is untouched.
- `.cleargate/knowledge/cleargate-protocol.md` numbering: §§ go 1–14 (line 511 begins §14). Use §15, not §10 (story frontmatter says "§10" but actual next free section is §15 — story text is wrong about the number, right about placement).
- Root `.gitignore` line 12 already carves out `/.claude/` (live dogfood gitignored). No collision with `.worktrees/`.

### STORY-013-02 — state.json Schema + Bounce Counters + Lifecycle Scripts

**Files to create (all under `.cleargate/scripts/` AND mirrored at `cleargate-planning/.cleargate/scripts/`):**
- `constants.mjs` — exports `VALID_STATES = ['Ready to Bounce','Bouncing','QA Passed','Architect Passed','Sprint Review','Done','Escalated','Parking Lot']`, `TERMINAL_STATES = ['Done','Escalated','Parking Lot']`, `SCHEMA_VERSION = 1`, `BOUNCE_CAP = 3`. JSDoc at top documents the `state.json` schema inline (STORY-013-02 §4.2 DoD).
- `init_sprint.mjs` — args `<sprint-id> --stories ID1,ID2,...` with optional `--force`. Resolves sprint-run dir `.cleargate/sprint-runs/<sprint-id>/`; if `state.json` exists and `--force` NOT passed, exit non-zero with stderr `state.json already exists at <path>; pass --force to overwrite`. Story entries initialize with `state: "Ready to Bounce"`, `qa_bounces: 0`, `arch_bounces: 0`, `worktree: null`, `updated_at: <ISO>`. Top-level: `schema_version: 1`, `sprint_id`, `execution_mode: "v1"` (placeholder; set by orchestrator), `stories: {…}`, `sprint_status: "Active"`, `last_action: "Sprint <id> initialised"`, `updated_at`.
- `update_state.mjs` — args `<STORY-ID> <new-state> | --qa-bounce | --arch-bounce`. Atomic write-then-rename via `fs.promises.writeFile(tmp)` + `fs.promises.rename(tmp, final)`. No-op idempotency: if new state equals current AND `updated_at` would be the only field to change, exit 0 without writing.
- `validate_state.mjs` — reads `state.json`; exports `validateState(state)` as in V-Bounce; adds invariants NOT in V-Bounce: `qa_bounces <= BOUNCE_CAP`, `arch_bounces <= BOUNCE_CAP`, `schema_version === 1`. CLI mode exits non-zero on any violation with stderr listing each.
- `validate_bounce_readiness.mjs` — args `<STORY-ID>`. Checks: (a) `state.json` exists + valid, (b) story present, (c) story state is `"Ready to Bounce"`, (d) `git status --porcelain` returns empty (dirty tree blocks). Exit non-zero on any fail with stderr listing dirty files or missing story.
- `state.schema.json` — JSON Schema Draft-07 describing the v1 shape. Consumed only for documentation + future validation; `validate_state.mjs` does not depend on it at runtime (no external deps allowed).

**Files to modify:** none.

**Schema changes (state.json v1, inline — LOCK THIS, future changes bump schema_version):**
```json
{
  "schema_version": 1,
  "sprint_id": "S-NN",
  "execution_mode": "v1",
  "sprint_status": "Active",
  "stories": {
    "STORY-NNN-NN": {
      "state": "Ready to Bounce",
      "qa_bounces": 0,
      "arch_bounces": 0,
      "worktree": null,
      "updated_at": "<ISO-8601>",
      "notes": ""
    }
  },
  "last_action": "<string>",
  "updated_at": "<ISO-8601>"
}
```

**Divergences from V-Bounce (port-but-adapt):**
1. **Auto-escalate on counter cap.** V-Bounce `update_state.mjs` at pinned SHA does NOT auto-flip state to `Escalated` when `qa_bounces`/`arch_bounces` hits 3 (lines 86–95: just increments). STORY-013-02 §2.1 scenario 4 REQUIRES auto-flip. Implementation: after increment, if counter === `BOUNCE_CAP`, set `state = 'Escalated'` in the same write; further `--qa-bounce` or `--arch-bounce` on an Escalated story exits non-zero with stderr `story <id> is already Escalated`.
2. **init_sprint refuses overwrite by default.** V-Bounce warns and overwrites (line 55 "state.json already exists. Overwriting..."). STORY-013-02 §2.1 scenario 2 REQUIRES non-zero exit. Implementation: `existsSync(stateFile) && !args.includes('--force')` → exit non-zero.
3. **Default state is "Ready to Bounce"** (STORY-013-02 §1.2). V-Bounce sets `'Draft'` (init_sprint.mjs line 62). We skip Draft/Refinement — those belong to Phase 2 (planning), and stories hit state.json only when Phase 2 is done.
4. **Path is `.cleargate/sprint-runs/<id>/state.json`.** V-Bounce uses `.vbounce/state.json` (single global file). Ours is per-sprint — STORY-013-05's circuit-breaker must also read this exact path.
5. **No `phase` field.** V-Bounce tracks `phase: 'Phase 1'`. We don't — that's a planning artefact.

**Test scenarios (from §2.1, all 6 required):**
1. init_sprint creates fresh state.json — invoke on fresh sprint, check schema_version=1, both stories `Ready to Bounce`, counters 0.
2. init_sprint refuses to overwrite — second invocation without `--force` exits non-zero; stderr names the file and suggests `--force`.
3. update_state transitions a story + idempotent no-op — two consecutive `update_state STORY-X Bouncing` calls; second is exit 0, no file change.
4. qa-bounce counter caps at 3 and auto-escalates — seed qa_bounces=2, invoke `--qa-bounce`, verify counter=3 AND state="Escalated". Further `--qa-bounce` exits non-zero.
5. validate_bounce_readiness blocks dirty tree — seed a dirty file, expect exit non-zero + stderr listing dirty files.
6. validate_state catches corrupted counter — hand-write qa_bounces=5 into state.json, run validate_state, expect non-zero + named invariant.

**Reuse:** none — first scripts. Use Node built-ins only (`fs/promises`, `path`, `url`, `child_process.execSync` for git status). Do NOT use `child_process.spawnSync` for git status — V-Bounce uses `execSync`, our v2 context is simpler.

**Gotchas from code inspection:**
- V-Bounce `validate_state.mjs` exports `validateState` (line 23) — preserve that export so `update_state.mjs` can `import { validateState } from './validate_state.mjs'` and gate writes on validity.
- V-Bounce uses `fs.writeFileSync` — for atomicity use write-tmp-then-rename (STORY-013-02 §1.2 explicit). Tmp path: `<final>.tmp.<pid>`.
- V-Bounce's `update_state.mjs` resets `worktree: null` on state `Done` (lines 118–120). Port this — future STORY-013-05/08 depend on `worktree` field lifecycle.
- Node 24 supports `node --test` natively. Each script's tests live at `.cleargate/scripts/<script>.test.mjs` using `node:test` + `node:assert`. No vitest, no runtime deps.
- `validate_bounce_readiness.mjs` requires `git status --porcelain` inside the target repo; spawn via `execSync('git status --porcelain', {cwd: ROOT, encoding:'utf8'})`. If invoked inside a worktree, `ROOT` is the worktree cwd — that's correct behavior per STORY-013-02 §1.2.
- Sprint-run dir may not exist on fresh init — `fs.mkdirSync(path.dirname(stateFile), {recursive:true})` before write.

### STORY-013-03 — run_script.sh Wrapper + Pre-Gate Scanner

**Files to create (all chmod 755 under `.cleargate/scripts/` AND mirrored at `cleargate-planning/.cleargate/scripts/`):**
- `run_script.sh` — bash wrapper. Args `<script-name> [args...]`. Resolves `<script-name>` relative to `.cleargate/scripts/`. Extension routing: `.mjs` → `node`, `.sh` → `bash`, anything else → exit 2 with stderr "unsupported extension: <ext>". Captures stdout/stderr to mktemp files via `> "$STDOUT_FILE" 2> "$STDERR_FILE"`. On exit 0: cat stdout to caller stdout, stderr to caller stderr, exit 0. On non-zero: preserve stdout, print structured block to stderr (format from §3.2).
- `pre_gate_runner.sh` — bash; `pre_gate_runner.sh qa|arch <worktree-path> <branch>`. Sources `pre_gate_common.sh`. QA mode runs `npm run typecheck`, greps staged diff against debug/TODO patterns from `gate-checks.json`, `npm test`. Arch mode: `npm run typecheck`, diff `package.json` vs `<branch>^` for new runtime deps, detect `.env*`, per-dir file count. Writes report to `<worktree-path>/.cleargate/reports/pre-<mode>-scan.txt` (NOT `.vbounce/reports/` — rewrite V-Bounce path). Exit 0 all-pass, 1 checks failed, 2 scan couldn't run (missing config, missing worktree).
- `pre_gate_common.sh` — shared helpers: `get_modified_files`, `record_result`, `print_summary`, `write_report`. Strip V-Bounce detectors for Python/Rust/Go/Java/Swift. Keep only Node+TS (detect by `tsconfig.json` + `package.json`).
- `init_gate_config.sh` — generates `.cleargate/scripts/gate-checks.json` on first run. Idempotent: if file exists, exit 0 without overwrite. Shape per STORY-013-03 §3.2 (flat `{schema_version:1, qa:{…}, arch:{…}}`) — diverges from V-Bounce array-of-objects shape; follow the story.
- `.cleargate/scripts/gate-checks.json` — committed with defaults. Shape:
  ```json
  {
    "schema_version": 1,
    "qa": {
      "typecheck": "npm run typecheck",
      "debug_patterns": ["console.log","console.debug","debugger"],
      "todo_patterns": ["TODO","FIXME","XXX"],
      "test": "npm test"
    },
    "arch": {
      "typecheck": "npm run typecheck",
      "new_deps_check": true,
      "stray_env_files": [".env",".env.local",".env.production"],
      "file_count_report": true
    }
  }
  ```

**Files to modify:** none (gate-checks.json is new).

**Schema changes:** none.

**Test scenarios (from §2.1, all 6 required):**
1. run_script.sh passes through a successful script — `run_script.sh ok.mjs` where ok.mjs prints "hello"; stdout="hello", exit 0.
2. run_script.sh prints a diagnostic block on failure — fail.mjs stderrs "oops" and exits 7; diagnostic block contains `## Script Incident`, `Exit code: 7`, `Suggested fix:`.
3. pre_gate_runner qa catches a debug statement — worktree committed file with `console.log`; exit 1; report names file+line.
4. pre_gate_runner arch flags a new runtime dep — package.json adds a dep vs sprint branch; exit 1; report lists "new runtime dep: …".
5. init_gate_config seeds config on first run — first run writes gate-checks.json, second run is no-op.
6. run_script.sh refuses unknown extension — `run_script.sh something.py` → exit 2, stderr "unsupported extension".

Use a throwaway `.worktrees/STORY-013-FAKE/` for scenarios 3–4. All six run as a bash test script (or `bats` if convenient — no new runtime dep, bats is system-installable).

**Reuse:** `constants.mjs` from STORY-013-02 is NOT used here (shell doesn't import JS); but the preflight check in `run_script.sh` for missing `state.json` greps for `.cleargate/sprint-runs/*/state.json` existence — must match STORY-013-02's exact path.

**Gotchas from code inspection:**
- V-Bounce's `pre_gate_runner.sh` sources `pre_gate_common.sh` via `source "${SCRIPT_DIR}/pre_gate_common.sh"` — that must happen before `set -euo pipefail` in common.sh (common.sh sets its own). Keep common.sh's `set -euo pipefail` at top (line 4).
- V-Bounce's `pre_gate_common.sh` uses `node -e "JSON.parse(...)"` for config parsing (line 85 of pre_gate_runner.sh) — acceptable: Node is already a dep of the project.
- `pre_gate_runner.sh` report-path divergence: V-Bounce writes `<worktree>/.vbounce/reports/pre-<mode>-scan.txt` (V-Bounce file lines 147–150); STORY-013-03 §1.2 says `<worktree-path>/.cleargate/reports/pre-<mode>-scan.txt`. Rewrite the path.
- The **"root cause" heuristic** in `run_script.sh` must match stderr against: `state.json not found` → "re-init sprint", `ENOENT` → "missing file", `EACCES` → "chmod", `SyntaxError` → "JS syntax". V-Bounce has 6 such branches (file lines 169–211); port verbatim with message rewording.
- `chmod 755` via git index: after `git add .cleargate/scripts/*.sh`, run `git update-index --chmod=+x .cleargate/scripts/*.sh` before commit, or just `chmod 755` pre-add on filesystems where `filemode=true`. Verify with `ls -l`.
- Pre-gate report directory `<worktree-path>/.cleargate/reports/` may not exist on first run — scanner does `mkdir -p`. Does NOT conflict with root-level `.cleargate/sprint-runs/S-09/` — worktrees are separate checkouts.

### STORY-013-04 — Sprint Context Template + Adjacent-Implementation Check

**Files to create:**
- `.cleargate/templates/sprint_context.md` — four fixed sections per STORY-013-04 §1.2: `## Locked Versions`, `## Cross-Cutting Rules`, `## Active FLASHCARD Tags`, `## Adjacent Implementations (Reuse First)`. Each with one-sentence purpose blurb. Frontmatter: `sprint_id`, `created_at`, `last_updated`.
- `cleargate-planning/.cleargate/templates/sprint_context.md` — mirror (manifest tracks templates, verify).
- `.cleargate/sprint-runs/S-09/sprint-context.md` — first live instance. Populate `## Locked Versions` from root `package.json` + each workspace's `package.json`; `## Active FLASHCARD Tags` from a grep of `.cleargate/FLASHCARD.md` for tags that appear in EPIC-013's §4.2 (must include `#worktree`, `#mcp`, `#schema` at minimum); `## Cross-Cutting Rules` lists EPIC-013 §0 architecture_rules; `## Adjacent Implementations` starts empty (populated as stories merge — but for SPRINT-09 this stays empty because dev runs are sequential and don't parallelize).

**Files to modify:**
- `.claude/agents/architect.md` — append new subsection `## Adjacent Implementation Check` to the Workflow (insert after current step 5 at line 44, before `## Guardrails` at line 46). Content: "Before writing per-story blueprint in the plan, grep merged stories in the current sprint (`git log sprint/S-XX --name-only | grep -E '^(cleargate-cli|src|\\.cleargate/scripts)/'`) for exports. List any reusable module as `Reuse (no duplication): <helper name from <file>>` in the per-story blueprint. If a candidate story would duplicate a listed module, flag in `Cross-story risks`." 10 lines max, single example.
- `cleargate-planning/.claude/agents/architect.md` — mirror the append. `diff` live vs shipped must be empty after.

**Schema changes:** none.

**Test scenarios (from §2.1, all 5 required):**
1. Template has all four fixed sections — grep the template for each heading.
2. Per-sprint instance created at M1 planning — `.cleargate/sprint-runs/S-09/sprint-context.md` exists; Locked Versions reflects current `package.json`; Active FLASHCARD Tags includes `#worktree` and `#mcp` (present after STORY-013-01).
3. Architect flags a reusable helper — synthetic: after STORY-013-02 merges, M2 Architect plan must cite `.cleargate/scripts/constants.mjs` exports (`VALID_STATES`, `TERMINAL_STATES`) as Reuse entries for any M2 story that reads state.
4. architect.md has the new subsection — grep for "Adjacent Implementation Check" in both live + mirror.
5. Scaffold mirror in sync — `diff .cleargate/templates/sprint_context.md cleargate-planning/.cleargate/templates/sprint_context.md` returns empty; same for `architect.md`.

**Reuse:**
- FLASHCARD tag grep pattern: `grep -E '#(worktree|mcp|schema|auth|test-harness|state-schema|concurrency)'` — reuse the vocabulary already in `.claude/skills/flashcard/SKILL.md` tag list (lines 37–50).
- Locked-versions snapshot: read `/Users/ssuladze/Documents/Dev/ClearGate/package.json`, `/cleargate-cli/package.json`, `/cleargate-planning/package.json`, `/admin/package.json`, `/mcp/package.json`. Extract `name + version + key deps (fastify, drizzle-orm, zod, @sveltejs/kit, tailwindcss)`.

**Gotchas from code inspection:**
- `.claude/agents/architect.md` current step 5 is the flashcard-record step (line 44). Adjacent Implementation Check is a NEW step in the Workflow — number it 4.5 or make it a sub-bullet of step 2 ("Read every story…"). Recommend: insert as a new subsection `## Adjacent Implementation Check` BETWEEN the numbered Workflow and `## Guardrails` (line 46). Not inside the numbered list — that's the contract, don't renumber.
- `cleargate-planning/MANIFEST.json` has `.claude/agents/architect.md` at line 7 with `overwrite_policy: "always"` — every edit to the live file must also update the mirror + regenerate MANIFEST.json SHA. But MANIFEST.json is rebuilt by `npm run build` in cleargate-cli, not hand-edited; Developer running STORY-013-04 only edits the two .md files and lets the next `npm run build` pick it up. Flag if MANIFEST SHA drift blocks typecheck.
- Template frontmatter must use `sprint_id: "S-NN"` (quoted) to match existing template conventions (see `cleargate-planning/.cleargate/templates/Sprint Plan Template.md` line 2).
- Per-sprint instance lives at `.cleargate/sprint-runs/<id>/sprint-context.md` — `.cleargate/sprint-runs/` is gitignored-partial (only `.active` is ignored via `.gitignore` line 19). Sprint-runs files ARE committed. `.cleargate/sprint-runs/S-09/sprint-context.md` will be committed with the STORY-013-04 commit — that's correct.
- STORY-013-04 is the **only story in M1 where the plan file consumer (Architect) also is the producer** — the template this story ships will retroactively have been needed during THIS M1 plan. Chicken-and-egg is noted in story §ClearGate Ambiguity Gate line 145; accept the loop: first live instance is authored BY this story.

## Cross-story risks

1. **state.json schema lock (SPRINT-09 R3, CRITICAL).** STORY-013-02 ships the v1 schema. Every M2 story (05 circuit-breaker, 06 flashcard gate, 07 close_sprint, 08 execution-mode flag, 09 planning v2) depends on exact field names: `stories[id].state`, `qa_bounces`, `arch_bounces`, `worktree`, `updated_at`; top-level `schema_version`, `sprint_id`, `execution_mode`, `sprint_status`. **Developer on 013-02 MUST match this doc exactly.** If 013-02 Dev changes a field name (e.g. `qa_bounces` → `qa_count`), every M2 story's contract breaks. QA on 013-02: diff the shipped schema against this plan's JSON block, field-by-field.

2. **V-Bounce divergences are intentional, not bugs.** STORY-013-02 Developer will read V-Bounce's `update_state.mjs` and see no auto-escalate logic; likewise no `init_sprint.mjs` overwrite refusal. Resist "port faithfully" reflex. Follow the STORIES, not V-Bounce, when they disagree. V-Bounce is reference, not canon.

3. **Path for state.json is load-bearing.** STORY-013-03's `run_script.sh` preflight greps `.cleargate/sprint-runs/*/state.json`. STORY-013-05's circuit-breaker (M2) reads it at `.cleargate/sprint-runs/<id>/state.json`. STORY-013-02 must ship exactly this path. **Not `.vbounce/state.json` or `.cleargate/state.json` — always per-sprint.**

4. **Three-surface landing (R9).** Every M1 story has a `.cleargate/` live path AND a `cleargate-planning/` mirror path. Developer on each story MUST verify with `diff` post-edit that both paths have identical content. STORY-013-03 additionally requires `chmod 755` on both mirrors (planning-mirror file modes must match). Reporter will flag any missing surface at close.

5. **Report-path divergence (013-03).** V-Bounce writes `<worktree>/.vbounce/reports/pre-<mode>-scan.txt`. Our story requires `<worktree>/.cleargate/reports/pre-<mode>-scan.txt`. If 013-03 Developer copies V-Bounce path verbatim, STORY-013-05 (M2) reads the wrong directory and circuit-breaker fails silently. QA: grep the shipped `pre_gate_runner.sh` for `.vbounce` — must return zero.

6. **protocol §10 vs §15.** STORY-013-01 story text says "protocol §10"; actual next free section number is §15 (§§1–14 exist). Developer on 013-01: USE §15. All story-text references to "§10" should be read as "the new worktree section." If the orchestrator also writes `cleargate-protocol.md` §10 somewhere else by accident during SPRINT-09, conflict is a merge-time kick-back.

7. **architect.md edit ordering (013-01 vs 013-04).** 013-01 patches `developer.md`; 013-04 patches `architect.md`. These are different files — no direct collision. But if orchestrator reorders (runs 04 before 01), 04's sprint-context grep for `#worktree` flashcard tag returns empty (tag only lands in 013-01). Enforce order.

8. **`.gitignore` precedence (SPRINT-09 R4).** Root `.gitignore` line 12 carves `/.claude/` for the live dogfood. Adding `.worktrees/` at the end is safe (last-match wins in gitignore; no overlap). But if Developer accidentally adds `!.worktrees/` anywhere or puts `.worktrees/` above `/.claude/`, ordering is fine but a future `!.worktrees/STORY-XXX/inner` override would get clipped — STORY-013-01 must add `.worktrees/` as a bare directory pattern with no negations.

9. **SPRINT-09 runs on v1 — do not exercise v2 scripts mid-sprint (R1).** The scripts ship, but no story in M1 invokes them. No `node .cleargate/scripts/init_sprint.mjs S-09 …`, no `.worktrees/STORY-013-01/` worktree. Dev test scenarios use throwaway IDs (`STORY-013-FAKE`, `S-FAKE`) on temp dirs, then clean up. If QA sees a real `.cleargate/sprint-runs/S-09/state.json` created during SPRINT-09, kick back.

10. **FLASHCARD.md tag vocabulary.** STORY-013-01 adds `#worktree` (new) and uses `#mcp` (new). Update `.claude/skills/flashcard/SKILL.md` tag list (lines 37–50)? **NO — that's out of scope for 013-01.** Tags can be added freely per SKILL line 37 ("append new tags freely, but prefer these"). Don't grow the skill.

## Open decisions for orchestrator

1. **Commit to `main` or to a `sprint/S-09` branch?** SPRINT-09 is v1, so we stay on `main` by default. If we want to dogfood the sprint-branch cut, STORY-013-01's Developer would cut `sprint/S-09` from `main` before committing. **Recommendation: stay on `main`** (R1 — don't use v2 behavior mid-v1-sprint). Escalate if orchestrator prefers partial dogfood.

2. **`gate-checks.json` committed vs generated?** STORY-013-03 §3.1 marks it as "(generated, not checked in — or: committed with defaults?)". Plan above commits defaults (both in `.cleargate/scripts/` and `cleargate-planning/` mirror) so first-sprint ingest is zero-config. Orchestrator: confirm. If orchestrator wants per-project regeneration, `init_gate_config.sh` must also handle "file present but stale" — that's out of scope for 013-03.

3. **MANIFEST.json regeneration.** STORY-013-04 patches `.claude/agents/architect.md` which is manifest-tracked. The Developer runs `npm run build` in `cleargate-cli/` to regenerate SHA? Or does a follow-up infra commit handle it? **Assumption: Developer runs the build as part of the commit** (same pattern as SPRINT-04). Escalate if different.

4. **New runtime dep in 013-02 `node --test`?** `node:test` is built-in since Node 18, shipped with Node 24 LTS. No new dep. Confirmed — no orchestrator action needed.

5. **`execution_mode` field in state.json on SPRINT-09's own fake-sprint tests.** If STORY-013-02 tests seed `state.json` for a throwaway `S-FAKE`, what `execution_mode` value? **Set to `"v1"` in test fixtures** — matches SPRINT-09's actual mode. When SPRINT-10 initializes state.json under v2, `init_sprint.mjs` will read the Sprint Plan's frontmatter `execution_mode` value; v1 is correct default. Confirm.

6. **`.cleargate/sprint-runs/S-09/sprint-context.md` committed.** STORY-013-04 creates this file as a live instance. Is it team-shared (commit) or per-machine (gitignore)? Plan above commits it — matches existing `.cleargate/sprint-runs/SPRINT-NN/REPORT.md` pattern. Orchestrator: confirm. If gitignored, `.gitignore` needs another pattern; that's additional 013-01 scope.

## Orchestrator Decisions (resolved 2026-04-21)

Answered by Vibe Coder (sandro). All six decisions above are now locked; every M1 Developer task brief must quote these answers verbatim.

| # | Decision | Rationale |
|---|---|---|
| 1 | **Cut `sprint/S-09` from `main` at sprint start.** Each M1 story commits sequentially to `sprint/S-09`. Merge to `main` ONLY at sprint close. Worktrees are NOT created (that's v2 behavior; this sprint is v1). "Soft v2" — branch hierarchy yes, per-story worktrees no. | Rollback safety without dogfooding unshipped features. If mid-sprint work breaks, `main` stays clean until sprint close. Supersedes decision-1 recommendation above ("stay on main"). |
| 2 | **Commit `.cleargate/scripts/gate-checks.json` baseline with Node+TS defaults.** `init_gate_config.sh` runs only when the file is missing (edge case post-uninstall). No auto-detection. | Stack is fixed (Node 24 + TS + Fastify + Drizzle + SvelteKit + Vitest). Auto-detection adds non-determinism between machines. |
| 3 | **Developer regenerates MANIFEST.json per story** via `npm run build` in `cleargate-cli/`. The existing `prebuild` hook handles it automatically. Commit the manifest alongside the story's scaffold edits. | Stale manifests compromise scaffold integrity. Per-story is auto-cheap; sprint-close batching leaves the branch dirty mid-sprint. |
| 4 | **`execution_mode: "v2"` in STORY-013-02 test fixtures.** Code being tested IS v2 code regardless of what sprint we're running. Fixture represents a v2 sprint's state. | Under v1 the code path is inactive, so no v1 fixtures needed. Supersedes plan's earlier "v1 in fixtures" suggestion (decision 5 above). |
| 5 | **Commit `.cleargate/sprint-runs/S-09/sprint-context.md`.** Per-sprint audit artefact (locked-deps snapshot + active FLASHCARD tags at sprint start); same policy as sprint plans and REPORT.md. | Diffable across sprints — reporter uses prior sprint-context to compute drift. Gitignoring loses the audit trail. |
| 6 | **No auto-update of `gate-checks.json` on stack change.** Explicit config; stack drift = human review. Add a `cleargate doctor` check that warns when `package.json` deps diverge from `gate-checks.json` signatures. *(Out of scope for STORY-013-03 — file as a follow-up for SPRINT-11.)* | Auto-update silently masks regressions. Doctor warning is lazy (runs only when invoked) but loud when drift appears. |

**Developer prerequisites downstream of these decisions:**

- **STORY-013-01 first command:** `git checkout -b sprint/S-09 main` before any edit. All M1 story commits land on `sprint/S-09`, not `main`.
- **STORY-013-01 scope addition:** nothing — `.cleargate/sprint-runs/*/sprint-context.md` is NOT gitignored (decision 5), so no `.gitignore` change beyond `.worktrees/`.
- **STORY-013-02 fixture values:** every seeded `state.json` uses `execution_mode: "v2"` (decision 4). Unit tests assert `init_sprint.mjs` accepts both `v1` and `v2` values; only `v2` is exercised in fixtures.
- **STORY-013-03 deliverable:** `gate-checks.json` IS a committed file (decision 2). Three-surface landing includes it: `.cleargate/scripts/gate-checks.json` + `cleargate-planning/.cleargate/scripts/gate-checks.json`.
- **STORY-013-04 committed instance:** `.cleargate/sprint-runs/S-09/sprint-context.md` is committed (decision 5); `git add` it in the same commit as the template.
- **Every story:** run `npm run build` in `cleargate-cli/` as part of the commit to refresh MANIFEST.json (decision 3).
