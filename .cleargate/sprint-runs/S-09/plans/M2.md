# Milestone M2 — Execution Phase v2 Agent + Protocol Wiring

**V-Bounce HEAD SHA pinned** (from M1.md line 3): `2b8477ab65e39e594ee8b6d8cf13a210498eaded`. Every §4.2 reference below cites this SHA.

**M2 goal:** wire v2 behavior on top of M1 infrastructure. Ship the circuit-breaker + walkthrough + CR-triage vocabulary (05), Sprint Report v2 + close pipeline (07), Sprint Planning v2 Architect Design Review (09), immediate flashcard gate (06), and the `execution_mode: v1|v2` routing flag (08). Every new behavior is gated behind `execution_mode: v2`; SPRINT-09 itself remains on v1 until STORY-013-08 merges and flips nothing for this sprint (the flag defaults `v1`).

**Story markdown pending:** STORY-013-05 through -09 files do NOT yet exist in `pending-sync/` (verified 2026-04-21: `ls .cleargate/delivery/pending-sync/STORY-013-*.md` returns only 01–04). This plan derives per-story scope from SPRINT-09 §Consolidated Deliverables lines 55–59, EPIC-013 §4.2 (lines 180–193), and §5 Gherkin scenarios (lines 232–310). **Orchestrator must draft the five STORY files before developer spawn** — see Open Decision 1.

## Stories: STORY-013-05, STORY-013-06, STORY-013-07, STORY-013-08, STORY-013-09

## Wave: W1 (sequential — SPRINT-09 runs `execution_mode: v1`, no parallel worktrees)

## Order

Strict: **05 → 07 → 09 → 06 → 08** (sprint plan line 72).

- **05 first** — defines event-type vocabulary (`UR:review-feedback`, `UR:bug`, `CR:bug|spec-clarification|scope-change|approach-change`, circuit-breaker categories `test-pattern|spec-gap|environment`) that 06/07/09 reference.
- **07 after 05** — `close_sprint.mjs` + `suggest_improvements.mjs` + Sprint Report v2 template consume 05's event types (§3 Execution Metrics tallies `CR` and `UR` counts; §4 Lessons cites circuit-breaker categories).
- **09 after 05/07** — Sprint Design Review §2 Execution Strategy cites the same event-type + state-transition vocabulary from 05 and the Sprint Report v2 schema from 07.
- **06 second-to-last** — smallest delta (one frontmatter field + protocol section + orchestrator gate). Minimal dependency on 05/07/09, but must run before 08 so the gate exists when the flag flips.
- **08 last** — flips `execution_mode` routing. If shipped earlier, every other M2 story would have to consume its CLI wrappers, creating coupling the sprint plan explicitly wants to avoid (R6).

## Per-story blueprint

### STORY-013-05 — Orchestrator Interrupt Handling (circuit breaker + walkthrough + mid-sprint CR triage)

**Files to modify:**
- `.claude/agents/developer.md` — append new `## Circuit Breaker` section after current `## Worktree Contract` (file ends at line 68; append). Rules: (a) after ~50 tool calls with no successful test run OR 2 consecutive identical failures, halt and write `STORY-NNN-NN-dev-blockers.md` to `.cleargate/sprint-runs/<id>/reports/` instead of a dev report; (b) Blockers Report has three required sections: `## Test-Pattern`, `## Spec-Gap`, `## Environment` — each with one sentence or `N/A`; (c) V-Bounce `skills/agent-team/SKILL.md` §"Step 2f: Green Phase Circuit Breaker". Gate rule verbatim: "These rules apply under `execution_mode: v2`. Under v1 they are informational."
- `.claude/agents/architect.md` — append `## Blockers Triage` section after current `## Adjacent Implementation Check` (line 48, from M1). Routes: Test-Pattern → re-launch Developer with fixture hint; Spec-Gap → return to orchestrator with user question; Environment → pre-gate re-run. 3 consecutive circuit-breaker hits on same story → `Escalated`.
- `.cleargate/knowledge/cleargate-protocol.md` — append new `## 16. User Walkthrough on Sprint Branch (v2)` (§15 is worktree, next free is §16) and `## 17. Mid-Sprint Change Request Triage (v2)` after current §15 (line 570 begins §15; append after its end). §16 codifies `UR:review-feedback` vs `UR:bug` split (V-Bounce SKILL Step 5.7 at pinned SHA), walkthrough runs on `sprint/S-XX` pre-merge-to-main, `UR:bug` counts toward Bug-Fix Tax, `UR:review-feedback` does not. §17 ports the 4-category table verbatim (Bug / Spec-Clarification / Scope-Change / Approach-Change) with bounce-counter effects per EPIC-013 §4.2 row 7.
- `cleargate-planning/.claude/agents/developer.md` — mirror Circuit Breaker append.
- `cleargate-planning/.claude/agents/architect.md` — mirror Blockers Triage append.
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — mirror §§16–17.

**Files to create:** none (Blockers Report is an artifact pattern, not a template file — the shape lives in developer.md).

**Schema changes:** none.

**Test scenarios (from EPIC-013 §5 lines 258–280, all 3):**
1. Circuit breaker on runaway Developer (§5 lines 258–263) — seed a fake tool-call log with 50 entries, spawn Developer dry-run, assert it emits `STORY-XXX-dev-blockers.md` with all three sections instead of a dev report.
2. User walkthrough splits enhancement from bug (§5 lines 273–280) — feed two pieces of mock feedback via a throwaway sprint markdown §4 append; assert `UR:review-feedback` does NOT touch Bug-Fix Tax, `UR:bug` DOES.
3. Mid-sprint CR classified to category + counter effect — for each of 4 categories, assert bounce-counter delta matches §17 table.

All scenarios are markdown-plus-grep tests (no runtime); wrap as a bash test script at `.cleargate/scripts/test/test_interrupt_contract.sh`.

**Reuse (from sprint-context.md Adjacent Implementations, lines 56–66):**
- `VALID_STATES`, `TERMINAL_STATES` from `.cleargate/scripts/constants.mjs` — Escalated state is already in the enum; do NOT redefine.
- `update_state.mjs --arch-bounce` / `--qa-bounce` from `.cleargate/scripts/` — circuit-breaker triage in architect.md INVOKES these via `run_script.sh`, does not wrap them.
- `run_script.sh` from `.cleargate/scripts/` — all architect.md script refs go through it.
- Protocol §15 worktree rules — do not restate; §§16–17 append, they don't modify §15.

**Gotchas from code inspection:**
- **§ numbering is load-bearing.** Protocol line 570 begins §15 (added by STORY-013-01 in M1). Story-text says "§§12-13" — that's stale numbering from EPIC-013 §4.2 table lines 186–188 which was drafted before M1 renumbered. Developer on 013-05: use §§16–17 (next free after M1's §15). Cite protocol line count verified 647 lines total.
- `.claude/agents/developer.md` ends at line 68 (M1 added Worktree Contract). Circuit Breaker goes after, with NO renumbering of existing sections.
- `.claude/agents/architect.md` ends at line 61 (M1 appended Adjacent Implementation Check at lines 46–48). Blockers Triage goes after that subsection, before `## Guardrails` at line 50 — insert between lines 48 and 50.
- Blockers Report path is `.cleargate/sprint-runs/<id>/reports/STORY-NNN-NN-dev-blockers.md`, NOT `.cleargate/reports/` (that's pre-gate scanner output per M1 STORY-013-03). Two different directories; don't conflate.

### STORY-013-07 — Sprint Report Close Pipeline (template + prefill + close + suggest_improvements + reporter rewrite)

**Files to create:**
- `.cleargate/templates/sprint_report.md` — new template. §§1–6 per EPIC-013 §4.2 row 9 (lines 190) + §4.5 post-close steps (lines 212–228): §1 What Was Delivered (user-facing vs internal split), §2 Story Results + Change Log of CRs, §3 Execution Metrics (bounce ratio, Bug-Fix Tax %, Enhancement Tax %, first-pass success rate, token-divergence flag), §4 Lessons (flashcards summary by tag), §5 Framework Self-Assessment split by Templates/Handoffs/Skills/Process/Tooling, §6 Change Log. Frontmatter: `sprint_id`, `status`, `generated_at`, `generated_by`, `template_version: 1`.
- `cleargate-planning/.cleargate/templates/sprint_report.md` — mirror.
- `.cleargate/scripts/prefill_report.mjs` — reads `.cleargate/sprint-runs/<id>/state.json` (M1 schema), `.cleargate/sprint-runs/<id>/token-ledger.jsonl`, all `STORY-*-dev.md` and `STORY-*-qa.md` reports in the sprint-runs dir. Backfills deterministic YAML frontmatter fields missing in any agent report (story_id, sprint_id, commit_sha, bounce counts). Writes in-place (atomic tmp+rename per M1 pattern). Idempotent: re-run on a fully-prefilled report is a no-op.
- `.cleargate/scripts/close_sprint.mjs` — args `<sprint-id>`. Steps: (1) load state.json via `validate_state.mjs`; (2) refuse if any story state ∉ `TERMINAL_STATES` (exit non-zero with stderr listing non-terminal stories, matches §5 scenario lines 282–288); (3) invoke `prefill_report.mjs` on all agent reports; (4) spawn `reporter` via stub (orchestrator-invoked — script only validates preconditions, does NOT fork the agent itself); (5) on Reporter success, flip `sprint_status` to `Completed` in state.json; (6) invoke `suggest_improvements.mjs` unconditionally.
- `.cleargate/scripts/suggest_improvements.mjs` — reads REPORT.md §5 Framework Self-Assessment tables + last sprint's suggestion file if present. Emits `.cleargate/sprint-runs/<id>/improvement-suggestions.md` with stable `SUG-<sprint>-<n>` IDs (R5). Auto-append-only: never rewrites existing entries; re-running produces no new entries if all suggestions already captured. Categorize per §5 template split.
- `.cleargate/scripts/test/test_close_pipeline.sh` — drives scenarios 7/8/9.
- `cleargate-planning/.cleargate/scripts/{prefill_report,close_sprint,suggest_improvements}.mjs` — mirrors (chmod optional; these are `.mjs` invoked via `run_script.sh`).

**Files to modify:**
- `.claude/agents/reporter.md` — rewrite §6 Synthesize to emit the new template shape (§§1–6 above) instead of the current PM/Dev split. Keep the Inputs + Workflow scaffold; replace the markdown skeleton starting at line 40 with the v2 template structure. Add three-source token reconciliation contract per EPIC-013 §4.5 step 4: token-ledger.jsonl primary, story-doc Token Usage secondary, task-notification tertiary, flag any divergence >20% in §5 Tooling (scenario 8 / §5 lines 298–302).
- `cleargate-planning/.claude/agents/reporter.md` — mirror.

**Schema changes:** none to state.json (locked at v1 from M1). Sprint Report template is the schema.

**Test scenarios (from EPIC-013 §5 lines 282–310, 4 scenarios):**
1. close_sprint refuses non-terminal state (§5 lines 282–288) — seed state.json with one story `Bouncing`, invoke script, assert exit non-zero + stderr message.
2. Sprint report written before state=Completed (§5 lines 290–295) — all stories terminal, invoke script; assert REPORT.md is written, user-ack simulated via `--assume-ack` flag, THEN state flips; `suggest_improvements.mjs` runs.
3. Token reconciliation flags divergent source (§5 lines 297–302) — seed ledger + task notifications with 36% divergence; assert Reporter output §5 contains divergence flag under Tooling.
4. run_script.sh self-repair on missing state.json (§5 lines 304–310) — delete state.json, invoke `run_script.sh update_state.mjs STORY-X "Bouncing"`; assert wrapper prints structured diagnostic, orchestrator (simulated by bash test) re-inits once, retry succeeds, incident logged to `## Script Incidents` in a throwaway agent report.

**Reuse (from sprint-context.md Adjacent Implementations):**
- `VALID_STATES`, `TERMINAL_STATES`, `SCHEMA_VERSION`, `BOUNCE_CAP` from `.cleargate/scripts/constants.mjs`.
- `validateState` export from `.cleargate/scripts/validate_state.mjs` — close_sprint.mjs MUST import this, not re-parse state.json independently.
- atomic write-then-rename helper pattern from `.cleargate/scripts/update_state.mjs` — reuse the same `<final>.tmp.<pid>` convention; do NOT invent new atomic-write code.
- `run_script.sh` from `.cleargate/scripts/` — suggest_improvements invoked via wrapper, same diagnostic-block contract.
- Token ledger at `.cleargate/sprint-runs/<id>/token-ledger.jsonl` — already populated by SubagentStop hook (`.claude/hooks/token-ledger.sh`, untouched per EPIC-013 §4.3 lines 198).

**Gotchas from code inspection:**
- **Reporter rewrite regression risk (R8, sprint line 91).** Current reporter.md is 129 lines with a tested SPRINT-08 output. Do NOT delete the current spec until scenario 2 passes on a SPRINT-08-shaped fixture. Fallback plan: keep `cleargate-planning/.claude/agents/reporter.md` at the OLD shape until dev report passes; swap both files in the same commit. If post-swap the SPRINT-09 Reporter itself fails, `git revert` the swap commit and run the old Reporter (per R8 mitigation).
- **Reporter §5b Flashcard audit (lines 32–37 of reporter.md).** Current spec has a stale-detection pass — PRESERVE this inside the new template's §4 Lessons section (do not drop it).
- **SubagentStop hook attribution bug** (flashcard 2026-04-19 `#reporting #hooks #ledger`) — ledger rows still tag against orchestrator session, not subagent. Reconciliation code must tolerate rows lacking `story_id`; attribute to `unassigned` bucket rather than crash.
- **Three-surface landing** — new `.mjs` scripts also go to `cleargate-planning/.cleargate/scripts/`; MANIFEST.json regenerates via `npm run build` in cleargate-cli (M1 Decision 3).
- Close pipeline does NOT archive the sprint file (`pending-sync/` → `archive/`) — that stays with the human orchestrator per EPIC-013 §4.5 step 7 "existing ingest hook takes it from there." Script scope ends at `Completed` flip + improvement-suggestions file.

### STORY-013-09 — Sprint Planning v2 (Architect Sprint Design Review + decomposition signals + enforcing Gate 2)

**Files to modify:**
- `.claude/agents/architect.md` — append `## Sprint Design Review` section after `## Blockers Triage` (added by 013-05). Contract: before human confirms a v2 Sprint Plan, Architect writes Sprint Plan §2 "Execution Strategy" containing (a) phase plan (parallel vs sequential story groups), (b) merge ordering from shared-file surface analysis (grep each story's "Files to modify" list for overlaps), (c) shared-surface warnings, (d) ADR-conflict flags. V-Bounce ref `skills/agent-team/SKILL.md` § "Architect Sprint Design Review (Phase 2 → Phase 3 transition)" at pinned SHA.
- `.cleargate/templates/story.md` — append two frontmatter fields after existing `complexity_label` (line 38 of template): `parallel_eligible: "y"` (default) and `expected_bounce_exposure: "low"` (default). Add a §0.1 blurb in the instructions explaining the fields.
- `.cleargate/templates/Sprint Plan Template.md` — insert new `## 2. Execution Strategy` section after the existing "Consolidated Deliverables" section. §1 table gains two columns: `Parallel?` and `Bounce Exposure`. Add `execution_mode: "v1"` field to frontmatter (already present in SPRINT-09 line 20 as precedent).
- `.cleargate/knowledge/cleargate-protocol.md` — amend §2 "The Front Gate (Triage)" Gate 2 sub-rule: for sprints with `execution_mode: v2`, 🔴 High-ambiguity epics BLOCK bounce start unless sprint frontmatter has explicit `human_override: true` + reason captured in §0 Readiness Gate. Under v1, advisory only (unchanged behavior).
- `cleargate-planning/.claude/agents/architect.md` — mirror.
- `cleargate-planning/.cleargate/templates/story.md` — mirror.
- `cleargate-planning/.cleargate/templates/Sprint Plan Template.md` — mirror.
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — mirror §2 amendment.

**Files to create:**
- `.cleargate/sprint-runs/S-09/sprint-10-design-review-dryrun.md` — per R7 (sprint line 90), final scenario output. Architect generates SPRINT-10's §2 Execution Strategy as if SPRINT-10 were a v2 sprint. **Not auto-promoted.** Contents: synthetic 3-story SPRINT-10 scope inferred from EPIC-013 R10 "first v2 validation run" + backlog (PROPOSAL-010 or similar), phase plan, merge ordering, shared-surface warnings, ADR flags. User reviews; orchestrator does NOT integrate into actual SPRINT-10 plan.

**Schema changes:** story.md frontmatter gains 2 fields (default values don't break existing stories); Sprint Plan Template gains 1 field + 2 columns (precedent exists in SPRINT-09 frontmatter).

**Test scenarios (derived from sprint plan lines 366–374):**
1. Story template carries new fields — grep both live + mirror templates for `parallel_eligible` + `expected_bounce_exposure`.
2. Sprint Plan Template §2 exists + new columns — grep for `## 2. Execution Strategy` + the two new §1 columns.
3. Gate 2 enforcing path — synthetic 🔴-ambiguity Epic + v2 Sprint Plan without `human_override`; invoke Gate-2 check (manual walkthrough against protocol §2); assert block message.
4. Gate 2 override path — same inputs + `human_override: true` with reason; assert Gate 2 passes.
5. Architect Sprint Design Review dry-run — produce `sprint-10-design-review-dryrun.md`; grep for §2 subsections (phase plan, merge ordering, shared-surface warnings, ADR flags).

**Reuse:**
- Protocol §2 structure already defines Gate 1 / Gate 2 prose (protocol.md line 20 starts §2). Amend the Gate 2 block in place; do not rewrite the §2 header.
- Architect `## Adjacent Implementation Check` + `## Blockers Triage` (from M1 + 013-05) — Sprint Design Review is a parallel subsection at the same level, do not nest inside them.
- Sprint Plan Template already has `execution_mode: v1` convention demonstrated in SPRINT-09 frontmatter line 20 — adopt the same YAML style.

**Gotchas from code inspection:**
- **R7: no live v2 test target.** DoD per sprint line 90: Architect generates SPRINT-10's Execution Strategy §2 as the final scenario, output at `.cleargate/sprint-runs/S-09/sprint-10-design-review-dryrun.md`. QA on 013-09: this file must exist at close, reviewed by user, NOT committed as the real SPRINT-10 plan.
- Protocol §2 amendment must not renumber §§3–17. Amendment is a sub-bullet addition to the existing Gate 2 text at lines 20–57.
- `parallel_eligible` and `expected_bounce_exposure` are v2-only signals. Existing SPRINT-09 M1-era stories (01–04) do NOT need retroactive field backfill — the fields default to `y`/`low` if missing, and SPRINT-09 is v1 anyway.
- Sprint Plan Template §1 table column additions may break existing SPRINT-09 markdown rendering — but SPRINT-09 sprint file (lines 1–123) does NOT use a story table with these columns (it uses a bulleted list). Safe.

### STORY-013-06 — Immediate Flashcard Gate

**Files to modify:**
- `.claude/agents/developer.md` — add `flashcards_flagged:` field to the `## Output shape` block (line 36–46 currently). Default empty list `[]`. When populated, contains one-liner flashcard candidates per `FLASHCARD.md` format (YYYY-MM-DD · #tag · lesson).
- `.claude/agents/qa.md` — add same `flashcards_flagged:` field to the Output shape block (lines 35–45). QA's list is additive to Developer's, not overwriting.
- `.cleargate/knowledge/cleargate-protocol.md` — append new `## 18. Immediate Flashcard Gate (v2)` (§§16–17 from 013-05 exist; next free is §18). Rule: after story N's merge into `sprint/S-XX`, orchestrator MUST process all `flashcards_flagged` entries from story N's dev + qa reports (record to `FLASHCARD.md` OR reject) BEFORE creating story N+1's worktree. Under v1, advisory only. V-Bounce ref `skills/agent-team/SKILL.md` §"Step 5.5: Immediate Flashcard Recording (Hard Gate)" at pinned SHA.
- `cleargate-planning/.claude/agents/developer.md` — mirror.
- `cleargate-planning/.claude/agents/qa.md` — mirror.
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — mirror §18.

**Files to create:** none.

**Schema changes:** dev + QA report frontmatter gains one optional field `flashcards_flagged: []`.

**Test scenarios (from EPIC-013 §5 lines 265–271, 1 required):**
1. Immediate flashcard hard gate (§5 lines 265–271) — synthetic `STORY-014-05-dev.md` with `flashcards_flagged: ["2026-04-22 · #test-harness · vitest fake-timers conflict"]`; orchestrator dry-run attempts to create `.worktrees/STORY-014-06/`; assert gate presents flashcards for approval, and only after approval does worktree creation proceed. Bash test drives this via a mock orchestrator step file.

**Reuse:**
- `.cleargate/FLASHCARD.md` append format from `.claude/skills/flashcard/SKILL.md` — do NOT invent a new format; gate writes to FLASHCARD.md via the existing skill.
- Protocol §§16–17 from 013-05 already exist — §18 numbering must be sequential (confirm §§16, 17 were added before 013-06 Developer spawns; enforcement via execution order).
- Developer.md and qa.md output-shape blocks already exist; add one line each, do not restructure.

**Gotchas from code inspection:**
- Story text says "protocol §11" but §11 is already `Document Metadata Lifecycle` (protocol line 356). Real section is §18 (after 013-05's §§16–17). Developer on 013-06: use §18, not §11.
- `flashcards_flagged` is a YAML list of strings, each matching FLASHCARD.md one-liner format. Do NOT use an object shape; keep it flat so the orchestrator can blindly append to FLASHCARD.md.
- Skill `.claude/skills/flashcard/SKILL.md` tag vocabulary (lines 37–50) — new entries must use existing tags or prefix-match; the gate does not enforce tag validation but the skill's record step does.
- **SPRINT-09 dogfood note** (sprint line 121): "Every story's flashcards processed at the Step-5.5-equivalent gate (we're building this rule during the sprint — apply it manually from STORY-013-06 onwards, as a dogfood check)." Post-013-06 M2 stories (only 013-08 remains) MUST have flashcards processed between merges even though v1 is still active. Orchestrator task.

### STORY-013-08 — Execution Mode Flag + CLI wrappers

**Files to modify:**
- `.cleargate/templates/Sprint Plan Template.md` — ensure `execution_mode: "v1" | "v2"` frontmatter field is documented (013-09 may have already added it; 013-08 owns the enum-constraint + orchestrator-routing behavior). Default `v1`.
- `cleargate-planning/.cleargate/templates/Sprint Plan Template.md` — mirror.
- `.cleargate/knowledge/cleargate-protocol.md` — new `## 19. Execution Mode Routing (v2)` section. Rule: orchestrator reads Sprint Plan frontmatter `execution_mode`; if `v2`, all §§15–18 rules are enforcing; if `v1`, advisory. The flag is sprint-scoped, NOT global. V-Bounce ref: none (ClearGate-specific opt-in design).
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — mirror §19.

**Files to create (CLI wrappers):**
- `cleargate-cli/src/commands/sprint.ts` — two sub-handlers: `sprintInitHandler({sprintId, stories})` shells out to `run_script.sh init_sprint.mjs <id> --stories <csv>`; `sprintCloseHandler({sprintId})` shells out to `run_script.sh close_sprint.mjs <id>`. Both read `.cleargate/sprint-runs/<id>/` relative to cwd, same convention as `cleargate wiki build`.
- `cleargate-cli/src/commands/story.ts` — `storyStartHandler({storyId})` shells out to create worktree (reads sprint branch from state.json, runs `git worktree add`); `storyCompleteHandler({storyId})` shells out to `run_script.sh complete_story.mjs <id>` (script stub; full complete_story.mjs is out of scope per EPIC-013 §0 target_files — it's listed but not expanded here; 013-08 only ships the wrapper, complete_story.mjs body is EPIC-013 future work or a bonus).
- `cleargate-cli/src/commands/gate.ts` — EXTEND existing file (currently has `gateCheckHandler` + `gateExplainHandler` per cli.ts line 10). Add `gateQaHandler` + `gateArchHandler` that shell out to `run_script.sh pre_gate_runner.sh qa|arch <worktree> <branch>`. Verify no collision with existing `gate check` semantics — `gate qa` / `gate arch` are new subcommand paths.
- `cleargate-cli/src/commands/state.ts` — `stateUpdateHandler({storyId, newState})` shells out to `run_script.sh update_state.mjs`; `stateValidateHandler({sprintId})` shells out to `run_script.sh validate_state.mjs`.
- `cleargate-cli/src/cli.ts` — wire 4 new command groups via `program.command('sprint')`, `.command('story')`, `.command('gate')` (already exists — extend), `.command('state')`. All handlers gate on `execution_mode: v2` by reading the active sprint file; under v1 they print `v1 mode active — command inert. Set execution_mode: v2 in sprint frontmatter to enable.` and exit 0.
- Unit tests: `cleargate-cli/src/commands/{sprint,story,gate,state}.test.ts` — mock `child_process.spawn` / `execSync`; assert correct script invocation + v1-inert behavior + v2-active behavior.

**Schema changes:** Sprint Plan Template frontmatter enum constraint on `execution_mode` (validation is advisory; no hard gate in 013-08 beyond CLI).

**Test scenarios (inferred from SPRINT-09 DoD lines 114–121 + R6 lines 89):**
1. `execution_mode: v2` present and inert under v1 (sprint line 117) — synthetic SPRINT-99.md with `execution_mode: "v1"`; invoke `cleargate sprint init SPRINT-99`; assert exit 0 with "v1 mode active — command inert" message.
2. `execution_mode: v2` active routes to v2 script — synthetic SPRINT-99.md with `execution_mode: "v2"`; invoke `cleargate sprint init SPRINT-99 --stories STORY-X`; assert `run_script.sh init_sprint.mjs` is invoked (mock spawn).
3. No CLI collision audit (R6 lines 89) — run `cleargate --help` tree and assert no duplicate subcommand names across sprint/story/gate/state groups + existing commands (join, init, wiki, push, pull, sync, conflicts, etc.). `gate` is the only existing group; `gate qa` + `gate arch` must not collide with `gate check` + `gate explain`.
4. All four CLI wrappers invoked via run_script.sh — grep compiled dist/ for direct `node .cleargate/scripts/*.mjs` calls; expect zero. All must route through `run_script.sh`.
5. Flag flip roundtrip — take SPRINT-99.md, change `execution_mode: "v1"` → `"v2"`, invoke `cleargate sprint init SPRINT-99`; assert behavior changes from scenario 1 to scenario 2 without CLI rebuild.

**Reuse:**
- `.cleargate/scripts/run_script.sh` from M1 — every CLI handler shells out through it, never direct `node` invocation (EPIC-013 §0 rule 5).
- `.cleargate/scripts/{init_sprint,update_state,validate_state,validate_bounce_readiness,close_sprint,suggest_improvements}.mjs` — all M1 + 013-07 scripts; CLI handlers are thin wrappers.
- `cleargate-cli/src/commands/gate.ts` current structure — `gateCheckHandler`/`gateExplainHandler` at `cli.ts` line 10. Extend the same file + same export pattern.
- Profile + mcpUrl globals from `cli.ts` lines 24–28 — new handlers ignore them (local scripts, no MCP round-trip).
- `cleargate-cli/src/commands/_stub.ts` pattern — use it for minimally-tested handlers if needed.

**Gotchas from code inspection:**
- **R1 (sprint line 84): SPRINT-09 own-build blocks invoking unshipped v2 commands.** 013-08 Developer MUST NOT invoke `cleargate sprint init` / `cleargate state update` mid-execution. Tests use `cleargate --help` output + unit-test mocks, never live invocation against SPRINT-09's own state.json. Scenarios 1/2/5 use synthetic `SPRINT-99.md` fixture in `cleargate-cli/src/commands/fixtures/`.
- **R6 CLI collision audit** — existing `gate` command has 2 subcommands (`check`, `explain`). `gate qa` + `gate arch` are new; confirm commander.js doesn't conflict (it won't — different subcommand names).
- `cli.ts` uses top-level `program.command('wiki')` as a command group pattern (line 63+ approximately). Follow the same pattern for `sprint`, `story`, `state`, and extend `gate`.
- `cleargate-cli/src/cli.ts` imports are all from `./commands/<name>.js` (compiled `.js`). Source is `.ts`; build step produces `.js`. New files must be `.ts` in source.
- `npm run build` in `cleargate-cli/` regenerates dist + MANIFEST.json (M1 Decision 3). Developer runs this pre-commit.
- **Flag-default semantics.** SPRINT-09 frontmatter line 20 already has `execution_mode: "v1"`. Do NOT flip to v2 as part of 013-08's own commit — that would activate v2 behaviors mid-sprint and violate R1. The flag stays `v1` for SPRINT-09. Flip happens at SPRINT-10 planning time, outside this sprint.
- **Post-013-08, all flashcards flagged mid-sprint must be processed per 013-06's gate** — dogfood rule from sprint line 121.

## Cross-story risks

1. **Protocol § numbering audit (critical).** Story text throughout references "§§10–13" and "§11" — those numbers are already occupied (§10 Knowledge Wiki, §11 Document Metadata, §12 Token Cost Stamping, §13 Scaffold Manifest, §14 Multi-Participant Sync, §15 Worktree Lifecycle from M1). **Real numbers for M2:** §16 User Walkthrough (013-05), §17 Mid-Sprint CR Triage (013-05), §18 Immediate Flashcard Gate (013-06), §19 Execution Mode Routing (013-08). Developers MUST use these numbers, NOT what the story text says. Architect has resolved the numbering here; QA verifies by grepping protocol.md for each new section after merge.

2. **Event-type vocabulary locked by 013-05.** 013-05 defines `UR:review-feedback`, `UR:bug`, `CR:bug|spec-clarification|scope-change|approach-change`, circuit-breaker categories `test-pattern|spec-gap|environment`. 013-07's Sprint Report v2 template §2/§3 and 013-09's Architect Design Review MUST use these exact tokens. If 013-05 ships different casing (e.g. `review-feedback` vs `reviewFeedback`), 013-07 and 013-09 contracts break. QA on 013-05: grep protocol.md §§16–17 for exact tokens + publish them in the sprint-context Adjacent Implementations table.

3. **Reporter rewrite fallback (R8).** 013-07 rewrites `reporter.md`. If the new spec regresses SPRINT-09's own close, sprint can't close. **Fallback plan:** (a) keep `.claude/agents/reporter.md` at current shape in-tree until scenario 2 passes against a SPRINT-08-shaped fixture committed at `.cleargate/sprint-runs/S-09/fixtures/sprint-08-shaped/`; (b) swap both live + planning mirror atomically in the final commit; (c) if SPRINT-09's actual Reporter run fails post-swap, `git revert` the swap commit and run the old Reporter; story returns to Developer. QA on 013-07: verify the fixture exists + the fallback-revert path is documented in the dev report.

4. **No live v2 test target for 013-09 (R7).** Design Review has no in-sprint consumer. DoD is a dry-run output at `.cleargate/sprint-runs/S-09/sprint-10-design-review-dryrun.md`. QA verifies the file exists, has all four §2 subsections, and is NOT mistaken for the real SPRINT-10 plan. Orchestrator does NOT auto-promote.

5. **Three-surface landing (R9) — CLI is a new surface for M2.** 013-08 is the only M2 story touching `cleargate-cli/`. All other stories land in `.cleargate/` + `cleargate-planning/` only. QA on 013-08: verify `cleargate-cli/dist/` regenerates + MANIFEST.json SHA updates + `npm run typecheck` clean in cleargate-cli workspace.

6. **`execution_mode` flag ordering.** 013-08 ships the flag routing LAST. Stories 05/06/07/09 all add "gated behind execution_mode: v2" text to their protocol sections and agent specs — that text is INFORMATIONAL until 013-08's orchestrator gate lands. If 013-08 is deferred (per R10), 05/06/07/09 ship with ungated v2 rules in prose, which is fine for scaffold but means no behavioral change until SPRINT-10.

7. **`flashcards_flagged` adoption ordering.** 013-06 adds the field to dev + qa report frontmatter. Stories 05/07/09 Developer reports cannot include `flashcards_flagged` in their own frontmatter (013-06 hasn't landed yet). Post-013-06 (stories 08 only), Developer MUST include the field. QA checks this only for stories merged AFTER 013-06.

8. **Sprint Plan Template double-edit (013-08 vs 013-09).** Both stories modify `.cleargate/templates/Sprint Plan Template.md` — 013-09 adds §2 Execution Strategy + new §1 columns + `execution_mode` field; 013-08 adds enum constraint + routing documentation for `execution_mode`. Serial execution order (09 before 08) means 013-09 lands the field first; 013-08 amends the surrounding prose. QA on 013-08: verify 013-09's §2 structure is preserved.

9. **Suggest-improvements idempotency (R5).** 013-07 scenario: run `suggest_improvements.mjs` twice on same sprint; second run must produce zero new entries. Implementation: stable `SUG-<sprint>-<n>` IDs computed from hash of the Self-Assessment row's (category, title) tuple. Developer on 013-07: test this explicitly or QA kicks back.

10. **V-Bounce divergences (same class as M1 R2).** 013-05 ports V-Bounce circuit-breaker "50 tool calls" heuristic — our count is orchestrator-observed, not agent-self-reported (Claude Code doesn't expose tool-call count to the agent). Developer on 013-05: write the rule as orchestrator-enforced (orchestrator tracks + terminates agent + spawns with Blockers Report prompt). V-Bounce does the same — confirm by reading `skills/agent-team/SKILL.md` §"Step 2f" at pinned SHA.

## Open decisions for orchestrator

1. **STORY-013-05..09 markdown files must be drafted before Developer spawn.** `ls .cleargate/delivery/pending-sync/STORY-013-*.md` returns only 01–04 as of 2026-04-21. This plan derives scope from SPRINT-09 §Consolidated Deliverables + EPIC-013 §4.2 + §5 Gherkin. **Decision needed:** (a) orchestrator drafts STORY-013-05..09 files using story template before M2 dev work begins (recommended — protocol §2 Gate 2 requires story file existence for bounce start); or (b) this plan serves as the de facto story spec and Developer agents work from M2.md directly (riskier — story template fields like `complexity_label`, `ambiguity`, `parallel_eligible` would not be set). **Recommendation: (a).** Orchestrator task before 013-05 Developer spawn.

2. **Commit scope for sprint-report template (013-07).** Does the new `sprint_report.md` template commit with its first consumer (reporter.md rewrite in same commit), or land as a pre-commit, then reporter.md in a second commit? Single-commit is cleaner; two-commit gives cleaner revert if reporter.md regresses. **Recommendation: single commit** (matches our "one commit per story" convention).

3. **Reporter.md swap fallback plan (R8).** Before final merge of 013-07, capture a SPRINT-08-shaped fixture at `.cleargate/sprint-runs/S-09/fixtures/sprint-08-shaped/` (a minimal state.json + token-ledger.jsonl + 2 dev reports mimicking SPRINT-08). Reporter rewrite is verified against this fixture before atomic swap. **Decision needed:** does orchestrator commit the fixture as part of 013-07 or as a separate prep commit?

4. **CLI verb-group collision risk (R6).** `cleargate-cli/src/cli.ts` currently has 18 command groups (verified by `ls cleargate-cli/src/commands/`). New groups `sprint`, `story`, `state` are new top-level verbs (no collision); `gate` is EXTENSION of existing group (adding `qa`, `arch` subcommands to existing `check`, `explain`). **Recommendation: proceed; no collision detected.** Orchestrator confirms 013-08 developer reads commander.js docs for subcommand-extension pattern.

5. **SPRINT-09's own state.json.** Sprint plan line 114 DoD says "terminal state in whatever state.json artefacts exist post-STORY-013-02." Does SPRINT-09 itself gain a `.cleargate/sprint-runs/SPRINT-09/state.json`? M1 Decision 1 cut `sprint/S-09` branch but did NOT create per-story worktrees or invoke init_sprint.mjs (R1). **Decision: no** — SPRINT-09 continues without a state.json (it's v1). Sprint DoD line 114 is forward-looking to SPRINT-10. Confirm.

6. **Dogfood flashcard gate mid-M2.** Sprint line 121: "apply it manually from STORY-013-06 onwards." Post-013-06, only 013-08 remains. Orchestrator manually processes flashcards between 013-06 merge and 013-08 start. **Action item — no decision needed.**

7. **R10 defer decision deferred to M2 close.** Sprint plan line 93: "if M1 took >60% of budget, defer STORY-013-09." M1 ran over with two dev stream-timeouts per user note. Plan proceeds with 013-09 included because 09 ships the Design Review contract needed for SPRINT-10 to be a v2 plan (per sprint line 90 + R7). **Checkpoint: if M2 tokens exceed 1.6M by 013-07 close, orchestrator escalates defer decision.** Architect does NOT decide.
