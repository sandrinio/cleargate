role: architect

# Milestone: M3 (W3 — Type system + Reporter prep wave)
## Stories: CR-030, CR-033, CR-035
## Wave: W3 (3 parallel Developer dispatches)

## Order

All three CRs ship in W3 as independent parallel dispatches. Within W3, **dispatch CR-030 first, CR-033 second, CR-035 in parallel** (CR-035 has zero overlap with the other two). Merge ordering: **CR-030 → CR-033 → CR-035**. Rationale per sprint plan §2.3:

- CR-030 + CR-033 both touch `.cleargate/knowledge/readiness-gates.md` (line-disjoint inserts but CR-030 owns the `proposal-approved` → `parent-approved` rename at L69, which CR-033's new `existing-surfaces-verified` predicate runs alongside in 3 of the same gate blocks; landing CR-030 first means CR-033 reads stable post-rename gate definitions).
- CR-030 + CR-033 both extend `cleargate-cli/src/lib/readiness-predicates.ts` (CR-030 only conditionally — Path (b) per its §0.5 Q2 needs zero engine change; CR-033 always — new shape #7).
- CR-035 is `.claude/agents/reporter.md` + optional `prep_reporter_context.mjs` digest tweak — disjoint from CR-030/CR-033's surface.

**Pre-W3 manual cache refresh required** (per sprint plan §3 risk row, until CR-038 lands): `for f in .cleargate/delivery/pending-sync/*.md; do cleargate gate check "$f"; done`. The orchestrator runs this before dispatching W3.

**CR-038 already landed in W2** (sprint plan §2.1) so the cache-refresh hook is now active for W3 — but the workaround command is the safety net if any W3 dispatch trips a stale-cache failure mode.

---

## Per-CR blueprint

### CR-030 — Initiative + Sprint first-class citizenship (bucket+type+stamp+id+predicate)

**Lane:** standard. 4 CLI engine files + 1 template + 1 readiness-gates file + tests, plus 2 mirrors. Multi-surface but every touch is additive (new union members, new prefix entries, new regex disjuncts) — no rewrites.

**Parallelism:** dispatch in parallel with CR-033 + CR-035. Merge first within W3 (rename of `proposal-approved` at readiness-gates.md L69 must precede CR-033's insertions in the same gate blocks).

#### Cross-story coupling
- **CR-033 follow:** CR-030 renames `proposal-approved` → `parent-approved` at `.cleargate/knowledge/readiness-gates.md:69`. CR-033's `existing-surfaces-verified` insertions land in the same 3 gate blocks (epic ready-for-decomposition, story ready-for-execution, cr ready-to-apply) but at different line offsets. CR-033 must rebase post-CR-030.
- **CR-034 base (already landed W1):** CR-030 inherits the `declared-item` predicate already used by 6 criteria; the new Initiative gate's `user-flow-populated` and `success-criteria-populated` criteria use `listed-item`, not `declared-item` (per CR-030 §1.5). No engine extension needed.
- **CR-031 base (already landed W1):** CR-030's `parent-approved` predicate path Path (b) splits into `initiative-triaged` + `proposal-approved` siblings; both rely on `resolveLinkedPath`'s archive fallback (CR-031 W1 work) since Initiatives move to `archive/` after triage stamps `triaged_at` + `spawned_items`. The L273–299 `resolveLinkedPath` already walks pending-sync + archive — no further change needed.

#### File surface (exact paths)

**CLI engine (4 files, no mirror — these are TS source):**

- `cleargate-cli/src/lib/work-item-type.ts` (76 lines today; verified 2026-05-03)
  - L8: `WorkItemType` union — extend from 5 → 7 members. Add `'initiative'` and `'sprint'`. Final: `'story' | 'epic' | 'proposal' | 'cr' | 'bug' | 'initiative' | 'sprint'`.
  - L14–20: `FM_KEY_MAP` — append two entries: `{ key: 'initiative_id', type: 'initiative' }` and `{ key: 'sprint_id', type: 'sprint' }`. Final: 7 entries.
  - L25–31: `PREFIX_MAP` — append two entries: `{ prefix: 'INITIATIVE-', type: 'initiative' }` and `{ prefix: 'SPRINT-', type: 'sprint' }`. **Order matters per FLASHCARD `2026-05-01 #citation-rewrite #scope-gap` (longest-alternative-first lesson from BUG-010):** append INITIATIVE- and SPRINT- *after* the 5 existing prefixes. Existing prefixes are all shorter or non-overlapping; the `.includes(prefix)` check at L58 is substring-based and order-sensitive only when one prefix is a substring of another. None of `INITIATIVE-`, `SPRINT-` are substrings of `STORY-`, `EPIC-`, `PROPOSAL-`, `CR-`, `BUG-` or vice versa, so simple append is safe. **VERIFY in test:** confirm `detectWorkItemType('STORY-NNN-NN')` still returns `'story'` (not `'initiative'`).
  - L69–75: `WORK_ITEM_TRANSITIONS` — append `initiative: ['ready-for-decomposition']` and `sprint: ['ready-for-execution']`. Final: 7 keys.

- `cleargate-cli/src/wiki/derive-bucket.ts` (38 lines today; verified 2026-05-03)
  - L9–16: `PREFIX_MAP` — already has SPRINT (`{ prefix: 'SPRINT-', type: 'sprint', bucket: 'sprints' }` at L12). Append ONE new entry: `{ prefix: 'INITIATIVE-', type: 'initiative', bucket: 'initiatives' }`. **DO NOT re-add SPRINT.** Final: 7 entries.

- `cleargate-cli/src/wiki/page-schema.ts` (126 lines today; verified 2026-05-03)
  - L7: `WikiPageType` union — already has `'sprint'`. Append `| 'initiative'`. Final: `'epic' | 'story' | 'sprint' | 'proposal' | 'cr' | 'bug' | 'topic' | 'initiative'`.

- `cleargate-cli/src/commands/stamp-tokens.ts` (verified 2026-05-03; relevant lines 192–220)
  - L194: `idKeys` array — append `'initiative_id'` and `'sprint_id'`. Final: 7 entries.
  - L204: regex `^(STORY|EPIC|PROPOSAL|CR|BUG)-\d+(-\d+)?` → `^(STORY|EPIC|PROPOSAL|CR|BUG|INITIATIVE|SPRINT)-\d+(-\d+)?`. Longest alternatives first (INITIATIVE before SPRINT before shorter). Per FLASHCARD `BUG-010 lesson`: alternation regex prefers longest match — but Node `RegExp` with `^...$` anchors picks the first match position; mixing INITIATIVE before STORY is fine because `STORY-NNN` doesn't match `INITIATIVE-` prefix. The lesson applies when one prefix is a substring of another (none here are).
  - L213: same regex pattern in the inner `idMatch` branch — same change.

**Templates (1 file × 2 ends — live + canonical mirror):**

- `.cleargate/templates/initiative.md` L29 — change `initiative_id: "INIT-{NNN}"` to `initiative_id: "INITIATIVE-{NNN}"`. Single line edit.
- `cleargate-planning/.cleargate/templates/initiative.md` L29 — same edit. Mirror **must be byte-equal** post-edit.

**Readiness gates (1 file × 2 ends — live + canonical mirror):**

- `.cleargate/knowledge/readiness-gates.md` (170 lines today; verified 2026-05-03)
  - **Edit 1 — rename criterion ID at L69:** change `      - id: proposal-approved` to `      - id: parent-approved`. Single line. The predicate body at L70 (`        check: "frontmatter(context_source).approved == true"`) STAYS as-is for now because we ship Path (b) — see Edit 2.
  - **Edit 2 — Path (b) implementation in epic.ready-for-decomposition gate (per CR-030 §0.5 Q2 resolved decision):** Path (b) keeps the predicate engine closed-set (no new shape). The `parent-approved` criterion expands to TWO siblings ORed by the gate evaluator. Per CR-030 §3 line 187 the simplest implementation is to **emit both criteria** in the gate definition AND have the evaluator interpret OR-of-two-criteria-with-id-prefix-`parent-approved-` as a logical OR. **Architect decision (no orchestrator question needed):** ship as TWO line-adjacent criteria with distinct IDs, no engine OR change. The evaluator already supports per-criterion pass/fail; gates fail when ANY criterion fails. To make Path (b) work, the gate definition emits BOTH `proposal-approved` AND `initiative-triaged` and the evaluator must treat them as "ANY pass = gate passes for this concern." This requires a small evaluator extension. **TO AVOID engine change**, use a different shape: emit ONE renamed criterion `parent-approved` with the predicate `frontmatter(context_source).approved == true` and ALSO add a parallel criterion `initiative-triaged` with `frontmatter(context_source).status == "Triaged"`. **Both criteria are required to pass.** This is wrong for Initiative-typed parents (Initiatives don't carry `approved`). **Open decision for orchestrator — see "Open decisions" section below.**
  - **Edit 3 — append new Initiative gate block AFTER L170 (current file end, after the Sprint gate's closing ``` ``` fence):**
    ```yaml
    - work_item_type: initiative
      transition: ready-for-decomposition
      severity: advisory
      criteria:
        - id: no-tbds
          check: "body does not contain marker 'TBD'"
        - id: user-flow-populated
          check: "section(1) has ≥1 listed-item"
        - id: success-criteria-populated
          check: "section(5) has ≥1 listed-item"
    ```
    Wrapped in a triple-backtick `yaml` fence matching the file's existing convention (see L51, L64, L87, L106, L129, L146, L161 fences). **Severity is `advisory`** per CR-030 §1.5 (Initiatives are stakeholder-authored intent, not engineering commitments).

- `cleargate-planning/.cleargate/knowledge/readiness-gates.md` — three byte-equal edits as above.

#### Implementation sketch

1. Edit `work-item-type.ts` (4 disjoint hunks at L8 / L14–20 / L25–31 / L69–75).
2. Edit `derive-bucket.ts` (1 hunk at L9–16).
3. Edit `page-schema.ts` (1 hunk at L7).
4. Edit `stamp-tokens.ts` (3 hunks at L194 / L204 / L213).
5. Edit live + canonical `initiative.md` (1 hunk each at L29).
6. Edit live + canonical `readiness-gates.md` (3 hunks each: L69 rename, body unchanged at L70 — see open decision, append new gate block after current EOF).
7. Run `npm run prebuild` (per sprint plan §3.5) to refresh the npm payload mirror at `cleargate-cli/templates/cleargate-planning/.cleargate/templates/initiative.md`.

#### Test scenarios (4 files; minimum 13 scenarios across all four)

CR-030 §3 specifies 4 test files. Two of them (`derive-bucket.test.ts`, `work-item-type.test.ts`) **do not exist yet** (verified `ls 2026-05-03`). Developer creates them.

- **`cleargate-cli/test/wiki/derive-bucket.test.ts`** (NEW FILE) — 4 scenarios minimum:
  1. `INITIATIVE-001_foo.md` → `{ type: 'initiative', id: 'INITIATIVE-001', bucket: 'initiatives' }`.
  2. `SPRINT-21_foo.md` → `{ type: 'sprint', id: 'SPRINT-21', bucket: 'sprints' }` (regression — already passing pre-CR but lock it down).
  3. `STORY-042-01_foo.md` → `{ type: 'story', id: 'STORY-042-01', bucket: 'stories' }` (regression).
  4. `UNKNOWN-001.md` → throws `cannot determine bucket`.

- **`cleargate-cli/test/lib/work-item-type.test.ts`** (NEW FILE) — 4 scenarios minimum:
  1. `detectWorkItemTypeFromFm({ initiative_id: 'INITIATIVE-001' })` → `'initiative'`.
  2. `detectWorkItemTypeFromFm({ sprint_id: 'SPRINT-21' })` → `'sprint'`.
  3. `detectWorkItemType('INITIATIVE-001_foo.md')` → `'initiative'`; `detectWorkItemType('SPRINT-21_foo.md')` → `'sprint'`.
  4. `WORK_ITEM_TRANSITIONS.initiative` deep-equals `['ready-for-decomposition']`; `WORK_ITEM_TRANSITIONS.sprint` deep-equals `['ready-for-execution']`.

- **`cleargate-cli/test/commands/stamp-tokens.test.ts`** (existing 326+ lines) — append 2 scenarios:
  1. Stamp Initiative fixture `INITIATIVE-001_test.md` with `initiative_id: INITIATIVE-001` in frontmatter → `work_item_id` captured as `INITIATIVE-001`. Pre-CR: stderr `cannot determine work_item_id`.
  2. Stamp Sprint fixture `SPRINT-21_test.md` with `sprint_id: SPRINT-21` in frontmatter → `work_item_id` captured as `SPRINT-21`. **Regression scenario** (FLASHCARD `2026-05-03 #stamp-tokens #sprint` evidence in CR-030 §1 amendment).

- **`cleargate-cli/test/lib/readiness-predicates.test.ts`** (existing 770+ lines) — append 3 scenarios in a new `describe('CR-030 parent-approved per-type field map')` block:
  1. Epic with `context_source: PROPOSAL-001.md` (Proposal has `approved: true`) → `proposal-approved` predicate (renamed → `parent-approved` if Path (a) chosen) passes. Regression baseline.
  2. Epic with `context_source: INITIATIVE-001_test.md` (Initiative has `status: "Triaged"`) → `initiative-triaged` predicate passes (Path b: NEW criterion).
  3. Initiative fixture passes its advisory gate when §1 has ≥1 listed-item AND §5 has ≥1 listed-item AND no TBDs.

#### Reuse (no duplication)

- **Reuse `WorkItemType` from `cleargate-cli/src/lib/work-item-type.ts`** — extending in place; do NOT introduce a parallel union elsewhere.
- **Reuse `parseFrontmatter`** (referenced by FLASHCARD `2026-04-19 #cli #frontmatter #parse`) — `stamp-tokens.ts` already calls it. No re-implementation.
- **Reuse `resolveLinkedPath`** at `readiness-predicates.ts:282` (post-CR-031 W1 walks pending-sync + archive) — `parent-approved` and `initiative-triaged` predicates inherit the archive fallback for free.
- **Reuse the existing YAML fence convention in `readiness-gates.md`** — append the new Initiative gate as a fenced ```yaml block matching L51 / L64 / etc. style.

#### Gotchas surfaced from code inspection

- **`stamp-tokens.ts:194` has a HARDCODED `idKeys` array** disjoint from `FM_KEY_MAP` in `work-item-type.ts:14`. Two sources of truth for the "frontmatter ID key" mapping. CR-030 must edit BOTH or stamping breaks silently. Long-term fix: collapse to one source. Short-term: maintain parity. **Add a flashcard:** `#stamp-tokens #fm-key-map · stamp-tokens.ts L194 idKeys MUST stay in sync with work-item-type.ts L14 FM_KEY_MAP — two sources of truth post-CR-030.`
- **`derive-bucket.ts` already has `'sprint'`** at L12 (`{ prefix: 'SPRINT-', type: 'sprint', bucket: 'sprints' }`). CR-030 §1 cites only Initiative as the missing piece for derive-bucket; **do not double-add SPRINT.** Verify with `grep -n "SPRINT" derive-bucket.ts` before editing.
- **`page-schema.ts:7` already has `'sprint'`** (`'epic' | 'story' | 'sprint' | 'proposal' | 'cr' | 'bug' | 'topic'`). Only `'initiative'` is missing.
- **`readiness-gates.md:161-170`** has a Sprint gate (added pre-W3, verified). CR-030 amendment §"AMENDED 2026-05-03" adds Sprint type to `WorkItemType` + `FM_KEY_MAP` + `PREFIX_MAP` + `WORK_ITEM_TRANSITIONS` but does NOT add a new readiness-gates Sprint gate (already exists). The amendment is purely TS engine alignment with the YAML gate that already references `work_item_type: sprint`. **Existing `risk-table-populated` predicate `body contains '| Mitigation'`** is unchanged.
- **Path (b) gate evaluator semantics — open ambiguity.** The CR-030 spec resolves §0.5 Q2 to "rename to `parent-approved` with per-type field semantics" but Path (b) implementation as written in §1.5 + §3 is internally inconsistent: emitting BOTH `proposal-approved` AND `initiative-triaged` as required-and-ALL criteria fails for either-type parents (Proposal has no `status: Triaged`; Initiative has no `approved: true`). The clean Path (b) requires evaluator OR-semantics for criterion siblings, which is an engine change. **See Open decisions for orchestrator.**
- **`PREFIX_MAP` substring check at `work-item-type.ts:58`** uses `.includes(prefix)`, not `.startsWith(prefix)`. Means `'INITIATIVE-001/foo'` would match — but a string with `STORY-` substring elsewhere wouldn't. Adding `INITIATIVE-` and `SPRINT-` is safe (none are substrings of existing prefixes). **Verify regression with the new test scenarios above.**
- **`cleargate-planning/.cleargate/scripts/`** does NOT contain `prep_reporter_context.mjs` (verified `ls 2026-05-03`). Only the live `.cleargate/scripts/` does. No CR-030 mirror-write needed for that script (CR-035 owns it; see CR-035 below).
- **`cleargate-cli/test/scripts/test_stamp_and_gate.sh:149,208`** has the string `proposal-approved: linked file not found: INITIATIVE-001_test.md` — these are EXPECTED-failure assertions. Post-CR-030, they must be updated to `parent-approved: ...` (assuming criterion rename) AND the failure case may flip (if Path (b) provides Initiative coverage). **Regression: Developer MUST update both test assertions to match the new criterion ID, OR confirm with QA that the test intent is preserved.**
- **`cleargate-cli/test/commands/gate.test.ts:655–724`** has 4 lines mentioning `proposal-approved` as expected output. Post-CR-030 these need rename to `parent-approved` (or kept as `proposal-approved` if Path (b) emits both as separate criteria — see Open decisions).

#### Acceptance link-back

CR-030 §4 — verify all 9 acceptance scenarios, with focus on:
- AC1 (bucket recognized — derive-bucket.ts test).
- AC2 (type detection — work-item-type.ts test).
- AC3 (token stamp — stamp-tokens.ts test + manual smoke via PostToolUse hook).
- AC4 (wiki ingest — `cleargate wiki ingest` against fixture; verifies `wiki/initiatives/INITIATIVE-001.md` written).
- AC5 (`parent-approved` resolves Initiative — readiness-predicates.test.ts).
- AC8 (scaffold mirror diffs empty — `diff` between live + canonical for both `initiative.md` and `readiness-gates.md`).

---

### CR-033 — `existing-surfaces-verified` predicate (CR-028 follow-up; closed-set shape #7)

**Lane:** standard. ~50–80 LOC predicate engine extension + 9 tests + 1 readiness-gates edit + 1 mirror.

**Parallelism:** dispatch in parallel with CR-030 + CR-035. Merge **second** within W3 (after CR-030's `parent-approved` rename lands at readiness-gates.md L69, so CR-033's insertions in the same gate blocks rebase against the post-CR-030 file).

#### Cross-story coupling
- **CR-030 → CR-033 merge order required.** Both edit `.cleargate/knowledge/readiness-gates.md` line-disjointly. CR-030 owns L69 rename + tail-append (after L170). CR-033 owns L82+L124+L143 inserts. Merge order CR-030 → CR-033 means CR-033's git apply lands cleanly without rebase conflicts.
- **CR-031 (W1, already landed) free zones in `readiness-predicates.ts`:** L273–299 (resolveLinkedPath body) — CR-033 does NOT touch. CR-034 (W1, already landed) free zones: L16, L80, L86 (parser enum), L494–495 (dispatch), L516–569 (countDeclaredItems) — CR-033 does NOT touch.
- **CR-030 → CR-033 Initiative exemption.** CR-030's new Initiative gate (severity advisory) MUST NOT include `existing-surfaces-verified` per CR-033 §2 line 143. CR-033 does not add the criterion to the Initiative gate. Cross-check: CR-030 lands the Initiative gate with 3 criteria (`no-tbds`, `user-flow-populated`, `success-criteria-populated`); CR-033's grep+inserts target `epic`, `story`, `cr` only.

#### File surface (exact paths)

**Predicate engine (1 file):**

- `cleargate-cli/src/lib/readiness-predicates.ts` (677 lines today, verified 2026-05-03; file ends at L677 per Read).
  - **Free zone 1 — type union extension (after L19):** the `ParsedPredicate` discriminated union at L12–19 ends with `| { kind: 'status-of'; ... }` at L19. Append a new union member: `| { kind: 'existing-surfaces-verified' }`. (Zero parameters — predicate body is fixed; no fields to capture.) **Insert at L19.5 (between current L19 and the `;` on L19; effectively a new disjunct line before the `;`).**
  - **Free zone 2 — parser case (after L113, before L115):** the `parsePredicate()` function (L39–116) currently has cases 1 (frontmatter), 2a-c (body markers), 3 (section), 4 (file-exists), 5 (link-target-exists), 6 (status-of). The status-of case ends at L113 with `return { kind: 'status-of', id, value };`. The closing `throw new Error(...)` is at L115. **Insert new case 7 between L113 and L115:**
    ```ts
    // 7. existing-surfaces-verified — closed-set shape, no parameters
    if (s === 'existing-surfaces-verified') {
      return { kind: 'existing-surfaces-verified' };
    }
    ```
  - **Free zone 3 — evaluator dispatch arm (in switch L143–158):** the `evaluate()` switch ends with `case 'status-of': return evalStatusOf(parsed, opts, projectRoot);` at L156–157. Insert new case before the closing `}` of the switch (L158):
    ```ts
    case 'existing-surfaces-verified':
      return evalExistingSurfacesVerified(doc, projectRoot);
    ```
  - **Free zone 4 — evaluator function (file tail, append after L677):** define `evalExistingSurfacesVerified(doc: ParsedDoc, projectRoot: string): { pass: boolean; detail: string }`. ~50–80 LOC per CR-033 §3. Implementation per CR-033 §1 spec:
    1. Locate `## Existing Surfaces` section in `doc.body` (split on `^## ` per `evalSection` pattern at L464).
    2. If section absent → return `{ pass: true, detail: 'not-applicable: ## Existing Surfaces section absent — reuse-audit-recorded already failing' }` (per CR-033 §3 line 117 "auto-skips with not-applicable" — interpret as pass with skip detail to avoid double-fail).
    3. Extract path-shaped substrings using regex `/[a-zA-Z0-9_./-]+\.[a-zA-Z]{1,5}(?::[a-zA-Z_][a-zA-Z0-9_]*)?/g`. Strip `:symbol` suffix on each match.
    4. For each unique extracted path: resolve via `path.resolve(projectRoot, p)`, sandbox-check `resolved.startsWith(projectRoot + path.sep) || resolved === projectRoot`, then `fs.existsSync(resolved)`.
    5. Sentinel handling per CR-033 §1 line 124: case-insensitive search for `no overlap found`, `no existing surface`, `no prior implementation`, `audit returned empty`. If zero paths AND ≥1 sentinel → pass. If zero paths AND no sentinel → fail with detail `'## Existing Surfaces' has no path citations and no "no overlap found" sentinel`.
    6. If any extracted path fails existence → fail with detail listing missing paths: `cited paths do not exist on disk: <p1>, <p2>, ...`.

**Readiness gates (1 file × 2 ends — live + canonical mirror):**

- `.cleargate/knowledge/readiness-gates.md` — three insertions (post-CR-030 line numbers, since CR-030 lands first):
  - **Insert 1 — epic.ready-for-decomposition gate, after L82 (`reuse-audit-recorded` line):** add new criterion immediately after the `reuse-audit-recorded` block:
    ```yaml
        - id: existing-surfaces-verified
          check: "existing-surfaces-verified"
    ```
    Indented 4 spaces matching surrounding criteria items.
  - **Insert 2 — story.ready-for-execution gate, after L124 (the `reuse-audit-recorded` line in story gate):** same insertion pattern.
  - **Insert 3 — cr.ready-to-apply gate, after L143 (the `reuse-audit-recorded` line in cr gate):** same insertion pattern.
  - **Insert 4 — Predicate Vocabulary section update at L9 area:** the file says "There are exactly **6 predicate shapes**" at L9. Update to "There are exactly **7 predicate shapes**." Add new vocabulary entry immediately after the existing `**6. status-of(...)**` block at L32–33:
    ```markdown
    **7. `existing-surfaces-verified`**
    Closed-set predicate (no parameters). Locates the `## Existing Surfaces` section in the document body, extracts path-shaped substrings via regex, asserts each cited path exists on disk relative to the project root. Passes when section is absent (defers to `reuse-audit-recorded`) OR all cited paths exist OR section contains a "no overlap found" / "no existing surface" / "no prior implementation" / "audit returned empty" sentinel. Sandbox-rejected paths (escaping project root) are treated as missing. Example: `existing-surfaces-verified` against an Epic body whose `## Existing Surfaces` cites `cleargate-cli/src/lib/work-item-type.ts:detectWorkItemTypeFromFm` passes when that path exists.
    ```
- `cleargate-planning/.cleargate/knowledge/readiness-gates.md` — same four insertions, byte-equal.

**Tests (1 file + fixtures dir):**

- `cleargate-cli/test/lib/readiness-predicates.test.ts` — append new `describe('CR-033 existing-surfaces-verified — L0 code-truth tightening')` block with all 9 scenarios per CR-033 §3:
  1. Section absent → `not-applicable` (pass with skip detail).
  2. Section present, cites `package.json` (real top-level file) → pass.
  3. Section present, cites `cleargate-cli/src/lib/work-item-type.ts:detectWorkItemTypeFromFm` → pass; symbol stripped.
  4. Section present, cites `cleargate-cli/src/lib/does-not-exist.ts` → fail; detail names the path.
  5. Section present, cites mix of real + missing → fail; detail names only the missing.
  6. Section present, no path matches, contains `no overlap found` → pass.
  7. Section present, no path matches, no sentinel → fail with sentinel-missing detail.
  8. Section present, cites `../../etc/passwd` → sandbox-rejected, treated as missing → fail.
  9. Section present, cites `cleargate-cli/package.json` (real subdir file) → pass.

- `cleargate-cli/test/fixtures/existing-surfaces/` (NEW DIR) — fixture markdown files for the above scenarios. ~9 small markdown bodies inlined in test (no new fixture dir required if Developer prefers `makeDoc()` helper — verified at test L796 the existing pattern uses inline body strings).

#### Implementation sketch

1. Edit `readiness-predicates.ts` (4 disjoint hunks at L19+ / L113+ / L156+ / L677+).
2. Edit live + canonical `readiness-gates.md` (4 hunks each: predicate count L9, predicate vocab L33+, 3 gate criterion inserts).
3. Append 9 test scenarios + describe block to `readiness-predicates.test.ts`.
4. Run `npm run prebuild`.

#### Test scenarios (1 file, 9 scenarios)

See File Surface > Tests above.

#### Reuse (no duplication)

- **Reuse `evalSection` body-split pattern** at `readiness-predicates.ts:464` (`body.split(/^(?=## )/m)`) for locating the `## Existing Surfaces` section.
- **Reuse the sandbox-check pattern** from `evalFileExists` at L580 (`resolved.startsWith(projectRoot + path.sep) || resolved === projectRoot`) — copy-paste into the new evaluator.
- **Reuse `fs.existsSync`** (already imported at L7).
- **Reuse `path.resolve`** (already imported at L8).
- **DO NOT introduce new imports.** All required Node primitives already imported.
- **DO NOT touch `reuse-audit-recorded` predicate.** It stays as `body contains '## Existing Surfaces'` per CR-028; CR-033 ships the strict sibling.

#### Gotchas surfaced from code inspection

- **Predicate count assertion at `readiness-gates.md:9`** — the file literally says "exactly **6 predicate shapes**." Updating to 7 is mandatory; missing this edit is a documentation drift that will trip a future contributor.
- **Section locator off-by-one (FLASHCARD `2026-04-19 #gates #predicate #section`):** `evalSection` uses `body.split(/^(?=## )/m)` — if body starts with `## `, `rawParts[0]` is section-1 (no preamble). Otherwise `rawParts[0]` is preamble. The new `evalExistingSurfacesVerified` must follow the same pattern. Cite this lesson in the implementation.
- **`## Existing Surfaces` has no number prefix.** Per FLASHCARD `2026-05-02 #qa #templates #readiness · CR-028` the template body uses `## Existing Surfaces` (no `## 3.5 Existing Surfaces`). The CR-033 predicate must search for the literal `^## Existing Surfaces` heading, not `^## \d+(\.\d+)?\s+Existing Surfaces`.
- **Path extraction regex collisions:** the CR-033 §1 spec regex `[a-zA-Z0-9_./-]+\.[a-zA-Z]{1,5}(:[a-zA-Z_][a-zA-Z0-9_]*)?` will match prose strings like `e.g.` (matches `e.g` — wait no, requires extension `[a-zA-Z]{1,5}` and `g` is 1 char, so `e.g` would match if regex allows). **Mitigation:** the existence check itself filters false positives — `e.g` does not exist as a file, so the criterion fails with detail naming `e.g`. Per CR-033 §0.5 Q1 resolution this is the accepted permissive trade-off. **Add a flashcard on completion:** `#predicates #existing-surfaces · existing-surfaces-verified regex matches prose-shaped strings like 'e.g.'; existence check filters false positives but error detail will name them — acceptable per CR-033 §0.5 Q1.`
- **Sentinel-phrase case insensitivity:** CR-033 §1 line 124 specifies "case-insensitive." Use `/no overlap found|no existing surface|no prior implementation|audit returned empty/i` per spec. **DO NOT downcase the body** — that breaks path-existence checks (filenames are case-sensitive on Linux).
- **Initiative gate exclusion is structural, not predicate-side.** CR-033 does NOT add `existing-surfaces-verified` to the Initiative gate (CR-030's new gate). CR-033 cannot prevent a future maintainer from adding it. **No code-side guard.** The exclusion is purely the gate-definition author's responsibility. CR-033 §2 line 143 states this explicitly.
- **Test fixture path realism:** scenarios 2/3/9 cite real paths in the repo. Use stable fixtures: `package.json` (top-level, never moves) and `cleargate-cli/src/lib/work-item-type.ts:detectWorkItemTypeFromFm` (post-CR-030 still exists). **AVOID citing files that may move during W3** (e.g. CR-030's `initiative.md` template — fixture might race CR-030's mirror prebuild).
- **`evalExistingSurfacesVerified` projectRoot dependence:** the function takes `projectRoot` to resolve cited paths. The test harness `makeDoc()` uses `process.cwd()` as default projectRoot — verify scenarios 2/3/9 run with the cleargate-cli repo as cwd OR pass an explicit `opts.projectRoot` matching the fixture file's repo. **Failure mode:** test passes locally but fails in CI with cwd elsewhere.

#### Acceptance link-back

CR-033 §4 — verify all 8 acceptance scenarios:
- AC1 (bug reproduces pre-CR; covered manually post-merge).
- AC2 (honest empty audit passes — fixture with sentinel only).
- AC3 (cited real path passes — `work-item-type.ts:detectWorkItemTypeFromFm`).
- AC4 (cited missing path fails — `does-not-exist.ts`).
- AC5 (mixed pass/fail — detail names only missing).
- AC6 (sandbox `../../etc/passwd` → fail).
- AC7 (Initiative exempt — gate definition has no `existing-surfaces-verified`; verify by reading CR-030's appended gate block post-merge).
- AC8 (end-to-end smoke deferred until W4+W5 close).

---

### CR-035 — Reporter §3 token total reads `.session-totals.json` + two-line split

**Lane:** fast (per sprint plan §2.4 lane audit). Reporter prompt edit + optional digest tweak. ≤2 files × ≤50 LOC net.

**Parallelism:** dispatch in parallel with CR-030 + CR-033. Disjoint surface — no shared file with either. Merge **third** in W3 ordering (last) but the wave is fully parallel; merge order is convention only, not collision-driven.

#### Cross-story coupling
- **CR-030 / CR-033 base:** zero overlap. CR-035 does not touch `readiness-gates.md`, `work-item-type.ts`, `derive-bucket.ts`, `stamp-tokens.ts`, `page-schema.ts`, or `readiness-predicates.ts`.
- **CR-036 follow (W4):** CR-035 establishes the two-line split format in `.claude/agents/reporter.md` §3. CR-036 (Reporter token diet — W4) builds on the post-CR-035 prompt as its baseline. Per sprint plan §2.2 row `.claude/agents/reporter.md`, merge order is CR-035 (W3) → CR-036 (W4).

#### File surface (exact paths)

**Reporter prompt (1 file × 2 ends — live + canonical mirror):**

- `.claude/agents/reporter.md` (verified 2026-05-03; relevant section is §"Workflow" step 2 at L46–53). The current text at L47 reads:
  > `Source 1 (primary): token-ledger.jsonl -- parse JSONL, sum delta.input + delta.output + delta.cache_read + delta.cache_creation per row (CR-018 v2 schema).`

  This is the OFF-BY-ONE source per CR-035 §1: the Reporter sums ledger row deltas, but the LAST row is the QA dispatch BEFORE the Reporter spawns itself. Reporter's own SubagentStop has not fired at sum-time. The "ledger-primary" label hides this.

  **Edit at §"Workflow" step 2 (replacing the current Source 1 / Source 2 / Source 3 / Divergence-flag block at L46–52):** REWRITE step 2 to specify three new behaviors:
  1. **Source 1 (primary): `.session-totals.json`** — read `.cleargate/sprint-runs/<id>/.session-totals.json` as the canonical sprint-total. Format: `{ "<session-uuid>": { input, output, cache_creation, cache_read, last_ts, last_turn_index } }`. Sum `input + output + cache_creation + cache_read` across ALL session-uuid keys to get the "Sprint total" line. Fallback: if the file is missing (legacy sprints), fall back to the current last-row `session_total` trick AND emit a `**Note:** .session-totals.json absent — falling back to last-row session_total (legacy mode).` line.
  2. **Source 2 (sprint-work breakdown): `token-ledger.jsonl` deltas filtered by `agent_type != 'reporter'`** — sum the deltas of all NON-reporter rows to get the "Sprint work" line. Reuse `sumDeltas()` from `cleargate-cli/dist/lib/ledger.js` (already cited in current step 2) but filter rows pre-sum.
  3. **Source 3 (Reporter analysis pass): TBD on first dispatch** — per CR-035 §0.5 Q2 path (b), report Reporter's own pass as `TBD — see token-ledger.jsonl post-dispatch` on the dispatch where the Reporter is itself running. The orchestrator's next-sprint pre-flight reads `token-ledger.jsonl` post-Reporter and surfaces the actual number; for THIS sprint's report, "TBD" is honest.
- **Format the §3 token-cost block as a two-line split (per CR-035 §1):**
  ```
  Token cost (sprint work, dev+qa+architect): 10,974,922
  Token cost (Reporter analysis pass):        TBD — see token-ledger.jsonl post-dispatch
  Token cost (sprint total):                  23,845,652
  ```
- **Replace the "ledger-primary" label** with `session-totals` (for Sprint total) or `ledger-deltas-by-agent` (for Sprint work) per CR-035 AC3.
- `cleargate-planning/.claude/agents/reporter.md` — same edit; byte-equal mirror.

**Optional — `prep_reporter_context.mjs` digest tweak (1 file, NO mirror per verified `cleargate-planning/.cleargate/scripts/` ls 2026-05-03):**

- `.cleargate/scripts/prep_reporter_context.mjs` — `buildTokenLedgerDigest()` currently spans L236–294 (verified). It calls `count_tokens.mjs` and emits a `## Token Ledger Digest` section with `**Total:**`, `**Per-agent breakdown:**`, `**Anomalies:**`. **Optional bake-in (per CR-035 §3 line 144):** add three new derived numbers to the digest output:
  - `sprint_work_tokens`: sum of deltas where `agent_type != 'reporter'`.
  - `sprint_total_tokens`: from `<sprintDir>/.session-totals.json` (read inline with `JSON.parse(fs.readFileSync(...))`).
  - `reporter_pass_tokens`: null at extraction time.

  **Architect decision: SHIP the optional bake-in.** Without it, the Reporter prompt edit alone leaves the LLM to do JSON.parse on disk via Bash tool — fragile + slow. Baking it into the digest at prep time is one ~30-line addition to `buildTokenLedgerDigest()` and gives the Reporter pre-computed numbers.

- **No mirror in `cleargate-planning/`** — verified the directory has 25 scripts but NOT `prep_reporter_context.mjs`. The script lives only at `.cleargate/scripts/`. **DO NOT create a mirror.** This is a divergence from the usual `cleargate-planning/` parity rule because this script is meta-repo–specific and not part of the npm payload.

**Tests (1 file — minimal):**

- `cleargate-cli/test/scripts/test_prep_reporter_context.sh` (NEW FILE per CR-035 §3 line 153) — bash smoke test:
  - Set up fixture sprint dir with known `token-ledger.jsonl` (3 dev rows + 1 reporter row) + `.session-totals.json` (cumulative).
  - Run `node .cleargate/scripts/prep_reporter_context.mjs <fixture-sprint-id>`.
  - Assert digest contains `sprint_work_tokens: <sum-of-3-dev-row-deltas>`, `sprint_total_tokens: <sum-of-session-totals>`, `reporter_pass_tokens: null`.
  - **Minimum 1 scenario** (smoke); ideally 2 (positive + missing-`.session-totals.json` fallback).

  Alternative if Developer finds bash test friction: skip this test file and rely on Reporter agent prompt manual smoke (rerun a small sprint at W4 or W5 close, verify §3 numbers). Per CR-035 §4 line 174 manual smoke is the named verification path.

#### Implementation sketch

1. Edit live + canonical `.claude/agents/reporter.md` §"Workflow" step 2 + §"Capability Surface" if it cites the obsolete sourcing.
2. Edit `.cleargate/scripts/prep_reporter_context.mjs` `buildTokenLedgerDigest()` to read `.session-totals.json` + filter ledger rows by `agent_type != 'reporter'`.
3. Add `cleargate-cli/test/scripts/test_prep_reporter_context.sh` smoke test.
4. NO `npm run prebuild` needed (no canonical scaffold change for `prep_reporter_context.mjs`); BUT Reporter prompt mirror IS in scaffold — `npm run prebuild` IS required for the Reporter prompt edit. **Verify both `cleargate-planning/.claude/agents/reporter.md` AND the npm payload at `cleargate-cli/templates/cleargate-planning/.claude/agents/reporter.md` are byte-equal post-prebuild.**

#### Test scenarios (1 file, 1–2 scenarios)

See File Surface > Tests above. CR-035 §3 line 153 specifies one test file; CR-035 §4 lists 5 acceptance scenarios but they are mostly observational ("read SPRINT-01_REPORT.md", "re-run a small sprint"). The mechanical verification is the digest-output smoke test.

#### Reuse (no duplication)

- **Reuse `count_tokens.mjs`** — already invoked by `buildTokenLedgerDigest()` at L240. Pass through unchanged; filter on the JS side after parsing.
- **Reuse `JSON.parse(fs.readFileSync(...))`** pattern — already used in same file. No new helper.
- **Reuse the existing §3 template structure** in `.cleargate/templates/sprint_report.md` — Reporter writes the two-line split into the existing token-cost row. **DO NOT add a new §3 subsection.**
- **DO NOT introduce a new shared module** for `.session-totals.json` parsing. Inline read; the spec says the file shape is stable and trivial.

#### Gotchas surfaced from code inspection

- **`.session-totals.json` shape verified 2026-05-03 at `.cleargate/sprint-runs/SPRINT-21/.session-totals.json`:**
  ```json
  {"fd518f2c-da3e-471e-a13d-35fcfb59d0b6":{"input":2378,"output":174733,"cache_creation":895423,"cache_read":17667628,"last_ts":"2026-05-03T21:02:39Z","last_turn_index":0}}
  ```
  Top-level is a map keyed by SESSION UUID, not a flat token bucket. CR-035 §1 line 27 shows a flat shape `{"input":413,"output":152820,"cache_creation":684220,"cache_read":23008199}` — that's WRONG for current ledger writers. **Real shape:** `Record<sessionUuid, { input, output, cache_creation, cache_read, last_ts, last_turn_index }>`. Reporter prompt + digest code MUST iterate Object.values and sum across sessions. **Add a flashcard on completion:** `#reporting #session-totals · .session-totals.json is keyed by session-uuid, not flat — sum Object.values to get cumulative.`
- **`count_tokens.mjs` already produces per-agent breakdown** (see `buildTokenLedgerDigest` L264–278). Filtering "non-reporter rows" can reuse the existing `digest.by_agent` map: `sprint_work = total - (digest.by_agent.reporter?.sum ?? 0)`. Simpler than re-parsing JSONL on the JS side.
- **Reporter prompt §3 v2.1 lane+hotfix metrics (L122–195) are unaffected.** CR-035 only touches §"Workflow" step 2 (sourcing) and the §3 token-cost lines. The lane+hotfix table remains as-is.
- **FLASHCARD `2026-04-19 #reporting #hooks #ledger #subagent-attribution`** notes that SubagentStop hook fires on orchestrator session not subagents — meaning ALL ledger rows currently land tagged with the orchestrator session UUID. Filtering `agent_type != 'reporter'` per ROW WORKS only if the ledger writer correctly tags `agent_type` per row (post-CR-026). **Verify with `head -1 .cleargate/sprint-runs/SPRINT-21/token-ledger.jsonl | jq .agent_type`** — if `agent_type` field is absent or always orchestrator, CR-035's filter has no signal and the two-line split collapses to one number with both lines equal. **Mitigation:** if `digest.by_agent` shows zero reporter dispatches in the fixture, the smoke test must scaffold a synthetic reporter row.
- **`buildTokenLedgerDigest` returns a markdown string** that the Reporter reads as input. The Reporter then re-emits this into §3 of the report. The Reporter prompt edit must explicitly say "use these pre-computed numbers from the digest" otherwise the LLM may re-derive its own (off-by-one) total.
- **The `ledger-primary` label must be removed everywhere** — the current Reporter prompt only mentions it implicitly via the `Source 1 (primary)` framing at L47. Grep `.claude/agents/reporter.md` for `ledger-primary`: 0 matches (verified). **The label originated in the Reporter's own emitted output**, not the prompt. CR-035 AC3 ("no `ledger-primary` label remaining") is verified by reading the next produced report — not by editing the prompt to delete the string.

#### Acceptance link-back

CR-035 §4 — 5 scenarios:
- AC1 (bug reproduces pre-CR — already documented in CR-035 §"context_source").
- AC2 (fix produces correct numbers — verify on next sprint close report).
- AC3 (source labeling correct — `session-totals` / `ledger-deltas-by-agent`, no `ledger-primary`).
- AC4 (CR-036 coupling — Reporter pass drops to ~80–100k post-CR-036 W4 merge — observational, not testable in W3).
- AC5 (scaffold mirror diffs empty — `diff .claude/agents/reporter.md cleargate-planning/.claude/agents/reporter.md`; expect zero output).

---

## Cross-story risks

A Developer working only on their CR might miss:

- **CR-030's `proposal-approved` rename has 6 downstream test-file references.** `cleargate-cli/test/lib/readiness-predicates.test.ts:567,569`, `cleargate-cli/test/scripts/test_stamp_and_gate.sh:149,208`, `cleargate-cli/test/commands/gate.test.ts:655,717,721,724`. Failing to update them = post-CR-030 test suite is RED. **CR-030 Developer MUST grep all test files for `proposal-approved` and update each reference.** Final disposition (rename vs keep — see Open decisions) determines per-line action.
- **CR-033's gate insertions land at the SAME 3 gate blocks where CR-030 just renamed `proposal-approved`.** If CR-033 dispatches before CR-030 merges, the line numbers (L82, L124, L143) shift after CR-030's tail-append (which only touches the file end, leaving these line numbers stable BUT moves the file-end insertion point). **Mitigation:** CR-030 merges first, CR-033 rebases. The orchestrator dispatches both in parallel BUT holds CR-033's commit until CR-030 lands.
- **CR-030's `stamp-tokens.ts:194` `idKeys` array vs `work-item-type.ts:14` `FM_KEY_MAP` parity.** Two sources of truth for the same mapping. CR-030 edits BOTH. If a future CR adds a new work-item-type and edits only `FM_KEY_MAP`, stamping silently breaks for the new type. **Long-term:** consolidate to one source. **Short-term flashcard.**
- **CR-033's Initiative exemption depends on CR-030's gate definition not including `existing-surfaces-verified`.** CR-030's W3 Initiative gate has 3 criteria (`no-tbds`, `user-flow-populated`, `success-criteria-populated`) — verified above. CR-033's gate-criterion inserts target ONLY epic / story / cr (verified). No collision, but the dependency is structural and cross-CR — surface in QA verification.
- **CR-035's `.session-totals.json` shape mismatch with CR-035 §1 spec.** The spec quotes a flat shape; the ACTUAL on-disk shape (verified) is keyed by session UUID. Developer following spec verbatim will write incorrect parser. **Architect override:** ship session-uuid-keyed parser per code-truth. Flashcard required.
- **CR-035's `prep_reporter_context.mjs` has NO mirror in `cleargate-planning/.cleargate/scripts/`** (verified). DO NOT create a mirror file — that's an incorrect scaffold injection. Reporter prompt DOES have a canonical mirror (`cleargate-planning/.claude/agents/reporter.md`); edit it. Both files in W3 must be byte-equal but only ONE script (the live one) gets touched.
- **`npm run prebuild` is required after CR-030 (initiative.md template) AND CR-035 (reporter.md prompt) but NOT after CR-033 (no scaffold-mirrored file edited).** CR-033 only edits the live `readiness-gates.md` AND its canonical mirror at `cleargate-planning/.cleargate/knowledge/readiness-gates.md`. Run `npm run prebuild` once after the W3 batch (or twice — once after CR-030 merge, once after CR-035 merge). Single run after all-three merge is fine. Without prebuild, the npm-payload mirror at `cleargate-cli/templates/cleargate-planning/...` drifts and the next `cleargate init` ships stale content.
- **W3 commit-count target: 3 commits (one per CR).** Per repo convention `feat(<id>): <desc>` per story. The orchestrator splits the W3 dispatch into 3 distinct Developer dispatches — do NOT batch.
- **Pre-W3 cache-refresh workaround:** orchestrator runs `for f in .cleargate/delivery/pending-sync/*.md; do cleargate gate check "$f"; done` before W3 dispatches. This is ONLY needed if CR-038 (W2 cache-refresh CLI) regressed; sprint plan §2.1 confirms CR-038 landed in W2.

---

## Open decisions for orchestrator

1. **CR-030 Path (b) implementation — engine OR-of-criteria OR rename-only.** CR-030 §0.5 Q2 resolves to "rename to `parent-approved` with per-type field semantics" (Path b). The clean Path (b) requires the gate evaluator to accept `id: parent-approved-proposal` AND `id: parent-approved-initiative` as siblings where ANY-pass = gate-pass. Today's evaluator treats every criterion as REQUIRED-pass (boolean AND). Three options:
   - **(α)** Ship Path (b) with TWO required criteria (`proposal-approved` + `initiative-triaged`) — both must pass — which BREAKS for either-type parents (Proposal lacks `status`; Initiative lacks `approved`).
   - **(β)** Ship Path (a) — extend predicate grammar with `if-type(context_source) == '<type>' then ...`. Engine change, ~20 LOC in `parsePredicate` + new evaluator branch.
   - **(γ)** Add evaluator OR-semantics for criteria sharing a prefix (`parent-approved-*`). ~10 LOC change. Backward compatible (no existing criterion uses the prefix).
   - **Architect recommendation: γ.** Smallest engine change, closest to spec intent, matches CR-030 §3 line 187 "have gate evaluator OR them when context_source resolves." Spec calls Path (b) "simpler — closed predicate grammar stays intact" — γ honors that.
   - **Question for orchestrator:** confirm γ before CR-030 dispatch. If user prefers β (named in spec as fallback), Architect re-issues this plan with adjusted predicate-engine extension. **Architect's default if no orchestrator response: γ.**

2. **CR-030 test-fixture rename surface.** Six existing test references to `proposal-approved` (test/lib/readiness-predicates.test.ts L567,569; test/scripts/test_stamp_and_gate.sh L149,208; test/commands/gate.test.ts L655,717,721,724) must be updated. **Decision the Developer needs:** keep both criteria visible (rename `proposal-approved` to `parent-approved-proposal` per option γ above) → 6 references rename to `parent-approved-proposal`. OR keep `proposal-approved` as the criterion ID for the Proposal branch and add a sibling `initiative-triaged` → 6 references stay AS-IS. **Architect default: rename to `parent-approved-proposal`** (cleaner symmetry with `initiative-triaged`); confirm with orchestrator before Developer dispatch.

3. **CR-035 optional `prep_reporter_context.mjs` digest extension.** Architect recommendation: ship it (per CR-035 plan above). **Question for orchestrator:** confirm fast-lane includes the ~30-line digest extension OR keep CR-035 to prompt-edit-only and defer digest extension to a follow-up CR. **Architect default: ship it now.** Lane stays fast (≤2 files, ≤50 LOC net).

4. **CR-035 test deliverable.** `test_prep_reporter_context.sh` is named in CR-035 §3 line 153 but does not exist today (verified). **Question for orchestrator:** mandate the test file (Developer creates) OR accept manual-smoke-only (per CR-035 §4 line 174). **Architect default: create it** — even a 1-scenario smoke is better than zero coverage.

5. **W3 dispatch mode.** CR-035 is fast-lane; CR-030 + CR-033 are standard-lane. Sprint plan §3 says "W3 spawns 3 parallel Developer dispatches." Confirm: orchestrator sends 3 parallel Task() calls in one round, each with a distinct dispatch marker (CR-030 / CR-033 / CR-035). Merge order CR-030 → CR-033 → CR-035 enforced by orchestrator on commit, not on dispatch.

---

flashcards_flagged:
- `#stamp-tokens #fm-key-map · stamp-tokens.ts:194 idKeys MUST stay in sync with work-item-type.ts:14 FM_KEY_MAP — two sources of truth post-CR-030.`
- `#predicates #existing-surfaces · existing-surfaces-verified regex matches prose-shaped strings (e.g. 'e.g.'); existence check filters false positives but error detail will name them — accepted per CR-033 §0.5 Q1.`
- `#reporting #session-totals · .session-totals.json is keyed by session-uuid, not flat — sum Object.values to get cumulative; spec quoted flat shape but live shape is map.`
- `#mirror #parity · cleargate-planning/.cleargate/scripts/ does NOT mirror prep_reporter_context.mjs (live-only). Do not create the mirror.`
