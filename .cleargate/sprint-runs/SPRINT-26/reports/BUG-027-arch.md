---
story: BUG-027
sprint: SPRINT-26
authored_by: architect
authored_at: 2026-05-05
mode: post-flight-review
dev_commit_verified: 12f4b8b
red_commit_verified: fe44ccd
---

ARCH: PASS

findings:
  - "Resolver chain order matches blueprint (Steps 1→4): Step 1 prior ledger row via `tail -1 ${LEDGER} | jq -r '.work_item_id'` (canonical token-ledger.sh:255-269); Step 2 dispatch-marker grep on `${HOOK_LOG}` (lines 271-285); Step 3 legacy CR-026 first-user-message jq scan (lines 289-308); Step 4 last-resort transcript anywhere-grep (lines 313-322). Implementation matches Open Decision 1 Option A as recommended in the blueprint."
  - "Mirror chain verified 3-way identical: canonical `cleargate-planning/.claude/hooks/token-ledger.sh` == npm-payload `cleargate-cli/templates/cleargate-planning/.claude/hooks/token-ledger.sh` == snapshot `cleargate-cli/test/snapshots/hooks/token-ledger.bug-027.sh` (488 lines each, byte-equal via diff -q)."
  - "MANIFEST.json regenerated by prebuild — `cleargate_version: 0.11.3`, `generated_at: 2026-05-05T15:27:36.820Z`. Single sha-changed entry expected per `npm run prebuild` workflow."
  - "Snapshot lock supersede pattern correct: hooks-snapshots.test.ts L142-152 demotes cr-044 to existence-only (`expect(fs.existsSync(snapshotPath)).toBe(true)` only); L155-176 establishes new byte-equality lock against bug-027.sh. Comment block L48-53 documents the supersede chain (CR-044 → BUG-027) per FLASHCARD 2026-05-04 #snapshot #hooks."
  - "Reuse mandate honoured: Step 1 leverages existing `${LEDGER}` variable (token-ledger.sh:99); Step 2 leverages existing `${HOOK_LOG}` (token-ledger.sh:67); both use the same `tail -1 | jq -r` and `grep -oE | head -1` patterns already present in the dispatch-marker block (lines 117-141 / CR-026 path-B). No new helper introduced; no duplication."
  - "Plan deviation (worktree-tracked .claude/hooks/token-ledger.sh sync) is acceptable per `cleargate-cli/test/scaffold/canonical-live-parity.red.node.test.ts` (CR-049 Scenario 5d). The file is force-tracked on `story/BUG-027` branch (history confirmed: SPRINT-23 chore(0972580) introduced the tracked copy); not the gitignored main-repo live hook. Skipping the sync would have made the parity Red test go red on merge. QA confirmed; Architect concurs — not a smell."
  - "No new dependencies: package.json + package-lock.json unchanged across QA-Red + Dev commits."
  - "No off-surface edits: Dev-only diff (fe44ccd..12f4b8b) touches exactly 5 files — canonical hook, worktree live hook, MANIFEST (auto), snapshot baseline file, snapshot test. All within M1 blueprint authorisation."
  - "Test scenarios S1-S4 covered with 9 node:test assertions in `cleargate-cli/test/scripts/token-ledger-resolver.red.node.test.ts` (red commit fe44ccd, 582 lines). Naming `*.red.node.test.ts` honours v2 immutability convention. QA confirmed full suite 131/131 green + vitest snapshot 8/8 green."
  - "Test path deviation noted but acceptable: blueprint suggested `cleargate-cli/test/hooks/token-ledger-fallback-resolver.node.test.ts`; QA-Red authored at `cleargate-cli/test/scripts/token-ledger-resolver.red.node.test.ts`. Same node:test runner, same coverage of all 4 scenarios; the `test/scripts/` location reflects QA's interpretation of resolver-as-script. Not a violation."
  - "Step 4's legacy `work_item_id fallback grep` log line is preserved (token-ledger.sh:319-321) so the dogfood verification command from BUG-027 §5 (`grep 'work_item_id fallback grep' .cleargate/hook-log/token-ledger.log` returns 0 EPIC-001 hits) remains testable. Fix is additive (preempt the path), not removal."

flashcards_flagged:
  - "ACK existing 2026-05-05 #snapshot #hooks #worktree from Dev/QA — story/BUG-027 branch force-tracks .claude/hooks/token-ledger.sh; canonical-live-parity Red test (CR-049 Scenario 5d) checks this. No new card needed; existing one captures the lesson."
  - "No new architectural gotchas surfaced beyond what M1 blueprint already documented. The blueprint's gotcha section (file:line citations at token-ledger.sh:236-281, :117-141, :155) accurately predicted the fix surface and the implementation matched."
