# STORY-054-06 — QA-Verify

role: qa

```
QA: pass
SUITE: 2526/2524/1/1 — only the pre-existing sync.node.test.ts network failure ("cannot reach https://cleargate-mcp.soula.ge (fetch failed)"), same test id and message as the documented baseline. Independently re-run in full (617.7s), not trusted from the Developer report. Exact match to Developer's reported 2526/2524/1/1; confirms the dispatch's own stated target ("2526/2525/1/1") was arithmetically wrong, as both QA-Red and the Developer already flagged.
QA_RED_UNTOUCHED: `git diff e9c780f a7f1c66 -- test/lib/readiness-predicates-task-breakdown.red.node.test.ts` is empty. Re-ran the file standalone: 10/10, 0 fail. Zero edits by the Developer, confirmed by hash-equivalent diff, not by trusting the commit message.
MUTANT_M3: PASS. Built a listed-item-counter probe independently (outside the test file, direct `evaluate()` call against `## Task Breakdown` + `- a plain bullet, no checkbox`): shipped implementation returns `{"pass":false,"detail":"## Task Breakdown is present but carries no `- [ ]` task rows"}`. Confirms the checker counts `- [ ]`/`- [x]` only, never `/^\s*- /gm`.
MUTANT_M8: PASS. Independent probe against `## 3.5 Task Breakdown` + one `- [ ]` row: `{"pass":true,"detail":"## Task Breakdown has 1 task row"}`. Confirms `headingTitleOf` numeric-prefix tolerance is real, not a literal `firstLine === '## Task Breakdown'` compare.
SECTION_SCOPED: PASS. Independent probe with `- [ ]` rows under `## 4. Quality Gates` and an empty `## Task Breakdown` (guidance-only, zero rows) in the same body: `{"pass":false,"detail":"## Task Breakdown is present but carries no `- [ ]` task rows"}`. Confirms the row count is scoped to the located section (`body.split(/^(?=## )/m)` + exact-match break), never counted over `doc.body`.
NON_VACUITY: PASS. Ran the real `evaluate('task-breakdown-complete', …)` against all three shipped, `<instructions>`+frontmatter-stripped templates on disk (`story.md`, `CR.md`, `Bug.md`): all three return `pass:false` ("carries no `- [ ]` task rows"). Guidance block is `> `-prefixed on every line, zero fenced or indented illustration, in all 6 files (3 live + 3 canonical) — confirmed by direct `awk` extraction, not grep.
SEVERITY: `enforcing` on all three blocks, both trees, byte-identical (mirror diff already silent). Quoted from live `.cleargate/knowledge/readiness-gates.md`: `:149` (`story` block, `ready-for-execution`) `severity: enforcing`; `:180` (`cr` block, `ready-to-apply`) `severity: enforcing`; `:205` (`bug` block, `ready-for-fix`) `severity: enforcing`. Canonical mirror identical at the same line numbers (parity diff silent — see MIRRORS).
N6_BUILD: PASS. `node -e "console.log(require('fs').readFileSync('cleargate-cli/dist/cli.js','utf8').includes('task-breakdown-complete'))"` → `true`. `dist/cli.js` mtime (Aug 28 12:14:26) is newer than both the source edit (12:02:49) and the Commit A timestamp (12:13:13), confirming the rebuild happened after the code landed. Did not use grep (per the dispatch's own warning — BSD grep silently exits 1 on this bundle).
INDEX_BUMP: PASS, both trees. `story.dod-declared` reads `check: "section(5) has ≥1 listed-item"` at live `:157-158` and canonical (same lines, mirror-silent). Not reverted.
VOCAB_OFFBYONE: The undocumented shape is `marker-absence` — pre-existing, NOT introduced by this story. `ParsedPredicate` has 11 members; `readiness-gates.md`'s Predicate Vocabulary documents 10 numbered shapes (1 frontmatter … 10 task-breakdown-complete). The gap: vocabulary entry **2** ("`body contains "<string>"` / `body does not contain "<string>"`") describes only the generic `body-contains` shape (parser branches 2b/2c), but `parsePredicate`'s branch 2a (`^body does not contain marker ['"]([A-Z]+)['"]$`) produces a DISTINCT union member, `{ kind: 'marker-absence' }`, used by every `no-tbds` criterion in the registry. It has no numbered entry of its own — folded silently into entry 2's prose. Traced by `git log -S"marker-absence" -- src/lib/readiness-predicates.ts`: introduced in `3f2011c` ("fix(BUG-008): SPRINT-14 M2 — gate criteria over-match"), long before this sprint. This story's docstring correction (6→11, i.e. the "10" the plan specified plus 1 for task-breakdown-complete) is arithmetically correct against the union count; it did not create or worsen the vocabulary gap, it just makes the union-vs-vocabulary mismatch visible by getting the union count right for the first time. Not a kickback — flagging per the dispatch's instruction for a human/report-level follow-on (candidate: give `marker-absence` its own numbered vocabulary entry, or fold `body-contains`+`marker-absence` into one shared entry number honestly).
HEADING: `## Task Breakdown` — exact, unnumbered, byte-identical in all 6 template files (verified via `awk` section extraction on all 3 live + 3 canonical). `## 9. Task Breakdown` appears nowhere in any template — grep confirms the only occurrences of that literal in the whole `cleargate-cli` tree are S3a/S3b's synthetic mutation strings in `gate-section-index-pinning.node.test.ts:517/567/585`.
STALE_SITES: 4/4 fixed in `readiness-predicates-prior-work-ambiguity.node.test.ts` — read all four sites directly: the `:214-219`-region block comment now explains the SYNTHETIC-body relationship to the real 054-06 shift and points at pinning S1b; the `:274`-region and `:356`-region test titles are retitled to name the synthetic body and disclaim template/registry correspondence; the "documents nine shapes" comment near `:390-393` is updated (now describes the tenth-shape addition). File re-run standalone: 22/22, 0 fail — matches Developer's report exactly.
MIRRORS: 4/4 silent. `diff` (not `git diff`) run directly between live and canonical for `story.md`, `CR.md`, `Bug.md`, `readiness-gates.md` — all four empty.
UNTOUCHED: Confirmed by diffing the full cli-repo branch against its merge-base (`git diff --stat 507f67c a7f1c66`) rather than trusting the Developer's per-file claims: exactly 3 files touched in the whole story — `readiness-predicates.ts`, `readiness-predicates-prior-work-ambiguity.node.test.ts`, and the new red test file. That proves, by construction, zero diff on `expected-headings.ts`, `gate-section-index-pinning.node.test.ts`, `KNOWN_UNPINNABLE` (inside that untouched file), the two block censuses (independently re-read: `gate-unit.node.test.ts:748` still asserts 11, `readiness-predicates.node.test.ts:714` still asserts 11 — both re-run green, 25/25 and 119/119), S4, S7, and `evalSection` (hunk-boundary check on the `readiness-predicates.ts` diff: 5 hunks at lines 1/20/131/180/1114-end of the pre-image, nowhere near the frozen 632-657 region). Pinning test independently re-run standalone: **14/14**, 0 fail, 0 skip — matches Developer's report.
REPRODUCED: (1) full cli suite from clean, no env override, from the outer main checkout — 2526/2524/1/1, 617.7s (independently, not copied from any report); (2) typecheck — clean, exit 0; (3) targeted red-file run — 10/10; (4) targeted `gate-section-index-pinning.node.test.ts` — 14/14; (5) targeted `gate-unit.node.test.ts` — 25/25; (6) targeted `readiness-predicates.node.test.ts` — 119/119; (7) targeted `readiness-predicates-prior-work-ambiguity.node.test.ts` — 22/22; (8) three hand-built mutant/scope probes calling the real exported `evaluate()` directly (M3, M8, section-scoping), outside any existing test file; (9) non-vacuity probe reading all 3 shipped, stripped templates directly off disk; (10) `git diff --stat` over the full cli branch against merge-base to prove the untouched-file list by construction rather than by spot-check; (11) confirmed the transient `skill-md-conditional-architect.red.node.test.ts` payload-drift QA-Red and TPV both saw is gone — green in this full run (line "payload SKILL.md is byte-identical to canonical (after prebuild)" passes). Outer repo touched nothing outside the two commits already on the branch; `git status --porcelain` in the outer repo shows only the pre-existing concurrent-session churn (EPIC-058 files, wiki index/log/product-state/roadmap, `.session-totals.json`/`.tmp.*`, `cleargate-planning/MANIFEST.json`) — none of it read into any decision here, none of it touched.
FINDINGS:
  1. Pre-existing, not a kickback: `marker-absence` (BUG-008, SPRINT-14) has no numbered entry in the Predicate Vocabulary — it is silently folded into entry 2's prose alongside the unrelated generic `body-contains` negation. 11 union members, 10 documented shapes. Worth a follow-on doc fix; does not block this story.
```
