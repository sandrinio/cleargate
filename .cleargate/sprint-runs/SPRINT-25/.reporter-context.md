# Reporter Context Bundle — SPRINT-25

_Generated: 2026-05-04T21:35:43.270Z_

---

## Sprint Plan Slices

_Source: SPRINT-25_SDLC_Wrap_Up_Plus_Docs.md_

## 1. Consolidated Deliverables

## 2. Execution Strategy (Architect SDR finalized — 2026-05-04)

## 5. Metrics & Metadata

## State.json Summary

Sprint: SPRINT-25 | Status: Active

CR-053 | Done | standard | qa_bounces=0 arch_bounces=0
CR-054 | Done | standard | qa_bounces=0 arch_bounces=0
CR-055 | Done | standard | qa_bounces=0 arch_bounces=0
CR-056 | Done | standard | qa_bounces=0 arch_bounces=0
CR-057 | Done | standard | qa_bounces=0 arch_bounces=0
CR-058 | Done | standard | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

---
milestone: M1
sprint_id: SPRINT-25
wave: W1
parallel: true
authored_by: architect
authored_at: 2026-05-04
items: [CR-053, CR-054, CR-055, CR-056, CR-058]
out_of_scope: [CR-057]
execution_mode: v2
---

# Milestone: M1 — SPRINT-25 Wave 1

## Items: CR-053, CR-054, CR-055, CR-056, CR-058
## Wave: W1 (5 parallel)

---

## Milestone summary

M1 ships the five W1 carry-overs of SPRINT-25's "SDLC Hardening Wrap-Up + Docs Aligned" arc as five parallel dispatches: **CR-053** (skip-list `MANIFEST.json` in copy-payload + drop the `.gitignore` stopgap + new regression test), **CR-054** (replace `${var:0:N}` char-truncation in `_truncate_stream` with `head -c $MAX_BYTES` byte-truncation in live + canonical `run_script.sh`, refresh JSDoc on `script-incident.ts:14-23`, ship 1 UTF-8 Red test), **CR-055** (refactor 4 caller `*.node.test.ts` files from spawnFn-arg-capture to `wrapScript` end-to-end + add canonical-pattern JSDoc to `wrap-script.ts`), **CR-056** (≤30 min investigation of the "CR-045 × architect" skill-candidate false-positive in `suggest_improvements.mjs:141-195`, then heuristic refactor with dedup + session-shared filter + threshold raise + 3-scenario Red test + findings report), and **CR-058** (rebrand README §3 to "Five-Role Agent Loop", add `devops` to §What `init` lays down, rewrite §Getting started step sequence to 7-step + Gate 3 + Gate 4, add §What's New SPRINT-22..24 recap, expand `cleargate-cli/README` Commands, ship `.cleargate/sprint-runs/SPRINT-25/lifecycle-diagram-prompt.md` for external image gen at Gate 4, and update `.cleargate/scratch/SDLC_hardening_continued.md`). M1 explicitly does **NOT** ship CR-057 (W2-only, depends on CR-054 byte-truncation merging first), does NOT design or ship any new skill (CR-056 §3 Out of scope), does NOT redraw `assets/lifecycle-diagram.svg` (handed off to user post-Gate 4), and does NOT touch `docs/INTERNALS.md` substantively (CR-058 verifies presence only).

---

## Order

**All 5 CRs are dispatchable concurrently.** No two W1 CRs touch the same file (verified per §2.2 of the sprint plan; re-verified at plan-time via `## Cross-CR file matrix` below).

**Two soft merge gates the orchestrator must hold:**
1. **CR-053 → CR-058 QA-Verify gate.** CR-058's `## What's New` section and §Getting started prose state that `cleargate init` "no longer writes root `MANIFEST.json`". That claim is true only post-CR-053-merge. Hold CR-058's QA-Verify dispatch until CR-053 state is `Merged`. Escape hatch: CR-058 may footnote the claim as "post-CR-053 (this sprint)" — equally valid; Dev's call.
2. **CR-054 + canonical mirror commit lockstep.** CR-054 edits `.cleargate/scripts/run_script.sh` (live) AND `cleargate-planning/.cleargate/scripts/run_script.sh` (canonical). Per FLASHCARD 2026-04-19 #wiki #protocol #mirror (line 165), both edits land in the **same commit** — DevOps merge step verifies `diff` between live + canonical returns empty. Per FLASHCARD 2026-05-01 #manifest #prebuild (line 76) AND FLASHCARD 2026-05-01 #scaffold #mirror #prebuild (line 72), the canonical edit invalidates `cleargate-planning/MANIFEST.json` SHAs — DevOps MUST re-run `npm run build` and stage the regenerated `cleargate-planning/MANIFEST.json` in the same commit.

**Within W1, no other ordering is constrained.** DevOps merges in completion-timestamp order modulo the two gates above.

---

## Cross-CR file matrix (verified no W1 collisions)

| CR | Modified files | Added files | Deleted lines |
|---|---|---|---|
| CR-053 | `cleargate-cli/src/init/copy-payload.ts` (L49 SKIP_FILES) | `cleargate-cli/test/init/copy-payload-manifest.red.node.test.ts` | `/.gitignore` L51-55 (`/MANIFEST.json` stopgap) |
| CR-054 | `.cleargate/scripts/run_script.sh` (L113-124 `_truncate_stream`) + canonical mirror at `cleargate-planning/.cleargate/scripts/run_script.sh` + `cleargate-cli/src/lib/script-incident.ts` (L14-23 JSDoc) | `cleargate-cli/test/scripts/run-script-utf8-truncation.red.node.test.ts` | (none) |
| CR-055 | `cleargate-cli/test/commands/sprint.node.test.ts`, `state.node.test.ts`, `gate.node.test.ts`, `story.node.test.ts` + `cleargate-cli/test/helpers/wrap-script.ts` (JSDoc top-of-file canonical-pattern block) | (none) | inline spawnFn-capture scenarios per file |
| CR-056 | `.cleargate/scripts/suggest_improvements.mjs` (L141-195 `scanSkillCandidates`) + canonical mirror at `cleargate-planning/.cleargate/scripts/suggest_improvements.mjs` (mirror EXISTS — verified at plan-time) | `cleargate-cli/test/lib/suggest-improvements-heuristic.red.node.test.ts` + `.cleargate/sprint-runs/SPRINT-25/skill-candidate-heuristic-findings.md` | (none) |
| CR-058 | `/README.md` (~150 LOC across §3, §What `init` lays down, §Getting started + add §What's New) + `/cleargate-cli/README.md` (Commands section ~+20 LOC) + `.cleargate/scratch/SDLC_hardening_continued.md` (~30 LOC retro) | `.cleargate/sprint-runs/SPRINT-25/lifecycle-diagram-prompt.md` | (none) |

**Collision check result:** zero shared mutable files across W1. CR-056's canonical mirror at `cleargate-planning/.cleargate/scripts/suggest_improvements.mjs` is shared with no other W1 CR (CR-054's canonical edit lives at `.../run_script.sh`).

**MANIFEST.json regen flag (per sprint plan §2.5 soft flag 5):**
- **CR-054 — REGEN REQUIRED.** Edits canonical-tracked file under `cleargate-planning/`. DevOps merge must run `npm run build` and stage regenerated `cleargate-planning/MANIFEST.json` in the same commit.
- **CR-056 — REGEN REQUIRED.** Edits canonical-tracked file under `cleargate-planning/.cleargate/scripts/`. Same regen contract.
- **CR-053, CR-055, CR-058 — NO REGEN.** Edits live entirely under `cleargate-cli/`, `cleargate-cli/test/`, root README/`.gitignore`, or `.cleargate/scratch/` — none of which feed `cleargate-planning/MANIFEST.json`.

---

## Per-CR blueprint

### CR-053 — `cleargate init` MANIFEST.json root-path bug

**Sprint-goal tie:** Closes the SPRINT-23/24 root-cause carry-over so post-CR-053 `cleargate init` runs no longer pollute user-repo roots; lets CR-058's "What's New" prose claim the fix without footnoting.

**File-surface table (key into Dev §3.1 of the CR):**

| File | Action |
|---|---|
| `cleargate-cli/src/init/copy-payload.ts` | modify — add `'MANIFEST.json'` (or `'cleargate-planning/MANIFEST.json'` per investigation finding) to `SKIP_FILES` set at L49 |
| `cleargate-cli/test/init/copy-payload-manifest.red.node.test.ts` | add — 1 Red scenario |
| `/.gitignore` | delete — lines 51-55 (the SPRINT-24 stopgap block: comment + `/MANIFEST.json`) |
| `cleargate-cli/scripts/copy-planning-payload.mjs` | read-only — verify payload structure puts `MANIFEST.json` under `cleargate-planning/` subdir of payload |

**Investigation step (Dev does this FIRST):** run `find cleargate-cli/templates -name "MANIFEST.json"`. Plan-time finding: `cleargate-cli/templates/cleargate-planning/MANIFEST.json` exists (single hit; payload is under `cleargate-planning/` subdir). If the bug is "copyPayload walks `payloadDir` rooted at `cleargate-cli/templates/cleargate-planning/` and reads MANIFEST.json as a top-level file inside that root", then the SKIP_FILES key is `'MANIFEST.json'` (relative to payloadDir, not absolute). Dev confirms via reading `copyPayload`'s walk root before adding the entry.

**Acceptance trace (CR-053 §4):**
1. `SKIP_FILES` at `copy-payload.ts:49` contains `'MANIFEST.json'` (or path-rewrite equivalent).
2. NEW Red test passes: fixture payload with root-level `MANIFEST.json` → not copied to `targetCwd`.
3. `/.gitignore` no longer contains `/MANIFEST.json` line (lines 51-55 removed; comment block too).
4. Smoke: fresh `cleargate init` in tmpdir leaves no `MANIFEST.json` at the user-repo root.
5. SPRINT-25's own Gate-4 close `cleargate init` re-sync writes no root `MANIFEST.json`.
6. `cd cleargate-cli && npm run typecheck && npm test` exits 0.

**Bounce risks:**
- **SKIP_FILES key form.** `SKIP_FILES` is a `Set<string>` and the walker checks against `relPath` (relative to `payloadDir`). If `payloadDir = cleargate-cli/templates/cleargate-planning`, then `MANIFEST.json` sits at `relPath = 'MANIFEST.json'` and `'MANIFEST.json'` is the right key. If `payloadDir = cleargate-cli/templates`, then `relPath = 'cleargate-planning/MANIFEST.json'` and that's the right key. Read `copy-payload.ts` `walk()` startup to confirm before keying.
- **Test fixture shape.** Existing init tests use `tmpdir` payload fixtures; the new Red test must construct a payload directory containing a root-level `MANIFEST.json`, invoke `copyPayload`, then assert `fs.existsSync(path.join(targetCwd, 'MANIFEST.json'))` is `false`. Use the existing tmpdir fixture pattern from neighbouring init tests (do not invent a new pattern).
- **`.gitignore` line removal hazard.** The stopgap is 5 lines (51-55) including a comment block. Remove the whole block, not just the bare line — orphaned comments confuse future readers.

**Mirror parity / MANIFEST regen flag:** **NO REGEN.** CR-053 edits `cleargate-cli/src/`, `cleargate-cli/test/`, and `/.gitignore` only — none feed `cleargate-planning/MANIFEST.json`.

**Approx LOC delta:** +5 (SKIP_FILES) + ~50 (test) + −5 (.gitignore stopgap) = **net ~+50 LOC**. Complexity: **S**.

---

### CR-054 — `run_script.sh` UTF-8 byte-correct truncation

**Sprint-goal tie:** Closes the CR-046 §0.5 Q3 deferred bug (`${var:0:N}` is char-index, not byte-count) so SPRINT-25+ incident JSON respects the 4096-byte cap on UTF-8 input. Pre-requisite for CR-057's W2 self-repair pattern matching against truncated stderr signatures.

**File-surface table:**

| File | Action |
|---|---|
| `.cleargate/scripts/run_script.sh` (live) | modify — replace `${content:0:$MAX_BYTES}` at L122 with `head -c $MAX_BYTES "$file"`; refactor `_truncate_stream` body L113-124 to read directly from `$file` rather than via `$content` variable |
| `cleargate-planning/.cleargate/scripts/run_script.sh` (canonical mirror) | modify — IDENTICAL diff to live, same commit (mirror parity invariant) |
| `cleargate-cli/src/lib/script-incident.ts` | modify — JSDoc on `MAX_STREAM_BYTES` at L14-23 + `TRUNCATION_SUFFIX` JSDoc — clarify "byte-count, not char-count"; document partial-multi-byte-char trade-off ("output may end with an escaped partial UTF-8 sequence; downstream `JSON.stringify` produces valid JSON via escape") |
| `cleargate-cli/test/scripts/run-script-utf8-truncation.red.node.test.ts` | add — 1 Red scenario |
| `cleargate-planning/MANIFEST.json` | regenerated by `npm run build` (DevOps step, same commit) |

**Per CR-054 §0.5 Q1 default:** byte-truncation strategy is `head -c $MAX_BYTES`, NOT a Node helper, NOT `dd bs=1`. Per Q2 default: ACCEPT escaped partial-char output (do not implement clean-boundary trim). Per Q5 default: keep `MAX_STREAM_BYTES = 4096`; document that total field byte length is `MAX_STREAM_BYTES + Buffer.byteLength(TRUNCATION_SUFFIX)` (~16 ASCII bytes overhead).

**Acceptance trace (CR-054 §4):**
1. `_truncate_stream()` in live AND canonical uses `head -c $MAX_BYTES "$file"` (or equivalent byte-count primitive). Function signature unchanged.
2. JSDoc on `script-incident.ts:14-23` documents byte-count semantics + partial-char trade-off.
3. NEW Red test, 1 scenario: UTF-8 fixture (e.g., 5000 cyrillic chars ≈ 10KB) wrapper-invoked → assert `Buffer.byteLength(incidentJson.stdout) ≤ MAX_STREAM_BYTES + Buffer.byteLength(TRUNCATION_SUFFIX) + 4` (small slack for line-buffering). Existing CR-046 ASCII wrapper tests still pass (no regression).
4. `JSON.parse` round-trip on the resulting incident file succeeds (no broken UTF-8 escaping that breaks parse).
5. Mirror parity: `diff .cleargate/scripts/run_script.sh cleargate-planning/.cleargate/scripts/run_script.sh` returns empty.
6. `cd cleargate-cli && npm run typecheck && npm test` exits 0.

**Bounce risks:**
- **`head -c` reads from file, not from stdin variable.** Current `_truncate_stream` reads `cat "$file"` into `$content` then char-slices. The byte-correct refactor should `head -c $MAX_BYTES "$file"` directly — no intermediate `$content` variable. This also drops the `byte_len=${#content}` length check (which itself was char-count, not byte-count); replace with `[[ $(wc -c < "$file") -le $MAX_BYTES ]]` for the no-truncation-needed branch, or unconditionally `head -c` (since `head -c N` on a shorter file emits the whole file with no suffix — the suffix-append must be conditional on truncation having occurred).
- **Suffix-append condition.** The current logic appends `TRUNCATION_SUFFIX` only when `byte_len > MAX_BYTES`. The byte-correct refactor must preserve this conditional: `head -c $MAX_BYTES` always emits ≤N bytes regardless of input size, so the suffix gate must be a separate `wc -c` check on the source file. Don't append the suffix unconditionally.
- **`wrapScript` test helper consumed.** Per CR-054 §Existing Surfaces and FLASHCARD 2026-05-04 #wrapper #e2e-test-pattern (line 9), the new Red test MUST use `wrapScript` from `cleargate-cli/test/helpers/wrap-script.ts` — do NOT inline a fresh tmpdir-spawnSync. spawnMock-style tests will not catch byte-vs-char regressions per FLASHCARD 2026-05-04 #cr-046 #wrapper #breaking-change (line 8).
- **UTF-8 fixture content.** Use `'р'.repeat(5000)` (cyrillic 'р', 2 bytes per char) → 10000 bytes. Avoid CJK multi-byte that may interact with terminal encoding. Or use a file-based fixture written with `fs.writeFileSync` UTF-8 default.
- **Mirror parity assertion.** Per FLASHCARD 2026-04-19 #wiki #protocol #mirror (line 165), both files updated in same commit. Per FLASHCARD 2026-05-04 #mirror #parity (line 30), Architect plans have historically miscounted mirror sites — the live + canonical for `run_script.sh` are both real and tracked; both edited.

**Mirror parity / MANIFEST regen flag:** **REGEN REQUIRED.** Canonical mirror edit invalidates `cleargate-planning/MANIFEST.json`. DevOps step: `npm run build` from repo root, then `git add cleargate-planning/MANIFEST.json` in the same commit as the source edit.

**Approx LOC delta:** ~−12/+15 in run_script.sh (twice, live + canonical) + ~+10 JSDoc + ~80 (test) = **net ~+90 LOC**. Complexity: **S**.

---

### CR-055 — wrapScript helper adoption in 4 caller tests

**Sprint-goal tie:** Closes the CR-050 leverage gap (caller tests bypass wrapScript helper); establishes the canonical wrapScript pattern for SPRINT-26+ caller tests via top-of-file JSDoc; catches the CR-046-class interface-level wrapper bugs that spawnFn-arg-capture cannot.

**File-surface table:**

| File | Action |
|---|---|
| `cleargate-cli/test/commands/sprint.node.test.ts` | modify — refactor spawnFn-arg-capture scenarios that invoke `run_script.sh` to consume `wrapScript`; preserve test count ±2 |
| `cleargate-cli/test/commands/state.node.test.ts` | modify — same |
| `cleargate-cli/test/commands/gate.node.test.ts` | modify — same |
| `cleargate-cli/test/commands/story.node.test.ts` | modify — same |
| `cleargate-cli/test/helpers/wrap-script.ts` | modify — add ~20-line top-of-file JSDoc section "## Canonical caller-test pattern" with a 5-10 line minimal example |

**Per CR-055 §0.5 defaults:** REPLACE (Q1) — swap spawnFn-arg-capture for wrapScript end-to-end; do not keep both patterns alongside. Preserve test count ±2 per file (Q2). Runtime budget: ≤2× current `time npm test` total (Q3); if breaches, scope-cut to 1 wrapScript scenario per file (escape hatch). Q5: NO Red test (refactor; semantics unchanged). Q4: YES, JSDoc canonical-pattern block in wrap-script.ts.

**Acceptance trace (CR-055 §4):**
1. All 4 files import `wrapScript` from `'../helpers/wrap-script.js'`.
2. Each file has ≥1 scenario that invokes the real wrapper via `wrapScript` (not `spawnFn`-arg-capture).
3. Test count post-refactor preserved within ±2 per file.
4. Suite runtime ≤2× pre-refactor measured via `time npm test`.
5. `wrap-script.ts` top-of-file JSDoc has `## Canonical caller-test pattern` section with 5-10 line example.
6. `cd cleargate-cli && npm run typecheck && npm test` exits 0.

**Bounce risks:**
- **Some spawnFn-arg-capture scenarios have NO wrapScript equivalent** — e.g., scenarios asserting `spawn called with cwd: X` or assertions on the exact arg array. Per CR-055 §0.5 Q2 default, keep a minimal spawnFn-arg-capture scenario alongside the wrapScript one in those cases. Do NOT delete the spawnFn-capture scenario if it tests a non-wrapper-invocation concern (e.g., a different caller's `git` spawn).
- **Test runtime explosion.** `wrapScript` spawns a real `tsx` process per scenario (~3-5s each). 4 files × 2-3 scenarios × 5s = 40-60s added. If the suite runtime budget breaches (per CR-055 §4 #4), scope-cut to 1 wrapScript scenario per file and document the cut in the Dev's commit message. Do not exceed 2× current.
- **`wrapScript` is async.** `await wrapScript(...)` requires the test fn to be `async`. spawnFn-capture tests are sync. Update each refactored scenario's test fn signature to `async () => { ... }`.
- **NODE_TEST_CONTEXT scrub already inside `wrapScript`** — Devs need NOT manually scrub it again (FLASHCARD #node-test #child-process is enforced by the helper). Don't double-scrub.
- **macOS realpathSync mitigation already handled** by the helper — Devs don't need to wrap tmpdir paths manually.

**Mirror parity / MANIFEST regen flag:** **NO REGEN.** Edits live entirely under `cleargate-cli/test/`.

**Approx LOC delta:** est. ~−40/+40 per file × 4 files (refactor offset) + ~+25 JSDoc canonical-pattern block = **net ~+25 LOC**. Complexity: **M**.

---

### CR-056 — Skill-candidate heuristic investigation + fix

**Sprint-goal tie:** Removes the every-sprint false-positive "CAND-SPRINT-NN-S01: CR-045 × architect" noise from `improvement-suggestions.md`; raises signal-to-noise on the close-pipeline Step 6.6 output so future real skill-candidates aren't dismissed as more of the same.

**File-surface table:**

| File | Action |
|---|---|
| `.cleargate/scripts/suggest_improvements.mjs` (live) | modify — `scanSkillCandidates` at L141-260; add session-shared filter, raise threshold from "≥3× tuple-count" to "≥3× across ≥2 distinct sprints AND not session-shared", dedup by hash across prior sprints' improvement-suggestions.md |
| `cleargate-planning/.cleargate/scripts/suggest_improvements.mjs` (canonical mirror — VERIFIED EXISTS at plan-time) | modify — IDENTICAL diff to live, same commit |
| `cleargate-cli/test/lib/suggest-improvements-heuristic.red.node.test.ts` | add — 3 Red scenarios |
| `.cleargate/sprint-runs/SPRINT-25/skill-candidate-heuristic-findings.md` | add — Dev's pre-fix investigation report (≤300 words) |
| `cleargate-planning/MANIFEST.json` | regenerated by `npm run build` (canonical mirror edit triggers SHAs invalidation) |

**Per CR-056 §0.5 defaults:** Q1 — ≤30 min Dev investigation (read source + trace attribution + confirm false-positive). Q2 — fix shape: dedup-by-hash-across-prior-sprints OR session-shared filter OR threshold raise (Dev picks based on findings; defaults to all three combined). Q3 — scope-cut threshold: if heuristic fix is structural-deep, ship 1-line guard + flashcard "needs SPRINT-26 redesign" + escalate to Reporter §6 Tooling. Q4 — NO flashcard cleanup in this CR. Q5 — threshold raise to "≥3× across ≥2 distinct sprints AND not session-shared".

**Acceptance trace (CR-056 §4):**
1. `.cleargate/sprint-runs/SPRINT-25/skill-candidate-heuristic-findings.md` exists, ≤300 words, diagnoses CR-045 × architect as false-positive (or surfaces a real pattern with evidence — see "Bounce risks" below).
2. Heuristic in `suggest_improvements.mjs:141-260` (live + canonical) deduplicates by hash across prior sprints' improvement-suggestions.md, filters session-shared attribution, raises threshold.
3. NEW Red test, 3 scenarios: (a) false-positive class (session-shared) is filtered out; (b) real pattern (≥3× across ≥2 sprints, not session-shared) still flagged; (c) dedup: same hash already in prior sprint's suggestions → not re-surfaced.
4. SPRINT-25's own Gate-4 close runs Step 6.6; resulting `.cleargate/sprint-runs/SPRINT-25/improvement-suggestions.md` does NOT contain CR-045 × architect (or contains it with `seen_in: [SPRINT-23, SPRINT-24, SPRINT-25] — known false-positive` marker).
5. Mirror parity: live = canonical for `suggest_improvements.mjs`.
6. `cd cleargate-cli && npm run typecheck && npm test` exits 0.

**Bounce risks:**
- **Real pattern surfaces (not false-positive).** Per CR-056 §3 Out of scope: NO skill ships in this CR. If investigation reveals a genuine recurring sub-flow (e.g., Architect repeatedly does the same work-item-frontmatter audit), Dev surfaces in `findings.md` + queues for SPRINT-26 + adds the false-positive class to the heuristic anyway (the class is still false-positive; orthogonal to the real-pattern discovery). Do NOT design a skill in CR-056.
- **Session-shared detection key.** Token-ledger entries have `session` (UUID), `work_item_id`, `agent_type`. Session-shared filter: if the same `session` UUID accounts for ≥2 of the ≥3 tuple counts AND those entries' `work_item_id`s diverge from the bucket's first-merged work-item, treat as session-attribution artifact. Plan-time observation: the bucket-key is `${entry.work_item_id}|${entry.agent_type}` (line 156); the filter must operate on the multi-line set per bucket, not per-entry.
- **Cross-sprint dedup hash semantics.** Existing dedup is intra-sprint via `existingContent.includes(\`<!-- hash:${hash} -->\`)` at line 191 — it skips re-surfacing within the SAME sprint's suggestions file. The cross-sprint dedup must extend this to scan prior sprints' `improvement-suggestions.md` files (e.g., glob `.cleargate/sprint-runs/*/improvement-suggestions.md`). Per CR-056 §0.5 Q2 default, when found in prior sprint's suggestions, surface as `seen_in: [SPRINT-23, SPRINT-24, SPRINT-25]` rather than treating as a new candidate.
- **Mirror parity per FLASHCARD 2026-04-19 #wiki #protocol #mirror.** Both `suggest_improvements.mjs` files updated in SAME commit; DevOps `diff` returns empty.
- **MANIFEST regen.** Per FLASHCARD 2026-05-01 #manifest #prebuild (line 76), canonical edit invalidates `cleargate-planning/MANIFEST.json` SHAs. DevOps re-runs `npm run build` and stages.
- **Test fixtures must come from real prior-sprint data OR carefully-crafted synthetic.** Use `.cleargate/sprint-runs/SPRINT-23/token-ledger.jsonl` and `SPRINT-24/token-ledger.jsonl` as fixture inputs (they exist; the false-positive is reproducible). For the "real pattern" scenario, hand-construct a synthetic ledger with 3 entries across 2 sprints, distinct sessions, same `work_item_id|agent_type`. Path-isolate via `CLEARGATE_FLASHCARD_PATH` (already supported per L142-144) AND pass a temp sprint-dir as `argv[2]` (the script accepts `<sprint-id>` and resolves `sprintDir` via `REPO_ROOT`; per FLASHCARD 2026-04-21 #test-harness #scripts #env line 128, add `CLEARGATE_SPRINT_DIR` env override if needed).
- **Existing FLASHCARD evidence.** Per CR-056 §Existing Surfaces, prior-sprint `improvement-suggestions.md` files at `.cleargate/sprint-runs/SPRINT-23/` and `.cleargate/sprint-runs/SPRINT-24/` are the smoking-gun evidence. Dev may cite them in `findings.md`.

**Mirror parity / MANIFEST regen flag:** **REGEN REQUIRED.** Canonical mirror edit triggers `cleargate-planning/MANIFEST.json` SHA refresh. Same-commit regen via DevOps `npm run build`.

**Approx LOC delta:** ~+40 (heuristic refactor) × 2 (live + canonical) + ~+100 (3 Red scenarios) + ~250 words (findings) = **net ~+200 LOC**. Complexity: **M**.

---

### CR-058 — README refresh + lifecycle diagram prompt

**Sprint-goal tie:** Brings the framework's most-read docs current with the SPRINT-22..24 reality (5-role 7-step loop, 4 gates, TPV, DevOps); ships the lifecycle-diagram prompt the user feeds to an external image generator at Gate 4 close (per user direction "do not draw diagram"); marks the SDLC Hardening arc complete in the scratch roadmap doc.

**File-surface table:**

| File | Action |
|---|---|
| `/README.md` | modify — line 7 tagline (rebrand "four-agent loop" → "five-role agent team"); line 56 §The problem table (same fix); §3 The Four-Agent Loop full rewrite as §3 The Five-Role Agent Loop (5 roles, Architect's 4 modes); §5 Quality Assurance add §Step 2.6b cross-sprint orphan reconciler reference; §6 MCP adapter verify Jira/Linear/GHP claim, soften if aspirational; §7 Self-Improving engine add TPV catch-rate + Hotfix Audit semantics; §What `init` lays down add `devops`, `cleargate-wiki-contradict`, `mid-sprint-triage-rubric.md`, `.cleargate/scripts/run_script.sh`; §Getting started rewrite step-sequence to 7-step loop + Gate 3 (`cleargate sprint preflight`) + Gate 4 (`close_sprint.mjs --assume-ack`); ADD §What's New (SPRINT-22..SPRINT-24) ~30-line CR recap; lifecycle diagram footnote: "Diagram refresh in flight — see SPRINT-25 lifecycle-diagram-prompt.md" |
| `/cleargate-cli/README.md` | modify — Commands section: add `cleargate sprint preflight`, `sprint init/close`, `gate check`, `doctor`, `state update/validate`, `story start/done/bouncing` (~+20 LOC) |
| `.cleargate/scratch/SDLC_hardening_continued.md` | modify — mark SPRINT-22/23/24 as complete (drop "placeholder" framing on SPRINT-24); add SPRINT-25 wrap-up retro section "SDLC Hardening arc closes here; framework ergonomic; SPRINT-26+ returns to product direction" |
| `.cleargate/sprint-runs/SPRINT-25/lifecycle-diagram-prompt.md` | add — image-gen prompt, ~80-120 lines, structured headings per CR-058 §0.5 Q5 default: `## Subject`, `## Style`, `## Layout`, `## Reference (boxes + arrows)`, `## Color palette`, `## Caption` |
| `assets/github-banner.svg` | read-only — verify presence (referenced from README) |
| `assets/lifecycle-diagram.svg` | read-only — verify presence (referenced from README); do NOT redraw |
| `docs/INTERNALS.md` | read-only — verify presence (referenced from README §Want to know more L201); flag separate CR-059 if substantively stale |
| `cleargate-planning/.claude/agents/architect.md`, `developer.md`, `devops.md`, `qa.md`, `reporter.md` | read-only — reference for accurate role-prose in §3 rewrite |
| `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` | read-only — reference for accurate 7-step loop prose |

**Per CR-058 §0.5 defaults:** Q1 — REBRAND to "Five-Role Agent Loop" (not "four + DevOps footnote"). Q2 — TPV is an Architect mode, not a 6th role; README mentions Architect has 4 modes (SDR, M1, TPV, post-flight). Q3 — verify MCP adapter claim pre-edit; soften if aspirational. Q4 — YES add §What's New ~30 lines. Q5 — markdown structured headings (not YAML). Q6 — UPDATE SDLC scratch in place (not archive).

**Plan-time grep results (Dev MUST NOT re-discover):**
- `grep -nE "four-agent loop|Architect → Developer → QA → Reporter" README.md` returns **4 matches** (lines 7, 56, 143, 176). All 4 must flip post-CR.
- `grep` same patterns in `cleargate-cli/README.md` returns **0 matches** (no rebrand needed there; just Commands section expansion).

**Acceptance trace (CR-058 §4):**
1. README §3 heading is "The Five-Role Agent Loop" (or equivalent rebranding); body describes 5 roles + Architect's 4 modes.
2. README §3 mentions all of: Sprint Design Review, Test Pattern Validation, DevOps merge step, post-flight architectural review.
3. README §What `init` lays down agent list includes `devops`.
4. README §Getting started step-sequence references 7-step per-story loop AND Gate 3 (preflight) AND Gate 4 (close).
5. NEW §What's New section lists CRs 042-052 at one-liner granularity.
6. `cleargate-cli/README.md` Commands section adds: `sprint preflight`, `gate check`, `doctor`, `state update/validate`, `story start/done`.
7. NEW `.cleargate/sprint-runs/SPRINT-25/lifecycle-diagram-prompt.md` exists, ≥80 lines, with structured headings (`## Subject`, `## Style`, `## Layout`, `## Reference`, `## Color palette`, `## Caption`).
8. `.cleargate/scratch/SDLC_hardening_continued.md` marks SPRINT-24 complete (not "placeholder"); adds SPRINT-25 wrap-up section.
9. **No broken references:** every file/agent/path mentioned in new README copy must exist on disk (QA-Verify spot-checks via grep).
10. Reporter Brief at Gate 4 surfaces the diagram prompt (Open Q5 default; Reporter contract).
11. `cd cleargate-cli && npm run typecheck && npm test` exits 0 (no code changes; baseline preserved).

**Bounce risks:**
- **CR-053 prose-coupling.** §What's New and §Getting started reference `cleargate init` "no longer writes root MANIFEST.json post-CR-053". This claim is true only AFTER CR-053 merges. Either (a) orchestrator gates CR-058 QA-Verify on CR-053 merge state (preferred — see "Order" §); or (b) CR-058 footnotes the claim as "post-CR-053 (this sprint)" — equally valid. Pick one in the Dev's first commit; do not waver.
- **MCP adapter Jira/Linear/GHP claim verification.** Per CR-058 §0.5 Q3, Dev MUST verify before keeping the claim verbatim. Plan-time directive: grep `mcp/src/adapters/` for adapter shipments; if Jira/Linear/GHP adapters are NOT shipped (only stubs or roadmap), soften to "MCP server with adapter framework; native Jira/Linear/GitHub Projects in development; see [INTERNALS.md] for adapter status". Do NOT keep an unverified claim.
- **§What's New scope creep.** Listing 11 CRs (042-052) at "one-liner granularity" is ~30 lines max. If it grows beyond ~50 lines, scope-cut to 5-7 highlight CRs + a `See sprint files for the rest.` footer. Do not write a multi-paragraph essay per CR.
- **§3 rewrite reference fidelity.** Read `cleargate-planning/.claude/agents/{architect,developer,qa,devops,reporter}.md` AND `.../skills/sprint-execution/SKILL.md` for the actual role contracts before rewriting prose. Do NOT paraphrase from memory; the README is the user-facing doc and inaccuracies here directly contradict the framework. Per FLASHCARD 2026-04-19 #wiki #ledger #role-attribution (line 99), token-ledger role-grep is hard-coded — README's role list MUST match the canonical agents/ files exactly (5 roles: architect, developer, qa, devops, reporter; no others; "TPV" is an Architect mode, not a role).
- **Gate naming consistency.** Use exactly: Gate 1 (Initiative), Gate 2 (Ambiguity), Gate 3 (preflight), Gate 4 (close). Do NOT introduce new gate names or numberings. The 4-gate vocab is canonical (see CLAUDE.md ClearGate-block § "Halt at gates" + § "Boundary gates").
- **TPV is an Architect mode.** Do NOT promote TPV to a 6th role. Per CR-058 §0.5 Q2 default, README §3 keeps "five-role" framing and lists Architect's 4 modes inline.
- **Lifecycle diagram prompt — no SVG redraw.** Per user direction (cited in CR-058 context_source) and §3 Out of scope: do NOT generate the SVG. Ship the prompt.md only; user runs through external generator post-Gate-4.
- **`docs/INTERNALS.md` substantive update is OUT of scope.** Per CR-058 §3 Out of scope: verify presence only. If grep shows it's substantively stale (e.g., references "four-agent loop" itself, missing TPV/DevOps), file a CR-059 followup and proceed without editing.
- **Wiki rebuild post-merge.** README is at `/README.md` — a tracked-but-non-delivery file. The wiki build skips it (wiki ingest only walks `.cleargate/delivery/`). No wiki ingest on this CR.

**TPV passthrough:** CR-058 has NO test surface (no Red test, no Green test). Architect Mode: TPV will pass-through with note: "prose-only — no test wiring to validate". Reporter §6 metrics: count CR-058 in TPV denominator with caveat, NOT as a TPV signal contributor (per sprint plan §2.5 soft flag 3).

**Mirror parity / MANIFEST regen flag:** **NO REGEN.** Edits live entirely under `/README.md`, `/cleargate-cli/README.md`, `.cleargate/scratch/`, and `.cleargate/sprint-runs/SPRINT-25/`. None feed `cleargate-planning/MANIFEST.json`.

**Approx LOC delta:** ~+150 README + ~+20 cleargate-cli/README + ~+30 SDLC scratch + ~+100 lifecycle prompt = **net ~+300 LOC** (all docs). Complexity: **M-L**.

---

## Cross-CR risks

Things a Developer working on only their own CR might miss:

1. **CR-058 → CR-053 merge gate.** CR-058 Dev claims `cleargate init` "no longer writes root MANIFEST.json". If CR-058 merges before CR-053, the README claims a fix that is not yet in. Orchestrator-gated via "hold CR-058 QA-Verify until CR-053 = Merged"; if Dev wants to ship faster, footnote escape hatch: "post-CR-053 (this sprint)".

2. **CR-054 + CR-056 both REQUIRE MANIFEST.json regen.** Both edit `cleargate-planning/.cleargate/scripts/`. Each merge commit MUST stage the regenerated `cleargate-planning/MANIFEST.json` in the same commit (per FLASHCARD 2026-05-01 #manifest #prebuild line 76). If two W1 CRs land back-to-back without a regen between them, the second's regen will overwrite the first's — which is fine if both are staged (npm run build is idempotent against current source state), but each Dev's commit individually must include its own regen.

3. **CR-055 wrapScript helper signature stability.** CR-055 refactors callers to consume `wrapScript`; CR-054 ships a NEW Red test that ALSO consumes `wrapScript`. Both must agree on `wrapScript`'s public API (no signature change). Plan-time check: `cleargate-cli/test/helpers/wrap-script.ts:31-56` defines `WrapScriptOptions` — neither CR alters this interface. CR-055's only edit to `wrap-script.ts` is JSDoc top-of-file (canonical-pattern block).

4. **CR-053 SKIP_FILES vs FIRST_INSTALL_ONLY confusion.** Per CR-053 §0.5 Q5 default: do NOT touch `FIRST_INSTALL_ONLY`. The two mechanisms serve different concerns: SKIP_FILES = "don't copy at all" (correct for MANIFEST.json which is a meta-repo build artifact); FIRST_INSTALL_ONLY = "copy on fresh install but never overwrite" (correct for `.gitignore`, `.cleargate/scripts/*`, etc.). If a Dev confuses the two and adds MANIFEST.json to FIRST_INSTALL_ONLY, the bug persists for fresh-install users (the file lands at root on first init).

5. **CR-056 false-positive may surface real pattern.** Per CR-056 §3: NO skill ships in this CR. If Dev's investigation reveals a genuine pattern (e.g., the same Architect sub-flow really does repeat across non-shared sessions), surface in `findings.md` + queue for SPRINT-26 — do NOT design the skill mid-sprint. The investigation budget is ≤30 min; skill design budget is much larger.

6. **CR-058 §3 role descriptions must match canonical agents/ files exactly.** Read `cleargate-planning/.claude/agents/{architect,developer,qa,devops,reporter}.md` and the sprint-execution SKILL.md for accurate prose. Per FLASHCARD 2026-04-19 #wiki #ledger #role-attribution (line 99), the token-ledger role-grep loop is hard-coded; README must match those role names exactly. No invented roles, no synonym swaps.

7. **wrapScript-based tests scrub `NODE_TEST_CONTEXT` already** — CR-054's new Red test and CR-055's refactored caller tests both rely on this. Don't double-scrub; don't override.

---

## TPV expectations

For each CR, what tests should exist post-QA-Red, what Architect's TPV mode validates:

- **CR-053 — TPV validates 1 Red scenario.** `cleargate-cli/test/init/copy-payload-manifest.red.node.test.ts` must exist, must be RED at QA-Red gate (asserts `MANIFEST.json` NOT copied; pre-fix the file IS copied, so the test fails red), must turn GREEN post-Dev. TPV verifies the test wires through `copyPayload`'s real walk + assertion against `targetCwd` filesystem state — NOT a mock.
- **CR-054 — TPV validates 1 Red scenario.** `cleargate-cli/test/scripts/run-script-utf8-truncation.red.node.test.ts` must exist, RED at QA-Red. TPV verifies the test consumes `wrapScript` (per FLASHCARD #wrapper #e2e-test-pattern line 9 — NOT spawnMock-style). UTF-8 fixture must be ≥4096 bytes (5000 cyrillic chars). Assertion: `Buffer.byteLength(incidentJson.stdout) ≤ MAX_STREAM_BYTES + Buffer.byteLength(TRUNCATION_SUFFIX) + slack`. Pre-fix `${var:0:N}` truncates by char-index → assertion fails red.
- **CR-055 — TPV validates refactor preserves test count + wires through wrapScript.** No NEW Red test (CR-055 §0.5 Q5: refactor; semantics unchanged). TPV checks: each of 4 caller test files imports `wrapScript`; each has ≥1 scenario invoking real wrapper (not spawnFn-arg-capture); test count per file ±2 of pre-refactor count; suite runtime ≤2× pre-refactor.
- **CR-056 — TPV validates 3 Red scenarios.** `cleargate-cli/test/lib/suggest-improvements-heuristic.red.node.test.ts`. Scenarios: (a) session-shared filtered out; (b) real pattern still flagged; (c) cross-sprint dedup works. TPV verifies fixtures use real `.cleargate/sprint-runs/SPRINT-23/token-ledger.jsonl` + synthetic 2-sprint multi-session ledger; verifies `CLEARGATE_FLASHCARD_PATH` test seam used (per FLASHCARD #test-harness #scripts #env line 128); verifies path-isolation via env override.
- **CR-058 — TPV PASS-THROUGH (prose-only).** No Red test ships. TPV emits a "prose-only — no test wiring to validate" note and passes through. Reporter §6 metrics: count CR-058 in TPV denominator with caveat, NOT as TPV signal contributor (per sprint plan §2.5 soft flag 3).

**TPV catch-rate metric (passive, per sprint plan §0 metrics):** SPRINT-25 has 6 standard-lane CRs (5 in W1, 1 in W2). Track: did TPV catch ≥1 wiring gap? If 0/6 (combined with SPRINT-24's 0/4 = 0/10 across two sprints), strong signal to downgrade per CR-047 §0.5 Q4 at SPRINT-26 kickoff.

---

## Open decisions for orchestrator

Things I will NOT decide — flag them up:

1. **CR-058 ↔ CR-053 sequencing posture.** Two valid options: (a) hard-gate CR-058 QA-Verify on CR-053 = Merged; or (b) let CR-058 ship in parallel with a "post-CR-053 (this sprint)" footnote. (a) is cleaner prose; (b) is faster execution. Orchestrator picks; communicate the choice in the Dev dispatch brief for CR-058. Default per sprint plan §2.3: option (a).

2. **CR-056 investigation budget overrun.** Per CR-056 §0.5 Q1 default: ≤30 min. If Dev's investigation exceeds 30 min (e.g., source is more tangled than expected), Dev surfaces a Blockers Report; orchestrator chooses scope-cut to 1-line guard (per Q3 default) or grant a ≤30 min extension. Do NOT silently exceed.

3. **CR-058 MCP adapter claim verification outcome.** Dev verifies pre-edit. Two paths: (a) keep claim verbatim if shipped; (b) soften per Q3 default. Outcome depends on `mcp/src/adapters/` grep; no plan-time decision possible. Surface in Dev's first commit message.

4. **CR-055 runtime budget overrun.** Per CR-055 §0.5 Q3 default: ≤2× current `time npm test`. If suite exceeds 2× post-refactor, Dev scope-cuts to 1 wrapScript scenario per file. Verify via `time npm test` measurement at QA-Verify; if exceeded and not cut, kick back to Dev.

5. **CR-058 Reporter Brief Gate-4 contract.** Per CR-058 §0.5 Q5 default + §4 #10: Reporter at Gate 4 surfaces the lifecycle-diagram-prompt.md in the Brief. This is a Reporter-side contract, not a Dev-side acceptance — orchestrator must ensure the SPRINT-25 close prompt asks Reporter to surface it. Track as a Gate-4 checklist item, not as a CR-058 acceptance gate.

---

## Risks & open hooks

| Risk | Watch |
|---|---|
| CR-053 SKIP_FILES key path-form ambiguity | Dev investigates `payloadDir` walk root before keying; first commit message must cite the form chosen |
| CR-054 mirror parity drift | DevOps `diff` on live + canonical; FLASHCARD 2026-04-19 #wiki #protocol #mirror line 165 |
| CR-054 + CR-056 MANIFEST regen forgotten | DevOps `npm run build` + `git add cleargate-planning/MANIFEST.json` in same commit; FLASHCARD 2026-05-01 #manifest #prebuild line 76 |
| CR-055 runtime explosion | `time npm test` measurement at QA-Verify; scope-cut to 1 scenario per file if breached |
| CR-056 real-pattern surprise | NO skill in CR-056; queue for SPRINT-26; flashcard the surprise |
| CR-058 four-agent-loop string lingers | grep at QA-Verify: `grep -nE "four-agent loop\|Architect → Developer → QA → Reporter" README.md cleargate-cli/README.md` returns 0 matches |
| CR-058 §3 role descriptions paraphrased from memory | Dev reads canonical agents/ files first; QA-Verify spot-checks role names match exactly |
| CR-058 INTERNALS.md substantively stale | Verify presence only; file CR-059 followup; do NOT edit |
| TPV mis-fires on prose-only CR-058 | Note in Reporter §6: "CR-058 in TPV denominator with caveat" |

**Watch hooks for orchestrator:**
- After CR-054 merge: re-spawn Architect for M2 plan (CR-057) — line numbers in `run_script.sh` will have shifted; M2 must re-pin them.
- After CR-053 merge: unblock CR-058 QA-Verify if option (a) gating chosen.
- At Gate 4: Reporter Brief surfaces lifecycle-diagram-prompt.md.
- At SPRINT-26 kickoff: TPV catch-rate decision per CR-047 §0.5 Q4 (combined with SPRINT-24 data).

### M2.md

# Milestone: M2 — CR-057 run_script.sh Self-Repair (Incident-Corpus Driven)
## CRs: CR-057
## Wave: W2 (sequential after W1; sole CR in this wave)

role: architect

## Milestone summary

M2 ships exactly one CR — CR-057 — and its scope is **branch-decided** at investigation time. CR-057 is a 2-phase CR: (Phase 1) Investigate the SPRINT-23 + SPRINT-24 `.script-incidents/*.json` corpus and tally failure patterns by `command + exit_code + stderr signature`; (Phase 2) either ship bounded self-repair logic targeting the top 1–3 recurring patterns (CODE-MODE) or document corpus-too-thin findings and defer self-repair (DOCS-MODE). Pre-evidence (Architect plan-time enumeration of `.cleargate/sprint-runs/SPRINT-23/.script-incidents/` = does not exist; `.cleargate/sprint-runs/SPRINT-24/.script-incidents/` = 3 files) shows the corpus contains zero recurring real-failure patterns — exit-127 appears 2× but both occurrences are synthetic test artefacts (intentionally fake binary + a tmp-wrapper without PATH context); exit-1 appears 1×. Per CR-057 §0.5 Q1 default ("≥2 occurrences per pattern"), the corpus does not clear the CODE-MODE threshold, and the DOCS-MODE outcome is the high-probability branch. Dev MUST still run the investigation and document the tally — they MUST NOT short-circuit to DOCS-MODE based on this plan; the investigation itself is part of the deliverable.

## Order

CR-057 is the sole CR in W2. M1 must merge in full before W2 dispatch — specifically CR-054 (UTF-8 byte-correct truncation in `run_script.sh`) must be on `main` so the self-repair sibling, if shipped, integrates against byte-correct stderr signatures rather than char-truncated ones (per Sprint Plan §2.1 W2 rationale). No parallelisable pairs in this wave.

## Per-CR blueprint

### CR-057

**Sprint-goal tie:** advances "Finish the SDLC Hardening arc" by closing CR-046 §0.5 Q3's deferred self-repair question — either ships bounded retry for real-observed patterns OR formally documents "no pattern recurred; revisit at threshold N" so the question stops floating between sprints.

**Branch decision criterion (resolved by Dev investigation, NOT by this plan):**
- **CODE-MODE iff** ≥1 (preferably 1–3) failure patterns each appear ≥2× in the combined SPRINT-23 + SPRINT-24 corpus AND the pattern is a real (non-synthetic) failure.
- **DOCS-MODE iff** the corpus tally fails the above bar. Synthetic test artefacts (e.g. intentionally-missing binaries, fake-PATH invocations) are excluded from the recurrence count.
- Pre-evidence at plan-time: corpus = 3 incidents; 2 share `exit_code: 127` BUT both are synthetic test artefacts; 1 has `exit_code: 1` ("Multiple state.json files found" — a usage error, not a flaky pattern). **High-probability branch: DOCS-MODE.** Dev MUST verify by reading each JSON and forming the tally themselves.

---

#### CODE-MODE branch (low-probability)

**File-surface table (CODE-MODE):**

| File | Action | LOC | Rationale |
| --- | --- | --- | --- |
| `.cleargate/scripts/_run_script_self_repair.sh` (or `.mjs`) | **NEW** sibling | ~50 | Self-repair logic; isolated from `run_script.sh` per CR-057 §0.5 Q5 default. |
| `cleargate-planning/.cleargate/scripts/_run_script_self_repair.sh` (or `.mjs`) | **NEW** canonical mirror | byte-identical | Mirror parity invariant (FLASHCARD 2026-04-19 #wiki #protocol #mirror). |
| `.cleargate/scripts/run_script.sh` | EDIT | ~10 additive | Call self-repair sibling on non-zero exit, before incident-write block. |
| `cleargate-planning/.cleargate/scripts/run_script.sh` | EDIT | byte-identical | Mirror parity. |
| `cleargate-cli/src/lib/script-incident.ts` | EDIT (interface extension) | ~5 | Add optional `retry_attempt: number` field + JSDoc; update `isScriptIncident` type guard to allow `undefined` for the new field (since it is optional). |
| `cleargate-cli/test/scripts/run-script-self-repair.red.node.test.ts` | **NEW** Red test | ~80 | N+1 scenarios: each pattern retries-once + `RUN_SCRIPT_NO_REPAIR=1` bypass scenario. Production-path integration test (per FLASHCARD 2026-05-04 #cr-046 #wrapper #breaking-change) — uses the real wrapper via `os.tmpdir()` + `spawnSync`, NOT spawnMock. |
| `.cleargate/sprint-runs/SPRINT-25/script-incident-corpus-analysis.md` | **NEW** findings report | n/a | Tally + pattern selection rationale; lives under sprint-runs (not knowledge/) per CR-057 §2 CODE-MODE bullet. |
| `cleargate-planning/MANIFEST.json` | REGEN | n/a | Triggered by `cleargate-planning/.cleargate/scripts/` edits. See **MANIFEST regen flag** below. |

**Re-pinned `run_script.sh` line ranges (post-CR-054, post-CR-056 — verified by Architect at plan-time, file is currently 210 LOC):**

| Anchor | Live file | Canonical file |
| --- | --- | --- |
| `EXIT_CODE=0; "$@" >"$STDOUT_TMP" 2>"$STDERR_TMP" \|\| EXIT_CODE=$?` | `L91-L92` | `L91-L92` |
| Pass-through `cat "$STDOUT_TMP"` + `cat "$STDERR_TMP" >&2` | `L97-L98` | `L97-L98` |
| **Success exit gate** `if [[ $EXIT_CODE -eq 0 ]]; then exit 0; fi` | `L103-L105` | `L103-L105` |
| **Non-zero dispatch point** (where self-repair sibling MUST be called — between L105 success-gate and L107 incident-write comment) | **insert at L106** (blank line) — between success-gate close and `# On failure` banner | **insert at L106** |
| `_truncate_stream()` (CR-054 byte-correct slice) | `L113-L128` | `L113-L128` |
| Incident-file write `cat > "$INCIDENT_FILE" <<JSON` | `L195-L207` | `L195-L207` |
| Final `exit $EXIT_CODE` | `L209` | `L209` |

The self-repair integration MUST happen at L106 (post success-gate, pre `# On failure` banner block at L107). This guarantees: (a) success path (L103–L105) is untouched; (b) the captured `STDOUT_TMP` and `STDERR_TMP` files are still readable from disk for pattern-match dispatch (the trap at L86 only fires on EXIT, not yet); (c) on retry-success, the wrapper proceeds with EXIT_CODE=0-equivalent semantics — Dev decides whether to overwrite EXIT_CODE in-place OR `exit 0` from the sibling. Recommended pattern: sibling exits with the post-retry exit code, parent script reads it via `EXIT_CODE=$?`, then the existing L107+ block writes the incident file (with the new `retry_attempt` field populated). This preserves observability per CR-057 §1 New Logic point 2.

**Sibling contract (if shipped):**
- Inputs (env): `RUN_SCRIPT_NO_REPAIR` (bypass flag, "1" = skip), the retained `STDOUT_TMP` + `STDERR_TMP` paths (sibling reads them), `EXIT_CODE`, original argv (re-exec target).
- Output: writes new exit code to a tmp marker file OR exits with the retried exit code; writes a `retry_attempt: 1` marker the parent reads when constructing the incident JSON.
- Hard caps: max 3 patterns (matched in declared priority order), max 1 retry per pattern, max 60s wall-clock total (single retry × at most 60s sibling-budget).
- Bypass: `[[ "${RUN_SCRIPT_NO_REPAIR:-}" == "1" ]] && exit $EXIT_CODE` first line.
- Pattern-match: case statement on `(command, exit_code, stderr_signature)` triple; signatures are exact substring matches against the captured stderr.

**Acceptance trace (CR-057 §4 CODE-MODE):**

1. Findings report tallies corpus + identifies top 1–3 recurring patterns (each ≥2 occurrences). → Dev writes `.cleargate/sprint-runs/SPRINT-25/script-incident-corpus-analysis.md`.
2. `_run_script_self_repair.sh` (or `.mjs`) implements bounded self-repair: max 3 patterns, max 1 retry each, max 60s. → Sibling contract above.
3. `run_script.sh` calls sibling on non-zero exit; preserves all existing behavior on success. → L106 insertion point; L103–L105 untouched.
4. ScriptIncident interface gains optional `retry_attempt` field; JSDoc updated. → `script-incident.ts` edit.
5. NEW Red test passes N+1 scenarios. → `run-script-self-repair.red.node.test.ts` (real-wrapper invocation pattern per FLASHCARD 2026-05-04 #wrapper #e2e-test-pattern).
6. `RUN_SCRIPT_NO_REPAIR=1` bypass works. → Covered by the +1 scenario.
7. Mirror parity: live = canonical for both `run_script.sh` and `_run_script_self_repair.sh|.mjs`. → DevOps merge step diff-checks both pairs.

**Bounce risks (CODE-MODE):**

- **R1: Sibling fork-bomb via `RUN_SCRIPT_ACTIVE` not propagated.** The wrapper sets `RUN_SCRIPT_ACTIVE=1` (live L89) before invoking the wrapped command. The retry MUST keep `RUN_SCRIPT_ACTIVE=1` exported when re-executing — otherwise on retry the wrapped command, if it itself shells out via `run_script.sh`, would re-enter the capture path. **Dev MUST verify** the sibling preserves `export RUN_SCRIPT_ACTIVE=1` across the retry exec.
- **R2: TRAP cleanup races the sibling.** `trap 'rm -f "$STDOUT_TMP" "$STDERR_TMP"' EXIT` at L86 fires on shell exit. If the sibling spawns a subshell that exits before the parent reads the retry result, the tmp files survive. If the parent's outer script-end fires while the sibling is mid-retry, the trap deletes the live capture files. **Mitigation:** sibling must complete synchronously before the parent's L107+ block runs; do NOT background the sibling.
- **R3: Schema-evolution orphan (FLASHCARD 2026-05-04 #cr-046 #wrapper #breaking-change).** `ScriptIncident.retry_attempt: number` added as optional (`number | undefined`) — older incident JSON files lack the field; `isScriptIncident` MUST treat it as optional. Dev verifies by parsing one of the 3 existing SPRINT-24 incident files through the updated type guard.
- **R4: 60s budget cap not enforced.** "max 60s total wall-clock" needs a `timeout` wrapper or explicit `SECONDS=0; … (( SECONDS < 60 ))` check. Bare `bash` retry without a budget cap can hang indefinitely on the very pattern self-repair is targeting. Dev MUST implement an actual wall-clock guard, not just a code comment claiming the cap.
- **R5: Stderr signature drift across CR-054.** CR-054 changed the stderr-truncation byte-count semantics; signatures captured pre-CR-054 may have char-truncated tails. Dev MUST construct signatures against POST-CR-054 stderr captures (i.e. re-running the failing command in the post-CR-054 wrapper produces the canonical signature for matching). Pre-CR-054 incident JSON entries are usable for tally-frequency only, not for verbatim signature lifting.

**MANIFEST regen flag (CODE-MODE — REQUIRED):**

CR-057 CODE-MODE edits files under `cleargate-planning/.cleargate/scripts/` (canonical mirror of `run_script.sh` + new `_run_script_self_repair.sh|.mjs` sibling). Per FLASHCARD 2026-05-01 #manifest #prebuild, this invalidates `cleargate-planning/MANIFEST.json` SHAs. **DevOps merge step for CR-057-CODE-MODE MUST run `npm run build` and stage the regenerated `cleargate-planning/MANIFEST.json` in the same commit as the source edits.** Same contract as CR-054 in M1. Orchestrator gates DevOps dispatch on `npm run build` exiting clean. The Red test file (`cleargate-cli/test/scripts/`) and the findings report (`.cleargate/sprint-runs/SPRINT-25/`) do NOT trigger MANIFEST regen — only the canonical `cleargate-planning/.cleargate/scripts/` edits do.

---

#### DOCS-MODE branch (high-probability)

**File-surface table (DOCS-MODE):**

| File | Action | LOC | Rationale |
| --- | --- | --- | --- |
| `.cleargate/knowledge/script-incident-corpus-analysis.md` | **NEW** knowledge doc | ~80 | Corpus tally + reasons no self-repair shipped + revisit-trigger threshold. |
| (optional) `.cleargate/delivery/archive/CR-046_*.md` OR `.cleargate/knowledge/cleargate-protocol.md` | EDIT (status-note pointer) | ~3 | Cite CR-057 findings per CR-057 §4 DOCS-MODE acceptance #2. Pick one, not both. |

No code edit. No mirror edit. No test edit. No interface edit.

**Acceptance trace (CR-057 §4 DOCS-MODE):**

1. Findings report at `.cleargate/knowledge/script-incident-corpus-analysis.md` documents:
   - Total incident count: **3** (SPRINT-23: 0; SPRINT-24: 3).
   - Failure-pattern tally with `(command, exit_code, stderr_signature)` triples (≥3 rows — one per incident).
   - Reason for no-self-repair decision: e.g. "no pattern recurred ≥2× excluding synthetic test artefacts (incidents 2 + 3 are intentional test fixtures, not real failures); the sole real failure (incident 1, validate_state.mjs multi-state.json error) is a usage error, not a flaky pattern amenable to retry".
   - Revisit-trigger threshold: e.g. "when corpus exceeds 20 real-failure incidents OR any single (command, exit_code, stderr_signature) triple exceeds 5 occurrences across ≥2 sprints".
2. CR-046 §0.5 Q3 status note added in CR-046's archive file OR `cleargate-protocol.md` cites CR-057 findings. Pick one, document the choice in the findings doc.
3. No code changes shipped. → Dev MUST NOT touch `run_script.sh`, `script-incident.ts`, or the canonical mirror.

**Bounce risks (DOCS-MODE):**

- **R6: Path mismatch — CR §2 says `.cleargate/sprint-runs/SPRINT-25/...` for CODE-MODE findings but `.cleargate/knowledge/...` for DOCS-MODE.** Dev MUST honour the DOCS-MODE path: `.cleargate/knowledge/script-incident-corpus-analysis.md` (NOT under sprint-runs/). Knowledge files persist; sprint-runs artefacts are sprint-scoped.
- **R7: Synthetic-vs-real classification.** Dev's tally MUST distinguish synthetic test artefacts (e.g. `totally_nonexistent_9999.mjs`, `/tmp/run_script_noshim.sh` — both deliberate fixtures) from real failures. Inflating the recurrence count by including synthetic fixtures would force a CODE-MODE branch built on noise. Dev annotates each tally row as `synthetic | real`.
- **R8: Status-note placement under archive-immutability.** CR-046 lives in `.cleargate/delivery/archive/`. The archive-immutability principle says archive files don't get rewritten post-archive. Pick the alternative (cite CR-057 in `cleargate-protocol.md`) to avoid the conflict, OR add the status note to CR-046's body via the explicit "post-archive amendment" comment pattern if the team accepts that. Dev surfaces the choice in the Brief.

**MANIFEST regen flag (DOCS-MODE — NOT REQUIRED):**

DOCS-MODE touches only `.cleargate/knowledge/` (not under `cleargate-planning/`). No canonical-mirror edit. **No MANIFEST regen needed.** This is the only meaningful divergence from M1 in terms of DevOps merge mechanics.

---

#### Branch decision criterion summary

Dev's investigation determines mode. Pre-evidence at plan-time strongly suggests DOCS-MODE:

| Signal | Threshold (CR-057 §0.5 Q1) | Plan-time observation | Verdict |
| --- | --- | --- | --- |
| Total incidents | n/a | 3 (SPRINT-23: 0; SPRINT-24: 3) | thin |
| Real-failure incidents | n/a | 1 (incident 1 only) | very thin |
| Patterns with ≥2 real-failure occurrences | ≥1 required for CODE-MODE | 0 | **fails CODE-MODE bar** |
| Patterns with ≥2 total occurrences (incl. synthetic) | n/a | 1 (`exit_code: 127`, but both synthetic) | excluded |

**Architect recommendation:** pre-size DOCS-MODE in dispatch text + provide both branches' acceptance criteria. Dev MUST run the tally themselves and document the verdict in the findings report — do NOT short-circuit to DOCS-MODE without producing the explicit per-incident classification table.

## Cross-CR coordination notes

None. CR-057 is alone in W2. All cross-CR coupling concerns (mirror parity ordering with CR-054, schema evolution on `script-incident.ts` shared with CR-054's JSDoc edit) were resolved in M1's merge ordering (CR-054 → CR-057). At W2 dispatch time, M1 is on `main` and CR-057 reads the merged state.

## TPV expectations

- **CODE-MODE:** Architect Mode: TPV evaluates the Red test for genuine production-path coverage. The Red test MUST: (a) invoke the real wrapper via `os.tmpdir()` + `spawnSync` (NOT spawnMock) — this is the pattern from FLASHCARD 2026-05-04 #wrapper #e2e-test-pattern, vindicated by CR-052 wrapScript helper; (b) construct N+1 scenarios where N = number of patterns shipped (each pattern × one retry-success scenario) plus 1 bypass scenario (`RUN_SCRIPT_NO_REPAIR=1` set, no retry occurs). TPV expected to PASS — wiring is to a real spawn, signatures are exact substring matches that verify-or-fail. Reuse `cleargate-cli/test/helpers/wrap-script.ts` (CR-052) where the helper applies; do not re-implement the tmp-copy fixture pattern from scratch.
- **DOCS-MODE:** No Red test. TPV pass-through (no test-pattern wiring to validate). Per Sprint Plan §2.5 Soft flag 3, count CR-057-DOCS-MODE in the TPV denominator with caveat ("prose-only, no test surface"), not as a TPV signal contributor. The 6/6 sprint-wide TPV catch-rate denominator stays, but CR-057-DOCS-MODE is not a fair TPV trial.

## Risks & open hooks

- **Top risk (Sprint Plan §3 row 1, "high likelihood"):** CR-057 finds no recurring patterns → DOCS-MODE. Plan-time evidence strongly confirms; orchestrator MUST pre-size dispatch text for DOCS-MODE. Mitigation: both branches' acceptance criteria fully specified above; no Architect re-dispatch needed at branch-decision time.
- **Open hook 1: Sibling format choice (`.sh` vs `.mjs`).** CR-057 §0.5 Q5 says "`.sh` (or `.mjs`)" — Dev decides during CODE-MODE implementation. Recommendation: `.sh` (zero-runtime-dep, matches `run_script.sh` conventions). Only choose `.mjs` if pattern matching needs richer logic than bash case-statements can express — and if so, that is itself a signal the pattern doesn't belong in self-repair (per CR-057 §0.5 Q5 default text).
- **Open hook 2: Status-note placement (DOCS-MODE).** Pick one: (a) edit `CR-046` archive file (archive-immutability concern), or (b) cite CR-057 in `cleargate-protocol.md`. Recommendation: (b) — protocol is the canonical living document; archive files are append-only-to-frontmatter at most. Dev surfaces the choice in their Brief.
- **Open hook 3: 60s wall-clock budget enforcement (CODE-MODE).** Hard cap MUST be implemented, not asserted in a comment. `timeout` (GNU coreutils — not always present on macOS) vs `bash` `SECONDS` arithmetic vs `gtimeout` (homebrew coreutils on macOS). Recommendation: `bash` `SECONDS` arithmetic to stay portable; verify on macOS BSD-bash and Linux bash before merge.
- **Open hook 4: `isScriptIncident` type-guard update (CODE-MODE).** The current type guard at `script-incident.ts:101-116` checks every field strictly. Adding optional `retry_attempt: number` requires the guard to accept `undefined` for that key (i.e. either absent OR `typeof === 'number'`). Dev MUST update the guard alongside the interface; QA verifies via a guard-roundtrip on one of the 3 pre-CR-057 incident JSON files (`retry_attempt` field absent → guard returns true).

## Adjacent Implementations (Reuse — no duplication)

| Story / CR | Module / Export | Path | When to reuse |
| --- | --- | --- | --- |
| CR-052 | `wrapScript` helper, `*.fixture.*` tmp-copy pattern | `cleargate-cli/test/helpers/wrap-script.ts` | CODE-MODE Red test; reuse for the real-wrapper invocation pattern. Do NOT re-implement spawnSync + tmpdir copy. |
| CR-054 | `MAX_STREAM_BYTES`, `TRUNCATION_SUFFIX`, `_truncate_stream` (bash) | `cleargate-cli/src/lib/script-incident.ts:32,38`; `.cleargate/scripts/run_script.sh:113-128` | CODE-MODE pattern-matching: signatures must match POST-CR-054 byte-correct stderr. Read from these constants, do not hard-code 4096 or `head -c` semantics. |
| CR-046 | `ScriptIncident` interface, `isScriptIncident` type guard | `cleargate-cli/src/lib/script-incident.ts:46-95,101-116` | Both modes: incident JSON parsing in the findings tally. CODE-MODE interface extension MUST keep the existing fields untouched; only ADD `retry_attempt?: number`. |

## Flashcards Devs MUST grep before starting

Append-only short list — Dev MUST grep `.cleargate/FLASHCARD.md` for each tag before any code edit:

- `#cr-046 #wrapper #breaking-change` — schema evolution requires production-path test coverage (the trap that orphaned 6 callers under CR-046's first cut).
- `#wrapper #e2e-test-pattern` — copy wrapper into `os.tmpdir()` + `spawnSync` the real wrapper; do NOT use spawnMock for wrapper-interface tests.
- `#mirror #parity` — canonical and live `.cleargate/scripts/` files MUST diff empty per commit; pair edits in the same commit.
- `#manifest #prebuild` — `cleargate-planning/MANIFEST.json` regen REQUIRED in same commit as any `cleargate-planning/` edit (CODE-MODE only; DOCS-MODE skips).
- `#wiki #protocol #mirror` — subagent + script files ship in BOTH canonical (`cleargate-planning/.cleargate/scripts/`) AND live (`.cleargate/scripts/`); post-edit `diff` returns empty or live diverges silently.

---

**Plan complete.** No code in this plan. Dev investigates first, branches per the criterion table, ships per the matching file-surface table.

## Git Log Digest

Git log unavailable for sprint/S-25 (no commits or branch not found).

## Token Ledger Digest

**Total:** 84,112,627 tokens (input: 632 / output: 295,735 / cache_read: 81,863,401)

**Per-agent breakdown:**
- architect: 46,023,125 (12 dispatches)
- developer: 13,386,311 (6 dispatches)
- qa: 24,703,191 (9 dispatches)

**CR-035 two-line split (pre-computed for Reporter §3):**
sprint_work_tokens: 84,112,627
sprint_total_tokens: 84,112,627
reporter_pass_tokens: null


## Flashcard Slice

No flashcard entries found in sprint window [2026-05-05 → 2026-05-16].

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
