# Milestone: M3 — CR-020 Brief-Driven SDLC Plan Phase

## Items: CR-020
## Wave: W3 (sequential — single CR; depends on M1 + M2 closure)

## Order

CR-020 is **one CR, executed by one Developer agent**. Per CR-020 §4.3 DoD line 9: "Commit message format: `feat(EPIC-protocol): CR-020 Brief-driven SDLC — Plan phase + 4-gate model` (single commit; this CR does not decompose into stories)." → **single atomic commit, 16 files**.

Internal edit order inside the commit (Developer should execute top-down so each subsequent edit can cite anchors that already exist in the working tree):

1. **Protocol slim** (`.cleargate/knowledge/cleargate-protocol.md`) — §0 insert at top → §2 rewrite → §3 rewrite → §4 rewrite → §5 rewrite → §6 rewrite. Edit live first; canonical mirror immediately after each section to keep the Edit-tool diffs reviewable.
2. **CLAUDE.md** — replace 4-bullet block with 6-bullet block (CR-020 §3.2.10). Live first, canonical second, byte-identical for the 6-bullet region.
3. **Templates** — six work-item templates × 2 mirrors = 12 files. Add `POST-WRITE BRIEF` block to `<instructions>` per §3.2.7 SECTION-MAP; for `story.md` additionally add §1.4 + §1.5 per §3.2.8; for `proposal.md` reframe to Initiative-only + drop the "CRITICAL PHASE GATE" line per §3.2 first row of the table; for `epic.md` strip "MUST link to approved proposal.md" from `<instructions>` line 3.
4. **Frontmatter `proposal_gate_waiver` audit** — see §Schema-changes below; this is a **no-op verify**, not a removal, in current template state.
5. **MANIFEST regen** — `cleargate-planning/MANIFEST.json` is built by `npm run build` prebuild step; the protocol + 6 of the 7 listed templates have their SHAs change. Developer must re-run the build script that produces MANIFEST.json (see Reuse) and stage the regenerated MANIFEST in the same commit.
6. **Run check suite** — `cleargate doctor`, `cleargate wiki lint`, `node .cleargate/scripts/state-scripts.test.mjs`, `node .cleargate/scripts/test_ratchet.mjs`. Pre-existing failures listed in sprint plan are out of scope.

Single commit. The CR's §2 atomicity ("acts as a Gate Reset" + DoD line 9 "single commit") is unambiguous — recommendation: do **not** split.

## Per-CR blueprint

### CR-020

#### Files to create
- None. (CR is doc-only modify, all 16 surfaces exist.)

#### Files to modify (16 total — 8 mirror pairs)

**Protocol (2):**
- `.cleargate/knowledge/cleargate-protocol.md` — insert §0 (between current line 4 blank line and `## 1. Your Role` at line 7); rewrite §2 (lines 20–55); rewrite §3 (lines 58–82); rewrite §4 (lines 85–122) — drop the "v2 enforcement rule" + "v2 story-file assertion" + "Gate 3 push gate" subsections per §3.2.4 final paragraph; rewrite §5 (lines 124–144); rewrite §6 (lines 148–157).
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` — identical edits. Pre-edit `diff` is empty (verified 2026-05-01); post-edit must remain empty.

**Templates (12):**
- `.cleargate/templates/proposal.md` — reframe `<instructions>` block (lines 1–12) for Initiative-only scope: keep §1/§2/§3 outline, drop the "CRITICAL PHASE GATE: Do NOT generate Epics or Stories…" sentence (line 10), append POST-WRITE BRIEF block per §3.2.7 mapped as Summary←§1.1 / Open Questions←(new §1.4) / Edge Cases←(new §3.3) / Risks←§2.2 / Ambiguity←bottom block.
- `.cleargate/templates/epic.md` — `<instructions>` line 3: strip "(MUST link to approved proposal.md)" parenthetical from the `Context Source` field description; line 14: strip "You MUST base this strictly on an approved: true proposal document." Append POST-WRITE BRIEF block: Summary←§1 / Open Questions←§6 / Edge Cases←§2 OUT-OF-SCOPE + §5 error scenarios / Risks←§3 / Ambiguity←bottom.
- `.cleargate/templates/story.md` — `<instructions>` line 3: strip "(MUST link to approved proposal.md)" from `Context Source`. Insert new sections §1.4 Open Questions + §1.5 Risks **between current line 92 (`### 1.3 Out of Scope` ends) and line 94 (`## 2. The Truth`)** per CR-020 §3.2.8 verbatim. Append POST-WRITE BRIEF block: Summary←§1.1 / Open Questions←§1.4 / Edge Cases←§2.1 error scenarios / Risks←§1.5 / Ambiguity←bottom.
- `.cleargate/templates/CR.md` — drop the "CRITICAL PHASE GATE: Do NOT invoke cleargate_push_item until all impacted downstream Epics/Stories…" sentence at line 11. Append POST-WRITE BRIEF block: Summary←§1 / Open Questions←(new §0.5 — add a stub `## 0.5 Open Questions` heading just below the H1 title at line 50, before §1) / Edge Cases←§2 / Risks←§2 / Ambiguity←bottom.
- `.cleargate/templates/Bug.md` — drop the "CRITICAL PHASE GATE: Do NOT invoke cleargate_push_item until reproduction steps are 100% deterministic…" sentence at line 11. Append POST-WRITE BRIEF block: Summary←§1 / Open Questions←(new §0.5 stub heading just below H1 line 52) / Edge Cases←§2 / Risks←§2 / Ambiguity←bottom.
- `.cleargate/templates/hotfix.md` — currently has **no `<instructions>` block** (verified — file starts at YAML frontmatter line 1). Add a brand-new `<instructions>` block above the frontmatter containing the POST-WRITE BRIEF section: Summary←§1 / Open Questions←(new §0.5 stub heading just below H1 line 37) / Edge Cases←§3 / Risks←§4 / Ambiguity←bottom (note: hotfix template currently has no Ambiguity block — Developer adds one to satisfy SECTION-MAP, or surfaces as an open decision; see §Open decisions below).
- All 6 canonical mirrors — identical edits.

**Brain files (2):**
- `CLAUDE.md` (live, lines 115–121, 4 non-contiguous bullets) — replace lines 115/117/119/121 (the 4 bullet lines) plus the 3 blank-line separators between them (lines 116/118/120) with the 6-bullet block from CR-020 §3.2.10. New block spans bullets 1–6 with one blank line between each = 11 lines (was 7 lines). Net delta: +4 lines.
- `cleargate-planning/CLAUDE.md` (canonical, lines 24–30, same shape) — identical edits to bullets-1–4 region. Live + canonical 6-bullet region byte-identical post-edit. **Pre-existing canonical-only bullets at lines 22, 44, 52, 58–59 ("Halt at gates" extension, "State-aware surface", "Cross-project orchestration", "Guardrails…") remain untouched.** STORY-024-04's "Doc & metadata refresh on close" bullet (live line 145 / canonical line 61) is **out of scope** and must not be deleted.

**MANIFEST regen (1):**
- `cleargate-planning/MANIFEST.json` — auto-regenerated by build prebuild. Tracks 7 of the 8 modified file pairs (every template + the protocol; CLAUDE.md is not tracked). Developer runs the build, stages the resulting MANIFEST.json, includes it in the same commit.

#### Schema changes

**`proposal_gate_waiver` field — no-op in templates.** CR-020 §3.2.9 says "Drop `proposal_gate_waiver` from all templates." `grep -n "proposal_gate_waiver" .cleargate/templates/ cleargate-planning/.cleargate/templates/` returns **zero matches** (verified 2026-05-01). The field never lived in any template — it has only been added ad-hoc to in-flight artifacts (EPIC-024, CR-020 itself, etc.) via the saved-memory pattern. **Developer's task here is verification, not edit.** Acceptance Gherkin scenario #6 ("proposal_gate_waiver field dropped from all templates") passes on no edit.

**Mirror parity invariant.** Every template + protocol edit applied identically to live + canonical. Reuse the `diff -r .cleargate/templates/ cleargate-planning/.cleargate/templates/` recipe pre- and post-edit. Pre-edit diff is currently **non-empty** (4 hits — see Gotchas below); these are pre-existing §-citation drift in `Sprint Plan Template.md` + `sprint_report.md` + `story.md` left over from STORY-024-02's mirror sync gap, all out of CR-020's scope. Post-edit diff for the 6 templates CR-020 touches must collapse to those same pre-existing hits (no new divergence from CR-020).

#### Test scenarios (from CR-020 §4.1 Gherkin, numbered)

1. **§0 Phase Map present** — `cleargate-protocol.md` contains `## 0. The Five Phases` before `## 1. Your Role`; lists exactly five phases (Plan, Prepare, Execute, Observe, Close); each phase mapped to its exit gate (Gate 1 / Gate 2 + Gate 3 / no-gate / no-gate / Gate 4).
2. **"Always Start with a Proposal" mandate retired** — file contains "Always Start with a Brief"; does NOT contain "Always Start with a Proposal"; §3 contains "Initiative is optional" and not "Proposal before everything".
3. **Four numbered gates, no decimals** — §4 contains exactly four gate headings: `Gate 1 — Brief`, `Gate 2 — Sprint Ready`, `Gate 3 — Sprint Execution`, `Gate 4 — Close-Ack`; does NOT contain "Gate 3.5"; Gate 1 prose explicitly states Brief approval implicitly grants the MCP push.
4. **Six work-item templates carry Brief instruction** — each `<instructions>` block contains a `POST-WRITE BRIEF` section + SECTION-MAP for Summary/Open Questions/Edge Cases/Risks/Ambiguity.
5. **Story template gains §1.4 + §1.5** — `templates/story.md` contains `### 1.4 Open Questions` heading and `### 1.5 Risks` heading; both between current §1.3 and §2.
6. **`proposal_gate_waiver` field absent from all templates** — `grep -l "proposal_gate_waiver" .cleargate/templates/ cleargate-planning/.cleargate/templates/` returns zero matches. **Currently passes on no edit; verify.**
7. **MCP-only sync — Linear/Jira mentions removed** — `grep -iE "linear|jira|github issues" cleargate-protocol.md` returns zero matches **in §6 MCP Tools and §5 Delivery Workflow**. (§14 historical context may keep them per CR-020 §4.1 Scenario 7 caveat. **Note**: §2 Front Gate currently contains Linear/Jira at lines 41–42 in Signal Words; this is out of CR-020 §3.2.2 scope per CR's narrow §2 rewrite spec. See Open decisions.)
8. **Mirror parity invariant** — `diff` empty for protocol + 6 templates + CLAUDE.md CLEARGATE-tag-block region. (Sprint Plan Template, sprint_report, story-line-32/120 pre-existing divergence is grandfathered per Gotcha G7.)
9. **No regression** — `cleargate doctor`, `cleargate wiki lint`, `node .cleargate/scripts/state-scripts.test.mjs`, `node .cleargate/scripts/test_ratchet.mjs` exit 0 (modulo pre-existing failures explicitly carved out in sprint plan).
10. **No accidental gate-semantic change at code level** — `cleargate-cli/src/` and `mcp/src/` byte-identical to pre-CR baseline. CR is doc-only; Developer must not edit any code path.
11. **CLAUDE.md retains tier-4 read-order entry** — session-start orientation block still contains numbered list with tier-4 entry referencing `cleargate-enforcement.md`; the 4-bullet block from STORY-024-03 has been replaced by the 6-bullet block from §3.2.10.

#### Reuse (no duplication)

- **Mirror-write recipe.** Use the established `diff <live> <canonical>` empty-check after every paired edit. Pattern documented in FLASHCARD `2026-04-19 #wiki #protocol #mirror`. After all CR-020 edits land: `diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` returns empty.
- **Protocol citation rewrite scope.** STORY-024-02 (M2) already rewrote the §15→§1 / §16→§2 / §17→§3 / §18→§4 / §19→§5 / §20→§6 / §22→§7 / §23→§8 / §24→§9 / §25→§10 / §26→§11 / §27→§12 mapping across all surfaces. Verified via `grep -nE "§(15|16|17|18|19|20|22|23|24|25|26|27)" .cleargate/knowledge/cleargate-protocol.md` returning zero hits in slim protocol body. **Reuse this state — no further citation rewrites needed in M3.** CR-020's new §0/§2/§3/§4/§5/§6 prose must not introduce any `§N` references where N is a moved-to-enforcement number; cite gate names + retained sections (§§1–14, §21) only.
- **Acceptance scenario #10 (no code-level change)** is verified by **reusing the M2 verification result**: STORY-024-02 was doc-only; CR-020 is doc-only; no code path under `cleargate-cli/src/` or `mcp/src/` was touched in M1+M2 except the pre-existing M2 spec drift. Reuse the SPRINT-16 closing baseline as the comparison anchor for the doc-only assertion.
- **MANIFEST regen.** `cleargate-planning/package.json` exposes the build script that runs `generate-changelog-diff.ts` + sha regen for tracked files (per protocol §13.6). Reuse — do not write a custom regenerator.
- **`<instructions>` block syntax.** All 5 of `proposal.md`, `epic.md`, `story.md`, `CR.md`, `Bug.md` open with `<instructions>` … `</instructions>` (XML-style, prose body). The hotfix template currently has **no such block**. CR-020 §3.2.7 SECTION-MAP names hotfix → Add a new `<instructions>` block at file top.

#### Gotchas surfaced from code inspection

- **G1. STORY-024-03's 4 bullets are NOT contiguous.** CLAUDE.md live lines 115/117/119/121 — each bullet is separated by a blank line (lines 116/118/120). CR-020 §3.2.10's 6-bullet replacement block is ALSO blank-separated. Developer's "replace as a contiguous block" must match the **7-line region (lines 115–121)** verbatim and produce an **11-line region** (6 bullets × 1 line + 5 blank separators). Net delta: +4 lines. Same shape applies to canonical at lines 24–30.

- **G2. `proposal_gate_waiver` field never existed in templates.** CR-020 §3.2.9 frames the change as "Drop the field from all templates." Verified: `grep -n "proposal_gate_waiver" .cleargate/templates/*.md cleargate-planning/.cleargate/templates/*.md` = zero matches. The field has only ever been added ad-hoc to in-flight artifacts (EPIC-024, CR-020 itself, etc.). **Developer's task is verification only.** Gherkin scenario #6 passes on no edit. **Do NOT add the field to templates and then remove it.** This was the root cause of confusion in user briefing context — sprint plan / context override premise that "6 templates retain `proposal_gate_waiver`" is incorrect; ground truth supersedes.

- **G3. §0 Phase Map insertion point.** Current `cleargate-protocol.md` line 1 is `# ClearGate Protocol`; line 3 is the "You are operating in a ClearGate-enabled repository…" prose; line 5 is `---` separator; line 7 is `## 1. Your Role`. CR-020 §3.2.1 says insert §0 "between the H1 title and the current §1 Your Role" — Developer must place the new `## 0. The Five Phases` heading after line 5 (the existing `---` separator) and before the current line 7. The §0 prose ends with "Read this section first. Drill into §1–§14 + §21 only as needed for the current task." — verify this hint is consistent with the post-edit numbered-section list (§§1–14 + §21 retained; §§15–20 + §§22–27 already moved to enforcement file in M2).

- **G4. CLAUDE.md mirror divergence is pre-existing — DO NOT reconcile.** Sprint plan §2.3 + §6 grandfathered this. Live CLAUDE.md lacks 4 bullets present in canonical (canonical lines 22 "Halt at gates" extension, 44 "State-aware surface", 52 "Cross-project orchestration", 58–59 "Guardrails…" mini-section). CR-020 §3.2.10's replacement scope is **only** the 4 bullets STORY-024-03 inserted (live 115–121 / canonical 24–30). The 6 replacement bullets must be byte-identical between live and canonical for the edited region. Pre-existing canonical-only bullets remain untouched. Cleanup is tracked as a future CR per sprint plan §6.

- **G5. STORY-024-04's close bullet is NOT in the replacement scope.** Live line 145 "**Doc & metadata refresh on close.**" / canonical line 61. CR-020 §3.2.10 specifies replacement of 4 mid-block bullets, not the end-of-block close bullet. Developer's diff for CLAUDE.md must show: 4 bullets removed (lines 115/117/119/121) + 6 bullets inserted (in same location); the close bullet at line 145 unchanged.

- **G6. CR-020 §3.2.10 verbatim match — relabel "Gate-3-class" → "Gate-4-class".** Old bullet 4 is "**Sprint close is Gate-3-class (CR-019).**". New bullet 6 is "**Sprint close is Gate-4-class (CR-019).**" (per CR-020 §3.2.10 verbatim block). Developer must NOT preserve the "Gate-3-class" wording when porting bullet 4 forward.

- **G7. Pre-existing template mirror divergence (out of CR-020 scope but in same file tree).** `diff -r .cleargate/templates/ cleargate-planning/.cleargate/templates/` returns 4 hit-sets pre-edit:
  - `Sprint Plan Template.md` (4 lines: §24→§9, §25.4→§10.4, §§15–18→§§1–18, §19→§5)
  - `sprint_report.md` (1 line: §§16–17→§§2–17)
  - `story.md` (2 lines: §24→§9, §20→§6)
  These are M2 cleanup misses (citation rewrite landed in live, canonical lagged). **CR-020 does not touch `Sprint Plan Template.md` or `sprint_report.md`** so those divergences must be left alone (out of scope). For `story.md`, CR-020 DOES edit the file (adds §1.4/§1.5 + Brief block); the developer's `story.md` edits must be applied to BOTH live and canonical such that the new edits are byte-identical, but the pre-existing line-32/line-120 divergence must remain (or be opportunistically fixed if the developer judges it inside the §3.2.8 + §3.2.7 edit range — surface as open decision; see Open decisions D2).

- **G8. MANIFEST tracking.** `cleargate-planning/MANIFEST.json` tracks 7 of the 8 modified files: `cleargate-protocol.md`, `Bug.md`, `CR.md`, `epic.md`, `hotfix.md`, `proposal.md`, `Sprint Plan Template.md`, `story.md` (verified via `grep -E '"path".*templates|protocol' MANIFEST.json`). CR-020 modifies 1 protocol + 6 templates (proposal/epic/story/CR/Bug/hotfix) — all 7 tracked. SHA values change. **Developer must regenerate MANIFEST in the same commit** per protocol §13.6 ("MANIFEST.json is built at `npm run build` (prebuild step in `cleargate-cli/package.json`)"). Failure to regen = `cleargate doctor --check-scaffold` will report drift on next session.

- **G9. Linear/Jira mentions at protocol §2 Front Gate (lines 41–42).** CR-020 §3.2.6 narrowly targets §6's "Linear, Jira, or GitHub" sentence. CR-020 §4.1 Gherkin scenario 7 only checks §6 + §5 for zero matches. §2 lines 41–42 ("Pull: 'pull', 'sync', 'what's in Linear/Jira', 'show me the sprint'" / "Push: 'push to Linear', 'create in Jira', 'sync this item'") will remain post-CR. This is consistent with CR-020 spec — these are *user-vocabulary signal words*, not protocol prose. Do not over-rewrite. (Note: `§14.5 Stakeholder-authored proposal flow` line 583 also references the label `cleargate:proposal` and uses "PM-tool taxonomy" — not Linear/Jira/GitHub direct mention; safe.)

- **G10. CR-020 self-reference inside §4 Gate 3.** §3.2.4 verbatim block contains the line "See `cleargate-enforcement.md` §<N> for full enforcement spec; specified by CR-021." The placeholder `§<N>` is intentional — CR-021 (SPRINT-18) will land the enforcement section. Developer should preserve `§<N>` literal text per CR-020 spec; do NOT substitute a real section number. (`cleargate-enforcement.md` post-M2 has 12 sections; CR-021 will likely append §13.)

- **G11. Hotfix template has no Ambiguity gate block.** Lines 1–62 of `hotfix.md` end at §4 Rollback — no closing `## ClearGate Ambiguity Gate` block. CR-020 §3.2.7 SECTION-MAP for hotfix says "Ambiguity ← bottom-of-doc ambiguity gate block." Two options: (a) Developer adds an Ambiguity block to hotfix.md as part of the Brief instruction implementation; (b) Developer adapts the SECTION-MAP for hotfix to skip Ambiguity. **Recommendation: option (a)** — adds 5 lines for parity with other 5 templates. Surface as Open Decision D1 if the orchestrator wants spec-strict (b).

- **G12. Story.md current §1.3 outline.** Verified line 91 `### 1.3 Out of Scope`, line 92 prose, line 93 blank, line 94 `## 2. The Truth`. CR-020 §3.2.8's verbatim block uses exact heading levels `### 1.4 Open Questions` + `### 1.5 Risks` and inserts between §1.3 and §2. Developer must place the new headings at lines 93–112 (inserting ~20 new lines), shifting §2 from line 94 to line 114 (approximately).

- **G13. CR-020 spec self-citation at §3.2.10 bullet 5.** The replacement-block bullet 5 reads "**Sprint Execution Gate (CR-021).**" — this is a forward reference to CR-021 which has not been drafted yet (per sprint plan §3 risk table + CR-020's own note "specified by CR-021"). This is intentional per CR-020 spec; ship it as-is. (CR-021 lands in SPRINT-18, the next sprint.)

- **G14. The protocol §4 Gate 4 prose redefines `--assume-ack` semantics consistent with CLAUDE.md bullet 6.** Sprint plan ADR scan flagged no conflict (§2.5). Developer must ensure the new §4 Gate 4 paragraph + the new CLAUDE.md bullet 6 say the same thing about `--assume-ack` reservation for automated tests. Both specs in CR-020 §3.2.4 + §3.2.10 already align — sanity-check during write.

- **G15. STORY-024-03's tier-4 read-order line in CLAUDE.md is OUT of CR-020 scope.** Live line 107 (canonical line 16): `4. .cleargate/knowledge/cleargate-enforcement.md — hook-enforced rules…`. CR-020 §3.2.10 explicitly states "Keep STORY-024-03's read-order tier 4 (`cleargate-enforcement.md`)." Acceptance scenario #11 verifies tier-4 still present post-CR. Developer's CLAUDE.md edit must NOT touch the read-order list (lines 104–107 live).

## Cross-story risks

None — M3 is a single-CR milestone. The internal cross-surface risks (live/canonical drift, MANIFEST regen, replacement-scope adherence) are captured in Gotchas G1–G15 above. No cross-story coupling to other M3 items because there are no other M3 items.

## Open decisions for orchestrator

**D1. Hotfix template Ambiguity block.** CR-020 §3.2.7 SECTION-MAP requires an Ambiguity binding for hotfix.md, but the current template has no such block. Recommendation: Developer adds a 5-line `## ClearGate Ambiguity Gate` block at end of hotfix.md mirroring the shape used by Bug.md / CR.md (status line + checkbox requirements). This is a +5 LOC addition not strictly enumerated in CR-020 §3.2; surfaces as a borderline scope expansion. **Architect recommendation: approve (option a).** The spec's SECTION-MAP intent is per-template parity; without this addition the hotfix Brief would be incomplete and Gherkin scenario #4 (SECTION-MAP completeness) would fail for hotfix.md.

**D2. story.md pre-existing line-32 + line-120 mirror divergence opportunistic fix.** Current canonical `story.md` carries stale `§9` and `§6` citations where live carries `§24` and `§20` (M2 missed the canonical sync for those two lines specifically — see G7). CR-020 edits story.md substantively (adds §1.4 + §1.5 + POST-WRITE BRIEF). Developer can either: (a) leave the lines 32/120 divergence in canonical untouched (strict scope discipline — out of CR-020 surface area); (b) fix both lines in canonical to match live as part of the same commit (cheap, ~2 chars × 2 lines, reduces post-CR diff noise). **Architect recommendation: option (b).** The fix is byte-trivial, the divergence is M2 sync hygiene that CR-020's edit pass naturally encounters, and §2.3 of the sprint plan grandfathered "edit-parity not state-parity" specifically because reconciling all of CLAUDE.md was scope-heavy — story.md's two-line drift is not. Surface to orchestrator: confirm (b) before Developer dispatches.

**D3. Sprint Plan Template.md + sprint_report.md mirror divergence (out of CR-020 scope).** Pre-existing M2 sync misses (G7). CR-020 does not touch these files. **Architect recommendation: leave alone.** Track as a follow-up cleanup CR alongside the CLAUDE.md mirror reconciliation noted in sprint plan §6. Surface for visibility only — no decision needed for M3 dispatch.

**D4. CR-020 §3.2.4 placeholder `§<N>` in Gate 3 prose.** Verbatim spec preserves `§<N>` as a literal forward-reference to CR-021's enforcement section. **Architect recommendation: ship as-is** (per G10). Not a real ambiguity — surface for visibility so orchestrator does not let Developer "fix" the placeholder.

---

## Architect FLASHCARD candidates (deferred — record post-execution)

- `2026-05-01 · #templates #proposal_gate_waiver · proposal_gate_waiver field never lived in templates — only in in-flight artifacts. CR-020 spec "drop from templates" is a no-op verify, not an edit.`
- `2026-05-01 · #claude-md #mirror · CLAUDE.md live↔canonical pre-divergent by 4 bullets (canonical-only) since pre-EPIC-024. Edit-parity invariant applies per-edit, not whole-file.`
- `2026-05-01 · #protocol #templates · MANIFEST.json prebuild script must run after every protocol/template edit; SHAs change → cleargate doctor flags drift if manifest stale.`

(Architect will record after Developer commits + QA approves — confirms the lessons survived contact with execution.)
