# Milestone: M2 — Protocol Split + Full Citation Rewrite
## Stories: STORY-024-02
## Wave: W2 (sequential after W1)

> Plan length is scope-driven (post-1700494 architect template). Slim shape: no per-story Files-to-create / Files-to-modify subsections — Developer reads STORY-024-02 §3 directly. This plan covers Order, Cross-story coupling, Test scenarios, Reuse, Gotchas (file:line). Architect (this) verified mirror parity, citation surface, and current `architect.md` line numbers as of 2026-05-01 post-W1 merge.

## Order

Strict serial within the story — no parallelism inside M2 (single story).

1. **Phase 1 — Create `cleargate-enforcement.md` (live + canonical mirror)** before anything else. Phase 2 deletes the source-of-truth bytes; Phase 1 must finish first or the content is lost.
2. **Phase 2 — Slim `cleargate-protocol.md` (live + canonical mirror).** Removes §§15-20 + §§22-27 entirely. No stub redirects.
3. **Phase 3 — Citation rewrite across all surfaces.** Runs after Phase 2 so the grep target (the slim file) and the rewrite target (`cleargate-enforcement.md` filename) are both stable.
4. **Phase 4 (folded in per orchestrator decision 4 from M1) — `architect.md` line-cap rewrite.** Replace stale "Small plans" line at architect.md:147 (live + canonical) with a slim-template-friendly statement. Co-located with Phase 3 because the same Developer touches `architect.md` for the Lane Routing citation rewrite at line 142 anyway — single-pass diff on that file.

All four phases land in **one atomic commit** per story §1.2 spec ("The repo MUST NOT contain a state where the protocol is slim but citations still point at moved §§ in `cleargate-protocol.md`"). Phase 4 is part of the same commit; commit message: `feat(EPIC-024): STORY-024-02 Protocol split + full citation rewrite`.

## Per-story blueprint

### STORY-024-02

#### Cross-story coupling

- **Wave 3 (CR-020) consumes the slim protocol.** CR-020 §3.2.1 inserts new §0 Phase Map at top of `cleargate-protocol.md` *after* this story slims it. CR-020 also rewrites §§2/3/4/5/6 inline. The unmerged CR-020 file in `pending-sync/` is itself in this story's citation-rewrite scope (sprint plan §2.3 final bullet).
- **Sprint plan §5a mid-sprint gate is binding.** After this story commits, orchestrator MUST run `cleargate gate check .cleargate/delivery/pending-sync/CR-020_Brief_Driven_SDLC_Plan_Phase.md` before dispatching Wave 3. Architect (this plan) confirms: CR-020 currently has **zero `§(15|16|17|18|19|20|22|23|24|25|26|27)` matches** (verified 2026-05-01 by `grep -nE '§(15|16|17|18|19|20|22|23|24|25|26|27)\b' CR-020*.md` → empty). CR-020 cites `cleargate-protocol.md` only for §0/§2/§3/§4/§5/§6 (all retained sections). **No CR-020 body edits required by this story** beyond a defensive re-grep at execution time. The §5a gate exists as a safety net, not a known-failing reconciliation.
- **W1 stories already shipped** (1700494, d2ea8fd, a892f4f). The `architect.md` Workflow step 4 reform from STORY-024-01 is in effect and authorizes this very plan's slim shape. The `CLAUDE.md` tier-4 read-order line from STORY-024-03 already names `cleargate-enforcement.md` (verified at line 6 of the canonical CLEARGATE block) — this story makes the named file actually exist.

#### Schema changes

None. No frontmatter delta, no DB migration, no template field change. Pure documentation refactor.

#### Test scenarios (from §2.1 Gherkin — Developer must cover all 7)

1. **Enforcement file shape.** `cleargate-enforcement.md` exists at live + canonical paths. Top-of-file index table maps new §1..§12 → source §15..§27 → title. Every section heading carries a `(source: protocol §<old>)` annotation. Bodies byte-identical modulo internal cross-reference renumbering.
2. **Protocol slim.** `grep -E '^## (15|16|17|18|19|20|22|23|24|25|26|27)\b' .cleargate/knowledge/cleargate-protocol.md` returns zero. `wc -l` reports ≤500 (was 1088). No stub redirects.
3. **Citation rewrite complete.** `grep -rE '§(15|16|17|18|19|20|22|23|24|25|26|27)\b' .cleargate/delivery/ .claude/agents/ cleargate-planning/ CLAUDE.md .cleargate/wiki/ | grep -i 'cleargate-protocol'` returns zero matches. Every prior moved-§ citation now points at `cleargate-enforcement.md` with the new §-number per the mapping table.
4. **Archive audit-trail preserved.** For three random sample files under `.cleargate/delivery/archive/` that previously carried citations: `git diff <pre-merge-sha> HEAD -- F` shows ONLY §-substitution lines and/or `cleargate-protocol.md` → `cleargate-enforcement.md` filename swaps. Zero YAML frontmatter diff hunks. Zero non-citation body changes.
5. **Mirror parity preserved.** Empty `diff` between `.cleargate/knowledge/cleargate-protocol.md` and `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`. Empty `diff` for the new `cleargate-enforcement.md` pair. Empty `diff` between `.claude/agents/architect.md` and `cleargate-planning/.claude/agents/architect.md` (Phase 4 edits both identically).
6. **§3.1 self-amendment justified.** Commit message contains the literal phrase `§3.1 self-amended` and references `protocol §20.1 option 2`. The §3.1 table inside the committed STORY-024-02 file lists the concrete grep-derived citation-rewrite file paths.
7. **No regression.** `cleargate doctor`, `cleargate wiki lint`, `node .cleargate/scripts/state-scripts.test.mjs`, `node .cleargate/scripts/test_ratchet.mjs` all exit 0.

Implicit corollary (Architect addendum, not in story Gherkin): **architect.md line 147 line-cap rewrite passes byte parity.** After Phase 4: `diff .claude/agents/architect.md cleargate-planning/.claude/agents/architect.md` returns empty AND line 147 no longer reads "Small plans. A 200-line plan is a bad plan." Verify by `grep -n "Small plans\|200-line\|60-120" .claude/agents/architect.md cleargate-planning/.claude/agents/architect.md` → zero hits in both.

#### Reuse (no duplication)

- **Citation grep recipe (canonical):** `grep -rnE '§(15|16|17|18|19|20|22|23|24|25|26|27)\b' .cleargate/delivery/ .claude/agents/ cleargate-planning/ CLAUDE.md .cleargate/wiki/`. Architect verified this returns **127 hits** as of 2026-05-01 (story spec quoted ~92 — the count drifted upward via SPRINT-16 + SPRINT-17 in-flight items; story §1.2 phrasing of "~92" is approximate, not load-bearing). The 127 hits include the 16 archive files + 9 pending-sync files + agents + wiki.
- **Mirror-write pattern.** After writing live, `cp .cleargate/knowledge/cleargate-enforcement.md cleargate-planning/.cleargate/knowledge/cleargate-enforcement.md` (and same for the slim protocol, and same for `architect.md`). Both files in each pair must be byte-identical. **Diff verification before commit:** the four mirror diffs (protocol × 2, enforcement × 2, architect.md × 2, CLAUDE.md tag-block × 2 if touched) must all be empty.
- **Archive-immutability verification recipe.** `git diff HEAD~ HEAD -- .cleargate/delivery/archive/<F>` for each archive file in scope must show only `^[-+].*§(15|16...|27)` substitution lines or `^[-+].*cleargate-(protocol|enforcement)\.md` filename-swap lines. **Forbidden in any archive diff:** `^[-+]updated_at:`, `^[-+]last_stamp:`, `^[-+]last_synced_body_sha:`, or any non-citation body change. Implementing script (Phase 3 step 1, story §3.2 Phase 3) MUST refuse to touch any line beginning with `^[a-z_]+:` in archive paths — that's the frontmatter detector.
- **MANIFEST entry pattern.** New `cleargate-enforcement.md` is protocol-tier; clone the existing entry shape from `cleargate-planning/MANIFEST.json:138-144` (the `cleargate-protocol.md` block). Insert sibling block immediately after `cleargate-protocol.md` (before `readiness-gates.md` at line 145). Required fields: `path`, `sha256` (computed post-write), `tier: "protocol"`, `overwrite_policy: "merge-3way"`, `preserve_on_uninstall: false`. The MANIFEST is in `cleargate-planning/` only — there is no live `MANIFEST.json` to mirror to.
- **§-mapping table (canonical, story §3.2 Phase 1 step 2):** `{15→1, 16→2, 17→3, 18→4, 19→5, 20→6, 22→7, 23→8, 24→9, 25→10, 26→11, 27→12}`. This drives both Phase 1 renumber and Phase 3 substitution. Subsection numbers carry the section prefix: `§15.1` → `§1.1`, `§18.2` → `§4.2`, `§20.1` → `§6.1`, etc.

#### Gotchas surfaced from code inspection (file:line)

- **`architect.md:147` (live AND canonical) carries a stale "Small plans" line that contradicts `architect.md:20`'s new "Plan length is scope-driven" statement.** Verified 2026-05-01:
  - Live: `/Users/ssuladze/Documents/Dev/ClearGate/.claude/agents/architect.md:147` reads `- **Small plans.** A 200-line plan is a bad plan. Target 60-120 lines per milestone. If a milestone needs more, it's over-scoped — flag that.`
  - Canonical: same text at `cleargate-planning/.claude/agents/architect.md:147`.
  STORY-024-01 inserted line 20 ("Plan length is scope-driven — there is no line cap. The reform from EPIC-024 is to drop §3.1 duplication, not to compress.") but did NOT delete line 147. **Phase 4 action:** in both files, replace line 147's bullet with: `- **Plan length is scope-driven.** No line cap. The reform from EPIC-024 was to drop §3.1 duplication, not to compress.` Same diff applied identically to both mirrors.

- **`architect.md:142` cites `protocol §24 "Lane Routing"` — must rewrite to `cleargate-enforcement.md §9 "Lane Routing"`.** Verified 2026-05-01:
  - Live: `.claude/agents/architect.md:142` reads `Full rubric, demotion mechanics, and forbidden-surface table are in protocol §24 "Lane Routing". These rules apply under \`execution_mode: v2\`.`
  - Canonical: same text at line 142 of canonical mirror.
  Per mapping `24 → 9`. **Phase 3 action:** rewrite to `Full rubric, demotion mechanics, and forbidden-surface table are in \`cleargate-enforcement.md\` §9 "Lane Routing". These rules apply under \`execution_mode: v2\`.` Apply identically to both mirrors. Same-file commit as Phase 4 — single editing pass on `architect.md`.

- **`architect.md:116` carries §-numbers that match the regex but are NOT moved-§ citations — false-positive risk.** The line reads: `After SPRINT-10 ships, \`max = 20\`. A story drafted before SPRINT-10 might cite \`§10\` ... The plan must use \`§21\` (next free after \`§20\`) and note: _"STORY text cites §10 — stale, rewritten to §21"_.` Both `§20` and `§21` appear in the regex but they are **illustrative prose for the Numbering Resolver**, not citations to the moved-§ contents. **Action: skip these matches.** The implementing rewrite script (story §3.2 Phase 3 step 1) checks "whether the citation refers to `cleargate-protocol.md` (by name OR by implication)" — these illustrative §s do not refer to a specific protocol section's content; they are pedagogical examples. Same false-positive applies in the same line at the canonical mirror.

- **`STORY-022-01.md:16` (under `.cleargate/wiki/stories/`) carries one moved-§ citation: `protocol §24`.** Verified 2026-05-01: the only wiki hit. The wiki is auto-rebuilt from raw items per `cleargate wiki build`. **Decision:** rewrite the wiki page in this commit (don't rely on auto-rebuild — the source raw item under `.cleargate/delivery/archive/STORY-022-01*` also needs the rewrite, and rebuild would re-emit a stale citation if the raw item isn't fixed first). Cleanest: rewrite the raw archive item AND the wiki page in this commit. The archive-immutability carve-out covers raw archive items; the wiki file is not under archive/ — full edit allowed.

- **Internal cross-references inside moved sections must renumber.** Architect read the protocol at lines 783, 797, 852, 875 — concrete examples of moved-§ → moved-§ cross-refs that need rewriting in the new file:
  - `cleargate-protocol.md:783` (inside §18.3): "story N+1 until the §18.2 processing loop is complete" → in `cleargate-enforcement.md` becomes "until the §4.2 processing loop is complete".
  - `cleargate-protocol.md:797` (inside §18.5): "the orchestrator applies the §18.2 processing loop manually" → "the §4.2 processing loop manually".
  - `cleargate-protocol.md:852` (inside §19.2): "until the Architect completes a Sprint Design Review (§15.1)" → "Sprint Design Review (§1.1)".
  - `cleargate-protocol.md:875` (inside §19.5): same `§15.1` → `§1.1`.
  Plus the subsection-heading numbers themselves: every `### §15.X` → `### §1.X`, every `### §16.X` → `### §2.X`, etc., across all 12 moved sections. Architect spot-counted: §15 has 5 subsections, §16 has 4, §17 has 3, §18 has 6, §19 has 5, §20 has 3+ (line 899 `### §20.3 §3.1 table contract` — note this subsection title contains a literal `§3.1` referring to the story-template contract, which stays unchanged). Developer must grep `^### §(15|16|17|18|19|20|22|23|24|25|26|27)\.\d+` inside the extracted bodies and apply the prefix renumber.

- **`### §20.3 §3.1 table contract` (cleargate-protocol.md:899) is a parsing trap.** The subsection HEADING contains both `§20.3` (its own number — must renumber to `§6.3`) AND `§3.1` (a reference to the **story template's** §3.1 file-surface table, which is in a different file entirely and **stays unchanged**). The implementing script must NOT blindly renumber every `§N.M` it sees inside moved-section bodies — it must distinguish (a) the subsection's own heading number from (b) cross-references to the same-file protocol § from (c) references to story-template §3.1 (which is in `.cleargate/templates/story.md`, not the protocol). Cleanest implementation: only renumber `§(15|16|17|18|19|20|22|23|24|25|26|27)(\.\d+)?` matches; leave any other `§N` token alone.

- **Pre-existing mirror parity is currently CLEAN for both knowledge files.** Verified 2026-05-01: `diff .cleargate/knowledge/cleargate-protocol.md cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` returns empty (both files 1088 lines, byte-identical). No scope-narrowing decision needed for this story. (Sprint plan §2.3's pre-existing CLAUDE.md CLEARGATE-block divergence applies to a different surface and is out of scope here.)

- **`cleargate-planning/MANIFEST.json:138-144` declares `cleargate-protocol.md` as protocol-tier.** Adding `cleargate-enforcement.md` to MANIFEST is **REQUIRED** for the canonical scaffold install path — without it, `cleargate init` would not propagate the new file when a downstream user runs the installer. Insert sibling entry immediately after line 144 (before `readiness-gates.md` at line 145). The `sha256` field must be computed against the canonical file's bytes post-write. **MANIFEST is in `cleargate-planning/` only** — no live mirror; this is a single-file edit. **Lane note:** `cleargate-planning/MANIFEST.json` is on the architect.md:132 forbidden-surfaces list for fast-lane purposes; this story is `lane: standard` so the prefix does not block. Surface this so QA understands the touch is intentional and within scope.

- **§11.4 archive-immutability is a one-time carve-out — do NOT run `cleargate stamp` on archived files.** A normal "stamp the archive" run would touch ~50 archived files for citation reasons that are inherently retroactive. The Developer's implementing script (story §3.2 Phase 3 step 1) MUST refuse stamp/timestamp side-effects on `.cleargate/delivery/archive/` paths and MUST refuse to write any line beginning with `^[a-z_]+:` (frontmatter field detector) in archive paths. Verification recipe is in `Reuse` above.

- **Self-amending §3.1 surface (per protocol §20.1 option 2 — which becomes `cleargate-enforcement.md §6.1` after this story).** The citation-rewrite file list cannot be enumerated ahead of time (story draft date 2026-04-30 said "~92"; current count is 127 due to in-flight items). Developer captures the deduped sorted list via grep and appends to STORY-024-02's §3.1 table in the same commit. **Catch:** the §20.1 reference inside the commit message itself (story §1.2 spec line: "self-amending §3.1 surface (protocol §20.1, option 2)") — the commit message and the story §1.2 prose currently say "protocol §20.1". After this story merges, `§20` no longer exists in the protocol — the rule has moved to `cleargate-enforcement.md §6.1`. **Decision:** the commit message preserves the historical citation as written in the story spec (`protocol §20.1 option 2`) for git-blame continuity per story §2.1 scenario 6 ("commit message contains 'protocol §20.1'"). The story file's §1.2 / §3.1 prose stays as-is (it's the spec, frozen at story draft time). Do NOT rewrite the commit message's §20.1 reference. **However**, any forward-looking prose elsewhere (e.g. STORY-024-02's body where it cites the rule for future readers) should be left at `§20.1` since the story file itself is in `pending-sync/` (will move to archive on close) and rewriting would alter the spec text. Architect ruling: **the citation rewrite leaves STORY-024-02.md's body §-references alone except where they cite a different moved §; the script's auto-rewrite IS run against pending-sync/STORY-024-02*.md but a guard is needed for the literal phrase `protocol §20.1` in the §1.2 self-amend justification — preserve verbatim.** Practical implementation: the script's substitution rule "skip matches inside `cleargate-enforcement.md` itself" extends to "skip matches inside the spec phrasing 'protocol §20.1' / 'protocol §20.1 option 2' that the spec itself enshrines as a verbatim commit-message string". Document this carve-out in the §3.1 self-amendment justification.

- **Pre-existing test failures remain pre-existing.** STORY-024-04 QA flagged 9 unrelated pre-existing test failures (hotfix-new ×2, test_close_sprint_v21 ×6, close-sprint-reconcile ×1). They were not caused by W1 work and will not be caused by this story. **Do not block on them.** The §2.1 scenario 7 ("No regression") evaluates against the pre-W1 baseline — if `state-scripts.test.mjs` and `test_ratchet.mjs` exit 0 (those are the named-in-spec tests), regression is satisfied. The other 9 failures are tracked outside SPRINT-17.

- **CR-020 file in pending-sync/ has zero `§(moved)` citations as of 2026-05-01.** Verified by `grep -nE '§(15|16|17|18|19|20|22|23|24|25|26|27)\b' .cleargate/delivery/pending-sync/CR-020*.md` → empty. CR-020 cites `cleargate-protocol.md` only for §0/§2/§3/§4/§5/§6 (all retained). The implementing script will scan CR-020 and emit zero substitutions. **The §5a mid-sprint gate is a defensive check, not a known reconciliation.** Surface this so the orchestrator understands the gate is expected to pass on first run.

- **Citation-surface false-positive in this very plan.** This M2 plan file (under `.cleargate/sprint-runs/SPRINT-17/plans/M2.md`) contains many `§(15|16|17|18|19|20|22|23|24|25|26|27)` matches in prose for documentation purposes (mapping table examples, gotcha citations). The story's grep target excludes `.cleargate/sprint-runs/**` per story §1.3 ("Touching `.cleargate/sprint-runs/**` — those are runtime artifacts, not citation surfaces") — so this plan is automatically out of scope. Verified: story §3.1 root-prefix list does NOT include `.cleargate/sprint-runs/`. No action needed; the script naturally skips this directory.

## Cross-story risks

None — single story milestone. The Wave-3 (CR-020) handoff risk is captured in §5a of the sprint plan and tracked as the orchestrator's mid-sprint gate, not as an in-milestone risk.

## Open decisions for orchestrator

1. **Phase 4 is folded in (orchestrator decision 4 from M1) — confirmed by the Architect.** No open decision; proceeding as a fourth phase in the same atomic commit. If the orchestrator prefers to ship Phase 4 as a separate trailing commit on the same branch (to keep the citation-rewrite diff visually clean), that's an implementation detail — Architect's recommendation is single-commit per story §1.2 atomicity rule, but a two-commit split is acceptable if the orchestrator wants it. **Recommendation: single commit.**

2. **CR-020 gate-check tooling.** Sprint plan §5a says `cleargate gate check <CR-020 file path>`. Architect did not verify the exact CLI invocation works against a `pending-sync/` CR file (vs an archived file or a sprint plan file). If `cleargate gate check` rejects the CR file, fall back to `node .cleargate/scripts/run_gates.mjs` or whichever script the entry-gate sweep used to pass CR-020 in `cached_gate_result.last_gate_check: 2026-05-01T06:05:18Z`. **Recommendation: orchestrator runs whatever was used during the 2026-05-01 entry-gate sweep.**

3. **Wiki rebuild post-merge.** Story §1.3 says "the wiki rebuild hook regenerates them after the citation-source files merge." The single wiki hit at `STORY-022-01.md:16` is being rewritten in-commit per the gotcha above. Architect's assumption: the post-commit wiki rebuild reads from raw archive items (also rewritten in-commit) and produces a stale-free wiki. **Open question:** does the rebuild hook fire automatically on post-commit, or does the orchestrator need to invoke `cleargate wiki build` manually after Wave 2 commit lands? **Recommendation: orchestrator runs `cleargate wiki build` once after the commit to be safe; if the auto-rebuild fires, the manual run is a no-op.**

---

**Plan-file accounting:** Architect read the following ground-truth sources to produce this plan:
- `.cleargate/delivery/pending-sync/SPRINT-17_Plan_Phase_Delivery.md` (sprint plan + populated §2 SDR)
- `.cleargate/delivery/pending-sync/EPIC-024_AI_Orientation_Surface_Slim.md` (parent epic)
- `.cleargate/delivery/pending-sync/STORY-024-02_Protocol_Split_And_Citation_Rewrite.md` (the story)
- `.cleargate/knowledge/cleargate-protocol.md` (lines 1-1088, full file inspected for section boundaries + internal cross-refs at lines 617-925, 956-1088)
- `.claude/agents/architect.md` (full 156 lines, both live and canonical mirror)
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` (mirror parity confirmed clean)
- `cleargate-planning/MANIFEST.json` (lines 130-165, protocol-tier entry shape captured)
- `cleargate-planning/CLAUDE.md` (CLEARGATE-block, tier-4 line confirmed at line 6)
- `.cleargate/FLASHCARD.md` (greps for `#protocol`, `#mirror`, `#section-numbering` — three relevant cards surfaced)
- Citation-surface greps across `.cleargate/delivery/`, `.claude/agents/`, `cleargate-planning/.claude/agents/`, `CLAUDE.md`, `cleargate-planning/CLAUDE.md`, `.cleargate/wiki/` (127 hits, distribution noted in `Reuse`)
