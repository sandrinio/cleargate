# Milestone: M1 — EPIC-024 Wave 1 (parallel surfaces)
## Stories: STORY-024-01, STORY-024-03, STORY-024-04
## Wave: W1 (parallel — three disjoint surfaces)

role: architect

> Plan template note: this milestone plan still uses the pre-EPIC-024 Architect plan shape (per-story `Files to create:` / `Files to modify:` retained). STORY-024-01 is what *retires* §3.1 duplication going forward — once it merges, M2 onward must follow the slim template. This M1 plan deliberately uses the old shape to stay readable today.

## Order

All three stories are **parallel-eligible** (sprint plan §2.1). Disjoint file regions:

- **STORY-024-01** — `.claude/agents/architect.md` Workflow step 4 only (lines 18–42 — the markdown-fenced template block + the line directly above it).
- **STORY-024-03** — `CLAUDE.md` CLEARGATE-tag-block only — two distinct regions: (a) session-start read-order list (live lines 103–106) gets a tier-4 line appended; (b) 4-bullet block inserted between "Halt at gates" (live line 112) and "Drafting work items" (live line 114).
- **STORY-024-04** — TWO new files (`.cleargate/knowledge/sprint-closeout-checklist.md` + `.cleargate/scripts/prep_doc_refresh.mjs`) plus one CLAUDE.md edit at the *end* of the CLEARGATE-tag-block (after the "Project overrides" / "Scope reminder" bullets — explicitly NOT in 024-03's insertion range).

**Recommended dispatch sequence (minimises rebase drift, NOT a hard ordering):**

1. Dispatch STORY-024-01 first — it touches no shared file.
2. Dispatch STORY-024-04 second — it creates two new files (zero collision risk) and edits a CLAUDE.md region disjoint from 024-03's.
3. Dispatch STORY-024-03 third (or in parallel with 024-04 if the orchestrator can spawn concurrently). 024-03 and 024-04 both edit `CLAUDE.md` + `cleargate-planning/CLAUDE.md` but at non-overlapping line ranges. If they land in parallel, the second commit will rebase cleanly because the regions are >15 lines apart.

The three stories MAY land in any order. The above is a tie-breaker if the orchestrator wants strict sequencing.

## Per-story blueprint

### STORY-024-01 — Architect Plan Slim

**Cross-story coupling:** None inside Wave 1. Wave 2 (STORY-024-02, M2) will re-edit the same file at line ~140 (the `§24 "Lane Routing"` citation in the Lane Classification section). Disjoint line range from this story's edit; mentioned for the M2 plan, not relevant here.

**Files to create:** None.

**Files to modify:**
- `/Users/ssuladze/Documents/Dev/ClearGate/.claude/agents/architect.md` — replace lines 20–42 (the markdown code-fence block in Workflow step 4) with the new template per Story §3.2 step 2; insert the one-line scope-driven note between line 18 ("Produce the plan with this structure:") and the opening fence.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/agents/architect.md` — byte-identical edit (canonical mirror).

**Schema changes:** None.

**Test scenarios (from §2.1 Gherkin):**
1. `grep "^- Files to create:"` against architect.md returns 0 hits inside Workflow step 4 fence; same for `^- Files to modify:`.
2. The fence retains `Cross-story coupling:`, `Test scenarios`, `Reuse`, `Gotchas`, `Schema changes` per-story; milestone level retains `Order`, `Cross-story risks`, `Open decisions`.
3. `diff .claude/agents/architect.md cleargate-planning/.claude/agents/architect.md` is empty.
4. `git diff <pre-merge-sha> HEAD -- .claude/agents/architect.md` shows zero hunks outside Workflow step 4 region or the one-line note immediately above it.

**Reuse (no duplication):**
- Pre-edit mirror is currently parity-clean (`diff` returns empty; verified 2026-05-01 — see Gotcha A1 below).
- The literal new template body is fully spelled out in STORY-024-01 §3.2 step 2 (lines 143–165 of the story file). Developer copies that block verbatim into the fence; do not paraphrase.
- The scope-driven note text is given verbatim in story §1.2 ("Plan length is scope-driven — there is no line cap. The reform from EPIC-024 is to drop §3.1 duplication, not to compress.") and §3.2 step 3.

**Gotchas surfaced from code inspection:**
- **A1 — pre-edit mirror parity confirmed** at `.claude/agents/architect.md` ↔ `cleargate-planning/.claude/agents/architect.md`: `diff` empty (verified 2026-05-01 by Architect SDR). MANIFEST tracks `.claude/agents/architect.md` at sha256 `297020d3344d9122c0ff95e00b436cb83640acbc78377684c04f6af8fb6fbcc4` (`cleargate-planning/MANIFEST.json` lines around the entry). After the edit lands, MANIFEST regeneration is **out of scope** — `cleargate doctor` checks the canonical hash drift, not the live; only `cleargate-planning/.claude/agents/architect.md` actually mutates the canonical-tracked sha. If the edit lands without re-running MANIFEST regen, doctor will fail. Cite: `cleargate-planning/MANIFEST.json` entry for `.claude/agents/architect.md`. **Action:** after both files mutate, regenerate `cleargate-planning/MANIFEST.json` (`cleargate doctor --fix-manifest` or equivalent) — see Open decision 1.
- **A2 — the fence's exact line range (20–42) is one-indexed and inclusive of the closing ` ``` ` line** (architect.md:42 is the closing fence; line 43 is blank, line 44 starts step 5 "Record flashcards…"). The replacement keeps lines 20 (opening fence) and the closing fence at the new last line. Story §1.2 and §3.2 say "lines 20–42 currently"; that matches the live file.
- **A3 — the mirror canonical and the live both stamp the entire fence inclusive of headings.** Don't half-replace (e.g., keep the `# Milestone:` and `## Stories:` headings); the new template body in story §3.2 step 2 starts from `# Milestone: <name>` and replaces the whole block.
- **A4 — the architect.md `## Guardrails` section at line 145 contains a stale guidance "Small plans. A 200-line plan is a bad plan. Target 60-120 lines per milestone."** This contradicts EPIC-024 §6 Q4 ("no cap"). Story §1.3 explicitly puts this OUT of scope ("Changing the Architect's agent definition outside Workflow step 4"). Developer MUST NOT touch line 145. The contradiction is intentional one-sprint debt — flag in §"Open decisions" below; resolution belongs in a follow-up story or in the post-EPIC-024 cleanup pass.

---

### STORY-024-03 — CLAUDE.md Gap-Fill (4 implicit rules + tier-4 read-order)

**Cross-story coupling:**
- Shares `CLAUDE.md` (live + canonical) with **STORY-024-04**. Different line ranges: 024-03 edits the upper half of the CLEARGATE-block (read-order list at lines 103–106 + bullet insertion between lines 112 and 114). 024-04 edits the lower half (after the "Scope reminder" bullet at line 134, immediately before `<!-- CLEARGATE:END -->` at line 135). No line overlap.
- Bullet 4 cites `--assume-ack` literally — that is the same flag-name discipline that CR-019 already shipped in the upper-CLAUDE.md "Guardrails for the conversational agent (me)" section (line 96 of live CLAUDE.md). The two locations now both surface the rule (upper section keeps the existing line, CLEARGATE-block adds the new bullet). Both are intentionally retained — the upper section is conversational-orchestrator-specific and may be removed later; the new bullet is the canonical scaffold-shipped rule.
- The new tier-4 line names `cleargate-enforcement.md`, a file that does NOT exist yet — STORY-024-02 (Wave 2, M2) creates it. This is acceptable per story §1.2: "024-03 ships the name promise, 024-02 fulfils it." Sprint plan §2.1 explicitly approved this.

**Files to create:** None.

**Files to modify:**
- `/Users/ssuladze/Documents/Dev/ClearGate/CLAUDE.md` — within the CLEARGATE-tag-block (lines 98–135):
  - Append a new tier-4 list entry after live line 106 (the existing `3. .cleargate/FLASHCARD.md` entry).
  - Insert the 4-bullet block between live line 112 (the "Halt at gates" bullet) and live line 114 (the "Drafting work items" bullet). Use a leading blank line for readability per story §1.2.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/CLAUDE.md` — within its CLEARGATE-tag-block (lines 7–51 of canonical; landmarks at canonical line 6 "Session-start orientation" and canonical line 15 "Halt at gates"). Apply the byte-identical edit. **Scope discipline:** modify ONLY the four new bullets and the new tier-4 line. The pre-existing canonical-only bullets at lines 29 ("State-aware surface"), 37 ("Cross-project orchestration"), and the "Readiness gates advisory-by-default" extension on line 15's "Halt at gates" bullet remain untouched (per sprint plan §2.3 mirror-parity scope narrowing decision).

**Schema changes:** None.

**Test scenarios (from §2.1 Gherkin — 8 total):**
1. CLEARGATE-block contains a bullet beginning `**Sprint mode.**` mentioning `execution_mode`, `v1`, `v2`.
2. CLEARGATE-block contains a bullet beginning `**Architect runs twice per sprint.**` mentioning "Sprint Design Review", "per-milestone" (or "Per-milestone plan"), and the literal `plans/M<N>.md`.
3. CLEARGATE-block contains a bullet referencing `CR-017` AND containing both "decomposition gate" AND "lifecycle reconciler".
4. CLEARGATE-block contains a bullet referencing `CR-019` AND containing "Gate-3-class" or "Gate 3" AND the literal `--assume-ack`.
5. Session-start orientation block contains a tier-4 entry naming `.cleargate/knowledge/cleargate-enforcement.md` with "read only when" qualifier.
6. **Mirror parity within the new region** — extracting the CLEARGATE-block region from both CLAUDE.md files and diff'ing them returns empty for the lines covering bullets 1–4 + tier-4. (Pre-existing canonical-only bullets remain; the diff IS NOT empty across the entire block — see Gotcha B1.)
7. Outside-block invariance — `git diff <pre-merge-sha> HEAD -- CLAUDE.md` shows every changed line within `<!-- CLEARGATE:START -->` ... `<!-- CLEARGATE:END -->`; same for `cleargate-planning/CLAUDE.md`.
8. None of the four new bullets contains a `§N` pattern.

**Reuse (no duplication):**
- The exact bullet body for all four bullets is given verbatim in STORY-024-03 §1.2 (story file lines 64–77). Developer copies these blockquotes verbatim into the live CLEARGATE-block at the insertion point, with one blank line between bullets per existing block style (each `**foo.**` bullet is its own paragraph in the existing live file, separated by blank lines — verify by reading live lines 108–134).
- Tier-4 read-order entry text is given verbatim in story §1.2 (story file lines 89–91).
- Mirror-parity verification recipe is in story §3.2 step 5 (the `awk` extraction trick).

**Gotchas surfaced from code inspection:**
- **B1 — pre-existing CLAUDE.md mirror divergence is REAL and DELIBERATE.** Confirmed by Architect SDR 2026-05-01: live CLEARGATE-block (lines 98–135) = 35 lines containing 8 bolded bullets. Canonical CLEARGATE-block (`cleargate-planning/CLAUDE.md` lines 1–51 wrapped at the marker pair) = 39 lines containing 11 bolded bullets. The 3 extra canonical bullets are: `**State-aware surface.**` (canonical line 29), `**Cross-project orchestration.**` (canonical line 37), and a "Readiness gates advisory-by-default" extension to the "Halt at gates" bullet (canonical line 15 vs live line 112 — same bullet, additional sentence in canonical). Sprint plan §2.3 narrows the mirror-parity Gherkin scope to "the 4 new bullets are byte-identical between live and canonical" — NOT "the entire block diff is empty." **Developer action:** after the edit lands, run the Gherkin scenario 6 (mirror parity) by extracting ONLY the lines covering the new bullets, NOT the full block. Suggested verification recipe — diff the four-bullet text directly between files rather than running `awk + diff` over the full block (which will fail due to the pre-existing divergence). Tracked as cleanup CR per sprint plan §6 follow-ups; explicitly out of SPRINT-17 scope.
- **B2 — the canonical "Halt at gates" bullet at canonical line 15 contains an extra sentence about advisory-by-default readiness gates** (`Readiness gates ('cached_gate_result.pass') are advisory by default — the push proceeds and the item body receives an '[advisory: gate_failed — <criteria>]' prefix; opt into hard-reject via 'STRICT_PUSH_GATES=true' on the MCP server.`). The live "Halt at gates" bullet at line 112 lacks this sentence. **Do NOT add the canonical sentence to the live bullet.** Out of scope per §1.3 ("Changing the existing 'Halt at gates' or 'Drafting work items' bullets"). Cite: `cleargate-planning/CLAUDE.md` line 15.
- **B3 — the canonical CLEARGATE-block has its own different opening line:** canonical line 1 is `## 🔄 ClearGate Planning Framework` *outside* the START tag wrapping (the canonical file's CLEARGATE-tag region begins at the START marker). Live line 99 is the same heading INSIDE the marker pair (live START at line 98, heading at 99). The marker placement differs by intent — this is fine; just be aware that "extract between markers" may produce slightly different first-line shapes. The recipe `awk '/<!-- CLEARGATE:START -->/,/<!-- CLEARGATE:END -->/'` works identically for both, and after the edit only the four-bullet region needs to match.
- **B4 — bullet style.** All existing bullets in the live CLEARGATE-block use `**Title.**` followed by inline body (no separate line), separated by blank lines (verify live lines 108, 110, 112, 114, 120, 126, 128, 130, 132, 134). The four new bullets in story §1.2 follow this pattern; do NOT use a list-prefix `-` or numbered prefix.
- **B5 — read-order list is currently a 1./2./3. numbered list at live lines 104–106.** The new tier-4 entry is `4. ` followed by the path. Match the existing indentation (no leading space; story body shows the literal text).
- **B6 — Gate parser blind to in-fence headings (sprint plan §6).** The CLEARGATE-block contains markdown code fences? No — verified by reading the block (lines 98–135 of live): no triple-backtick fences inside the block. So this gotcha does NOT apply to STORY-024-03; it is mentioned for context only because the story file itself contains fenced markdown that demonstrates the new tier-4 entry. The gate parser's section-splitter will see the new bullets as belonging to the `## 🔄 ClearGate Planning Framework` section (live line 99); no `## ` heading is added, so no section-numbering shift.

---

### STORY-024-04 — Sprint Closeout Doc & Metadata Refresh Checklist

**Cross-story coupling:**
- Shares `CLAUDE.md` (live + canonical) with **STORY-024-03**. Different line range. See cross-story risks below for the exact partition.
- Creates `.cleargate/knowledge/sprint-closeout-checklist.md` — a NEW knowledge doc. The MANIFEST scaffold registry currently tracks two knowledge docs (`cleargate-protocol.md`, `readiness-gates.md`). Adding a third may require a new MANIFEST entry — see Gotcha D1.
- Creates `.cleargate/scripts/prep_doc_refresh.mjs` — `cleargate-planning/.cleargate/scripts/` is NOT tracked in MANIFEST (verified — no `.cleargate/scripts/*` entries in the 31-file manifest). So adding a new script does NOT require a MANIFEST entry. Only the canonical mirror copy in `cleargate-planning/.cleargate/scripts/prep_doc_refresh.mjs` needs to exist.
- Forward references CR-022 (Wave-X future sprint) for `close_sprint.mjs` wiring — explicitly out of scope per story §1.3.

**Files to create:**
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/sprint-closeout-checklist.md` — primary canonical content per story §3.2 (the long markdown table block).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/sprint-closeout-checklist.md` — byte-identical canonical mirror.
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/scripts/prep_doc_refresh.mjs` — generator script per story §3.2 outline (~120 LOC).
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/scripts/prep_doc_refresh.mjs` — byte-identical canonical mirror.

**Files to modify:**
- `/Users/ssuladze/Documents/Dev/ClearGate/CLAUDE.md` — within the CLEARGATE-tag-block (lines 98–135), insert a single new bullet near the **end** of the block, after the existing "Scope reminder" bullet at line 134 and before `<!-- CLEARGATE:END -->` at line 135. (Bullet text per story §3.2 "CLAUDE.md addition" block — story file lines 366–367.)
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/CLAUDE.md` — byte-identical edit at the analogous end-of-block location. Canonical landmark: insert after canonical line 39 ("Project overrides") and before the END marker. **Match the exact insertion offset relative to the END marker** — the canonical's pre-existing extra bullets ("State-aware surface" at line 29, "Cross-project orchestration" at line 37) sit before "Project overrides" and are NOT touched.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/MANIFEST.json` — see Gotcha D1; if MANIFEST regeneration is required, this file mutates.

**Schema changes:** None.

**Test scenarios (from §2.1 Gherkin — 7 total):**
1. The canonical knowledge doc exists at the documented path AND lists ≥9 named categories AND each item has a trigger condition (no "etc." catch-alls).
2. `diff .cleargate/knowledge/sprint-closeout-checklist.md cleargate-planning/.cleargate/knowledge/sprint-closeout-checklist.md` is empty.
3. Script runs against a fixture sprint (use SPRINT-16 — confirmed available at `.cleargate/sprint-runs/SPRINT-16/state.json` per Architect SDR; sprint_status: Completed) and writes `.cleargate/sprint-runs/SPRINT-16/.doc-refresh-checklist.md`. Every category appears; items match `- [ ]` if a changed file matches the trigger pattern, `- [x] ... no changes detected, skip` otherwise.
4. Script against a non-existent sprint (e.g. `S-99`) exits 1 with a clear error, writes nothing.
5. CLAUDE.md CLEARGATE-block contains a reference to `.cleargate/knowledge/sprint-closeout-checklist.md` AND `.doc-refresh-checklist.md`, mirrored byte-identically to canonical for the new bullet.
6. Script run against any sprint produces `git status --porcelain` showing only `.cleargate/sprint-runs/<id>/.doc-refresh-checklist.md` (and possibly token-ledger artifacts) as modified — no README, CHANGELOG, MANIFEST.json, package.json, CLAUDE.md, INDEX.md, wiki/, knowledge/*.md mutations.
7. Outside-block invariance for both CLAUDE.md files — every diff line within the marker pair.

**Reuse (no duplication):**
- The full canonical-checklist body (categories 1–9 + trigger-condition tables) is given verbatim in STORY-024-04 §3.2 (story file lines 222–295). Developer copies that block as-is into the new knowledge doc. No paraphrasing.
- The script outline (`getChangedFiles`, `evaluateChecklist`, `renderChecklist`, `main`) is given as a skeleton in story §3.2 (lines 297–362) — Developer fills in the concrete category trigger regexes from the canonical checklist body.
- For the `getChangedFiles` strategy: SPRINT-16 has no `sprint/SPRINT-16` branch (verified — the closing commit is `cc47c72 chore(SPRINT-16): close sprint`, all on main). So Strategy 2 (`--since <start_date>`) must work. Read sprint frontmatter from `.cleargate/delivery/archive/SPRINT-16_*.md` or fallback to `state.json`. The script must accept either path source.
- CLAUDE.md addition text is given verbatim in story §3.2 lines 366–367 (the blockquote starting `**Doc & metadata refresh on close.**`).
- For SPRINT-16 fixture sanity-check: the closing commit's `--name-only` produces the relevant changed-file set (run `git log cc47c72 --name-only` or `git diff db9eaf4..cc47c72 --name-only` to scope SPRINT-16's changes). `db9eaf4` is the BUG-023 fix immediately preceding sprint close.

**Gotchas surfaced from code inspection:**
- **D1 — MANIFEST schema permits but does not require new knowledge-doc entries.** `cleargate-planning/MANIFEST.json` tracks 31 files; only `.cleargate/knowledge/cleargate-protocol.md` and `.cleargate/knowledge/readiness-gates.md` are tracked under the `.cleargate/knowledge/` prefix. The schema (verified at the sample entry — `{path, sha256, tier, overwrite_policy, preserve_on_uninstall}`) implies tracked-file enforcement. **Decision required (Open decision 2):** does the new `sprint-closeout-checklist.md` get added to MANIFEST? `cleargate doctor` may flag a missing-tracked-file or may pass with the new file in canonical (since canonical is the source of truth for what `cleargate init` ships). Story §3.1 says: "After commit, `cleargate-planning/MANIFEST.json` may need a new entry for the new knowledge doc — verify with `cleargate doctor`." Treat this as a verify-after-commit step; if doctor fails, regen MANIFEST and add to the same commit.
- **D2 — Gate-parser code-fence blindness (sprint plan §6 follow-up; STORY-024-04 hit it during drafting).** The readiness-gate predicate evaluator splits the story body on `^## ` without respecting fenced code blocks. STORY-024-04 §3.2 contains markdown content with `## N. <title>` lines INSIDE a triple-backtick fence (the canonical knowledge doc body, story file lines 234, 243, 249, 256, 262, 270, 276, 284, 289). To pass the entry gate, story-024-04's drafter demoted those in-fence headings from `## ` to `### `. **For the new knowledge doc itself**, the headings ARE the canonical content and `### ` is what's in the story spec — so Developer copies them verbatim (`### 1. Project READMEs`, etc.). **Do NOT promote them back to `## `** even though semantically they are top-level sections of the new doc. Reasoning: any future gate-parser run on the new knowledge doc will fail with the same ambiguity if `## ` headings appear in odd numbers, and the story spec (which the SDR validated) uses `### `. Cite: STORY-024-04 §3.2 (the literal heading lines).
- **D3 — Script must NOT modify any docs.** Story §2.1 scenario 6 is a hard contract: the script writes only `.cleargate/sprint-runs/<id>/.doc-refresh-checklist.md`. Developer must NOT use `git status` shellouts that may mutate state, must NOT call `cleargate stamp` from the script, must NOT touch any of the surfaces it lists. Pure read-and-emit.
- **D4 — Script's exit-code contract.** Story §2.1 scenario 4 specifies "exits 1" on missing sprint AND "exits 2" usage error (story §3.2 main() comment). Developer must wire both. `process.exit(1)` for missing sprint dir; `process.exit(2)` for missing argv[2].
- **D5 — Mirror parity for new files is straightforward IF the developer creates them via byte-identical writes.** Use `cp` after writing the live copy, or write both paths from the same buffer. Verify with `diff` post-edit.
- **D6 — CLAUDE.md insertion at end-of-block.** Live line 134 is the "Scope reminder" bullet; live line 135 is the END marker. Insert the new bullet between 134 and 135 with a leading blank line. Canonical: between line 39 (Project overrides) and the END marker. (Note: canonical's "Scope reminder" bullet is missing — the canonical block ends with "Project overrides" plus the END marker. This is part of the pre-existing divergence and is OUT of scope.) **Action:** insert the new bullet as the LAST bullet before END in both files; the line offset relative to END differs between files but the semantic position is the same.
- **D7 — Bullet style consistency.** Use `**Doc & metadata refresh on close.**` followed by inline body, separated by a blank line from the prior bullet, matching the existing style at live line 134 (`**Scope reminder.** ClearGate is...`).
- **D8 — Mirror parity scope narrowing applies here too.** Sprint plan §2.3 explicitly extends the "edit-parity not state-parity" decision to STORY-024-04's CLAUDE.md edit. Developer's mirror Gherkin (scenario 5) verifies only the new bullet matches across files; the pre-existing canonical-only bullets remain and the full-block diff will still be non-empty. Recipe: diff the new bullet text directly across files, not the full block.

## Cross-story risks

- **CR1 — STORY-024-03 ↔ STORY-024-04 both edit `CLAUDE.md` + `cleargate-planning/CLAUDE.md` in parallel.** Line-range partition (verified by Architect SDR 2026-05-01):
  - **024-03 region (live):** lines 103–106 (read-order list; appends a new line 107) AND between lines 112 (Halt at gates) and 114 (Drafting work items).
  - **024-04 region (live):** between line 134 (Scope reminder) and line 135 (END marker).
  - **Gap between regions:** ≥20 lines (line 114 → line 134). A two-developer parallel commit will rebase cleanly; git's three-way merge handles disjoint inserts inside the same file without conflict.
  - **Canonical analogues:** 024-03 edits canonical lines 1–6 (read-order area) + between canonical lines 15 (Halt at gates) and 17 (Drafting work items). 024-04 inserts after canonical line 39 (Project overrides). Same disjoint-region property holds.
  - **Mitigation:** dispatch order is non-binding. If the orchestrator wants belt-and-braces, dispatch 024-04 first (its edit region is closer to EOF and any rebase against 024-03 is trivial), then 024-03. Either order works.
- **CR2 — Both 024-03 and 024-04 cite "cleargate-enforcement.md" or analogous future-state names.** 024-03's tier-4 read-order entry names `cleargate-enforcement.md`. 024-04's checklist body (Story §3.2 category 7 "Frontmatter version stamps") references `.cleargate/knowledge/cleargate-enforcement.md` as a future stamp candidate. Neither story creates the file (that's STORY-024-02 in M2). Both citations are forward-references and are explicitly approved by sprint plan §2.1. No action required for M1; QA must NOT flag these as broken references.
- **CR3 — All three stories must verify `cleargate doctor` exits 0 post-edit.** STORY-024-04's new knowledge doc may trigger a doctor warning if MANIFEST is not regenerated (Gotcha D1). STORY-024-01's mirror edit of `architect.md` mutates the canonical hash that MANIFEST tracks; if MANIFEST is not regenerated, doctor fails on hash drift for `.claude/agents/architect.md`. **Mitigation:** Developer of each story includes MANIFEST regeneration in the same commit if the canonical hash changes. STORY-024-03 mutates ONLY `cleargate-planning/CLAUDE.md` which is NOT tracked in MANIFEST (verified — MANIFEST does not list `cleargate-planning/CLAUDE.md` itself), so 024-03 has no MANIFEST impact.
- **CR4 — Mirror-parity invariant per FLASHCARD `2026-04-19 #wiki #protocol #mirror`.** Every story in this milestone has a mirror-parity Gherkin scenario. The pre-existing CLAUDE.md divergence (35 vs 39 lines) is grandfathered per sprint plan §2.3. For 024-01's `architect.md` edit, the live↔canonical pair is currently parity-clean (verified `diff` empty 2026-05-01) — the post-edit `diff` MUST be empty (no narrowing). For 024-04's new files, both copies are written fresh — the post-create `diff` MUST be empty. For 024-03 + 024-04 CLAUDE.md edits, the new-region `diff` MUST be empty (full-block `diff` will not be empty due to grandfathered divergence).

## Open decisions for orchestrator

1. **MANIFEST regeneration policy for STORY-024-01 + STORY-024-04.** When the canonical `cleargate-planning/.claude/agents/architect.md` (tracked) or a new canonical knowledge doc mutates, does Developer regenerate `cleargate-planning/MANIFEST.json` in the same commit, or is that a separate sprint-close pass? Architect's reading: regenerate in the same commit (it's a pure derivation; pre-commit doctor will fail otherwise). Confirm.
2. **STORY-024-04 MANIFEST entry for `sprint-closeout-checklist.md`.** Add to MANIFEST as a tracked knowledge doc, or leave it untracked? Existing knowledge docs (`cleargate-protocol.md`, `readiness-gates.md`) are tracked. By precedent, the new doc should be tracked. If tracked, Developer needs the proper `tier`, `overwrite_policy`, `preserve_on_uninstall` values — request the exact field values from the orchestrator OR use the same values as `readiness-gates.md` (which is the closer analogue: a non-protocol knowledge doc).
3. **STORY-024-04 fixture-sprint choice for the script-test Gherkin scenario.** Sprint plan and Architect SDR confirm SPRINT-16 is closed (`state.json` shows `sprint_status: "Completed"`) and is the most recently closed sprint. Use SPRINT-16 as the fixture? Alternative: use SPRINT-15. Either works; SPRINT-16 is the sharper test because it includes a CHANGELOG-format change (STORY-016-03) that exercises CHANGELOG-trigger logic. Recommendation: SPRINT-16.
4. **STORY-024-01 line-145 stale guidance.** The live `architect.md:145` "Small plans. A 200-line plan is a bad plan. Target 60-120 lines per milestone." contradicts EPIC-024 §6 Q4 ("no cap"). Story §1.3 keeps it OUT of scope. File a separate cleanup story (post-EPIC-024) or fold into M2 (STORY-024-02 already touches `architect.md` at line 140 for citation rewrite — same-file, adjacent-line)? Architect's recommendation: fold into M2 as a one-line additional edit since 024-02 already opens the file in the same hunk neighborhood. Confirm with orchestrator before M2 plan is drafted.
5. **STORY-024-03 — should the close-related guardrail at the upper-CLAUDE.md "Guardrails for the conversational agent (me)" section (live line 96) be removed once the new bullet 4 lands inside the CLEARGATE-block?** The two now redundantly state the same `--assume-ack` rule. Story §1.3 puts the upper section OUT of scope — leave both. Tracked here so a future cleanup CR can deduplicate.

---

**Plan length: ~190 lines.** Justified — the milestone hosts three parallel stories with disjoint surfaces, each requiring its own gotcha cluster and reuse map. Most lines come from the gotcha sections (B1 / D1 / D2 / D6 / CR1) which capture surveying-the-existing-divergence work the Developer would otherwise repeat per story. Per Architect Workflow guidance ("If a milestone needs more, it's over-scoped — flag that") this milestone is NOT over-scoped — the verbosity is concentrated in mirror-divergence diagnosis, not duplicate file-surface tables.
