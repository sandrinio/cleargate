# STORY-033-04 — Developer Report (CAPSTONE, L3)

role: developer

STORY: STORY-033-04
STATUS: done
COMMIT: fbc4bd8fb18a717d0c1de35c0d9283ced2287160
TYPECHECK: pass
TESTS: 23 passed, 0 failed
FILES_CHANGED:
- `.cleargate/scripts/launch_wave.mjs` (new — wave launcher; exports validateVerdicts + mintRunId + worktreeAddCommand + shouldRunParallel + launchWave)
- `cleargate-planning/.cleargate/scripts/launch_wave.mjs` (canonical mirror)
- `.cleargate/knowledge/cleargate-protocol.md` (working — added §23 Parallel-Wave Execution Contract)
- `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` (canonical — added §23, appended at end since canonical lacks §22)
- `.cleargate/knowledge/cleargate-enforcement.md` (working — added §1.6 parallel-wave worktree contract)
- `cleargate-planning/.cleargate/knowledge/cleargate-enforcement.md` (canonical mirror)
- `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` (canonical — Phase C rewrite: §C.0 mode selector + kill-switch, §C.0.1 wave launch, §C.7 serial barrier merge, §C.9 between-wave flashcard gate)
- `cleargate-planning/MANIFEST.json` (regenerated by `npm run prebuild`)

## What was built

1. **`launch_wave.mjs`** — exports the segment-verdict `validateVerdicts(verdicts)` discriminated-union validator (raises a validation Error NAMING the offending `storyId` on a malformed verdict: missing required field, unknown discriminant, or non-GREEN without a valid `blocker`). Also exports `mintRunId` (stable distinct RUN_ID per segment), `worktreeAddCommand` (ClearGate-managed `.worktrees/STORY-X` bash command, NOT Workflow `isolation:'worktree'`), `shouldRunParallel` (kill-switch selector — false on `v2-serial` OR `CLEARGATE_PARALLEL_WAVES=off`), and `launchWave` (the `parallel()` driver with injected `parallel`/`segmentRunner` seams; returns a plan with worktree commands + minted RUN_IDs when no live runtime seam is supplied — the SPRINT-32 serial-build case). `BLOCKER_TYPES` enumerates the five §22 true-blocker kinds.
2. **SKILL.md Phase C rewrite** (canonical) — `§C.0` mode table + kill-switch zero-behavior-change guarantee; `§C.0.1` wave-launch mechanics (SKIP_FLASHCARD_GATE save/set/restore, serial worktree pre-creation, parallel() launch, barrier validate/flashcards/merge/restore, resumeFromRunId complete-then-resume, idempotent segments); `§C.7` serial barrier merge (one worktree at a time, never two concurrently); `§C.9` between-wave flashcard relocation with `.processed-<hash>` markers.
3. **protocol §23** — "Parallel-Wave Execution Contract" with the verdict schema table, RUN_ID attribution invariant, barrier consolidation + serial merge rule, in-segment true-blocker re-map (inherits §22).
4. **Kill-switch** — `CLEARGATE_PARALLEL_WAVES=off` + `execution_mode: v2-serial|v2-parallel` documented in SKILL.md §C.0 and protocol §23; `shouldRunParallel()` is the wired selector.

## Verification

- 23/23 QA-Red tests pass (`cd cleargate-cli && tsx --test test/scripts/wave-execution-barrier.red.node.test.ts`): 4 validator-unit groups (6 sub-tests) + C1 file-existence (2) + C2–C6 content/fixture assertions.
- Typecheck clean (`npm run typecheck` exit 0; the `.mjs` launcher + `.test.ts` are outside the `src/**`-only tsconfig scope by design).
- Stash-verify (mandatory for non-trivial logic): removing `launch_wave.mjs` flips 8 tests to fail (validator group + C1) — confirms the validator is genuinely exercised, not vacuously green.
- Three-way mirror parity (canonical ↔ npm payload) verified empty-diff post-`prebuild` for SKILL.md, launch_wave.mjs, protocol.md, enforcement.md. Additive parity (working ↔ canonical) verified for enforcement.md.
- Adjacent-test sweep: 116/121 pass in the touched-file neighborhood. The 5 failures are ALL pre-existing, NOT caused by this story (see Pre-existing failures below).

## Pre-existing failures (NOT introduced by this story)

- **3× "protocol live and mirror are byte-identical"** (`protocol-section-12/13/14`, `enforcement-section-13`): The working `.cleargate/knowledge/cleargate-protocol.md` has `§22 Sprint Execution Autonomy` that the canonical `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` LACKS — a pre-existing STORY-071-01 divergence confirmed at HEAD (working §22 count=1, canonical=0, both pre-my-edit). Per per-edit parity (FLASHCARD #mirror #parity #161/#14: never reconcile pre-existing divergence as a side effect), I did NOT reconcile §22; I landed §23 additively in both files. The byte-identical mirror test was already red on the baseline.
- **2× "sprint preflight --help"** (`build-manifest.node.test.ts` STORY-025-06 scenario): these spawn the built `cleargate-cli/dist/cli.js`, which is absent in this worktree (FLASHCARD #38/#39 — worktrees need a build). I touched no `src/` or sprint-preflight code.

## Plan-default decisions taken (per M2.md §STORY-033-04 §1.4)

- GREEN+ESCALATED mixed wave → merge GREEN immediately (serial) + resume only ESCALATED via resumeFromRunId (documented in SKILL.md §C.0.1 + §C.7 + protocol §23.3). [recommended default]
- Verdict validator defined inside `launch_wave.mjs` and EXPORTED for STORY-033-05/Reporter reuse. [recommended default]
- §23 in canonical appended at end (canonical has no §22) — additive, leaves the §22 divergence untouched.

r_coverage:
  - { r_id: "R1-launcher", covered: true, deferred: false, clarified: false }
  - { r_id: "R2-verdict-validator", covered: true, deferred: false, clarified: false }
  - { r_id: "R3-worktree-bash-not-workflow", covered: true, deferred: false, clarified: false }
  - { r_id: "R4-skip-flashcard-gate-env", covered: true, deferred: false, clarified: false }
  - { r_id: "R5-skill-phase-c-rewrite", covered: true, deferred: false, clarified: false }
  - { r_id: "R6-kill-switch", covered: true, deferred: false, clarified: false }
  - { r_id: "R7-autonomous-after-gate2", covered: true, deferred: false, clarified: false }
  - { r_id: "R8-in-segment-blocker-remap", covered: true, deferred: false, clarified: false }
  - { r_id: "R9-protocol-section-23", covered: true, deferred: false, clarified: false }
plan_deviations:
  - { what: "§23 appended at end of canonical protocol (not after §22)", why: "canonical protocol lacks §22 — pre-existing STORY-071-01 divergence; appending at end is the only additive option that preserves per-edit parity without reconciling §22", orchestrator_confirmed: false }
adjacent_files:
  - "cleargate-cli/test/scripts/protocol-section-12.node.test.ts"
  - "cleargate-cli/test/scripts/protocol-section-13.node.test.ts"
  - "cleargate-cli/test/scripts/protocol-section-14.node.test.ts"
  - "cleargate-cli/test/scripts/enforcement-section-13.node.test.ts"
  - "cleargate-cli/test/scaffold/sprint-execution-mirror.node.test.ts"
flashcards_flagged:
  - "2026-05-29 · #mirror #parity #protocol · working .cleargate/knowledge/cleargate-protocol.md has §22 (STORY-071-01) that canonical cleargate-planning lacks — pre-existing divergence; live↔canonical byte-identical protocol mirror tests are RED on baseline. Append new §N at end of canonical; never reconcile §22 as a side effect."
