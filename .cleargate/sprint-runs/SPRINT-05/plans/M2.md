role: architect

# SPRINT-05 · M2 Plan — CLI Layer (9 stories)

## Summary

M2 consumes the frozen M1 libs (`codebase-version`, `stamp-frontmatter`, `ledger-reader`, `sha256`, `manifest`, `claude-md-surgery`, `settings-json-surgery`) and produces the full user-facing CLI surface for EPIC-001 / EPIC-008 / EPIC-009. Six new Commander subcommands land (`stamp`, `gate check|explain`, `stamp-tokens`, `doctor --check-scaffold` **with base command**, `upgrade`, `init` extension) plus one cross-repo edit in `mcp/` for `server_pushed_at_version`, plus the authoritative `readiness-gates.md` YAML data file. All CLIs register in `cleargate-cli/src/cli.ts`; three stories (001-05, 008-05, stub replacement) each delete/overwrite the existing `stubHandler('stamp')` line at cli.ts:46-49 — orchestrator must serialize edits to `cli.ts` across those three stories. **Shared-surface coordination:** STORY-009-04 creates the `doctor` base command with a subcommand dispatcher in M2; STORY-008-06 in M3 extends it with `--session-start` and `--pricing` — the dispatcher shape 009-04 ships is what 008-06 merges into, so 009-04 MUST land before M3 starts.

## Parallelism + serialization map

Nine Developer agents, four concurrency waves:

- **Wave A (spawn immediately, 5 parallel):** 001-05, 001-06, 008-01, 009-03, 009-04.
  - 001-05 is independent (wraps `codebase-version` + `stamp-frontmatter`).
  - 001-06 is independent (cross-repo in `mcp/`; no dep on any other M2 story).
  - 008-01 is pure authoring of `.cleargate/knowledge/readiness-gates.md`; gates 008-02.
  - 009-03 is an `init.ts` extension; independent of 009-04.
  - 009-04 creates `commands/doctor.ts` + dispatcher; blocks 008-06 (M3). Must land before M3 starts.
- **Wave B (serialized after 008-01 commits):** 008-02.
  - 008-02's grammar parser tests load fixture criteria from 008-01's authored YAML. Spawn 008-02 after 008-01's commit lands, not before.
- **Wave C (parallel with Wave B):** 008-05, 009-05.
  - 008-05 wraps M1's `ledger-reader`; no M2 dep. Parallel-safe with 008-02/009-05.
  - 009-05 consumes M1's `manifest` lib; no M2 dep. Parallel-safe.
- **Wave D (serialized after 008-02 commits):** 008-03.
  - 008-03's CLI imports `evaluate()` from 008-02's `readiness-predicates.ts` + `writeCachedGate()` from `frontmatter-cache.ts`. Spawn after 008-02 commit.

### Serialization chain (hard order)

**008-01 → 008-02 → 008-03** (gates data → evaluator → CLI).

### Shared-file contention inside M2

- `cleargate-cli/src/cli.ts`: edited by 001-05, 008-03, 008-05, 009-04, 009-05. Each adds a new `program.command(...)` block; no two stories touch the same block. **001-05 replaces the existing stub at cli.ts:46-49** — the Developer for 001-05 must Read cli.ts before Write and preserve all other registrations. Other Developers append only.
- `cleargate-cli/src/commands/init.ts`: only 009-03 edits; no contention.
- `cleargate-cli/src/commands/doctor.ts`: only 009-04 creates in M2; 008-06 extends in M3.
- `cleargate-cli/package.json`: 008-01 (maybe) adds `js-yaml` + `@types/js-yaml`; 008-02 uses it; 009-05 adds `diff` + `@types/diff`. Orchestrator coordinates single dep-add commit per story — NEVER two dep-adds in parallel.
- `.cleargate/knowledge/readiness-gates.md` + mirror: only 008-01 creates.

## Orchestrator-level decision (must resolve BEFORE 001-06 spawns)

**MCP cross-repo dependency — decision: Option B (port ~30-line helper into `mcp/src/utils/codebase-version.ts`).**

Reasoning:
1. `mcp/package.json` already has `"cleargate": "0.1.0-alpha.1"` (line 25) but `cleargate-cli/package.json` exports map (lines 12-23) only exposes `.` (the CLI bundle) and `./admin-api` — **no `./lib/*` export path exists**. Adding one requires either (a) splitting `codebase-version.ts` out of the tsup bundle into its own entry, or (b) exposing `./lib/codebase-version` as a secondary tsup entry with its own types. Both add maintenance cost to every subsequent `src/lib/*` module.
2. The helper is 108 lines (verified: `cleargate-cli/src/lib/codebase-version.ts:1-108`) — not 30 as the sprint file estimates, but still well under a week's drift-tolerance threshold.
3. MCP runs in a container with a frozen SHA at build time; the helper's `git rev-parse` + `git status --porcelain` calls are identical in both environments (same shape of `execSync` output).
4. Option B decouples `mcp`'s deploy cadence from `cleargate-cli` npm publish cadence — STORY-001-06 can land + deploy to Coolify without waiting on `@cleargate/cli@0.2.0` publish.

**Action for STORY-001-06 Developer:** copy `cleargate-cli/src/lib/codebase-version.ts` verbatim into `mcp/src/utils/codebase-version.ts` (rename `lib` → `utils` only). Do NOT add `@cleargate/cli` import in push-item.ts. Update `mcp/src/utils/codebase-version.ts` header comment to reference EPIC-001 STORY-001-06 + note it's a port of `cleargate-cli/src/lib/codebase-version.ts` — if that file changes, port the diff manually.

---

## Per-story sketch

### STORY-001-05 — `cleargate stamp <file>` CLI (L1, Wave A)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/stamp.ts`
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — **replace** existing stub at lines 46-49 (`program.command('stamp').description('stamp a delivery artifact into the MCP server').action(stubHandler('stamp'))`) with a real handler registration; update `description` to `"stamp ClearGate metadata fields into a file's frontmatter"`.
- **Schema / API**:
  ```ts
  export interface StampCliOptions { cwd?: string; now?: () => Date; stdout?: (s: string) => void; exit?: (code: number) => never; }
  export async function stampHandler(file: string, opts: { dryRun?: boolean }, cli?: StampCliOptions): Promise<void>
  ```
- **Flow**: resolve `file` to abs path; call `getCodebaseVersion({ cwd })`; if `--dry-run`, read current frontmatter, compute what `stampFrontmatter` would produce, print unified-diff-style lines to stdout, exit 0 (no write); else call `stampFrontmatter(absPath, { now, version })` and print one-liner summary (`[stamped] <file> (<reason>)`). Exit 0 on success.
- **Test shape** (vitest `cleargate-cli/src/commands/stamp.test.ts`):
  1. `stamp on fresh file stamps all 4 fields` — fixture with frontmatter lacking timestamps; after stamp, all 4 version fields populated.
  2. `stamp on already-stamped file advances updated_at only` — created_at immutable; updated_at + updated_at_version change.
  3. `stamp --dry-run prints diff and leaves file untouched` — assert file bytes unchanged, stdout contains diff markers.
  4. `stamp on archive path is noop-archive` — path under `/.cleargate/delivery/archive/` returns `reason: 'noop-archive'`, file bytes unchanged, exit 0.
  5. `stamp with non-existent file exits 1 with stderr message` — no frontmatter helpers called.
  6. `stamp exit code is 0 on success, 1 on read failure` — test seam `exit` asserts integer.
- **Reuse (no duplication)**: `getCodebaseVersion` from `cleargate-cli/src/lib/codebase-version.ts`; `stampFrontmatter` from `cleargate-cli/src/lib/stamp-frontmatter.ts`. Do NOT hand-roll YAML or git-shelling — use the M1 helpers.
- **Gotchas**:
  - FLASHCARD `#cli #determinism #test-seam` — thread `now` and `exit` as options so tests can freeze time + assert exit codes.
  - FLASHCARD `#cli #commander #optional-key` — when building the `opts` object passed to `stampFrontmatter`, conditionally assign `now` / `version` rather than `{ now: undefined }` — Commander option-value shapes can carry `undefined` that breaks `in` checks.
  - FLASHCARD `#tsup #cjs #esm` — no top-level await in stamp.ts.
  - `parseFrontmatter` strips one leading blank line from body on read; `stampFrontmatter` already re-adds it on write (verified `stamp-frontmatter.ts:174-175`). Don't re-parse the post-write file byte-compare with the pre-write file in tests; compare parsed frontmatter dicts instead.

### STORY-001-06 — MCP `push_item` writes `server_pushed_at_version` (L1, Wave A, cross-repo)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/utils/codebase-version.ts` (verbatim port of `cleargate-cli/src/lib/codebase-version.ts` — see orchestrator decision above).
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/tools/push-item.ts` — inject `server_pushed_at_version` into `args.payload` before the two `tx.insert` / `tx.update` sites (insert at lines 62-72 and lines 94-105).
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/mcp/src/tools/push-item.test.ts` — add roundtrip assertion.
- **Schema / API**:
  ```ts
  // push-item.ts (internal): before insert/update
  const version = getCodebaseVersion({ cwd: process.cwd() });
  const stampedPayload = { ...args.payload, server_pushed_at_version: version.version_string };
  ```
  Replace `args.payload` in the two insert/update `currentPayload`/`payload` fields with `stampedPayload`.
- **Flow**: MCP computes version once per push call (not once per transaction retry); treats `args.payload` as opaque JSON blob (it's `z.record(z.string(), z.unknown())` per push-item.ts:18). Test seam: allow dependency injection of `getCodebaseVersion` via an optional `ctx.getVersion?: () => string` or a module-scope override; simpler — import the helper directly and use `vi.mock('../utils/codebase-version.js', …)` in the test.
- **Test shape** (extend `mcp/src/tools/push-item.test.ts`):
  1. `push_item injects server_pushed_at_version into current_payload` — mock `getCodebaseVersion` to return `{ version_string: 'b1c4d8a' }`; push; read `items.currentPayload` → assert `server_pushed_at_version === 'b1c4d8a'`.
  2. `push_item stamps in item_versions payload roundtrip` — same mock; read `item_versions.payload` for the written version; assert field present.
  3. `push_item preserves caller-supplied payload fields` — caller payload has `frontmatter: {foo: 'bar'}`; after push, both `foo` and `server_pushed_at_version` present.
  4. `push_item version stamp is per-call, not per-retry` — assert `getCodebaseVersion` called exactly once per `pushItem()` invocation.
- **Mirror-discipline**: none — `mcp/` has no scaffold mirror. Port-helper comment must cite source file path (see Orchestrator decision).
- **Gotchas**:
  - `push-item.ts:41-123` uses `ctx.db.transaction(...)` — retries on serialization failure will re-run the transaction callback. Call `getCodebaseVersion` BEFORE `ctx.db.transaction(...)` to pin the version string for the life of a single push call.
  - Vitest CWD for the MCP test suite is `mcp/` — `getCodebaseVersion({ cwd: process.cwd() })` in tests will resolve against the monorepo root's git. Mock the helper in tests; do NOT rely on real git output.
  - No MCP schema change needed — `currentPayload` is already `jsonb`.

### STORY-008-01 — Author `readiness-gates.md` (L2, Wave A)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/readiness-gates.md`
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/readiness-gates.md` (mirror, byte-identical)
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/package.json` — add `"js-yaml": "^4.1.0"` + `"@types/js-yaml": "^4.0.9"` (only if orchestrator confirms no other M1/M2 story already added it; first M2 story to need yaml parsing adds the dep).
- **Schema / API**: pure doc authoring. Fenced ```yaml blocks; one block per `{work_item_type, transition}`. Must use ONLY the 6 closed-set predicate shapes verified in EPIC-008 §4 lines 159-165.
- **Required sections** (in order):
  1. Title + one-paragraph overview.
  2. `## Predicate Vocabulary` — one paragraph per predicate shape with a concrete example (6 paragraphs total).
  3. `## Severity Model` — advisory vs. enforcing split; why Proposal never blocks `approved: true`.
  4. `## Gate Definitions` — six fenced ```yaml blocks in this order: `proposal.ready-for-decomposition` (advisory), `epic.ready-for-decomposition` (enforcing), `epic.ready-for-coding` (enforcing), `story.ready-for-execution` (enforcing), `cr.ready-to-apply` (enforcing), `bug.ready-for-fix` (enforcing).
- **Gate criteria** — mirror EPIC-008 §4 table at lines 195-204:
  - Proposal: §2 architecture populated; §3 touched files populated; depends_on refs resolve via `link-target-exists`.
  - Epic (decomposition): `frontmatter(context_source).approved == true`; `body does not contain 'TBD'`; §6 has zero unanswered questions; §4 ≥1 file path + all exist (`file-exists`).
  - Epic (coding): all linked Stories present (`link-target-exists` per ref); §5 Gherkin has ≥1 happy + ≥1 error scenario; §6 empty.
  - Story (execution): parent Epic `status-of == 🟢`; §3 files verified; §4 DoD ≥1 checked-checkbox; Gherkin covers §1.2.
  - CR (apply): blast radius section populated; all invalidated items `status-of == 🔴`; sandbox paths exist.
  - Bug (fix): `section(repro) has ≥3 listed-item`; failing-test path exists; severity field populated in frontmatter.
- **Test shape** — 008-01 has no direct unit tests (pure doc). A yaml-parse fixture test lands in 008-02's suite:
  1. `js-yaml parse of readiness-gates.md returns 6 blocks` — each block has `work_item_type`, `transition`, `severity`, `criteria[]`.
  2. `every criterion.check parses via readiness-predicates.parse()` — no malformed predicates.
- **Mirror-discipline**: after writing the live file, `cp` to `cleargate-planning/.cleargate/knowledge/readiness-gates.md` and `diff` must return empty. Commit both in one commit.
- **Gotchas**:
  - FLASHCARD `#wiki #schema #lint` — YAML block schema rules: keep field set closed and explicit; any unknown key breaks 008-02's parser.
  - The `status-of(<[[ID]]>)` predicate resolves via `.cleargate/wiki/index.md` — wiki must be built before `gate check` can use those criteria. NOT 008-01's problem to solve; note in the `## Severity Model` section that status-of predicates can return `unknown` when wiki is stale.
  - Predicate grammar must match 008-02's closed set EXACTLY — no `frontmatter(.).field matches /regex/`, no `body section /xyz/`, no synonyms.

### STORY-008-02 — Predicate evaluator + frontmatter-cache libs (L3, Wave B)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/readiness-predicates.ts`
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/frontmatter-cache.ts`
  - No edits to `stamp-frontmatter.ts` (reuse as-is via import).
- **Schema / API** (public exports, frozen for 008-03 consumption):
  ```ts
  // readiness-predicates.ts
  export type ParsedPredicate =
    | { kind: 'frontmatter'; ref: string; field: string; op: '==' | '!=' | '>=' | '<='; value: string | number | boolean }
    | { kind: 'body-contains'; needle: string; negated: boolean }
    | { kind: 'section'; index: number; count: { op: '>=' | '==' | '>'; n: number }; itemType: 'checked-checkbox' | 'unchecked-checkbox' | 'listed-item' }
    | { kind: 'file-exists'; path: string }
    | { kind: 'link-target-exists'; id: string }
    | { kind: 'status-of'; id: string; value: string };
  export interface ParsedDoc { fm: Record<string, unknown>; body: string; absPath: string; }
  export interface EvalOptions { projectRoot?: string; wikiIndexPath?: string; }
  export function parsePredicate(src: string): ParsedPredicate;  // throws on malformed
  export function evaluate(predicate: string, doc: ParsedDoc, opts?: EvalOptions): { pass: boolean; detail: string };

  // frontmatter-cache.ts
  export interface CachedGate { pass: boolean; failing_criteria: { id: string; detail: string }[]; last_gate_check: string; }
  export function readCachedGate(absPath: string): Promise<CachedGate | null>;
  export function writeCachedGate(absPath: string, result: CachedGate, opts?: { now?: () => Date }): Promise<void>;
  ```
- **Closed-set scope boundary** (NON-NEGOTIABLE per sprint file "Scope adjustments to watch for mid-sprint"):
  - Exactly 6 predicate shapes listed above. Any predicate that doesn't parse to one of these → `throw new Error('unsupported predicate shape: <src>')`. Do NOT add `matches`, regex, nested boolean, or any extensibility hook.
  - Grammar parsing: use a hand-rolled tokenizer + switch on first token. NOT a full PEG grammar. Target ≤150 LoC for `parsePredicate`.
- **Evaluator internals**:
  - `frontmatter(<ref>).<field>`: `ref === '.'` → `doc.fm`; else resolve `ref` as a relative path from `doc.absPath` or treat as a frontmatter-key-containing-path (see 008-01 criteria — `context_source` is a frontmatter key, not a filesystem path; parse accordingly).
  - `body contains "<s>"` / `body does not contain "<s>"`: substring search on `doc.body`; `detail` cites occurrence count.
  - `section(N) has <count> <item-type>`: walk `doc.body` splitting on `^## ` headers; N is 1-indexed; count by regex (`- [x]`, `- [ ]`, `- ` respectively).
  - `file-exists(<path>)`: resolve relative to `opts.projectRoot ?? process.cwd()`; `fs.existsSync`.
  - `link-target-exists([[ID]])`: read `opts.wikiIndexPath ?? .cleargate/wiki/index.md`; grep for `[[ID]]`.
  - `status-of([[ID]]) == <value>`: resolve ID via wiki index → extract `status:` field from that page's frontmatter → compare.
- **Sandbox (MUST)**: no `execSync`, no `child_process`, no network module imports, no `require('fs').writeFile` in evaluator (read-only FS). FS reads limited to files under `opts.projectRoot ?? process.cwd()` (normalize with `path.resolve` + startsWith check).
- **`writeCachedGate` idempotency**:
  - Reuse `stampFrontmatter` semantics: parse existing frontmatter, inject/update `cached_gate_result`, serialize with same key-order rules, re-add blank line separator before body.
  - If existing `cached_gate_result` deep-equals new result AND `last_gate_check` was explicitly passed the same value (via `opts.now`), write nothing (byte-identical rerun). Compare by JSON.stringify of the two result objects.
  - `last_gate_check` is ISO-8601 with second precision (match `stamp-frontmatter.ts:70` `toIsoSecond`).
- **Test shape** (vitest, `readiness-predicates.test.ts` + `frontmatter-cache.test.ts`):
  1. `parsePredicate accepts all 6 shapes with valid fixtures` (6 asserts).
  2. `parsePredicate throws on malformed` — 5 negative cases: `eval('rm -rf /')`, `body matches /regex/`, `frontmatter().foo == bar` (empty ref), `section(abc) has ≥1 X`, random garbage.
  3. `evaluate frontmatter(.).approved == true → pass` and `!= true → fail`.
  4. `evaluate body contains 'TBD' → pass / does not contain → fail` with detail citing count.
  5. `evaluate section(2) has ≥1 checked-checkbox` on a fixture with 3 checked + 1 unchecked → pass.
  6. `evaluate file-exists on missing path → fail`.
  7. `evaluate link-target-exists resolves via wikiIndexPath seam`.
  8. `evaluate status-of reads linked file's frontmatter.status`.
  9. `sandbox: evaluate rejects FS reads outside projectRoot` (path traversal via `../../etc/passwd`).
  10. `sandbox: evaluate does not call execSync/child_process` (vi.spyOn on `node:child_process`).
  11. `writeCachedGate byte-identical rerun test` — write twice with same inputs (injected `now`), assert `fs.readFile` returns identical bytes.
  12. `writeCachedGate preserves existing frontmatter keys in original order`.
  13. `readCachedGate returns null when cached_gate_result absent`.
- **Reuse (no duplication)**: `parseFrontmatter` from `cleargate-cli/src/wiki/parse-frontmatter.ts`; `stampFrontmatter`'s serialize-YAML rules (extract the internal `serializeFrontmatter` helper into a shared `cleargate-cli/src/lib/frontmatter-yaml.ts` if needed, OR replicate the same 30-line function in `frontmatter-cache.ts` — Developer picks, orchestrator approves no-extraction if it keeps M2 surface small).
- **Gotchas**:
  - FLASHCARD `#cli #determinism #test-seam` — `now` must be injectable for byte-identical rerun test.
  - FLASHCARD `#cli #vitest #vi-mock-hoisting` — if tests mock `node:fs`, use `vi.hoisted()` for any shared state.
  - FLASHCARD `#wiki #synthesis #corpus-shape` — real-corpus `status:` values are textual (`Draft`/`Ready`/`Active`), NOT emoji-prefixed. `status-of == 🟢` predicate (from 008-01 Story gate for "parent Epic 🟢") needs 008-01's YAML to use the real status text, not the emoji. Flag back to 008-01 if YAML uses 🟢 — reject at parse time.
  - FLASHCARD `#init #inject-claude-md #regex` does NOT apply here (no CLAUDE.md surgery).
  - Do NOT add a 7th predicate shape. If 008-01's criteria need one, push back on 008-01 — amend the YAML, not the evaluator.

### STORY-008-03 — `cleargate gate check|explain` CLI (L2, Wave D)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/gate.ts`
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — append a new `program.command('gate')` with two subcommands `check` and `explain` (pattern: mirror the `wiki` sub-program at cli.ts:51-103).
- **Schema / API**:
  ```ts
  export interface GateCliOptions { cwd?: string; now?: () => Date; stdout?: (s: string) => void; stderr?: (s: string) => void; exit?: (code: number) => never; gatesDocPath?: string; wikiIndexPath?: string; }
  export async function gateCheckHandler(file: string, opts: { verbose?: boolean; transition?: string }, cli?: GateCliOptions): Promise<void>
  export async function gateExplainHandler(file: string, cli?: GateCliOptions): Promise<void>
  ```
- **`check` flow**:
  1. Resolve `file`; parse frontmatter; detect work-item type from ID key (`proposal_id` → `proposal`; `epic_id` → `epic`; `story_id` → `story`; `cr_id` → `cr`; `bug_id` → `bug`).
  2. Load gates YAML via `js-yaml` (from `opts.gatesDocPath ?? .cleargate/knowledge/readiness-gates.md`); parse all fenced ```yaml blocks.
  3. Determine transition: if `--transition` provided, use it; else default to next-unpassed per `cached_gate_result` (if `pass === true` for N already, pick N+1; Epic has 2 transitions, others 1).
  4. For each criterion in the selected gate: call `evaluate(criterion.check, parsedDoc, {projectRoot, wikiIndexPath})`. Collect failures.
  5. Write `{pass, failing_criteria, last_gate_check}` via `writeCachedGate(file, result, { now })`.
  6. Severity routing: if gate.severity === 'advisory' → exit 0 regardless; if 'enforcing' → exit 0 on pass, exit 1 on any failing criterion.
  7. Output format:
     - Compact (default): one line header (`Gate: <type>.<transition> (<severity>)`) + one line per failing criterion (`❌ <id>: <detail>` for enforcing, `⚠ <id>: <detail> (advisory)` for advisory). On pass: `✅ <type>.<transition> passed (N criteria)`.
     - `-v`: append per-criterion expected-vs-actual block from `evaluate(...).detail`.
- **`explain` flow**:
  1. Resolve file; parse frontmatter; read `cached_gate_result`.
  2. If absent → `stdout('no gate check cached; run: cleargate gate check <file>')`; exit 0.
  3. Render ≤50-LLM-token summary: `<type>.<transition>: <pass|fail> at <last_gate_check>; <N> failing: <comma-joined ids>`.
  4. Read-only — do NOT call `evaluate` or `writeCachedGate`.
- **Test shape** (vitest, `gate.test.ts`):
  1. `check passing Epic → exit 0, cached_gate_result.pass = true`.
  2. `check failing enforcing Story → exit 1, stdout lists failing criteria, cached_gate_result.pass = false`.
  3. `check failing Proposal → exit 0 (advisory), stdout prefixes ⚠`.
  4. `check -v outputs full detail per failing criterion`.
  5. `check --transition override uses explicit transition even when already passed`.
  6. `check on file with no ID frontmatter errors with 'unable to detect work-item type'`.
  7. `explain is read-only — frontmatter bytes unchanged after call`.
  8. `explain with no cached result prints hint and exits 0`.
  9. `type-detection unit test: detects proposal/epic/story/cr/bug from each ID key`.
  10. `transition-inference: Epic with no cached gate → picks ready-for-decomposition; Epic already passed decomposition → picks ready-for-coding`.
- **Reuse (no duplication)**: `evaluate` + `parsePredicate` from 008-02; `readCachedGate` + `writeCachedGate` from 008-02; `parseFrontmatter` from wiki; `js-yaml` for reading 008-01's YAML blocks.
- **Gotchas**:
  - FLASHCARD `#cli #commander #optional-key` — `opts.transition` from Commander may be `undefined` — strip the key before passing through.
  - FLASHCARD `#cli #determinism #test-seam` — thread `now` through to 008-02's `writeCachedGate`.
  - Output is agent-facing; keep emoji to exactly `❌` / `⚠` / `✅`. Do NOT add color codes (no TTY check). ≤50-token budget for `explain` output is a hard cap — measure with a tokenizer fixture in tests.
  - Severity routing exit code test must NOT mock `process.exit` — inject via `cli.exit` test seam and assert integer.

### STORY-008-05 — `cleargate stamp-tokens <file>` CLI (L1, Wave C)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/stamp-tokens.ts`
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — append registration. Commander description: `"stamp draft_tokens from token-ledger into a work-item file (hook-invoked)"`.
- **Schema / API**:
  ```ts
  export interface StampTokensCliOptions { cwd?: string; now?: () => Date; stdout?: (s: string) => void; exit?: (code: number) => never; sprintRunsRoot?: string; }
  export async function stampTokensHandler(file: string, opts: { dryRun?: boolean }, cli?: StampTokensCliOptions): Promise<void>
  ```
- **Flow**:
  1. Resolve `file`; if path contains `/.cleargate/delivery/archive/` → exit 0 (freeze per 008-05 §1.2 "Archive rule"); print `[frozen] <file>`.
  2. Parse frontmatter; extract `work_item_id` from ID key (same mapping as 008-03: `proposal_id` etc.); fallback to filename regex `(STORY|EPIC|PROPOSAL|CR|BUG)-\d+(-\d+)?`.
  3. Call `readLedgerForWorkItem(work_item_id, { sprintRunsRoot })` → `SessionBucket[]`.
  4. Idempotency check: read existing `draft_tokens.last_stamp` from frontmatter; if all ledger rows have `ts < last_stamp` and the aggregate totals match, exit 0 with no write.
  5. Aggregate: `input = sum(buckets.totals.input)`; same for output/cache_read/cache_creation. `model` = comma-joined unique `row.model` values across all buckets' rows.
  6. Build `draft_tokens` object:
     ```
     draft_tokens:
       input: <sum> | null
       output: <sum> | null
       cache_creation: <sum> | null
       cache_read: <sum> | null
       model: "<comma-joined>" | null
       last_stamp: <ISO-8601>
       sessions:
         - { session: <id>, model, input, output, cache_read, cache_creation, ts: <first row ts> }
         - ...
     ```
  7. No ledger rows → set all number fields to `null` + add `stamp_error: "no ledger rows for work_item_id <id>"` at frontmatter top level.
  8. `--dry-run`: print planned diff; no write.
  9. Write via a new helper — **do NOT call `stampFrontmatter`** (that helper manages only `created_at`/`updated_at` fields). Implement a small `writeDraftTokens(absPath, tokens, stampError?)` that preserves key order + nested indent.
- **Test shape** (vitest, `stamp-tokens.test.ts`, 7 tests):
  1. `first stamp on file with no draft_tokens populates input/output/sessions[]` (ledger fixture with 2 sessions).
  2. `re-stamp with no new rows is byte-identical noop` (inject same `now`).
  3. `missing ledger rows writes draft_tokens: all-null + stamp_error` (work_item_id not in fixture).
  4. `archive path freezes — file bytes unchanged, exit 0`.
  5. `--dry-run prints diff but does not write`.
  6. `aggregation correctness: 3 rows across 2 sessions → sums match`.
  7. `model comma-join: 3 unique models across 2 sessions → "haiku, sonnet, opus"` (sorted alphabetically).
- **Reuse (no duplication)**: `readLedgerForWorkItem` from `cleargate-cli/src/lib/ledger-reader.ts` (M1); `parseFrontmatter` from wiki. Do NOT re-read ledger files directly.
- **Gotchas**:
  - FLASHCARD `#reporting #hooks #ledger` — pre-008-04 rows may have empty `work_item_id`; `ledger-reader` already normalizes by defaulting from `story_id` (verified `ledger-reader.ts:80-84`). No extra handling needed here.
  - FLASHCARD `#cli #determinism #test-seam` — inject `now` and `sprintRunsRoot`; without them the idempotency test is flaky.
  - Nested YAML in frontmatter (`sessions: - {...}`) is NOT supported by `parseFrontmatter` (verified `parse-frontmatter.ts:43-45`: throws on `{`). This is a **blocker** — either (a) extend `parseFrontmatter` to skip past nested blocks without parsing them, or (b) serialize `sessions` as a JSON string value (`sessions: "[{...}]"`). **Decision: go with (b)** — serialize the entire `draft_tokens` value as a single-line flow-style YAML string (`draft_tokens: {input: 100, output: 200, sessions: [...]}`) and document in 008-05 test case 1 that read-back happens via `JSON.parse(fm.draft_tokens.replace(/^\{/, '[').replace(/\}$/, ']'))` is NOT done — instead, use `js-yaml` to re-parse just the `draft_tokens` value when needed by 008-07. Flag this as an open decision below.
  - Archive check must be the first branch — before reading frontmatter — to avoid parse errors on archive files (which may have non-canonical shapes).

### STORY-009-03 — `cleargate init` snapshot + restore (L2, Wave A)

- **Files to touch**:
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/init.ts` — add two new steps to `initHandler`:
    - **Step 3.5 (before copyPayload)**: detect `.cleargate/.uninstalled` marker; prompt restore.
    - **Step 7 (after existing Step 6 bootstrap, renumber Step 7 "Done" → Step 8)**: write `.cleargate/.install-manifest.json` atomically.
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/prompts.ts` — minimal Y/n readline prompt helper (cited in story §3 as "existing"; verified absent — create here).
- **Schema / API**:
  ```ts
  // prompts.ts
  export interface PromptOptions { stdin?: NodeJS.ReadableStream; stdout?: (s: string) => void; }
  export async function promptYesNo(question: string, defaultYes: boolean, opts?: PromptOptions): Promise<boolean>;
  ```
  Extend `InitOptions` with:
  ```ts
  promptYesNo?: (q: string, defaultYes: boolean) => Promise<boolean>;  // test seam
  readInstallManifest?: () => ManifestFile;  // test seam around loadPackageManifest
  ```
- **Restore flow** (Step 3.5):
  1. Check `fs.existsSync(path.join(cwd, '.cleargate', '.uninstalled'))`.
  2. If present: read marker JSON; extract `uninstalled_at`, `prior_version`, `preserved[]`.
  3. Prompt `"Detected previous ClearGate install (uninstalled <ts>, prior version <v>). Restore preserved items? [Y/n]"` (default Y).
  4. On Y: iterate `preserved[]`; for each, if file exists on disk → log `[preserved] <path>`; if missing → log warning `[warn] preserved path missing on disk: <path>` (non-fatal).
  5. On N: log `[cleargate init] discarding preservation; preserved files untouched on disk`.
  6. Remove the `.uninstalled` marker regardless of Y/N AFTER Step 6 (bootstrap) completes successfully.
- **Snapshot flow** (Step 7):
  1. Load package manifest via `loadPackageManifest({ packageRoot: payloadDir })` (M1 lib).
  2. Build `snapshot: ManifestFile = { ...pkgManifest, installed_at: now() }`. Note: `ManifestFile` doesn't currently have an `installed_at` field; extend the interface in `manifest.ts` (additive — no breaking change). OR add it as an untyped extra field on a separate `InstallManifest` interface — **Developer picks; orchestrator blessing: extend `ManifestFile` with optional `installed_at?: string`**.
  3. Write atomically to `path.join(cwd, '.cleargate', '.install-manifest.json')` via the existing `writeAtomic` helper at `init.ts:87-92`.
- **Test shape** (extend `init.test.ts`, 5 new tests):
  1. `fresh init writes .install-manifest.json with correct cleargate_version + installed_at + files[]`.
  2. `init detects .uninstalled marker and prompts` (mock `promptYesNo` → true).
  3. `init with N choice proceeds without restore and still removes marker after init` (mock → false).
  4. `atomic snapshot write: if writeAtomic throws, final file does not exist` (mock `fs.renameSync` to throw).
  5. `missing preserved path logs warning but init continues` (preserved[] lists non-existent path).
- **Gotchas**:
  - FLASHCARD `#tsup #bundle #import-meta` — `payloadDir` test seam already threaded through `initHandler` (verified `init.ts:44-45`). Use `loadPackageManifest({ packageRoot: opts.payloadDir ?? resolveDefaultPayloadDir() })` — pass the SAME `payloadDir` used for `copyPayload` so snapshot matches shipped files.
  - `writeAtomic` at `init.ts:87-92` uses `.tmp.<ts>` suffix, not `.tmp` — reuse as-is; don't introduce a second atomic writer.
  - The `.uninstalled` marker format is NOT yet standardized (009-07 writes it in M3). For 009-03, assume marker is JSON with shape `{uninstalled_at: string, prior_version: string, preserved: string[]}`. Flag as cross-story risk.

### STORY-009-04 — `cleargate doctor` base + `--check-scaffold` (L2, Wave A, BLOCKS M3)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/doctor.ts`
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — append `program.command('doctor')` with modes dispatched via mutually-exclusive flags.
- **Schema / API** (the dispatcher shape 008-06 extends in M3):
  ```ts
  export interface DoctorCliOptions { cwd?: string; now?: () => Date; stdout?: (s: string) => void; stderr?: (s: string) => void; exit?: (code: number) => never; packageRoot?: string; }
  export interface DoctorFlags { checkScaffold?: boolean; sessionStartMode?: boolean; verbose?: boolean; /* reserved for M3 008-06: sessionStart, pricing */ }
  export async function doctorHandler(flags: DoctorFlags, cli?: DoctorCliOptions): Promise<void>;

  // Internal dispatcher — EXPORTED so 008-06 can add cases without re-editing the switch:
  export type DoctorMode = 'check-scaffold' | 'session-start' | 'pricing' | 'hook-health';
  export function selectMode(flags: DoctorFlags): DoctorMode;
  ```
- **Dispatcher contract for 008-06**:
  - `selectMode` throws if multiple mutually-exclusive flags are set (e.g. `--check-scaffold` + `--session-start`).
  - Default (no flag): return `'hook-health'` → prints minimal hook-config report.
  - 009-04 ships cases: `'check-scaffold'` + `'hook-health'`. 008-06 adds `'session-start'` + `'pricing'` cases by extending the switch in `doctorHandler` — not by redefining it.
  - **009-04 Developer note in code**: add a top-of-file comment citing "008-06 (M3) extends this file — do NOT re-create the command".
- **`--check-scaffold` flow**:
  1. Load package manifest + install snapshot via M1 `loadPackageManifest` + `loadInstallSnapshot`.
  2. For each manifest entry: compute `currentSha` via `computeCurrentSha`; classify via `classify(pkgSha, installSha, currentSha, tier)`.
  3. Build `DriftMap`; write atomically via `writeDriftState(projectRoot, driftMap)` + include `last_refreshed: now().toISOString()` at map root (extend `writeDriftState` contract — additive).
  4. Summary line: `"Scaffold drift: N user-modified, N upstream-changed, N both-changed, N clean"`. Append `"Run cleargate upgrade to review."` on any upstream-changed | both-changed.
  5. `user-artifact` tier skipped in summary + stdout listing (they return `untracked` per `manifest.ts:193-196`).
  6. `-v` / `--verbose`: one line per non-clean file: `<path>  <state>  (<installed>→<current> vs <package>)` with short-hashed SHAs (6 chars).
- **Daily-throttled refresh**:
  - Read existing `.drift-state.json.last_refreshed`; if `now - last_refreshed < 24h` AND `--session-start-mode` flag set → reuse cached map, skip SHA recompute.
  - `--session-start-mode` is a hidden flag used by the M3 session-start hook; 009-04 implements it.
- **Test shape** (vitest, `doctor.test.ts`, 8 tests):
  1. `clean repo: all manifest files match — summary reports 0/0/0/N`.
  2. `user-modified detected: edit a file post-install — shows in summary`.
  3. `upstream-changed surfaces "Run cleargate upgrade" pointer`.
  4. `user-artifact tier silently skipped — does NOT appear in drift output`.
  5. `daily-throttle: last_refreshed 10min ago + --session-start-mode → cache reused (no SHA compute)` — assert `computeCurrentSha` spy not called.
  6. `interactive mode bypasses throttle — last_refreshed recent but no flag → recomputes`.
  7. `-v emits per-file lines with short-hash triple`.
  8. `selectMode: check-scaffold + session-start combo throws` (dispatcher contract test).
- **Reuse (no duplication)**: all M1 manifest lib functions (`loadPackageManifest`, `loadInstallSnapshot`, `computeCurrentSha`, `classify`, `writeDriftState`). Do NOT re-implement SHA computation or drift classification.
- **Gotchas**:
  - FLASHCARD `#tsup #bundle #import-meta` — `packageRoot` test seam mandatory (threads through to `loadPackageManifest`).
  - `.drift-state.json` schema extension: M1's `writeDriftState` writes the `DriftMap` directly as the JSON root; 009-04 needs `last_refreshed` as a sibling. Either (a) wrap `{last_refreshed, drift: DriftMap}` — breaking change to the file shape OR (b) add `last_refreshed` key inside `DriftMap` (it's `{[path]: entry}`, so a key named `__last_refreshed` would collide with a real path `__last_refreshed`). **Decision: break M1's shape — wrap to `{last_refreshed: string, drift: DriftMap}`**. `writeDriftState` signature stays the same (second arg is `DriftMap`); extend with third arg `{lastRefreshed: string}` OR change signature. **Orchestrator decides — see open decisions.**
  - M3 STORY-008-06 Developer MUST read this file top-to-bottom before editing. Do NOT create a second `doctor.ts`.

### STORY-009-05 — `cleargate upgrade` three-way merge (L3, Wave C)

- **Files to touch**:
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/upgrade.ts`
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/merge-ui.ts` — diff renderer + 3-choice prompt.
  - Create `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/editor.ts` — spawn `$EDITOR` safely.
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — register `upgrade` command.
  - Edit `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/package.json` — add `"diff": "^5.2.0"` + `"@types/diff": "^5.2.0"` (only if 008-01 didn't already add `js-yaml` — coordinate dep-adds single-file single-commit).
- **Schema / API**:
  ```ts
  // merge-ui.ts
  export type MergeChoice = 'k' | 't' | 'e';
  export function renderInlineDiff(ours: string, theirs: string, path: string): string;
  export async function promptMergeChoice(opts: { path: string; state: DriftState; stdin?: NodeJS.ReadableStream; stdout?: (s: string) => void; }): Promise<MergeChoice>;
  // editor.ts
  export async function openInEditor(filePath: string, opts?: { editor?: string; env?: NodeJS.ProcessEnv }): Promise<{ exitCode: number }>;
  export function containsConflictMarkers(content: string): boolean;
  // upgrade.ts
  export interface UpgradeCliOptions { cwd?: string; now?: () => Date; stdout?: (s: string) => void; exit?: (code: number) => never; packageRoot?: string; promptMergeChoice?: typeof promptMergeChoice; openInEditor?: typeof openInEditor; }
  export async function upgradeHandler(flags: { dryRun?: boolean; yes?: boolean; only?: string }, cli?: UpgradeCliOptions): Promise<void>;
  ```
- **Flow** (per PROP-006 §2.6):
  1. Load package manifest + install snapshot; compute current SHAs; build DriftMap via M1's `classify`.
  2. Filter by `--only <tier>` if provided (e.g. `--only protocol`).
  3. For each tracked file, route by `entry.overwrite_policy`:
     - `always` → overwrite silently.
     - `never` → skip silently.
     - `merge-3way` (= "prompt-on-drift" in sprint/story text — confirm with M1 manifest schema; `manifest.ts:39` defines `'merge-3way'`) → interactive prompt.
     - `preserve` → skip silently.
  4. On each successful file: update `install-manifest.json` atomically — write new `sha256` for that file; keep rest of snapshot untouched.
  5. After all files: refresh `.drift-state.json` via the 009-04 path (or inline-call `writeDriftState`).
- **Three-way merge prompt**:
  - Print `[<tier>] <path>  state=<drift-state>`.
  - Print inline unified-diff (ours vs theirs) via `diff.createPatch(path, ours, theirs, 'installed', 'upstream')`.
  - Prompt `[k]eep mine / [t]ake theirs / [e]dit in $EDITOR:`.
  - `k`: file unchanged; snapshot.sha256 = current_sha.
  - `t`: overwrite file with package content; snapshot.sha256 = package_sha.
  - `e`: write a conflict-marker blob to `<file>.cleargate-merge`:
    ```
    <<<<<<< ours (installed)
    <ours>
    =======
    <theirs>
    >>>>>>> theirs (upstream)
    ```
    Open via `openInEditor`; on exit, read back; if `containsConflictMarkers` → error, leave merge file in place, do NOT update snapshot; else overwrite `<file>` with edited content, delete `.cleargate-merge`, update snapshot.sha256 to hash of edited content.
- **Incremental execution**: each file's snapshot write is independent; a crash/failure on file N does not roll back file N-1. Re-running `upgrade` picks up only files still drifted (re-classifies against the updated snapshot).
- **Test shape** (vitest, `upgrade.test.ts`, 10 tests):
  1. `--dry-run prints plan with 3 proposed actions; no disk changes; snapshot unchanged`.
  2. `keep-mine: file unchanged, snapshot.installed_sha = current_sha`.
  3. `take-theirs: file = package content, snapshot.installed_sha = package_sha`.
  4. `edit-in-editor happy path: markers resolved, snapshot = post-edit sha`.
  5. `edit-in-editor with unresolved markers: error, no snapshot update, .cleargate-merge persists`.
  6. `incremental survival: fail on file 2, file 1 persists, file 3 still offered`.
  7. `always-policy: silent overwrite, no prompt`.
  8. `never-policy: silent skip, no prompt`.
  9. `--yes auto-takes-theirs on all merge-3way files`.
  10. `--only protocol: only protocol-tier files considered`.
- **Reuse (no duplication)**: M1 `loadPackageManifest`, `loadInstallSnapshot`, `computeCurrentSha`, `classify`, `writeDriftState`, `hashNormalized`. `diff` npm package for the patch-renderer. Do NOT hand-roll unified-diff.
- **Gotchas**:
  - FLASHCARD `#cli #determinism #test-seam` — inject `promptMergeChoice` + `openInEditor` via `UpgradeCliOptions` so tests don't fork editors.
  - FLASHCARD `#cli #vitest #vi-mock-hoisting` — if tests mock `node:child_process` for editor-spawn, use `vi.hoisted()`.
  - FLASHCARD `#tsup #cjs #esm` — `diff` package exports CJS + ESM; verify ESM import works under tsup bundle; if not, import from `diff/lib/*.js` explicitly.
  - `openInEditor` uses `spawn` with `stdio: 'inherit'`; DO NOT use `execSync` (blocks event loop, breaks tests).
  - Snapshot update after each file MUST be atomic (reuse `writeAtomic` pattern from init.ts:87-92) — partial writes corrupt `.install-manifest.json` and make upgrade non-resumable.
  - `$EDITOR` may be unset in CI — the test seam `openInEditor` avoids the real spawn; in production, if env var absent, error with `"$EDITOR not set; cannot [e]dit option"`.

---

## Cross-story shared concerns

### 1. `doctor` subcommand dispatcher design (009-04 ships, 008-06 extends)

STORY-009-04 exports `selectMode(flags: DoctorFlags): DoctorMode` and a top-level switch in `doctorHandler`. STORY-008-06 (M3) adds two cases (`'session-start'`, `'pricing'`) and two new flags (`sessionStart`, `pricing`) to the same `DoctorFlags` interface without renaming or reshaping the 9 existing keys. 009-04 Developer must add a comment block at the top of `doctor.ts` naming 008-06 as the extender and listing the mutually-exclusive flag rule.

### 2. New npm deps — single dep-add per story

- `js-yaml` + `@types/js-yaml` — added by STORY-008-01 (first M2 story to need YAML parse).
- `diff` + `@types/diff` — added by STORY-009-05.
- Orchestrator must NOT spawn 008-01 and 009-05 in parallel if both need to edit `package.json` in the same second. Dep-add commits are single-file; subsequent stories pick up the dep via lockfile.

### 3. `cli.ts` append contention

Five M2 stories append a new `program.command(...)` to `cleargate-cli/src/cli.ts` (001-05 replaces the stub; 008-03, 008-05, 009-04, 009-05 append). Each Developer must Read the file before Write and append their block BEFORE the final `void program.parseAsync(process.argv);` line (cli.ts:110). Orchestrator should serialize cli.ts edits across these five stories — two Developers editing cli.ts simultaneously = merge conflict guaranteed.

### 4. Work-item type detection

008-03 and 008-05 both map frontmatter ID keys to work-item types (`proposal_id` → `proposal`, etc.). Extract to a shared util `cleargate-cli/src/lib/work-item-type.ts` in the first story that needs it (008-02 or 008-03, orchestrator picks). Both stories must import from the same module to avoid divergence.

### 5. Frontmatter serializer reuse

008-02's `writeCachedGate` and 008-05's `writeDraftTokens` both need YAML serialization that preserves key order. 008-02 extracts the helper first (either as `serializeFrontmatter` in `frontmatter-yaml.ts` or inline in `frontmatter-cache.ts`); 008-05 imports from wherever 008-02 lands it. Do NOT re-implement in 008-05.

### 6. `.uninstalled` marker format

STORY-009-03 (M2) reads the marker; STORY-009-07 (M3) writes it. 009-03 assumes shape `{uninstalled_at: string, prior_version: string, preserved: string[]}`. If 009-07 diverges, 009-03 test fixture must update. Flag for orchestrator to re-verify when 009-07 plan goes through.

### 7. `.drift-state.json` schema

M1 `writeDriftState` writes `DriftMap` (flat `{path: entry}`) at the JSON root. 009-04 needs `last_refreshed` at the root → requires wrapping to `{last_refreshed, drift}`. This breaks 009-01's contract. Either 009-04 extends the M1 lib signature OR 009-04 writes a SECOND file `.drift-state-meta.json` carrying only `last_refreshed`. See Open Decisions.

---

## M2 exit criteria (copied from sprint file)

- [ ] All new CLIs dispatch via `cleargate --help` — covered by 001-05, 008-03, 008-05, 009-04, 009-05 cli.ts registration tests.
- [ ] `cleargate doctor` base command + subcommand dispatcher landed — covered by 009-04.
- [ ] `cleargate gate check` runs against this repo's existing pending-sync items (dogfood dry-run) — 008-03 test includes a smoke test against `.cleargate/delivery/pending-sync/*.md`; expected: many items fail `last_gate_check-is-null` initially, which informs 008-07 initial-migration handling.
- [ ] MCP push writes the new frontmatter field and a cleargate-side integration test proves it roundtrips — 001-06 test #1 + #2.

## Open decisions for the orchestrator

1. **MCP cross-repo (Option A vs B)** — **architect picks Option B** (port helper into `mcp/src/utils/`). Orchestrator confirms before 001-06 spawns.
2. **`.drift-state.json` shape** — 009-04 needs `last_refreshed` at root; M1's `writeDriftState` writes `DriftMap` directly. Options: (a) extend `writeDriftState` signature to `(projectRoot, {lastRefreshed, drift})` — additive but breaks M1's one-arg contract; (b) wrap all drift entries in `{drift: DriftMap, last_refreshed}` at write time, keep M1 signature; (c) write separate `.drift-state-meta.json`. **Recommendation: (a)** — cleanest single-source-of-truth.
3. **Nested YAML in `draft_tokens` frontmatter (008-05)** — `parseFrontmatter` at `parse-frontmatter.ts:43-45` throws on `{`. Options: (a) extend `parseFrontmatter` to skip (but not parse) nested values; (b) serialize `draft_tokens` as a single quoted JSON string (`draft_tokens: '{"input":100,...}'`); (c) flat-key expansion (`draft_tokens.input: 100`, `draft_tokens.output: 200`, `draft_tokens.sessions.0.input: ...`). **Recommendation: (a)** — future-proofs predicate evaluator against any nested YAML encountered in the wild.
4. **Shared frontmatter-serializer location** — 008-02 and 008-05 both need it. Extract into `cleargate-cli/src/lib/frontmatter-yaml.ts`? Or inline-duplicate? **Recommendation: extract in 008-02, import in 008-05.**
5. **Work-item type detection helper location** — 008-03 and 008-05 both need the `id → type` map. Extract into `cleargate-cli/src/lib/work-item-type.ts` in 008-02 or 008-03? **Recommendation: extract in 008-03, import in 008-05 (after its Wave-C spawn sees 008-03 exists).**
6. **`cli.ts` append serialization** — confirmed as sequential, not parallel. Orchestrator must not spawn two cli.ts-editing Developers simultaneously. Safe order: 009-04 first (biggest shape — dispatcher), then 001-05 (replaces stub), then 008-03, 008-05, 009-05 can be any order.
