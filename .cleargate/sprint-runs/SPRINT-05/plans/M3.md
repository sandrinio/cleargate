role: architect

# SPRINT-05 · M3 Plan — Integration + Protocol (4 stories, SERIALIZED)

## Summary

M3 stitches the M1 libs + M2 CLIs into live hooks, templates, protocol, and the most-destructive CLI (`uninstall`). Four stories run **strictly serialized** on a single Developer subagent at a time — they all edit the same shared files (`.claude/settings.json` + mirror, `cleargate-protocol.md` + mirror, all 7 templates + mirror, plus `cli.ts`, `doctor.ts`, `wiki-lint.ts`). Running them in parallel would guarantee merge conflicts on the shared-file surface.

Dogfood-impact warning: the moment STORY-008-06 lands, every future `Edit` / `Write` to `.cleargate/delivery/**` in this repo triggers the new `stamp-and-gate.sh` chain, and every new Claude Code session gets SessionStart context injection. A broken chain silently breaks all subsequent story drafts; a buggy SessionStart summary poisons every downstream subagent. The SessionStart registration is gated to be the **last** hunk of STORY-008-06's commit; any QA failure after that hunk rolls back via `git revert <008-06-SHA>`.

## Serialization order (HARD — no parallelism)

1. **STORY-008-06** — `stamp-and-gate.sh` + `session-start.sh` hooks + `cleargate doctor --session-start` + `--pricing` modes + `pricing.ts` lib + settings.json surgical merge (replaces SPRINT-04's inline `wiki ingest` PostToolUse entry).
2. **STORY-008-07** — `draft_tokens` + `cached_gate_result` stubs added to all 7 templates (live + mirror) + Protocol §12 appended + `wiki-lint.ts` extension refusing 🟢-candidates with failing/stale gate.
3. **STORY-009-07** — `cleargate uninstall` command (preservation-first, typed confirmation, `--dry-run`, `--preserve`/`--remove`/`--yes`/`--path`/`--force`, `.uninstalled` marker).
4. **STORY-009-08** — Protocol §13 appended (6 subsections). Ships LAST because §13's numbering depends on §12 already existing.

Each Developer reads ONLY its story section below, plus its story file. Prior story must be QA-approved + committed before the next developer spawns.

---

## STORY-008-06 · Hooks + Doctor Extensions

**Story file:** `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/delivery/pending-sync/STORY-008-06_Hooks_And_Doctor.md`

### Files to create

- `/Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/stamp-and-gate.sh` (executable, `chmod +x`)
- `/Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/session-start.sh` (executable)
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/stamp-and-gate.sh` (byte-identical mirror)
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/session-start.sh` (byte-identical mirror)
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/lib/pricing.ts` (USD pricing table + `computeUsd(draftTokens, model): {usd, breakdown}`)

### Files to modify

- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/doctor.ts` — replace the two `case 'session-start':` / `case 'pricing':` stubs (lines 292–296) with full implementations. Dispatcher contract is frozen at `selectMode()` (lines 74–91); do NOT re-edit the switch shape, only the two stub cases.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — extend the existing `doctor` block (lines 146–167): add `--session-start` + `--pricing <file>` commander options; forward to `doctorHandler({sessionStart, pricing, ...})`. Remove the "STORY-008-06 (M3) adds..." help-note since this ships it.
- `/Users/ssuladze/Documents/Dev/ClearGate/.claude/settings.json` — **surgical merge** via `settings-json-surgery.ts`:
  1. `removeClearGateHooks(settings)` — this drops the current SPRINT-04 inline `wiki ingest` PostToolUse entry (line 19 of `.claude/settings.json` matches `isClearGateCommand` via the `wiki ingest` substring test at `settings-json-surgery.ts:33`).
  2. Insert the two new hook entries per Epic §4 schema. SubagentStop `token-ledger.sh` entry is preserved untouched.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/settings.json` — same surgical transform, using absolute `${CLAUDE_PROJECT_DIR}/…` paths (matches the mirror's existing convention at line 8 for token-ledger.sh).

### Hook contracts

**`stamp-and-gate.sh`** (PostToolUse):
```
#!/usr/bin/env bash
set -u
REPO_ROOT="/Users/ssuladze/Documents/Dev/ClearGate"   # live; mirror uses ${CLAUDE_PROJECT_DIR}
LOG="${REPO_ROOT}/.cleargate/hook-log/gate-check.log"
mkdir -p "$(dirname "$LOG")"
FILE=$(jq -r '.tool_input.file_path' 2>/dev/null || echo "")
[ -z "$FILE" ] && exit 0
case "$FILE" in *.cleargate/delivery/*) : ;; *) exit 0 ;; esac
TS=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
# Ordered chain — stamp MUST precede gate (gate may read draft_tokens)
node "${REPO_ROOT}/cleargate-cli/dist/cli.js" stamp-tokens "$FILE" >>"$LOG" 2>&1
SR1=$?
node "${REPO_ROOT}/cleargate-cli/dist/cli.js" gate check "$FILE" >>"$LOG" 2>&1
SR2=$?
node "${REPO_ROOT}/cleargate-cli/dist/cli.js" wiki ingest "$FILE" >>"$LOG" 2>&1
SR3=$?
echo "[$TS] stamp=$SR1 gate=$SR2 ingest=$SR3 file=$FILE" >>"$LOG"
exit 0   # ALWAYS 0 — severity enforcement is at wiki lint, not hook
```

**`session-start.sh`** (SessionStart):
```
#!/usr/bin/env bash
set -u
REPO_ROOT="/Users/ssuladze/Documents/Dev/ClearGate"
node "${REPO_ROOT}/cleargate-cli/dist/cli.js" doctor --session-start 2>/dev/null || true
```

Hook stdout becomes session context; cap is enforced inside `doctor --session-start` (≤10 items + overflow pointer + ≤100 LLM-tokens target).

**settings.json hunks** (both live + mirror):
```json
{
  "hooks": {
    "SubagentStop": [ /* existing token-ledger.sh — untouched */ ],
    "PostToolUse": [{
      "matcher": "Edit|Write",
      "hooks": [{
        "type": "command",
        "command": "/Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/stamp-and-gate.sh"
      }]
    }],
    "SessionStart": [{
      "hooks": [{
        "type": "command",
        "command": "/Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/session-start.sh"
      }]
    }]
  }
}
```
Mirror uses `${CLAUDE_PROJECT_DIR}/.claude/hooks/…` instead of the live absolute path. Keep `matcher: "Edit|Write"` on the PostToolUse outer entry — existing SPRINT-04 convention + stamp-and-gate.sh itself filters path.

### doctor.ts extension blueprint

Replace the stub block at `doctor.ts:292-296`:

```ts
case 'session-start':
  await runSessionStart(cwd, stdout);
  break;
case 'pricing':
  await runPricing(flags.pricingFile ?? '', cwd, stdout, stderr, exit);
  break;
```

Add new flag key `pricingFile?: string` to `DoctorFlags` (after line 59). `selectMode` already accepts `pricing: true`; the Commander action in cli.ts passes both `pricing: !!opts.pricing` and `pricingFile: opts.pricing` (Commander `.option('--pricing <file>')` yields a string or `undefined`).

`runSessionStart(cwd, stdout)`:
- Read `<cwd>/.cleargate/delivery/pending-sync/*.md`.
- For each, `parseFrontmatter(raw)` (from `wiki/parse-frontmatter.js`); extract `cached_gate_result` via `readCachedGate` equivalent inline parse (or reuse `readCachedGate` by path).
- Collect ids where `cached_gate_result.pass === false`.
- Render: first 10 `"<ID>: <first failing criterion id>"`, overflow → `"…and N more — run cleargate doctor for full list"`.
- Cap whole output ≤ 100 LLM tokens: budget via character count (≤ 400 chars is a safe proxy; tighten if overflow).

`runPricing(filePath, cwd, stdout, stderr, exit)`:
- Resolve `filePath` relative to `cwd`.
- Read + parseFrontmatter → pluck `draft_tokens` (shape from `stamp-tokens.ts:42-50`: `{input, output, cache_creation, cache_read, model, sessions[]}`).
- `import { computeUsd } from '../lib/pricing.js'` → call with the populated token counts + model.
- Emit one line: `"<file>: <model> — input:<N> output:<N> cache_read:<N> cache_creation:<N> ≈ $<USD>"`.
- Null tokens → emit `"draft_tokens unpopulated — run cleargate stamp-tokens first"` and `exit(1)`.

`pricing.ts`:
- Export `PRICING_TABLE: Record<string, {input: number; output: number; cache_read: number; cache_creation: number}>` (USD per 1M tokens).
- Seed with current Claude pricing for `claude-opus-4-7`, `claude-sonnet-4-5`, `claude-haiku-4-5` (use `PROP-005 §2.6` numbers; if absent, use Anthropic public pricing as of 2026-04-19).
- Export `computeUsd(draftTokens, modelOverride?): {usd: number; unknownModel: boolean}`. Unknown model → `{usd: 0, unknownModel: true}`; caller emits a warning.
- No network. No config file. Numbers live in source.

### Test shape (vitest, `cleargate-cli/test/commands/`)

Create `test/commands/doctor-session-start.test.ts` + `test/commands/doctor-pricing.test.ts` + `test/hooks/stamp-and-gate.test.ts` + `test/lib/pricing.test.ts`.

Named cases:
1. `doctor --session-start` with 3 fixture items (2 failing, 1 passing) → stdout names both failing ids.
2. `doctor --session-start` with 15 failing items → stdout shows 10 + "…and 5 more — run cleargate doctor for full list".
3. `doctor --session-start` with zero failing → stdout is empty or single "no blocked items" line (≤ 100 tokens).
4. `doctor --pricing <fixture>` with populated `draft_tokens.model = "claude-opus-4-7"` → USD string present, nonzero.
5. `doctor --pricing <fixture>` with null draft_tokens → exit(1), stderr cites "run cleargate stamp-tokens first".
6. `doctor --pricing <fixture>` with unknown model → warn + USD $0.00.
7. `doctor` with two mutually-exclusive flags (`--session-start --pricing foo.md`) → `selectMode` throws, exit(1). (Existing dispatcher test pattern; extend.)
8. Shell: feed stamp-and-gate.sh a json `{"tool_input":{"file_path":"/tmp/foo/.cleargate/delivery/pending-sync/X.md"}}`, stub the three `node cleargate-cli/dist/cli.js` calls via a `PATH` override, assert log line has `stamp=0 gate=0 ingest=0`.
9. Shell: path NOT under `.cleargate/delivery/` → exit 0, no log line, no calls.
10. Shell: gate returns non-zero → stamp-and-gate.sh still exits 0 + log records `gate=<nonzero>`.
11. `pricing.ts`: round-trip known model at known counts hits hand-computed USD; unknown model returns `{unknownModel: true, usd: 0}`.

### Mirror-discipline checks (mandatory post-edit)

```
diff /Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/stamp-and-gate.sh /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/stamp-and-gate.sh
# expected: diff only on the REPO_ROOT line (live uses absolute, mirror uses ${CLAUDE_PROJECT_DIR}) — ALL OTHER LINES IDENTICAL
diff /Users/ssuladze/Documents/Dev/ClearGate/.claude/hooks/session-start.sh /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/hooks/session-start.sh
# same divergence pattern
diff <(jq -S . /Users/ssuladze/Documents/Dev/ClearGate/.claude/settings.json) <(jq -S . /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.claude/settings.json)
# expected: only the command-path prefix differs (absolute vs ${CLAUDE_PROJECT_DIR}); hook SHAPE + count identical
```

### Risks / gotchas

- **SPRINT-04 legacy hook removal** — current `.claude/settings.json:13-23` has an inline `case "$FILE" in ... npx … wiki ingest …` command. `isClearGateCommand` at `settings-json-surgery.ts:33` matches on the `'wiki ingest'` substring → `removeClearGateHooks` drops it cleanly. **Verify via test** that calling `removeClearGateHooks` on the current live settings.json strips exactly that one PostToolUse entry and leaves SubagentStop untouched.
- **FLASHCARD `#hooks #protocol`** — hook stdin is the JSON `{tool_input:{file_path:…}}`. No `pathPattern` field, no `$CLAUDE_TOOL_FILE_PATH` env. Our stamp-and-gate.sh uses `jq -r '.tool_input.file_path'` on stdin per this flashcard.
- **FLASHCARD `#wiki #protocol #mirror`** — post-edit mirror diff is MANDATORY. Two-shell-script divergence is intentional (REPO_ROOT absolute vs `${CLAUDE_PROJECT_DIR}`); every other byte must match.
- **FLASHCARD `#cli #commander #optional-key`** — Commander action handler receives `opts.pricing` as string-or-undefined; pass `pricingFile: opts.pricing` directly and guard `runPricing('', …)` with a clear "missing --pricing value" error + exit(1).
- **FLASHCARD `#tsup #cjs #esm`** — no top-level await in doctor.ts. Keep existing `export async function doctorHandler` structure.
- **Dist-path assumption** — hooks call `node <REPO_ROOT>/cleargate-cli/dist/cli.js`. That path is frozen; the tsup bundle MUST exist before these hooks fire. If running Developer subagent BEFORE `npm run build`, the first PostToolUse fires → `dist/cli.js not found` → log records `stamp=127`. **Developer must `npm run build` in cleargate-cli BEFORE committing** so the next subagent's writes don't tank. Add this to the commit checklist.
- **SessionStart registration is the LAST hunk of this commit** — order the settings.json merge so SessionStart is appended last. If QA kicks back, `git revert <sha>` is the escape hatch. Post-commit QA step: close + reopen a fresh Claude Code session and visually inspect the injected context.
- **`cleargate doctor --help` must list all modes after M3 closes** (per sprint-file Risk row 4). Developer runs `node dist/cli.js doctor --help` after build and confirms `--session-start` + `--pricing <file>` appear.

### Commit checklist

- [ ] `npm run build` in `cleargate-cli/` — ensures `dist/cli.js` exists for hooks to call.
- [ ] `npm run typecheck` + `npm test` clean in `cleargate-cli/`.
- [ ] `cleargate doctor --help` shows `--session-start` + `--pricing <file>`.
- [ ] Manual: write a throwaway `.cleargate/delivery/pending-sync/TEST-999.md` → `.cleargate/hook-log/gate-check.log` has a new `stamp=0 gate=? ingest=0 file=…TEST-999.md` line within 2s.
- [ ] Delete the throwaway file (do NOT commit it).
- [ ] Mirror diff checks pass.
- [ ] Commit `feat(EPIC-008): STORY-008-06 PostToolUse+SessionStart hooks + doctor --session-start/--pricing`.

---

## STORY-008-07 · Templates + Protocol §12 + Wiki-Lint

**Story file:** `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/delivery/pending-sync/STORY-008-07_Templates_Protocol_WikiLint.md`

### Files to modify (ALL mirrored — 008-07 touches 18 files total)

Templates (live + mirror — **14 files**):
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/{proposal,epic,story,CR,Bug,initiative,Sprint Plan Template}.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/{proposal,epic,story,CR,Bug,initiative,Sprint Plan Template}.md`

Protocol (live + mirror — **2 files**):
- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`

Wiki lint (**2 files**):
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/wiki/lint-checks.ts` — add two new pure-function checks.
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/wiki-lint.ts` — wire the new checks into the per-page loop (around lines 67–87).

### Template stubs — Read-before-Write mandatory

Every template already has M1's `created_at` / `updated_at` / `created_at_version` / `updated_at_version` / `server_pushed_at_version` fields (verified: grep result above shows them on all 5 write-templates; `initiative.md` + `Sprint Plan Template.md` have 4 of 5, which matches M1's "no `server_pushed_at_version` on non-write templates" rule).

**Do not clobber M1.** Insert the new stubs AFTER the existing metadata block, BEFORE the closing `---`. Exact patch pattern (apply to each template):

```yaml
# … existing metadata ends with (e.g., epic.md line 29) …
server_pushed_at_version: null
draft_tokens:
  input: null
  output: null
  cache_read: null
  cache_creation: null
  model: null
  sessions: []
cached_gate_result:
  pass: null
  failing_criteria: []
  last_gate_check: null
---
```

For `initiative.md` + `Sprint Plan Template.md` (no `server_pushed_at_version`), insert after `updated_at_version` line. Stub values are ALL `null` / `[]` — Developer must NOT stamp real timestamps here (templates are static scaffolding).

Per-template line anchors (verified via grep):
- `epic.md` line 29 `server_pushed_at_version: null` → insert after.
- `proposal.md` line 19 → insert after.
- `story.md` line 33 → insert after.
- `CR.md` line 24 → insert after.
- `Bug.md` line 26 → insert after.
- `initiative.md` line 17 `created_at_version:` → insert after `updated_at_version:` (locate with grep; one line below line 17).
- `Sprint Plan Template.md` line 19 → insert after `updated_at_version:` (one below).

### Protocol §12 append

`cleargate-protocol.md` live ends at line 426 (verified: file is 426 lines; last heading is `## 11. Document Metadata Lifecycle` at line 351, last subsection §11.6 ends at 426). Append at end of file. Section spec per story §1.2:

```markdown
---

## 12. Token Cost Stamping & Readiness Gates

### §12.1 Overview
Two-capability bundle: (1) `draft_tokens` frontmatter stamp populated by a PostToolUse hook from the sprint token ledger; (2) closed-set predicate engine + `cleargate gate check` CLI writing `cached_gate_result` into frontmatter, blocking wiki-lint on enforcing types (Epic/Story/CR/Bug), advising on Proposals.

### §12.2 Token stamp semantics
- Idempotent within a session (re-stamp = no-op when last_stamp + totals unchanged).
- Accumulative across sessions: `sessions[]` gains one entry per session; top-level totals are sums; `model` is comma-joined across distinct values.
- Missing ledger row → `draft_tokens:{…null…, stamp_error:"<reason>"}` — never fabricate.
- Archive-path stamping is a no-op (freeze-on-archive).
- Sprint files record only planning-phase tokens; story tokens attribute to their own files (no double-count).

### §12.3 Readiness gates
- Central definitions: `.cleargate/knowledge/readiness-gates.md` keyed by `{work_item_type, transition}`.
- Predicates are a CLOSED set (6 shapes): `frontmatter(...)`, `body contains`, `section(N) has count`, `file-exists`, `link-target-exists`, `status-of`. No shell-out, no network.
- Severity: Proposal = advisory (exit 0, records `pass:false` without blocking). Epic/Story/CR/Bug = enforcing (exit non-zero at CLI; wiki lint refuses).

### §12.4 Enforcement points
- v1: `wiki lint` only. MCP-side `push_item` enforcement is deferred post-PROP-007.
- Staleness: `cached_gate_result.last_gate_check < updated_at` → lint error for ALL types (catches silent hook failures).

### §12.5 Hook lifecycle
- PostToolUse `stamp-and-gate.sh` chains `stamp-tokens → gate check → wiki ingest` on every Write/Edit under `.cleargate/delivery/**`. Exit always 0.
- SessionStart `session-start.sh` pipes `cleargate doctor --session-start` (≤100 LLM-tokens, ≤10 items + overflow pointer) into context.
- Every invocation logs to `.cleargate/hook-log/gate-check.log`; `cleargate doctor` surfaces last-24h failures.

### §12.6 Cross-references
- §4 Phase Gates: "Gate 2 (Ambiguity) is machine-checked via `cleargate gate check`; see §12."
- §10.8 Wiki-lint enforcement: extended by the gate-check hook; staleness check added per §12.4.
```

Also apply the **two in-section cross-references** per story §1.2:
1. In §4 "Phase Gates" (lines 85–112) — add a terminal one-liner: `> Gate 2 (Ambiguity) is machine-checked via \`cleargate gate check\`; see §12.`
2. In §10.8 — add one sentence: `The gate-check hook (§12.5) runs before ingest; staleness (§12.4) is a lint error.`

Rule: APPEND-ONLY to existing sections. Do not restructure §4 or §10. Cross-refs are one line each, at the end of their respective section.

### Wiki-lint extension (`lint-checks.ts` + `wiki-lint.ts`)

Add two new pure-function checks in `lint-checks.ts`:

```ts
export function checkGateFailure(page: LoadedWikiPage, rawRoot: string): LintFinding | null
export function checkGateStaleness(page: LoadedWikiPage, rawRoot: string): LintFinding | null
```

Logic:
- Both read the **raw** work-item file (not the wiki page) via `page.page.raw_path`.
- `parseFrontmatter(raw)` → extract `cached_gate_result` + `updated_at`.
- `checkGateFailure`: if `cached_gate_result.pass === false` AND raw frontmatter declares `status` in `{Ready, Active}` or `ambiguity` === `"🟢 Low"` AND work-item type is Epic/Story/CR/Bug (detect via `detectWorkItemTypeFromFm` in `work-item-type.ts`) → emit `{category: 'gate-failure', line: 'gate-failure: <raw_path> failed criteria: <ids>'}`. Proposal / Sprint / Initiative → return null.
- `checkGateStaleness`: if `cached_gate_result.last_gate_check` exists AND `< updated_at` (ISO-8601 lexical compare) → emit `{category: 'gate-stale', line: 'gate-stale: <raw_path> last_gate_check=<X> < updated_at=<Y>'}`. Applies to ALL types.
- Null `cached_gate_result.pass` → both checks return null (drafts pre-migration).
- Missing `updated_at` → staleness returns null.

Wire into `wiki-lint.ts` per-page loop (after existing `checkExcludedPathIngested` at line 86):
```ts
const gateFail = checkGateFailure(page, repoRoot);
if (gateFail) findings.push(gateFail);
const gateStale = checkGateStaleness(page, repoRoot);
if (gateStale) findings.push(gateStale);
```

### Test shape

Unit tests at `cleargate-cli/test/wiki/lint-checks-gate.test.ts`:
1. **Enforcing fail** — Epic raw file with `status:"Ready"` + `cached_gate_result:{pass:false, failing_criteria:[{id:"no-tbds",...}]}` → `checkGateFailure` emits `gate-failure: … failed criteria: no-tbds`.
2. **Advisory tolerated** — Proposal raw file with `status:"Ready"` + `pass:false` → `checkGateFailure` returns null.
3. **Null pass ignored** — Epic raw file with `pass:null` → null return (drafts pre-migration).
4. **Staleness** — `last_gate_check = "2026-04-18T…"` + `updated_at = "2026-04-19T…"` → emits `gate-stale: …`.
5. **Fresh** — `last_gate_check ≥ updated_at` → null.

Template grep test at `test/scripts/template-stubs.test.ts`:
6. For each of 7 live + 7 mirror templates: assert frontmatter contains `draft_tokens:` AND `cached_gate_result:` keys.

Protocol grep test at `test/scripts/protocol-section-12.test.ts`:
7. Assert `## 12. Token Cost Stamping & Readiness Gates` present, + `### §12.1` … `### §12.5` present, + live/mirror diff empty.

### Mirror-discipline checks

```
diff -r /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/ /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/templates/
# expected: EMPTY
diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md
# expected: EMPTY
```

### Risks / gotchas

- **Read-before-Write the templates** — M1 already added 5 metadata fields at specific line positions. If Developer Writes the entire template instead of patching, M1's fields get clobbered. **Mandatory: Read each template, append new keys after the existing metadata block, Write back the full file preserving M1's fields.**
- **Templates are additive only** — the stub values are ALL null / empty arrays. Do not put real timestamps in templates; they're scaffolding.
- **`Sprint Plan Template.md`** — path has a SPACE; shell glob quoting matters. Verified both live + mirror have it.
- **Protocol section numbering** — §11 is already present at line 351. §12 must be appended AT END (after line 426). Do NOT insert above §11.6 "Stale Detection Threshold" (lines 415–426) — that's §11's last subsection, not a §12 slot.
- **FLASHCARD `#wiki #schema #lint`** — WikiPage schema has no generic `cached_gate_result` field. The new lint checks MUST re-read the RAW work-item file (via `page.page.raw_path`), not the wiki page. Pattern lifted from `checkInvalidatedCitations` at `lint-checks.ts:204-255` (re-parses raw via `parseFrontmatter`).
- **FLASHCARD `#gates #predicate #yaml`** — `readiness-gates.md` unwrap `[0]` on yaml.load() — does not apply here (we're READING `cached_gate_result` from individual work items, not gate definitions).
- **Wiki-lint category names** — must be new strings (`gate-failure`, `gate-stale`); do NOT reuse existing categories listed at `lint-checks.ts:7` (those are 8 SPRINT-04 categories).
- **Existing repo will have pre-migration items** — running `cleargate wiki lint` on this repo post-landing WILL emit `gate-stale` lines for every existing pending-sync item (their `last_gate_check` is null / missing). Per sprint-file Constraints: "expected: initial migration pass". DoD accepts this; a second pass after 008-06's hook stamps them will clean up.

### Commit checklist

- [ ] `npm run typecheck` + `npm test` clean in `cleargate-cli/`.
- [ ] `diff -r .cleargate/templates/ cleargate-planning/.cleargate/templates/` empty.
- [ ] Protocol live + mirror diff empty.
- [ ] `grep "^## 12\." .cleargate/knowledge/cleargate-protocol.md` returns one match.
- [ ] Commit `feat(EPIC-008): STORY-008-07 template stubs + protocol §12 + wiki-lint gate checks`.

---

## STORY-009-07 · `cleargate uninstall`

**Story file:** `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/delivery/pending-sync/STORY-009-07_Uninstall_Command.md`

### Files to create

- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/commands/uninstall.ts` (verified missing; new file).

### Files to modify

- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-cli/src/cli.ts` — register `program.command('uninstall')` with options `--dry-run`, `--preserve <tiers>`, `--remove <tiers>`, `--yes`, `--path <dir>`, `--force`. Wire to `uninstallHandler`. Add after the `upgrade` block (line ~187) before `void program.parseAsync`.

### Dependencies (from M1/M2 — verified present)

- `src/lib/manifest.ts` — `loadInstallSnapshot(cwd)` returns snapshot or null; snapshot's `files[]` are the authoritative list.
- `src/lib/claude-md-surgery.ts` — `removeBlock(content)` at line 38 + marker presence checks.
- `src/lib/settings-json-surgery.ts` — `removeClearGateHooks(settings)`. Already handles the full ClearGate hook set (token-ledger, stamp-and-gate, session-start, wiki-ingest legacy).

### Signature

```ts
export interface UninstallOptions {
  cwd?: string;
  path?: string;              // resolved target dir (defaults to cwd)
  dryRun?: boolean;
  yes?: boolean;              // skip typed confirmation
  force?: boolean;            // override uncommitted-changes refusal
  preserve?: string[];        // tier ids
  remove?: string[];          // tier ids
  stdout?: (s: string) => void;
  stderr?: (s: string) => void;
  exit?: (code: number) => never;
  promptName?: () => Promise<string>;      // test seam
  promptYesNo?: (q: string) => Promise<boolean>;  // test seam
  now?: () => Date;
  git?: (args: string[]) => { stdout: string; code: number };  // test seam for `git status --porcelain`
}
export async function uninstallHandler(opts: UninstallOptions): Promise<void>;
```

### Flow (implement verbatim per story §1.2)

1. **Resolve target.** `target = opts.path ? path.resolve(opts.path) : process.cwd()`. Require `<target>/.cleargate/.install-manifest.json`. Missing → `stdout("no ClearGate install detected at <target>")` + `exit(0)`.
2. **Idempotency short-circuit.** If `<target>/.cleargate/.uninstalled` exists AND `.install-manifest.json` does NOT → `stdout("already uninstalled")` + `exit(0)`.
3. **Uncommitted-changes check.** Run `git -C <target> status --porcelain` on files within the install manifest. If non-empty AND `!opts.force` → `stderr("Uncommitted changes to tracked files: <list>. Commit, stash, or pass --force.")` + `exit(1)`. Skip silently if target is not a git repo.
4. **CLAUDE.md marker check.** Read `<target>/CLAUDE.md` (if present). If present but missing `<!-- CLEARGATE:START -->` OR `<!-- CLEARGATE:END -->` → `stderr("CLAUDE.md is missing <!-- CLEARGATE:START --> marker")` + `exit(1)`. If CLAUDE.md absent, skip (no block to strip).
5. **Classify every file.** Load install manifest; for each entry compute preservation category per story §1.2 Preservation table (8 categories: `shipped-work-items`, `flashcard`, `retrospectives`, `pending-drafts`, `token-ledgers`, `protocol-templates`, `wiki`, `hook-logs`). Apply `--preserve` / `--remove` CLI overrides (each flag is comma-split tier list). Category-less paths default to `remove` iff tier is `protocol|template|agent|hook|skill|cli-config`; default-keep iff tier is `user-artifact`.
6. **Always-remove set** (no prompt; still respected in `--dry-run`): `.claude/agents/*.md` (framework-owned), `.claude/hooks/{token-ledger,stamp-and-gate,session-start}.sh`, `.claude/skills/flashcard/**`, `.cleargate/.install-manifest.json`, `.cleargate/.drift-state.json`, CLAUDE.md CLEARGATE block, `@cleargate/cli` in `package.json` deps.
7. **Interactive prompts** (skipped when `--yes` or `--dry-run`): one prompt per non-default category ("Remove `.cleargate/delivery/pending-sync/**`? [y/N]"). `promptYesNo` is a test seam.
8. **Preview summary.** `"Will remove N files, keep M files, update CLAUDE.md to strip CLEARGATE block, remove @cleargate/cli from package.json."`
9. **Typed confirmation** (skipped when `--yes`). Project name derived from CWD basename OR `package.json` `name` field; `promptName` returns user input. Mismatch → `stdout("name mismatch — aborting")` + `exit(1)`.
10. **If `--dry-run`: emit plan + exit(0). Nothing on disk changes.**
11. **Execute removal.** Iterate `remove` list → `fs.rmSync(p, {force: true})`. Iterate always-remove set. Call `removeBlock(claudeMd)` + write back. Load settings.json → `removeClearGateHooks(settings)` → write back. Edit `package.json` via JSON parse (preserve key order via sequential rewrite) → remove `@cleargate/cli` from `dependencies` / `devDependencies`. Do NOT regenerate `package-lock.json` (out of scope — user runs `npm install` after).
12. **Write `.uninstalled` marker** at `<target>/.cleargate/.uninstalled`:
    ```json
    {
      "uninstalled_at": "<ISO-8601>",
      "prior_version": "<from install manifest>",
      "preserved": ["<path>", …],
      "removed": ["<path>", …]
    }
    ```
13. **Empty-dir cleanup.** If `<target>/.cleargate/` contains only the `.uninstalled` marker → leave it (marker needs to persist for future `init` restore). Otherwise leave remaining preserved files in place.
14. **Restore hint.** If any preservations → `stdout("Preserved N items. Run cleargate init in this directory to restore.")`.

### Test shape (vitest, `test/commands/uninstall.test.ts`)

One test case per Gherkin scenario from story §2.1 (12 scenarios). Each test uses `os.tmpdir()` fixtures via `fs.mkdtempSync`:

1. `--dry-run` preview only; no FS changes.
2. Typed confirmation wrong name → abort.
3. Preserves user artifacts by default (`--yes`).
4. Removes framework files.
5. Surgery on CLAUDE.md + settings.json.
6. Writes `.uninstalled` marker with correct shape.
7. Refuses on missing CLAUDE.md markers.
8. Refuses on uncommitted changes without `--force`; `--force` overrides.
9. Idempotent re-run (second run prints "already uninstalled").
10. Single-target — does NOT recurse into nested `.cleargate/` (fixture has a nested `cleargate-planning/.cleargate/`; assert it's untouched).
11. Empty `.cleargate/` after `--remove all` → directory removed.
12. Missing `.install-manifest.json` → "no ClearGate install detected" + exit 0.

Plus 3 unit tests:
13. Category classifier (in / out / cross-category-override).
14. Marker writer (JSON shape round-trips).
15. Preservation-flag parser (`--preserve flashcard,archive`; invalid tier → clear error).

### Mirror-discipline

N/A for this story — no scaffold files touched. Post-uninstall fixture state is validated in tests.

### Risks / gotchas

- **BLAST RADIUS.** This is the most-destructive command in the CLI. The sprint-file Risk row 9 + story Notes section demand: **all wet tests run in `os.tmpdir()` — NEVER in the meta-repo CI**. Developer MUST NOT run `cleargate uninstall` without `--dry-run` against the current working repo during dev.
- **FLASHCARD `#init #inject-claude-md #regex`** — `claude-md-surgery.ts`'s regex is GREEDY; works correctly for `removeBlock`. No re-work needed.
- **FLASHCARD `#keyring #napi #posix-modes`** — not directly relevant (no keyring writes), but remind QA to verify `.uninstalled` marker has mode 644 not 600.
- **FLASHCARD `#cli #commander #optional-key`** — `opts.path` / `opts.preserve` / `opts.remove` may all be `undefined`. Pass omitting the key when unset (don't spread `{path: undefined}` into option defaults).
- **FLASHCARD `#cli #vitest #vi-mock-hoisting`** — if mocking `fs` or `child_process`, use `vi.hoisted()` for any variable referenced in the `vi.mock()` factory.
- **Nested `.cleargate/` single-target rule** — Gherkin scenario 10 is non-obvious: the meta-repo has `cleargate-planning/.cleargate/` which is a nested ClearGate install. A bug here would wipe the canonical scaffold. Test this explicitly with a fixture that has both `<target>/.cleargate/` and `<target>/nested/.cleargate/` — only the top-level is touched.
- **Uncommitted-changes check narrowness** — only check `git status --porcelain` on files listed in the install manifest. A user's unrelated uncommitted work must NOT block uninstall (would be infuriating UX).
- **Settings.json surgery side-effects** — `removeClearGateHooks` returns a new object; if the result has `hooks:{}` empty, `settings-json-surgery.ts:85-92` deletes the `hooks` key entirely. This is desirable (matches "no ClearGate hooks" end-state). Assert in test 5.
- **`package.json` editing** — do NOT run `npm install` automatically. Print a hint: `"Removed @cleargate/cli from package.json. Run `npm install` to update package-lock.json."`.
- **QA manual walkthrough mandatory** — per story §Notes, Risk row 9, AND sprint-file Ops DoD. QA runs wet uninstall in a scratch `os.tmpdir()` fixture with FLASHCARD-seeded files + archive items, then re-runs `cleargate init`, and confirms FLASHCARD + archive round-trip byte-identical.

### Commit checklist

- [ ] `npm run typecheck` + `npm test` clean.
- [ ] **All 15 test cases pass in `os.tmpdir()` fixtures — zero FS writes outside tmpdir.**
- [ ] `cleargate uninstall --help` shows all flags.
- [ ] `cleargate uninstall --dry-run` against the current meta-repo produces a preview WITHOUT changing disk (CI-safe smoke test).
- [ ] Manual walkthrough on a scratch tmpdir (QA step — signed off in QA report).
- [ ] Commit `feat(EPIC-009): STORY-009-07 cleargate uninstall with preservation + marker`.

---

## STORY-009-08 · Protocol §13

**Story file:** `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/delivery/pending-sync/STORY-009-08_Protocol_Section_13.md`

### Files to modify

- `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md`
- `/Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`

### Protocol §13 append

**Prerequisite:** §12 must already exist at the end of the protocol file (landed by STORY-008-07). If absent → STOP, re-check order.

Append at end-of-file (after §12.6). Concrete shapes pulled from EPIC-009 §4. Section body must contain:

```markdown
---

## 13. Scaffold Manifest & Uninstall

### §13.1 Overview
Three-surface model: package manifest (shipped in `@cleargate/cli`), install snapshot (`.cleargate/.install-manifest.json` written at init), current state (live FS). Drift is classified pairwise into 4 states (clean / user-modified / upstream-changed / both-changed) + `untracked` for user-artifact tier. SHA256 over normalized content (LF / UTF-8 no-BOM / trailing-newline) is the file identifier.

### §13.2 Install
`cleargate init` copies the bundled payload, then writes `.cleargate/.install-manifest.json`:

```json
{
  "cleargate_version": "0.2.0",
  "installed_at": "2026-04-19T10:00:00Z",
  "files": [
    {"path": ".cleargate/knowledge/cleargate-protocol.md", "sha256": "…", "tier": "protocol", "overwrite_policy": "merge-3way", "preserve_on_uninstall": "default-remove"}
  ]
}
```

If a `.cleargate/.uninstalled` marker exists at init time, init prompts "Detected previous ClearGate install … Restore preserved items? [Y/n]". Y = blind-copy preserved paths back into the new install (v1); mismatches log a warning and do not fail.

### §13.3 Drift detection
`cleargate doctor --check-scaffold` compares the three surfaces and writes `.cleargate/.drift-state.json` (daily-throttled refresh). SessionStart-triggered refresh runs at most once per day. Agent never auto-overwrites on upstream-changed drift — it emits a one-line advisory at triage; `cleargate upgrade` is always human-initiated. `user-artifact` tier (sha256: null) is silently skipped in drift output; surfaces only in uninstall preview.

### §13.4 Upgrade
`cleargate upgrade [--dry-run] [--yes] [--only <tier>]` drives a three-way merge for `merge-3way` policy files. Per-file prompt: `[k]eep mine / [t]ake theirs / [e]dit in $EDITOR`. Execution is incremental: successes are committed to disk + `.install-manifest.json` updated before the next file is processed; a mid-run error leaves earlier successes intact.

### §13.5 Uninstall
`cleargate uninstall [--dry-run] [--preserve …] [--remove …] [--yes] [--path <dir>] [--force]` is preservation-first. Defaults: `.cleargate/delivery/archive/**`, `FLASHCARD.md`, `sprint-runs/*/REPORT.md`, `pending-sync/**` → keep. `.cleargate/knowledge/`, `.cleargate/templates/`, `.cleargate/wiki/`, `.cleargate/hook-log/` → remove. Safety rails: typed confirmation (project name), single-target (no recursion into nested `.cleargate/`), refuse on uncommitted manifest-tracked changes without `--force`, CLAUDE.md marker-presence check. Always-removed (no prompt): `.claude/agents/*.md`, ClearGate hooks, `flashcard/` skill, CLAUDE.md CLEARGATE block, `@cleargate/cli` in `package.json`, `.install-manifest.json`, `.drift-state.json`. Writes `.cleargate/.uninstalled` marker:

```json
{
  "uninstalled_at": "2026-04-19T11:00:00Z",
  "prior_version": "0.2.0",
  "preserved": [".cleargate/FLASHCARD.md", ".cleargate/delivery/archive/**"],
  "removed": [".cleargate/knowledge/cleargate-protocol.md"]
}
```

Future `cleargate init` in the same dir detects this marker and offers restore.

### §13.6 Publishing notes
`MANIFEST.json` is built at `npm run build` (prebuild step in `cleargate-cli/package.json`) and shipped in the npm tarball (`files[]`). Never computed at install time. `generate-changelog-diff.ts` diffs `MANIFEST.json` between the previous published version and the current one at release time; CHANGELOG.md auto-opens with a "Scaffold files changed" block per release. Content-identical entries (path-moved-only, metadata-changed-only) are collapsed to avoid noise.
```

Plus the **one-line cross-reference in §4** (per story §1.2): add `> (See §13 for scaffold lifecycle commands)` at the end of §4 Phase Gates if not already covered by §12's §4 cross-ref. If §12 already added a Gate-2 line, append §13's as a second line.

### Test shape

Grep tests at `test/scripts/protocol-section-13.test.ts`:
1. `## 13. Scaffold Manifest & Uninstall` present.
2. Subsections `### §13.1` through `### §13.6` all present.
3. No `TBD` string anywhere in §13.
4. §13.2 contains a fenced ` ```json` block with `install_manifest` fields; §13.5 contains a fenced JSON block with `uninstalled_at`.
5. §4 contains "(See §13 …)" cross-ref.
6. Mirror diff: live vs `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md` empty.

### Mirror-discipline

```
diff /Users/ssuladze/Documents/Dev/ClearGate/.cleargate/knowledge/cleargate-protocol.md /Users/ssuladze/Documents/Dev/ClearGate/cleargate-planning/.cleargate/knowledge/cleargate-protocol.md
# expected: EMPTY
```

### Risks / gotchas

- **Section-numbering collision** — story §2.2 demands: before editing, grep for `## 11.`, `## 12.`, `## 13.` in the protocol. §11 must be at line ~351 (verified — present), §12 must be at end-of-file (landed by 008-07), §13 does NOT exist. If §12 is missing → STOP; 008-07 did not land. If §13 already exists → STOP; someone double-applied.
- **Append-only rule** — do NOT restructure §§1–10. Do NOT insert between §11 and §12. Cross-ref in §4 is a single line at the END of §4 (before the `---` separator if any). This is protocol edit THREE of three in this sprint; the rule has held for §11 and §12 and must hold for §13.
- **Mirror discipline** — always write to live, then copy to `cleargate-planning/.cleargate/knowledge/cleargate-protocol.md`. FLASHCARD `#wiki #protocol #mirror`.
- **Wiki lint impact** — adding §13 will trigger the new `checkGateStaleness` for `cleargate-protocol.md` itself IF it has frontmatter. Protocol file has no YAML frontmatter (it's a plain knowledge doc, not a delivery doc), so gate-stale won't fire. Verify by grep'ing for `^---$` at top — if absent, safe.

### Commit checklist

- [ ] `grep -c "^## 13\." cleargate-protocol.md` returns `1`.
- [ ] `grep -c "TBD" §13 slice` returns `0`.
- [ ] Mirror diff empty.
- [ ] `npm run typecheck` + `npm test` clean in `cleargate-cli/` (wiki-lint grep test may now flag existing pre-migration items — acceptable per sprint-file Constraints; new tests from 008-07 + 009-08 must be green).
- [ ] Commit `docs(EPIC-009): STORY-009-08 protocol §13 scaffold manifest & uninstall`.

---

## Cross-story risks (orchestrator sees all four)

- **settings.json surgery is the sprint's single-point-of-failure.** 008-06 is the ONLY story that edits `.claude/settings.json` in M3. If `removeClearGateHooks` drops the wrong entry OR the hook merge mis-nests, all downstream hook firings fail and 008-07 / 009-07 / 009-08 developers get broken dogfood. **Orchestrator MUST verify 008-06's settings.json diff post-commit: only SPRINT-04's inline `wiki ingest` PostToolUse entry is removed; SubagentStop token-ledger entry is untouched; new PostToolUse stamp-and-gate + SessionStart entries are added.**
- **Protocol file is touched TWICE in M3 (§12 by 008-07, §13 by 009-08).** If 008-07 accidentally writes §13 numbering, 009-08 will double-number. Each story asserts the prerequisite exists + own section does NOT yet exist before writing.
- **Templates touched TWICE this sprint** (001-01 in M1, 008-07 in M3). 008-07's Developer MUST Read before Write to preserve M1's 5 metadata fields. Sprint-file Risk row 1 explicit mitigation.
- **Existing pending-sync items will fail new wiki-lint checks** (added by 008-07) with `gate-stale` until 008-06's hook runs on each one. First run post-M3 will show noise; this is expected initial-migration behavior, not a regression. Do not attempt to stamp the entire backlog in 008-07 — that's out of scope.
- **009-07 depends on `.install-manifest.json` existing** in the target. Meta-repo currently does NOT have one (no `cleargate init` has run against meta-repo). 009-07 tests use `os.tmpdir()` fixtures that synthesize one; the `--dry-run` smoke test at CI level cannot run on meta-repo because there's no manifest. Story Acceptance Criterion 12 covers the "missing manifest" case explicitly — exit 0 with a clean message.

## Dogfood impact warning

- **008-06 lands → all future writes to `.cleargate/delivery/**` trigger the new PostToolUse chain.** If `stamp-tokens` throws unhandled, ALL subsequent story drafts fail to save cleanly. Remediation: `git revert <008-06-sha>`; hook registration reverts atomically with the rest of the commit.
- **008-06 lands → every new Claude Code session gets SessionStart context injection.** If `doctor --session-start` emits >100 tokens or crashes, the next subagent's context is poisoned. Remediation: same revert.
- **008-07 lands → new wiki-lint checks will flag existing pre-migration items.** Acceptable per sprint constraints.
- **009-07 lands → `cleargate uninstall` exists but no meta-repo data is at risk** (no `.install-manifest.json` at meta-repo root means it exits 0 safely). Production risk is exclusively on downstream users.
- **009-08 lands → pure doc; zero behavior change.**

## M3 exit criteria (from sprint file, verbatim)

- Dogfood session boot → SessionStart hook emits blocked-gate summary in context.
- An intentional TBD in a pending-sync Epic → `wiki lint` fails.
- `cleargate uninstall --dry-run` on a throwaway tmpdir fixture produces the expected preservation preview.
- `diff .claude/settings.json cleargate-planning/.claude/settings.json` returns empty (modulo the absolute-path vs `${CLAUDE_PROJECT_DIR}` divergence on command strings — acceptable; see 008-06 mirror note).

## Open decisions for the orchestrator

1. **SessionStart 100-token budget** — sprint file asserts ≤100 LLM-tokens for the blocked-items summary. Current meta-repo has ~20+ items in `.cleargate/delivery/pending-sync/`. At 10 items × ~10 tokens each (`"EPIC-008: no-tbds"`) = 100 tokens with zero overhead. **Decision needed:** accept 10-item cap + short overflow pointer OR tighten per-item rendering (ID only, no criterion). Architect recommends: ID + single-criterion-id, truncated to 10 items, matches the sprint-file Acceptance Criterion scenario "3 items blocked: EPIC-X, STORY-Y, BUG-Z" — criterion id optional, ID-only safer at scale.
2. **Hook-chain failure semantics** — per story §1.2 the hook exits 0 even on gate-check failure (severity is enforced at `wiki lint`, not hook time). But what about `stamp-tokens` failure? If `stamp-tokens` fails mid-chain, should `gate check` + `wiki ingest` still run? Architect recommendation: YES, run all three unconditionally; each logs its own exit code; hook aggregate exit is always 0. Matches the shell blueprint above. **Decision needed:** confirm this over a "fail-fast on stamp failure" alternative.
3. **mcp/ settings.json (out of M3 scope)** — the `mcp/` nested repo also has a `.claude/settings.json` (not verified in this plan). M3 does NOT touch it. If mcp/ has a legacy inline `wiki ingest` entry, it silently survives. Flag for orchestrator: decide whether to schedule a mcp-side cleanup as a follow-up CR.
4. **009-07 git-repo detection** — story §1.2 requires refusal on uncommitted changes. What about a target that is NOT a git repo (bare dir)? Architect recommendation: skip the check silently (cannot detect uncommitted changes without git). **Decision needed:** confirm "skip silently" over "require git repo".
5. **Cross-ref insertion order in §4 (protocol)** — §12 adds "Gate 2 machine-checked" one-liner; §13 adds "see §13 for scaffold lifecycle". If both land, §4 gets two terminal lines. Architect recommendation: separate bullets, both terminal, §12's first. **Decision needed:** confirm ordering.
