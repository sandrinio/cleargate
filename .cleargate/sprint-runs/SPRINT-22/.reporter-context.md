# Reporter Context Bundle — SPRINT-22

_Generated: 2026-05-04T09:42:04.076Z_

---

## Sprint Plan Slices

_Source: SPRINT-22_SDLC_Hardening.md_

## 1. Consolidated Deliverables

## 2. Execution Strategy

## 5. Metrics & Metadata

## State.json Summary

Sprint: SPRINT-22 | Status: Active

CR-042 | Done | standard | qa_bounces=0 arch_bounces=0
CR-043 | Done | standard | qa_bounces=0 arch_bounces=0
CR-044 | Done | standard | qa_bounces=0 arch_bounces=0

## Milestone Plans

### M1.md

role: architect

# Milestone: M1 — SDLC Hardening Wave 1
## Stories: CR-042, CR-043, CR-044
## Wave: W1 (3 parallel Developer dispatches)

## Order

**Parallel execution at dispatch time (3 simultaneous Devs).** Merge order is strictly serial:

1. **CR-042 merges first.** Single-touch on `reporter.md` (lines 106-110). Independent of CR-043/CR-044 surfaces. Lands cleanly without rebase.
2. **CR-043 merges second.** Inserts a NEW `### C.3.5 Spawn QA-Red` subsection in `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` between current §C.2 (ends L213) and current §C.3 Spawn Developer (begins L215). After merge, current §C.3 stays at the same line offset; numbering shifts (C.3 becomes C.3.5 + Dev step renumbers conceptually but Architect's instruction is to use **C.3a (QA-Red) / C.3b (Developer)** to avoid renumbering downstream §C.7 Blockers Triage cross-reference at L241).
3. **CR-044 merges third.** Rebases on top of CR-043. Touches §C.6 (currently L275-290) — replaces inline orchestrator git/state mechanics with a DevOps dispatch step. After CR-043's insertion, §C.6 sits ~10-15 lines lower; CR-044 Dev must compute fresh offset post-rebase.

**Parallelizable at dispatch time (no shared edited file at the moment of dispatch):**
- CR-042 ‖ CR-043 ‖ CR-044 — yes. The shared file (SKILL.md) is touched by CR-043 and CR-044 in disjoint regions (CR-043: pre-§C.3; CR-044: §C.6); merge ordering enforces conflict-freedom.

## Per-story blueprint

---

### CR-042 — reporter.md L108 "fresh session" claim corrected

**Cross-story coupling:** None at the file level (touches only `cleargate-planning/.claude/agents/reporter.md` + audit grep across other agents). Indirect coupling: CR-044 introduces a new `devops.md` — CR-042 should NOT preemptively audit that file (it does not exist yet at CR-042 dispatch time). If audit grep across `cleargate-planning/.claude/agents/*.md` finds matches in `architect.md|developer.md|qa.md`, fix in same commit per CR-042 §0.5 Q2 and §3 verification point #2.

**Schema changes:** None. Doc-only.

**File surface (verbatim from CR-042 §3):**
- `cleargate-planning/.claude/agents/reporter.md` (lines 106-110 — "Fresh Session Dispatch (CR-036)" subsection); replace inaccurate sentence per CR-042 §0.5 Q1 Recommended:
  > "Reporter dispatch runs in the orchestrator's session_id; the SubagentStop hook attributes tokens to the work_item via the dispatch marker (.dispatch-<session-id>.json)."
- (Conditional) `cleargate-planning/.claude/agents/{architect,developer,qa}.md` — only if grep hits.
- `cleargate-cli/templates/cleargate-planning/.claude/agents/reporter.md` — auto-regenerated via `cd cleargate-cli && npm run prebuild`.
- `cleargate-planning/MANIFEST.json` — auto-regenerated.

**Implementation sketch:**
1. Read `cleargate-planning/.claude/agents/reporter.md:106-110`. The current L108 contains the inaccurate "the `Task` tool already creates a new conversation per dispatch" parenthetical and the "verify the dispatch payload contains no `--resume`" guidance.
2. Edit L108 to remove the parenthetical and replace with the dispatch-marker truth (per CR-042 §0.5 Q1 Recommended sentence above). Preserve surrounding paragraph structure (heading "## Fresh Session Dispatch (CR-036)" stays; the "Reporter starts cold each time" closing line at L110 stays; subsection heading text "Fresh Session Dispatch" can stay because the dispatch marker model is still a fresh-context contract — only the *mechanism* description was wrong).
3. Audit other agents: `grep -nE 'fresh session|new conversation|per dispatch|Task tool creates' cleargate-planning/.claude/agents/{architect,developer,qa}.md`. Confirmed clean as of 2026-05-04 (Bash check ran). Likely 0 hits; doc accordingly in dev report.
4. Audit SKILL.md L380 (`cleargate-planning/.claude/skills/sprint-execution/SKILL.md`): the "Fresh session" callout is **accurate as written** ("write_dispatch.sh already spawns a clean shell child; the Agent tool path requires no session-continuation flag"). Do NOT modify SKILL.md L380 — that line correctly describes the dispatch-marker / shell-child model. CR-042 only edits reporter.md L108.
5. `cd cleargate-cli && npm run prebuild` to regenerate npm payload + MANIFEST.json.
6. Verify mirror parity: `diff cleargate-planning/.claude/agents/reporter.md cleargate-cli/templates/cleargate-planning/.claude/agents/reporter.md` empty.
7. Live `.claude/agents/reporter.md` re-sync is a post-merge/sprint-close orchestrator action (Gate 4 doc-refresh checklist) — Dev does NOT touch live `.claude/`.

**Test shape:**

CR-042 is doc-only. No code tests. Verification is grep-driven, executed in the dev report's "Test Commands" section per CR-042 §4:
- `grep -n "Task tool creates new conversation\|new conversation per dispatch\|fresh session" cleargate-planning/.claude/agents/reporter.md` → expect 0 hits matching the inaccurate phrasing (subsection HEADING `## Fresh Session Dispatch` may legitimately remain — it is not the inaccurate claim; only the L108 sentence body changes).
- `grep -nE "fresh session|new conversation|per dispatch" cleargate-planning/.claude/agents/{architect,developer,qa}.md` → expect 0 hits.
- `diff cleargate-planning/.claude/agents/reporter.md cleargate-cli/templates/cleargate-planning/.claude/agents/reporter.md` → expect empty.

No `*.node.test.ts` file added. (Doc-only CR; npm test stays no-op for this surface.)

**Risks:**
- **Subsection heading ambiguity:** the heading "## Fresh Session Dispatch (CR-036)" at L106 contains the phrase "fresh session" — Dev may grep-delete it overzealously. Architect ruling: heading stays; only L108 sentence body changes. The "fresh-session contract" is real (cold-context dispatch) — only the Task-tool-conversation-mechanism claim is inaccurate.
- **Mirror parity drift** if Dev forgets prebuild. CR-042 §4 acceptance #3 is the gate.
- **Live `.claude/` re-sync is NOT in scope.** Dev's commit only touches canonical + npm payload. Sprint-close Gate-4 doc-refresh handles live re-sync.

**Reuse (no duplication):**
- `cd cleargate-cli && npm run prebuild` — existing script; do NOT reimplement copy logic.
- Mirror-parity grep pattern follows the same shape as SPRINT-21 CR-038 dev work; Dev can reference that history (FLASHCARD `2026-05-04 #docs #agent-defs`).

**Acceptance link-back (CR-042 §4 verification points → code change):**
- §4.1 (grep returns 0 hits on inaccurate phrases) ↔ L108 edit.
- §4.2 (audit other agents) ↔ conditional edits to architect.md/developer.md/qa.md if hits found; otherwise dev report cites grep output.
- §4.3 (mirror parity diff empty) ↔ `npm run prebuild` invocation.
- §4.4 (positive assertion: dispatch-marker attribution sentence present) ↔ L108 replacement text.
- §4.5 (MANIFEST.json regenerated) ↔ prebuild side effect.
- §4.6 (commit message notes live-resync needed) ↔ Dev commit message line.

---

### CR-043 — Red/Green TDD Discipline + qa.md mode dispatch + pre-commit hook + sample fixture

**Cross-story coupling:**
- **SKILL.md edit zone (canonical):** inserts `### C.3a Spawn QA-Red` between L213 (end of §C.2 worktree creation) and L215 (start of current §C.3 Spawn Developer). To preserve §C.7 reference at L241 ("If `STATUS=blocked`: route per §C.7"), use **letter suffix renaming**: insert as `### C.3 Spawn QA-Red (standard lane only)` and rename current §C.3 → `### C.4 Spawn Developer`; current §C.4 → `### C.5 Spawn QA-Verify`; current §C.5 → `### C.6 Architect Pass`; current §C.6 → `### C.7 Story Merge`; current §C.7 → `### C.8 Blockers Triage`; current §C.8 → `### C.9 Flashcard Gate`; current §C.9 → `### C.10 Mid-cycle User Input`. **All in-text §C.x cross-references must be updated in same commit.** Affected references in current SKILL.md: L184 ("§C.7 Blockers Triage" reference style), L241 ("route per §C.7"), L259 ("Return to §C.3"), L432 (counter table cites C-section steps).
- **Conflict with CR-044 SKILL.md edit:** CR-044 edits the post-renaming §C.7 Story Merge (currently §C.6 at L275-290). Merge order CR-043 → CR-044 means CR-044's Dev must rebase on top of CR-043's renumbering. CR-044 dev MUST recompute offsets post-CR-043 merge.
- **Forbidden-region rule in `developer.md`** (CR-043 acceptance #3) does NOT collide with CR-044's developer.md edits (CR-044 modifies developer.md only via cross-references, per CR-044 §3 "Modify" list). Both edits append to existing sections; no line collision.

**Schema changes:**
- `cleargate-planning/.claude/agents/qa.md` (existing 110 lines) — add a new "## Mode Dispatch — Red vs Verify" section (recommended location: after the "## Pack-First Ingest" block at L29, before "## Lane-Aware Playbook" at L31). The mode is set by orchestrator dispatch text; qa.md prompt reads which mode to run. New section documents the two dispatch shapes.
- `cleargate-planning/.claude/agents/developer.md` (existing 152 lines) — add a new "## Forbidden Surfaces" section (recommended location: after "## Worktree Contract" at L92-99, before "## Lane-Aware Execution" at L101). Lists `**/*.red.test.ts` as immutable.
- `.claude/hooks/pre-commit-surface-gate.sh` is currently an 11-line stub at `cleargate-planning/.claude/hooks/pre-commit-surface-gate.sh:1-11` that delegates to `.cleargate/scripts/file_surface_diff.sh`. **Two implementation options for Red-immutability check:**
    - **Option A (preferred):** add the Red-immutability check INSIDE the stub (canonical: `cleargate-planning/.claude/hooks/pre-commit-surface-gate.sh`), running BEFORE the delegating exec. Approximately 20 LOC: detect story branch, list `*.red.test.ts` files in HEAD diff, query `git log --diff-filter=A --pretty=%s -- '*.red.test.ts'` for the QA-Red commit subject pattern (e.g. `qa-red(STORY-NNN-NN):`), reject if Dev's HEAD modifies any `*.red.test.ts` post-QA-Red commit. Bypass via `SKIP_RED_GATE=1` env var.
    - **Option B (rejected):** add to `.cleargate/scripts/file_surface_diff.sh`. **Rejected** — file_surface_diff.sh is the §3.1 file-surface contract enforcer, not a generic hook. Mixing concerns increases blast radius. Architect ruling: **use Option A.**
- `cleargate-cli/test/_node-test-runner.md` — append a new "## Red/Green naming convention" section (after the existing "## QA verification recipe" section ending L103). Document: `*.red.test.ts` is QA-Red-authored, immutable for Developer. `*.test.ts` is Dev-authored. `*.node.test.ts` (per existing doc) remains the new node:test convention. **Subtle clarification:** the protocol says `*.red.test.ts` for Red tests, but `*.node.test.ts` for new tests. The combined naming is `*.red.node.test.ts` for Red-phase node:test files. Document this combined form explicitly to avoid Dev confusion.

**File surface (verbatim from CR-043 §3 + cross-checked against frontmatter constraints):**
- **Modify:**
    - `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` — insert §C.3a (QA-Red dispatch) before current L215; renumber §C.3..C.9 → §C.4..C.10; update all in-prose §C.x cross-references.
    - `cleargate-planning/.claude/agents/qa.md` — add "## Mode Dispatch" section.
    - `cleargate-planning/.claude/agents/developer.md` — add "## Forbidden Surfaces" section.
    - `cleargate-planning/.claude/hooks/pre-commit-surface-gate.sh` — extend with Red-immutability check (Option A).
    - `cleargate-cli/test/_node-test-runner.md` — append Red/Green section.
- **Add (sample fixture — per SPRINT-22 frontmatter constraint, NOT `test/fixtures/`):**
    - `cleargate-cli/examples/red-green-example/calculator.red.node.test.ts`
    - `cleargate-cli/examples/red-green-example/calculator.node.test.ts`
    - `cleargate-cli/examples/red-green-example/calculator.ts`
    - `cleargate-cli/examples/red-green-example/README.md`
- **Auto-regenerated:** `cleargate-cli/templates/cleargate-planning/.claude/...` + `cleargate-planning/MANIFEST.json` via `npm run prebuild`.

**Implementation sketch:**

1. **SKILL.md edit (largest):** insert at L214 (currently blank) a new subsection:
    ```markdown
    ### C.3 Spawn QA-Red (standard lane only — fast lane skips this step)

    ```bash
    bash .cleargate/scripts/write_dispatch.sh STORY-NNN-NN qa
    ```

    Dispatch prompt MUST inject: `Mode: RED — write failing tests against §4 acceptance, no implementation Read access. Tests must fail with "not yet implemented" errors against the clean baseline. File-naming: *.red.test.ts (immutable post-Red). Forbidden: editing implementation files.`

    QA-Red returns:

    ```
    QA-RED: WRITTEN | BLOCKED
    RED_TESTS: <list of *.red.test.ts files>
    BASELINE_FAIL: <count of failing scenarios>
    flashcards_flagged: [ ... ]
    ```

    On `QA-RED: BLOCKED`: surface Spec-Gap to human.
    On `QA-RED: WRITTEN`: orchestrator commits the Red tests on the story branch with subject `qa-red(STORY-NNN-NN): write failing tests`, then proceeds to §C.4 Spawn Developer.

    **Fast lane skip:** if `state.json.stories[<id>].lane === "fast"`, skip this entire step.
    ```
    Then renumber existing §C.3..C.9 by +1 (using letter suffix or simple renumber — Architect locks **simple renumber**: §C.3→C.4, §C.4→C.5, §C.5→C.6, §C.6→C.7, §C.7→C.8, §C.8→C.9, §C.9→C.10). Update §10 quick-reference table (L425-428) cross-refs only if any cite §C.6 or higher (none do — table is counter-name-keyed).
    Update prose cross-refs:
    - L184: "Worktree → Developer → QA → (Architect pass for `lane: standard` v2 only) → Merge → Flashcard Gate" → "Worktree → **QA-Red** → Developer → QA-Verify → (Architect pass for `lane: standard` v2 only) → Merge → Flashcard Gate"
    - L241 ("route per §C.7"): renumber to §C.8
    - L259 ("Return to §C.3"): renumber to §C.4
    - "## C.4 Spawn QA" header (L243): rename to "## C.5 Spawn QA-Verify"
    - "## C.5 Architect Pass" (L263): renumber to "## C.6 Architect Pass"
    - "## C.6 Story Merge" (L275): renumber to "## C.7 Story Merge"
    - "## C.7 Blockers Triage" (L292): renumber to "## C.8 Blockers Triage"
    - "## C.8 Flashcard Gate" (L304): renumber to "## C.9 Flashcard Gate"
    - "## C.9 Mid-cycle User Input" (L324): renumber to "## C.10 Mid-cycle User Input"
2. **qa.md edit:** insert "## Mode Dispatch — Red vs Verify" after L29. Body documents the two dispatch shapes verbatim (Mode: RED prompt + Mode: VERIFY prompt). Cite SKILL.md §C.3 + §C.5 as canonical sources.
3. **developer.md edit:** insert "## Forbidden Surfaces" after L99 (end of "Worktree Contract"). Body: `**/*.red.test.ts` is immutable; pre-commit hook enforces; bypass via `SKIP_RED_GATE=1` is logged.
4. **pre-commit-surface-gate.sh edit:** extend the canonical hook stub. New shape (~25 LOC):
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

    # CR-043: Red-test immutability check (Option A — runs BEFORE file-surface delegation)
    if [[ "${SKIP_RED_GATE:-}" != "1" ]]; then
      CURRENT_BRANCH="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "")"
      if [[ "${CURRENT_BRANCH}" == story/STORY-* ]]; then
        # Look for staged modifications to *.red.test.ts files
        STAGED_RED="$(git diff --cached --name-only --diff-filter=M 2>/dev/null | grep -E '\.red\.(node\.)?test\.ts$' || true)"
        if [[ -n "${STAGED_RED}" ]]; then
          # Check whether a qa-red commit exists on this branch
          if git log --pretty=%s "${CURRENT_BRANCH}" 2>/dev/null | grep -qE '^qa-red\('; then
            echo "[red-gate] REJECT: Developer commits cannot modify *.red.test.ts files post-QA-Red." >&2
            echo "[red-gate] Modified files: ${STAGED_RED}" >&2
            echo "[red-gate] Bypass: SKIP_RED_GATE=1 (logged in sprint §4)." >&2
            exit 1
          fi
        fi
      fi
    fi

    # Delegate to file-surface-diff (existing behavior preserved)
    SCRIPT="${REPO_ROOT}/.cleargate/scripts/file_surface_diff.sh"
    if [[ ! -f "${SCRIPT}" ]]; then
      echo "[surface-gate] WARNING: file_surface_diff.sh not found — skipping" >&2
      exit 0
    fi
    exec bash "${SCRIPT}" "$@"
    ```
5. **_node-test-runner.md edit:** append "## Red/Green naming convention" section. Document `*.red.test.ts` (vitest legacy) and `*.red.node.test.ts` (node:test, new) — per CR-043, new Red tests MUST use `*.red.node.test.ts` to match the SPRINT-22 frontmatter constraint that all NEW tests are `*.node.test.ts`.
6. **Sample fixture:** create 4 files under `cleargate-cli/examples/red-green-example/`. The directory is OUTSIDE the `test/**/*.node.test.ts` glob, so `npm test` does NOT auto-run it. README.md explains: "this is pedagogical; run manually via `npx tsx --test cleargate-cli/examples/red-green-example/calculator.red.node.test.ts` to see Red phase fail; then run `calculator.node.test.ts` to see Green pass."
    - `calculator.red.node.test.ts` — uses `import { describe, it } from 'node:test'; import assert from 'node:assert/strict'; import { add } from './calculator.js';` — asserts `add(2,3) === 5`. Fails when `calculator.ts` exports `function add() {}` (returns undefined).
    - `calculator.node.test.ts` — Dev-authored Green test; covers edge cases (negatives, zero, large numbers). Uses same imports.
    - `calculator.ts` — exports `function add(a: number, b: number): number { return a + b; }`.
    - `README.md` — pedagogy: "Red is QA-Red-authored, immutable. Green is Dev-authored. Run them via `npx tsx --test <path>`. The pre-commit hook rejects `git commit` modifying `*.red.test.ts` on a story branch post-qa-red commit."
7. `cd cleargate-cli && npm run prebuild` to regenerate npm payload + MANIFEST.json.

**Test shape:**

Per CR-043 §4 acceptance points + SPRINT-22 frontmatter constraint (all NEW tests = `*.node.test.ts`):

- **NEW:** `cleargate-cli/test/hooks/red-gate.node.test.ts` (NEW node:test file). Minimum **4 scenarios**:
    1. `it('rejects Dev commit modifying *.red.test.ts after qa-red commit on story branch')` — fixture: temp git repo with story/STORY-X-Y branch, qa-red commit, then attempt to amend `calculator.red.test.ts`; assert hook exits 1 with "REJECT" string.
    2. `it('allows Dev commit modifying *.red.test.ts when SKIP_RED_GATE=1 set')` — same fixture + env var; assert exit 0.
    3. `it('allows Dev commit on non-story branch')` — branch = main; assert exit 0 (delegates to file_surface_diff.sh).
    4. `it('allows Dev commit when no qa-red commit exists on branch yet')` — story branch but no qa-red commit; assert exit 0 (Red phase not yet started; modification allowed).
- **NEW:** `cleargate-cli/test/examples/red-green-example.node.test.ts` (NEW node:test file). Minimum **2 scenarios**:
    1. `it('Red test fails against empty calculator.ts impl')` — copy fixture to tmpdir, replace `calculator.ts` with empty stub, run `npx tsx --test calculator.red.node.test.ts`, assert non-zero exit.
    2. `it('Both Red and Green tests pass against full calculator.ts impl')` — original fixture, run both files, assert exit 0.
- **NO new vitest tests.** Existing test files unchanged.
- **NO test for SKILL.md/qa.md/developer.md/_node-test-runner.md content edits** — those are doc-only and are verified via grep in dev report (acceptance #1, #2, #3, #7).

**Total new test file count: 2 files, 6 scenarios.**

**Pre-commit verification:** `cd cleargate-cli && npm run typecheck && npm test` — both must pass. `npm test` routes to node:test (`tsx --test 'test/**/*.node.test.ts'`), which picks up the 2 new files.

**Risks:**
- **SKILL.md numbering ripple:** the simple renumber strategy adds +1 to all §C.x ≥ §C.3. Dev MUST update all in-prose cross-references in same commit (L184, L241, L259). Architect prelocked the affected line numbers above.
- **Hook behavior on initial qa-red commit itself:** when the qa-red commit lands, it is *adding* `*.red.test.ts` (not modifying). The hook check is `--diff-filter=M` (modifications only), so qa-red's own additions pass. Verified in scenario #4.
- **Naming combo confusion:** existing `*.test.ts` (vitest) + new `*.node.test.ts` (node:test) + Red `*.red.test.ts` + Red node:test `*.red.node.test.ts`. Dev may write `*.test.red.ts` or another permutation. Architect ruling: canonical is `*.red.node.test.ts` for Red node:test (red infix BEFORE node infix, both BEFORE test). Document explicitly in _node-test-runner.md edit.
- **Fixture location enforcement:** SPRINT-22 frontmatter constraint says `cleargate-cli/examples/`. CR-043 §3 ALSO says `cleargate-cli/examples/`. CR-043 §4 acceptance #5 + Test Commands cite `test/fixtures/red-green-example/`. **Architect override:** sprint frontmatter wins. Use `cleargate-cli/examples/red-green-example/`. Acknowledge the CR-043 acceptance text drift in dev report.
- **Pre-commit hook cwd:** the hook runs from `git rev-parse --show-toplevel` — works in worktrees (CR-043 frontmatter mentions worktree mechanics, FLASHCARD `2026-04-21 #protocol`). No special handling needed for `.worktrees/STORY-X-Y/` cwd.
- **Live `.claude/` re-sync:** the hook lives in canonical `cleargate-planning/.claude/hooks/`; the live `/.claude/hooks/` is gitignored. Sprint-close Gate-4 doc-refresh handles re-sync. Dev does NOT touch live.
- **Vitest leak risk in test execution:** none — both new test files are `*.node.test.ts`. No vitest invoked. FLASHCARD `2026-05-01 #vitest #leak #posttest` mitigation not applicable.
- **PreToolUse:Task hook attribution:** the qa-red dispatch uses `agent_type=qa` (per CR-043 §0.5 Q8 — option A-hybrid uses existing qa.md, no new agent_type). Token-ledger does NOT need a new agent_type for QA-Red (CR-044 adds `devops`, not CR-043). Verified: `write_dispatch.sh` line 20 still accepts `qa`.

**Reuse (no duplication):**
- Existing `qa.md` agent file — extend, do NOT replace.
- Existing `developer.md` "Worktree Contract" + "Lane-Aware Execution" + "Circuit Breaker" sections — add "Forbidden Surfaces" between Worktree Contract (L92-99) and Lane-Aware Execution (L101+). Same shape as those sections.
- Existing pre-commit-surface-gate.sh stub structure — extend, do NOT rewrite.
- Existing `_node-test-runner.md` — append; do NOT restructure.
- `cd cleargate-cli && npm run prebuild` — existing.
- `tsx --test` (existing in package.json L46-54).

**Acceptance link-back (CR-043 §4 verification points → code change):**
- §4.1 (qa.md covers BOTH modes) ↔ "## Mode Dispatch" section insert in qa.md.
- §4.2 (SKILL.md §C documents 5-step loop with verbatim dispatch templates) ↔ §C.3 insert + renumbering + Mode-template prose blocks.
- §4.3 (developer.md "Forbidden Surfaces") ↔ developer.md section insert.
- §4.4 (pre-commit hook rejects post-QA-Red diffs to *.red.test.ts) ↔ pre-commit-surface-gate.sh extension + `red-gate.node.test.ts` 4 scenarios.
- §4.5 (sample fixture runs end-to-end) ↔ `examples/red-green-example/` 4-file fixture + `red-green-example.node.test.ts` 2 scenarios.
- §4.6 (mirror parity: qa.md canonical ↔ npm payload empty post-prebuild) ↔ `npm run prebuild` invocation + diff check.
- §4.7 (_node-test-runner.md documents naming + Dev-immutability) ↔ doc append.
- §4.8 (process acceptance — retrospective at SPRINT-23 close) ↔ no SPRINT-22 code change; tracked in CR-043 §4 explicitly.

---

### CR-044 — DevOps Role Agent (sonnet) — owns merge + worktree teardown + state transitions

**Cross-story coupling:**
- **SKILL.md edit zone:** edits §C.6 Story Merge (currently L275-290; post-CR-043 merge: L286-301 approximately, due to CR-043's QA-Red insert + heading renumbers). Replaces the inline orchestrator-runs-git-merge prose with a "spawn DevOps agent" dispatch step. **Dev must rebase on top of CR-043's merge** and recompute line offsets.
- **Cross-references in agent prompts:** `architect.md`, `qa.md`, `reporter.md` need cross-references to the new DevOps role wherever they cite merge/state mechanics. Architect did NOT find any current explicit references in those files (verified via grep `git merge|worktree remove|update_state.mjs` against `cleargate-planning/.claude/agents/*.md` returns 0 hits in non-developer.md files at draft time). **Net edit footprint to non-developer agent files: minimal — likely zero or a single one-line note.** Confirmed during dev report.
- **token-ledger.sh validator:** at `.claude/hooks/token-ledger.sh:227` (canonical mirror at `cleargate-planning/.claude/hooks/token-ledger.sh`), the legacy fallback role-iteration loop is `for role in architect developer qa reporter cleargate-wiki-contradict; do`. Add `devops` to this list.
- **write_dispatch.sh validator:** at `.cleargate/scripts/write_dispatch.sh:20`, the comment lists valid agent_types. The script does NOT actually validate the agent_type arg — it accepts any string and writes the dispatch JSON. CR-044 §3 Modify "agent_type validator" expectation is therefore **add a literal validation block** to write_dispatch.sh. Dev: add ~8 LOC after L50 (`AGENT_TYPE="${2}"`) to validate against the canonical set `developer|architect|qa|reporter|cleargate-wiki-contradict|devops`. Reject unknown with exit 1 + log line.
- **suggest_improvements.mjs:** at `.cleargate/scripts/suggest_improvements.mjs:147-156` (also canonical mirror at `cleargate-planning/.cleargate/scripts/`), the agent_type field is read but not validated against a set. Inspect: it counts unique tuples — no allowlist enforcement. CR-044 §3 expects `devops` "added to recognized agent types"; in practice this means: ensure the script does not crash on `devops` rows. Verified at L147-156 — generic Object key, no allowlist. **No edit needed for suggest_improvements.mjs.** Architect ruling: still document this in the dev report as "verified compatible; no edit required."
- **state.json schema:** no field change. DevOps writes `Done` via existing `update_state.mjs`. No edit to state.json schema.

**Schema changes:**
- New file: `cleargate-planning/.claude/agents/devops.md` (NEW agent prompt; ~120 LOC; sonnet model frontmatter; tools restricted to `Read, Edit, Bash, Grep, Glob` per CR-044 §4 acceptance #1 — note: NO Write tool, DevOps does not author code).
- Modified: SKILL.md §C.7 (post-CR-043 renumber; was §C.6) — replace orchestrator-runs-git-merge inline prose with "spawn DevOps via Agent tool with dispatch text per CR-044 §3.1 Context Pack" prose block. Append §C.7's required-reports check to add `STORY-NNN-NN-devops.md` to the v2 standard-lane required list.
- Modified: SKILL.md §1 Agent Roster table (L58-63) — add a `devops` row. Existing 4 rows: architect / developer / qa / reporter. New row: `devops | sonnet | Per-story, after QA-Verify + Architect post-flight | One commit (merge no-ff) + STORY-NNN-NN-devops.md report`.
- Modified: SKILL.md §1 Wall-clock budgets table (L70-74) — add a `devops` row. Recommended: ≤ 5 min (mechanical work, no agent inference cycles).
- Modified: token-ledger.sh L227 (legacy fallback role iteration) — add `devops` to the bash for-loop.
- Modified: write_dispatch.sh — add ~8 LOC agent_type validator block.

**File surface (verbatim from CR-044 §3):**
- **Modify:**
    - `cleargate-planning/.claude/skills/sprint-execution/SKILL.md` — §C.7 (post-renumber) rewrite; §1 Agent Roster + Wall-clock tables append.
    - `cleargate-planning/.claude/agents/architect.md`, `qa.md`, `reporter.md` — cross-references where needed (likely 0 edits each per architect grep above; document as "verified clean" otherwise).
    - `.claude/hooks/token-ledger.sh` (canonical: `cleargate-planning/.claude/hooks/token-ledger.sh`) — add `devops` to L227 role iteration list.
    - `.cleargate/scripts/write_dispatch.sh` (canonical: `cleargate-planning/.cleargate/scripts/write_dispatch.sh`) — add agent_type validator.
    - `.cleargate/scripts/suggest_improvements.mjs` — verified no edit needed; document in dev report.
- **Add:**
    - `cleargate-planning/.claude/agents/devops.md` — NEW agent prompt with `model: sonnet` frontmatter; tools `Read, Edit, Bash, Grep, Glob`; CR-044 §3.1 Context Pack reproduced verbatim as the dispatch contract section.
    - (Implicit) `STORY-NNN-NN-devops.md` and `STORY-NNN-NN-devops-blockers.md` report shapes — documented in SKILL.md §C.7 and devops.md.
- **Auto-regenerated:** `cleargate-cli/templates/cleargate-planning/.claude/...` + MANIFEST.json via prebuild.

**Implementation sketch:**

1. **Create `cleargate-planning/.claude/agents/devops.md`** with frontmatter:
    ```yaml
    ---
    name: devops
    description: Use AFTER QA-Verify pass + Architect post-flight pass on a Story. Owns mechanical merge, worktree teardown, state transition to Done, mirror parity diff post-merge. Never authors code. Halts on conflict.
    tools: Read, Edit, Bash, Grep, Glob
    model: sonnet
    ---
    ```
    Body: role prefix `role: devops`, capability surface, the 9-step ACTIONS list verbatim from CR-044 §3.1, output shape, on-conflict halt rules, post-merge report template.
2. **SKILL.md §C.7 (post-renumber from §C.6) rewrite:** replace L275-290 (currently — pre-rebase) with:
    ```markdown
    ### C.7 Story Merge — DevOps Dispatch

    After QA-Verify pass + (v2 standard-lane only) Architect post-flight pass, spawn DevOps:

    ```bash
    bash .cleargate/scripts/write_dispatch.sh STORY-NNN-NN devops
    ```

    Then `Agent(subagent_type=devops, ...)` with the dispatch prompt template per CR-044 §3.1. Inputs: story ID, sprint ID, worktree path, story branch, sprint branch, dev/qa/arch commit SHAs, files-changed manifest, canonical-touched flag, lane.

    DevOps performs: merge no-ff, post-merge test (touched files only), worktree remove, branch delete, `update_state.mjs <id> Done`, mirror parity diff (live ↔ canonical), prebuild if canonical changed.

    DevOps writes `STORY-NNN-NN-devops.md`. On conflict: writes `STORY-NNN-NN-devops-blockers.md` and halts; orchestrator escalates.

    **Required reports before merge** (DevOps verifies; halts if any missing):
    - `STORY-NNN-NN-dev.md` (always required)
    - `STORY-NNN-NN-qa.md` (required unless lane=fast skipped QA)
    - `STORY-NNN-NN-arch.md` (required for v2 standard-lane only)

    **Orchestrator MUST NOT run `git merge`, `git worktree remove`, `git branch -d`, `update_state.mjs`, or `npm run prebuild` directly. These are DevOps-owned. Violations are tracked in §4 Execution Log.**
    ```
3. **SKILL.md §1 table inserts:** add devops rows to Agent Roster (L58-63) and Wall-clock budgets (L70-74). Mirror existing row syntax.
4. **token-ledger.sh L227:** change `for role in architect developer qa reporter cleargate-wiki-contradict; do` to `for role in architect developer qa reporter devops cleargate-wiki-contradict; do`.
5. **write_dispatch.sh validator:** insert after L50 (`AGENT_TYPE="${2}"`):
    ```bash
    case "${AGENT_TYPE}" in
      developer|architect|qa|reporter|devops|cleargate-wiki-contradict)
        ;;
      *)
        printf '[%s] error: invalid agent_type: %s\n' "$(date -u +%FT%TZ)" "${AGENT_TYPE}" >> "${LOG}"
        printf 'error: invalid agent_type: %s (expected developer|architect|qa|reporter|devops|cleargate-wiki-contradict)\n' "${AGENT_TYPE}" >&2
        exit 3
        ;;
    esac
    ```
6. **suggest_improvements.mjs:** verify no edit needed (already accepts arbitrary `agent_type` strings via Object key); document this verification in dev report.
7. `cd cleargate-cli && npm run prebuild` to regenerate npm payload + MANIFEST.json.

**Test shape:**

Per CR-044 §4 acceptance + SPRINT-22 frontmatter (all NEW tests = `*.node.test.ts`):

- **NEW:** `cleargate-cli/test/scripts/write-dispatch-validator.node.test.ts`. Minimum **3 scenarios**:
    1. `it('accepts developer|architect|qa|reporter|devops|cleargate-wiki-contradict')` — fixture: invoke `write_dispatch.sh` with each valid agent_type via `child_process.execFileSync`, assert exit 0 and dispatch JSON written.
    2. `it('rejects unknown agent_type with exit 3')` — invoke with `bogus`, assert exit 3 and stderr contains "invalid agent_type".
    3. `it('rejects empty agent_type with exit 1')` — invoke with empty string, assert exit 1 (existing behavior preserved).
- **NEW:** `cleargate-cli/test/hooks/token-ledger-devops.node.test.ts`. Minimum **2 scenarios**:
    1. `it('attributes devops dispatch via dispatch marker')` — fixture: write `.dispatch-<sid>.json` with `{agent_type: "devops", work_item_id: "STORY-X-Y"}`, fire token-ledger.sh with mock transcript, assert ledger row has `agent_type: "devops"`.
    2. `it('legacy fallback recognizes role: devops in transcript')` — fixture: transcript with `role: devops` line, no dispatch marker, fire hook, assert ledger row has `agent_type: "devops"` (validates the L227 list edit).
- **No tests for devops.md content** — doc verification via grep in dev report (CR-044 §4 acceptance #1).
- **No tests for SKILL.md content** — doc verification via grep (CR-044 §4 acceptance #2).
- **No tests for sample-DevOps-dispatch end-to-end** (CR-044 §4 acceptance #5) — that is a fixture-story integration smoke test, deferred to SPRINT-23 retrospective per CR-044 §4 acceptance #7. Architect ruling: SPRINT-22 ships the role + validators + ledger compatibility; the end-to-end smoke is a SPRINT-23 task.

**Total new test file count: 2 files, 5 scenarios.**

**Pre-commit verification:** `cd cleargate-cli && npm run typecheck && npm test`.

**Risks:**
- **SKILL.md rebase hazard:** CR-043 inserts §C.3 QA-Red and renumbers §C.3..C.9 → §C.4..C.10. CR-044 edits the renumbered §C.7 Story Merge (was §C.6). Dev MUST `git merge sprint/S-22` (or rebase) before editing SKILL.md to pick up CR-043's renumbering. If Dev edits L275-290 directly without rebase, the merge will conflict at §C.6/§C.7 boundary. Architect mitigation: dispatch CR-044 Dev with explicit "rebase on sprint/S-22 before editing SKILL.md" instruction.
- **token-ledger.sh L227 placement:** the role iteration is in the LEGACY fallback block (no-sentinel path). The PRIMARY path is dispatch-marker attribution (L121+) which already accepts any agent_type via the dispatch JSON. So `devops` is already accepted on the primary path — the L227 edit only fixes the legacy fallback. Document this in dev report.
- **write_dispatch.sh exit code 3:** existing exit codes are 0/1/2 per L26-29. Adding exit 3 is an extension; verify it doesn't break existing callers. `pre_gate_runner.sh`, `init_sprint.mjs`, `close_sprint.mjs`: none of these invoke write_dispatch.sh per repo grep. Safe.
- **devops tools restricted (no Write):** CR-044 §4 #1 says "tools restricted to Read, Edit, Bash, Glob, Grep — no Write — DevOps doesn't author code, only reports + scripted ops". But the CR-044 §3.1 Context Pack OUTPUT section says DevOps writes `STORY-NNN-NN-devops.md`. **Edit can write a new file via Edit-create-pattern in Claude Code's Edit tool** — confirmed (Edit creates files when target does not exist). So the no-Write restriction is consistent with report-writing via Edit. Document explicitly in devops.md.
- **No new agent_type for QA-Red:** verified — CR-043 reuses `agent_type=qa` (option A-hybrid). CR-044 adds only `devops` to the agent_type set. Token-ledger does not need a `qa-red` agent_type entry.
- **Live `.claude/` re-sync:** canonical edits to hooks/agents do NOT auto-propagate to live `/.claude/`. Sprint-close Gate-4 doc-refresh handles re-sync. Dev does NOT touch live.
- **suggest_improvements.mjs no-edit decision risk:** if a future iteration adds an allowlist (e.g. for ledger validation), `devops` would silently fall outside it. Mitigation: document the verification in dev report so future readers see the explicit choice.

**Reuse (no duplication):**
- Existing `update_state.mjs` — DevOps invokes; do NOT reimplement state transitions.
- Existing `cd cleargate-cli && npm run prebuild` — DevOps invokes when canonical changed.
- Existing `git worktree remove` / `git branch -d` semantics — DevOps invokes.
- Existing `.cleargate/sprint-runs/<id>/reports/` directory — DevOps writes there; no new dir.
- `write_dispatch.sh` validator pattern — same shape as `cleargate gate` typecheck/test exit-code conventions (cite `feedback_dispatch_path_choice_explicit.md` for option-A explicit-mechanics-copy rule).
- Token-ledger sentinel + dispatch-marker contract — DevOps dispatches use the existing path; no new hook code.

**Acceptance link-back (CR-044 §4 verification points → code change):**
- §4.1 (devops.md exists with correct frontmatter + tools + Context Pack) ↔ NEW `cleargate-planning/.claude/agents/devops.md`.
- §4.2 (SKILL.md §C.6 documents DevOps dispatch) ↔ §C.7 (post-renumber) rewrite + §1 table appends.
- §4.3 (token-ledger accepts agent_type=devops) ↔ L227 list edit + `token-ledger-devops.node.test.ts` 2 scenarios.
- §4.4 (write_dispatch.sh devops exits 0) ↔ validator block + `write-dispatch-validator.node.test.ts` 3 scenarios.
- §4.5 (sample DevOps dispatch end-to-end — orchestrator runs zero merge calls) ↔ DEFERRED to SPRINT-23 retrospective per CR-044 §4 #7.
- §4.6 (mirror parity diff post-merge — DevOps surfaces drift) ↔ devops.md ACTION step #5 documents the diff; no SPRINT-22 code test.
- §4.7 (process acceptance — retrospective at SPRINT-23 close) ↔ no SPRINT-22 code change.

---

## Cross-story risks

1. **SKILL.md rebase ordering (CR-043 → CR-044).** CR-043 renumbers §C.3..C.9. CR-044 edits the post-renumber §C.7. CR-044 Dev MUST rebase on sprint/S-22 (with CR-043 already merged) before editing SKILL.md. Dispatch-time mitigation: orchestrator merges CR-042 first (independent), then CR-043, then dispatches CR-044 Dev with rebase instruction.
2. **Reporter doc fix (CR-042) does NOT require SKILL.md edit at L380.** The L380 "Fresh session" callout is **accurate** (describes dispatch-marker / shell-child model correctly). CR-042 only edits reporter.md L108. Dev must NOT overzealously grep-and-replace SKILL.md L380.
3. **Pre-commit hook stub vs scripts/file_surface_diff.sh.** CR-043 anchor cites `.claude/hooks/pre-commit-surface-gate.sh` as the file to edit. The canonical path is `cleargate-planning/.claude/hooks/pre-commit-surface-gate.sh` (11-line stub). Edit canonical, not live. Live re-syncs at sprint close.
4. **Sample-fixture location:** CR-043 §4 Test Commands cite `test/fixtures/red-green-example/` but SPRINT-22 frontmatter mandates `cleargate-cli/examples/`. Architect override: use `cleargate-cli/examples/red-green-example/`. Dev report acknowledges the CR-043 acceptance text drift (proposing CR-043 acceptance text update post-merge as a closeout footnote).
5. **`*.node.test.ts` for ALL new tests.** SPRINT-22 frontmatter is explicit: `npm test` routes to node:test only. Vitest is opt-in. CR-043 Red sample is `*.red.node.test.ts` (Red infix BEFORE node infix). CR-044 tests are plain `*.node.test.ts`. Zero new vitest files in this milestone.
6. **token-ledger.sh primary path already accepts `devops`.** L227 edit only fixes the LEGACY (no-sentinel) fallback. The PRIMARY dispatch-marker path at L121-141 reads `agent_type` from the JSON file and accepts any string. Test scenario #1 in `token-ledger-devops.node.test.ts` validates the primary path; scenario #2 validates the legacy fallback.
7. **No new `qa-red` agent_type.** CR-043 reuses `agent_type=qa` (option A-hybrid). CR-044's token-ledger edit adds only `devops`. The qa-red dispatch lands in the ledger as `agent_type: qa` with `work_item_id: STORY-NNN-NN`. The Reporter's per-agent_type aggregation will combine QA-Red + QA-Verify under a single `qa` bucket. **Acceptable for SPRINT-22.** A future CR can split if needed (CR-047 SPRINT-23 candidate).
8. **Mirror parity audits at sprint close.** Three CRs together touch many canonical files. Sprint-close Gate-4 doc-refresh checklist must include: live re-sync of `qa.md`, `developer.md`, `devops.md`, `reporter.md`, `pre-commit-surface-gate.sh`, `token-ledger.sh`, `write_dispatch.sh`, `SKILL.md`, plus prebuild verification. SPRINT-22 sprint plan §3 row 5 already names this.

## Open decisions for orchestrator

1. **CR-043 §0.5 Q3 (file-naming convention `*.red.test.ts`)** — accepted as Recommended in §0.5 prose but NOT marked accepted in the YAML status. Architect proceeds with `*.red.node.test.ts` (Red+node combined per SPRINT-22 frontmatter). Confirm before dispatch if the user has a contrary preference.
2. **CR-043 §0.5 Q1 (lane scope for QA-Red)** — recommended `standard only` (fast lane skips QA-Red). Architect plan implements this. Confirm before dispatch.
3. **CR-044 §0.5 Q5 (token-ledger pricing tier for devops)** — recommended `sonnet default tier`. Architect plan reflects this in `model: sonnet` frontmatter. No price-table edit needed if reporter.md does not hard-code per-agent rates. Confirm during CR-044 Dev kickoff.
4. **CR-044 §4 #5 (sample DevOps dispatch end-to-end)** — Architect deferred to SPRINT-23 retrospective. Confirm: SPRINT-22 ships the role + validators only; the end-to-end smoke test is not blocking CR-044 close.

---

flashcards_flagged:
  - "2026-05-04 · #skill-md #renumbering · CR-043 inserts §C.3 QA-Red — renumber §C.3..C.9 → §C.4..C.10 + update L184/L241/L259 cross-refs in same commit."
  - "2026-05-04 · #pre-commit #red-gate · pre-commit-surface-gate.sh is an 11-line stub delegating to file_surface_diff.sh — Red-immutability check goes IN the stub, before the exec, not in file_surface_diff.sh."
  - "2026-05-04 · #naming · Red+node combined naming is *.red.node.test.ts (red infix BEFORE node infix). Documented in _node-test-runner.md per CR-043."
  - "2026-05-04 · #token-ledger #devops · token-ledger.sh primary path (dispatch-marker, L121-141) already accepts arbitrary agent_type — L227 legacy fallback list edit only matters when dispatch marker is missing."
  - "2026-05-04 · #fixtures #sprint-22 · Sample fixtures go in cleargate-cli/examples/, NOT cleargate-cli/test/fixtures/, to keep them out of the npm test glob."

## Git Log Digest

_Branch: sprint/S-22 ^main_

```
commit 2178e9ad11a5a4ad0c3157f7503b7cdecd7ac4ab
Author: sandrinio <sandrinio@users.noreply.github.com>
Date:   Mon May 4 13:35:49 2026 +0400

    chore(SPRINT-22): W1 closeout — flashcards + state + processed markers + ledger
    
    W1 (CR-042 + CR-043 + CR-044 phase A + phase B) post-merge artifacts:
    - 7 flashcards approved (Architect M1 findings + Dev/QA/Arch discoveries)
    - state.json: CR-042 + CR-043 + CR-044 → Done — ALL 3 SPRINT-22 items now Done
    - 7 processed markers
    - token-ledger updated (~10 dispatches: SDR+M1, 3 Dev, 3 QA-Verify, 2 Arch + phase B Dev/QA/Arch)
    
    Sprint completion: 3/3 items merged + Done.
    - CR-042 ✓ — reporter.md fresh-session doc-bug fix (fast lane)
    - CR-043 ✓ — Red/Green TDD discipline + qa.md mode dispatch + pre-commit hook + sample fixture
    - CR-044 ✓ — DevOps role agent (sonnet) — phase A (§1 tables + agent + validators) + phase B (§C.7 body rewrite)
    
    The new 5-step standard-lane execution loop (Architect → QA-Red → Dev → QA-Verify
    → Architect post-flight → DevOps) is now structurally documented in SKILL.md §C.
    Pre-commit hook enforces *.red.test.ts immutability for Devs. DevOps agent (sonnet)
    owns merge + worktree teardown + state transitions + mirror parity diff.
    
    Ready for sprint walkthrough + Gate-4 close.

 .cleargate/FLASHCARD.md                                  |  7 +++++++
 .cleargate/sprint-runs/SPRINT-22/.processed-1889c0a7895a |  0
 .cleargate/sprint-runs/SPRINT-22/.processed-2f1d73ca915f |  0
 .cleargate/sprint-runs/SPRINT-22/.processed-3c054bf43716 |  0
 .cleargate/sprint-runs/SPRINT-22/.processed-5a9b34b3b624 |  0
 .cleargate/sprint-runs/SPRINT-22/.processed-923f9cde239d |  0
 .cleargate/sprint-runs/SPRINT-22/.processed-93d2b5c6bba6 |  0
 .cleargate/sprint-runs/SPRINT-22/.processed-e5947ec43fc3 |  0
 .cleargate/sprint-runs/SPRINT-22/.session-totals.json    |  2 +-
 .cleargate/sprint-runs/SPRINT-22/state.json              | 16 ++++++++--------
 .cleargate/sprint-runs/SPRINT-22/token-ledger.jsonl      | 11 +++++++++++
 11 files changed, 27 insertions(+), 9 deletions(-)

commit 5922c909a250a5bb764ebda1661ebb9f0bfe3b53
Merge: 1b67efc d239120
Author: sandrinio <sandrinio@users.noreply.github.com>
Date:   Mon May 4 13:34:22 2026 +0400

    merge(story/CR-044 phase B): SKILL.md §C.7 Story Merge → DevOps dispatch block
    
    W1 (3/3 phase B) — DevOps role complete.
    
    §C.7 body fully restructured:
    - Old: orchestrator runs git checkout/merge/worktree remove/branch -d in 4-line bash block
    - New: orchestrator dispatches DevOps agent via write_dispatch.sh + Agent tool
      with §3.1 Context Pack reproduced inline; DevOps owns all mechanical actions
    - Required-reports table includes new STORY-NNN-NN-devops.md alongside dev/qa/arch
    - Forbidden-pattern callout: orchestrator MUST NOT run git merge / git worktree
      remove / git branch -d / update_state.mjs / npm run prebuild for standard-lane
      stories post-CR-044 — DevOps does them all
    - STATUS=blocked handling documented
    
    Mirror parity clean post-prebuild.
    
    QA: PASS (4/4 phase-B checks). Architect post-flight: PASS (4/4 phase-B checks).

commit d23912076e144db5330fc8591bac7861b7d76742
Author: sandrinio <sandrinio@users.noreply.github.com>
Date:   Mon May 4 13:34:22 2026 +0400

    chore(SPRINT-22): CR-044 phase B QA + Architect post-flight reports — both PASS

 .../sprint-runs/SPRINT-22/reports/CR-044-arch.md   | 126 +++++++++++++++++++++
 .../sprint-runs/SPRINT-22/reports/CR-044-dev.md    |  36 ++++++
 .../sprint-runs/SPRINT-22/reports/CR-044-qa.md     |  33 ++++++
 3 files changed, 195 insertions(+)

commit 4e4ade1fe6dde34a83e6379cbf7196ea2eebe8df
Author: sandrinio <sandrinio@users.noreply.github.com>
Date:   Mon May 4 13:28:52 2026 +0400

    feat(SPRINT-22): CR-044 phase B — SKILL.md §C.7 Story Merge → DevOps dispatch block
    
    Replace legacy orchestrator-runs-git-merge prose in §C.7 with DevOps
    dispatch contract: write_dispatch.sh invocation, §3.1 Context Pack
    inline, STATUS=done/blocked handlers, forbidden-
...[truncated]
```

## Token Ledger Digest

**Total:** 206,072,246 tokens (input: 18,382 / output: 1,125,285 / cache_read: 197,086,801)

**Per-agent breakdown:**
- architect: 206,072,246 (12 dispatches)

**CR-035 two-line split (pre-computed for Reporter §3):**
sprint_work_tokens: 206,072,246
sprint_total_tokens: 206,072,246
reporter_pass_tokens: null


## Flashcard Slice

_Entries within sprint window [2026-05-04 → 2026-05-15]:_

2026-05-04 · #snapshot #hooks · token-ledger.sh snapshot-lock supersede pattern: cr-NNN.sh becomes new authoritative baseline; hooks-snapshots.test.ts byte-equality assertion flips to new lock; prior cr-N-1 demoted to historical (existence-only check).
2026-05-04 · #token-ledger #devops · token-ledger.sh primary dispatch-marker path (L121-141) already accepts arbitrary agent_type strings — L227 legacy fallback list edit only affects no-sentinel transcript-grep path; not blocking for new agent types.
2026-05-04 · #fixtures #sprint-22 · Sample/example test fixtures live in `cleargate-cli/examples/` NOT `cleargate-cli/test/fixtures/` — avoid the `test/**/*.node.test.ts` glob so `npm test` doesn't auto-run intentionally-failing Red examples.
2026-05-04 · #naming #red-green · Red+node combined naming: `*.red.node.test.ts` (red BEFORE node infix). Wrong: `*.node.red.test.ts`, `*.red.ts` — those won't be picked up by the npm test glob OR won't be marked immutable.
2026-05-04 · #skill-md #renumbering · SKILL.md §C insert + renumber: forward-only handoff (§C.N footer hands off to §C.N+1) is idiomatic; no need for backward "see §C.N-1" pointers. Update cross-refs by literal string match (line numbers shift).
2026-05-04 · #pre-commit #stub-extension · `.claude/hooks/pre-commit-surface-gate.sh` is an 11-line stub that delegates to file_surface_diff.sh — extensions (Red-immutability check, etc.) go IN the stub BEFORE the exec line, not in the delegated script.
2026-05-04 · #node-test #child-process · NODE_TEST_CONTEXT=child-v8 causes nested tsx --test invocations to skip silently (exit 0); delete env var in child process env (`delete env.NODE_TEST_CONTEXT`) before spawning child tsx test processes to get real pass/fail.
2026-05-04 · #cost-framing #pricing · cache_read at $0.30/MTok vs cache_creation at $3.75/MTok (Sonnet 4.6) — saving cache_read tokens by forcing re-ramp can NET NEGATIVE in dollars. Always compute both directions before recommending fresh-session/cache-bust optimizations.
2026-05-04 · #spike #recommendation · CR-039 spike: session_id is shared across orchestrator+subagents (no SDK override). Token-count savings (~16M/sprint) is real but dollar-net is ~-$1.58/sprint at current pricing. PARTIAL/NO-GO; CR-041+CR-042 deferred to SPRINT-22.
2026-05-04 · #docs #agent-defs · reporter.md L108 claim "Task tool creates new conversation per dispatch" is INACCURATE per ledger evidence (1 session_id per sprint). CR-042 fixes this in SPRINT-22.
2026-05-04 · #session-reset #agent-tool · Claude Code Agent/Task tool shares orchestrator session_id — no per-dispatch session isolation; SubagentStop won't fire for `claude -p` subprocess dispatches.
2026-05-04 · #qa #worktree #mirror · test_close_pipeline reporter.md mirror check fails in worktrees — live `.claude/agents/` is gitignored at `/.claude/`; suppress or skip this check in worktree context.
2026-05-04 · #close-pipeline #step-3.5 · close_sprint.mjs Step 3.5 is v2-fatal post-CR-036 — bundle ≥2KB or close exits 1; v1 advisory preserved. Use CLEARGATE_SKIP_BUNDLE_CHECK=1 in tests.
2026-05-04 · #reporter #budget · Reporter token budget: 200k soft warn / 500k hard advisory + auto-flashcard. token-ledger.sh hook emits via stdout (CR-032 chat-injection).
2026-05-04 · #reporter #fresh-session · Reporter dispatched in fresh session via write_dispatch.sh shell child — Agent tool path requires no --resume flag. Verified post-CR-036.
2026-05-04 · #mirror #parity · Architect plan said close_sprint.mjs is live-only; canonical mirror EXISTS at cleargate-planning/.cleargate/scripts/close_sprint.mjs — both updated for parity.
2026-05-04 · #test-harness #fixtures · sprint-v1-legacy fixture had execution_mode:v2 + schema_version:1 — inconsistent; fixed to v1. v1-era sprints should have execution_mode:v1.
2026-05-04 · #qa #test-count · Dev's "pre-existing failure count" can be FILE count not TEST count — QA must distinguish; spot-check by running one to confirm the actual scope.
2026-05-04 · #qa #gates #regression · AND-semantics in an enforcing gate means any new required criterion breaks ALL existing items of that type — test FULL gate runs against real parent files on disk, not isolated predicates.
2026-05-04 · #gate #or-group · gate.ts or_group?: optional field on GateCriterion — criteria sharing same or_group value pass-as-group when ≥1 member passes; backward-compat: criteria without or_group still required-AND.
2026-05-04 · #stamp-tokens #fm-key-map · stamp-tokens.ts L194 idKeys array + work-item-type.ts L14 FM_KEY_MAP are DUAL sources of truth for work-item key mapping — must update both when adding work-item types.
2026-05-04 · #predicates #existing-surfaces · existing-surfaces-verified permissive regex requires file extension — extension-less paths (`etc/passwd`) are silently dropped via sentinel-missing branch, not sandbox-rejection. Test with `.conf`/`.md`/etc.
2026-05-04 · #predicates #existing-surfaces · existing-surfaces-verified pass/fail: section absent → pass (not-applicable); zero paths + no sentinel → fail; zero paths + sentinel → pass; missing paths → fail with list.
2026-05-04 · #reporting #session-totals · .session-totals.json is UUID-keyed map not flat — sum Object.values; spec quoted flat shape but live shape is `Record<sessionUuid, {input, output, ...}>`.
2026-05-04 · #mirror #parity · cleargate-planning/.cleargate/scripts/ does NOT mirror prep_reporter_context.mjs — live-only by design; do NOT create the canonical mirror.
2026-05-04 · #mirror #dogfood-split · Live `.claude/agents/` is gitignored — canonical edits to agent prompts require `cleargate init` re-sync post-merge; QA cannot verify live parity via tracked-file diff.

## REPORT Template

Template path: `/Users/ssuladze/Documents/Dev/ClearGate/.cleargate/templates/sprint_report.md`
